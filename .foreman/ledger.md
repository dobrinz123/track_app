# Foreman Ledger — Transilvania Motor Ring track-session app

BASELINE: (empty repo, git initialized 2026-08-06, branch main, no commits yet) | untracked: .claude/, docs/, .foreman/ | 2026-08-06

MODE: Codex-boosted Full (Agent tool ✓, real shell ✓, Codex CLI 0.145.0 ✓ ChatGPT-subscription login — consent: user's mission spec explicitly requests Codex workers; echo check passed, default seat)

SEATS: LEAD = Fable 5 (frontier). Claude: sonnet (WORKHORSE), haiku (FAST), opus/fable (FRONTIER). Codex: account-default = gpt-5.6-sol (observed in exec headers), ChatGPT login; treated as FRONTIER-class for algorithmic work; per-dispatch effort via `-c model_reasoning_effort`.

## Plan
See docs/implementation-plan.md (WP0–WP16).

## Routing
- WP1 scaffold → Sonnet: well-specified config-heavy work
- WP2, WP3 research → Haiku scouts (read-only web research, bounded)
- WP4–WP7, WP9, WP10 → Codex (algorithmic/geometry/timing/fixtures)
- WP8, WP11–WP14 → Sonnet (implementation with clear contracts)
- WP15 → Codex read-only reviewer; WP16 → foreman-verifier (Claude) + Codex read-only (cross-family)

## Tasks
| id | state | owned paths | job id |
|----|-------|-------------|--------|
| WP0 | DONE (LEAD) | docs/**, .foreman/ledger.md | — |
| WP1 | REPORTED(DONE) + LEAD deterministic checks passed (typecheck/test/lint green, commit 79e974b); blind verification batched into core-group gate | root configs, packages/core scaffold, apps/mobile | wave1-scaffold |
| WP5 | REPORTED(DONE) + LEAD gates green (287/287, commit dabbdc6); blind verification batched into core-group gate | packages/core/src/geometry/**, test/geometry/** | br3xpj3dd |
| WP6 | DISPATCHED (Codex, effort=high, background, log .foreman/scratch/wp6-codex-output.log) | packages/core/src/timing/** | wave2-timing |
| WP8 | REPORTED(DONE_WITH_CONCERNS) — concerns triaged: test placement accepted (vitest convention); geometry failures belong to in-flight WP5, judged at WP5 collection | packages/core/src/statemachine/**, test/statemachine/** | wave2-sm |
| WP11a | REPORTED(DONE) + LEAD verified slice (35/35, lint ✓, commit b6241b6); group gate pending | packages/core/src/persistence/**, test/persistence/** | wave2-persist |
| WP12 | REPORTED(DONE) + LEAD verified (mobile tsc ✓ eslint ✓ expo export 1.5MB hbc ✓, commit 73d0d9c) | apps/mobile/src/platform/**, app.json | wave2-platform |
| WP4 | REPORTED(DONE_WITH_CONCERNS→resolved: tests relocated by LEAD) + gates green 366/366, commit c8de532 | packages/core/src/profile/**, test/profile/** | wave3-schema |
| WP4b | REPORTED(DONE_WITH_CONCERNS→resolved: test relocated) + gates 371/371, asset provenance verified by LEAD, commit 8eb9046 | packages/core/scripts/**, assets/** | wave3-tmr |
| WP7 | DISPATCHED (Codex effort=high, background, log wp7-codex-output.log) | packages/core/src/matching/**, src/calibration/**, test/{matching,calibration}/** | bl0hsl1ru |
| WP11b | REPORTED(DONE) + LEAD verified slice (64/64), commit d813aeb | persistence-sql + contract suite | wave4-sqlite |
| WP12b | REPORTED(DONE_WITH_CONCERNS→resolved by ADR-0003 amendment: SDK gap accepted + WP14 watchdog mandate) commit d96d59b | apps/mobile platform + app.json | wave4-ios |
| WP-docs | REPORTED(DONE, amendment applied) + LEAD spot-checks, commit 22515da | README, ios-no-mac-workflow, validation checklist | wave4-docs |
| WP7 | REPORTED(DONE) + LEAD gates 423/423, commit 94b4d86; in core-group blind verification | matching/, calibration/ | bl0hsl1ru |
| WP9 | REPORTED(DONE_WITH_CONCERNS→sandbox-only concern; LEAD gates 458/458) commit cf508e2 | reference/ | bxebf85az |
| WP10 | REPORTED(DONE) + LEAD gates 474/474 + production-composition spot-check, commit b1e0dd8 | fixtures/, replay/ | brahtmjo2 |
| WP14 | REPORTED(DONE_WITH_CONCERNS→deviations reviewed & accepted; limitations queued to known-limitations doc) + LEAD gates 484/484 + offline audit re-run + composition bootstrap inspection, commit 4f39338 | controller/, index.ts, session/, eas.json | wave6-integration |
| WP-perf | DISPATCHED (Codex effort=high, background, log wp-perf-codex-output.log) | track-matcher.ts, test/perf/**, docs/verification/performance.md | bc35jr2w5 |
| WP-sec | ACCEPTED (DONE): 0 CRIT/HIGH, 3 MED (M1 profile-array DoS, M2 unbounded session telemetry, M3 no deletion UI), 3 LOW, 6 INFO clean | .foreman/scratch/security-review-findings.md | wave7-sec |
| WP-secfix | REPORTED(DONE) + LEAD gates 502/502, commit 699b663 — all 6 findings fixed w/ tests | see ticket | wave7-fix |
| WP-perf | REPORTED(DONE) + LEAD verified slice (3.67x measured, 44 relevant tests), commit fadf080 | matcher + perf tests + perf doc | bc35jr2w5 |
| WP-docs2 | REPORTED(DONE) + LEAD spot-checks, commit e368945; surfaced 5 real integration oddities → WP-oddfix | docs set | wave7-docs |
| WP-oddfix | REPORTED(DONE) + LEAD gates 513/513 + export:ios, commit 12a6378 — corridor wiring, reason copy, diagnostics UI, background checkpoint, recovery lap numbering | see ticket | wave8-odd |
| FINAL-VERIFY | COMPLETE. Claude blind verifier: PASS_WITH_NOTES (18/18 DoD rows, 1 LOW → fixed bf9397a). Codex cross-review: nominal FAIL from sandbox write-denials (no app defect; gates pass in real workspace, dual-reproduced) + 1 MEDIUM (sectorIndex assertions → fixed c3ca773) + procedural note (HEAD moved mid-verify — logged). PROJECT VERIFIED at c3ca773: 515/515 tests, all gates green. | — | done |

FINAL STATE v1: delivered (see docs/verification/final-report.md). — Post-delivery phase 2 opened 2026-08-06: user directives = premium UI redesign (kill generic blue/system font/centered text; dark stays), multi-circuit-ready selection UI, app naming (Haiku research → user picks), legal text relocated to About/CircuitDetail (ODbL compliance kept — LEAD decision, deletion refused as license-required). Bundle id FROZEN at app.circuittimer.tmr regardless of display-name choice (resign data preservation).

| id | state | owned paths | job id |
|----|-------|-------------|--------|
| WP-name | ACCEPTED (DONE, 24 candidates, 6 verified finalists) → USER DECISION: app name = TRACE. Display name applied in app.json (CFBundleDisplayName); APP_NAME constant flips at UI-worker collection. Bundle id UNCHANGED (app.circuittimer.tmr). | scratch/naming-research.md | wave9-name |
| WP-catalog | DISPATCHED (Codex effort=medium) | packages/core/src/catalog/**, test/catalog/**, index.ts (1 line) | bmbgre6tu |
| WP-ui2 | DISPATCHED (Sonnet) | apps/mobile/src/ui/**, session/circuitCatalog.ts, App.tsx, fonts deps | wave9-ui |
| WP-docs2 | DISPATCHED (Sonnet) | docs/algorithms/**, docs/*.md, README index | wave7-docs |
| VERIFY-CORE | VERDICT: PASS_WITH_NOTES — all 8 tickets OK, 7/7 adversarial probes passed, purity guard fires. Finding 1 (MEDIUM, uncommitted generate:tmr script) fixed by LEAD commit. Notes 3 (matcher hint gives no compute savings — full search every call; spec requires local-first search) → queued for WP-perf pass. Note 4 accepted (stricter debounce consistent with contracts). Core group = VERIFIED. | — | wave5-verify |
| WP13a | REPORTED(DONE) + LEAD verified (tsc/lint/export 890-module 2MB hbc, commit b4c83bf) | apps/mobile/src/ui/**, src/session/**, App.tsx | wave3-ui |

NOTE: WP11b (SQLite adapter) intentionally held until WP4b collects — package-lock.json write overlap (concurrent npm installs). Facade integration notes: add speedKph to FacadeState at WP14; SessionHistoryStore interface lives outside SessionFacade (accepted design call).
| WP2 | ACCEPTED (DONE_WITH_CONCERNS; concerns dispositioned — see Decisions) | docs/research/**, data/osm/** | wave1-circuit |
| WP3 | ACCEPTED (DONE; scout report consumed) | .foreman/scratch/platform-research.md | wave1-platform |
| WP4–WP16 | PENDING | see plan | — |

## Attempts
(append-only)
- WP0 | 1 | LEAD | — | DONE | n/a | docs written | 2026-08-06
- WP3 | 1 | Haiku scout | ticket inline | DONE→ACCEPTED | n/a (read-only) | .foreman/scratch/platform-research.md (Expo SDK 57, 1Hz GNSS, perf.now monotonic) | 2026-08-06
- WP2 | 1 | Haiku scout | ticket inline | DONE_WITH_CONCERNS→ACCEPTED (concerns dispositioned in Decisions) | LEAD Overpass verification confirmed OSM ways | docs/research/transilvania-motor-ring.md, data/osm/ | 2026-08-06
- WP1 | 1 | Sonnet | ticket inline | DONE; LEAD re-ran typecheck+test+lint = green | typecheck ✓ test 2/2 ✓ lint ✓ expo export ✓ (worker) re-verified by LEAD | commit 79e974b | 2026-08-06
- WP5 | 1 | Codex effort=high | ticket-wp5-geometry.md | DISPATCHED | — | job br3xpj3dd → wp5-codex-output.log | 2026-08-06
- WP8 | 1 | Sonnet | ticket-wp8-statemachine.md | DONE_WITH_CONCERNS; 257 sm tests pass; concerns triaged by LEAD | worker: typecheck ✓ lint ✓ sm-suite 257/257 ✓ | pending group verification | 2026-08-06
- WP11a | 1 | Sonnet | ticket-wp11a-persistence-core.md | DISPATCHED | — | — | 2026-08-06

## Decisions
- 2026-08-06 USER CLARIFICATION: Expo Go = dev-only; on-track = standalone EAS-built offline iOS app installed from Windows (TestFlight paid path OR AltStore/Sideloadly free path w/ 7-day expiry). ADR-0004 amended; docs worker re-scoped mid-flight via SendMessage. WP14 MUST verify: TMR profile statically imported (inlined in Hermes bundle, never fetched) + zero network calls in session paths + eas.json with development/preview/production profiles.
- 2026-08-06 USER PRODUCT DECISION: iOS is primary target (ADR-0003). WP12b dispatched (iOS hardening); docs/validation checklist to be iOS-first in WP14/15; ios export added to verification gates. Architecture stays cross-platform.
- WP2 concerns dispositioned: (a) no public pit/SF/sector data → gates are app-defined + versioned per ADR-0002, never labeled official; (b) turn-count conflict (17 vs 10) → not modeled in MVP, documented; (c) OSM IDs missing → resolved by LEAD Overpass query: way 488429454 (circuit, closed, ~3706 m ≈ verified 3.708 km) + way 488429716 (pit lane); raw JSON archived in data/osm/, ODbL attribution mandated (ADR-0002). New task WP4b: deterministic TMR profile generator from OSM data.
- Stack: Expo/RN + TS monorepo (ADR-0001) — local verifiability + worker proficiency.
- Codex consent: granted in mission spec ("Codex agents — algorithmic and verification work"); billing = ChatGPT subscription.
- Contracts authored by LEAD in docs/architecture/contracts.md; workers materialize verbatim.
- Verifier cadence: blind verification per integration group (core group, app group, final), not per micro-ticket — logged as a deliberate batching decision.

## Scratch
.foreman/scratch/

## Pre-install gate (2026-08-09)
- IPA forensics (LEAD): PASS — bundle id/plist/v2-gates-binary/fonts/icon/unsigned all verified in the actual artifact.
- Blind verifier: FAIL (discipline) — binary behavior clean, but 2/4 controller fixes unpinned (B1 PB-capture, B2 flush-propagation) + DevReplay in release bundle (B4).
- Codex cross-review: FAIL (lifecycle) — C1 one-shot controller (CRITICAL), C2 bootstrap gate, C3 GNSS races, C4 endSession no-flush, C5 recovery pointer, C6 replay swap leaks, C7 silent command errors, C8 unawaited checkpoint, C9 keep-awake gap on Learn, C10 recovery lock+copy, C11 preflight collector linger.
- DECISION: DO NOT INSTALL current artifact; single batched fix wave WP-lifecycle (Sonnet) with pre-decided designs; install slots preserved. Re-verify + fresh build after.
- WP-lifecycle: DONE, all 14 findings fixed with sensitivity-proven tests (590 total green); LEAD re-verified gates + bootstrap-gate UI live check; commit 361b708; build 31283955432 GREEN (11m6s); final ipa forensics PASS (bundle id, v2 binary, all fix markers, no bg modes); TRACE-release-FINAL-2026-08-09.ipa delivered to user with install green-light. Install slots consumed by this campaign: ZERO.

## Phase 3 — Coaching (opened 2026-08-10, user directive)
Goal: advisory on-track guidance — corner severity + braking zones + live cues. Contracts authored by LEAD (contracts.md Coaching addendum, commit pending push). Plan: WPC1 corners analysis (Codex, DISPATCHED bh9srayjn) → WPC2 braking zones + CoachEngine (Codex, ticket staged) → WPC3 controller/facade/settings wiring (Sonnet) → WPC4 dashboard coach strip + corners list + optional expo-speech audio (Sonnet) → E2E preview + Codex cross-review → build. Validation anchor: TMR corner count must land 8-12 vs reference map's T1-T10.
- Phase 3 COMPLETE (2026-08-10): WPC1 corners (Codex) → WPC1b speed model v2 + observed calibration (Codex, LEAD bucket 1.65g) → WPC2 zones+CoachEngine (Codex) → WPC3 wiring (Sonnet) → WPC4 UI+voice (Sonnet) → LEAD live E2E (cues verified in preview) → Codex cross-review FAIL (5H/6M) → WPC5 fix wave (Sonnet, incl. direction-split 9→12 corners real bug; T1/T2 obs unlocked) → Codex re-verify (10/11 FIXED + 2 new) → LEAD takeover residues (immediate invalidation, background voice gating) → 676 tests green → build 31344276647 GREEN → ipa forensics PASS (all coach markers, observed asset, DevReplay excluded). Delivered TRACE-COACH-release-2026-08-10.ipa.
- Voice pack: user-selected ElevenLabs voice id SLniEZuwscN3JIxjTknk (voice-library link provided 2026-08-10); GT-minimal vocabulary amendment sent to WPC6 (3 clips: "Brake hard."/"Brake."/"Lift.", CORNER_AHEAD silent). Awaiting: WPC6 delivery, then ELEVENLABS_API_KEY (env-only) from user to generate.
- Voice pack CLOSED (2026-08-10): user generated 3 clips locally (22 chars ElevenLabs credit, key env-only, never in repo/chat). Populated clip map broke vitest (vite-node parses mp3 require as JS; 24 mobile tests red). Three config-level fixes failed (vite plugin stub, absolute alias, regex alias — reverted). LEAD fix at source: safeRequire(() => require(...)) in generated file + emitter patched to match (conditional block, eslint-disable per entry); vitest.config.ts restored to simple form; stub deleted. Gates all real-exit-code green: typecheck 0, tests 694 (580 core + 114 mobile), lint 0, expo export 0 with all 3 mp3s content-hash-verified in dist/assets. Commit 064668a pushed; build 31383570618 dispatched (variant=both). Pending: watch → ipa forensics (mp3s bundled, clip map markers) → deliver final voice ipa.
- Voice build DELIVERED (2026-08-10): run 31383570618 GREEN (15m, variant=both, SHA 064668a). Release ipa forensics PASS: all 3 mp3s byte-identical in Payload/TRACE.app/assets/assets/voice/, bytecode markers brake-hard/Brake hard./Lift./safeRequire present (prev release had brake-hard=0 -> clip map populated delta confirmed), DevReplay residual strings = 3 = accepted baseline from previous PASS, bundle id app.circuittimer.tmr intact. Delivered builds/ipa/TRACE-VOICE-release-2026-08-10.ipa (13.7 MB). Install slots consumed by voice campaign: ZERO.
- LEAD live E2E post-voice-build (2026-08-10, web preview): full production flow driven via DevReplay "Three timed laps" (looping). Calibration accepted 92% -> dashboard live (GNSS GOOD, sectors, delta vs reference, PB 1:31.178) -> 16 laps -> hold-to-end -> Results (seam laps L1/L4/L7/... correctly INVALID pause-gap/reverse-travel, PB lap green) -> History persisted (1 stored session, per-lap validity chips). VOICE VERIFIED WITH INSTRUMENTATION: 188 real clip playbacks captured (brake.mp3 x120, lift.mp3 x68, tts fallback x0, brake-hard x0 = correct, TMR has no sev-6). CoachStrip live cues confirmed on screen (BRAKE IN -> C9 . 5 . 99 km/h . 9 m, distance countdown, correct corner order). Watchdog restarts: 6 (loop-seam self-recovery). Startup console errors seen initially were STALE HMR bundle history (fresh tab clean); one benign web-only noise: expo-keep-awake's own useKeepAwake unmount rejection (ERR_KEEP_AWAKE_TAG_INVALID x2, library-internal, no native path on iOS). No app-code errors. Verdict: voice ipa remains GREEN for install.
- Graphify refresh (2026-08-10, foreman): graph was stale (built 01:02, missed WPC3-6). Worker ran the incremental runbook: 188 changed code files AST-extracted (1739 nodes/5019 edges merged), then LEAD verified and closed the worker's non-code CONCERN with a semantic pass — cache resolved 78/89 changed docs/images, 1 extraction agent covered the 11 true misses (Phase-3 tickets, ledger, contracts.md coaching addendum, voice-pack.md; 48 nodes/79 edges/3 hyperedges). Final graph: 2025 nodes, 4713 edges, 155 communities, health OK (0 dangling/missing/collapsed), detect_incremental reports 0 pending. New communities labeled (Voice Coach & Audio Clips, Coach Engine, Coach UI Components, Voice Pack Generator Script, ...). graph.html regenerated (2.19 MB).

## Phase 4 — OBD Telemetry (opened 2026-08-10, user directive "PORNESTE FAZA 4 CU FOREMAN INCEPE CU P4a")
Baseline: 897a7b7, tree clean. LEAD: Fable (frontier). Codex: ChatGPT login, verified seat gpt-5.6-sol. Consent: standing (mission spec).
Goal P4a: hardware-free spike — app-side ELM327-WiFi transport + parsing + recording, validated against a deterministic simulated adapter. Roadmap context in memory (phase4-obd-telemetry): P4a spike -> P4b UI -> P4c ESP32 CAN device -> P4d coaching fusion.
Plan: LEAD authors Telemetry contracts addendum -> WPT1 ELM327 core engine + simulator (Codex, workspace-write) -> cross-verify (Claude) -> WPT2 mobile wiring: TCP transport, TelemetryProvider, SQLite recording w/ retention cap, settings, dev screen (Sonnet) -> cross-verify (Codex read-only) -> gates -> E2E preview.
Constraints carried forward: 100% offline at runtime (local socket only), READ-ONLY OBD (mode 01 PIDs only, never clear-codes/actuation), advisory-only labeling, monotonic timestamps, retention caps (security finding M2 lineage), no Expo Go (native TCP needs dev-client/standalone).
| WP | seat | status |
| WPT0 contracts | LEAD | PENDING |
| WPT1 elm327 core | codex gpt-5.6-sol | PENDING |
| WPT2 mobile wiring | sonnet | PENDING |
- WPT0 contracts: DONE (LEAD) — Telemetry addendum appended to contracts.md (channels/transport/session/simulator/recording semantics, retention cap 200k, read-only mode-01 rule).
- WPT1 DISPATCHED: codex gpt-5.6-sol high, workspace-write, bg job b5bkvgho6, ticket .foreman/scratch/ticket-wpt1-elm327.md, out /tmp/wpt1-out.log.
- WPT1 COLLECTED: Codex DONE_WITH_CONCERNS (sandbox could not spawn vitest/tsc; in-process claims). LEAD ran authoritative gates: typecheck 0, tests 720 green (26 new), lint 0. Checkpoint commit 4fceb7b.
- WPT1 VERIFIED: blind foreman-verifier PASS_WITH_NOTES — formulas re-derived w/ independent vectors, 2 mutations caught by tests, tree clean/HEAD unchanged. Notes: sim-transport knobs untested, UNABLE TO CONNECT untested, no session-level split E2E -> batched into WPT-cov ticket.
- WPT2 DISPATCHED (sonnet, bg): mobile wiring per ticket-wpt2-mobile.md. WPT-cov DISPATCHED (sonnet, bg): ticket-wptcov-simtests.md. Parallel OK: disjoint write sets (apps/mobile+lockfile vs packages/core/test/telemetry/simulatedTransport.test.ts).
- WPT-cov COLLECTED: sonnet DONE — 1 new test file, 6 tests (32/32 telemetry green, exit 0 claimed; LEAD will re-gate with WPT2). Write set respected.
- WPT2 CROSS-VERIFY (codex read-only, commit 6e83281): FAIL — 2 HIGH (H1 telemetry flush opens nested SQLite transaction vs controller lap/PB transaction -> "cannot start a transaction within a transaction" can break endSession; H2 end-flush fire-and-forget -> last batch lossy + telemetry left running if controller persistence rejects) + 8 MED (provider stop/start generation race, lap0 not NULL, cap-exact flag, recovery bypasses telemetry start, delete-all leaves telemetry_samples, simulate honored in release, close-during-import race, monitor stale state) + 1 LOW (port field). Clean: retry cleanup, timeout double-settle, SQL binding, migration idempotence, settings hydration, offline/read-only mandates, plist.
- WPT3 fix wave DISPATCHED (sonnet, bg): ticket-wpt3-fixwave.md with 11 LEAD-authored binding designs (F1 no recorder transactions + tail-chain serialization; F2 awaited allSettled end barrier + finally; F3 generation tokens; F4 lap0->NULL; F5 cap>=; F6 recovery hook; F7 mobile delete-all incl telemetry_samples; F8 __DEV__ gate via injected isDev; F9 closed-check post-import; F10 subscribe replay; F11 port draft validation).
- WPT3 re-verify (codex): FAIL — F3-F6,F8-F11 FIXED; F1/F2 PARTIALLY (worker serialization self-deadlocked all repo transactions = NEW HIGH N1; end-barrier awaited wrong promise); F7 blocked by N1. Second failed wave on same area -> LEAD TAKEOVER.
- LEAD takeover (commits 4192a7c + this): sqlWriteGate.ts — gate held only around whole transactions (gateSqlTransactions) + telemetry batch INSERTs (recorder constructor gate param); inner transaction statements ungated (reentrancy-safe, kills N1); stopTelemetryRecording caches+returns shared in-flight shutdown promise (real final flush awaited by onSessionEnded barrier); N1 pin test (deadlocks under old design) + gate ordering test. Codex confirm pass: N1 CONFIRM fixed, F2 CONFIRM fixed, no new defects except delete-all straggler-flush race -> fixed inline (await shutdown, DELETE+verify under gate). Accepted residual (documented): controller emits sessionComplete before telemetry flush settles (needs packages/core realFacade barrier — deferred to P4b; loss window = ms-scale app-kill from Results).
- P4a CLOSED: contracts + ELM327 core engine (Codex) + sim transport w/ fault injection + mobile wiring (Sonnet) + 2 fix rounds. Final: 762 tests green (612 core + 150 mobile), typecheck/lint/export 0. Telemetry is settings-gated OFF by default; zero impact on existing flows when disabled. Next: P4b (gauges UI + lap overlay), P4c (ESP32 CAN), P4d (coaching fusion).

## Phase 4b — Telemetry UI (opened 2026-08-10, user directive)
Baseline: 5c2dd82 + contracts amendment commit. Plan: WPB-A (sonnet): sessionComplete barrier (facade relay hold, 2s cap) + dashboard TelemetryStrip. WPB-B (sonnet): readLapTelemetry + LapDetail bucket charts (View-based, NO new deps — decision: no react-native-svg, bar-sparklines from Views). Parallel, disjoint write sets. Then one Codex cross-review over both, batched fixes if needed, E2E preview w/ simulate.
- P4b contracts amendment committed. WPB-A DISPATCHED (sonnet, bg): barrier + TelemetryStrip, ticket-wpb-a-session.md. WPB-B ticket staged (ticket-wpb-b-lapdetail.md) — SERIALIZED after A (composition.ts overlap; parallel-disjoint rule).
- WPB-A COLLECTED: sonnet DONE — barrier in SwappableFacade (relaySessionCompleteBarrier pure fn + 2s cap), TelemetryStrip w/ fixed slot, pure viewmodel helpers; worker also fixed a real pre-existing bug (stale telemetryShutdown leaking into a later disabled session's barrier). +20 tests (782 claimed green). LEAD will re-gate together with WPB-B before commit.
- WPB-B DISPATCHED (sonnet, bg) after A collected (serialization honored).
- WPB-A+B COLLECTED, gates green (791 tests), commit 50f6125. Codex cross-review: FAIL — 1 HIGH (strip adds normal-flow row: 360x640@1.3 font scale pushes Hold-to-End off-screen = on-track control failure) + 3 MED (card rendered when not polling; stale telemetryShutdown feeds replay barrier; LapDetail read rejection unhandled) + 1 LOW (bucketCount unvalidated). Clean: barrier ordering/cap/once, facade swaps, lap-number scheme agreement, SQL, bucket edges, strip unsubscribes.
- WPB fix wave DISPATCHED (sonnet, bg): ticket-wpb-fixwave.md — LEAD binding designs: H1 strip becomes zero-height overlay pinned in delta zone (removes reserved slot; supersedes contracts "fixed slot" bullet), M1 render-nothing-unless-polling, M2 clear telemetryShutdown at facade swap-ins, M3 rejection -> empty rows + warn, L1 positive-integer guard.
- WPB fix wave COLLECTED (sonnet DONE): H1 strip -> zero-height overlay in delta zone (360x640@1.3 budget back to 600dp pre-P4b baseline), M1 render-null-unless-polling, M2 telemetryShutdown cleared at all 3 facade swap-ins (pinned), M3 loadLapTelemetry rejection->[]+warn (pinned x5), L1 bucketCount TypeError guard (pinned x5). LEAD gates green: 802 tests, typecheck/lint/export 0. Commit 8dbcf16.
- LEAD live E2E (web preview, 360x640 mobile viewport): sim telemetry pipeline verified in UI (Telemetry monitor CONNECTED, RPM@10.1Hz/speed@5Hz/throttle@5Hz/coolant@0.7Hz = poll-plan ratios); dashboard WORST CASE verified visually — coach cue (BRAKE IN C5) + telemetry strip (RPM/THR/COOLANT overlay) + live delta + timer/sectors/PB row + Hold-to-End ALL visible at 360x640, no occlusion of delta digits, no overflow (H1 confirmed fixed in the exact reviewer scenario); sessionComplete barrier functional (Results reached, NEW PERSONAL BEST); console clean (only known benign web keep-awake library noise). LapDetail charts not visually exercisable on web (db=null in-memory -> no recorded rows; hidden-section behavior is the correct rendering there) — chart visuals remain device-verified-by-tests+arithmetic only.
- P4b CLOSED: contracts amendments + barrier + TelemetryStrip + readLapTelemetry/bucketTelemetry + LapDetail sparklines; 1 Codex cross-review (1H/3M/1L) + 1 batched fix wave; 802 tests green. Telemetry UI ships dark until telemetryEnabled.

## P4b+ channel revision (2026-08-11, user directive: oil temps > coolant on strip, rpm record-only, G-forces recorded/analyzed not displayed)
Baseline a0d4c4a + contracts amendment. Plan: WPT-core2 (codex): engineOilC PID 0x5C + custom-PID support + poll plan revision. WPT-mobile2 (sonnet, after core): GForceProvider on existing motionCapture/expo-sensors, strip revision w/ thresholds, trans-PID setting, chart order, TelemetryScreen rows. Cross-verify: foreman-verifier on core, codex on mobile. Sequential.
- WPT-core2 COLLECTED: codex DONE_WITH_CONCERNS (sandbox spawn denial, in-process claims). LEAD gates authoritative: 811 green (621 core + 190 mobile), typecheck/lint 0. Commit 056e0d9. Custom-PID: verbatim hex request, last-byte-minus-40, unconfigured transOilC filtered + one warning; no poll defaults hardcoded in core (confirmed).
- WPT-mobile2 DISPATCHED (sonnet, bg): ticket-wpt-mobile2.md — poll plan revision, transOilPidHex setting, GForceProvider on motionCapture, strip slots THR|ENG OIL|TRANS OIL (coolant fallback), TelemetryScreen rows, chart order. Verification plan after collection: foreman-verifier (core scope) + codex read-only (mobile scope), parallel on committed state.

## P4c-hw — OBD dongle hardware (opened 2026-08-11 overnight, user directive: schematic + production PCB + 3D-printable enclosure, autonomous, token-efficient)
Env installs (user pre-authorized): KiCad 10.0.5 per-user via winget OK (kicad-cli at %LOCALAPPDATA%\Programs\KiCad\10.0\bin); OpenSCAD MSI UAC-canceled -> official portable zip at %LOCALAPPDATA%\Programs\OpenSCAD-portable (2021.01, verified). Python via uv (graphify interpreter).
LEAD design doc: hardware/DESIGN.md (binding) — ESP32-C3-MINI-1 + TJA1051T/3 (hw listen-only via S pin), TPS54202 buck w/ 8V UVLO, SMBJ24A/PTC/SS34 protection, PESD2CAN on bus pins, JLCPCB-assemblable BOM, 55x25mm 2-layer, CAN term DNP.
Plan: HW1 (sonnet) schemdraw SVG schematic; HW3 (sonnet) OpenSCAD enclosure + STL — parallel, disjoint dirs; then HW2 (sonnet, DRC-loop) pcbnew-scripted board -> kicad-cli DRC 0 errors -> JLCPCB gerbers/BOM/CPL. Verification: LEAD visual+netlist review of SVG/renders, DRC as deterministic gate, Codex read-only design review of the full hardware package at the end. Note honest limitation recorded in DESIGN.md §6 (no simulation/bench validation yet).
Concurrent: WPT-mobile2 (channel revision) still in flight — disjoint write sets (apps/mobile vs hardware/), parallel OK.
- HW4 firmware ADDED (user directive): ESP32-C3 firmware via foreman — PlatformIO (headless toolchain, auto-installs on first build = the real compile gate), TWAI/CAN driver, WiFi SoftAP + TCP :35000, minimal ELM327-compatible subset (existing app works unmodified: AT handshake + mode-01 PIDs incl. 0x5C + custom-PID passthrough); host-side unit tests (pio test -e native) for the ELM parser/PID codec; LEAD design note: "read-only" mandate = standard 0x7DF mode-01 queries allowed, NO writes/clears/actuation; hardware S-pin listen-only reserved for future passive sniff mode. Dispatch AFTER a current worker slot frees (3 in flight); Sonnet seat (needs real shell for pio gates), Codex read-only review after.
- HW3 COLLECTED: sonnet DONE — case.scad parametric (4x M2 standoffs verified in source after preview ambiguity; worker self-caught 2 geometry bugs via visual check), STLs manifold Simple:yes, previews rendered. LEAD visual review PASS. Committed. Caveats logged: 38mm bridge over plug aperture, snap fit needs first-article test.
- Core2 blind-verify (foreman-verifier): PASS_WITH_NOTES — all 6 criteria pass with verifier's own vectors + mutation reasoning; 2 minor notes for the batched fix list: (a) customPids type accepts accelerometer channels (mobile misconfig could poll latG over OBD — no current production path, guard cheap), (b) decodeCustomResponse misaligns on odd-length hex tail instead of erroring. Held for one batched fix wave together with mobile2 codex review findings (pending).
- HW1 COLLECTED: sonnet DONE — SVG schematic committed, 44-connection cross-check, deterministic; J2 ambiguity resolved per BOM (6-pin incl IO9/EN, sane for auto-flash). LEAD visual: readable, diagonal cross-wires cosmetic-only (schemdraw). HW2 PCB DISPATCHED next (sonnet, DRC hard gate).
- Mobile2 codex review: FAIL — 1 HIGH (custom-PID field accepted destructive services: '04' = clear DTCs polled at 0.5Hz = read-only mandate violation) + 2 MED (odd-length hex sync-throw wedges provider + kills G startup; mode-01 collision mislabels engine oil as trans oil) + 2 LOW (trim-vs-verbatim wording; chart test copies literals). Clean: G filter math + g-units doc-verified, clock sharing, generation guards, independence (async paths), thresholds, cap accounting, signed-G chart normalization.
- Fix wave DISPATCHED (sonnet, bg): ticket-fix-channelrev.md — LEAD designs: 3-layer service whitelist 21/22 (shared validator module, UI+provider+core), sync-throw isolation per provider, odd-length as channel error, chart constant moved to pure module, trim wording; + core verifier notes (a) latG/longG customPids guard, (b) odd-tail decode error. Covers all 7 findings, one wave.
- HW4 COLLECTED (worker LOST-in-wait-loop x2, reconciled by LEAD): firmware artifacts complete on disk; LEAD ran gates authoritatively — pio run -e esp32c3 SUCCESS exit 0 (firmware.bin+bootloader+partitions built), pio test -e native exit 0 (36/36 incl. read-only-guard suite). Host gcc absent -> WinLibs MinGW installed via winget (user pre-auth). Committed. Codex read-only firmware review batched into the final hardware-package review after HW2.
- Fix wave COLLECTED: sonnet DONE, all 7 findings closed (3-layer 21/22 whitelist kills the destructive-service HIGH; start isolation; collision; odd-length; wording; chart pin; core notes a+b). LEAD gates: 875 tests green (626 core + 249 mobile), typecheck/lint 0. Commit b5cc584. Channel revision now fully verified both families.
- HW2 wave 1 COLLECTED: sonnet DONE_WITH_CONCERNS — DRC gate NOT met (19 violations + 4 unconnected, from 127+34 initial; 87% reduction, honest report). Root cause: congestion at 55x25 (two hotspots). Worker's pin-map assumptions for U2/U3/D3 flagged unverified. LEAD response: DESIGN.md rev A2 (board 62x30 — free win, case width governed by 38mm aperture; SW1 relocation; BINDING datasheet pin maps supplied; local 0.15mm at U1). Wave-1 WIP checkpointed. HW2b wave 2 DISPATCHED (sonnet, changed input — legitimate second wave, not a blind retry): re-place hotspots first, DRC hard gate stands, production outputs only from clean board.
- HW2b COLLECTED: DRC 0/0 exit 0 (LEAD re-verified), root-cause re-placement, U3/D3 pin corrections from rev-A2 maps, production regenerated, 3D render reviewed. Committed.
- Hardware package Codex review: NO-GO — 7 HIGH: U3 pin map WRONG (LEAD's own rev-A2 from-memory map — accountability noted: no more memory-sourced electrical facts), FB divider 5.18V would kill U1, TJA1051T/3 VCC needs 5V (CAN dead at 3.3V), D2 reversed on board (pad convention), BOM 21/30 rows no LCSC + impossible 0603 22uF/35V, TVS clamp 39V > buck 30V abs max, LED2 on strapping IO8 (+ LED polarity reversed). MEDs: antenna keepout absent, J1962 footprint absent, UVLO actually 6.2V, firmware rejects spaced hex, CAN response uncorrelated. Clean: read-only guard single chokepoint, ELM vectors, TWAI pins, PESD2CAN, R120 DNP consistency.
- DESIGN rev A3 committed (12 datasheet-derived items; TCAN330 3.3V transceiver w/ silent pin replaces TJA1051; worker mandated to verify every electrical fact + LCSC id against live sources). Remediation wave DISPATCHED (sonnet, bg) across schematic+board+BOM+firmware; gates: DRC 0/0, schematic re-crosscheck, pio build+native tests, BOM zero blank LCSC.
- A3 re-verdict (codex): still NO-GO but CONVERGING — 10/12 prior FIXED, 2 PARTIAL; 7 NEW narrower defects (F1 PTC 16V-rated, SW1 part/footprint variant mismatch, enclosure not re-rendered for 68mm board growth, antenna keepout undersized vs module spec, J1/J2 invalid as CPL rows, C2 far from U3 VIN, UVLO hysteresis 0.34V < TI 0.5V). Firmware fixes: clean, no new defects. Mandatory first-article bench list captured in reverdict log.
- FINAL fix wave DISPATCHED (sonnet, bg): the 7 items, hardware-only, verified-sources rule, DRC/openscad/BOM gates. After collection: LEAD gates -> commit -> push -> morning report with honest GO/NO-GO state.
- FINAL VERDICT (codex, rev A4 / 314fc03): GO — all 7 FIXED with datasheet/LCSC re-verification by the reviewer, no new defects. Design-level fab-ready, CONTINGENT on the documented first-article bench test list (in hwpkg-reverdict log + morning report). P4c-hw CLOSED after: DESIGN A1->A4, 3 review rounds (NO-GO 12 findings -> NO-GO 7 -> GO), 6 hardware/firmware workers + 2 LEAD takeover rounds on quality-of-inputs (memory-sourced electrical facts banned after LEAD's own U3/TCAN pin map errors). Deliverables: schematic SVG+generator, KiCad board DRC 0/0, JLCPCB production set (gerbers/bom 31-31 LCSC/cpl hand-fit-aware), ESP32-C3 firmware (build 0, 45/45 native), enclosure STLs @68mm.
- Zero-warnings pass (user mandate): full-severity DRC was hiding 22 warnings behind the error-only gate — holes_co_located x11 (duplicate vias + vias on J2 THT pads; drill-break risk) + silk x11. Fixed at generator level (via dedupe/THT-skip, REF_POS overrides). LEAD re-verified: --severity-all --all-track-errors -> 0 violations, 0 unconnected, exit 0. tracks_crossing was already 0. Production regenerated from the clean board. Gate standard for any future board work: all severities, not error-only.
- FINAL RELEASE BUILD (2026-08-11): pre-flight LEAD gates all green (app 875 tests + export, firmware build + 45/45, hw DRC all-severity 0/0); E2E preview: GPS pipeline full session (calibration 92% -> laps w/ live delta + PB -> Results) with revised telemetry strip (THR|ENG OIL|COOLANT fallback) + coaching cue w/ observed speeds live simultaneously; console clean. Build 31480637824 SUCCESS on 27af210. IPA forensics PASS: all telemetry markers (strip, schema, elm327, sim, engineOilC/transOilC/latG, gforce, adapter IP), NSLocalNetworkUsageDescription, TcpSocket+ExpoLocation+Audio symbols in native binary, 3 voice mp3s, bundle id intact. Delivered TRACE-TELEMETRY-release-2026-08-11.ipa (13.8MB). Install slots consumed by these campaigns: ZERO.

## Field incident (2026-08-13): calibration impossible at TMR
User on-track report: calibration stalled 81-90% every lap. LEAD diagnosis from code: coverage = 25m bins, acceptance needs >=0.95 + gap <=100m + rejected <=0.3; samples count only within corridorWidthM=15 of the OSM centerline (asset admits "not validated on-site"). Consistent 81-90% = systematic lateral mismatch over ~370-700m -> OFF_CORRIDOR every lap -> acceptance mathematically unreachable. NOT a phone-GPS fault.
Fix dispatched (sonnet, ticket-calib-field-fix.md, LEAD binding designs): two-phase bias-adaptive coverage (wide 40m learn corridor retained, bias estimated then coverage recomputed bias-corrected), acceptance relax to 0.85/250m post-correction, POOR_GNSS computed against wide set, failure screen gains actionable gap-location diagnostics, 4 scenario tests incl. pre-fix-fails proof. Runtime matching/timing untouched (15m + appliedBias as before).
- User follow-ups: (1) "off track" shown while on circuit — same root cause (15m corridor vs unvalidated OSM line); live indicator addendum sent to in-flight calib worker (wide 40m corridor for display). (2) Track-map discrepancy visual requested — ticket-trackmap-viz.md staged (View-based map, raw vs matched dots, lateral offset readout on calibration screen); DISPATCH AFTER calib fix worker collects (overlapping write set: engine + ActiveCalibrationScreen). Then: gates, build, deliver patched ipa.
- User directive: do NOT trigger the ipa build after calib-fix + track-map land — first joint visual test in preview (user reviews the map/calibration behavior), build ONLY after user OK.

## Campaign — Circuit N+1: MotorPark Romania, Adâncata (opened 2026-08-26, user directive)
Baseline: 14067bc, tree has 2 pre-existing modified KiCad files (hardware/kicad/trace-dongle/*.kicad_prl|pro) — NOT ours, left untouched. LEAD: Fable 5 (frontier). Mode: Codex-boosted Full (Agent ✓, real shell ✓, codex-cli 0.145.0 SEAT-OK echo check passed; consent explicit in mission text "review-urile încrucișate pe Codex dacă e disponibil").
Goal: add circuit "MOTOR PAK ROMANIA 927005 Adâncata" per docs/NEXT-CIRCUIT-PLAYBOOK.md §2: OSM geometry (archived + ODbL), deterministic generator, asset regression tests, corner analysis vs published map, replay fixtures + soak, catalog entry, preview E2E. NO ipa build without full verification + explicit user OK on preview visual test.
Hard rules carried: OSM geometry treated as UNVALIDATED until field-proven (bias-adaptive calibration thresholds must not regress); gates on real exit codes (typecheck/test/lint/expo export ios); no facts from memory — live sources cited.
| WP | seat | status |
|----|------|--------|
| CN-scout-repo | haiku | DONE — full pipeline map: generate:tmr script (data/osm inputs, byte-stable pin test), asset schema keys, catalog ENTRIES map (apps/mobile/src/session/circuitCatalog.ts), driveLap fixtures profile-parametrized, soak pattern, CORNER_ANALYSIS_VERSION=3 w/ TMR 12-corner pin |
| CN-scout-web | haiku (web) | DONE_WITH_CONCERNS→partially REJECTED by LEAD: citation-backed facts (lengths 4052/4129, 14-16 turns, width 11-16m, CW primary, modular 3-ring design) accepted; §6 OSM "raw" file FABRICATED (fake fields, wrong lengths) — LEAD re-fetched Overpass live, archived real geom+tags to data/osm/overpass-motorpark-{geom,tags}.json, verified splice topology (way 333031201 main loop 3326m + ext 949617051 867m − 137m chord arc = 4056.2m ≈ published 4052m, 0.1%), direction CW by shoelace, S/F seam on main straight between pit ways. Addendum in scratch/motorpark-research.md |
| CN-W1 | sonnet | attempt 1 LOST (Claude Code process restart mid-run; reconciled: zero files written, HEAD 14067bc unchanged) → attempt 2 DONE: generator w/ fail-loud splice assertions, asset 102 pts / 4056.261 m / CW / corridor 16, +6 tests. LEAD gates authoritative: typecheck 0, test 0 (638 core + 275 mobile = 913), lint 0. Commit 6de8dd8. Fabricated scout file deleted. |
| CN-corners | LEAD | VERIFIED vs published map (LapMeta CW, 16 turns; overlay rendered from asset, N-up): all 10 detected corners map to published turns with matching directions (C1=T1 R, C2=T4 R, C3=T5 L, C4=T6 L, C5=T7 R, C6=T9 R, C7=T10 L, C8=T13+T14 R compound 198°, C9=T15 R, C10=T16 L); T2/T3/T8/T11/T12 are r>125m kinks below threshold by design. S/F between pit entry/exit on main straight, pit parallels whole straight. No CORNER_ANALYSIS_VERSION change. Exact-pin follow-up sent to W1 seat (same context). |
| CN-W1b | sonnet (same seat) | DONE — exact corner pin (10, RRLLRRLRRL, monotone entries, C8>150° documented). Commit 1a10ec5. |
| CN-W2 | sonnet | DONE_WITH_CONCERNS → dispositioned by LEAD: (1) composition.ts outside WRITE SET — ACCEPTED (startDevReplaySession hardcoded TMR one layer below the screen; optional `circuit` param, TMR default, byte-identical for old callers); (2) new devReplayScenarios.ts — ACCEPTED (pure-TS testability); (3) real bug fixed in fixtures/scenarios.ts pitLaneTransitLap (S/F at seam → empty beforeEntry loop) — ACCEPTED, cross-review to confirm TMR-neutral. Soak MotorPark: coverage 0.9816, gap 50m @4000-4050, p95 lateral 2.55m, thresholds untouched. About copy already circuit-generic (ODbL kept). NEW FINDING: CircuitDetailScreen hardcodes TMR regardless of row tapped → CN-scout-plumb dispatched to trace selected-circuit propagation before a fix ticket. LEAD gates: typecheck 0, test 0 (649+279=928), lint 0, expo export 0 (motorpark-romania in hbc). |
| CN-scout-plumb | haiku | DONE — app is single-circuit end-to-end: TMR hardcoded in navigation (no circuitId param), CircuitDetailScreen, createProductionController, SqlSessionHistoryStore filter, deleteAllStoredUserData, coachingConfig (TMR_CORNERS + observed speeds), PersonalBestScreen, ActiveCalibrationScreen track map. Playbook §2 step 5 assumption ("UI built for multiple circuits") was list-only. |
| CN-contracts | LEAD | DONE — "Multi-circuit selection addendum" appended to docs/architecture/contracts.md (selectedCircuitId setting, catalog entry = profile+runtime+corners w/ overlay only where shipped, gate rebuild on circuit change when idle, per-circuit history store rebuilt on select, recovery circuit via listSessions scan, delete-all spans catalog, CircuitDetail param). |
| CN-REV1 | codex (read-only, high) | FAIL — 1 HIGH (pit polyline out-and-back: pit ways SHARE node 8829181316, no gap; naive concat adds 82m artificial travel — LEAD ACCOUNTABILITY: my addendum's "41m gap" was derived from endpoint coordinates, not node ids; reproduced by LEAD on raw JSON) + 1 MED (asset tests don't independently pin splice/S-F; loader length test vacuous) + 1 LOW (soak lap bound relaxed 200s→220s without need) + 1 INFO (reversed comment in scenarios.ts). Clean: splice topology (4056.244m independent), CW, determinism, gate geometry/straightness, pit-exit false-trigger impossible (inPit-only), provenance, soak fidelity, TMR hashes unchanged. |
| CN-REV2 | codex (read-only, high) | FAIL — 3 HIGH (H1 selection during bootstrap lost: selectCircuit early-returns pre-repository, UI navigates anyway; H2 mid-session selection APPLIED not refused, test enshrined it; H3 preflight/recovery don't await rebuildInFlight → stale controller race) + 4 MED (concurrent selects unserialized; DevReplay doesn't select circuit → calibration map shows TMR geometry for a MotorPark replay; delete-all aborts on rejection; activeSessionCircuitId must be transactional + cleared everywhere) + 3 LOW (gate compares raw unknown id; "Official" render branch; vacuous provenance test). Clean: static imports, corners/overlay split, history reassign, idle-only rebuild, hydration defaults, 360pt. Sandbox note: vitest EPERM in read-only sandbox → source-derived. |
| CN-VERIFY | foreman-verifier (claude, blind) | PASS_WITH_NOTES on 529078f — all 9 criteria with own evidence (gates 943/943 + export w/ both circuits in hbc; genuine Overpass files; determinism md5 stable; independent splice 4056.24m + pit 637.54m no backtracking; 3 mutation probes all caught; corners 10/12; corners/calibration/matching diff EMPTY; plumbing trace OK; ODbL visible). Notes: E2E not yet done (in progress), recovery deviation (→ FIX2), LOW "Official" render branch (→ FIX2). |
| CN-contracts-2 | LEAD | DONE — recovery/selection amendment appended (activeSessionCircuitId transactional, selectCircuit awaits ready + serialized + refused mid-session, await rebuildInFlight at entry points, DevReplay selects circuit, resolved-id gate compare, "official" only in negations). |
| CN-E2E | LEAD (web preview) | IN PROGRESS — MotorPark: selection list ✓, CircuitDetail (10 corners, ODbL, 4.056 km) ✓, DevReplay "MotorPark — Three timed laps" → calibration map renders MotorPark outline w/ GPS+on-track dots, coverage 96%, offset 0.1m → ACCEPTED 90% ✓ → dashboard GNSS GOOD, MotorPark coaching cues (C4/C6/C10/C1/C2) ✓, S/F crossing at seam → LAP 1 ✓. Anomaly under investigation: current-lap timer showed 30:14 right after the crossing (replay-clock artifact vs seam-specific? TMR comparison pending). Fixture drained (non-looping) at 38:34 virtual. RESOLVED: lap-timer anomaly is an ENVIRONMENT artifact — extension preview window is occluded (`document.hidden === true`), Chrome intensive timer throttling stalls replay sample delivery (TMR run stalled identically at 1.3 km / 36%) while the 10x virtual clock keeps running. Not a MotorPark defect. Results ("No laps recorded", correct for the truncated run) + History ("MotorPark România · full", session stored under MotorPark) verified. Definitive lap/PB/results E2E deferred to AFTER FIX2 on a headless browser (agent-browser) immune to occlusion. |
| CN-FIX2 | sonnet | DONE — H1–H3, M1–M4, L1–L3 all FIXED (selectCircuit awaits ready + selectionChain + SESSION_ACTIVE refusal; rebuildInFlight awaited at gate/recovery/select; DevReplay selects circuit; delete-all per-circuit try/catch + errorText; activeSessionCircuitId in one withTransactionAsync, recovery order persisted→scan→selection; resolved-id compare; no "Official" branch; single-line provenance). LEAD gates: typecheck 0, test 0 (651 + 304 = 955), lint 0, expo export ios 0. Commit 5d7bcb2. |
| CN-REV3 | codex (read-only, high) | DISPATCHED bg on 5d7bcb2 — re-verify FIX2 + hunt new defects (selectionChain/rebuildInFlight/devReplayLock interplay, transaction vs SQL write gate, recovery paths), ticket-cnrev3-codex-reverify.md |
| CN-REV3 result | codex | FAIL — FIXED: H1 H2 M1 M2 L1 L3; PARTIAL: H3 (no lock after the await in resumeRecovery), M3 (telemetry step skipped on rejection), M4 (PendingRecovery lacks circuitId), L2 (bare 'official' status value); NEW: N1 HIGH recovery loses circuit if selection changes before Resume, N2 HIGH resumeRecovery uses a controller disposed by a coaching rebuild, N3 HIGH selectionChain vs preflight gate commit different circuits, N4 MED telemetry delete skipped, N5 MED DevReplay cleanup races scenario, N6/N7 LOW. Clean: no deadlock cycle, SQL gate OK, TMR default equivalent. DECISION: 2nd Sonnet wave on this area left concurrency holes → escalate seat to frontier Claude (opus) with LEAD binding design = ONE lifecycleLock (contracts "lifecycle lock amendment"); ticket-cnfix3-lifecycle-lock.md STAGED (dispatch after the running E2E finishes). |
| CN-FIX3 | opus (frontier seat, escalated) | DONE_WITH_CONCERNS → all ACCEPTED: single FIFO lifecycleLock replaces selectionChain/rebuildInFlight/withDevReplayLock; invariant "idle controller always built for resolved selection" (stronger than the literal N3 mechanism — startPreflight is a controller no-op); PendingRecovery.circuitId (recovery wins); telemetry delete always; DevReplay runGeneration cancel; statusLabel capitalized + 'official'→"Source-declared"; getProductionCircuitId() diagnostic. Pre-fix failing-test evidence on HEAD logged. LEAD gates: typecheck 0, test 0 (651 + 316 = 967), lint 0, expo export ios 0. Commit 1e0f8c9. |
| CN-REV4 | codex (read-only, high) | FAIL (converging) on 1e0f8c9 — FIXED: H3, M3/N4, M4/N1, L2/N6, N2; PARTIAL: N3 (beginCalibration bypasses the lock; controller.start() reports idle while awaiting provider → selection admitted, disposed controller finishes starting — core lacks disposed re-check after await), N5 (cancelled DevReplay still writes selection), N7. NEW HIGH: delete-all vs async endSession persistence / terminal-controller background checkpoint recreates deleted data. MED: cancelled run changes selection. LOW: unbundled recovery falls back instead of discard; test gaps. Clean: FIFO lock survives throws, no post-await re-entry found, gate rejection doesn't wedge facade, TMR never-selects path equivalent. |
| CN-contracts-4 | LEAD | DONE — "facade boundary amendment": mutating facade commands inside the lock; core start() disposed re-check; delete-all refuses mid-session + rebuilds fresh controller before deleting; background checkpoint only mid-session; cancelled DevReplay = no side effects; unbundled recovery → discard. |
| CN-FIX4 | opus (same seat, new input) | DONE — A–F FIXED with pre-fix failing evidence on fd9a5c6: runLockedCommand + whenCommandsSettled (facade commands inside the lock incl. async provider work); core start() disposed re-checks + un-mint; delete-all refuses mid-session + fresh controller before deletes; background checkpoint only mid-session; cancelled DevReplay no side effects; unbundled recovery discard; +9 tests. LEAD gates: typecheck 0, test 0 (653 + 326 = 979), lint 0, expo export ios 0. Commit a2681e3. |
| CN-REV5 | codex (read-only, high) | FAIL (MED-only) on a2681e3 — FIXED: async facade start, delete-all vs endSession, delete-all vs background checkpoint, unbundled recovery discard; PARTIAL: N5 (cancel mid-await leaves selection applied — consistent but off-spec), test gaps. NEW MED: queued endSession can miss telemetry shutdown started by the lock holder; core disposed start() stops the SHARED provider now owned by its replacement; delete-all admitted while a replay controller is calibrating/armed (dev-only). Clean: no lock re-entry, FIFO no deadlock, lastError surfacing, aborted start id invisible to callbacks, paused checkpoints, TMR default unchanged. |
| CN-contracts-5 | LEAD | DONE — "closing amendment": telemetry re-stop inside locked endSession; core abort never stops the shared provider; delete-all refused DEV_REPLAY_ACTIVE; DevReplay cancellation honored only before the selection write (no rollback, consistency guaranteed). |
| CN-FIX5 | opus (same seat) | DONE — 4/4 FIXED (telemetry re-stop inside locked endSession; core abort never stops shared provider + 2-controller test; DEV_REPLAY_ACTIVE refusal + "Nothing was deleted" copy; cancellation before-write-only, consistency pinned). Item 4 had no red-to-green (HEAD already consistent) — characterization pin, flagged honestly. LEAD gates: typecheck 0, test 0 (654 + 329 = 983), lint 0, expo export ios 0. Commit 12e413d (incl. playbook §3b). |
| CN-REV6 | codex → ABORTED: ChatGPT usage limit hit mid-review ("try again at 11:58 PM"), 52k tokens spent, no verdict. Per skill quota rule: no retries; fallback to Claude seat of the same class → CN-VERIFY-2 (foreman-verifier, blind, bounded to the 4 FIX5 items + mutation probes). Codex seat marked UNAVAILABLE until 23:58. |
| CN-VERIFY-2 | foreman-verifier (claude, blind) | PASS_WITH_NOTES on 12e413d — 4/4 FIX5 items FIXED with mutation probes (each fix reverted in a scratch copy → its test fails), 983/983, TMR default path unchanged, no provider subscription leak. LOW notes: stale core comment + mock-facade refusal wording (fixed by LEAD, commit 3ddf456, doc/string only, gates green); orphaned running GNSS provider when a replacement controller never starts (accepted residual, battery-only, documented as open item). |
| CN-E2E-regress-3 | LEAD (headless) | run5 INVALID — LEAD's own cosmetic edit triggered an HMR reload mid-run (all screens = selection). Re-run as run6 on 3ddf456 with the tree untouched. |
| CN-E2E-regress-2 | LEAD (headless) | PASS on a2681e3 — identical outcomes (calibration accepted, 13 laps, PB 1:39.733, NEW PB, hold-to-end through the locked endSession, History "MotorPark România · Full circuit"). Facade-boundary change is behavior-neutral on the happy path. |
| CN-docs | LEAD | DONE — playbook §3b (lessons 5–10: scout-fabricated data, node-id topology, modular circuits, corner counts, single-circuit app gap, headless E2E mechanics); project memory updated. |
| CN-E2E-regress | LEAD (headless) | PASS on 1e0f8c9 — identical outcomes to the pre-FIX3 run (calibration 90%, 13 laps, PB 1:39.733, NEW PB, seam laps INVALID), lifecycle lock introduced no behavioral change on the happy path. |
| USER DECISION | — | layout label: option 3 — keep layoutIds ('main'/'full', PB keys untouched), friendly UI labels ("Main layout" / "Full circuit") in list chip, detail row, history/PB headers → CN-FIX3b sent to the FIX3 seat (opus, context intact). |
| CN-FIX3b | opus | DONE — layoutLabel() table-driven helper + list chip/detail row/history/PB headers; ids/keys untouched; +3 tests. LEAD gates: typecheck 0, test 0 (651 + 319 = 970), lint 0, expo export ios 0. Commit fd9a5c6. E2E run3 (headless) PASS — labels verified on selection chips ("MAIN LAYOUT"/"FULL CIRCUIT"), detail row, History header "MotorPark România · Full circuit"; same timing outcomes (PB 1:39.733, 13 laps). Screenshots sent to user. |
| ultrareview | user-triggered (cloud) | DONE on 5d7bcb2 (pre-FIX3 HEAD): 2 findings (normal) — resumeRecovery wrong-circuit + disposed-controller races (= Codex REV3 N1/N2) and delete-all skipping telemetry cleanup (= N4). All three already fixed in 1e0f8c9 (CN-FIX3); independent cross-family corroboration of the Codex findings. No new items. Pending REV4 confirmation. |
| USER Q | — | "why MAIN vs FULL?" — answered: layoutId (TMR 'main' legacy; MotorPark 'full' = complete 3-ring layout, leaves room for short configs); layoutId is part of the PB key → decide before real data. LEAD recommendation: keep ids, friendlier UI label. AWAITING user choice. |
| CN-E2E-final | LEAD (agent-browser headless, 360x640) | PASS on 5d7bcb2 — selection (both circuits) → MotorPark detail (10 corners, single-line ODbL provenance) → DevReplay "MotorPark — Three timed laps" → calibration accepted 90% (map + ON TRACK + offset) → dashboard 14 laps @10x, coaching cues on MotorPark corners, live delta, PB 1:39.733 (lap 1,4,7,10,13 identical = deterministic fixture), seam laps INVALID pause-gap/reverse-travel (same as TMR's documented loop-seam behavior) → hold-to-end → Results NEW PERSONAL BEST + sector bests → History "MotorPark România · full · 13 laps". All screens fit 360x640. Screenshots in scratchpad/e2e-run1 (sent to user). Lessons: extension preview window was occluded (timer throttling) → headless is the reliable E2E path; `agent-browser open` never returns against Metro (bound with timeout); screenshot paths must be absolute (daemon cwd). Status labels lowercase regression from FIX2 → FIX3 addendum. |
| CN-FIX2 | sonnet | STAGED — ticket-cnfix2-mobile.md (REV2 H1–H3/M1–M4/L1–L3 + verifier LOW + provenance copy tidy); dispatch AFTER E2E observation ends (mobile edits would hot-reload the preview) |
| CN-FIX1 | sonnet | DONE — F1–F4 FIXED (pit 720.28→637.54m on shared node, connector 8829250717 excluded, fail-loud topology asserts; raw-OSM independent pins for splice/S-F/pit; soak <200s; comment). Centerline/gates byte-identical. LEAD core gates: vitest 651/651 exit 0, tsc 0. Commit eb2e838 (full gates incl. mobile deferred to W3 collection — mobile tree mid-edit). |
| CN-W3 | sonnet | DONE_WITH_CONCERNS → dispositioned: (1) recovery circuit via listSessions is UNREACHABLE for crashed sessions (sessions row written only at endSession — LEAD design gap); worker fell back to the persisted selection (correct in the common case since selection persists before start) — residual edge (switch circuit after crash, before resume) → next fix wave: persist `activeSessionCircuitId` beside the active-session pointer (mobile-only), contracts addendum to be amended; (2) SessionResultsScreen navigate param cascade — ACCEPTED. Remaining TMR refs = defaults/fallbacks only. LEAD gates: typecheck 0, test 0 (651 core + 292 mobile = 943), lint 0, expo export ios 0 (both circuits in hbc). |
| CN-E2E-final-2 | LEAD (headless) | PASS on 3ddf456 (run7; run6 was a script timing failure — app not yet bundled at 8s, fixed with a readiness poll): app ready 30s, calibration accepted, 14 laps, PB 1:39.733, NEW PB, Results, History "MotorPark România · Full circuit · 13 laps". |
- CAMPAIGN CLOSED (2026-08-26): MotorPark România delivered at 3ddf456 — 12 commits over 14067bc, 983 tests (654 core + 329 mobile), typecheck/lint/expo export ios 0. Verification chain: LEAD Overpass re-fetch (scout data fabricated), corner map overlay check, Codex REV1–REV5 (all HIGH fixed, each wave tests-first), cloud ultrareview (corroborated REV3), Claude blind verifiers ×2 (PASS_WITH_NOTES), 7 headless E2E runs (5 valid, all identical). Escalation used once (Sonnet→Opus after 2nd concurrency wave). Codex quota exhausted at REV6 → Claude fallback per skill. NO ipa build triggered; awaiting user's explicit OK on the preview visual test. Open: geometry field-unvalidated (playbook field protocol), no observed speeds, orphaned-provider battery residual, published-length divergence documented. Playbook §3b + memory updated.
- REOPENED (2026-08-26, user finding): "orphaned GNSS provider" residual rejected by the user as a real problem — agreed (session-less GPS watcher is user-visible: location icon + battery). Root cause: core controller owns the provider only once `providerRunning` is set; a controller disposed while awaiting `start()` never sets it, so nobody stops the provider (pre-FIX5 the abort path stopped it but could stop the replacement's). Fix at the owner: composition stops the GNSS provider after every rebuild inside lifecycleLock (fresh controller is idle by construction; later starts restart it). Contracts "provider ownership amendment" appended. CN-FIX6 DISPATCHED (opus, same seat).
| CN-FIX6 | opus | DONE — gnssProvider.stop() after every rebuild inside lifecycleLock (guarded, non-throwing), 5 ownership tests (4 red on HEAD). LEAD gates: typecheck 0, test 0 (654 + 334 = 988), lint 0, expo export ios 0. Commit 981df2d. |
| CN-VERIFY-3 | foreman-verifier (claude, blind) | PASS_WITH_NOTES on 981df2d — stop inside lock after install, mutation probe 4/5 fail, ordering proven (all session starts are locked sections; GnssLocationProvider serializes start/stop), no other GNSS start() callers. LOW: ticket test (1) not literal (window unreachable since FIX4 → fix is defensive there), null-provider path dead-defensive; INFO: rejecting dispose() can still throw before the guarded stop (pre-existing). |
| CN-E2E-final-3 | LEAD (headless) | PASS on 981df2d (run8): calibration accepted, 14 laps, PB 1:39.733, NEW PB, History "MotorPark România · Full circuit · 13 laps". |
- CAMPAIGN CLOSED (final, 2026-08-26): MotorPark România at 981df2d — 14 commits over 14067bc, 988 tests, all gates 0, 6 valid headless E2E runs identical. User's orphaned-provider finding accepted and closed at the owner. Report republished. Awaiting user's explicit OK for the ipa build.

## Phase 4e — ENET telemetry via MHD WiFi Adapter (opened 2026-08-27, user directive)
User facts: car = 2026 Toyota GR Supra MK5 (BMW B58, ZF 8HP automatic); owns an MHD WiFi Adapter. Research (scratch/mhd-adapter-research*.md, cited): the adapter is a BMW ENET (HSFZ/UDS over WiFi TCP) bridge, NOT ELM327; supported by BimmerLink/BimmerCode for the A90; single ECU client at a time; TCP port undocumented publicly; B58/8HP oil-temperature DIDs not public.
USER DECISION: use the MHD adapter (new ENET transport in the app), DROP transmission oil temperature. Purpose: data for a future LLM corner-by-corner analysis of the car's limits (G, braking, acceleration) → prioritise speed/rpm/throttle/brake/steering over temperatures. Custom ESP32 dongle no longer the first path.
Plan: research spike (ENET/HSFZ protocol from open sources, ECU addresses, B58 DIDs, achievable rates) → LEAD contracts addendum (ENET transport) → tickets. Codex seat unavailable until 23:58 (quota).
| WP | seat | status |
| P4e-scout-proto | sonnet (web) | DISPATCHED — enet-protocol-research.md |
| P4e-scout-repo | haiku | DISPATCHED — telemetry transport seams |
| P4e-scout-repo | haiku | DONE — seams: ObdTransport {connect,send,onData,onClose,close} (contracts.ts:24-30), Elm327SessionEngine owns handshake/poll/decode (elm327Session.ts), pidCodec mode-01 decoders, SimulatedElm327Transport, telemetryProvider.ts builds transport+session from settings (simulate/TCP), TcpObdTransport (react-native-tcp-socket), recorder cap 200k, TelemetryScreen monitor, gforceProvider 25Hz same sample path, coach has NO telemetry input yet. |
- HANDOFF DOC (user directive): docs/roadmap/phase-5-llm-corner-analysis.md — goal (LLM per-corner analysis), channel priorities, build order (P4e ENET → cornerMetrics pure module → car envelope → opt-in export/LLM → UI), constraints. Linked from playbook §5 and memory.
| P4e-scout-proto | sonnet (web) | DONE_WITH_CONCERNS — VERIFIED (primary): HSFZ header/control words, TCP 6801, UDP 6811; ediabaslib GPL-3 / scapy GPL-2 → reimplement, never vendor. MEDIUM: DME 0x12, tester 0xF4/0xF1. NOT FOUND: DSC/EGS/SZL addresses, any public B58/DSC DID table, mode-01-over-ENET behavior, keep-alive interval, achievable Hz; Supra exposes NO brake/steering on mode-01 (SupraMKV snippet). Forums 403'd on direct fetch → snippet-tier evidence flagged. |
- P4e-T0 contracts: DONE (LEAD) — "ENET telemetry addendum" (frame layout, whitelist {01,22,3E}, EnetChannelSpec with provenance, defaults, diagnostics, DID-probe dev screen, simulator).
- Codex seat: quota reset confirmed (SEAT-OK echo, 2026-08-27 early) → available for P4e cross-review.
| P4e-T1 | sonnet | DISPATCHED — ENET/HSFZ+UDS core engine + simulator + whitelist + tests, ticket-p4e-t1-enet-core.md |
| P4e-T2 | sonnet | STAGED — mobile wiring ticket drafted (API surface placeholder until T1 collects) |
| P4e-T1 | sonnet | DONE — enet/{hsfzCodec,udsCodec,enetChannelSpecs,enetSession,simulatedEnetTransport}, 68 tests; concerns accepted (error-frame bytes exposed raw; protocol anomalies don't count toward consecutiveErrors; 0x78 chain cap 20; TesterPresent fire-and-forget). LEAD gates: typecheck 0, test 0 (722 core + 334 mobile), lint 0. Commit f069a4f. |
| P4e-REV1 | codex (read-only, high) | DISPATCHED bg on f069a4f — core engine cross-review (framing vectors, whitelist bypass attempts, UDS parsing, engine races, decode math, licensing) |
| P4e-T2 | sonnet | DISPATCHED — mobile wiring (settings, EnetTcpTransport, provider, monitor, DID probe dev screen) on f069a4f |
| P4e-REV1 | codex | FAIL — HIGH: HSFZ layouts flattened (error frames expected/received, long alive-check identification string; wrong short-alive reply; 65535 bound invented); responses not correlated to the in-flight request (late response resolves the next request; delayed 7F 3E 31 marks a channel unsupported). MED: corrupted-length "resync" invalid (must drop connection); spec validation gaps (channel↔PID consistency, non-integer offsets, NaN/Infinity, byteLength 3). LOW: TesterPresent starvation with tiny interval; ACK latency misattributed. Clean: length excludes control word, fragmentation vectors, whitelist unbypassable, UDS parsing, stop guard, no GPL similarity, ELM untouched. |
- P4e contracts amendment (LEAD): control-specific frames, 4096 bound + fatal reconnect, correlation rule, spec validation, TesterPresent clamp. P4e-FIX1 DISPATCHED (sonnet, core-only, API additive; parallel with T2 mobile).
| P4e-T2 | sonnet | DONE (uncommitted, gates deferred until FIX1 collects — core mid-edit) — settings (adapterType/enet*), EnetTcpTransport (Uint8Array, binary-safe), enetSettingsValidation, provider engine selection (ENET states mapped onto Elm327State for TelemetryStrip), monitor ENET fields, DidProbeScreen (__DEV__, whitelist-enforced), 31 tests (mobile 365). Deviation ACCEPTED: route in RootNavigator.tsx (App.tsx has no route table). Note: transOilC polling still gated by transOilPidHex — irrelevant (trans oil out of scope), recorded. |
| P4e-FIX1 | sonnet | DONE_WITH_CONCERNS — H1 discriminated HsfzFrame union + control-specific encoders + 4096 bound (cited vectors pass), H2 response correlation + unmatchedResponses, M1 fatal framing → transport closed via failure path, M2 channel↔PID table + decode validation + decodeErrors, L1 TesterPresent clamp/no-starvation, L2 ACK attribution; +26 tests (enet 94, core 748). Concern: stricter validation breaks 1 mobile T2 test that used a deliberately wrong PID → P4e-T2b follow-up sent to the T2 seat. |
| P4e-T2b | sonnet | DONE — mobile NRC test rewritten to a did-mode unscripted spec; DidProbeScreen typechecks against the new frame union. LEAD gates on FIX1+T2: typecheck 0, test 0 (748 core + 365 mobile = 1113), lint 0, expo export ios 0. Commit 053c274. |
| P4e-REV2 | codex | DISPATCHED bg on 053c274 — FIX1 re-verify + mobile wiring review |
| P4e-E2E | LEAD (headless) | STAGED — scratchpad/e2e-enet.sh (settings → ENET + simulate → monitor polling → DID probe) |
| P4e-REV2 | codex | FAIL — Part A: H1/M1/M2/L1/L2 FIXED, H2 PARTIAL (malformed correct-address response clears pending; late response w/ empty slot uncounted). Part B HIGH: structurally invalid spec JSON members throw (blur/render); DID probe bypasses telemetry gate + single-client rule (2nd connection while polling). MED: custom ENET channels never polled (fixed ELM plan; transOilC gated by transOilPidHex); EnetTcpTransport not marked closed on remote close; probe response uncorrelated. LOW: persisted malformed values unvalidated; 360pt overflow risk on address rows; test gaps. Clean: byte safety, provider selection, whitelist unbypassable, __DEV__ gating, no fetch/UDP. |
- Contracts amendment (LEAD): correlation completeness, spec-derived ENET poll plan + rate table, untrusted JSON structural validation, probe gating/exclusivity/correlation, one-shot transport, hydration repair, 360pt rule. P4e-FIX2-core (FIX1 seat) + P4e-FIX2-mobile (T2 seat) DISPATCHED in parallel (disjoint write sets).
| P4e-FIX2-core | sonnet | DONE — malformedResponses counter (pending untouched), unmatched-when-idle counted, ENET_DEFAULT_CHANNEL_RATES_HZ exported; 6 tests red on HEAD → green (core 754). Uncommitted, awaiting FIX2-mobile for joint gates. |
| P4e-FIX2-mobile | sonnet | DONE — H1 structural type guard (never throws), H2 didProbe.ts pure module + gating (ENET enabled + provider idle/stopped/failed) + simulate, M1 spec-derived poll plan via ENET_DEFAULT_CHANNEL_RATES_HZ, M2 one-shot transport (red→green ×4), M3 probe correlation (review scenario), L1 repairPersistedEnetSettings in SqlSettingsStore.create, L2 label flexShrink + narrower hex inputs (not layout-tested — no RN renderer), L3 +53 tests incl. static no-raw-network test. LEAD gates (core+mobile): typecheck 0, test 0 (754 + 418 = 1172), lint 0, expo export ios 0. Commit 2e73057. |
| P4e-REV3 | codex (bounded) | DISPATCHED bg on 2e73057 |
| P4e-E2E | LEAD (headless) | RUNNING on 2e73057 — scratchpad/e2e-enet2 |
| P4e-E2E | LEAD (headless, 360x640) | PASS on 2e73057 — Settings: Vehicle telemetry on → adapter ENET → simulate → monitor CONNECTED, ENET (BMW), target 0x12, rpm 1114 @4.3Hz, speed 35 @4.3Hz, throttle 80% @4.1Hz, coolant 71 @0.2Hz, oil 76 @0.5Hz, frames 117/230; DID probe screen gated ("Stop telemetry first"). Run 1 hung at agent-browser `open` (session clash) → killed; run 2 clean. |
| P4e-REV3 | codex (bounded) | FAIL (converging, 0 new) — FIXED: Part A H2 ×2, M1, M3, L1, L2; PARTIAL: H1 (channel membership unvalidated; mixed invalid array returns ok), H2 (no shared adapter reservation probe↔provider retry), M2 (connection-phase error/timeout not terminal), L3 (tests for those). P4e-FIX3 DISPATCHED (T2 seat): ENET_SPEC_CHANNELS core set + structural membership, enetAdapterReservation pure module used by provider start/retry and probe, connection-phase terminalization. |
| P4e-FIX3 | sonnet (T2 seat) | DONE — ENET_SPEC_CHANNELS (core, +2 tests) + structural membership + any-invalid-member → ok:false/fallback; enetAdapterReservation pure module (10 tests) wired into provider start/retry (blocked → idle + diagnostics) and DID probe (per-request claim, finally-release); connection-phase failures terminalize the transport (rule: never-connected socket never fires onClose). All red→green verified by the worker. LEAD gates: typecheck 0, test 0 (756 core + 440 mobile = 1196), lint 0, expo export ios 0. Commit 3027d94. |
| P4e-REV4 | codex (bounded) | DISPATCHED bg on 3027d94 |
| P4e-E2E-2 | LEAD (headless) | RUNNING on 3027d94 |
| P4e-REV4 | codex (bounded) | FAIL — H1 FIXED, M2 FIXED, H2 PARTIAL + 2 NEW HIGH in the reservation: same-owner reacquire without generation identity (stop→start overlap opens a 2nd socket, old stop releases gen-2's claim); exception paths leak the claim (construction/start throw, retry w/o catch, session.stop rejection) → probe refused forever. Clean: race test, probe finally-release, ELM untouched. |
| P4e-E2E-2 | LEAD (headless) | PASS on 3027d94 — CONNECTED, 5 channels at plan rates, probe gated. |
- P4e-FIX4 DISPATCHED (T2 seat): token-based reservation (no same-owner reacquire; release only by current token), start awaits in-flight stop, try/finally release on every provider path, probe token release.
| P4e-FIX4 | sonnet (T2 seat) | DONE — symbol tokens, no same-owner reacquire, release-by-current-token; start awaits in-flight stop (ENET-gated — worker caught its own ELM regression via the full suite and re-scoped); try/finally release on construction/start/retry/stop-rejection; probe token release; overlap test rewritten to "exactly one socket"; +4 tests. LEAD gates: typecheck 0, test 0 (756 + 444 = 1200), lint 0, expo export ios 0. Commit 69b0a31. |
| P4e-REV5 | codex (bounded) | DISPATCHED bg on 69b0a31 |
| P4e-REV5 | codex (bounded) | FAIL — both REV4 HIGHs FIXED; NEW HIGH: unguarded reservation notify() → throwing subscriber leaks the claim / skips resolveStopping (later starts wait forever); NEW MED: stop()'s unconditional finally changed ELM rejected-stop cleanup semantics (ELM must stay byte-identical). P4e-FIX5 DISPATCHED (guarded notify + nested finally for resolveStopping; ELM cleanup semantics restored, ENET-only finally). |
| P4e-FIX5 | sonnet (T2 seat) | DONE — guarded notify() + guarded subscribe replay; nested finally so resolveStopping always runs; ELM stop path restored to the exact pre-FIX4 sequence (pinned by a rejecting-stop test: diagnostics + listeners retained); +5/+1 tests. LEAD gates: typecheck 0, test 0 (756 + 449 = 1205), lint 0, expo export ios 0. Commit 23dba91. |
| P4e-REV6 | codex (bounded) | DISPATCHED bg on 23dba91 |
| P4e-E2E-3 | LEAD (headless) | RUNNING on 23dba91 |
| P4e-REV6 | codex (bounded) | PASS_WITH_NOTES on 23dba91 — both FIX5 items FIXED, zero new findings. |
| P4e-E2E-3 | LEAD (headless) | PASS on 23dba91 — CONNECTED, 5 channels at plan rates, probe gated. |
- P4e CLOSED (2026-08-27, autonomous overnight): ENET/HSFZ+UDS telemetry for the MHD WiFi Adapter at 23dba91 — 9 commits over 4703df3, 1205 tests (756 core + 449 mobile), all gates 0; 6 Codex rounds (REV1–REV6: 6+9+3+2+2 findings, all fixed, last round clean), 3 valid headless E2E runs on the simulator. Field-empirical remaining: adapter IP/port, DME mode-01-over-ENET behavior, B58/DSC DIDs (DID-probe screen). No ipa build (awaiting user OK). Report published.

## Phase 4f — ENET auto-discovery & DID sweep (opened 2026-08-27, user directive "mergi cu P4f, autonom")
Goal: after joining the adapter WiFi (manual on iOS — entitlement), the app finds IP/port itself (TCP scan + HSFZ TesterPresent validation), auto-connects, and a dev DID sweep with heuristics proposes channel specs for one-tap confirmation. Contracts addendum appended (LEAD). Plan: T1 core pure modules → T2 mobile (expo-network, Find adapter, auto-connect, sweep screen, tagging) → Codex cross-review → fix waves → E2E on simulator → report.
| WP | seat | status |
| P4f-T1 | sonnet | DISPATCHED — ticket-p4f-t1-core.md |
| P4f-T1 | sonnet | DONE — enetDiscovery (candidates + bounded/abortable level-1/2 runner), didSweep (resumable plan, adaptive pacing), didHeuristics (exact-width decodes, ranked suggestions) + enetSpecsFromSuggestion, simulator DID scenario + discovery probe factory; 32 tests. LEAD gates: typecheck 0, test 0 (788 core + 449 mobile), lint 0. Commit 52d7e6e. |
| P4f-REV1 | codex (high) | DISPATCHED bg on 52d7e6e — core cross-review |
| P4f-T2 | sonnet | DISPATCHED — mobile wiring (expo-network, Find adapter, auto-connect, DidSweep screen, tagging), ticket-p4f-t2-mobile.md |
| P4f-REV1 | codex | FAIL — HIGH: discovery limits not hard (cap 16 unenforced, budget doesn't cancel actives, hanging close hangs run); sweep boundary doesn't enforce 0x22 / validate / correlate (0x6E accepted, 0x78 final, DID echo unstripped). MED: non-/24 masks drop the phone subnet, noncanonical IPs, level-2 accepts unknown controls, late abort, pause/resume loses outcomes, unbounded priority ranges/pacing, heuristics ignore timing/scale, steering zero-crossing miss. LOW: spec builder unvalidated; test gaps. Clean: ordering/dedupe/cap, TesterPresent via whitelist, bounded DID space, simulator determinism, additive diff. |
- Contracts amendment (LEAD): hard bounds, sweep boundary with low-level sendRequest (BREAKING for T2 — notified), time-aware heuristics, canonical candidates. P4f-FIX1 DISPATCHED (T1 seat) in parallel with T2.
| P4f-FIX1 | sonnet (T1 seat) | DONE — discovery hard bounds (cap 16, budget/abort close actives w/ 200ms close race, sync abort, level-2 kinds, canonical IPs, phone /24 always), sweep boundary rewritten (sendRequest(pdu) low-level API, real parser + correlation + DID strip, 0x78 bounded, per-request timeout, pause re-check, accumulator across resumes, validated/clipped ranges, pacing clamp), time-aware heuristics (all 4 Codex counterexamples), validated spec builder; 56 tests (core 812). Pre-fix: old code HUNG on the new tests (confirms H1). Mobile impact: didSweepController.ts:217 uses old `request` param → 1 TS error + 5 tests red until T2 adapts (T2 was notified). Uncommitted — joint gates after T2. |
| P4f-T2 | sonnet | DONE_WITH_CONCERNS → dispositioned: (1) built against the new sweep API at integration — fine; (2) tag-as flow tested only at pure level, GNSS speed context unwired in observation — ACCEPTED scope cut (documented); (3) 360pt not visually verified → E2E. expo-network@57.0.1 (SDK-matched, single root lockfile). LEAD gates on FIX1+T2: typecheck 0, test 0 (812 + 491 = 1303), lint 0, expo export ios 0. Commit 91c23d0. |
| P4f-REV2 | codex (high) | DISPATCHED bg on 91c23d0 — FIX1 re-verify + mobile review |
| P4f-E2E | LEAD (headless) | STAGED — scratchpad/e2e-p4f.sh (Find adapter + DID sweep on simulator) |
| P4f-E2E-1 | LEAD (headless) | PARTIAL on 91c23d0 — Settings ENET rows + "Auto-discover on connect" + "Find adapter" render; DID sweep screen runs (SWEEPING 0x122E · 4655/65536 · 166 req/s, reservation notice). FINDINGS for the fix wave: (a) simulate mode: Find adapter uses real sockets → "No adapter answered (scanned 1)" — must use createSimulatedDiscoveryProbeFactory under telemetrySimulate; (b) sweep buckets NRC/timeouts/unmatched stay 0/0/0 while requests proceed → responses not classified (counting/display or simulator answer bug). |
| P4f-REV2 | codex | FAIL — Part A: candidates/level-2/abort/pause/ranges/heuristics/spec builder FIXED; PARTIAL: budget hard cap 8s missing + concurrency NaN, 0x78 re-sends instead of extending wait, sync-throw in withTimeout. Part B HIGH: sweep connects before reserving; reservation released while socket open + reconnect on one-shot transport; sendRawUdsRequest correlates addresses only, re-sends on 0x78, no TesterPresent. MED: stop doesn't abort auto-discovery; observation from paused refused; cadence ≠ 1 Hz + no GNSS context; reentrant start leaks. LOW: 360pt progress text; test gaps. Clean: expo-network SDK-matched (docs cited), static no-raw-network, Find adapter reservation/finally/cancel/provenance, auto-connect once + token reuse, ELM untouched, hydration, tagging. |
- Contracts amendment (LEAD): sweep interface {send,nextResponse,keepAlive}, controller-owned transport lifecycle (reserve→open→run→close→release), abortable auto-discovery, simulated discovery under simulate, cadence rule. P4f-FIX2-core (T1 seat) + P4f-FIX2-mobile (T2 seat) DISPATCHED in parallel.
| P4f-FIX2-core | sonnet (T1 seat) | DONE — budget hard cap 8s + concurrency sanitization (NaN previously = ZERO workers), SweepTransport {send,nextResponse,keepAlive} with 0x78 re-await (no re-send), bounded unmatched retries, keepAlive cadence, guardedCall containment, simulator unknown-DID NRC 0x11→0x31 + core E2E test (simulator + real framing + runner: NRC counts > 0, 3 responders found); 23/49 new tests red on HEAD. Core 819 green. Uncommitted — joint gates after FIX2-mobile. |
| P4f-FIX2-mobile | sonnet (T2 seat) | DONE_WITH_CONCERNS — H1/H2 lifecycle (reserve→fresh transport→run→close→release; refused start opens no socket), H3 raw UDS channel, M1 abortable auto-discovery on stop, M2 observation from paused reuses claim, M3 round-robin cadence + GNSS speed from facade, E2E-a simulated discovery under simulate, L 360pt; +12 tests. CONCERN (LEAD rejects as final state): core interface flickered during the wave → the controller re-implemented runner logic locally (resolveOneDid: correlation/0x78/keep-alive) instead of delegating to core. Snapshot gates: typecheck 0, test 0 (819 + 503 = 1322), lint 0, expo export ios 0. Commit e053dcb. P4f-T3 (integration: delegate to core runDidSweep, delete duplication) DISPATCHED to the T2 seat. |
| P4f-T3 | sonnet (T2 seat) | DONE — controller delegates to core runDidSweep via SweepTransport over ObdTransport; resolveOneDid + local keep-alive deleted (−277/+138); observation = core runner over one-DID plans; integration test simulator→channel→core runner (3 responders, NRC>0). LEAD gates: typecheck 0, test 0 (819 + 502 = 1321), lint 0, expo export ios 0. Commit ff92597. |
| P4f-REV3 | codex (bounded) | DISPATCHED bg on ff92597 |
| P4f-E2E-2 | LEAD (headless) | RUNNING on ff92597 |
| P4f-E2E-2 | LEAD (headless) | PASS on ff92597 — simulate mode: Find adapter → "192.168.4.1:6801 · level 2 · 6ms · Apply"; DID sweep: 166 req/s, NRC bucket = requests (4658/4658; the E2E-1 zero-bucket defect is gone), reservation notice, GNSS-context hint; responders sit above the 30 s window (found in the core integration test). |
| P4f-REV3 | codex (bounded) | FAIL (converging) — Part A all FIXED; Part B: H1/H2 PARTIAL (close/release race on slow close + Stop; no lifecycle try/finally; completion can overwrite 'stopped'), M1 PARTIAL (network-info read unabortable), rest FIXED. NEW MED: per-DID observation runs reset keep-alive/pacing/error budget. LOW: budget 0 → 8000. Clean: no duplicated wire logic in the controller, ordinary paths correct, simulator NRC substantive, ELM untouched. |
- Contracts amendment (LEAD): core runDidObservation (single loop owns keep-alive/pacing/error budget), budget 0 = no probes, retained transport until close settles + lifecycle try/finally + generation guard, abortable network-info read. P4f-FIX3-core (T1 seat) + P4f-FIX3-mobile (T2 seat) DISPATCHED in parallel.
| P4f-FIX3-core | sonnet (T1 seat) | DONE — runDidObservation (single loop; shared createErrorBudget/createKeepAliveTicker with the sweep runner), budget ≤0 = no probes; 9 tests (8 red on HEAD, incl. the "0 keep-alives in a 10 s fast window" counterexample). Core 828 green. Uncommitted — joint gates after FIX3-mobile. |
| P4f-FIX3-mobile | sonnet (T2 seat) | DONE — teardown retains transport until close settles / joins in-flight teardown; runGuarded/finishSweepRun single lifecycle with generation recheck; observation = ONE core runDidObservation call; readNetworkInfoRaced (abort + 1500 ms); +5 tests red→green. LEAD gates (core+mobile): typecheck 0, test 0 (828 + 507 = 1335), lint 0, expo export ios 0. Commit 4dd9990. |
| P4f-REV4 | codex (bounded) | DISPATCHED bg on 4dd9990 |
| P4f-E2E-3 | LEAD (headless) | RUNNING on 4dd9990 |
| P4f-E2E-3 | LEAD (headless) | PASS on 4dd9990 — identical to E2E-2 (Find adapter level 2 · 7 ms · Apply; sweep 167 req/s, NRC bucket = requests, reservation notice). |
| P4f-REV4 | codex (bounded) | FAIL (converging) — FIXED: abortable network-info, core observation runner, budget ≤0; PARTIAL: lifecycle (sync close() throw uncontained → claim leak). NEW MED: observation tMs absolute vs GNSS series relative (time-domain mismatch in speed correlation); pacing wait rechecks only stopped. Clean: shared budget/ticker refactor behavior-preserving, stopObservationEarly semantics, ELM untouched. P4f-FIX4 core (relative tMs + startedAtMs, pause/expiry recheck) + mobile (contain sync close throw, unconditional release, consistent time base) DISPATCHED in parallel. |
| P4f-FIX4-core | sonnet (T1 seat) | DONE — tMs relative to observation start + startedAtMs; pacing wait rechecks stopped → deadline → paused (waits out pause); 4 tests red on HEAD (the extra-send bug reproduced live). Core 831 green. Uncommitted — joint gates after FIX4-mobile. |
| P4f-FIX4-mobile | sonnet (T2 seat) | DONE — closeQuietly contains sync/async close failures; stop() teardown in try/finally → unconditional release; time base verified consistent (screen anchors GNSS to the same start); +3 tests red→green. LEAD gates: typecheck 0, test 0 (831 + 510 = 1341), lint 0, expo export ios 0. Commit 199ff67. |
| P4f-REV5 | codex (bounded) | DISPATCHED bg on 199ff67 |
| P4f-E2E-4 | LEAD (headless) | RUNNING on 199ff67 |
| P4f-REV5 | codex (bounded) | FAIL (converging) — FIXED: close containment + unconditional release, post-pacing rechecks; PARTIAL: time domain (controller ignores startedAtMs, UI anchors GNSS at the tap → offset = connection delay); NEW MED: extra send possible after a slow keep-alive. P4f-FIX5 core (single shouldContinue before every send in both runners) + mobile (observation anchor in the UI's clock domain) DISPATCHED in parallel. |
| P4f-E2E-4 | LEAD (headless) | PASS on 199ff67 — same outcomes (Find adapter level 2, sweep ~167 req/s, NRC = requests). |
| P4f-FIX5-mobile | sonnet (T2 seat) | DONE — onObservationStarted anchor (captured when the core loop begins, after connect) + snapshot.observationAnchorWallClockMs; screen buffers raw GNSS instants and re-bases to the anchor (samples before the anchor dropped); 3 s connect-delay test (anchor ≥ tap+3000, ±50 ms recovery, tap-anchoring off by >2500 ms). Mobile 511. Uncommitted — joint gates after FIX5-core. |
| P4f-FIX5-core | sonnet (T1 seat) | DONE — one shouldContinue() (stopped → deadline → paused-wait → error budget) before every send in both runners; 4 tests (red on HEAD: [1,2] instead of [1]); worker caught two test-construction pitfalls (wrong keep-alive site, guardedCall timeout masking) before trusting red. Core 835. LEAD gates (core+mobile): typecheck 0, test 0 (835 + 511 = 1346), lint 0, expo export ios 0. Commit d850c12. |
| P4f-REV6 | codex (bounded) | DISPATCHED bg on d850c12 |
| P4f-E2E-5 | LEAD (headless) | RUNNING on d850c12 |
| P4f-REV6 | codex (bounded) | FAIL (residual only) — shouldContinue gate FIXED in both runners; anchor PARTIAL (controller reads the clock before core captures startedAtMs → skew); LOW: anchor not reset on re-observation from complete. P4f-FIX6 core (onStarted(startedAtMs) callback) + mobile (anchor from onStarted only; reset on re-observation) DISPATCHED. |
| P4f-E2E-5 | LEAD (headless) | PASS on d850c12 (level 2 · 6 ms; sweep 166 req/s). |
| P4f-FIX6 | sonnet ×2 | DONE — core onStarted(startedAtMs) at the exact capture point (ticking-clock test); controller anchors ONLY from onStarted (skewing-clock test: exact call-count match) and resets the anchor on re-observation from complete. LEAD gates: typecheck 0, test 0 (836 + 513 = 1349), lint 0, expo export ios 0. |
- P4f CLOSED (2026-08-27): auto-discovery + auto-connect + DID sweep/heuristics/tagging at the commit below — 11 commits over 73e2fce, 1349 tests, all gates 0; 6 Codex rounds (REV1–REV6: 12+11+4+3+2+2 findings, all fixed, last round residual-only), 4 valid headless E2E runs on the simulator (Find adapter level 2; sweep ~120–167 req/s with classified responses). Field-empirical remaining: real adapter answers, DME mode-01 over ENET, B58/DSC DIDs (sweep + tagging). No ipa build (awaiting user OK). Report published.

## Build (2026-08-27, user directive "da build sa testam" — driveway connectivity test)
Pre-flight: pipeline complete for 5ea99b4 (gates 0/0/0/0, 1349 tests, preview E2E ×(6 MotorPark + 3 ENET + 5 P4f), Codex + blind verifiers). Pushed 14067bc..5ea99b4 (33 commits) to origin/main. Build unsigned iOS run 33082027368 dispatched (variant=both) on 5ea99b4. Next: watch → ipa forensics (bundle id app.circuittimer.tmr, markers: motorpark-romania asset, enet/hsfz engine, discovery, DidSweep/DidProbe must be __DEV__-excluded in release, ODbL, 3 voice mp3s) → deliver with driveway test protocol. Install slots consumed so far: 0.
- Build 33082027368 SUCCESS on 5ea99b4 (~14 min, variant=both). IPA forensics PASS: bundle id app.circuittimer.tmr intact; main.jsbundle 2.96 MB with markers motorpark-romania=1, transilvania-motor-ring=1, HSFZ=10, enet=21, "ENET (BMW)"=1, "Find adapter"=1, "Auto-discover"=2, UdsServiceNotAllowed=1 (whitelist present), ODbL=1, "Full circuit"/"Main layout"=1; DevReplay residual strings 5 (dev screens __DEV__-gated, string residue accepted, prior baseline 3); 3 voice mp3s present; native TcpSocket/ExpoLocation/ExpoNetwork/ExpoSensors symbols present. Delivered builds/ipa/TRACE-ENET-MOTORPARK-release-2026-08-27.ipa (13.9 MB) with driveway test protocol. Install slots consumed by these campaigns: 0 (this install = user's decision, 1 slot).
- FIELD RESULT (2026-08-27 17:47–17:49, user's driveway, engine idling, 2026 GR Supra + MHD WiFi Adapter, build 5ea99b4): telemetry monitor CONNECTED via ENET, target 0x12, phone 192.168.4.7 (adapter subnet 192.168.4.x), per-channel 4.5–4.7 Hz, ACK latency p50/p95 6/12–14 ms, frames tx/rx 1088/2141; DME answers MODE-01 over ENET: rpm 656–678, speed 0, throttle 14–15 %, coolant 53→58 °C, engine oil 58→60 °C (PID 0x5C answered!). First attempt showed CONNECTION FAILED with the hint "Join the adapter's WiFi (MHD_XXXX) first" (before joining WiFi) — expected. USER FINDINGS: (1) "throttle 14–15 % with no pedal" → PID 0x11 is throttle PLATE (idle opening), not the accelerator pedal — add pedal channel (0x49/0x4A) + relabel; (2) brake/steering/G not shown in the monitor → G is recorded not displayed (channel-revision decision) — show latG/longG in the monitor; brake/steering need DIDs (sweep, dev-client); (3) BUG: switching ELM327 → ENET without restarting the app left ENET unable to connect until force-quit → scout dispatched. Install slot consumed: 1.

## Phase 4g — field revision (opened 2026-08-27 after the driveway test)
Scout (haiku) root cause for the ELM→ENET switch bug: start() no-op while `running`; fail handler never resets it; retry fires with stale adapterType; ELM socket left open on async init failure (adapter single client). Contracts "field revision" addendum appended. P4g ticket (pedal PID 0x49 + relabel, G rows in monitor, switch-bug lifecycle fix with fingerprint, hidden developer mode for DID probe/sweep in release) DISPATCHED to the T2 seat.
| P4g | sonnet (T2 seat) | DONE — core accelPedalPct (PID 0x49, 100/255·A, +12 tests) + simulators/LapDetail exhaustive maps; monitor rows (Accelerator pedal / Throttle plate / latG / longG via summarizeGForceSamples); switch-bug fix (fingerprint, teardown+relaunch, generation-scoped transport close — worker caught a shared-field bug that could close a newer generation's transport —, retry abort on change, settings watcher with discovery guard, 200 ms force-close for hung connect; 9 field-sequence tests red→green); hidden developer mode (7 taps on version, routes shipped in release, entry points gated). LEAD gates: typecheck 0, test 0 (841 + 542 = 1383), lint 0, expo export ios 0 (strings confirmed). Commit d56c6bc. |
| P4g-REV1 | codex (high) | DISPATCHED bg on d56c6bc |
| P4g-E2E | LEAD (headless) | RUNNING on d56c6bc |
| P4g-E2E | LEAD (headless) | PASS on d56c6bc — monitor CONNECTED with 6 channels (Throttle plate 80 %, Accelerator pedal 46 %, rpm/speed/coolant/oil) at plan rates; G rows absent on web (no accelerometer — expected); probe gated. |
| P4g-REV1 | codex (high) | FAIL — HIGH: ELM teardown not serialized with the next Start (stopping only for ENET; Start during the 200 ms force-close launches ENET with the old socket open; watcher can emit stopped over the newer generation; double Start); settings changes during auto-discovery ignored (watcher early-returns on current===null; discovery result persisted over the user's edit; can build an ELM transport for an ENET session). MED: telemetrySimulate missing from the fingerprint; field-repro test never produces the async failure + coverage gaps. LOW: dev-tap window measured between taps. Clean: PID 0x49, force-close generation-scoped, healthy no-op, retry fingerprint, G rows, dev gating, no deep-link, no DevReplay fixtures in release. P4g-FIX1 DISPATCHED (T2 seat). |
| P4g-FIX1 | sonnet (T2 seat) | DONE — unified doStop (shared stopping for ELM+ENET, force-close inside, finally cleanup), starting coalescing, generation-guarded stopped, watcher aborts in-flight discovery regardless of current, discovery fingerprint start-vs-completion, telemetrySimulate in fingerprint, dev-tap window anchored to first tap, real async-init field test; two prior tests rewritten to the mandated unified semantics (flagged). LEAD gates: typecheck 0, test 0 (841 + 552 = 1393), lint 0, expo export ios 0. Commit d992353. |
| P4g-REV2 | codex (bounded) | DISPATCHED bg on d992353 — incl. explicit check of what changed on the ELM path |
| P4g-E2E-2 | LEAD (headless) | RUNNING on d992353 |
| P4g-REV2 | codex (bounded) | FAIL — H2/M1/M2/L1 FIXED; H1 PARTIAL (stale 'stopped' over a newer discovery when current===null); NEW HIGH: `starting` latch never clears (p.finally identity mismatch) → all later Starts no-op forever; ELM change on rejecting stop = mandated unification (accepted, contracts note appended). P4g-FIX2 DISPATCHED. |
| P4g-E2E-2 | LEAD (headless) | PASS on d992353 (CONNECTED, probe gated) — cannot catch the latch bug (single Start). |
| P4g-FIX2 | sonnet (T2 seat) | DONE — starting latch (starting = p, finally as side effect) at both call sites; stale 'stopped' requires current===null AND generationCounter unchanged since the stop; ELM rejecting-stop cleanup documented; +3 tests red→green. LEAD gates: typecheck 0, test 0 (841 + 555 = 1396), lint 0, expo export ios 0. Commit 3e687b2. |
| P4g-REV3 | codex (bounded) | DISPATCHED bg on 3e687b2 |
| P4g-E2E-3 | LEAD (headless, now with Stop→Start cycle) | RUNNING on 3e687b2 |
| P4g-E2E-3 | LEAD (headless) | PASS on 3e687b2 — CONNECTED → Stop → STOPPED → Start → CONNECTED again (restart cycle now covered in preview); probe gated. |
| P4g-REV3 | codex (bounded) | PASS on 3e687b2 — latch + stale-stopped FIXED, zero new findings. |
- P4g CLOSED (2026-08-27) at 3e687b2: pedal PID 0x49 + relabel, G rows in monitor, adapter-switch lifecycle fix (unified teardown, fingerprint incl. simulate, discovery abort on settings change, latch fix), hidden developer mode for DID probe/sweep in release; 3 Codex rounds (5+5+2 findings → last clean), 3 E2E runs incl. Stop→Start cycle; 1396 tests. BUILD 2 dispatched for the second driveway test (user's standing "da build sa testam"; install = 1 slot, user's decision).
- Build 2: run 33094567866 SUCCESS on e0d82d9 (release only). IPA forensics PASS: bundle id app.circuittimer.tmr; markers motorpark-romania=1, HSFZ=10, enet=23, "Find adapter"=1, runDidSweep=1, "DID sweep"=2/"DID probe"=8 (routes now shipped in release behind developer mode, as designed), UdsServiceNotAllowed=1, ODbL=1, accelPedalPct=1, "Accelerator pedal"=1, "Throttle plate"=1, "Developer mode on"=1; TcpSocket/ExpoNetwork native symbols present; 13.95 MB. Delivered builds/ipa/TRACE-ENET-v2-pedal-devmode-release-2026-08-27.ipa with the driveway test-2 protocol (pedal 0 %→rise, G rows, ELM↔ENET switch without restart, developer mode → DID sweep). |

## Phase 4h — field revision 2 (opened 2026-08-27 after driveway test 2, build e0d82d9)
FIELD RESULT 2 (19:53–19:55): ENET CONNECTED again (phone 192.168.4.9, 4.8–4.9 Hz, ACK 6/9–11 ms); pedal 0x49 vs plate 0x11 are distinct (idle 15/14 %, revving 35–39/20 %) but 0x49 has a ~15 % rest offset; G rows absent in the monitor (accelerometer provider only runs in a driving session); calibration started 61 km from MotorPark (OFF TRACK, offset 61154 m, 0 %) with the Cancel button below the fold and no header back → user felt stuck. Contracts "Field revision 2" appended (distance guard, calibration escape, pedal 0x5A + 0x49 normalized fallback, G provider with monitor). P4h ticket DISPATCHED to the T2 seat.
| P4h | sonnet (T2 seat) | DONE — circuitProximity (haversine to S/F midpoint, >3 km warn with Back / Continue anyway), calibrationEscape (sticky Cancel footer + header back with inline confirm), pedal 0x5A primary + 0x49 rest-offset-normalized fallback (ELM 8 s grace window; ENET unsupportedChannels; diagnostics pedalSource; core mutable accelPedalPidSource — to be reviewed), G provider started with the monitor; +~40 tests. Concerns: no provider-level ENET fallback test; ELM fallback is a timeout heuristic. LEAD gates: typecheck 0, test 0 (852 + 583 = 1435), lint 0, expo export ios 0. Commit 0009720. |
| P4h-REV1 | codex (high) | DISPATCHED bg on 0009720 (incl. the mutable-global pedal source smell, guard watcher vs shared GNSS provider, escape loops, fallback↔latch interplay) |
| P4h-E2E | LEAD (headless) | RUNNING on 0009720 — MotorPark flow + ENET monitor with Stop→Start |

## Phase 4i — sweep results usable (opened 2026-08-27 after sweep test 1)
FIELD: user ran the sweep to 0x53F5 (21494/65536) at 15.8 req/s on the real adapter — 608 responders, NRC 20744, timeouts 142; responders include huge blobs (0x4097 200+ bytes, 0x4098 ASCII software IDs, 0x4113 tables); 58 screenshots could not convey the data; progress not persisted. Contracts addendum appended (persistence + resume, JSON export via share sheet, candidate filtering, range presets). P4i ticket DISPATCHED to the T2 seat. Haiku transcribing the screenshots into field3/responders.csv.
P4h-REV1 (Codex, read-only, high): 6 HIGH (back-confirm loop, distance guard bypass + duplicate GNSS watcher, global pedal PID state, Stop-during-fallback relaunch, G-provider ownership not ref-counted) + 4 MEDIUM (ENET fallback detection, cancel-vs-async-start race, NaN at offset 100, speed===0 learning). E2E on 0009720 GREEN (MotorPark ACCEPTED, ENET CONNECTED, Stop→Start reconnect). P4h-FIX1 DISPATCHED (Opus seat, disjoint write set from P4i). User asked to stop spending tokens on the screenshot transcription — transcription (121/608 DIDs, partial) shelved; the user will re-run with the P4i export.
P4i worker DONE (Sonnet): core didCandidates.ts + didObservationPhases.ts (+45 tests, 890 core); mobile didSweepSchema/didSweepStore (SQL + in-memory fallback, close/reopen/resume test), controller incremental persistence + resumePersistedRun + guided 4-phase observation over the filtered set, didSweepExport.ts (expo-file-system ~57.0.6 File/Paths API + expo-sharing ~57.0.16, never throws), DidSweepScreen (Resume, presets, collapsed responders, ranked CANDIDATES with per-phase CHANGED, Share results); app.json gained the expo-sharing plugin (expo install side effect). Worker gates 0 (890 core + 661 mobile). "Copy summary" omitted (no expo-clipboard dep). Awaiting P4h-FIX1 before LEAD gates.
P4h-FIX1 worker DONE_WITH_CONCERNS (Opus): all 10 findings fixed tests-first (37 red assertions pre-fix); concerns: no screen-render test harness (screen wiring pinned at module level only), contracts note added by LEAD. LEAD gates on the integrated tree: typecheck 0, test 0 (890 core + 661 mobile = 1551), lint 0, expo export iOS 0 → COMMIT 74a21e9. Dispatched: Codex P4h-REV2/P4i-REV1 (bounded re-verify + P4i review, bg) and headless E2E (MotorPark, ENET, new e2e-p4i.sh: preset/pause/resume/stop, candidates count, guided phases, CHANGED badges, Share fallback) on the existing Expo web server (pid 18576, port 8082).
P4h-REV2/P4i-REV1 (Codex gpt-5.6-sol; first two dispatches wasted: stdin-open hang, then wrong model 'gpt-5' rejected by ChatGPT account; third run had Windows-sandbox exec errors but completed via file reads). Verdict: P4h 7 FIXED / 3 PARTIAL (Start-during-fallback double launch, G refcount release without acquire on disabled/web/recovery/delete-all paths, queued calibration cancel); P4i 3 HIGH (fire-and-forget non-run-scoped flushes → kill/restart data loss & cross-write, changing-value pre-pass not integrated + phase starvation at 15.8 req/s, export omits guided series) + 5 MEDIUM (keep-alive gap at phase boundaries, throttle-only labelled BRAKE/STEERING?, Resume picks non-resumable run, unmount overwrites completed status, unknown lint rule in disable comment). Dispatched P4h-FIX2 (Opus) + P4i-FIX1 (Sonnet), disjoint write sets.
P4h-FIX2 worker DONE_WITH_CONCERNS (Opus): F1 single generation-guarded launch continuation, F2 acquire/release pairing, F3 queued-calibration cancel tokens; 6 new red→green tests; its gates red only on the concurrent P4i-FIX1 worker's in-flight files (foreign). Contracts P4h-FIX2 note added by LEAD. Awaiting P4i-FIX1.
P4i-FIX1 worker DONE (Sonnet): queued/run-scoped/transactional flushes (1 s window), changing-value pre-pass integrated, adaptive phase duration + cursor rotation, controller-owned 1.5 s keep-alive across pre-pass+phases, getGuidedSamples → export, brake/steering/throttle labels, selectResumableRun, monotone terminal status, lint comment removed. LEAD gates on the integrated tree: typecheck 0, test 0 (900 core + 692 mobile = 1592), lint 0, expo export iOS 0 → COMMIT ccb2cfc. Pre-fix E2E run (bzlily5us) invalidated by worker HMR reloads (stopped). Dispatched: Codex P4h-REV3/P4i-REV2 (bounded re-verify of the 11 REV2 findings, stdin closed, gpt-5.6-sol) + clean E2E (MotorPark, ENET, P4i) on ccb2cfc.
P4h-REV3/P4i-REV2 (Codex): 10 FIXED / 1 PARTIAL (terminal flush not awaited; ≤1 s re-send bound on hard kill) / 0 NOT FIXED; 0 new HIGH; 5 new MEDIUM in the sweep fix diff (guided samples leak across runs, double-count on queued flush, orphan responder rows, keep-alive unhandled rejection, pre-pass countdown frozen at 2 s). P4i-FIX2 ticket written; dispatch held until the clean E2E run finishes (worker edits would HMR-reload it).
LEAD FAILURE (2026-08-28 00:10): the clean E2E run (bozk398cb) hung ~30 min at the ENET stage (agent-browser session stuck) — stopped; P4i-FIX2 dispatched without waiting. Also found that agent-browser wrote screenshots into the repo root (daemon cwd) and `git add -A` committed 10 PNGs in 74a21e9/ccb2cfc — removed in a follow-up commit + .gitignore guard; history not rewritten (not pushed, small files).
LEAD FAILURE 2 (00:20): the h2/h3 MotorPark E2E stages were invoked with the wrong argument order (`e2e-motorpark.sh <scenario> <out-dir>`; LEAD passed only the dir) → scenario never ran, screenshots went to cwd (repo root). Not a code regression. Correct invocation to be re-run on the post-FIX2 tree. Stray-PNG cleanup committed as 072430e.
USER CLARIFICATION (00:30): the end goal covers BOTH circuits (Transilvania Motor Ring + MotorPark), not MotorPark only → recorded in docs/roadmap/phase-5-llm-corner-analysis.md (scope clarification) and memory.
USER PRODUCT CONSTRAINT (00:40): the app must not be Supra-only — target is an AI circuit trainer for beginners on any car. Recorded: roadmap "Product constraint" + contracts "Vehicle-agnostic telemetry addendum" (binding) + memory.
P4i-FIX2 worker DONE (Sonnet): R1–R6 fixed tests-first (awaited terminal flush + "Saving…", run-keyed guided samples, snapshot-time index with rollback, run-existence check in transaction, keep-alive failure → visible error, real pre-pass countdown); vitest/rollup parser quirk when importing expo-file-system transitively → export-touching tests live in didSweepExport.test.ts. LEAD gates: typecheck 0, test 0 (904 core + 704 mobile = 1608), lint 0, expo export iOS 0 → COMMIT 9b4d643 (also carries the vehicle-agnostic addendum + Phase 5 scope). Dispatched Codex P4i-REV3 (bounded, medium) ∥ full E2E via e2e-all.sh (correct args, 45 s per-command timeouts).
USER REQUEST (01:05): start the LLM lap analysis in parallel; brake/lift advice must be SAFE (never rely on our brake data to send the driver into the barrier). LEAD wrote the binding "Phase 5 — LLM corner coaching: safety contract" (deterministic metrics, demonstrated envelope from clean laps, validator bounds 10 m / 3 km/h / 1 change per corner, post-session only, honesty gates, opt-in) and ticket P5a (core-only, disjoint from P4i files) → DISPATCHED (Opus).
USER DECISION (01:15): no API keys in the app → LLM via our own backend; client-side validator mandatory. Recorded in the roadmap; backend is a separate future ticket (P5c).
USER DIRECTION (01:20–01:30, after LEAD's structured questions): audience = user first then public; written per-corner report; own clean laps as the only reference; RO+EN setting; V1 observations only; priorities = time loss per corner, consistency, brake/lift points, min/exit speed; session-only; and the key pivot: NOT an LLM — a deterministic on-device analysis engine (no backend, no API keys). P5a worker (LLM framing) STOPPED before writing code; contracts "Phase 5 REVISION" + roadmap REVISION written; ticket P5a rewritten (engine + RO/EN report). Dispatch next.
TOOLING (01:50): agent-browser wedged after the first E2E stage (two daemons 32828/32844, `open` never returning even with isolated AGENT_BROWSER_SESSION) — killed both daemons (taskkill); E2E for ENET + sweep to be re-run after P4i-FIX3 lands. LEAD wrote docs/architecture/analysis-engine.md (distance-domain alignment, delta curve, channel matrix tiers 0/1/2, per-corner metrics incl. steering/friction circle, clean-lap rules, insights, RO/EN report, tests) and sent the P5a worker an alignment message. User went to sleep at ~01:45: "ocupă-te și de algoritm" — autonomous until morning.
TOOLING (02:15): agent-browser cannot launch at all any more (even `open` without URL hangs after killing orphaned Chrome-for-Testing processes and clearing stale default.pid/.port) — abandoned for tonight (~35 min lost); ENET + sweep E2E will run through the Claude-in-Chrome extension on the user's Chrome. MotorPark E2E on 9b4d643 is valid (ACCEPTED, laps, history).
P5a worker DONE (Opus, 34 min): packages/core/src/coaching/{types,distanceDomain,cornerMetrics,cleanLap,envelope,sessionInsights,reportText,index}.ts + tests (104 coaching tests; core 1008). Concerns: 3 suites pre-existed from the stopped first attempt (adopted as spec), yaw-spike rule tightened (≥2 intervals + implausible lateral g; bare 150 deg/s flagged every fixture lap from centreline-vertex jitter), corner-window gap 2000 ms, tier-1/2 estimators synthetic-only (no fixture has OBD channels), suggestion bounds not implemented (V1 off). LEAD core gates: typecheck 0, core tests 1008, eslint coaching 0 → COMMIT 4d6f849 (core + docs only; mobile FIX3 still in flight). Codex P5a-REV1 dispatched (bg).
P4i-FIX3 worker DONE (Sonnet): X1 terminal flush failures visible (persisting/persistError, Share gated), X2 slice-aware retry, X3 wall-clock ticker. LEAD gates on the full tree: typecheck 0, test 0 (1008 core + 709 mobile = 1717), lint 0, expo export 0 → COMMIT 5fc3069. E2E via Chrome extension on 5fc3069: ENET monitor CONNECTED (4.2 Hz, pedal rel., Stop→STOPPED→Start CONNECTED), sweep preset 0x1000–0x1FFF run/pause/resume/stop OK (167.9 req/s simulated; 0 responders in that range — simulated DIDs live at 0x1E1C/0x1E20/0x1E24); G rows absent on web (device sensors unavailable on web — expected). Codex P5a-REV1: 8 HIGH + 6 MEDIUM (grid bypass, S/F wrap arithmetic + split corner, fixed windows, unavailable checks → clean, yaw rule, 2-lap gap, throttle-on ordering; monotonicity, t(s) integration, sustained-300ms, sector comparison, negative delta wording, NaN metadata, consistency basis) → ticket P5a-FIX1 written; dispatch after the sweep E2E finishes (HMR).
E2E (Chrome extension, 5fc3069) sweep: range 0x1E00–0x1E3F → 64/64 COMPLETE, 3 responders, 3 candidates, "Resume from 0x1A69" offered for the earlier stopped run (persistence OK), guided observation pre-pass + baseline/brake/steering/throttle with per-phase CHANGED (0x1E20, 0x1E24 changed; 0x1E1C static), Share → "Export ready (8390 bytes)" web fallback. Cosmetic: status label "SWEEPCOMPLETE"/"OBSERVATIONCOMPLETE" lacks a space (polish later). Verification complete → pushed main, triggered "Build unsigned iOS" variant=release (build 3); Codex P4i-REV4 (bounded on FIX3) running in parallel — ipa is delivered only if it finds no HIGH. P5a-FIX1 dispatched (Opus).
Codex P4i-REV4 (bounded on FIX3): X3 FIXED, X1/X2 PARTIAL (second-order: resume-during-pending-pause-flush lets `persisting` clear early; two consecutive failed slices → only the last retried), 0 NEW → build 3 (5fc3069) proceeds with these documented as known residuals; P4i-FIX4 ticket written + dispatched (Sonnet) for build 4.
BUILD 3 DELIVERED (02:12): GitHub Actions run 33124751623 on 5fc3069 (variant release) → builds/ipa/TRACE-v3-sweep-export-release-2026-08-28.ipa (14,079,822 bytes, md5 19676e5a8850827a2a3d73f9689ce7c5). Forensics: bundle id app.circuittimer.tmr, main.jsbundle 3.25 MB with markers (Start guided observation, Share results, Resume from, Save failed, BRAKE?, trace-did-sweep, 49 raw, Continue anyway, motorpark-romania, HSFZ, lifecycleLock), native ExpoFileSystem/ExpoSharing/ExpoSensors/ExpoNetwork/TcpSocket present; "Distance to circuit"/"Accelerator pedal (rel.)" not greppable as ASCII (em-dash strings stored UTF-16 in Hermes; partial markers present). Test protocol: builds/ipa/TEST-3-PROTOCOL.md. Known residuals: P4i-REV4 X1/X2 (FIX4 in flight). No install green-light issue: all playbook gates + Codex + E2E done.
P4i-FIX4 worker DONE (Sonnet): Y1 pendingFlushCount → persisting true until every outstanding flush settles; Y2 pendingRetrySlices[] (all retried once). Red→green shown via stash. LEAD mobile gates: typecheck 0, tests 711 → COMMIT (mobile paths only; core coaching under P5a-FIX1).
P5a-FIX1 worker DONE (Opus, 39 min): all 8 HIGH + 7 MEDIUM + test gaps; 42 red-first tests; coaching suite 104 → 153; concerns: 1 Hz brake-onset accuracy is intrinsic to sampling (resolution proven ≤ 2 m), YAW_IMPLIED_TOLERANCE_SAMPLES=2 needed for MotorPark's 35° OSM vertices, (mobile 709→711 is FIX4's two tests, not date drift). LEAD gates on the full tree: typecheck 0, test 0 (1057 core + 711 mobile = 1768), lint 0, expo export 0 → COMMIT 6668300. Codex P5a-REV2 (bounded, high) dispatched. Pushing main.
Codex P5a-REV2 (bounded, high): 10 FIXED / 5 PARTIAL (H2 time anchored at first sample not S/F, H5 availability by any evidence, H6 tolerance in samples + 1 Hz skip, M2 t(s) anchored to timestamps, M3 missing speed = confirmation) / 0 NOT FIXED; NEW: 0 HIGH, 1 MEDIUM (M8 steering derivative across the virtual S/F seam). P5a-FIX2 ticket written + dispatched (Opus).
P5a-FIX2 worker DONE_WITH_CONCERNS (Opus, 24 min): Z1 S/F-anchored grid + ΣΔs/v integral (validate, not scale; drift flag), Z2 coverage-fraction availability (≥ 90 %), Z3 yaw tolerance 8 m / 400 ms + 1 Hz evaluation / unavailable above 1 s, Z4 IMU brake needs speed at both endpoints, Z5 seam-aware steering derivative; +15 red-first tests (core 1072). Concern flagged by the worker and accepted by LEAD as a real defect: lap-completeness coverage counted one sample per 1 % bucket → a real 1 Hz iPhone GNSS lap would be `incomplete` → follow-up FIX2b sent to the same worker (same rule as per-check coverage; drift → TIME_INTEGRATION_DRIFT limitation; fullThrottleFraction per run).
P5a-FIX2b DONE (same Opus worker): 1 Hz-safe lap completeness (0.25 → 0.97 at 1 Hz; 150 m hole → incomplete), TIME_INTEGRATION_DRIFT limitation (RO/EN), fullThrottleFraction per run; +7 tests. LEAD gates on the full tree: typecheck 0, test 0 (1079 core + 711 mobile = 1790), lint 0, expo export 0 → COMMIT 3f0f20b, pushed. Codex P5a-REV3 (bounded) dispatched; its result goes into the next session's P5b ticket if anything is PARTIAL. Remaining known gap (worker): drift limitation only for laps whose grid was built (needs a clean reference lap).

## NIGHT SUMMARY 2026-08-28 (04:05) — for the morning
DELIVERED: build 3 ipa `builds/ipa/TRACE-v3-sweep-export-release-2026-08-28.ipa` + `TEST-3-PROTOCOL.md` (commit 26fd66e; app code 5fc3069). Verified by all gates, 4 Codex rounds on P4h/P4i, E2E MotorPark (replay, laps, history) + ENET (monitor CONNECTED, Stop/Start) + sweep (persist/resume, guided phases, CHANGED badges, export) + ipa forensics.
ANALYSIS ENGINE (Phase 5a, deterministic, on-device, no LLM): packages/core/src/coaching/** — distanceDomain (S/F-anchored 1 m grid, t(s)=ΣΔs/v, Δt), cornerMetrics (lift/brake/peak decel/min & exit speed/latG/friction circle/throttle-on/full-throttle/turn-in/steering, speed-drop-derived windows, S/F-wrap corners joined), cleanLap (clean/unverified/anomalous with coverage-fraction availability, yaw vs centreline-implied turn), envelope, sessionInsights (time loss vs best clean lap incl. 2-lap case, consistency with basis, brake/lift table, min/exit, limitations incl. unverified laps + drift), reportText RO/EN (observations only). 175 coaching tests, both circuits. Two Codex rounds closed (REV1 8H+6M, REV2 5 partial + 1 new); REV3 pending. Design: docs/architecture/analysis-engine.md; binding: contracts.md "Phase 5 REVISION".
FOR BUILD 4 (already committed on main after 5fc3069): P4i-FIX4 (persistence residuals). NEXT: P5b mobile Analysis screen (post-session report RO/EN + export) — needs the user's OK on the report look; user's driveway test 3 + sweep JSON → vehicle profile toyota-supra-b58.
Totals tonight: 9 commits after 0009720, tests 1435 → 1790, Codex rounds 7, workers 8 (3 Opus, 5 Sonnet). Waste logged: transcription (~15 min), Codex mis-dispatches (~20 min), agent-browser (~65 min), wrong E2E args (~10 min).
Codex P5a-REV3 (3rd attempt: 1st blocked by Windows sandbox exec, 2nd "argument list too long" with the diff in argv, 3rd via stdin prompt with the diff embedded): Z1/Z4/Z5/(b)/(c) FIXED; Z2/(a) PARTIAL (isolated readings still mark whole 1 % buckets → inflated coverage), Z3 PARTIAL (yaw availability by median interval); NEW MEDIUM: overview "no data" contradicts partial-coverage limitation. P5a-FIX3 ticket written + dispatched (Sonnet). LESSON: for large Codex prompts use `codex exec - < file`.
P5a-FIX3 worker DONE (Sonnet, 8 min): Q1 metre-span coverage union, Q2 per-interval yaw availability, Q3 overview wording; +5 tests. LEAD gates: typecheck 0, test 0 (1084 core + 711 mobile = 1795), lint 0 → COMMIT + push. Phase 5a engine closed for the night (3 Codex rounds; last residuals fixed, not re-verified by Codex — first item of the P5b review). Session ends ~04:50.

## 2026-08-29 — field test 3 results (user, driveway)
User sent two sweep exports (archived: data/field/sweeps/2026-08-29-supra-dme12-{0x1000-0x1FFF,0x4000-0x4FFF}.json). 0x1000–0x1FFF: 17 responders, only 0x1701 dynamic (1/s counter). 0x4000–0x4FFF: 533 responders, 128 small candidates; phases ran 17 s (adaptive duration worked) but the observed rate was ~8–11 req/s → 1–2 samples per DID per phase → brake ranking = idle noise; genuine throttle-phase signals: 0x4659 (0→4095), 0x4600, 0x4601, 0x4807, 0x472E, 0x4206; brake-shaped only 0x4536 (0/65535) and 0x4520; no steering (expected in DSC, address 0x29 — hypothesis, unverified). Persistence/Resume/export worked in the field. P4j ticket (batched observation ≥ 5 samples/DID/phase from the measured rate, focused shortlist, 9–32-byte blocks with per-byte diff, target-address display) DISPATCHED (Sonnet). User advised to try target 0x29 with the current build.
Field (user, 11:04 UTC): sweep at target 0x29 (hypothesised DSC) over 0x4000–0x4FFF → the ECU ANSWERS (20 responders, 0 timeouts, NRC 0x31 elsewhere): 0x4000 = ASCII "AG2RBFR0200" (ident), 0x4100–0x4112 = static configuration constants (1000, 2325, 2350, 2000, …); no live data in this range → live DIDs of ECU 0x29 are elsewhere; user advised to run the FULL range at 0x29 in chunks with Resume (~73 min at ~15 req/s). Archived data/field/sweeps/2026-08-29-supra-ecu29-0x4000-0x4FFF.json.
P4j worker DONE (Sonnet, 26 min): planObservationBatches (≤16/batch, ≥5 samples/DID/phase from the measured rate), margin change rule, filterCandidatePool (1–32 bytes) + per-byte block diffing, batched/focused controller flows, export batchIndex/blockCandidates/hex target, screen UI. LEAD gates: typecheck 0, test 0 (1112 core + 723 mobile = 1835), lint 0, expo export 0 → COMMIT 6c38985. Dispatched: Codex P4j-REV1 (stdin) + a cited web-research scout for BMW DSC/SZL addresses and brake/steering DIDs (LEAD will verify the cited URLs before use). USER CONSTRAINT (13:30): no more random driveway tests — fuel; tests are engine-off (ignition on) and only once with a targeted range.
Codex P4j-REV1 (stdin dispatch worked): 3 HIGH (≥5 samples not guaranteed — duration math only, NRC/timeouts starve DIDs; margin rule misses 0→1 switches and falls back to naive distinctness when undersampled; batched samples/summaries memory-only → lost on restart, reset by later observations) + 6 MEDIUM (bounds not enforced, pause unsupported mid-batch & stop resolves before release, 8/9 & 32/33-byte split evades consistency, UI shows only first changed phase, legacy flows widened to 1–32 bytes, export schema changed without version bump). P4j-FIX1 ticket written + dispatched (Opus). E2E of the batched flow interrupted by the research scout sharing the Chrome tab — to be re-run after FIX1.
RESEARCH (scout, cited; LEAD verified the key page live via WebFetch): no public DSC/SZL live DID list for F/G; addresses DSC=0x29, EPS=0x30, ICM=0x1C (packetpilot/bmw-f, beemuu); DME B58 live DIDs on thesecretingredient.neocities.org/bmw/dme/b58 (hobby table, provenance unknown): 0x4A1D brake booster sensor V (u16×5/1024), 0x5892 booster diff hPa (i16×10/256), 0x58B7 "Current brake pressure" hPa (u8), 0x4811 long accel (i8×0.217), 0x4812 lat accel (i16/640). Cross-check with our field sweep: 0x4A1D answers (0x0000), 0x4811 (0x00), 0x4812 (0xFF4A = −0.28 m/s², plausible) — but none was observed in the guided phases: the two-sample PRE-PASS dropped them as static (user likely did not press the brake during the pre-pass) → design gap: pre-pass must not exclude (batched flow already skips it). Draft vehicle profile written: data/vehicle-profiles/toyota-supra-b58.draft.json (hypotheses with provenance). Next single field test (engine OFF, ignition ON, build 4): DME sweep 0x5000–0x5FFF + focused observation on the shortlist.
P4j-FIX1 worker DONE (Opus, 35 min): H1 count-guaranteed phases, H2 set-difference + mean-shift/boolean rule with `insufficient` tri-state, H3 observation samples/summaries persisted (schema v2, two new tables) and read by export, M1–M6, plus LEAD additions (pre-pass orders only; shortlist accepts non-responders; SHORTLIST_PRESETS data constant incl. "Supra B58 brake/accel (hypothesis)"). Field shapes in tests: 297→305→295 static; 0→1 and 0/1 toggling → brakeCandidate. Concerns: miss budget counts missed round slices (core runner has no per-DID outcome), no RN render tests possible, a transient git stash (restored, verified). LEAD gates: typecheck 0, test 0 (1128 core + 762 mobile = 1890), lint 0, expo export 0 → COMMIT 82404bc. Dispatched Codex P4j-REV2 (stdin); E2E of batched/focused flows via Chrome extension next.
Codex P4j-REV2: 10 FIXED / 2 PARTIAL (insufficient DIDs still ranked; unmount cleanup fire-and-forget) / 0 NOT FIXED; NEW 0 HIGH, 3 MEDIUM (export live-vs-store precedence by length, persisted observation groups collapsed, slice tMs reset). E2E (Chrome extension, 82404bc, throttled hidden tab at 1.2 req/s): batched observation OBSERVATIONCOMPLETE with 20 samples/DID (count guarantee works), focused shortlist + Supra preset visible, Share 8657 B. P4j-FIX2 ticket written + dispatched (Sonnet) → then build 4.
P4j-FIX2 worker DONE (Sonnet): V1 insufficient rank excluded, V2 reservation whenFree/markReleasing + retry-after-release, V3/V4 export reconciled by observationId+seq and grouped per observation, V5 monotone slice tMs. LEAD gates: typecheck 0, test 0 (1130 core + 770 mobile = 1900), lint 0, expo export 0 → COMMIT b3e9ade, pushed. BUILD 4 triggered (variant release) on b3e9ade; FIX2 not Codex-re-verified (5 bounded items; documented residual practice as for build 3); E2E re-run of the sweep flow on b3e9ade in parallel.
E2E re-run on b3e9ade abandoned: the Chrome extension tab is hidden > 5 min → Chrome intensive timer throttling (sweep advances only while an eval runs; 7/64 after 40 s). Not stealing the user's window focus. Coverage for build 4: E2E of the batched/focused flow on 82404bc (parent of the FIX2 commit) + 1900 unit tests incl. FIX2's red-first tests. Residual: no E2E on the exact build-4 commit (FIX2 = export/ranking/reservation logic).
BUILD 4 DELIVERED (16:05): GitHub Actions run on b3e9ade (variant release) → builds/ipa/TRACE-v4-batched-observation-release-2026-08-29.ipa (14,120,966 bytes, md5 de2c7a870e9a9b1022896935cbb0b07a). Forensics: bundle id app.circuittimer.tmr; JS markers present (Start batched guided observation, Start focused observation, Supra B58 brake/accel, Insufficient evidence, No response, Pause after this batch, schemaVersion, targetAddressNumeric, HSFZ, lifecycleLock, motorpark-romania); native ExpoFileSystem/ExpoSharing/ExpoSensors/TcpSocket present. Protocol: builds/ipa/TEST-4-PROTOCOL.md (engine OFF, ignition ON, ~8 min: DME sweep 0x5000–0x5FFF + focused shortlist; optional 0x29 0x5000–0x6FFF batched).
builds/ gitignored: ipa + protocols are local-only artifacts (ledger records md5)

## 2026-08-29 (20:50) — field test 4 results (user, engine OFF / ignition ON, build 4)
User sent two exports (archived data/field/sweeps/2026-08-29-test4-dme12-0x5000-0x5FFF.json, …-test4-ecu29-0x5000-0x58F2.json). What was actually run: (a) DME 0x12 sweep 0x5000–0x5FFF COMPLETE in 4.5 min (899 responders, 29 timeouts, NRC 0x31 elsewhere) + BATCHED observation stopped after batch 6 of 57 (112 DIDs, 5 samples/DID/phase — the count guarantee held; 16 DIDs of the interrupted batch correctly `insufficient`); the FOCUSED shortlist (protocol step 3b) was NOT run — 0x4A1D/0x4811/0x4812 absent, 0x5892 (=0x0000) and 0x58B7 (=0x00) answer but were never observed in a brake phase. (b) ECU 0x29 sweep 0x5000–0x6FFF stopped by the user at 0x58F2 (2291 DIDs, 14.2 req/s, 32 responders) + batched observation on 30 DIDs (2 batches, 5 samples/phase).
FINDINGS: **0x29 DID 0x500C (1 byte) 0x04 → 0x05 in 3/5 brake-phase samples** (and the first steering sample, brake still held) → brake switch bit0, field-observed; **0x29 DID 0x500B (2 bytes) 0x0002 → 0x0006 once in the brake phase only** (bit2, weak — one sample); DME 0x5468 (2 bytes) 13636 → 13611 → 13587 across brake/steering/throttle = battery-voltage-like (mV, dips under load) — not a driver input; DME 0x5422 one 0x00 sample at the start of the steering phase (weak/transition). No steering angle found yet (0x29 0x58F3–0x6FFF unswept). App defect surfaced by the data: no settle time at phase transitions → 0x500C was ranked `changedInSeveral` instead of `brakeCandidate` because the first steering-phase sample still carried the brake → ticket P4k (transition settle / discard-first-window) written. Vehicle profile updated (0x29 as the brake source, 0x500C field-observed, 0x500B weak, 0x5468 hypothesis, unobserved DME pressure DIDs kept as hypotheses). Next single engine-off test written: builds/ipa/TEST-5-PROTOCOL.md (same build 4: Resume 0x29 to 0x6FFF + focused `500B 500C …`; DME focused `4A1D 5892 58B7 4811 4812 5468`).
USER FACT (21:15): with the engine off the steering wheel cannot be turned (EPS unpowered) → steering phase in engine-off tests is void; steering search = engine-off sweep of 0x29 0x58F3–0x6FFF for the responder list, then ONE ~2-min idle focused observation; otherwise steering stays tier-2 (GPS+gyro estimate) in the analysis engine.
USER FACT (21:20): engine off → no booster vacuum, brake pressure barely builds → DME 0x4A1D/0x5892 (booster sensors) void engine-off; only the switch (0x29 0x500C/0x500B) is testable engine-off. Protocol restructured: TEST-5 engine-off = Resume 0x29 to 0x6FFF + focused 500B/500C; TEST-6 = ONE ~2-min idle run for steering + pressure (0x29 candidates, DME 4A1D/5892/58B7/4811/4812).
USER REQUEST (21:35): a target-driven discovery tool ('find the brake: read the channels we think, press 5×, show candidates, found; next signal'). LEAD wrote contracts 'Signal Finder (Phase 4l)' + ticket P4l (Opus); dispatch after P4k lands (shared files).
USER REQUIREMENT (21:40): reports must be easily exportable — one-tap Share, JSON + ≤1-page human summary (.md), for the Signal Finder (P4l S5) and the analysis report (P5b). Recorded in contracts (Signal Finder §8).
P4k worker DONE (Sonnet, 13 min): SETTLE_MS=1500 + isSettlingSample in core (settleMs option on both summarizers, evidence only), controller wires settle into guided/batched/focused + count guarantee counts non-settling samples only (incidental fix: responses inside the settle window no longer counted as misses), screen shows 'Get ready…'. Field-shape tests: 0x500C → brakeCandidate with settle (changedInSeveral without). NOTE: repo is npm workspaces — gates are 'npm run …', not 'pnpm -w'. LEAD gates on the full tree: typecheck 0, test 0 (1139 core + 771 mobile = 1910), lint 0 (6 pre-existing warnings), expo export iOS 0 → COMMIT. Dispatching P4l Signal Finder (Opus).
Codex P4k-REV1 (medium, read-only; sandbox exec errors but file reads worked): 0 HIGH, 1 MEDIUM (final slice can overrun the 3× cap; settling can leave DIDs short at the deadline), 1 LOW (0x5422 fixture all-zero → vacuous). Settling exclusion confirmed wired in numeric + block summaries across guided/batched/focused live+final; tri-state/monotone tMs/export/persistence intact; 0x500C fixture faithful. Ticket P4k-FIX1 written + dispatched (Sonnet) in parallel with P4l (disjoint write sets).
P4k-FIX1 worker DONE (Sonnet, 14 min): K1 slice clamped to the remaining hard cap (red: 20085 ms > 18000 ms cap → green 18075 ms), K2 0x5422 fixture now exercises the settle exclusion. Worker gates: test 0 (1167 core + 785 mobile incl. P4l WIP tests), lint 0, expo export 0; typecheck 2 ONLY from the concurrent P4l WIP files. LEAD: committing the FIX1 files alone as a checkpoint; full-tree gates re-run when P4l lands (before Codex/E2E/build).
P4l worker DONE_WITH_CONCERNS (Opus, 33 min): core signalFinder {targets,metronome,scoring}, mobile signalFinderController (per-ECU passes over hypotheses + cached responders, reuses raw UDS channel + runDidObservation + 'sweep' reservation), export JSON v1 + .md summary RO/EN, SignalFinderScreen (dev-gated route), schema v3 vehicle_profile_bindings + store, provider binding lookup. Concerns: (1) brake channel NOT emitted yet — TelemetryChannelId union + LapDetailScreen Record outside the write set → P4l-FIX1; (2) settle at step edges implemented as a window SHIFT (evidence windows start settleMs later) instead of P4k's discard, so 1 Hz / 2 s windows stay sufficient — ACCEPTED by LEAD (documented); (3) expo-haptics absent → no-op hook, no new native deps; (4) no language/vehicleProfileId setting yet (summary EN on screen, session-local profile picker) → P5b/FIX1; (5) reservation owner 'sweep' reused. LEAD gates on the full tree: typecheck 0, test 0 (1167 core + 824 mobile = 1991), lint 0 (6 pre-existing warnings), expo export 0 → COMMIT. Next: P4l-FIX1 (brake end-to-end) ∥ Codex P4l-REV1.
Codex P4l-REV1 (high, 175k tokens): 4 HIGH (H1 block-level baseline changes ignored in verdict; H2 analog-bipolar not enforced; H3 bindings never reach live telemetry = FIX1 F1 already in flight; H4 binding decoder reads one byte only) + 6 MEDIUM (M5 cached responders beyond 16 dropped instead of extra passes; M6 sufficiency gate collapses press/hold; M7 on-screen summary from stale snapshot; M8 .md summary not bounded; M9 persisted bindings not exportable as canonical profile JSON; M10 edge matching = presence not ordered transitions). Verified OK: migration v2→v3, confirm overwrite, ECU order, NRC→no response, reservation lifecycle, JSON schema, next-step, double-run guard. Split: FIX2 (core scoring: H1 H2 H4-scoring M6 M10, Opus, disjoint files) dispatched now ∥ FIX1 gets H4-decoder via message; FIX3 (M5 M7 M8 M9) after FIX1 lands.
P4l-FIX2 worker DONE (Opus, 10 min): scoring.ts — per-DID baseline gate (noise-floor aware; raw hex for boolean-edge), bipolar two-sidedness (one-sided → capped with reason), per-step sufficiency gate, ordered transitions with extra-transition subtraction (netEdges = matched − extra; arriving-sample rule keeps field 0x500C found), block min/max of the selected offset; +8 red-first tests (signalFinder 36). Worker gates: typecheck 0, test 0 (1190 core + 840 mobile incl. FIX1 WIP), lint 0. LEAD: checkpoint commit of the two FIX2 files; full-tree gates after FIX1.
P4l-FIX1 worker DONE_WITH_CONCERNS (Opus, 21 min): brakeSwitch/brakePct in TelemetryChannelId + ENET_SPEC_CHANNELS (decodeValue fn decoder, null → channel error), H4 decoder = big-endian scalar 1–4 bytes / offset for blocks, provider appends the confirmed binding as a real DID poll entry addressed to its ECU on the SAME session (EnetChannelSpec.targetAddress overrides per request; one socket, one reservation), recording additive (channel is free TEXT, pinned by test), coaching detectBrake order brakePct → brakeSwitch → longG → gpsSpeed (RO/EN labels), language setting (locale default at hydration, Settings row, summary uses it), reservation owner 'signalFinder'. Wire trace: 0x29 0x500C 04→05 and 0x500B 0002→0006 → brakeSwitch 0→100. Concern: composition.ts not wired (dep defaults to []) → FIX3 J5. LEAD gates on the full tree: typecheck 0, test 0 (1190 core + 840 mobile = 2030), lint 0, expo export 0 → COMMIT + push. Dispatching P4l-FIX3 (Sonnet: M5 M7 M8 M9 + J5).
TOOLING WASTE (22:10): Codex P4l-REV2 log polluted — the run echoed the REV1 transcript (prompt referenced the REV1 log file) and its own git show calls were blocked by the Windows sandbox; no verdict produced (~15 min). Re-dispatched as REV2b with a self-contained prompt (findings inline, 'read files directly if exec is blocked'). LESSON: never reference a previous Codex log in a prompt; inline the findings.
Codex P4l-REV2b (high, files read directly — sandbox blocked exec): F4/F6/F10 FIXED; F1 PARTIAL (analog blocks with a +1 counter escape the DID cap), F2 PARTIAL (one-sided bipolar still 'probable'), F3 NOT FIXED (composition.ts not wired — FIX3 J5 in flight); NEW 2 HIGH (brakeSwitch linearly interpolated → fabricated early onset up to ~1 s; live payload length not checked vs binding → fabricated 0/100), 2 MEDIUM (silent 0x29 stalls DME polling 1500 ms per request at 5 Hz; persistent null decode feeds the global error budget → session dies after 5), 2 LOW (locale default masked when language absent; UI/export show gross matchedEdges not netEdges). Scenario (a) every-sample toggler: no false verdict; multi-ECU source matching + NRC attribution correct. Ticket P4l-FIX4 (Opus) dispatched on disjoint files; L12 sent to the FIX3 worker as an addendum.
P4l-FIX4 worker DONE_WITH_CONCERNS (Opus, 19 min): N1 DID restlessness on every shape (other bytes exact; scored byte noise floor), N2 one-sided bipolar → unrelated, N3 brakeSwitch zero-order hold + brakeOnsetUncertaintyM (±X m in report; envelope latest-brake bound uses the pessimistic edge), N4 payload length vs binding length, N5 per-entry timeout 300 ms + back-off after 3 misses (×4, floor 0.25 Hz, immediate recovery; DME ≥ 4 Hz with a silent 0x29 — pre-fix stalled), N6 per-channel decode budget (degraded after 5; diagnostics degradedChannels/channelWarnings/effectiveHzByChannel), N7 locale default only when language absent. Worker gates: typecheck 0, test 0 (1206 core + 872 mobile = 2078), lint 0, expo export 0. Concerns: block DIDs with live neighbour bytes now false-negative (accepted for build 5; counter-detector later), monitor screen doesn't show degradedChannels yet. LEAD: checkpoint commit of FIX4 files; full gates after FIX3.
P4l-FIX3 worker DONE (Sonnet, 29 min): J1 ≤16-DID passes over the full ECU list (40 → [16,16,8]), J2 document from controller.getSnapshot(), J3 .md budget ≤60 lines/≤4096 chars with (+N more), J4 buildVehicleProfileDocument + Share profile (kind trace-vehicle-profile v1), J5 composition wires readVehicleProfileBindings (cache refreshed on bootstrap, provider start, confirm), J6 netEdges/extras/cap reason shown + exported. LEAD gates on the full tree (FIX3+FIX4): typecheck 0, test 0 (1206 core + 872 mobile = 2078), lint 0 (6 pre-existing warnings), expo export 0 → COMMIT + push; BUILD 5 triggered in parallel with the web E2E of the Signal Finder flow (delivered only if E2E passes). Residual: FIX3/FIX4 not Codex-re-verified (documented practice as for builds 3/4); block DIDs with live neighbour bytes → false negative (FIX4 N1 rule).
LEAD data fix (22:55, from the web E2E): Signal Finder targets showed 'Brake pressure' and 'Steering angle' as engine-off testable — contradicts the user's field facts (no booster vacuum, EPS unpowered) → targets.ts engineRequirement 'running' for both (comments cite the facts); discovery SWEEP steps are always engine-off (no driver action) so the next-step text stays honest; regression tests added. Gates: typecheck 0, test 0 (1206 + 872 = 2078), lint 0, expo export 0 → COMMIT + push; build 5 re-triggered (the first build-5 run 33271336527 on 3b1f185 succeeded but carried the wrong labels → discarded). E2E (Chrome extension, web, simulated ENET): Settings → LANGUAGE row present; Version×7 → Dev: Signal Finder (ENET) → profile chips, 6 targets with engine requirement + hypothesis counts; Find Brake switch (Supra) → read 2 DIDs on 0x29, 'Nothing answered', 'No response: 0x500C (0x29), 0x500B (0x29)', 'Next step: sweep 0x29 0x58F3–0x6FFF, ≈ 7 min, engine off', summary rendered ('Nothing conclusive yet — engine off'), SHARE / SHARE JSON buttons present, Share profile disabled until a confirm. Extension blocked click scripts on this screen ('Cookie/query string data') → clicks via a11y refs.
E2E residual (23:00): the web preview keeps in-memory settings; a Metro HMR/full reload reset 'simulate' to off, so the second Find used EnetTcpTransport on web → banner 'Cannot read properties of undefined (reading connect)' (web has no TCP sockets — not a Signal Finder defect; the same transport runs the sweep on device since build 3). Re-running the flow after reload was blocked by extension script blocks/renderer stalls (~25 min) → web E2E stopped; unverified on web: Share tap (export/share paths are unit-tested: signalFinderExport 29 tests) and a second consecutive Find with the simulator. Labels fix verified on screen after HMR (Brake pressure / Steering angle = engine running / moving).
BUILD 5 DELIVERED (23:05): GitHub Actions run 33271729440 on 2fdcac5 (variant release) → builds/ipa/TRACE-v5-signal-finder-release-2026-08-29.ipa (14,167,114 bytes, md5 bc067bec7719053f1d2cc062c2c65231). Forensics: bundle id app.circuittimer.tmr, main.jsbundle 3.44 MB; markers present: Signal Finder ×6, Confirm as, Share the vehicle profile, trace-signal-finder, trace-vehicle-profile, vehicle_profile_bindings ×7, brakeSwitch, netEdges, verdictCapReason, engine running / moving, Next step, LANGUAGE, brakeOnsetUncertaintyM, degradedChannels; plus the build-3/4 markers (HSFZ, DID sweep, lifecycleLock, both circuits). ('Find Brake switch'/'Get ready…' are composed/UTF-16 strings — not greppable, expected.) Protocol: builds/ipa/TEST-5-PROTOCOL.md (engine OFF: Signal Finder → Brake switch → FIND → Confirm → Share ×3). Content vs build 4: P4k(+FIX1) settle window, P4l Signal Finder (+FIX1..FIX4), brake channel end-to-end, language RO/EN, engine-requirement data fix. Tests 1900 → 2078. Residuals: FIX3/FIX4/data-fix not Codex-re-verified; web E2E partial (see above); block DIDs with live neighbour bytes → false negative; monitor screen does not show degradedChannels yet.

## 2026-08-29 (23:20) — field test 5 (build 5, Signal Finder) — user feedback + data
USER FEEDBACK: "inhuman to press the brake/accelerator that many times — the tests are robotic". Exports archived: data/field/signal-finder/2026-08-29-{brakeSwitch,accelPedal}.{json,md}.
WHAT HAPPENED (from the JSON): the finder built 91 passes (87 x <=16 cached 0x12 responders from the old sweeps + 4 on 0x29) and ran ONE FULL 24 s METRONOME PER PASS -> the user did the 5-press script 4x for brake (97 s) and ~3x for accel before stopping; the hypothesis passes (0x29 0x500C/0x500B, 0x12 0x4659) were queued LAST -> never reached -> "0/10" everywhere on the Supra profile. The 1372 never-read DIDs were reported as "No response (NRC or silence)" — an honesty bug. With 16 DIDs/pass at ~15 req/s each 2.1 s window got 1–2 samples -> everything insufficient (undersampled). LEAD FAILURE: the P4l spec allowed "passes" and the E2E on the simulator could not reveal it (no cached responders on web).
REAL SIGNALS IN THE DATA ANYWAY: DME 0x12 DID 0x4002 (1 byte) = 0x01 at rest -> 0x19 in EVERY press window of the brake run, 0x01 in every release window, 0 baseline changes, 0 extras (10/10) and flat 0x01 throughout the accelerator run -> BRAKE PEDAL in the DME (field-observed, no DSC needed). DME 0x4007 (2 bytes) 0x9001 -> 0x9000 while the accelerator is pressed (bit0 clears; flat in the brake run) -> accelerator idle flag (field-observed). 0x4659 = 0x27FF constant with the engine off (its 0->4095 in test 3 was engine-running throttle). 0x1701 uptime counter correctly rejected.
DECISIONS: (1) Signal Finder v2 = ONE human-paced metronome per FIND (3 reps x press 3 s / release 3 s + baseline 3 s ~ 21 s), DID budget per run from the measured rate (>= 3 samples per window), hypotheses + previously-changing DIDs FIRST across ECUs in one session (per-entry target address), extra rounds only on explicit tap, unread DIDs reported as "not read", sparse-but-consistent evidence is found. Contract "Signal Finder REVISION" items 9–13; ticket P4m (Opus) with both exports as regression fixtures. (2) Vehicle profile: 0x4002 brake pedal + 0x4007 accel idle flag added as field-observed (DME, so no second ECU needed).
P4m worker DONE (Opus, 30 min): plan.ts (budget = floor(rate*3/3) clamped [4,12]; pools hypotheses → changed-before → cached; notRead), one script per find (3 reps x 3 s, baseline 3 s, 21 s), one transport for all ECUs (createMultiEcuUdsChannel: one raw channel per ECU on the same socket), nextRound(), sparse-found + single-bit flag rule + never-moved → unrelated, export schemaVersion 2 (rounds/budget/notRead), RO/EN prompts + verbs, Next round button; field fixtures: 0x4002 found (sparse), 0x4007 found (sparse, flagBit 0), 0x1701/0x4659 unrelated. LEAD gates on the full tree: typecheck 0, test 0 (1234 core + 879 mobile = 2113), lint 0, expo export 0 → COMMIT + push; BUILD 6 triggered; Codex P4m-REV1 (self-contained prompt) in parallel. Concerns: one-bit-difference switch rule under analog-monotone (judgement call, documented), multi-ECU single socket proven only on the HSFZ double — field risk for test 6.
Codex P4m-REV1 (high): 3 HIGH (rate assumed 15/s and exported as measured; serial polling lets a silent 0x29 starve 0x12 within the fixed budget; partial/stopped runs count planned DIDs as read → noResponse) + 6 MEDIUM (same DID on two ECUs split across rounds; pools not filtered to the target's ECUs; finder outcomes not persisted; one-bit flag + sparse can promote LSB noise; never-moved with thin coverage; RO mode still shows English evidence/banners/a11y/export markers) + 1 LOW (zero-DID find runs no script). Build 6 (ipa ec988d1d…, run 33274665142) NOT delivered. Ticket P4m-FIX1 (Opus) dispatched; M6 deferred.
USER RULE (00:20, binding): NO ipa build and NO ipa delivery without the FINAL verifications (all gates + Codex round with 0 HIGH + E2E). Builds must not be triggered 'in parallel' with the review to save time — build 6 (triggered before Codex P4m-REV1 finished, which then found 3 HIGH) was exactly this mistake; its ipa was removed from builds/ipa. Order from now on: gates → Codex → fixes → gates → E2E → build → forensics → deliver.
P4m-FIX1 worker DONE_WITH_CONCERNS (Opus, 35 min): runner.ts (composite (ecu,did) round runner, 2 s probe → measured rate + silent ECUs, 300 ms per-DID timeout, back-off after 3 misses, attempted keys), plan by (ecu,did) with silent refill, flag exception only via catalog declaration or ≥2 agreeing samples per window, never-moved needs complete coverage (avg-≥2 clause deliberately dropped — would re-insufficient the field DIDs; 0x4659 now insufficient → contracts item 13 amended by LEAD), pools filtered to target ECUs, Find disabled at 0 DIDs, export schema 3 (rateSource, silent), signalFinderStrings.ts RO/EN complete. LEAD gates on the full tree: typecheck 0, test 0 (1251 core + 892 mobile = 2143), lint 0, expo export 0 → COMMIT + push. Per the user rule: Codex P4m-REV2 next, NO build until 0 HIGH + E2E.
E2E (Chrome extension, web, simulated ENET, tree = P4m-FIX1 commit): Settings → telemetry ENET + simulate → Dev: Signal Finder → Supra profile (Brake switch: 3 hypotheses; Accelerator: 2; pressure/steering 'engine running / moving') → Find Brake switch → probe → ONE script 'Hold still — foot OFF the brake' 1.7s 'Step 1/7 · 0x12' → RESULT 'Read 1 DID across 1 ECU in 1 round (0x12) · engine off · rate 1000.0/s measured' / 'No response: 0x4002 (0x12)' (simulator NRCs it — correct) / 'Not read: 2 — ECU 0x29 silent' (probe dropped the silent ECU — X2 works) / next step with minutes from the measured rate / summary v3 with the same lines; no crash, no per-pass loop. Earlier 'reading connect' banner = web with simulate OFF (settings reset on reload), reproduced deterministically then avoided; renderer stalls were caused by my long eval scripts (≥45 s), not by the app. LOW noted: the finder runner polls at the maximum rate (no pacing to the planned Hz) — harmless on hardware (bounded by ack latency), hot on the in-memory simulator.
Codex P4m-REV2 (high, static — sandbox blocked exec): 1–5, 7, 8, 10 FIXED; 9 PARTIAL (English target/profile labels, 'more' marker, raw errors in RO); NEW 2 HIGH (probe deadline leaves later ECUs unattempted yet classifies them silent; measured rate excludes timeout cost and per-DID silence inside a live ECU can burn ~10 s before back-off) + 4 MEDIUM (round-long back-off empties later windows; attempted recorded before send success; stop() does not await pool collection; unbounded close() blocks stop/release). Per the user rule: NO build. Ticket P4m-FIX2 (Opus) dispatched (+ LOW pacing from the E2E).
P4m-FIX2 worker DONE (Opus, 19 min): probe = complete pass over every (ecu,did) (300 ms each), silent only when attempted; rate includes timeout cost (requests/elapsed); per-DID silent → notRead unless hypothesis (one retry); windowed back-off cooldown (one retry per window); attempted only after send resolved; run registered before pool collection (stop honoured); close bounded 2 s + destroy fallback + release in nested finally; pacing to the planned rate; RO: localized title/labels/errors/'more'. Gates on the full tree: typecheck 0, test 0 (1259 core + 901 mobile = 2160), lint 0, expo export 0 → COMMIT + push. Codex P4m-REV3 (bounded) next; build only after 0 HIGH + E2E.
E2E (web, simulator, FIX2 tree): Find Brake switch (Supra) → probe → one 7-step script → RESULT 'Read 1 DID across 1 ECU in 1 round (0x12) · engine off · rate 1.8/s measured', 'No response: 0x4002 (0x12)' (sim NRC), 'Not read: 2 — ECU 0x29 silent' — flow OK. DEFECT FOUND BY LEAD: the timeout-inclusive probe rate is dominated by the silent entries (2 × 300 ms vs one instant answer → 1.8/s) although those entries are DROPPED from the round → the budget (floor(rate×3/3), min 4) and the next-step sweep estimate ('≈ 56 min' for 6413 DIDs) are computed from a rate the retained DIDs will never see; on hardware (0x12 ~15/s, 0x29 silent) this shrinks the budget to the 4-DID floor for no reason. Fix (P4m-FIX3): budget/estimate rate = rate of the RETAINED (answering) entries (answers / their own elapsed time), timeout cost kept only for entries that stay in the round. Renderer stalls again correlated with my long eval scripts, not the app.
Codex P4m-REV3 (high, static): 11 FIXED, 14/15/16 FIXED; 12 PARTIAL (probe-silent hypotheses retained), 13 PARTIAL (one retry per window), 9 PARTIAL (raw English errors in RO banners); NEW 2 HIGH (cooldown resets misses to threshold−1 → first miss re-cools the DID for the whole window; per-entry pacing drifts after slow exchanges → live entries can drop below 3 samples/window) + 3 MEDIUM (silent hypothesis retried every window; complete-pass probe stalls up to 3.6 s with no UI; timeout fallback may release the reservation while close is still pending). Plus LEAD E2E: budget/estimate rate dominated by dropped silent entries. Ticket P4m-FIX3 (Opus) dispatched — simpler mechanisms mandated. NO build.
P4m-FIX3 worker DONE (Opus, 30 min): Z1 retained-entry rate (diagnostic timeout-inclusive kept), Z2 per-window miss allowance, Z3 pass-level pacing, Z4 one explicit retry then drop, Z5 probing phase + progress on screen, Z6 teardown 2 s soft / 5 s hard + adapterTeardownPending refuses the next Find, Z7 raw errors only in export diagnostics; export schema 4; silentEcus = wholly-silent ECUs only. LEAD: contracts item 13 bumped to schema 4. Gates on the full tree: typecheck 0, test 0 (1264 core + 911 mobile = 2175), lint 0, expo export 0 → COMMIT + push. Codex P4m-REV4 (bounded) next; then E2E; build only after 0 HIGH.
E2E (web, simulator, FIX3 tree): Find Brake switch (Supra) → 'Probing the ECUs… 1/3 (up to 1 s)' → one 7-step script (RELEASE the brake, Step 5/7 · 0x12) → RESULT 'Read 1 DID across 1 ECU in 1 round (0x12) · rate 500.0/s measured' (retained-entry rate, no longer dragged by the dropped silent entries), 'No response: 0x4002 (0x12)' (sim NRC), 'Not read: 2 — ECU 0x29 silent', next step ≈ 1 min from the live rate; summary v4 identical. No crash, no stalls attributable to the app. Waiting for Codex P4m-REV4 before any build.
Codex P4m-REV4 (high, static): NEW 2 HIGH (re-plan after the explicit retry still uses the pre-retry rate; hypotheses on WHOLLY silent ECUs never get the promised retry — silentDids only lists misses on live ECUs) + 2 MEDIUM (probe bound shown is not an upper bound: send timeout + 0x78 extensions, and progress stops during retries; schema-4 export copies the raw error text unredacted) + 1 LOW (late close settlement leaves the stale 'still shutting down' error). Ticket P4m-FIX4 (Opus) dispatched. NO build.
P4m-FIX4 worker DONE (Opus, 13 min): retry candidates = attempted-but-unanswered hypotheses (incl. wholly-silent ECUs / zero-answer), probe+retry merged then summarised once (rate/silent decided after the retry), honest probe bound entries×timeout×(2+extensions) with progress through the retry, export diagnostics redaction (URLs/paths/IPs/host:port/tokens → ‹redacted›, 120 chars), late close clears error+phase; contracts item 14 (teardown residual) added. LEAD gates on the full tree: typecheck 0, test 0 (1267 core + 919 mobile = 2186), lint 0, expo export 0 → COMMIT + push. Codex P4m-REV5 (bounded) + E2E next; build only after 0 HIGH.
