# Ticket P5a-FIX2 — Codex P5a-REV2 partials (H2, H5, H6, M2, M3) + NEW M8, tests first, bounded, packages/core ONLY
Source: `.foreman/scratch/p5arev2-codex-output.log` (final "codex" section). Spec docs/architecture/analysis-engine.md; binding contracts.md "Phase 5 REVISION". HEAD 6668300.
Z1 (H2+M2) Time-at-distance must be anchored at S/F, not at each lap's first recorded sample: define t(0)=0 at the start/finish crossing (interpolate the crossing time from the two samples around s=0 / lap boundary), and build t(s) by integrating ΣΔs/v(s) on the grid (v from smoothed Doppler, Δs/Δt fallback), with the measured timestamps used only to validate/scale total lap time (report a `timeIntegrationDriftMs` quality flag when |∫ − measured| > 2 %). Wrap segments then use lap-end/lap-start terms correctly. Tests: reference starts at s=0, slower lap's first fix at s=20, sector 950→50 → small positive loss (never a full-lap negative); 10 m at 20 m/s inside a 2 s timestamp gap contributes 0.5 s.
Z2 (H5) Availability of each safety check = COVERAGE FRACTION over the lap distance (≥ 90 % of grid cells have the needed channel within its max age), not "any evidence"; below that → check unavailable → lap `unverified` with the coverage % in limitations. Tests: channels present only for the first 10 % → unverified.
Z3 (H6) Projection tolerance for the implied-yaw comparison expressed in DISTANCE (≤ 8 m) and TIME (≤ 0.4 s), not samples; at 1 Hz the check must still evaluate (window = max(200 ms, one sample interval)) instead of being skipped — if the rate is too low for a meaningful spin check, mark the yaw check unavailable (→ unverified), never silently clean. Tests at 1 Hz, 5 Hz, 25 Hz.
Z4 (M3) IMU brake cross-check: missing speed at either endpoint → NOT confirmed (fall back to the dv/ds-only estimator or return null with a quality flag). Test: tilted-phone −0.3 g with absent speed → no brake point.
Z5 (M8) Seam-aware steering derivative: smoothness/corrections computed per contiguous run (tail and head separately), never across the virtual S/F join. Test: −20° at 999 m / +20° at 0 m → no correction, derivative excludes the seam.
Gates (repo root, real exit codes): typecheck, test, lint all 0.
WRITE SET: packages/core/src/coaching/**, packages/core/test/coaching/**. No commit, no agents, no Expo server. Report DONE/DONE_WITH_CONCERNS/BLOCKED with evidence + totals.
