# Ticket P4n-FIX1 — Codex P4n-REV1 (1 HIGH + 1 MEDIUM + 1 LOW), tests first — Sonnet
Source: .foreman/scratch/p4nrev1-codex-output.log (final list). HEAD 8bd8477 (+ ledger). No build until this lands with 0 HIGH.
Q1 (HIGH) Boolean brake decode must not be "≠ rest ⇒ pressed": order of precedence (a) persisted `flagBit` (from the Signal Finder score, stored in the binding evidence) → bit test on the decoded scalar; (b) persisted ACTIVE value (`activeValueHex` — store it at confirm time from the score's active level) → equality with active ⇒ 100, equality with rest ⇒ 0, anything else ⇒ NO sample (unknown level, never fabricated); (c) neither present → keep "≠ rest" but mark the binding `coarse: true` in diagnostics and in the monitor label ("Brake switch (coarse)"). The confirm path (signalFinderController / SignalFinderScreen) must persist `flagBit` and `activeValueHex` into evidence for boolean bindings. Tests: 0x500C (rest 04, active 05, flagBit 0): 04 → 0, 05 → 100, 0x06 → no sample under (b)… but with flagBit 0 → 0x06 → bit0 = 0 → 0; 0x4002-like (rest 01, active 19, no flagBit): 0x83 with the engine running → NO sample (not 100) under (b); legacy evidence without either → coarse.
Q2 (MEDIUM) `brakeBindingsSignature` includes every decoding-relevant field (channel, ecu, did, length, byteOffset, restValueHex, activeValueHex, flagBit, observedMin/Max, decodeKind) and is computed only over bindings that actually enter the poll plan (a binding overridden by a configured spec does not count). Tests: reconfirming 0x58B7 with a new max → changed; overridden binding → unchanged.
Q3 (LOW) The "Stop → Start to apply" banner appears only when the running adapter is ENET and the confirmed binding changed the effective plan signature (not for ELM327, not for identical reconfirmations, not for non-polled channels).
Gates (real exit codes, repo root): `npm run typecheck`, `npm run test`, `npm run lint`, `npx expo export --platform ios` in apps/mobile — all 0. Report totals + red→green per item.
WRITE SET: apps/mobile/src/session/{telemetryProvider,signalFinderController}.ts, apps/mobile/src/persistence/didSweepStore.ts (evidence fields, additive), apps/mobile/src/ui/screens/{TelemetryScreen,SignalFinderScreen}.tsx (+ strings), tests. No sweep/observation/reservation changes. No commit, no agents, no Expo dev server.
