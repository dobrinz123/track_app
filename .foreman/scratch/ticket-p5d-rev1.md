You are a read-only reviewer. Repo: D:\CODE\APLICTIE_Circuit (npm workspaces). If shell exec is blocked, read files directly. Review commit a101d4d ("P5d(+T6): Test Loop / learned circuits…") against docs/architecture/contracts.md "Test Loop mode (Phase 5d)". Files: packages/core/src/testloop/** (+ tests incl. traces), packages/core/src/{contracts.ts,profile/schema.ts} (geometryStatus 'ad-hoc'), apps/mobile/src/session/{testLoopController,testLoopGuards,learnedCircuitStore,composition,circuitCatalog}.ts, apps/mobile/src/persistence/learnedCircuitSchema.ts, apps/mobile/src/ui/screens/{TestLoopScreen.tsx,testLoopStrings.ts,CircuitSelectionScreen.tsx}, and their tests. Questions: (1) loop closure — false closures (GPS drift near start at standstill, driving past the start mid-lap, parking lots with overlapping passes); the 25 m / ±45° / 300 m constants; (2) centreline/corner derivation — degenerate inputs (straight out-and-back, tiny loop, duplicate points, big GPS jumps), corner count explosions on wiggly traces; (3) the ad-hoc gate — any path where a learned circuit could reach 'official'/enable suggestions or voice cues; the frozen-profile interplay; (4) learned store — id collisions, delete-refused-while-sessions logic, restart reload ordering in bootstrap (registered before controller/history?), the web in-memory caveat; (5) selection screen — the learned list vs bundled ordering, the removed placeholder, resolveSelectedCircuit bug fix correctness; (6) session pipeline on learned geometry — S/F crossing detection with the generous 25 m corridor, off-track anomaly behaviour on narrow streets; (7) RO/EN parity and the legal/low-speed intro presence. Output: numbered findings (HIGH/MEDIUM/LOW, file:line, one-line fix), then "NEW: n HIGH, m MEDIUM". No edits.
