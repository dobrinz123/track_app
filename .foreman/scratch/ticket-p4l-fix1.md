# Ticket P4l-FIX1 — Signal Finder concerns → brake channel END-TO-END, tests first — Opus
Context: commit "P4l: Signal Finder" (see ledger). Worker concerns 1, 4, 5 are in scope here; concern 2 (window shift) is accepted as-is.
F1 Brake channel end-to-end (concern 1): add `brakeSwitch` (0/100 boolean-as-percent) and `brakePct` to `TelemetryChannelId` in packages/core/src/telemetry/contracts.ts, to every exhaustive Record over it (LapDetailScreen.tsx and any other), to the ENET poll plan builder so that a confirmed binding (vehicle_profile_bindings, channel brakeSwitch/brakePressure) becomes a real DID poll entry (ecu address from the binding — the raw UDS channel must be able to address a DIFFERENT target than the DME within one session, or open a second channel to that ECU; document which, keep the reservation single-owner), and to TelemetrySample so the provider EMITS brake samples decoded via the existing decodeBrakeBindingValue. Test: fake transport answering 0x29/0x500C 04→05 → provider samples show brakeSwitch 0 → 100. Recording pipeline: brake samples land in the session recording like the other channels (check the lap recorder/schema and add the channel additively; bump any schema version you touch).
F2 Language (concern 4): add `language: 'ro' | 'en'` to settingsStore (default from device locale: ro → 'ro', else 'en'), a Settings row, and pass it to the Signal Finder summary builder. Test the default rule.
F3 Reservation (concern 5): add owner 'signalFinder' to enetAdapterReservation so the UI can name the holder correctly; behaviour identical.
F4 Coaching engine input: expose brakeSwitch to packages/core/src/coaching channel availability (tier 1 brake available when the channel has coverage) — only the availability/detection path (brake onset from the switch edge when present, else the existing IMU estimate); add one test on both circuits' fixtures with a synthetic switch channel. If this exceeds ~2 h, report NEEDS_INPUT with the seam found and stop.
Gates (real exit codes, repo root, npm workspaces): `npm run typecheck`, `npm run test`, `npm run lint`, and `npx expo export --platform ios` in apps/mobile — all 0. Report totals.
WRITE SET: packages/core/src/telemetry/contracts.ts, packages/core/src/telemetry/enet/** (poll plan), packages/core/src/coaching/** (F4 only), apps/mobile/src/session/{telemetryProvider,enetAdapterReservation,signalFinderExport}.ts, apps/mobile/src/session/signalFinderController.ts (language plumbing only), apps/mobile/src/persistence/** (additive), apps/mobile/src/settings/** (language), apps/mobile/src/ui/screens/{LapDetailScreen,SettingsScreen,SignalFinderScreen}.tsx, tests. No commit, no agents, no Expo dev server. Report DONE / DONE_WITH_CONCERNS / BLOCKED / NEEDS_INPUT with red→green evidence + totals.
