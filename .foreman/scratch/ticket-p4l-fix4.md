# Ticket P4l-FIX4 — Codex P4l-REV2b residuals + new (core + provider + settings), tests first — Opus
Source: .foreman/scratch/p4lrev2b-codex-output.log (final section). HEAD e2e5a2c (+ FIX3 in flight on signalFinderController/Export/Screen/composition — DO NOT touch those four files).
N1 (F1 PARTIAL, HIGH) scoring.ts: whole-response baseline restlessness caps the DID at `unrelated` for EVERY shape (not only boolean-edge); an analog block with a rolling counter byte advancing by 1 must not be `found`. Keep the analog noise floor for the scored byte only. Test: analog block, counter byte +1 per sample, action byte correlated → unrelated.
N2 (F2 PARTIAL, MEDIUM) scoring.ts: `analog-bipolar` requires `bipolarSides === 'both'` for BOTH `found` and `probable`; one-sided → `unrelated` with verdictCapReason. Test.
N3 (NEW HIGH) coaching cornerMetrics.ts: boolean channels (brakeSwitch) must NOT be linearly interpolated onto the metre grid — use step/hold (value of the last observed sample); brake onset from the switch = the first sample ≥ threshold, with an onset uncertainty = the sampling interval (expose `brakeOnsetUncertaintyM` or equivalent in the metrics/limitations so the report says "±X m" and the safety bound uses the pessimistic edge). Test: 0→100 at 1 Hz → onset at the first 100 sample's metre, never earlier; uncertainty ≈ distance covered in one interval.
N4 (NEW HIGH) telemetryProvider.ts: reject any live payload whose length differs from the persisted binding length (when non-null) before decoding → no sample (channel error path), never a fabricated 0/100. Test: 2-byte binding, 1-byte and 4-byte payloads → no brake sample.
N5 (NEW MEDIUM) enet: a silent ECU for the brake entry must not stall DME polling: per-entry timeout for binding-sourced DIDs (e.g. 300 ms) and exponential back-off after N consecutive misses (drop to ≤ 0.5 Hz, surface a channel warning), recovering when it answers. Test with a fake transport that never answers 0x29: DME channel rate stays ≥ 80 % of its nominal rate.
N6 (NEW MEDIUM) enetChannelSpecs.ts / enetSession.ts: a `decodeValue === null` is a per-channel skipped sample (per-channel error counter, channel marked degraded after 5), NOT fed into the global consecutive-error budget that kills the session. Test: 20 consecutive null decodes on the brake channel → session alive, other channels flowing.
N7 (LOW) sqlSettingsStore.ts: when the persisted settings have no `language`, default from the device locale (ro → 'ro'), otherwise keep the persisted value; never flip a chosen value. Test both.
Gates (real exit codes, repo root): `npm run typecheck`, `npm run test`, `npm run lint`, `npx expo export --platform ios` in apps/mobile — all 0. Report totals.
WRITE SET: packages/core/src/telemetry/signalFinder/scoring.ts, packages/core/src/coaching/** (N3), packages/core/src/telemetry/enet/{enetChannelSpecs,enetSession}.ts, apps/mobile/src/session/telemetryProvider.ts, apps/mobile/src/persistence/sqlSettingsStore.ts, matching tests. No commit, no agents, no Expo dev server. Report DONE / DONE_WITH_CONCERNS / BLOCKED with red→green evidence + totals.
