# Ticket P4m-FIX1 — Codex P4m-REV1 (3 HIGH + 6 MEDIUM), tests first — Opus
Source: .foreman/scratch/p4mrev1-codex-output.log (final numbered list). HEAD 02d5214. Build 6 is NOT delivered until this lands.
X1 (H1) Measure, never assume: before the metronome, a ~2 s PROBE reads every planned DID once per ECU (round-robin) → measured req/s AND per-ECU liveness (an ECU with zero answers in the probe is `silent`). The budget is (re)computed from the measured rate; the export/snapshot say `measuredReqPerSec` only when measured, else `rateSource: 'assumed'` with the value, shown in the header ("rate assumed 15/s — probe failed").
X2 (H2) Silent ECU must not starve the live one: DIDs of a `silent` ECU are dropped from the round (listed as "not read — ECU 0x29 silent") and the budget is refilled from the next pool; during the run, a per-DID request timeout of 300 ms for binding/finder reads and back-off after 3 consecutive misses (same numbers as telemetryProvider N5). Test: fake transport with 0x12 answering and 0x29 silent → 0x12 DIDs keep ≥ 3 samples per window; header lists 0x29 as silent.
X3 (H3) Only ATTEMPTED (ecu,did) keys count as read: the runner returns attempted keys; `readDids`, `noResponseDids`, counts and the export derive from them; a stopped/partial run leaves the untouched DIDs in `notRead`. Test with a stop after the first window.
X4 (M4) Plan and accumulate by composite (ecu,did) — the same DID number on two ECUs lands in the same round.
X5 (M5) Cached/changed pools are filtered to the target's ECUs (hypothesis + discovery ECUs), all ECUs only for a target with `ecu: null`.
X6 (M7) The single-bit flag exception under analog shapes applies only when (a) the DID is a hypothesis with `expectedShape: 'boolean-edge'`/flag metadata, or (b) every press AND release window has ≥ 2 agreeing samples (no sparse + flag combination). Keep 0x4007 `found` via (a): make it a boolean-edge hypothesis in the Supra catalog (it is one). Update scoring.test.ts:673 accordingly with a reason.
X7 (M8) `never-moved → unrelated` only with complete action-window coverage and the required average sampling; otherwise `insufficient`.
X8 (M9) RO/EN completeness: every visible string on SignalFinderScreen (evidence line, statuses, engine text, banners, next-step, share controls, a11y labels) and every export string (truncation markers, notes) comes from the RO/EN string table; target labels/notes get RO variants in targets.ts data. Test: render-free check that the RO table has every key of the EN table and no EN literal remains in the screen file (grep-style unit test on the strings module).
X9 (M10) Find is disabled (with reason) when the plan has zero DIDs; "one script per find" holds literally.
Skip M6 (persist finder outcomes) — separate ticket later.
Gates (real exit codes, repo root): `npm run typecheck`, `npm run test`, `npm run lint`, `npx expo export --platform ios` in apps/mobile — all 0. Report totals + red→green evidence.
WRITE SET: packages/core/src/telemetry/signalFinder/**, apps/mobile/src/session/{signalFinderController,signalFinderExport}.ts, apps/mobile/src/ui/screens/SignalFinderScreen.tsx (+ a strings module next to it), tests. Do not touch sweep/observation flows, reservation, telemetryProvider. No commit, no agents, no Expo dev server.
