# Ticket P4g — field revision after the first driveway test (core + mobile, one worker)
Binding: "Telemetry addendum — field revision" (contracts.md, end). Field facts + user findings: `.foreman/ledger.md` (FIELD RESULT 2026-08-27). Scout evidence for the switch bug: `telemetryProvider.ts:873-879` (start() no-op while `running`), `:833-837` (fail handler never resets `running`), `:843-870` (retry fires with stale adapterType), `tcpObdTransport.ts:96-114` (socket left open on async init failure).
Tests first (fail on HEAD 5ea99b4), then fix, one wave:
1. CORE (small, additive): `accelPedalPct` in `TelemetryChannelId`; `pidCodec` mode-01 PID 0x49 decoder 100/255·A (document EMPIRICAL on the Supra); `ENET_SPEC_CHANNELS` + `ENET_DEFAULT_CHANNEL_RATES_HZ` (5 Hz) + `DEFAULT_ENET_CHANNEL_SPECS` entry; ELM mode-01 definitions; tests (vector A=0x80 → 50.2 %).
2. MOBILE poll plans: `accelPedalPct` 5 Hz on both adapter types; monitor rows: "Accelerator pedal" (0x49), "Throttle plate" (0x11, relabeled), plus `latG`/`longG` rows with observed Hz whenever the G provider is running (no driving-dashboard change). Settings copy updated. Tests.
3. MOBILE switch bug (`telemetryProvider.ts`): (a) a generation records its config fingerprint {adapterType, host, port}; `start()` while `running` with a FAILED/stopped generation OR a different fingerprint → tear down fully (cancel retry timer, abort discovery, close the transport explicitly, release reservation, `running=false`) then launch from CURRENT settings; (b) the fail handler closes the generation's transport explicitly (ELM and ENET) before scheduling a retry; (c) the retry timer captures the fingerprint and aborts if settings changed meanwhile; (d) settings subscription: adapterType/host/port change while a generation exists → stop it (state 'stopped', diagnostics "settings changed"); the monitor's Start always yields a fresh launch. Tests reproducing the field sequence: ELM start fails async (socket open) → user sets adapterType 'enet' → Start → ELM socket closed, ENET session built and polling; retry fires after a type change → no ELM socket opened; fingerprint change while polling → stopped; unchanged settings → no-op as before (ELM byte-identical otherwise).
4. MOBILE hidden developer mode: `developerModeEnabled` setting (default false), toggled by 7 taps on the About version text in Settings (toast "Developer mode on/off"); when on (or `__DEV__`), Settings shows "Dev: DID Probe (ENET)" and "Dev: DID Sweep (ENET)" and the routes are registered in release too (screens must not import DevReplay fixtures); DevReplay stays `__DEV__`-only. The read-only whitelist and reservation rules are unchanged. Tests for the toggle logic (pure) and route gating.
Gates (real exit codes): `npm run typecheck` 0, `npm test` 0, `npm run lint` 0, `cd apps/mobile && npx expo export --platform ios` 0 (confirm `accelPedalPct` and "Developer mode" strings in the hbc). WRITE SET: packages/core/src/telemetry/{contracts.ts,pidCodec.ts}, packages/core/src/telemetry/enet/enetChannelSpecs.ts (+ core tests); apps/mobile/src/session/{telemetryProvider.ts,settingsStore.ts,enetSettingsValidation.ts}, apps/mobile/src/persistence/sqlSettingsStore.ts, apps/mobile/src/ui/screens/{TelemetryScreen,SettingsScreen}.tsx, apps/mobile/src/ui/navigation/RootNavigator.tsx, apps/mobile/test/**. No commit, no agents. Report DONE/DONE_WITH_CONCERNS/BLOCKED with per-item evidence and totals.
