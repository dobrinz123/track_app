# Ticket P4l — Signal Finder (target-driven discovery wizard), tests first — Opus
Binding spec: docs/architecture/contracts.md "Signal Finder (Phase 4l)". Read it, then: packages/core/src/telemetry/enet/{didObservationPhases,didObservationBatches,didCandidates,didSweep}.ts, apps/mobile/src/session/{didSweepController,didSweepExport,enetAdapterReservation,telemetryProvider}.ts, apps/mobile/src/persistence/{didSweepSchema,didSweepStore}.ts, apps/mobile/src/ui/screens/DidSweepScreen.tsx, data/vehicle-profiles/toyota-supra-b58.draft.json, and the field data data/field/sweeps/2026-08-29-test4-ecu29-0x5000-0x58F2.json (0x500C is the real brake-switch shape: 04→05).
Deliver:
S1 core: `packages/core/src/telemetry/signalFinder/{targets.ts,metronome.ts,scoring.ts,index.ts}` — target definitions (data; Supra hypotheses copied from the profile with provenance; generic defaults for unknown cars = hypothesis-free, discovery ranges only), metronome step timeline builder (press/hold/release windows with settle), pure scoring per DID (matchedEdges/expectedEdges, baselineChanges, analog sign correlation, insufficient) → verdicts found/probable/unrelated/insufficient. Tests from the field shapes: 0x500C (5 presses → found), 0x500B single edge → probable/unrelated as the rule says, counter DIDs → unrelated, battery-voltage dip (13636→13611) → unrelated, 1 Hz sampling with 2 s windows → not insufficient.
S2 mobile session: `apps/mobile/src/session/signalFinderController.ts` — iterates ECU addresses, builds the DID list per ECU (hypotheses + cached responders from did_sweep_responders filtered by shape, ≤16 per pass, rate-derived), runs the metronome (haptics via expo-haptics if present, else no-op), records samples per step, scores, exposes a snapshot (idle/preparing/reading/scoring/result). Reuses the adapter reservation and transport factory exactly like didSweepController; no duplicate transport code. Reads must tolerate NRC (→ "no response" for that DID).
S3 persistence: vehicle-profile bindings table (schema v3: `vehicle_profile_bindings`: profileId, channel, ecu, did, length, decode, status, evidenceJson, updatedAtUtc) + store functions; `confirmBinding()` from the result screen; ENET telemetry provider resolves `brakePct`/`brakeSwitch` (boolean channel → 0/100) from the binding if present. Export of the finder session via the sharing path (schemaVersion 1, "trace-signal-finder").
S4 UI: `SignalFinderScreen.tsx` reachable from the Developer screen: target list with engine requirement + status (found/confirmed/missing), Find button, metronome view (big PRESS/HOLD/RELEASE + countdown), result list with verdict badges and evidence (edges matched, ECU, raw min/max), "Confirm as <target>", "Next step" line when nothing found (ECU ranges + minutes from the measured rate), Share.
Gates (real exit codes, not piped): `npm run typecheck`, `npm run test`, `npm run lint` (npm workspaces, from the repo root), `npx expo export --platform ios` in apps/mobile — all 0. Report totals.
WRITE SET: the new files above, didSweepSchema.ts/didSweepStore.ts (additive migration v3 only), telemetryProvider.ts (binding lookup only), Developer screen navigation entry, tests. Do NOT modify the sweep/observation flows. No commit, no agents, no Expo dev server. Report DONE / DONE_WITH_CONCERNS / BLOCKED / NEEDS_INPUT with evidence + totals.
S5 EXPORT (user requirement 2026-08-29, binding): one tap "Share" on the result screen exports TWO files through the share sheet: (a) the full JSON session (`trace-signal-finder-<yyyy-mm-dd>-<target>.json`, schemaVersion 1) and (b) a human-readable summary `trace-signal-finder-<yyyy-mm-dd>-<target>.md` (≤ 1 page: target, engine state, ECUs + DID count read, per-DID verdict table with evidence, confirmed bindings, next step with minutes). Same summary is shown on screen. Reuse the sweep export/share path (expo-file-system + expo-sharing); if the platform share sheet cannot take two files, share the .md first and offer a second button for the JSON. Test the summary builder as a pure function (RO/EN via the existing language setting if present, else EN).
