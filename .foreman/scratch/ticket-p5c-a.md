# Ticket P5c-A — clean-lap rules per Phase 5 REVISION 2 (user F1), tests first — Sonnet
Binding: docs/architecture/contracts.md "Phase 5 REVISION 2" R2-1. Code: packages/core/src/coaching/cleanLap.ts (+ types, sessionInsights, reportText where labels surface), tests in packages/core/test/coaching/**.
A1 Reclassify: anomalous ONLY for incomplete / off-track / weak GPS (accuracy > threshold on > 5 % of the lap, or gap > 1.5 s). Remove |longG| > 1.2 g, ABS-like oscillation and yaw-spike/slide as ANOMALY causes.
A2 Convert those three signatures into informative LAP LABELS (e.g. labels: ['HEAVY_BRAKING', 'ABS_SUSPECTED', 'SLIDE_ROTATION']) carried on the lap result and rendered in the report text (RO/EN) as neutral observations ("turul 3: frânare foarte tare în T1 (−1,3 g)"), never as exclusion. Detection thresholds stay as heuristics for the LABEL only; keep them conservative and document that cars reach 1.3–1.5 g on track (user field fact, GR86 1.3 g lateral).
A3 Reference lap + demonstrated envelope use clean laps under the new definition (a lap with ABS/slide labels and on-track/good-GPS is eligible). Property tests: the envelope never selects values from anomalous (new definition) laps; a 1.3 g lap is clean when on-track with good GPS.
A4 Update every test that asserted the old exclusions (document each change with a one-line reason); keep the completeness/coverage rules untouched.
Gates (real exit codes, repo root): `npm run typecheck`, `npm run test`, `npm run lint` — all 0 (core-only change; run mobile tests too since reportText flows into the app). Report totals + red→green.
WRITE SET: packages/core/src/coaching/**, packages/core/test/coaching/**. Nothing in apps/mobile (its tests must stay green as-is; if one asserts the old rule, update it and say so). No commit, no agents.
