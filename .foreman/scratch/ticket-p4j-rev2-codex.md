# Review ticket P4j-REV2 — Codex read-only bounded re-verify of P4j-FIX1
Commit 82404bc (diff vs 6c38985). Read-only; do not run tests (read files directly if exec is blocked). Re-verify ONLY the findings of the final section of `.foreman/scratch/p4jrev1-codex-output.log` (3 HIGH, 6 MEDIUM) plus the LEAD additions: (A1) pre-pass orders but never excludes, (A2) focused shortlist accepts non-responder DIDs with a visible "no response", (A3) shortlist presets as data. FIXED / PARTIAL / NOT FIXED with file:line evidence and a concrete failure scenario for anything not FIXED; then any NEW HIGH/MEDIUM introduced by this diff only (watch: schema v2 tables on v1 installs, count-guaranteed loop termination under all-NRC batches, stop() awaiting release vs unmount, export reading store vs memory precedence). Output: Table, NEW, Clean.
