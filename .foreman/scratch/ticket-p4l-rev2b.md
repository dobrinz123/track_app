You are a read-only reviewer. Repo: D:\CODE\APLICTIE_Circuit (npm workspaces). Note: shell exec may be blocked by the Windows sandbox — if `git show` fails, read the files directly (they are the post-commit state; you can diff mentally against the descriptions below). Bounded re-verification of an earlier review of the Signal Finder feature against two commits: ad70909 "P4l-FIX2" (packages/core/src/telemetry/signalFinder/scoring.ts + its test) and e2e5a2c "P4l-FIX1" (packages/core/src/telemetry/contracts.ts, packages/core/src/telemetry/enet/enetChannelSpecs.ts, packages/core/src/coaching/{types,cornerMetrics,reportText}.ts, apps/mobile/src/session/{telemetryProvider,enetAdapterReservation,settingsStore,signalFinderExport}.ts, apps/mobile/src/persistence/{sqlSettingsStore,telemetryRead}.ts, apps/mobile/src/ui/screens/{SettingsScreen,SignalFinderScreen,LapDetailScreen}.tsx, tests packages/core/test/telemetry/enet/enetBrakeChannel.test.ts, packages/core/test/coaching/brakeSwitchChannel.test.ts, apps/mobile/test/session/telemetryProviderBrakeChannel.test.ts, packages/core/test/telemetry/signalFinder/scoring.test.ts).
Earlier findings to re-verify (say FIXED / PARTIAL / NOT FIXED with file:line evidence):
F1 a block DID whose whole response changes at baseline (rolling counter byte + action byte) could be `found` because only the winning byte's baselineChanges was used.
F2 `analog-bipolar` never required excursions on both sides of rest.
F3 confirmed bindings never reached live ENET telemetry (no brake channel in TelemetryChannelId / poll plan / samples / recording).
F4 the binding decoder read one byte only (0x500B 0002→0006 invisible; blocks had null min/max).
F6 the "<2 samples per window" sufficiency gate collapsed press+hold windows.
F10 edge matching tested presence, not ordered transitions inside the settle-shifted windows (quiet-baseline random toggler → found).
Then hunt NEW defects introduced by the two commits, especially: (a) the "arriving sample" transition rule and netEdges = matched − extra — can a DID toggling on every sample still reach `found`/`probable`? (b) the brake DID poll entry addressed to ECU 0x29 inside the DME (0x12) session — response source matching, timeouts on 0x29 stalling DME channels, NRC handling; (c) decodeValue returning null → channel error: does one bad read kill the whole poll plan or only that channel? (d) coaching detectBrake with brakeSwitch at 1–5 Hz: onset resolution and the existing "brake later" safety bound; (e) language default at hydration only — can it flip a language the user already chose? Output: numbered list (severity HIGH/MEDIUM/LOW, file:line, one-line fix), then "NEW: n HIGH, m MEDIUM". No edits.
