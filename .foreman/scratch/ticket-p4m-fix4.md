# Ticket P4m-FIX4 — Codex P4m-REV4 (2 HIGH + 2 MEDIUM + 1 LOW; E/F partial), tests first — Opus
Source: .foreman/scratch/p4mrev4-codex-output.log (final list). HEAD = P4m-FIX3 commit. No build until this lands with 0 HIGH. Keep mechanisms simple; do not add new concepts.
W1 (H1) The explicit retry round is part of the measurement: after the retry, recompute the retained-entry rate from probe + retry answers (or simply run the retry BEFORE summarising) and re-plan with that rate. Test: hypothesis silent in the probe, answers on retry → retained in the plan and the plan's rate reflects its exchange.
W2 (H2) The one explicit retry applies to EVERY probe-silent hypothesis, including those on wholly silent ECUs and the zero-answer case (retry all hypotheses when nothing answered). Only after the retry is an ECU declared silent. Test: 0x29 wholly silent in the probe, answers on retry → 0x29 retained, its DIDs in the round; and the reverse (still silent → dropped, listed "not read — no answer to the probe or its one retry").
W3 (M3) Probe bound honesty: the on-screen bound = entries × (send timeout + response timeout + 0x78 extension budget) computed from the runner's constants (not 300 ms × entries), and progress keeps advancing during the retry round (probe progress covers probe + retry: n/N where N = probe entries + retry entries). Test the bound formula and that progress reaches N/N.
W4 (M4) Export redaction: `diagnostics.rawError` is redacted — keep only the error name/code and the first 120 chars with IPv4/IPv6 addresses, host:port pairs, file paths and anything that looks like a token (long base64/hex) replaced by "‹redacted›". Test with an error containing an IP, a path and a hex token.
W5 (L5) When the late close settles, clear both the pending flag AND the teardown error (snapshot.error returns to null unless another error exists). Test.
W6 (F partial, document only) Contract text: the 5 s hard release keeps the shared reservation from being stuck forever; the residual (a close that settles after release) is accepted and flagged — add one sentence to contracts.md "Signal Finder REVISION" as item 14 (LEAD will review). E partial: covered by W3.
Gates (real exit codes, repo root): `npm run typecheck`, `npm run test`, `npm run lint`, `npx expo export --platform ios` in apps/mobile — all 0. Field fixtures must stay green. Report totals + red→green per item + changed strings (RO/EN).
WRITE SET: packages/core/src/telemetry/signalFinder/**, apps/mobile/src/session/{signalFinderController,signalFinderExport}.ts, apps/mobile/src/ui/screens/{SignalFinderScreen.tsx,signalFinderStrings.ts}, docs/architecture/contracts.md (W6 only, one sentence), tests. Do not touch sweep/observation flows, the reservation module, telemetryProvider. No commit, no agents, no Expo dev server.
