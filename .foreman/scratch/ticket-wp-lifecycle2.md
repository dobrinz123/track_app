TASK: Fix wave 2 — close the RESIDUE from Codex's cross-verification of fix wave 1 (report: .foreman/scratch/fixwave-codex-verify-output.log — read it in full first; it contains file:line evidence for everything below). This is the final pre-install gate; the C7 regression is the most dangerous item this project has had.

EXPECTED OUTCOME: `npm run typecheck`, `npm test`, `npm run lint`, `npm run export:ios` green; every item below fixed with a revert-detecting test (UI-only items exempted where stated). Paste decisive output.

FINDINGS + BINDING DESIGNS:

F1 (C7 regression + NEW-HIGH, duplicate sample listener / dirty failed-start): packages/core/src/controller/sessionController.ts — restructure the start path: (a) create the provider subscription ONLY AFTER `await provider.start()` succeeds; (b) on start failure, roll back the state mutation (snapshot the machine state before mutating; restore it) so the controller is NOT left 'calibrating'; (c) retry after failure must yield exactly ONE subscription (guard: if a subscription handle exists, unsubscribe before resubscribing); (d) `lastError` clearing per original spec: cleared on next successful state change, not merely before the next command (realFacade). TESTS (sensitivity-proven): failing-then-succeeding provider → after retry, feeding N samples ingests each exactly once (count via repository writes or emissions); failed start leaves state unchanged and zero subscriptions.

F2 (C1 residue, gate not serialized/caught): apps/mobile/src/session/composition.ts + SwappableFacade — the preflight gate runs under a single in-flight promise (concurrent startPreflight calls share one replacement); gate rejection → caught → surfaces via lastError mechanism (no unhandled rejection), stale facade never receives the forwarded command on failure. sessionController.dispose(): detach sample listener + watchdog in a `finally` so a rejecting provider.stop() cannot skip detachment. TESTS: concurrent double-startPreflight → exactly one replacement; dispose with rejecting stop still detaches (no emissions after).

F3 (C2 residue, early real-facade install + false comment + no retry): composition bootstrap — install the production facade inner ONLY at full readiness (after history+settings+recovery init), immediately before bootstrapState→'ready'; on failure keep PendingFacade truly installed (fix the code to match the comment); export `retryBootstrap(): Promise<void>` that re-runs the bootstrap sequence from a clean slate (guarded against concurrent retries); CircuitDetailScreen failed-banner gains an inline Retry button wired to it. TESTS: commands remain no-ops until 'ready' (not merely until controller creation); failed bootstrap → retryBootstrap succeeds on a subsequently-working DB.

F4 (C6 residue + NEW-MED, unserialized replay transitions + unflushed replay): composition — one shared async transition lock across startDevReplaySession / restoreProductionFacade / useMockFacadeForDevReplay (queue, never overlap); restore path: `await replayController.flush()` (persist completed-lap work) BEFORE dispose; unmount-vs-new-start ordering safe under the lock. TESTS: rapid start/restore/start storm → final state deterministic, exactly one live controller, disposed replay emits nothing; replay with a completed lap then restore → lap persisted.

F5 (C10 residue + NEW-MED, vanished checkpoint): composition.resumeRecovery — if loadCheckpoint returns null: abort (clear banner, clear activeSessionId pointer, do NOT start a session, surface a lastError-style notice). TEST: null-checkpoint resume → no session started, pointer cleared.

F6 (B4 residue, LOW, DevReplay statically bundled): RootNavigator — move the DevReplayScreen import inside the `__DEV__` branch via inline `require` (Metro constant-folds `__DEV__` in release and drops the dead branch, excluding the module + its fixture deps from the release graph). Keep types clean (typeof import). Verify: `npx expo export --platform ios` in release mode, then grep the produced .hbc for 'Run replay fixture' → MUST be absent; paste the check. (If Metro still includes it, document why and gate at the metro.config level — but attempt the require approach first.)

MUST NOT: touch timing/geometry/delta algorithms, bundle id, app.json; no new deps; no modals; don't weaken existing tests; no subagents; no git commit.

OUTPUT FORMAT: First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED. Then per-finding fix + test + sensitivity proof method, commands + pasted results incl. the F6 hbc grep.

WRITE SET: packages/core/src/controller/sessionController.ts, packages/core/test/controller/**, apps/mobile/src/session/**, apps/mobile/src/ui/screens/CircuitDetailScreen.tsx, apps/mobile/src/ui/screens/DevReplayScreen.tsx, apps/mobile/src/ui/navigation/RootNavigator.tsx, apps/mobile/test/**.
