# Ticket P4j-FIX1 — Codex P4j-REV1 fix wave (3 HIGH + 6 MEDIUM + test gaps), tests first
Source: `.foreman/scratch/p4jrev1-codex-output.log` (final section). HEAD 6c38985. Binding: DID-sweep addenda in contracts.md.
HIGH:
H1 Sample guarantee by COUNT, not duration: each batch phase runs round-robin until EVERY DID has ≥ minSamplesPerPhase POSITIVE samples, with a bounded policy: a DID that fails (NRC/timeout) 3× in a phase is marked `insufficient` for that phase (excluded from ranking, reported), and a hard cap of 3× the nominal duration ends the phase. Countdown shows the nominal duration and "extending…" when past it. Tests: timeouts on one DID, NRC on all, pacing overhead, slow tail DID.
H2 Change rule that catches booleans and level shifts: a DID "changed in phase P" if (a) the set of values in P differs from the baseline value set (any value in P not seen in baseline, or vice-versa) AND (b) for numerically decodable widths (1–4 bytes: u8/u16/u32) the phase mean shifts vs baseline by more than max(3 raw units, 2× baseline P90−P10) OR the value set is boolean-like (≤ 2 distinct values overall) and differs; for 5–8 bytes compare byte-wise (any byte offset with a level shift). Baseline jitter must not disqualify a candidate (use baseline spread as the noise floor, never "distinct count"). Never fall back to naive distinctness when samples < min — mark `insufficient` instead. Same rule for block offsets. Tests: stable 0→1, toggling 0/1, baseline jitter + level shift, 297→305→295 noise (NOT changed), 3–8-byte values.
H3 Persist guided/batched samples and summaries per run (SQLite table `did_sweep_observation_samples` + summaries JSON on the run; in-memory fallback), keyed by runId; export reads them from the store (not only live memory); a later observation on the same run APPENDS (new observationId) instead of resetting; kill/reopen → Share still includes series/candidates/block offsets/batchIndex. Tests with the recreated-controller pattern.
MEDIUM: M1 hard bounds: batchSize ≤ 16, focused shortlist ≤ 16 DIDs (error shown), minSamplesPerPhase ≤ 20, invalid hex tokens shown as an error (not silently dropped), keyboardType default (A–F reachable). M2 pause()/resume supported during batched observation at batch boundaries (state persisted); `stop()` resolves only after transport close + reservation release; unmount awaits it (fire-and-forget only with `.catch`). M3 group and validate all samples per DID BEFORE numeric/block routing; length inconsistency → DID marked inconsistent, never split into two candidates. M4 UI shows changed offsets per phase (all phases). M5 legacy `startObservation()`/`startGuidedObservation()` back to the 1–8-byte filter (byte-identical behaviour; tests). M6 export `schemaVersion: 2` with a note; keep `targetAddress` hex string; add `targetAddressNumeric`.
Gates (real exit codes): typecheck, test, lint, expo export iOS all 0.
WRITE SET: packages/core/src/telemetry/enet/{didObservationBatches.ts,didObservationPhases.ts,didCandidates.ts} + tests; apps/mobile/src/persistence/didSweep*.ts; apps/mobile/src/session/{didSweepController.ts,didSweepExport.ts}; apps/mobile/src/ui/screens/DidSweepScreen.tsx; apps/mobile/test/**. No commit, no agents, leave the Expo server alone. Report DONE/DONE_WITH_CONCERNS/BLOCKED with per-finding evidence + totals.
