TASK: Read-only cross-family review of the ENTIRE coaching phase (commits d68132d..c6a3e68: corners analysis, speed model v2 + observed overlay, braking zones, CoachEngine, controller/facade wiring, UI + voice). The foreman verified it live in web preview (cues fire correctly, ordered, C9 correctly CORNER_AHEAD). Your job: find what live-preview and unit tests cannot — on-phone/on-track failure modes and algorithmic edge cases.

READ: packages/core/src/{corners,coach}/**, controller/sessionController.ts (coaching sections), apps/mobile/src/session/{voiceCoach.ts,tmrProfile.ts,composition.ts (coachingConfig)}, apps/mobile/src/ui/components/{CoachStrip,CornersList}.tsx, screens {ActiveDashboardScreen,SettingsScreen,CircuitDetailScreen}.tsx, assets observed-speeds JSON, and the tests.

HUNT FOR: wrap-around cue math near S/F (C1 brake zone starts at 299m; C9 exit ~3550 — approach across the line); cue behavior while inPit (should cues be suppressed — judge); toggle race (coachingEnabled flipped mid-session — controller built per session: stale coaching state?); zone refresh on PB replace mid-lap (partial state); voice: utterance spam on rapid corner chains (C8→C9), speech while backgrounded, expo-speech failure path; UI: fixed-slot claim (does the strip REALLY not shift layout when cue toggles), severity color accessibility on amber; distance countdown slew at 1 Hz; observed-speeds asset validation against a future CORNER_ANALYSIS_VERSION bump changing corner ids (staleness guard?); safety posture: any copy that could read as instruction rather than advisory.

MUST NOT: modify any file. OUTPUT: first line PASS/FAIL/PASS_WITH_NOTES; findings by severity with file:line + on-track scenario; final line SAFE TO SHIP or list of blockers.
