# Review ticket P4h-REV2 / P4i-REV1 — Codex read-only cross-review (bounded)
Commit 74a21e9 (diff vs 0009720). Read-only; do not run tests (report from reading). Output: HIGH / MEDIUM / Test gaps / Clean, file:line, concrete failure scenarios. Two parts:
A) Re-verify the 10 findings of `.foreman/scratch/p4hrev1-codex-output.log` (6 HIGH, 4 MEDIUM) against the fix: for each, FIXED / PARTIAL / NOT FIXED with evidence. Pay attention to: allow-once interceptor re-arm after a rejected cancel; PreflightScreen single watcher lifetime across push/blur/unmount and Continue-anyway; lifecycleGeneration vs stopShared concurrency; G refcount on every session start/stop path incl. recovery and delete-all; calibration cancel-during-start under the lifecycle lock.
B) Review P4i (contracts.md addendum "DID sweep — results persistence, export & candidate filtering", binding): apps/mobile/src/persistence/didSweep{Schema,Store}.ts, apps/mobile/src/session/{didSweepController,didSweepExport}.ts, apps/mobile/src/ui/screens/DidSweepScreen.tsx, packages/core/src/telemetry/enet/{didCandidates,didObservationPhases}.ts. Focus: persistence batching vs stop/kill data loss; resume correctness (never re-send a visited DID, accumulator restore, retention 5 runs); adapter reservation and keep-alive/TesterPresent cadence during the guided phases (one connection, phases sequential, no leaked socket on stop/early-stop/unmount); hard read-only whitelist untouched; export never throws on web; candidate filter correctness (ASCII-like, length bounds, changed-per-phase ranking); 360pt layout risks.
