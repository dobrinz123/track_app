# Ticket P4k — guided/batched/focused observation: settle window at phase transitions, tests first, small
Source: field test 4 (data/field/sweeps/2026-08-29-test4-ecu29-0x5000-0x58F2.json). HEAD b3e9ade (+data commits).
FIELD FACT: ECU 0x29 DID 0x500C read 0x04 in baseline/throttle and 0x05 in 3/5 samples of the BRAKE phase; the FIRST sample of the STEERING phase (tMs 312) was still 0x05 because the driver's foot was still on the brake when the phase switched → computeDidCandidateSummaries ranked it `changedInSeveral` instead of `brakeCandidate`. Same pattern: DME 0x5422 single 0x00 at tMs 815 of the steering phase.
W1 Add a SETTLE WINDOW to the observation runner: after each phase prompt is shown, samples taken during the first `SETTLE_MS = 1500` ms of every phase (except baseline, which needs none) are recorded (they stay in the export/persisted samples with a `settling: true` flag or are simply tagged in the summary input) but EXCLUDED from the per-phase change evaluation. The count guarantee (≥ N positive samples per DID per phase) must count only non-settling samples, so phases extend by the settle time — keep the 3× cap. Pure function in packages/core (e.g. `partitionSettlingSamples(samples, settleMs)` or a `settleMs` option on computeDidCandidateSummaries / computeDidBlockCandidateSummaries) + controller wiring in apps/mobile/src/session/didSweepController.ts (guided, batched, focused flows). Screen: the phase prompt shows "Get ready…" during the settle window (existing countdown text is fine to reuse).
W2 Regression tests from the field shape (red first): 0x500C series exactly as above → `brakeCandidate` with settle 1500 ms; without settle → `changedInSeveral` (documents the old behaviour). 0x5422-like single-sample-at-start → `static` with settle. A DID that changes at tMs 3000 of the steering phase still → `steeringCandidate`. Count guarantee test: 5 samples required, 2 of them settling → phase keeps sampling until 5 non-settling.
W3 Export: `schemaVersion` stays 2 if samples are unchanged in shape; if you add a `settling` flag per sample, bump to 3 and note it in didSweepExport's schemaNote. Persisted samples unchanged in schema unless needed (prefer recomputing the flag from tMs at summary time — no schema change).
Gates (real exit codes, never piped through grep): `pnpm -w typecheck`, `pnpm -w test`, `pnpm -w lint`, and `npx expo export --platform ios` in apps/mobile all 0. Report the test totals (core + mobile).
WRITE SET: packages/core/src/telemetry/enet/didObservationPhases.ts (+ didObservationBatches.ts only if the count plan needs it), packages/core/test/telemetry/enet/**, apps/mobile/src/session/didSweepController.ts (+ its tests), apps/mobile/src/ui/screens/DidSweepScreen.tsx (prompt only). No commit, no agents, no Expo dev server. Report DONE / DONE_WITH_CONCERNS / BLOCKED with evidence (red→green output excerpts) + totals.
