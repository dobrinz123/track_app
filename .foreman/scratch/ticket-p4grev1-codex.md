# Review ticket P4g-REV1 — Codex read-only cross-review: field revision (adapter-switch lifecycle, pedal PID, monitor rows, hidden developer mode)
Adversarial READ-ONLY reviewer. Diff: `git diff 5ea99b4..d56c6bc`. Binding: "Telemetry addendum — field revision" (contracts.md, end). Field context + scout root cause: `.foreman/scratch/ticket-p4g-field-revision.md`. Read-only Node/Git path if spawning is denied.
Attack list (file:line + concrete failing scenario):
1. Adapter-switch lifecycle (`apps/mobile/src/session/telemetryProvider.ts`): reproduce mentally the FIELD sequence (ELM start against the ENET adapter fails async with the socket open → user switches to ENET → Start). Is the old ELM socket guaranteed closed before the ENET connect? Fingerprint compare correctness (host/port defaults, trimmed strings, simulate flag); `start()` while running with a HEALTHY polling generation and unchanged fingerprint → must remain a no-op (ELM byte-identical); settings watcher: `applyingDiscoveryResultUpdate` guard races (discovery persisting host while the user edits); retry timer + fingerprint abort; 200 ms force-close race — can it close a NEWER generation's transport (the worker says it fixed a shared-field bug — verify generation scoping); `running`/`stopping`/reservation/discovery flags after each path; double Start; Start during `stopping`.
2. Hidden developer mode: routes now registered in release — can a non-dev user reach DidProbe/DidSweep without the flag (deep link/navigation param)? Is the read-only whitelist + reservation still the only path to the wire from those screens? `registerDevTap` window logic; hydration repair; DevReplay still `__DEV__` and its fixtures not pulled into release by the new imports (check RootNavigator imports).
3. Pedal PID 0x49: decoder formula/rounding, channel added everywhere exhaustive switches exist (simulators, LapDetail chart map non-chartable placeholder — any UI regression?), ENET default spec + rate, ELM poll plan — both adapter types poll it; monitor labels.
4. G rows: `summarizeGForceSamples` correctness (rate estimate, staleness → rows hidden), no impact on the driving dashboard.
5. Tests: substantive? which of the above lack coverage?
OUTPUT: first line PASS / FAIL / PASS_WITH_NOTES; findings by severity with file:line + scenario + evidence; Clean list. Stdout only. No agents.
