TASK: Wire Phase 4 P4a telemetry into the TRACE mobile app (apps/mobile): TCP transport, session-scoped TelemetryProvider, SQLite recording, settings, and ONE minimal live-values screen. Binding spec: "Telemetry addendum" in docs/architecture/contracts.md (read first). Keep it LEAN — no gauges, no charts, no polish; that is Phase 4b.

EXPECTED OUTCOME:
(a) apps/mobile/src/session/tcpObdTransport.ts — ObdTransport impl over react-native-tcp-socket (ADD this dependency to apps/mobile package.json; lazy-import the native module inside connect() following the exact pattern voiceCoach.ts uses for expo-audio/expo-speech so vitest never loads it). connect() with a 5s timeout; onData decodes bytes as ascii; onClose wired to socket error/close.
(b) apps/mobile/src/session/telemetryProvider.ts — createTelemetryProvider({settingsStore, monotonicNow}): when settings.telemetryEnabled, builds transport (TcpObdTransport with settings.adapterHost/adapterPort, OR SimulatedElm327Transport from @circuit/core when settings.telemetrySimulate is true), creates Elm327Session (default config, pollPlan: rpm 10Hz, speedKph 5Hz, throttlePct 5Hz, coolantC 0.5Hz), exposes start(), stop(), onSample(cb), onStateChange(cb), getDiagnostics(). Reconnect policy: on 'failed', ONE retry after 3s, then stay failed until next session (keep it simple). MUST NOT interact with lap timing in any way.
(c) Recording per addendum: new table telemetry_samples (session_id, lap_number NULLABLE, t_mono_ms, channel, value), TELEMETRY_SCHEMA_VERSION migration following the existing migration pattern in the persistence layer; batched inserts (25 samples or 1s, whichever first), flush on lap crossing and endSession through the same flush discipline the session controller already uses (find lapPersistenceTail / flush with Promise.all in composition — mirror it, do not refactor it); hard cap 200000 rows/session then stop recording and set diagnostics flag capReached.
(d) Settings: add telemetryEnabled (default false), telemetrySimulate (default false, dev-only UI), adapterHost (default '192.168.0.10'), adapterPort (default 35000) to the settings store + migration + Settings screen section "TELEMETRY (OBD)" with the same visual language as existing sections; copy must say advisory/experimental, English.
(e) apps/mobile/src/ui/screens/TelemetryScreen.tsx — minimal: connection state line, one row per channel (label + last value + observed Hz), stop/start button; register route; entry point = a row in the Settings telemetry section ("Open telemetry monitor"). Match the app's premium dark style (see CoachStrip.tsx / SettingsScreen.tsx for tokens) but keep layout simple.
(f) app.json: add NSLocalNetworkUsageDescription to ios.infoPlist explaining the local adapter connection (English, mirrors existing location string tone).
(g) Session integration: provider starts/stops with the session lifecycle in composition.ts (start when session starts and telemetryEnabled; stop on endSession/dispose). Samples flow to the recorder. A dead/absent adapter must leave sessions 100% functional (assert in a test).

CONTEXT: Repo D:\CODE\APLICTIE_Circuit, npm workspaces. Core engine already merged at packages/core/src/telemetry (createElm327Session, SimulatedElm327Transport, contracts). Study apps/mobile/src/session/voiceCoach.ts (lazy native imports + lifecycle), composition.ts (session wiring, flush discipline), settingsStore.ts (settings + migration pattern), the persistence layer for SQLite migrations, test/support/sqlJsDatabase.ts for DB tests. Mobile tests: vitest, node env, test/**/*.test.ts, NO react component tests (logic modules only) — do not add a renderer.

CONSTRAINTS: Only ONE new npm dependency (react-native-tcp-socket, current stable major). npm install to update the lockfile. Do not import react-native-tcp-socket at module top level anywhere. All timestamps via the injected monotonic clock already used by the session (find it in composition.ts). English UI copy. Do not touch packages/core at all (its test dir is another worker's write set).

MUST DO:
1. Tests in apps/mobile/test/session/: telemetryProvider lifecycle (start with simulate transport, samples arrive with monotonic timestamps; stop() tears down; 'failed' plus single-retry pinned), recorder batching (25/1s boundaries), flush-on-endSession, 200k cap sets capReached and stops inserts, settings defaults + migration, and the dead-adapter test: session controller flow (use existing test doubles) completes a lap normally with the telemetry provider in 'failed' state.
2. Run from repo root, each captured with REAL exit codes (cmd > log 2>&1; ec=$?): npm run typecheck, npm test, npm run lint, and cd apps/mobile && npx expo export --platform ios. Report all four exit codes; do not use piped greps for gating.
3. Keep the diff tight: no refactors of existing files beyond the minimal wiring points.

MUST NOT: No subagents. No packages/core changes. No extra dependencies. No renaming/moving existing files. Do not touch .github/workflows.

WRITE SET: apps/mobile/src/session/tcpObdTransport.ts, apps/mobile/src/session/telemetryProvider.ts, apps/mobile/src/session/composition.ts, apps/mobile/src/session/settingsStore.ts, the mobile persistence/migration files (additive migration only), apps/mobile/src/ui/screens/TelemetryScreen.tsx, apps/mobile/src/ui/screens/SettingsScreen.tsx, apps/mobile/src/ui/navigation/RootNavigator.tsx, apps/mobile/src/ui/navigation/types.ts, apps/mobile/app.json, apps/mobile/package.json, package-lock.json, apps/mobile/test/session/**.

OUTPUT FORMAT: First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED. Then: file list with brief purpose; the four gate exit codes; test counts; any deviation and why; concerns.
