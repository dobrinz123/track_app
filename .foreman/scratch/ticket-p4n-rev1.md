You are a read-only reviewer. Repo: D:\CODE\APLICTIE_Circuit (npm workspaces). If shell exec is blocked, read files directly. Review the latest commit on main ("P4n: both brake bindings polled…") — files: apps/mobile/src/session/telemetryProvider.ts (resolveBrakeBindingsFromProfile, buildBrakeEnetChannelSpec, buildEnetConfig, brakeBindingsChangedSincePoll, formatBrake*Display), apps/mobile/src/ui/screens/TelemetryScreen.tsx, apps/mobile/src/ui/screens/SignalFinderScreen.tsx (confirm banner hint), packages/core/src/telemetry/signalFinder/targets.ts (Supra brakeSwitch hypotheses), and the tests telemetryProviderBrake*.test.ts. Field facts: brakeSwitch binding = ECU 0x29 DID 0x500C 1 byte (0x04 rest / 0x05 pressed); brakePressure binding = ECU 0x12 DID 0x58B7 1 byte (0 rest, observed max 64). Questions: (1) with BOTH bindings confirmed, does the ENET poll plan contain two entries with the right target addresses, decode kinds and rates, and can one silent entry (0x29) stall the DME entries (per-entry timeout/back-off from P4l-FIX4 still applied to both)? (2) brakePct scaling: observedMin/Max from the binding evidence — what happens when max is 0/null/equal to min, when a live value exceeds max, and is the raw value shown honestly? (3) the boolean decode for 0x500C: rest 0x04, active 0x05 — is "pressed" decided by equality to the active value, inequality to rest, or a bit? A rest byte that changes with the engine (like 0x4002: 0x01 off / 0x83 running) must not read as pressed — is that guarded for the confirmed 0x500C (whose rest is stable) and documented for others? (4) the "Stop → Start to apply" hint: set correctly, cleared after restart, not shown when nothing changed? (5) any new exhaustive Record/union misses (typecheck passes, but runtime lists like the monitor's channel order)? Output: numbered findings (HIGH/MEDIUM/LOW, file:line, one-line fix), then "NEW: n HIGH, m MEDIUM". No edits.
