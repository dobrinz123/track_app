# Ticket P4o-FIX1 — Codex P4o-REV1 (1 HIGH + 3 MEDIUM + 2 LOW), tests first — Sonnet
Source: .foreman/scratch/p4orev1-codex-output.log (final list). HEAD = P4o commit. No build until this lands with 0 HIGH.
V1 (H1 + M2) Level counting for the two-level cap must be robust AND not collapse staircases: cluster the SCORED series values (press + release + baseline) with a DIAMETER bound around a representative (a new cluster starts when a sorted value is more than `floor` away from the current cluster's FIRST value — no chaining), and count only clusters with SUPPORT ≥ 2 samples (or ≥ 10 % of samples, whichever is larger). `twoLevel` = supported clusters ≤ 2. Tests: (a) noisy two-state `[0×10, 10×8, 14×1]` → 2 levels (14 unsupported) → capped; (b) staircase `0..12` step 1 with floor 3 → ≥ 3 clusters → not capped; (c) field 0x58B7 press values 0x1A/0x30/0x36/0x40… → not capped; (d) 0x4002 0x83/0x9B → capped; (e) boolean targets never capped.
V2 (M3) A fresh find() (new session id) resets `replacedBindings: []`; export `replaced[]` reflects only this session's replacements. Test.
V3 (M4) Engine-running detection uses rpm samples that are FRESH (≤ 5 s old by monotonic clock) AND from an active telemetry session; absent/stale → "not detected" wording (never "off"); an old positive rpm must not suppress the warning. Test the pure predicate with ages 1 s / 10 s / no sample.
V4 (L5) `stop()` and `nextRound()` clear `pendingReplace`. Test.
V5 (L6) fieldFixtures.test.ts replays the two 2026-08-30 exports (data/field/signal-finder/2026-08-30-brakePressure.json and …-brakePressure-2.json) through the scorer: 0x12/0x58B7 (analog target) → `found`, 0x12/0x4002 (analog target) → capped `two-level`, and from …-brakeSwitch.json 0x29/0x500C (boolean target) → `found` uncapped.
Gates (real exit codes, repo root): `npm run typecheck`, `npm run test`, `npm run lint`, `npx expo export --platform ios` in apps/mobile — all 0. Report totals + red→green per item.
WRITE SET: packages/core/src/telemetry/signalFinder/{scoring,targets}.ts, apps/mobile/src/session/{signalFinderController,signalFinderExport}.ts, apps/mobile/src/ui/screens/{SignalFinderScreen.tsx,signalFinderStrings.ts}, tests (+ fixtures read via fs). No sweep/observation/reservation/telemetryProvider changes. No commit, no agents, no Expo dev server.
