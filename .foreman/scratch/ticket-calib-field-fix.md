TASK: Fix the field calibration failure (user drove TMR; calibration stalled at 81-90% coverage every lap). Root cause diagnosed by LEAD: systematic lateral mismatch between the OSM centerline (unvalidated on-site, confidenceNotes admit it) and real driven positions — a contiguous stretch of track falls outside corridorWidthM=15m every lap, so its 25m coverage bins never fill; acceptance needs >=0.95 coverage AND longest gap <=100m AND rejectedFraction <=0.3, which makes success IMPOSSIBLE on that geometry. Designs below are BINDING.

BINDING DESIGNS (packages/core/src/calibration/calibration-engine.ts unless stated):
D1 Two-phase, bias-adaptive coverage:
  - feed(): keep the existing tight-corridor acceptance for `acceptedPoints`/direction/lateral stats UNCHANGED, but ALSO retain every quality-ok sample whose |lateralM| <= LEARN_WIDE_CORRIDOR_M = 40 in a new `wideSamples` list (store {distanceM, lateralM, unwrappedProgressM, localPoint}; cap at MAX_ACCEPTED_POINTS, same overrun flag semantics).
  - finish(): estimate bias as today (from tight-accepted points; if tight set is very small — < 50 points — fall back to median-lateral-based bias from wideSamples). Then RE-project the wideSamples with the bias applied (subtract bias in local frame, re-project onto centerline via the existing projectOntoPolyline) and recompute coverage + lateral stats from the bias-corrected samples that now fall within corridorWidthM. Coverage/gap/lateral acceptance runs on the BIAS-CORRECTED coverage. Direction and rate logic unchanged.
D2 Acceptance relax (post-correction): INSUFFICIENT_COVERAGE below 0.85 (was 0.95); COVERAGE_GAP above 250m (was 100m); POOR_GNSS above 0.5 rejected fraction COMPUTED AGAINST the wide-corridor accept set (a tight corridor must not masquerade as bad GNSS). Confidence formula unchanged (it naturally scores lower coverage/looser lateral lower).
D3 Failure diagnostics for the driver (mobile): when calibration FAILS, CalibrationResultScreen must show, in plain English: coverage percent, the longest uncovered stretch as "No signal match between X.X km and Y.Y km from start/finish" (compute gap start/end distanceM in the engine and expose in diagnostics as uncoveredGapStartM/uncoveredGapEndM — additive fields), and the top 3 rejection reasons with counts. Keep the premium-dark styling; copy advisory in tone.
D4 CALIBRATION_ENGINE version/compat: no schema/persistence changes expected; if any pinned diagnostics test breaks, update pins with the new semantics — never weaken D1/D2 to keep an old pin.
D5 Tests (packages/core/test/calibration/, extend existing):
  a. Constant-offset scenario: synthetic clean lap along the TMR runtime profile centerline displaced laterally by +12m constant + 1.5m gaussian noise (seeded) -> old code fails (assert would fail pre-fix), new code ACCEPTS with estimated bias ~12m (+-2m) and coverage >= 0.95 post-correction.
  b. Sectional-offset scenario: offset 18m over a contiguous 600m stretch, 5m elsewhere -> ACCEPTS via wide corridor + bias, coverage >= 0.85, confidence reflects the looseness.
  c. Genuinely-bad scenario stays rejected: a lap missing a real 700m of samples (no GPS at all there) -> INSUFFICIENT_COVERAGE + COVERAGE_GAP still fire; uncoveredGapStartM/EndM point into the missing stretch.
  d. Rejected-fraction semantics: tight-corridor rejections alone (all within 40m) do not trigger POOR_GNSS.
D6 Do NOT change corridorWidthM in the TMR asset (runtime matching keeps 15m + appliedBias as today) and do NOT touch lap-timing/matcher code paths.

CONTEXT: Repo D:\CODE\APLICTIE_Circuit. Read calibration-engine.ts fully first; TrackMatcher and projectOntoPolyline are in packages/core/src (find them); UI: apps/mobile/src/ui/screens/CalibrationResultScreen.tsx (+ ActiveCalibrationScreen shows live percent — leave live percent semantics as-is: it may keep showing tight-corridor coverage during the lap; note in a comment that final acceptance may exceed it after bias correction, and ALSO surface a small live "signal OK / weak" line if trivially available — optional, skip if not clean).
CONSTRAINTS: Pure TS in core; deterministic seeded tests; no new deps; house test style (fake data builders exist in test/support — reuse).
MUST DO: Gates from repo root, REAL exit codes (cmd > log 2>&1; ec=$?): npm run typecheck, npm test, npm run lint, cd apps/mobile && npx expo export --platform ios. Report all four + test counts.
MUST NOT: No subagents. No changes outside packages/core/src/calibration/**, packages/core/src/controller/** (ONLY if diagnostics plumbing requires passing new fields through), packages/core/test/**, apps/mobile/src/ui/screens/CalibrationResultScreen.tsx, apps/mobile/test/**. Do not touch hardware/, firmware/, telemetry, voice.
WRITE SET: as in MUST NOT allow-list.
OUTPUT FORMAT: First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED. Then: per-design confirmation with the pinning test; the pre-fix-fails evidence for D5a (run the new test against the old logic mentally or via a temporary revert and state what failed); four gate exit codes + counts; deviations/concerns.
