TASK: Live track-map discrepancy visual on the calibration screen — the driver must SEE where the app thinks they are vs where GPS puts them, to diagnose OSM-centerline mismatches on-site. (Field context: user was told "off track" while on the circuit; calibration stalled 81-90% — systematic lateral mismatch confirmed. The bias-adaptive calibration fix landed just before this ticket; build on its current state of the engine/screens.)

BINDING DESIGN:
V1 Rendering: pure View-based (NO react-native-svg, no new deps — same philosophy as the LapDetail sparklines). New component apps/mobile/src/ui/components/TrackMapView.tsx: renders inside a fixed-aspect container (a) the circuit outline as ~200 decimated centerline points (tiny 2-3px absolutely-positioned dots, muted color), (b) a start/finish marker (small amber tick at distance 0), (c) the RAW GPS position dot (amber, 8px), (d) the MATCHED/projected-on-centerline position dot (green when the live on-track indicator says on-track, red otherwise, 8px), (e) a thin implied line is NOT needed between them — proximity is the signal. Fit: compute bounding box of the centerline once, scale/translate into the container preserving aspect; positions update via props at the screen's existing state-update cadence (no timers of its own).
V2 Data plumbing (lean): pure helper apps/mobile/src/session/trackMapModel.ts — (a) decimateCenterline(centerline, n=200) -> local-frame points, computed ONCE per profile (memo); (b) mapping from facade calibration state to marker positions. Core side: extend the calibration progress()/facade calibration state with additive fields {rawLocalX, rawLocalY, matchedLocalX, matchedLocalY, lateralM, distanceM} for the LAST fed sample (packages/core: calibration-engine keeps the last sample's projection — it already computes match + local point; expose, do not recompute). Controller/facade plumb the additive fields through the existing calibration state object (additive only — no breaking shape changes; update pinned tests additively).
V3 On-screen info row under the map (ActiveCalibrationScreen): "Offset: NN m" (lateralM, 1 decimal, red when > corridorWidthM), "Position: N.N km" (distanceM/1000 from S/F). Keep the existing progress UI untouched above it; map goes in the empty lower area (fixed height ~55% of width, no reflow of existing elements).
V4 Circuit outline source: the runtime profile's centerline in its local frame — find how composition/core exposes the RuntimeProfile to the mobile side (controller deps); add a minimal exported accessor if none exists (single function, additive).
V5 Tests: decimateCenterline (count, endpoints preserved, deterministic); marker mapping math (known centerline + known sample -> expected container coordinates, aspect preserved); additive core fields pinned (last-sample projection values for a fed synthetic sample); no test renderer for the .tsx (house rule) — all logic in the pure model module.

CONTEXT: Repo D:\CODE\APLICTIE_Circuit. Read first: packages/core/src/calibration/calibration-engine.ts AS IT NOW IS (bias-adaptive fix just merged), apps/mobile/src/ui/screens/ActiveCalibrationScreen.tsx, how facade exposes state.calibration (realFacade/controller), LapDetailScreen's sparkline styling for visual language. English UI copy, advisory tone.
CONSTRAINTS: No new deps. Surgical diffs. The map is a CALIBRATION screen feature only (not the driving dashboard). Performance: position dots re-render must not re-render the 200 outline dots (memoize the outline layer).
MUST DO: Gates from repo root with REAL exit codes (cmd > log 2>&1; ec=$?): npm run typecheck, npm test, npm run lint, cd apps/mobile && npx expo export --platform ios. Report all four + counts.
MUST NOT: No subagents. No changes outside: packages/core/src/calibration/**, packages/core/src/controller/** (additive plumbing only), packages/core/test/**, apps/mobile/src/session/trackMapModel.ts (new), apps/mobile/src/ui/components/TrackMapView.tsx (new), apps/mobile/src/ui/screens/ActiveCalibrationScreen.tsx, apps/mobile/test/**. Do not touch dashboard, telemetry, voice, hardware, firmware.
OUTPUT FORMAT: First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED. Then: per-design confirmation + pinning tests; four gate exit codes + counts; deviations/concerns.
