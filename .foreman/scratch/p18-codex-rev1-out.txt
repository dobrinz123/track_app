OpenAI Codex v0.153.4
--------
workdir: D:\CODE\APLICTIE_Circuit
model: gpt-6-astra
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a0cf01-3d11-7d61-981a-6b4a833d91ab
--------
user
# Review ticket P18-REV1 — Codex read-only cross-review: delete-all device wipe (PR #2) + Signal Finder early wake (PR #3)
Adversarial READ-ONLY reviewer. Both changes are merged on `main`; `git pull` first. Full diff: `git diff dd84864..edb7bd5`. The two parts separately:
- A (delete-all device wipe, PR #2): `git diff dd84864..ae12a10`
- B (Signal Finder early wake, PR #3): `git diff dd84864..8c495b6`
Context: `docs/HANDOFF-2026-09-23-cloud.md` explains both changes and the tests added.
Binding: HANDOFF.md item 1 (a VIN is personal data; delete-all must remove it), docs/legal/privacy-policy.*.md §3.3 and §7 as changed in A, compliance-checklist 3.5. Read-only Node/Git path if spawning is denied.
Attack list (file:line + concrete failing scenario):
1. Completeness (A): list every table and every `settings` key the app writes (grep `INSERT`, `INSERT OR REPLACE`, `INSERT OR IGNORE`, `runAsync` under `apps/mobile/src` and `packages/core/src`). Name each one that holds driver- or vehicle-identifying data and survives `deleteAllStoredUserData()`. Check whether the policy text in A claims more than the code deletes, or less.
2. Transaction and verify (A, `deviceDataWipe.ts`): does `scrubStoredSettings` race `SqlSettingsStore.persist()`, which writes the whole in-memory blob asynchronously? Can a later persist write the VIN back? Look for an `update()` that runs between the wipe and the in-memory reset, including the VIN detector at `composition.ts` ~1037 finishing a read during delete-all. Does the verify step prove anything the DELETE could not have silently skipped?
3. Learned circuits (A): `DELETE ... WHERE circuit_id NOT IN (SELECT circuitId FROM sessions)`: confirm `sessions.circuitId` really can never be NULL on a migrated database (a NULL would make NOT IN delete nothing). An adoption in flight (`adoptionProgress`, `testLoopProvider`, journal) during delete-all. The selection fallback through `unlockedApplySelection` while holding `lifecycleLock`. What `circuitCatalog.list()` returns to the per-circuit verify loop after the learned registry is republished.
4. In-memory reset (A, `forgetDeviceUserDataInMemory`): it runs even when the on-disk wipe threw. Is there any path where memory says "gone" but disk still has the VIN and the UI reports success? `vinDetectionState` stays `'done'` with `cachedDetectedVin = null`: can the Signal Finder or VIN auto-select then misbehave until restart? Does the active-profile subscription fire a bindings refresh that races the wipe?
5. Tests (A): four composition test doubles gained `migrateDidSweepSchema`. Did that hide a real production path where the tables do not exist (web preview, first launch, a failed migration)? `wipeDeviceUserData` rejects on a missing table: is that the right failure for those paths, and is it reported rather than swallowed?
6. Runner loop (B, `runner.ts` ~583–592): can the new `while` spin or overshoot? Cases: `wakeAtMs` in the past, `endAtMs < boundaryMs`, `control.stopped` set mid-sleep, `control.paused`, and a clock that never advances (fake clock). Does `Math.max(1, …)` add up to a real delay per pass? Is the 2%-slow-clock test a fair model of the production failure, or does it pass for a different reason?
7. Anything else in either diff that changes behaviour the PR descriptions do not mention.
OUTPUT: first line PASS / FAIL / PASS_WITH_NOTES; findings by severity with file:line + scenario + evidence; Clean list. Stdout only. No agents.

codex
I’ll review both diffs and the binding documents, then trace the wipe and runner paths against the attack list. I’ll use read-only commands and no agents. The environment prohibits writes, so I can’t run `git pull`; I’ll check the local checkout and report that limitation.

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "git status --short; git log -5 --oneline; rg --files -g AGENTS.md -g HANDOFF.md -g '*privacy-policy*' -g '*compliance*' -g '*HANDOFF-2026-09-23*'" in D:\CODE\APLICTIE_Circuit
 succeeded in 170ms:
docs\legal\privacy-policy.ro.md
docs\legal\privacy-policy.en.md
docs\legal\compliance-checklist.md
docs\HANDOFF.md
docs\HANDOFF-2026-09-23-cloud.md
apps\mobile\AGENTS.md

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'git diff dd84864..edb7bd5 --stat; git diff dd84864..ae12a10 -- apps/mobile/src packages/core/src; git diff dd84864..8c495b6 -- packages/core/src; Get-Content apps/mobile/AGENTS.md; Get-Content docs/HANDOFF-2026-09-23-cloud.md' in D:\CODE\APLICTIE_Circuit
 succeeded in 47ms:
# Expo SDK 57

This app is on Expo SDK 57 (`expo` in `package.json`), which is newer than most training data, so recalled Expo and React Native APIs may be out of date. When a change touches an Expo or React Native API, check it against the versioned docs at https://docs.expo.dev/versions/v57.0.0/ instead of relying on memory.
# Handoff: cloud session, 2026-09-23

For the local lead session. Read `docs/HANDOFF.md` first, then this.

All of the cloud session's work is on `main`. **`git pull` brings in everything.** No build has been made,
nothing is installed, and nothing has been seen on the phone. The cloud session did not have Codex, so none
of this has had the cross-family review that HANDOFF rule 1 requires before a build. That review is the
first thing to do (see "What to do next").

---

## What is on `main`

The session's commits start after `966887d` ("Handoff brief for a fresh session"). To see everything it
changed:

```
git pull
git log --oneline 966887d..HEAD
git diff 966887d..HEAD --stat
```

There are three pieces of work. Only 2 and 3 change code.

| # | What | Where | Merged as |
|---|---|---|---|
| 1 | Prompt audit of the agent instruction files | docs, `apps/mobile/AGENTS.md`, `.claude/settings.local.json` | PR #1 + `dd84864` |
| 2 | Delete-all now clears the VIN and the vehicle data | `apps/mobile` + legal docs | PR #2 (`ae12a10`) |
| 3 | Signal Finder round no longer ends on an early timer wake-up | `packages/core` | PR #3 (`8c495b6`) |

---

## 1. Prompt audit (documentation and config only)

The report and the patch it proposed are in `docs/audits/prompt-audit-2026-09-23.md` and `.patch`. They
were applied as follows:

- **`apps/mobile/AGENTS.md`** (loaded on every turn through `apps/mobile/CLAUDE.md`)
  - Before: "Expo HAS CHANGED, read the docs before writing any code".
  - Now: it says why (Expo SDK 57 is newer than most training data) and applies only to changes that touch
    Expo or React Native APIs.
- **`docs/HANDOFF.md`**
  - "Read these before doing anything" now reads "Before changing an area, read the documents that cover it".
  - A pressure-only sentence under rule 1 is removed; the rule and its reason are unchanged.
- **`docs/NEXT-CIRCUIT-PLAYBOOK.md` Â§0**
  - The reference to a `/fable-foreman` skill that doesn't exist is removed, and so is "Sonnet-class workers".
    The lead / worker / cross-reviewer process stays.
  - "Read this before touching anything" is scoped to the area being changed.
  - "(non-negotiable process)" is removed from the heading.
- **`docs/roadmap/phase-5-llm-corner-analysis.md`**: a status note at the top says the LLM plan (Â§3 step 4)
  and the backend decision were replaced by the deterministic engine. The file used to say so only at the
  very bottom.
- **`.claude/settings.local.json`**: 14 one-off permission entries are removed (absolute Windows paths, one
  specific commit message, echo/cp/awk one-liners). **Check that your local allowlist still has what you
  use.**

---

## 2. Delete-all now clears the VIN and the vehicle data (HANDOFF item 1)

### The problem

"Delete all my data" (Settings â†’ DATA) wiped the session tables through `@circuit/core`'s `deleteUserData`
and the mobile `telemetry_samples` step. It never touched:
- the VIN, `settings.lastSeenVin` inside the `app-settings` row (in the EU a VIN is personal data);
- `vehicle_profile_bindings`, `signal_finder_ruled_out` and `did_sweep_runs` / `_responders` /
  `_observation_samples` / `_observation_summaries`;
- the per-session vehicle snapshots, stored as `settings` keys `vehicle-profile-snapshot:<sessionId>`;
- `learned_circuits` (the geometry of circuits the driver taught the app).

### The fix

- **New `apps/mobile/src/persistence/deviceDataWipe.ts`:**
  - `wipeDeviceUserData(db)` runs inside ONE transaction:
    1. `DELETE` from the six vehicle tables, then counts each one.
    2. `DELETE FROM learned_circuits WHERE circuit_id NOT IN (SELECT circuitId FROM sessions)`. A learned
       circuit that a remaining session still uses is kept, so that session can still be analysed and
       replayed. It is counted as "remaining", so the wipe reports failure.
    3. Deletes every `vehicle-profile-snapshot:*` settings key.
    4. Rewrites the `app-settings` row without the VIN, then reads it back to confirm. If the row can't be
       parsed at all, it is deleted, because it might still hold a VIN.
    5. Returns `{ ok, remaining }`, where `remaining` lists only what was left behind.
  - `vehicleIdentityResetPatch(settings)` builds the settings patch. It always sets `lastSeenVin: null`. It
    also resets `activeVehicleProfileId` / `activeVehicleProfileSource` to their defaults **only when the
    source is `'vin'`**: a profile the VIN picked reveals the car's make and model. A profile the user
    chose is a preference and stays.
- **`apps/mobile/src/session/composition.ts`, `unlockedDeleteAllStoredUserData()`:**
  - Calls the wipe after the telemetry step. Like the telemetry step, it always runs, even when an earlier
    step failed.
  - A failure makes the overall `ok` false and adds "vehicle or learned-circuit data remained or could not
    be deleted" to `errorText`. The Settings screen therefore shows the error, not the success banner.
  - Then `forgetDeviceUserDataInMemory()` clears the in-memory copies:
    - the settings store (same patch, so the next `persist()` can't write the VIN back);
    - `cachedDetectedVin`, `vehicleProfileBindingsCache` and `sessionVehicleSnapshots`;
    - the learned circuits: `memoryLearnedCircuits` is emptied, then the store is refreshed and the catalog
      republished;
    - if the selected circuit no longer exists, the selection falls back to TMR through
      `unlockedApplySelection` (we are already inside `lifecycleLock`).
  - The circuit fallback is wrapped in try/catch, so it can never turn delete-all into an error the UI
    can't handle.
- **Small exports:** `SETTINGS_KEY` from `sqlSettingsStore.ts`, `SESSION_VEHICLE_SNAPSHOT_KEY_PREFIX` from
  `sessionVehicleSnapshot.ts`.

**What delete-all keeps, on purpose:** units, language, adapter address and port, the coaching / voice /
suggestion toggles, the selected circuit, and a vehicle profile the user chose. None of these identify the
driver or the car. The test-loop adoption journal (`testLoopAdoption`) also survives. It holds only a
circuit id and a session id, and the next launch cleans it up.

### Documentation changed with it

- `docs/legal/privacy-policy.en.md` and `.ro.md`: Â§3.3 (VIN) and Â§7 (right to erasure) now say what
  delete-all removes and what it keeps. The old "known limitation" text is gone.
- `docs/legal/compliance-checklist.md`: item 3.5 is DONE.
- `docs/HANDOFF.md`: item 1 is marked fixed.

### Tests

- **New `apps/mobile/test/persistence/deviceDataWipe.test.ts`** (8 tests, real SQLite via sql.js):
  - the full wipe;
  - preferences and unrelated settings keys are kept;
  - a learned circuit that a session still uses is kept and reported;
  - an empty device;
  - an unreadable settings row;
  - a missing table rolls back and rejects;
  - the two `vehicleIdentityResetPatch` cases.
- **New end-to-end test** in `apps/mobile/test/session/composition.circuitSelection.test.ts` ("HANDOFF
  release blocker: â€¦"):
  - Sets up a VIN, a binding, a sweep run, a learned circuit and a snapshot, then calls
    `deleteAllStoredUserData()` and checks memory and disk.
  - **Verified to fail without the `composition.ts` change** (`expected 'WZ1DB0C04LW000001' to be null`).
- **Four composition test files** (`circuitSelection`, `facadeBoundary`, `gForceOwnership`,
  `telemetryRecording`) now call `migrateDidSweepSchema`, as `openAppDatabase()` does in production. Their
  databases never had those tables. Before this change their six delete-all tests failed with
  `no such table: vehicle_profile_bindings`.

---

## 3. Signal Finder: an early timer wake-up no longer ends the round

### The problem

In `packages/core/src/telemetry/signalFinder/runner.ts`, when every entry is backed off, `runFinderRound`
sleeps with `setTimeout` until the next evidence-window boundary. Then it ends the round if
`clock.now() < boundaryMs`.

The timer and `Date.now()` are separate clocks. A timer can fire a millisecond before `Date.now()` reaches
its target: locally, 22 out of 400 wake-ups did. When that happened, the round ended in the first backed-off
window. A DID that recovered moments later was never sampled again and scored `insufficient`. This is the
same result the Y3 fix (Codex P4m-REV2 finding 13) had removed.

It showed up once in CI: the Y3 test failed on the push run of PR #2 (run 35843788977), and the round ended
after 212 ms of 600.

### The fix

The sleep is now a loop. It keeps waiting (at least 1 ms each time) until the round's own clock reaches
`min(boundary, end of round)`, or until a stop. After that, the existing check decides whether the round
continues. Nothing else in the runner changed.

### Tests

- **New test in `packages/core/test/telemetry/signalFinder/runner.test.ts`** ("Y3: a cooldown sleep that
  wakes before the clock reaches the window boundaryâ€¦"):
  - The clock runs 2 % slower than the timers, so every wake-up is early.
  - Failed 3/3 before the fix (the round ended at ~98 ms). Passes after.
- **Signal Finder suite:** all 169 tests, 5 runs in a row.
- **Other core code that sleeps and then compares the clock:** `didSweep.ts` just keeps looping after an
  early wake-up, so it isn't affected.

---

## Verification done in the cloud (on the merged `main`, `edb7bd5`)

| Gate | Result |
|---|---|
| `npm run typecheck` | 0 |
| `npm run lint` | 0 errors, 6 warnings (the existing `didSweep*` ones) |
| `npm test` | 3407 passed (1787 core + 1620 mobile) |
| `cd apps/mobile && npx expo export --platform ios` | 0 |

In GitHub CI, Gates (typecheck / lint / test) was green on both PRs. License policy and osv-scan are red,
the same way as on `main` before this session (HANDOFF Â§6: leave them, don't widen the policy).

**Not verified:**
- No run on a device. There is no React Native render harness, so the Settings screen itself is not
  covered.
- No Codex review.

---

## What to do next

1. **Pull and re-run the gates locally** on real exit codes (playbook Â§0):

   ```
   git pull
   npm ci
   npm run typecheck > tc.log 2>&1; echo $?
   npm test > test.log 2>&1; echo $?
   npm run lint > lint.log 2>&1; echo $?
   cd apps/mobile && npx expo export --platform ios; echo $?
   ```

2. **Run the Codex review.** The ticket is `.foreman/scratch/ticket-p18-rev1-codex.md`:

   ```
   codex exec --sandbox read-only -C <repo> - < .foreman/scratch/ticket-p18-rev1-codex.md
   ```

   - It reviews `git diff dd84864..edb7bd5` (items 2 and 3 above) and names seven specific things to attack.
   - The review must end with zero HIGH findings before any build (HANDOFF rule 1).
   - If Codex runs out of quota partway through, say so. A partial run is not a pass.
3. **Fix the findings** in one batched fix wave (playbook Â§0), then run Codex again on the fix diff.
4. **Check delete-all on the phone,** if it fits the install budget before Monday's session at MotorPark
   (28 Sep):
   - Delete all data on a phone that has a VIN and a learned circuit.
   - The success banner appears.
   - The Signal Finder shows no VIN.
   - The learned circuit is gone from the list.
   - Units and language are unchanged.
   - Monday's test protocol does not depend on this.
5. **Add a ledger entry** in `.foreman/ledger.md` for this session and the review result. The cloud session
   did not write one.

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "Get-Content apps/mobile/src/persistence/sqlSettingsStore.ts; rg -n 'INSERT|runAsync|CREATE TABLE|settings.*key' apps/mobile/src/persistence packages/core/src/persistence; rg -n 'vinDetectionState|cachedDetectedVin|adoptionProgress|testLoopAdoption|testLoopProvider|refreshVehicleProfileBindingsCache|settingsStore.subscribe' apps/mobile/src/session/composition.ts; Get-Content apps/mobile/src/session/settingsStore.ts; Get-Content docs/legal/privacy-policy.en.md; Get-Content docs/legal/compliance-checklist.md | Select-String -Pattern '3.5' -Context 2,2; Get-Content docs/HANDOFF.md -TotalCount 85" in D:\CODE\APLICTIE_Circuit
 succeeded in 255ms:
apps/mobile/src/persistence\didSweepSchema.ts:17: * `CREATE TABLE IF NOT EXISTS` keeps this idempotent across repeated app
apps/mobile/src/persistence\didSweepSchema.ts:27: * are plain `CREATE TABLE IF NOT EXISTS` additions -- no column was added to
apps/mobile/src/persistence\didSweepSchema.ts:43: * accumulates. Another plain `CREATE TABLE IF NOT EXISTS` addition -- no
apps/mobile/src/persistence\didSweepSchema.ts:56: * untouched for every other. Another plain `CREATE TABLE IF NOT EXISTS`
apps/mobile/src/persistence\didSweepSchema.ts:63:CREATE TABLE IF NOT EXISTS did_sweep_schema_migrations (
apps/mobile/src/persistence\didSweepSchema.ts:67:CREATE TABLE IF NOT EXISTS did_sweep_runs (
apps/mobile/src/persistence\didSweepSchema.ts:85:CREATE TABLE IF NOT EXISTS did_sweep_responders (
apps/mobile/src/persistence\didSweepSchema.ts:99:CREATE TABLE IF NOT EXISTS did_sweep_observation_samples (
apps/mobile/src/persistence\didSweepSchema.ts:113:CREATE TABLE IF NOT EXISTS did_sweep_observation_summaries (
apps/mobile/src/persistence\didSweepSchema.ts:123:CREATE TABLE IF NOT EXISTS vehicle_profile_bindings (
apps/mobile/src/persistence\didSweepSchema.ts:138:CREATE TABLE IF NOT EXISTS signal_finder_ruled_out (
apps/mobile/src/persistence\didSweepSchema.ts:161:    await db.runAsync('INSERT INTO did_sweep_schema_migrations (version) VALUES (?)', [DID_SWEEP_SCHEMA_VERSION]);
apps/mobile/src/persistence\didSweepSchema.ts:163:    await db.runAsync('UPDATE did_sweep_schema_migrations SET version = ?', [DID_SWEEP_SCHEMA_VERSION]);
apps/mobile/src/persistence\expoSqlDatabase.ts:14://   - runAsync(sql, params) -> {changes} -> db.runAsync(sql, params) (also returns
apps/mobile/src/persistence\expoSqlDatabase.ts:21:    runAsync: (sql: string, params: readonly SqlBindValue[] = []) => db.runAsync(sql, params as SqlBindValue[]),
apps/mobile/src/persistence\expoSqlDatabase.ts:58: * so a second store (`SqlSettingsStore`, the v2 `settings` key-value table)
apps/mobile/src/persistence\expoSqlDatabase.ts:78:  // batch INSERT, a trace chunk write, a settings row) for its own duration.
apps/mobile/src/persistence\sessionVehicleSnapshot.ts:83: * With `INSERT OR REPLACE` the recovery call overwrote the snapshot of the
apps/mobile/src/persistence\sessionVehicleSnapshot.ts:91: * `INSERT OR IGNORE` against `settings(key TEXT PRIMARY KEY)` is that rule in
apps/mobile/src/persistence\sessionVehicleSnapshot.ts:109:    await db.runAsync('INSERT OR IGNORE INTO settings (key, value) VALUES (?, ?)', [
apps/mobile/src/persistence\sessionVehicleSnapshot.ts:132:    const rows = await db.getAllAsync<{ value: string }>('SELECT value FROM settings WHERE key = ?', [
apps/mobile/src/persistence\sqlSettingsStore.ts:18: * `SettingsStore` backed by the `settings` key-value table added in the
apps/mobile/src/persistence\sqlSettingsStore.ts:57:    const rows = await db.getAllAsync<{ value: string }>('SELECT value FROM settings WHERE key = ?', [
apps/mobile/src/persistence\sqlSettingsStore.ts:156:    // install from before these settings existed carries no key at all and
apps/mobile/src/persistence\sqlSettingsStore.ts:198:      await this.db.runAsync('INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)', [
apps/mobile/src/persistence\sqlSettingsStore.ts:216: * The `settings` key this log lives under. A SEPARATE key from
apps/mobile/src/persistence\sqlSettingsStore.ts:264:    const rows = await db.getAllAsync<{ value: string }>('SELECT value FROM settings WHERE key = ?', [
apps/mobile/src/persistence\sqlSettingsStore.ts:290:    await db.runAsync('INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)', [
apps/mobile/src/persistence\sqlSettingsStore.ts:307:    await db.runAsync('DELETE FROM settings WHERE key = ?', [UNVALIDATED_MATCHING_KEY]);
apps/mobile/src/persistence\learnedCircuitSchema.ts:10: * repository, and `CREATE TABLE IF NOT EXISTS` throughout so relaunching the
apps/mobile/src/persistence\learnedCircuitSchema.ts:26:CREATE TABLE IF NOT EXISTS learned_circuit_schema_migrations (
apps/mobile/src/persistence\learnedCircuitSchema.ts:30:CREATE TABLE IF NOT EXISTS learned_circuits (
apps/mobile/src/persistence\learnedCircuitSchema.ts:54:    await db.runAsync('INSERT INTO learned_circuit_schema_migrations (version) VALUES (?)', [
apps/mobile/src/persistence\learnedCircuitSchema.ts:58:    await db.runAsync('UPDATE learned_circuit_schema_migrations SET version = ?', [
apps/mobile/src/persistence\sqlWriteGate.ts:9: * awaited `runAsync`, which was queued BEHIND it: a guaranteed self-deadlock
apps/mobile/src/persistence\sqlWriteGate.ts:17: * ALREADY OPEN transaction on the SAME connection: a `runAsync` issued while
apps/mobile/src/persistence\sqlWriteGate.ts:78:    runAsync: (sql, params) => gate.exclusive(() => inner.runAsync(sql, params)),
apps/mobile/src/persistence\telemetryRecorder.ts:40: * ONE parameterized multi-row `INSERT` statement (`writeBatch` below) --
apps/mobile/src/persistence\telemetryRecorder.ts:48: * it, this INSERT included, so the recorder no longer holds the gate itself
apps/mobile/src/persistence\telemetryRecorder.ts:143:   * F1 fix: ONE parameterized multi-row `INSERT` for the whole batch -- no
apps/mobile/src/persistence\telemetryRecorder.ts:144:   * `withTransactionAsync`, no per-row `runAsync` loop (never more than one
apps/mobile/src/persistence\telemetryRecorder.ts:146:   * multi-row `INSERT` is already atomic in SQLite.
apps/mobile/src/persistence\telemetryRecorder.ts:170:    // here as well would queue this INSERT behind its own gate acquisition.
apps/mobile/src/persistence\telemetryRecorder.ts:171:    await this.db.runAsync(
apps/mobile/src/persistence\telemetryRecorder.ts:172:      `INSERT INTO telemetry_samples (session_id, lap_number, t_mono_ms, channel, value) VALUES ${placeholders}`,
apps/mobile/src/persistence\telemetrySchema.ts:12: * repository/settings store. `CREATE TABLE IF NOT EXISTS` keeps this
apps/mobile/src/persistence\telemetrySchema.ts:25:CREATE TABLE IF NOT EXISTS telemetry_schema_migrations (
apps/mobile/src/persistence\telemetrySchema.ts:29:CREATE TABLE IF NOT EXISTS telemetry_samples (
apps/mobile/src/persistence\telemetrySchema.ts:49:    await db.runAsync('INSERT INTO telemetry_schema_migrations (version) VALUES (?)', [TELEMETRY_SCHEMA_VERSION]);
apps/mobile/src/persistence\telemetrySchema.ts:51:    await db.runAsync('UPDATE telemetry_schema_migrations SET version = ?', [TELEMETRY_SCHEMA_VERSION]);
114:import { TestLoopLocationProvider } from './testLoopProvider';
724:export async function refreshVehicleProfileBindingsCache(): Promise<void> {
796:settingsStore.subscribe((settings) => {
800:  void refreshVehicleProfileBindingsCache();
862:let vinDetectionState: VinDetectionState = 'idle';
863:/** The result of the ONE completed attempt this app run has made (or will make) -- `null` before `vinDetectionState === 'done'`, and forever after if the read found nothing usable. */
864:let cachedDetectedVin: string | null = null;
999:  if (vinDetectionState !== 'idle') return cachedDetectedVin;
1008:  vinDetectionState = 'attempting';
1030:  vinDetectionState = 'done';
1031:  cachedDetectedVin = vin;
1063:    void refreshVehicleProfileBindingsCache();
1708:  const provider: LocationProvider = testLoopProvider ?? gnssProvider;
1986:settingsStore.subscribe((settings) => {
2360:let testLoopProvider: TestLoopLocationProvider | null = null;
2379:let adoptionProgress: AdoptionProgress | null = null;
2441:  if (testLoopProvider === null && !testLoopHoldsGForce) return;
2625:    providerAttached: testLoopProvider !== null,
2705:    testLoopProvider = provider;
2739:  const provider = testLoopProvider;
2740:  testLoopProvider = null;
2759:  const provider = testLoopProvider;
2774:    const ledger = (adoptionProgress ??= { circuitId: circuit.profile.circuitId });
2880:    testLoopProvider = null;
2882:    adoptionProgress = null;
3065:    // awaited -- `refreshVehicleProfileBindingsCache()` never throws, and
3067:    void refreshVehicleProfileBindingsCache();
3091:      await refreshVehicleProfileBindingsCache();
3627:  cachedDetectedVin = null;
3648:  await refreshVehicleProfileBindingsCache();
import { TMR_CIRCUIT_PROFILE } from './tmrProfile';

export type SpeedUnits = 'kmh' | 'mph';

/**
 * ENET telemetry addendum (contracts.md, 2026-08-27, binding, Phase 4e). The
 * second OBD transport, next to ELM327: BMW ENET (HSFZ framing over TCP
 * carrying UDS PDUs). Defaults to `'elm327'` -- every existing install (and
 * every field below this one) hydrates unchanged, and the ELM327 path stays
 * byte-identical in behavior regardless of this addendum (`telemetryProvider.ts`
 * only ever builds ENET machinery when this is `'enet'`).
 */
export type AdapterType = 'elm327' | 'enet';

/**
 * Ticket P4l-FIX1 F2 (binding, the P4l worker's own concern 4): the app's ONE
 * language. The exportable Signal Finder summary was already written in both
 * RO and EN (`signalFinderExport.ts`), but the screen hard-coded `'en'`
 * because no language setting existed. Same two-value vocabulary as
 * `@circuit/core`'s coaching `ReportLanguage`, so one setting drives every
 * user-facing text this app renders in more than one language.
 */
export type AppLanguage = 'ro' | 'en';

/**
 * The DEFAULT language for a device locale (BCP-47 tag or a legacy
 * underscore form): Romanian for a `ro` PRIMARY subtag -- `ro`, `ro-RO`,
 * `ro_RO`, `ro-MD` -- English for everything else, including an absent or
 * unreadable locale. Matched on the primary subtag alone, never a prefix, so
 * `roa`/`rom` (other languages that merely start with "ro") stay English,
 * and `hu-RO` (Hungarian spoken in Romania) is Hungarian, not Romanian.
 * Pure: takes the locale rather than reading one, so the rule can be pinned
 * by a test without any native locale API.
 */
export function defaultLanguageForLocale(locale: string | null | undefined): AppLanguage {
  if (typeof locale !== 'string') return 'en';
  const primary = locale.trim().toLowerCase().split(/[-_]/)[0] ?? '';
  return primary === 'ro' ? 'ro' : 'en';
}

/**
 * The device's own locale, read through the `Intl` global (present in Hermes
 * and in Node/vitest -- no new native dependency, and no `react-native`
 * import, so this module stays directly importable by vitest). Any failure
 * -- a runtime built without full ICU, a throwing resolver -- degrades to
 * `null`, which `defaultLanguageForLocale` maps to `'en'`.
 */
export function readDeviceLocale(): string | null {
  try {
    return new Intl.DateTimeFormat().resolvedOptions().locale ?? null;
  } catch {
    return null;
  }
}

export interface CoverageBinsSetting {
  /** Fraction thresholds, ascending, in (0,1), used to bucket calibration coverage for display. */
  thresholds: readonly number[];
}

export interface AppSettings {
  units: SpeedUnits;
  /** Live delta magnitude (ms) below which the dashboard shows neutral/gray instead of faster/slower. */
  deltaDeadbandMs: number;
  coverageBins: CoverageBinsSetting;
  /**
   * Phase 3 coaching addendum toggle. When `false`, `composition.ts` builds
   * every `SessionController` (production AND DevReplay) with coaching
   * disabled -- no `CoachEngine` is instantiated and `FacadeState.coachCue`
   * stays `null` for the whole session, regardless of the TMR corner data
   * being available. Advisory-only either way (contracts.md's Coaching
   * addendum); this only controls whether cues are computed/surfaced at all.
   * Defaults to `true` -- UI rendering of the cue itself is a later work
   * package, this ticket only plumbs the data.
   */
  coachingEnabled: boolean;
  /**
   * Phase 3 coaching addendum sub-toggle (voice cues via `session/voiceCoach.ts`
   * + expo-speech). Only meaningful while `coachingEnabled` is also `true` --
   * `voiceCoach.ts` gates on BOTH fields, never speaks with `coachingEnabled`
   * `false` regardless of this value, and `SettingsScreen` only shows this
   * row when `coachingEnabled` is on. Defaults to `false`: voice is opt-in,
   * unlike the on-screen coach cues themselves.
   */
  voiceCoachEnabled: boolean;
  /**
   * Ticket P5c-B (contracts.md "Phase 5 REVISION 2" R2-3, user-ratified): the
   * opt-in gate for the whole trackday suggestion stage â€” the between-stint
   * pit view, its bounded per-corner suggestions, and the live brake-cue
   * updates between laps. Defaults to `false`, and OFF means OFF everywhere:
   * no lap boundary reads the recording, no cue ever moves, no suggestion is
   * computed or exported, and the pit-view entry point is not shown. Turning
   * it on never lifts a bound â€” a cue may still only move to a point a clean
   * lap of the SAME outing already demonstrated, by at most 10 m / +3 km/h,
   * once per corner per stint. Advice is never shown while driving.
   */
  suggestionsEnabled: boolean;
  /**
   * Telemetry addendum (Phase 4 / P4a, docs/architecture/contracts.md).
   * Advisory/experimental: reads vehicle telemetry (RPM, speed, throttle,
   * coolant temperature) from a local WiFi OBD-II adapter, strictly
   * read-only on the vehicle bus. Defaults to `false` -- opt-in, and never
   * required for lap timing (`composition.ts`'s session integration MUST
   * leave every session 100% functional with this on and the adapter
   * dead/absent).
   */
  telemetryEnabled: boolean;
  /**
   * Dev-only: when `true` (and `telemetryEnabled`), `telemetryProvider.ts`
   * builds a `SimulatedElm327Transport` (`@circuit/core`) instead of the real
   * `TcpObdTransport`, so telemetry can be exercised without a physical
   * adapter. `SettingsScreen` only shows this toggle in `__DEV__`. Defaults
   * to `false`.
   */
  telemetrySimulate: boolean;
  /** WiFi OBD-II adapter host (Telemetry addendum: adapter is a local WiFi AP). */
  adapterHost: string;
  /** WiFi OBD-II adapter TCP port. */
  adapterPort: number;
  /**
   * Telemetry addendum â€” channel revision (2026-08-11, binding). Advanced,
   * vehicle-specific setting: the raw hex OBD/mode-22 request for
   * transmission oil temperature, sent verbatim (no standard mode-01 PID
   * exists for this channel). Decoded as the last response data byte minus
   * 40, same as every other OBD temperature channel. Empty string (the
   * default) means "not configured" -- `telemetryProvider.ts`'s poll plan
   * then omits `transOilC` entirely rather than polling a request nobody
   * supplied. Hex characters and spaces only; validated on blur by
   * `SettingsScreen`'s `parseHexPidDraft()`.
   */
  transOilPidHex: string;
  /**
   * Multi-circuit selection addendum (contracts.md, 2026-08-26, ticket
   * CN-W3): the ONE circuit every production `SessionController`, the
   * session-history store, coaching corners, the calibration track map, and
   * the circuit/PB screens are built for. Defaults to Transilvania Motor
   * Ring (`DEFAULT_SETTINGS`, below) -- existing installs with no persisted
   * value hydrate to this exact default, so TMR's behavior is unchanged.
   * An id absent from the bundled catalog resolves back to this default with
   * a `console.warn` (`circuitCatalog.ts`'s `resolveSelectedCircuit`) --
   * never a crash, never a fetch.
   */
  selectedCircuitId: string;
  /**
   * ENET telemetry addendum (binding, Phase 4e): which OBD transport
   * `telemetryProvider.ts` builds. Defaults to `'elm327'` -- existing behavior
   * unchanged for every install with no persisted value.
   */
  adapterType: AdapterType;
  /**
   * ENET adapter IP (its own local WiFi AP, same "adapter is a local WiFi
   * AP" model as the ELM327 `adapterHost` above, but a SEPARATE field -- the
   * two adapter types are never conflated, so switching `adapterType` back
   * and forth never clobbers either adapter's own remembered address).
   * Defaults to `''`: unlike the ELM327 adapter (a fixed default AP address),
   * the user reads the ENET adapter's IP from its own web UI -- `SettingsScreen`'s
   * copy explains this, there is no sane universal default to pre-fill.
   */
  enetHost: string;
  /** ENET adapter TCP port (contracts.md addendum: HSFZ over TCP 6801, verified). */
  enetPort: number;
  /** UDS tester (source) address ENET frames are sent from. EMPIRICAL (addendum: default 0xF4, alt 0xF1) -- never hardcoded without this override. */
  enetTesterAddress: number;
  /** UDS target (destination) address ENET frames are sent to. EMPIRICAL (addendum: default 0x12 = DME) -- never hardcoded without this override. */
  enetTargetAddress: number;
  /**
   * Advanced, vehicle-specific: a JSON array of `EnetChannelSpec` (`@circuit/core`),
   * each entry a `mode: 'obd01' | 'did'` request plus (for `did`) a decode
   * formula and REQUIRED provenance -- see the ENET addendum's own
   * `EnetChannelSpec` doc comment. Empty string (the default) means "use the
   * built-in defaults" (`DEFAULT_ENET_CHANNEL_SPECS`: obd01 rpm/speedKph/
   * throttlePct/coolantC/engineOilC, all EMPIRICAL on the ENET path, no
   * default `did` specs). Validated through `@circuit/core`'s
   * `validateEnetChannelSpecs` (`enetSettingsValidation.ts`'s
   * `resolveEnetChannelSpecs`/`validateEnetChannelSpecsJson`) both by
   * `SettingsScreen` (inline warnings on blur) and by `telemetryProvider.ts`
   * (re-validated on every `start()`, same "never trust a persisted value
   * blindly" rule as `transOilPidHex`/`buildCustomPids` above) -- malformed
   * JSON falls back to the built-in defaults with a `console.warn`, never a
   * crash.
   */
  enetChannelSpecsJson: string;
  /**
   * ENET auto-discovery & DID sweep addendum (binding, Phase 4f): where
   * `enetHost`/`enetPort` came from -- `''` (the default) means "entered
   * manually" (or never set); `discovered <date>` (ISO date) means a "Find
   * adapter" tap or the provider's own auto-discovery-on-connect-failure
   * detour applied a discovery hit. Display-only (SettingsScreen shows it
   * next to the host/port fields) -- never read by `telemetryProvider.ts`'s
   * connection logic itself.
   */
  enetHostProvenance: string;
  /**
   * ENET auto-discovery addendum (binding, Phase 4f): when `true` (the
   * default) and `adapterType === 'enet'`, `telemetryProvider.ts` runs
   * discovery ONCE per `start()` -- immediately when no host is configured,
   * or after the first connect attempt to a configured host fails -- and
   * applies a level-2 hit automatically. `false` reverts to the pre-addendum
   * behavior: a configured host is dialed directly, and a failure gets the
   * plain single reconnect retry, with no discovery involved at all.
   */
  enetAutoDiscover: boolean;
  /**
   * Field revision (2026-08-27, binding, "hidden developer mode"): when
   * `true` (or the build is `__DEV__`), `SettingsScreen` shows "Dev: DID
   * Probe (ENET)"/"Dev: DID Sweep (ENET)" -- the dev-only ENET diagnostic
   * tools stay REGISTERED as routes in every build, release included (App
   * Store review sees one binary), only their Settings-screen entry points
   * are hidden until this is on. Toggled by 7 taps on the About section's
   * version text within a short window (`registerDevTap`, below) -- never
   * shown as its own settings row. `DevReplay` is UNAFFECTED: it stays
   * `__DEV__`-only regardless of this setting. Defaults to `false`.
   */
  developerModeEnabled: boolean;
  /**
   * Ticket P4l-FIX1 F2 (binding): the app language (`'ro' | 'en'`). The
   * static default below is `'en'`; the DEVICE-LOCALE default
   * (`defaultLanguageForLocale(readDeviceLocale())`) is applied once, at
   * hydration, ONLY when nothing was ever persisted -- so a user who chose a
   * language keeps it even when they travel, and an existing install with no
   * stored value adopts its device's language on first launch after this
   * ticket rather than being forced to English.
   */
  language: AppLanguage;
  /**
   * Ticket P4p G1 (binding, field test 9 BUG-A): the ONE vehicle profile the
   * whole app works against -- the Signal Finder's target catalog AND the
   * confirmed bindings `telemetryProvider.ts` polls
   * (`composition.ts`'s binding cache reads `listBindings(THIS id)`).
   *
   * Bindings have always been stored per profile
   * (`vehicle_profile_bindings.profile_id`), but nothing persisted WHICH
   * profile the app was using: the Signal Finder screen defaulted its chip to
   * `'generic'` in React state, and the composition layer hard-coded
   * `'generic'` for telemetry. Field test 9: the user confirmed
   * brakePressure = 0x12/0x58B7 under `toyota-supra-b58` (export
   * `2026-08-31-steeringAngle-2.json`) while the monitor kept polling
   * 0x12/0x4002 from the generic profile (export `-1.json`) and showed a
   * meaningless 0/100 %.
   *
   * Defaults to `'generic'` -- the hypothesis-free catalog. An id with no
   * bundled catalog resolves back to the generic catalog at read time
   * (`resolveSignalTargetCatalog`), exactly like `selectedCircuitId` does.
   */
  activeVehicleProfileId: string;
  /**
   * Codex R2 fix (ticket P4q follow-up, binding, MEDIUM): WHY
   * `activeVehicleProfileId` is what it is -- `'user'` (an explicit chip tap,
   * `composition.ts`'s `setActiveVehicleProfileIdExplicit`), `'vin'` (VIN
   * auto-select, `applyVinAutoSelect`), or `'default'` (nothing chose it yet
   * -- the static default, or the one-time confirmed-bindings migration
   * heuristic). PERSISTED (not merely an in-memory flag): the pre-fix
   * in-memory-only guard let VIN auto-select overwrite a profile the user had
   * deliberately picked in an EARLIER app run, the instant the app restarted.
   * `decideVinAutoSelect` reads this (never `'user'`) to decide whether it
   * may act at all -- see its own doc comment.
   */
  activeVehicleProfileSource: 'user' | 'vin' | 'default';
  /**
   * Ticket P4q (binding, user: "the app should know the car from OBD from
   * the start if possible"): the VIN the ENET one-shot read
   * (`@circuit/core`'s `vinRead.ts`, driven by `composition.ts`'s
   * `maybeDetectVehicleFromVin`) last decoded successfully, persisted so the
   * Signal Finder screen can show "VIN: <value>" even before this app run has
   * read one itself. `null` = never read (or the last attempt found nothing
   * usable) -- never a fabricated placeholder.
   */
  lastSeenVin: string | null;
  /**
   * Ticket P6a (binding): switches the LIVE G-force provider
   * (`session/gforceProvider.ts`) from the first-order low-pass gravity
   * estimate to a `MadgwickAhrs` (gyroscope + accelerometer) one, AND starts
   * capturing the gyroscope as the new `yawRateDps` telemetry channel.
   *
   * Defaults to `false`, and OFF means OFF completely: the gyroscope is never
   * subscribed to, not one `yawRateDps` row is recorded, and the accelerometer
   * path runs the SAME code that produced every field-confirmed recording so
   * far. Read once per `start()` and frozen for that run, so flipping it never
   * swaps the estimator underneath a lap already being recorded. It can never
   * affect lap timing either way -- the G provider never touches
   * `SessionFacade`/`SessionController` (see its own module doc comment).
   */
  imuFusionEnabled: boolean;
  /**
   * Ticket P7R E3 (binding): records the phone's gyroscope as the
   * `yawRateDps` telemetry channel, WITHOUT switching the live gravity
   * estimate to the Madgwick filter.
   *
   * Split out of {@link imuFusionEnabled}, which used to decide both. The two
   * carry different risk and therefore get different defaults. Capture is
   * purely ADDITIVE -- it adds a channel and changes no existing value, so
   * `latG`/`longG` come out of the same field-proven low-pass path they
   * always have -- and it defaults to `true`, because a yaw trace recorded at
   * a real circuit is what the fusion sign convention can finally be settled
   * against offline. Fusion REPLACES the estimator behind those two channels
   * and stays off by default until that is settled.
   *
   * Read once per G-provider `start()` and frozen for that run, exactly like
   * {@link imuFusionEnabled}, and equally unable to affect lap timing -- the
   * G provider never touches `SessionFacade`/`SessionController`.
   */
  imuGyroCaptureEnabled: boolean;
  /**
   * Ticket P6a (binding): applies a Savitzky-Golay smoother
   * (`@circuit/core`'s `savitzkyGolay`, 9-sample window, quadratic) to the
   * recorded `latG`/`longG` series in the POST-SESSION analysis read path
   * (`session/analysisAssembly.ts`, reached through `analysisViewModel.ts`'s
   * runner).
   *
   * ANALYSIS ONLY, never live and never the between-stint trackday stage: the
   * filter is non-causal -- a 9-sample window at 25 Hz needs 160 ms of FUTURE
   * samples -- so it may only ever run over a finished recording, never over a
   * signal something is being cued from. Defaults to `false`; with it off the
   * analysis reads exactly the rows it always did.
   */
  analysisSmoothingEnabled: boolean;
}

/**
 * Ticket P4p G1: the only thing the initial-profile heuristic below needs to
 * know about a persisted binding. Deliberately NOT
 * `VehicleProfileBinding` (`persistence/didSweepStore.ts`): this module is the
 * settings vocabulary and must not depend on the persistence layer.
 */
export interface VehicleProfileBindingSummary {
  profileId: string;
  channel: string;
  status: string;
}

/** The profile id every install starts on, and the one an unresolvable id falls back to. */
export const GENERIC_VEHICLE_PROFILE_ID = 'generic';

/**
 * Ticket P4p G1 (binding): the ONE-TIME migration heuristic, run only on the
 * first launch after this setting existed (i.e. only when the persisted
 * settings row never carried a choice of its own -- see
 * `SqlSettingsStore.activeVehicleProfileIdWasStored`) and never afterwards.
 *
 * The situation it exists for is exactly field test 9's: the user confirmed
 * the same CHANNEL under two profiles, and the newer, engine-running confirm
 * lives on the car-specific profile while the app was reading generic. So:
 * a non-generic profile is preferred when it carries `field-confirmed`
 * bindings AND generic carries a binding for at least one of the SAME
 * channels -- the only case where the two stores actually contradict each
 * other about a channel the monitor polls.
 *
 * `null` means "no migration" (keep `'generic'`): nothing confirmed, only
 * generic confirmed, or a car profile whose channels generic never touched
 * (nothing contradicts, so nothing is guessed). Ties break on the number of
 * overlapping field-confirmed channels, then alphabetically, so the result is
 * deterministic. Pure -- it decides, it never writes, and NOTHING is ever
 * deleted either way.
 */
export function chooseInitialActiveVehicleProfileId(
  bindings: readonly VehicleProfileBindingSummary[],
): string | null {
  const genericChannels = new Set(
    bindings.filter((binding) => binding.profileId === GENERIC_VEHICLE_PROFILE_ID).map((binding) => binding.channel),
  );
  if (genericChannels.size === 0) return null;
  const overlapByProfile = new Map<string, number>();
  for (const binding of bindings) {
    if (binding.profileId === GENERIC_VEHICLE_PROFILE_ID) continue;
    if (binding.status !== 'field-confirmed') continue;
    if (!genericChannels.has(binding.channel)) continue;
    overlapByProfile.set(binding.profileId, (overlapByProfile.get(binding.profileId) ?? 0) + 1);
  }
  const ranked = [...overlapByProfile.entries()].sort((a, b) => (b[1] - a[1]) || (a[0] < b[0] ? -1 : a[0] > b[0] ? 1 : 0));
  return ranked[0]?.[0] ?? null;
}

/**
 * Field revision (2026-08-27, binding; window semantics corrected P4g-FIX1):
 * pure tap-counter for the "7 taps on the About version text toggles
 * developer mode" gesture -- `state` is `null` before the first tap (or
 * after a toggle just fired, resetting the count). `firstTapAtMs` anchors
 * the window to the FIRST tap of the current run: the whole sequence (tap 1
 * through the tap that reaches `threshold`) must land within `windowMs` of
 * THAT first tap, not merely within `windowMs` of the immediately preceding
 * tap -- otherwise taps spaced just under the window apart (e.g. 1.9s, each
 * individually "recent") would accumulate indefinitely and the gesture would
 * effectively have no time limit. A tap that arrives after the current run's
 * window has elapsed restarts the count at 1 and becomes the new anchor (so
 * idle taps spread across a session, e.g. 3 taps now and 4 taps an hour
 * later, never silently accumulate into an accidental toggle). `toggled` is
 * `true` exactly on the tap that reaches `threshold` -- the caller
 * (`SettingsScreen`) flips `developerModeEnabled` and shows the toast on
 * THAT tap only, never on every tap past the threshold while the user keeps
 * tapping.
 */
export interface DevTapState {
  count: number;
  firstTapAtMs: number;
}

export const DEV_TAP_THRESHOLD = 7;
export const DEV_TAP_WINDOW_MS = 2_000;

export function registerDevTap(
  state: DevTapState | null,
  nowMs: number,
  threshold: number = DEV_TAP_THRESHOLD,
  windowMs: number = DEV_TAP_WINDOW_MS,
): { state: DevTapState; toggled: boolean } {
  const withinWindow = state !== null && nowMs - state.firstTapAtMs <= windowMs;
  if (!withinWindow) {
    const toggled = threshold <= 1;
    return { state: { count: toggled ? 0 : 1, firstTapAtMs: nowMs }, toggled };
  }
  const state2 = state as DevTapState;
  const count = state2.count + 1;
  const toggled = count >= threshold;
  return { state: { count: toggled ? 0 : count, firstTapAtMs: toggled ? nowMs : state2.firstTapAtMs }, toggled };
}

export const DEFAULT_SETTINGS: AppSettings = {
  units: 'kmh',
  deltaDeadbandMs: 100,
  coverageBins: { thresholds: [0.25, 0.5, 0.75, 1] },
  coachingEnabled: true,
  voiceCoachEnabled: false,
  suggestionsEnabled: false,
  telemetryEnabled: false,
  telemetrySimulate: false,
  adapterHost: '192.168.0.10',
  adapterPort: 35_000,
  transOilPidHex: '',
  selectedCircuitId: TMR_CIRCUIT_PROFILE.circuitId,
  adapterType: 'elm327',
  enetHost: '',
  enetPort: 6_801,
  enetTesterAddress: 0xf4,
  enetTargetAddress: 0x12,
  enetChannelSpecsJson: '',
  enetHostProvenance: '',
  enetAutoDiscover: true,
  developerModeEnabled: false,
  language: 'en',
  activeVehicleProfileId: GENERIC_VEHICLE_PROFILE_ID,
  activeVehicleProfileSource: 'default',
  lastSeenVin: null,
  // Ticket P6a (binding): both experimental paths are OPT-IN. These two
  // `false`s are the guarantee that this ticket changed nothing in the field.
  imuFusionEnabled: false,
  // Ticket P7R E3 (binding): capture ON, fusion OFF. The gyroscope channel is
  // additive and cannot move latG/longG; the fused estimator can, and its
  // sign has never been checked against a real corner.
  imuGyroCaptureEnabled: true,
  analysisSmoothingEnabled: false,
};

/**
 * Persistence-agnostic settings surface consumed by `SettingsScreen` (S12).
 * `InMemorySettingsStore` is a dev-only stand-in; a later work package swaps
 * it (in `apps/mobile/src/session/composition.ts`) for one backed by durable
 * on-device storage without changing this interface or any screen.
 */
export interface SettingsStore {
  getSettings(): AppSettings;
  subscribe(cb: (s: AppSettings) => void): () => void;
  update(patch: Partial<AppSettings>): void;
}

/** In-memory settings store â€” DEV-ONLY, resets on app restart. */
export class InMemorySettingsStore implements SettingsStore {
  private settings: AppSettings = { ...DEFAULT_SETTINGS };
  private readonly listeners = new Set<(s: AppSettings) => void>();

  getSettings(): AppSettings {
    return this.settings;
  }

  subscribe(cb: (s: AppSettings) => void): () => void {
    this.listeners.add(cb);
    cb(this.settings);
    return () => {
      this.listeners.delete(cb);
    };
  }

  update(patch: Partial<AppSettings>): void {
    this.settings = { ...this.settings, ...patch };
    for (const listener of this.listeners) listener(this.settings);
  }
}
> **DRAFT â€” NOT YET IN FORCE.**
> This document was prepared for review by a qualified lawyer licensed in Romania and the European
> Union. It is **not legal advice** and must not be published, linked from an app store listing, or
> relied on by users until that review is complete.
> Drafted: **23 September 2026**. Document version: **1.0-draft**.
> Placeholders written as `[OWNER: â€¦]` are facts only the app's owner can supply; every one of them
> is also listed in `docs/legal/compliance-checklist.md` Â§8.

# TRACE â€” Privacy Policy

**Applies to:** the TRACE mobile application for iOS and Android (bundle identifier
`app.circuittimer.tmr`), version 1.0.0 and later, until replaced by a newer version of this policy.

**Last updated:** 23 September 2026
**Effective from:** `[OWNER: date of first public release]`

---

## 1. In one paragraph

TRACE is a lap timer and driving-analysis app for **closed racetracks**. It records where your car
is, how fast it is going, and â€” if you connect an OBD-II adapter â€” what your car's own sensors
report, so it can time your laps and tell you what happened on each corner. **All of it stays on
your phone.** TRACE contains no network code at all: there is no account, no server, no cloud
backup, no analytics, no advertising and no crash reporting. The only way data leaves your phone is
if you tap a share button yourself and choose where to send it. You can delete everything from
inside the app.

---

## 2. Who is responsible for your data (the controller)

| | |
|---|---|
| Controller | `[OWNER: full legal name â€” individual, PFA/ÃŽI, or SRL]` |
| Registered address | `[OWNER: full postal address in Romania]` |
| Contact for privacy matters | `[OWNER: email address, e.g. privacy@â€¦]` |
| Data Protection Officer | Not appointed. TRACE does not carry out large-scale monitoring on a controller's behalf; the processing described here happens entirely on the user's own device. `[OWNER/LAWYER: confirm no DPO is required under GDPR Art. 37]` |
| EU representative | Not required â€” the controller is established in Romania, inside the EU. |

The General Data Protection Regulation (Regulation (EU) 2016/679, "GDPR") applies to this app.

**A note on what "controller" means here.** For almost everything TRACE records, we never receive
the data at all â€” it is written to a private database inside the app's own sandbox on your phone and
we have no access to it. We are nonetheless treating ourselves as the controller for this processing,
because we decide what the app records and why. Where that distinction changes your rights in
practice, this policy says so explicitly (see Â§7).

---

## 3. What TRACE records, why, and on what legal basis

Everything in this table is stored **only** in a private SQLite database inside the app on your
device, and in files the app writes into its own cache folder when you export something.

### 3.1 Location data

| | |
|---|---|
| What | Latitude, longitude, horizontal accuracy, speed, heading and altitude, sampled continuously while a session is running |
| Why | This is the entire product. Lap and sector times are produced by detecting when your position crosses a start/finish or sector line; the analysis of each corner is computed from the same trace |
| When | Only while a session is active (calibration lap and timed laps), and only while the app is in the foreground |
| Where stored | `telemetry` table, per lap, plus a "raw trace" of everything captured even when no lap was detected |
| Legal basis | **Consent** (GDPR Art. 6(1)(a)), given by granting the operating system's location permission and starting a session. The app cannot start a session without it |
| Sent anywhere | **No** |

**Precise location is mandatory for this app to work.** iOS's "reduced accuracy" mode gives a
position circle roughly 1.9 km wide, which cannot time a lap. TRACE treats reduced accuracy as a
hard failure at the pre-session check and tells you how to turn Precise Location on, rather than
silently producing wrong times.

**Background location is never requested.** The app never asks for "Always" permission on iOS and
never declares `ACCESS_BACKGROUND_LOCATION` on Android. If you leave the app, recording stops.

**Under EU law, a position trace is personal data even though your name is nowhere in it.** A
sequence of precise positions and times says where a person was, and the European Data Protection
Board's *Guidelines 01/2020 on processing personal data in the context of connected vehicles and
mobility related applications* (version 2.0, adopted 9 March 2021) treats location data as requiring
particular care for exactly this reason. We treat your trace accordingly.

### 3.2 Vehicle data from the OBD-II port (optional)

| | |
|---|---|
| What | Engine RPM, vehicle speed, throttle plate position, accelerator pedal position, coolant temperature, intake air temperature, calculated engine load, engine oil temperature, transmission oil temperature (if you configure a custom PID for it), brake-pedal switch state and brake pressure |
| Why | So the analysis can say *where* you braked and *how hard*, and where you got back on the throttle â€” which is what turns a lap time into a usable observation |
| When | Only if you turn the "Telemetry" setting on **and** connect an adapter. It is off by default |
| Where stored | `telemetry_samples` table (one row per sample: session, lap, timestamp, channel, value) |
| Legal basis | **Consent** (Art. 6(1)(a)), given by enabling the setting and connecting the adapter |
| Sent anywhere | **No.** The connection is to an adapter on your own local Wi-Fi, not to the internet |

**TRACE only reads from the vehicle bus.** The app sends standard read-only OBD-II requests
(service 01) and the two read-only diagnostic services 0x21/0x22 used for manufacturer-specific
readings. It never sends a command that writes to, actuates, or clears anything in the car.

### 3.3 The vehicle identification number (VIN)

| | |
|---|---|
| What | Your car's 17-character VIN, read once per app run from the engine control unit (ISO 14229-1 data identifier `0xF190`) when a BMW-ENET-type adapter is connected |
| Why | To recognise which car you are in and select the right vehicle profile automatically, so you do not have to identify your car by hand every session |
| When | Only on the ENET adapter path, only when telemetry is enabled, and at most once per app launch |
| Where stored | `settings` table, key `lastSeenVin` |
| Legal basis | **Consent** (Art. 6(1)(a)) |
| Sent anywhere | **No** |

**A VIN is personal data.** It identifies a specific vehicle, and through registration records a
specific keeper â€” the EDPB's connected-vehicles guidelines say so directly. TRACE handles it as
such:

- It is **never written into an exported analysis, session report or raw session export.** We
  checked; it is not in those documents.
- Where the app writes the VIN to a developer log, it is **masked** â€” first three and last two
  characters only, e.g. `WBA************12`.
- It is shown in full on the Signal Finder screen, which is where you would need to read it.

- "Delete all my data" (Â§7) removes the stored VIN, and with it a vehicle profile the app selected
  from the VIN. The app reads the VIN again the next time a BMW-ENET-type adapter is connected.

### 3.4 Device motion sensors

| | |
|---|---|
| What | Accelerometer readings (converted to lateral and longitudinal g) and gyroscope readings (converted to yaw rate) |
| Why | To measure cornering and braking forces, and to tell a real corner from GPS noise. Gyroscope readings are used only if you turn that setting on |
| When | While a session is running |
| Where stored | Same `telemetry_samples` table, as channels `latG`, `longG`, `yawRateDps` |
| Legal basis | **Consent** (Art. 6(1)(a)) |
| Sent anywhere | **No** |

### 3.5 Timing, laps and your own judgements

| | |
|---|---|
| What | Session start time, lap numbers, lap and sector times, validity flags and the reason a lap was marked invalid, calibration attempts, your personal-best reference lap, and the verdicts **you** record when you agree or disagree with the app's judgement of a lap |
| Why | To show your history, to compare against your best lap, and â€” for your verdicts â€” so the app can be corrected by the person who was actually driving |
| Where stored | `sessions`, `laps`, `checkpoints`, `reference_laps`, `lap_verdicts`, `calibration_attempts` tables |
| Legal basis | **Consent** (Art. 6(1)(a)) |
| Sent anywhere | **No** |

### 3.6 App settings and diagnostic tooling

| | |
|---|---|
| What | Units, display preferences, language, adapter host and port, which vehicle profile is active, confirmed per-vehicle signal bindings, and â€” if you use the built-in diagnostic tools â€” records of OBD "sweep" runs including raw hexadecimal responses from your car's control units |
| Why | To remember how you configured the app, and to let the app find the right brake/throttle signal on your specific car |
| Where stored | `settings`, `vehicle_profile_bindings`, `signal_finder_ruled_out`, `did_sweep_*` tables |
| Legal basis | **Consent** (Art. 6(1)(a)) |
| Sent anywhere | **No** |

> **A caution about the diagnostic tools.** The Signal Finder and DID Sweep screens record raw
> responses from your car's control units. If you sweep a range that happens to include the VIN
> data identifier, the raw hexadecimal in that record â€” and in any export of it you choose to share
> â€” may contain your VIN. Check a sweep export before sending it to anyone.

### 3.7 GNSS diagnostics

The app keeps a rolling in-memory window of the last 300 position samples to compute signal-quality
statistics (sample intervals, accuracy percentiles, count of rejected mock locations on Android).
**This is never written to disk and never leaves the device**; it exists only for the lifetime of
the running process.

---

## 4. What TRACE does *not* do

- **No account.** There is no sign-up, no login, no email address, no password. The app uses a
  single fixed local identifier internally to tag rows in its own database.
- **No servers.** As of this version, TRACE contains no networking code whatsoever. An audit of the
  source found zero uses of `fetch`, `XMLHttpRequest`, `axios`, `WebSocket` or over-the-air updates
  anywhere in the application or its domain package. The circuit maps are compiled into the app
  itself, not downloaded.
- **No analytics, no advertising, no tracking.** There is no analytics SDK, no advertising SDK, no
  crash-reporting service, and no third-party library in the app that collects data. TRACE does not
  track you across other companies' apps or websites, does not use advertising identifiers, and
  does not share anything with data brokers.
- **No selling or sharing of personal data.** There is nothing to sell; we never receive it.
- **No profiling and no automated decisions with legal effect.** The app's coaching analysis is an
  automated evaluation of your driving, but it produces advice you are free to ignore. It has no
  legal or similarly significant effect within the meaning of GDPR Art. 22.
- **No children's data collected knowingly.** TRACE is intended for licensed drivers taking part in
  circuit activities and is not directed at children. `[OWNER: confirm the age rating you will
  declare in both stores.]`

---

## 5. When data leaves your phone â€” and only then

TRACE has export buttons. They are the only route out, and you press them.

| Export | Where it is | What it contains |
|---|---|---|
| **Share report** (session results, session history) | End of a session, and each row of session history | The session report as JSON, plus a one-page human-readable Markdown summary: lap and sector times, validity and its reasons, circuit provenance, calibration state |
| **Share report / Share the JSON file** (analysis screen) | Analysis screen | The per-corner analysis document, in Romanian or English according to your language setting |
| **Raw session export** | Session history | Everything on disk for that session with no interpretation: the full GNSS trace of every lap, the unclaimed trace of a session where no lap was detected, and every OBD/motion sample recorded |
| **Signal Finder export**, **DID Sweep export** | The respective diagnostic screens | The guided-observation or sweep record, including raw responses from your car |

When you tap one of these, the app writes the file into its own cache folder (files are named like
`trace-report-<circuit>-<date>-<session>.json`) and hands it to your operating system's standard
share sheet. **From that point on, where the file goes is your choice and is governed by whatever
service you send it to** â€” a messaging app, email, cloud storage. We receive nothing.

Exported reports and analyses **do not contain your VIN**. A raw session export contains your full
position trace, which is precise location data â€” treat it as you would a photograph of where you
were.

---

## 6. How long the data is kept

**Until you delete it.** There is no automatic expiry, no retention timer and no background clean-up
of sessions in this version. A session recorded today is still on your phone in five years unless
you remove it, or uninstall the app.

Two technical limits apply, and neither is a retention policy:

- At most **200,000 vehicle/motion telemetry rows per session** are stored; recording of that
  channel stops for the session once the cap is reached.
- The diagnostic **DID sweep store keeps the five most recent runs** and drops older ones.

Because nothing is transmitted, there is no server-side copy with its own retention period.
Uninstalling TRACE removes its private database along with it, in the normal way your operating
system removes an app's data.

**If you have exported files**, those files are yours and have their own life: this policy cannot
reach them. Delete them from wherever you sent them.

---

## 7. Your rights, and exactly how to use each one in this app

Under GDPR Articles 15â€“22 you have the rights listed below. Because your data is on your own device
and we hold no copy, most of these are things you exercise **directly in the app** â€” that is faster
and more complete than asking us, because we would have nothing to send you.

### Right of access (Art. 15) â€” "give me a copy of my data"

**In the app:** Session history â†’ the session you want â†’ **Share report** for the readable version,
or **raw session export** for everything on disk without interpretation. The analysis screen's
**Share report / Share the JSON file** gives you the corner-by-corner analysis. These exports are
the complete machine-readable record; there is no hidden copy elsewhere.

### Right to rectification (Art. 16) â€” "this is wrong, correct it"

**In the app:** you can record your own verdict on any lap the app judged, which is stored alongside
the app's judgement and used in reporting. Measured values (a recorded position, a recorded brake
pressure) are raw sensor readings and are not editable â€” correcting a measurement would mean
falsifying a record of what the sensors reported.

### Right to erasure (Art. 17) â€” "delete it"

**In the app:** **Settings â†’ DATA â†’ "Delete all my data"**, then confirm. This permanently removes
every session, lap, checkpoint, GNSS trace, vehicle and motion sample, lap verdict, calibration
attempt and your personal-best reference lap â€” for every circuit, not just the one selected â€” and
verifies the deletion actually landed before reporting success. It cannot be undone.

It also removes the stored VIN, a vehicle profile the app selected from the VIN, confirmed
per-vehicle signal bindings, ruled-out signals, diagnostic sweep records, the record of which
vehicle profile each session used, and the geometry of circuits you taught the app.

What it keeps are your **preferences**: units, language, adapter address and port, coaching, voice
and suggestion toggles, the selected circuit, and a vehicle profile you chose yourself. None of these
identify you or your car. Uninstalling the app removes them too.

Two honest caveats:

1. The control is **refused while a session is running**; end the session first. This exists so the
   deletion cannot race a live recording and leave data behind.
2. If any part of the deletion fails, the app says so instead of reporting success. In that case a
   learned circuit that a surviving session still uses is kept, so that session can still be
   analysed; run the deletion again.

### Right to restriction (Art. 18) â€” "stop using it for now"

**In the app:** turn off telemetry, coaching and suggestions in Settings; do not start a session. The
app records nothing when no session is active.

### Right to data portability (Art. 20) â€” "give it to me in a usable format"

**In the app:** the same exports as under access. They are JSON â€” open, documented, machine-readable
and not tied to TRACE.

### Right to object (Art. 21) and to withdraw consent (Art. 7(3))

**On your device:** withdraw the location permission in your operating system's settings, or turn
telemetry off, at any time. Withdrawal stops future recording; it does not retroactively make past
recording unlawful, and it does not by itself delete what is already stored â€” use the deletion
control for that.

### Right not to be subject to automated decision-making (Art. 22)

Not engaged. TRACE's analysis produces advice, not decisions with legal or similarly significant
effects.

### If you want to ask us instead

Write to `[OWNER: privacy contact email]`. Please understand what we can and cannot do: **we have no
copy of your data**, so we cannot send you an export, and we cannot delete anything from your phone.
What we can do is help you use the controls above, answer questions about what the app records, and
correct this policy if it is wrong. We will respond within **one month** of your request, as GDPR
Art. 12(3) requires, and will tell you if we need the extension that article allows.

### Right to complain to a supervisory authority (Art. 77)

You may lodge a complaint with the Romanian supervisory authority:

> **Autoritatea NaÈ›ionalÄƒ de Supraveghere a PrelucrÄƒrii Datelor cu Caracter Personal (ANSPDCP)**
> B-dul G-ral. Gheorghe Magheru 28-30, Sector 1, 010336 BucureÈ™ti, Romania
> Telephone: +40 318 059 211
> Email: anspdcp@dataprotection.ro
> Web: https://www.dataprotection.ro/

If you live in another EU or EEA country, you may complain to your own national authority instead.

---

## 8. Security

- The database lives in the app's **private sandbox directory**, which the operating system protects
  from other apps.
- On a modern iPhone or Android phone, that storage is covered by the device's **full-disk
  encryption**, which is active whenever the device is locked with a passcode. **Set a passcode.**
  Without one, this protection does not apply.
- **We do not encrypt the database separately** with a key of the app's own. If someone has your
  unlocked phone, they have your sessions.
- There is **no transmission to secure**: nothing is encrypted in transit because nothing is in
  transit.
- The Wi-Fi OBD adapter connection is a plain TCP link on your own local network, with no
  encryption. It carries vehicle telemetry only, and only over the few metres between adapter and
  phone. If you use the project's own prototype dongle, **change its default Wi-Fi password.**
- Files you export sit in the app's cache and then wherever you send them. The share sheet's
  destination is outside our control.

**There is no backup.** If you lose or reset your phone, your sessions are gone unless you exported
them yourself. This is the price of the app holding nothing.

---

## 9. International transfers

None. No data is transferred outside your device, and therefore none is transferred outside the
European Economic Area.

---

## 10. Changes to this policy â€” and the backend that does not exist yet

We will keep a version number and a date at the top of this document, and publish the previous
version alongside it. If a change materially affects what is collected or where it goes, we will
tell you **inside the app before the change takes effect**, and where the change requires your
consent we will ask for it rather than assume it.

**About a future server.** There is a plan to add a backend service so that a language model can
produce a written driving analysis. **It does not exist. No such service is contacted by this
version of the app, and no code in this version is capable of contacting one.** When and if it is
built, it will arrive as a **numbered amendment to this policy** â€” a new section describing exactly
what is sent, to whom, on what legal basis and for how long it is kept â€” together with a separate,
explicit opt-in inside the app. The local-only processing described in this document will remain
available and will remain the default. We are structuring the policy this way so you can see the
change as a change, rather than finding it folded into a rewrite.

---

## 11. Contact

`[OWNER: name]`
`[OWNER: postal address]`
`[OWNER: privacy email]`
`[OWNER: support URL]`

---

*Romanian version: `privacy-policy.ro.md`. In case of divergence between the two language versions,
`[OWNER/LAWYER: state which version governs â€” Romanian is the safer choice for a Romanian
controller and Romanian consumers].`*

  | 3.3 | Foreground-only location; no background/Always permission | **DONE** | `permissions.ts` never calls 
`requestBackgroundPermissionsAsync`; `app.json` sets the location plugin's background flags false |
  | 3.4 | In-app delete control | **DONE** | Settings â†’ DATA â†’ "Delete all my data", with confirm-again, refusal 
while a session is active, and verify-empty before reporting success |
> | 3.5 | Delete-all clears the stored VIN, VIN-selected vehicle profile, confirmed vehicle bindings, ruled-out 
signals, DID sweep records, per-session vehicle snapshots and learned-circuit geometry | **DONE** | 
`wipeDeviceUserData()` (`apps/mobile/src/persistence/deviceDataWipe.ts`) runs inside `deleteAllStoredUserData()` and 
verifies every table empty before success is reported. Preferences are kept by design (units, language, adapter 
address, toggles, selected circuit, a user-chosen profile). A learned circuit a surviving session still uses is kept 
and the wipe reports failure. Privacy policy Â§3.3 and Â§7 updated to match |
  | 3.6 | In-app export (access + portability) | **DONE** | Session report share (JSON + Markdown), analysis export, 
raw session export; all user-initiated through the OS share sheet |
  | 3.7 | Learned circuits can be deleted individually | **DONE** | `CircuitDetailScreen` calls 
`deleteLearnedCircuit()` |
# Handoff â€” TRACE, 2026-09-23

Brief for a fresh session. Self-contained: you should not need the previous conversation.

---

## What this is

**TRACE** â€” a GNSS lap timer and driver-coaching app for racetracks. TypeScript npm-workspaces monorepo:
`packages/core` is a pure engine (one runtime dependency, `zod`), `apps/mobile` is Expo SDK 57 / React
Native 0.86 on iOS, installed by sideloading. Repo is **public**: `dobrinz123/track_app`.

State at this commit: **3397 tests green**, typecheck 0, lint 0 errors (6 pre-existing warnings in
`didSweep*` files â€” leave them). Build 13 delivered to the owner.

**Before changing an area, read the documents that cover it.** They are current and were written for exactly this purpose:

| Document | What it gives you |
|---|---|
| `docs/architecture/map-core-timing.md` | How a GNSS sample becomes a lap time; the invariants and why |
| `docs/architecture/map-core-analysis.md` | The analysis engine, the data model, schema v1â†’v6 |
| `docs/architecture/map-mobile-app.md` | The composition root, the screens, **the platform fences** |
| `docs/architecture/map-data-and-infra.md` | Assets, generators, CI, firmware, and a clean-clone runbook |
| `docs/architecture/flow-review.md` | End-to-end journeys and where they still break |
| `docs/architecture/public-release-plan.md` | What blocks App Store / Play, ordered |
| `docs/legal/` | Draft privacy policy, terms, store privacy labels, permission strings, compliance checklist |
| `.foreman/ledger.md` | 200 KB append-only history. Grep it; do not read it whole |

---

## The hard rules this project runs on

These are the owner's, learned the hard way. Breaking them is how this app lost him a track day.

1. **No build without the full chain.** Gates â†’ independent review at **zero HIGH findings** â†’ then build.
2. **Never report success you did not observe.** Weeks of work went into making a failed read surface as
   FAILED rather than as "there was nothing". If you cannot verify something, say so.
3. **Field facts come from the owner, never guessed.** What happened at the track, how the phone is
   mounted, what he saw on screen â€” ask. Technical trade-offs are yours to decide; do not quiz him on those.
4. **Honesty gates are load-bearing, not decoration.** `geometryStatus`, calibration provenance, the
   four-valued availability model. Do not widen one to make a feature appear.
5. **Never delete a lap boundary you cannot confirm â€” mark it.** Three attempts at deciding the pit
   question each silently deleted or invented real laps. The rule that finally worked is that nothing is
   deleted, so no threshold can be outrun.
6. Gates run from the repo root; **`npx expo export --platform ios` runs from `apps/mobile`** â€” from the
   root it fails on AppEntry resolution, and that is expected, not a defect.
7. There is **no React Native render harness** and adding one has been declined repeatedly. Put decisions
   in pure tested modules and keep JSX thin. Never claim render coverage you do not have.

---

## Open work, in the order I would take it

### 1. ~~`deleteUserData` does not delete the VIN~~ â€” fixed

"Delete all my data" now also clears the VIN, a VIN-selected vehicle profile, vehicle bindings,
ruled-out signals, DID sweep records, per-session vehicle snapshots and learned-circuit geometry
(`apps/mobile/src/persistence/deviceDataWipe.ts`). Preferences are kept. The privacy policies and
compliance checklist item 3.5 are updated.

### 2. MotorPark's corner segmentation reads wrong

Corner 1 is a 158 m "corner" with a 324 m brake distance. The numbers are true; the label is not useful.
This is the resolution limit of a coarse traced line, not a bug in the gate. `analyzeCorners` against the
densified MotorPark asset is the place to look. The owner sees this on Monday.

### 3. Off-track classification is the last ungated geometry-sensitive verdict

`packages/core/src/coaching/cleanLap.ts:762` calls a lap off-track at |lateral| > 15 m from the traced
line. On a badly traced circuit it can drop clean laps from every corner's clean set. It fails closed, so
it only ever reduces what is claimed â€” but it is the one place a traced line still decides a verdict.

### 4. Flow-review leftovers

F8 (a "CALIBRATION UNKNOWN" flash at every session end, from an unordered cache refresh), F9
(`PAUSE_GAP`/`RECOVERY` have no plain-language copy, so locking your phone yields "â€¢ pause gap"), F10
(learned circuits always draw three sectors against an empty `sectorGates`), F11 (a personal best can link
to "Lap not found"), plus a ten-item *correct but confusing* list. Details in `flow-review.md`.

### 5. Store readiness

`docs/architecture/public-release-plan.md` and `docs/legal/compliance-checklist.md`. The long poles are
legal identity and EU DSA trader status â€” Apple **removes** EU apps without it â€” and the fact that Android
has never once been built. Twenty-one questions only the owner can answer are consolidated in the
checklist's Â§8; get answers before doing store work, not during.



exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -n 'INSERT|runAsync' apps/mobile/src packages/core/src --glob '*.ts' --glob '"'!*.test.ts'"'" in D:\CODE\APLICTIE_Circuit
 succeeded in 580ms:
apps/mobile/src\persistence\deviceDataWipe.ts:71:    await tx.runAsync('DELETE FROM settings WHERE key = ?', [SETTINGS_KEY]);
apps/mobile/src\persistence\deviceDataWipe.ts:75:  await tx.runAsync('UPDATE settings SET value = ? WHERE key = ?', [JSON.stringify(scrubbed), SETTINGS_KEY]);
apps/mobile/src\persistence\deviceDataWipe.ts:91:      await tx.runAsync(`DELETE FROM ${table}`);
apps/mobile/src\persistence\deviceDataWipe.ts:95:    await tx.runAsync(
apps/mobile/src\persistence\deviceDataWipe.ts:101:    await tx.runAsync('DELETE FROM settings WHERE substr(key, 1, length(?)) = ?', [prefix, prefix]);
apps/mobile/src\persistence\expoSqlDatabase.ts:14://   - runAsync(sql, params) -> {changes} -> db.runAsync(sql, params) (also returns
apps/mobile/src\persistence\expoSqlDatabase.ts:21:    runAsync: (sql: string, params: readonly SqlBindValue[] = []) => db.runAsync(sql, params as SqlBindValue[]),
apps/mobile/src\persistence\expoSqlDatabase.ts:78:  // batch INSERT, a trace chunk write, a settings row) for its own duration.
apps/mobile/src\persistence\telemetrySchema.ts:49:    await db.runAsync('INSERT INTO telemetry_schema_migrations (version) VALUES (?)', [TELEMETRY_SCHEMA_VERSION]);
apps/mobile/src\persistence\telemetrySchema.ts:51:    await db.runAsync('UPDATE telemetry_schema_migrations SET version = ?', [TELEMETRY_SCHEMA_VERSION]);
apps/mobile/src\persistence\telemetryRecorder.ts:40: * ONE parameterized multi-row `INSERT` statement (`writeBatch` below) --
apps/mobile/src\persistence\telemetryRecorder.ts:48: * it, this INSERT included, so the recorder no longer holds the gate itself
apps/mobile/src\persistence\telemetryRecorder.ts:143:   * F1 fix: ONE parameterized multi-row `INSERT` for the whole batch -- no
apps/mobile/src\persistence\telemetryRecorder.ts:144:   * `withTransactionAsync`, no per-row `runAsync` loop (never more than one
apps/mobile/src\persistence\telemetryRecorder.ts:146:   * multi-row `INSERT` is already atomic in SQLite.
apps/mobile/src\persistence\telemetryRecorder.ts:170:    // here as well would queue this INSERT behind its own gate acquisition.
apps/mobile/src\persistence\telemetryRecorder.ts:171:    await this.db.runAsync(
apps/mobile/src\persistence\telemetryRecorder.ts:172:      `INSERT INTO telemetry_samples (session_id, lap_number, t_mono_ms, channel, value) VALUES ${placeholders}`,
apps/mobile/src\persistence\didSweepSchema.ts:161:    await db.runAsync('INSERT INTO did_sweep_schema_migrations (version) VALUES (?)', [DID_SWEEP_SCHEMA_VERSION]);
apps/mobile/src\persistence\didSweepSchema.ts:163:    await db.runAsync('UPDATE did_sweep_schema_migrations SET version = ?', [DID_SWEEP_SCHEMA_VERSION]);
apps/mobile/src\persistence\sqlWriteGate.ts:9: * awaited `runAsync`, which was queued BEHIND it: a guaranteed self-deadlock
apps/mobile/src\persistence\sqlWriteGate.ts:17: * ALREADY OPEN transaction on the SAME connection: a `runAsync` issued while
apps/mobile/src\persistence\sqlWriteGate.ts:78:    runAsync: (sql, params) => gate.exclusive(() => inner.runAsync(sql, params)),
apps/mobile/src\persistence\sessionVehicleSnapshot.ts:83: * With `INSERT OR REPLACE` the recovery call overwrote the snapshot of the
apps/mobile/src\persistence\sessionVehicleSnapshot.ts:91: * `INSERT OR IGNORE` against `settings(key TEXT PRIMARY KEY)` is that rule in
apps/mobile/src\persistence\sessionVehicleSnapshot.ts:109:    await db.runAsync('INSERT OR IGNORE INTO settings (key, value) VALUES (?, ?)', [
apps/mobile/src\persistence\sqlSettingsStore.ts:198:      await this.db.runAsync('INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)', [
apps/mobile/src\persistence\sqlSettingsStore.ts:290:    await db.runAsync('INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)', [
apps/mobile/src\persistence\sqlSettingsStore.ts:307:    await db.runAsync('DELETE FROM settings WHERE key = ?', [UNVALIDATED_MATCHING_KEY]);
apps/mobile/src\persistence\learnedCircuitSchema.ts:54:    await db.runAsync('INSERT INTO learned_circuit_schema_migrations (version) VALUES (?)', [
apps/mobile/src\persistence\learnedCircuitSchema.ts:58:    await db.runAsync('UPDATE learned_circuit_schema_migrations SET version = ?', [
apps/mobile/src\session\adoptionJournal.ts:94:  await db.runAsync('INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)', [
apps/mobile/src\session\adoptionJournal.ts:138:  const result = await db.runAsync(
apps/mobile/src\session\adoptionJournal.ts:155:  const result = await db.runAsync('DELETE FROM settings WHERE key = ? AND value = ?', [
apps/mobile/src\session\adoptionJournal.ts:189:    await tx.runAsync('DELETE FROM telemetry WHERE sessionId = ?', [sessionId]);
apps/mobile/src\session\adoptionJournal.ts:190:    await tx.runAsync('DELETE FROM laps WHERE sessionId = ?', [sessionId]);
apps/mobile/src\session\adoptionJournal.ts:191:    await tx.runAsync('DELETE FROM checkpoints WHERE sessionId = ?', [sessionId]);
apps/mobile/src\session\adoptionJournal.ts:192:    await tx.runAsync('DELETE FROM sessions WHERE sessionId = ?', [sessionId]);
apps/mobile/src\session\adoptionJournal.ts:193:    await tx.runAsync('DELETE FROM telemetry_samples WHERE session_id = ?', [sessionId]);
apps/mobile/src\session\adoptionJournal.ts:201:        await tx.runAsync('DELETE FROM settings WHERE key = ?', [key]);
packages/core/src\persistence-sql\schema.ts:31:// than a SELECT and an INSERT sharing a transaction -- no second round trip
packages/core/src\persistence-sql\sqlDatabase.ts:12://   - runAsync(source: string, params: SQLiteBindParams): Promise<SQLiteRunResult> -- SQLiteRunResult has
packages/core/src\persistence-sql\sqlDatabase.ts:17://     execAsync/runAsync/getAllAsync/getFirstAsync/getEachAsync/PRAGMA journal_mode=WAL; the
packages/core/src\persistence-sql\sqlDatabase.ts:20://     bind params (`runAsync(source, ...params)`); we only ever call the array-params overload, and
packages/core/src\persistence-sql\sqlDatabase.ts:31:  runAsync(sql: string, params?: readonly SqlBindValue[]): Promise<SqlRunResult>;
packages/core/src\persistence-sql\sqlSessionRepository.ts:209:      await this.db.runAsync('INSERT INTO schema_migrations (version) VALUES (?)', [SQL_SCHEMA_VERSION]);
packages/core/src\persistence-sql\sqlSessionRepository.ts:215:      await this.db.runAsync('UPDATE schema_migrations SET version = ?', [SQL_SCHEMA_VERSION]);
packages/core/src\persistence-sql\sqlSessionRepository.ts:235:    await this.db.runAsync('INSERT OR REPLACE INTO checkpoints (sessionId, payload, lapCount) VALUES (?, ?, ?)', [
packages/core/src\persistence-sql\sqlSessionRepository.ts:272:   * `SELECT` followed by an `INSERT` -- not even two statements sharing this
packages/core/src\persistence-sql\sqlSessionRepository.ts:301:        await tx.runAsync(
packages/core/src\persistence-sql\sqlSessionRepository.ts:302:          'INSERT OR REPLACE INTO telemetry (sessionId, lapNumber, payload) VALUES (?, ?, ?)',
packages/core/src\persistence-sql\sqlSessionRepository.ts:306:      await tx.runAsync(
packages/core/src\persistence-sql\sqlSessionRepository.ts:307:        `INSERT INTO checkpoints (sessionId, payload, lapCount) VALUES (?, ?, ?)
packages/core/src\persistence-sql\sqlSessionRepository.ts:331:      await tx.runAsync(
packages/core/src\persistence-sql\sqlSessionRepository.ts:332:        `INSERT OR REPLACE INTO sessions
packages/core/src\persistence-sql\sqlSessionRepository.ts:357:      await tx.runAsync('DELETE FROM laps WHERE sessionId = ?', [s.sessionId]);
packages/core/src\persistence-sql\sqlSessionRepository.ts:359:        await tx.runAsync('INSERT INTO laps (sessionId, lapNumber, payload) VALUES (?, ?, ?)', [
packages/core/src\persistence-sql\sqlSessionRepository.ts:434:    await this.db.runAsync('INSERT OR REPLACE INTO telemetry (sessionId, lapNumber, payload) VALUES (?, ?, ?)', [
packages/core/src\persistence-sql\sqlSessionRepository.ts:456:        await tx.runAsync(
packages/core/src\persistence-sql\sqlSessionRepository.ts:457:          'INSERT OR REPLACE INTO telemetry (sessionId, lapNumber, payload) VALUES (?, ?, ?)',
packages/core/src\persistence-sql\sqlSessionRepository.ts:480:   * Ticket P12 item A. `INSERT OR REPLACE` keyed `(sessionId, lapNumber)` --
packages/core/src\persistence-sql\sqlSessionRepository.ts:487:    await this.db.runAsync(
packages/core/src\persistence-sql\sqlSessionRepository.ts:488:      'INSERT OR REPLACE INTO lap_verdicts (sessionId, lapNumber, payload) VALUES (?, ?, ?)',
packages/core/src\persistence-sql\sqlSessionRepository.ts:520:    await this.db.runAsync(
packages/core/src\persistence-sql\sqlSessionRepository.ts:521:      'INSERT OR REPLACE INTO calibration_attempts (attemptId, sessionId, startedAtUtc, payload) VALUES (?, ?, ?, ?)',
packages/core/src\persistence-sql\sqlSessionRepository.ts:573:    // Atomic replace: DELETE+INSERT wrapped in one transaction, per the
packages/core/src\persistence-sql\sqlSessionRepository.ts:574:    // ticket's explicit instruction (rather than a single INSERT OR REPLACE)
packages/core/src\persistence-sql\sqlSessionRepository.ts:575:    // so the atomicity is real and testable -- if the INSERT half fails for
packages/core/src\persistence-sql\sqlSessionRepository.ts:579:      await tx.runAsync(
packages/core/src\persistence-sql\sqlSessionRepository.ts:583:      await tx.runAsync(
packages/core/src\persistence-sql\sqlSessionRepository.ts:584:        'INSERT INTO reference_laps (userId, circuitId, layoutId, layoutVersion, payload) VALUES (?, ?, ?, ?, ?)',
packages/core/src\persistence-sql\sqlSessionRepository.ts:599:      await tx.runAsync('DELETE FROM laps WHERE sessionId IN (SELECT sessionId FROM sessions WHERE userId = ?)', [
packages/core/src\persistence-sql\sqlSessionRepository.ts:602:      await tx.runAsync(
packages/core/src\persistence-sql\sqlSessionRepository.ts:606:      await tx.runAsync(
packages/core/src\persistence-sql\sqlSessionRepository.ts:613:      await tx.runAsync(
packages/core/src\persistence-sql\sqlSessionRepository.ts:617:      await tx.runAsync(
packages/core/src\persistence-sql\sqlSessionRepository.ts:621:      await tx.runAsync('DELETE FROM sessions WHERE userId = ?', [userId]);
packages/core/src\persistence-sql\sqlSessionRepository.ts:622:      await tx.runAsync('DELETE FROM reference_laps WHERE userId = ?', [userId]);
packages/core/src\persistence-sql\sqlSessionRepository.ts:627:      await tx.runAsync('DELETE FROM checkpoints WHERE substr(sessionId, 1, length(?)) = ?', [
packages/core/src\persistence-sql\sqlSessionRepository.ts:631:      await tx.runAsync('DELETE FROM telemetry WHERE substr(sessionId, 1, length(?)) = ?', [
packages/core/src\persistence-sql\sqlSessionRepository.ts:639:      await tx.runAsync('DELETE FROM lap_verdicts WHERE substr(sessionId, 1, length(?)) = ?', [
packages/core/src\persistence-sql\sqlSessionRepository.ts:643:      await tx.runAsync('DELETE FROM calibration_attempts WHERE substr(sessionId, 1, length(?)) = ?', [
apps/mobile/src\session\composition.ts:1328:  // this recorder's INSERTs against every transaction itself.
apps/mobile/src\session\composition.ts:1626:      await tx.runAsync('DELETE FROM settings WHERE key = ?', [ACTIVE_SESSION_SETTINGS_KEY]);
apps/mobile/src\session\composition.ts:1627:      await tx.runAsync('DELETE FROM settings WHERE key = ?', [ACTIVE_SESSION_CIRCUIT_SETTINGS_KEY]);
apps/mobile/src\session\composition.ts:1628:      await tx.runAsync('DELETE FROM settings WHERE key = ?', [ACTIVE_SESSION_STARTED_SETTINGS_KEY]);
apps/mobile/src\session\composition.ts:1630:      await tx.runAsync('INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)', [
apps/mobile/src\session\composition.ts:1634:      await tx.runAsync('INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)', [
apps/mobile/src\session\composition.ts:1639:        await tx.runAsync('INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)', [
apps/mobile/src\session\composition.ts:2412: * collision that an `INSERT OR REPLACE` would silently resolve by destroying
apps/mobile/src\session\composition.ts:2589:      await database.runAsync('DELETE FROM learned_circuits WHERE circuit_id = ?', [
apps/mobile/src\session\composition.ts:2889: * P5d-FIX1 item 10: a plain INSERT, with a fresh id on the (vanishingly
apps/mobile/src\session\composition.ts:2890: * unlikely) collision, instead of an INSERT OR REPLACE that would silently
apps/mobile/src\session\composition.ts:3760:    // shared write gate, so no straggler batch INSERT can land between (or
apps/mobile/src\session\composition.ts:3773:        await tx.runAsync('DELETE FROM telemetry_samples');
apps/mobile/src\session\learnedCircuitStore.ts:123:   * plain INSERT, so an id that is somehow already taken is REPORTED rather
apps/mobile/src\session\learnedCircuitStore.ts:135:      await this.db.runAsync(
apps/mobile/src\session\learnedCircuitStore.ts:136:        `INSERT INTO learned_circuits
apps/mobile/src\session\learnedCircuitStore.ts:156:    await this.db.runAsync(
apps/mobile/src\session\learnedCircuitStore.ts:157:      `INSERT OR REPLACE INTO learned_circuits
apps/mobile/src\session\learnedCircuitStore.ts:217:    await this.db.runAsync('DELETE FROM learned_circuits WHERE circuit_id = ?', [circuitId]);

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "Get-Content apps/mobile/src/persistence/expoSqlDatabase.ts; rg -n 'vinDetectionState|cachedDetectedVin|adoptionProgress|testLoopAdoption|testLoopProvider|refreshVehicleProfileBindingsCache|settingsStore.subscribe' apps/mobile/src/session/composition.ts; rg -n 'CREATE TABLE|circuitId.*NOT NULL' packages/core/src/persistence-sql; Get-Content apps/mobile/src/persistence/openAppDatabase.ts" in D:\CODE\APLICTIE_Circuit
 exited 1 in 260ms:
import { openDatabaseAsync, type SQLiteDatabase } from 'expo-sqlite';
import type { SqlBindValue, SqlDatabase } from '@circuit/core/src/persistence-sql';
import { SqlSessionRepository } from '@circuit/core/src/persistence-sql';
import { migrateTelemetrySchema } from './telemetrySchema';
import { migrateDidSweepSchema } from './didSweepSchema';
import { createSqlWriteGate, gateSqlTransactions, type SqlWriteGate } from './sqlWriteGate';

// Thin adapter from expo-sqlite's `SQLiteDatabase` to the `SqlDatabase`
// interface `SqlSessionRepository` (packages/core/src/persistence-sql) is
// written against. No SQL logic lives here -- this file only translates one
// async API shape into another, confirmed 1:1 against
// node_modules/expo-sqlite/build/SQLiteDatabase.d.ts:
//   - execAsync(sql)                    -> db.execAsync(sql)
//   - runAsync(sql, params) -> {changes} -> db.runAsync(sql, params) (also returns
//     lastInsertRowId, which SqlRunResult does not need and structurally ignores)
//   - getAllAsync<T>(sql, params)       -> db.getAllAsync<T>(sql, params)
//   - withTransactionAsync(fn)          -> db.withTransactionAsync(fn)
export function wrapExpoSqliteDatabase(db: SQLiteDatabase): SqlDatabase {
  const handle: SqlDatabase = {
    execAsync: (sql: string) => db.execAsync(sql),
    runAsync: (sql: string, params: readonly SqlBindValue[] = []) => db.runAsync(sql, params as SqlBindValue[]),
    getAllAsync: <T>(sql: string, params: readonly SqlBindValue[] = []) => db.getAllAsync<T>(sql, params as SqlBindValue[]),
    // Ticket P10B H5-B: expo-sqlite's own `withTransactionAsync` takes a
    // zero-argument task, so the transaction-scoped handle this contract
    // promises is supplied here -- and it is THIS object, the ungated
    // connection. A caller inside the callback therefore reaches the
    // connection directly instead of re-entering whatever wrapper
    // (`gateSqlTransactions`) sits above it.
    withTransactionAsync: (fn: (tx: SqlDatabase) => Promise<void>) =>
      db.withTransactionAsync(() => fn(handle)),
  };
  return handle;
}

// F1/N1: transaction-vs-telemetry mutual exclusion lives in ./sqlWriteGate.ts
// (see its module doc comment for why only WHOLE units are gated, never the
// statements a transaction callback itself issues -- the naive every-call
// FIFO this file previously shipped self-deadlocked every repository
// transaction, N1).

/**
 * Opens (creating and migrating if necessary) the on-device SQLite database
 * and returns a ready-to-use `SqlSessionRepository` over it.
 *
 * `dbName` is passed straight through to expo-sqlite's `openDatabaseAsync`
 * (e.g. `"circuit-timer.db"`); WAL mode is attempted by
 * `SqlSessionRepository`'s migration step and applies here on real SQLite
 * (platform-research.md §6).
 */
export async function createSqliteSessionRepository(dbName: string): Promise<SqlSessionRepository> {
  const db = await openDatabaseAsync(dbName);
  return SqlSessionRepository.create(wrapExpoSqliteDatabase(db));
}

/**
 * Opens the on-device SQLite database once and returns BOTH the migrated
 * `SqlSessionRepository` and the raw `SqlDatabase` handle it was built from,
 * so a second store (`SqlSettingsStore`, the v2 `settings` key-value table)
 * can share the exact same connection instead of opening `dbName` twice.
 * `SqlSessionRepository.create` is what actually runs the migration
 * (including the v2 `settings` table) -- this must be awaited before the raw
 * `db` handle is used for anything settings-related.
 *
 * Also applies `migrateTelemetrySchema` (Telemetry addendum) and
 * `migrateDidSweepSchema` (DID sweep results persistence addendum, P4i) over
 * the SAME connection -- both mobile-owned additive migrations entirely
 * independent of `SqlSessionRepository`'s own (packages/core is out of the
 * ticket that added these calls' write set); see `./telemetrySchema.ts`'s
 * and `./didSweepSchema.ts`'s own doc comments.
 */
export async function openAppDatabase(
  dbName: string,
): Promise<{ db: SqlDatabase; repository: SqlSessionRepository; writeGate: SqlWriteGate }> {
  const raw = await openDatabaseAsync(dbName);
  // F1/N1 fix, extended by P10B H5-B: EVERY unit of work on this connection
  // acquires `writeGate` -- a whole repository transaction for its entire
  // BEGIN..COMMIT span, and each standalone statement (a `TelemetryRecorder`
  // batch INSERT, a trace chunk write, a settings row) for its own duration.
  // Statements issued inside a transaction callback go through the `tx`
  // handle that callback is given, straight to the connection, which is what
  // keeps full serialization deadlock-free (the self-deadlock N1 hit, and the
  // reason no caller may wrap a gated statement in `exclusive()` itself).
  const writeGate = createSqlWriteGate();
  const db = gateSqlTransactions(wrapExpoSqliteDatabase(raw), writeGate);
  const repository = await SqlSessionRepository.create(db);
  await migrateTelemetrySchema(db);
  await migrateDidSweepSchema(db);
  return { db, repository, writeGate };
}
114:import { TestLoopLocationProvider } from './testLoopProvider';
724:export async function refreshVehicleProfileBindingsCache(): Promise<void> {
796:settingsStore.subscribe((settings) => {
800:  void refreshVehicleProfileBindingsCache();
862:let vinDetectionState: VinDetectionState = 'idle';
863:/** The result of the ONE completed attempt this app run has made (or will make) -- `null` before `vinDetectionState === 'done'`, and forever after if the read found nothing usable. */
864:let cachedDetectedVin: string | null = null;
999:  if (vinDetectionState !== 'idle') return cachedDetectedVin;
1008:  vinDetectionState = 'attempting';
1030:  vinDetectionState = 'done';
1031:  cachedDetectedVin = vin;
1063:    void refreshVehicleProfileBindingsCache();
1708:  const provider: LocationProvider = testLoopProvider ?? gnssProvider;
1986:settingsStore.subscribe((settings) => {
2360:let testLoopProvider: TestLoopLocationProvider | null = null;
2379:let adoptionProgress: AdoptionProgress | null = null;
2441:  if (testLoopProvider === null && !testLoopHoldsGForce) return;
2625:    providerAttached: testLoopProvider !== null,
2705:    testLoopProvider = provider;
2739:  const provider = testLoopProvider;
2740:  testLoopProvider = null;
2759:  const provider = testLoopProvider;
2774:    const ledger = (adoptionProgress ??= { circuitId: circuit.profile.circuitId });
2880:    testLoopProvider = null;
2882:    adoptionProgress = null;
3065:    // awaited -- `refreshVehicleProfileBindingsCache()` never throws, and
3067:    void refreshVehicleProfileBindingsCache();
3091:      await refreshVehicleProfileBindingsCache();
3627:  cachedDetectedVin = null;
3648:  await refreshVehicleProfileBindingsCache();
packages/core/src/persistence-sql\sqlSessionRepository.ts:144:   * has already been migrated re-applies only `CREATE TABLE IF NOT EXISTS`
packages/core/src/persistence-sql\sqlSessionRepository.ts:165:    // v2: the settings key-value table. `CREATE TABLE IF NOT EXISTS` makes
packages/core/src/persistence-sql\sqlSessionRepository.ts:170:    // tables. Both are `CREATE TABLE IF NOT EXISTS`, so like `SQL_DDL_V2`
packages/core/src/persistence-sql\schema.ts:65:CREATE TABLE IF NOT EXISTS schema_migrations (
packages/core/src/persistence-sql\schema.ts:69:CREATE TABLE IF NOT EXISTS sessions (
packages/core/src/persistence-sql\schema.ts:72:  circuitId TEXT NOT NULL,
packages/core/src/persistence-sql\schema.ts:84:CREATE TABLE IF NOT EXISTS laps (
packages/core/src/persistence-sql\schema.ts:91:CREATE TABLE IF NOT EXISTS checkpoints (
packages/core/src/persistence-sql\schema.ts:97:CREATE TABLE IF NOT EXISTS telemetry (
packages/core/src/persistence-sql\schema.ts:104:CREATE TABLE IF NOT EXISTS reference_laps (
packages/core/src/persistence-sql\schema.ts:106:  circuitId TEXT NOT NULL,
packages/core/src/persistence-sql\schema.ts:121:CREATE TABLE IF NOT EXISTS settings (
packages/core/src/persistence-sql\schema.ts:129: * `CREATE TABLE IF NOT EXISTS` above cannot add a column to a table it did
packages/core/src/persistence-sql\schema.ts:165: * `CREATE TABLE IF NOT EXISTS` is all the idempotence they need -- applied on
packages/core/src/persistence-sql\schema.ts:177:CREATE TABLE IF NOT EXISTS lap_verdicts (
packages/core/src/persistence-sql\schema.ts:184:CREATE TABLE IF NOT EXISTS calibration_attempts (
Get-Content: 
Line |
   2 |  … stence-sql; Get-Content apps/mobile/src/persistence/openAppDatabase.t …
     |                ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     | Cannot find path 'D:\CODE\APLICTIE_Circuit\apps\mobile\src\persistence\openAppDatabase.ts' because it does not exist.

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command '$p='"'apps/mobile/src/session/composition.ts'; "'$s=Get-Content $p; foreach($r in @(@(710,808),@(985,1068),@(2745,2890),@(3000,3100),@(3650,3870))) { for($i=$r[0];$i -le $r[1];$i++){ '"'{0}:{1}' -f "'$i,$s[$i-1] } }; Get-Content apps/mobile/src/persistence/sqlWriteGate.ts; rg -n '"'settings|KEY|write' apps/mobile/src/session/adoptionJournal.ts; rg -n 'INSERT|runAsync|CREATE TABLE' packages/core/src apps/mobile/src --glob '*.ts' | Measure-Object" in D:\CODE\APLICTIE_Circuit
 succeeded in 100ms:
803:/**
804: * Ticket P4p G1 (binding), the ONE-TIME initial-profile migration. Runs from
805: * `runBootstrap()` immediately after settings hydrate, and only while the
806: * persisted row never carried a profile choice of its own
807: * (`SqlSettingsStore.activeVehicleProfileIdWasStored`). The RULE itself is
808: * pure and lives in `settingsStore.ts`
985: * R3 fix (Codex follow-up, binding, LOW): the connect-and-read attempt
986: * (`transport.connect()` -- which itself starts with a LAZY native
987: * `react-native-tcp-socket` import -- then the one-shot read) is raced
988: * against {@link VIN_READ_OVERALL_TIMEOUT_MS}. A hung `connect()` (a dead
989: * adapter, a native import that never resolves) therefore never holds the
990: * shared reservation past that bound: `finally` below closes the transport
991: * and releases the token EITHER way, whichever branch of the race actually
992: * settled it first. The losing side's eventual settlement (a `connect()`
993: * that resolves or rejects long after the timeout already won) is harmless --
994: * `Promise.race` itself attaches a handler to every promise it is given, so
995: * it can never surface as an unhandled rejection, and this function has
996: * already returned by then.
997: */
998:export async function maybeDetectVehicleFromVin(): Promise<string | null> {
999:  if (vinDetectionState !== 'idle') return cachedDetectedVin;
1000:  const settings = settingsStore.getSettings();
1001:  // Q1 (binding): ENET only -- the ELM327 session has no mode-09 support.
1002:  if (settings.adapterType !== 'enet') return null;
1003:  if (settings.enetHost.trim() === '') return null; // nothing configured to connect to.
1004:  if (sharedEnetAdapterReservation.holder() !== null) return null; // never steals a running flow's adapter.
1005:  const token = sharedEnetAdapterReservation.tryAcquire('signalFinder');
1006:  if (token === null) return null; // lost the race to acquire -- another trigger point retries later.
1007:
1008:  vinDetectionState = 'attempting';
1009:  const transport = new EnetTcpTransport({ host: settings.enetHost, port: settings.enetPort });
1010:  let vin: string | null = null;
1011:  try {
1012:    const attempt = (async (): Promise<string | null> => {
1013:      await transport.connect();
1014:      const channel = createRawUdsChannel(transport, settings.enetTesterAddress, VIN_READ_ECU);
1015:      return readVinFromChannel(channel);
1016:    })();
1017:    const timedOut = new Promise<'timeout'>((resolve) => {
1018:      setTimeout(() => resolve('timeout'), VIN_READ_OVERALL_TIMEOUT_MS);
1019:    });
1020:    const raced = await Promise.race([attempt, timedOut]);
1021:    vin = raced === 'timeout' ? null : raced;
1022:  } catch (error) {
1023:    console.warn('[composition] the one-shot VIN read failed', error);
1024:    vin = null;
1025:  } finally {
1026:    await transport.close().catch(() => undefined);
1027:    sharedEnetAdapterReservation.release(token);
1028:  }
1029:
1030:  vinDetectionState = 'done';
1031:  cachedDetectedVin = vin;
1032:  if (vin !== null) {
1033:    // Q2 (binding): logged so a pattern can be added LATER from the real
1034:    // car -- never invented here. R4 fix (binding, LOW): MASKED -- a VIN is
1035:    // personal data, and the full value is never written to a log line; the
1036:    // driver reads it in full only on the Signal Finder screen / `lastSeenVin`.
1037:    console.info(`[composition] VIN detected: ${maskVin(vin)}`);
1038:    settingsStore.update({ lastSeenVin: vin });
1039:    applyVinAutoSelect(vin);
1040:  }
1041:  return vin;
1042:}
1043:
1044:const baseTelemetryProvider: TelemetryProvider = createTelemetryProvider({
1045:  settingsStore,
1046:  monotonicNow: () => telemetryClock.now(),
1047:  isDev: telemetryIsDev,
1048:  readVehicleProfileBindings: () => vehicleProfileBindingsCache,
1049:});
1050:
1051:/**
1052: * Thin wrapper, not a second implementation: every method delegates straight
1053: * to {@link baseTelemetryProvider}, EXCEPT `start()`, which also fires a
1054: * defensive cache refresh (fire-and-forget -- see (c) above) before
1055: * delegating. Both of this app's `start()` call sites --
1056: * `startTelemetryRecording()` below and `TelemetryScreen.tsx`'s manual
1057: * monitor -- go through this SAME exported singleton, so neither needs its
1058: * own refresh call.
1059: */
1060:export const telemetryProvider: TelemetryProvider = {
1061:  ...baseTelemetryProvider,
1062:  start(): void {
1063:    void refreshVehicleProfileBindingsCache();
1064:    baseTelemetryProvider.start();
1065:    // Ticket P4q (binding): one of this app's two VIN-detection trigger
1066:    // points ("when ... telemetry connects") -- fire-and-forget, and called
1067:    // AFTER `baseTelemetryProvider.start()` on purpose: that call may itself
1068:    // synchronously acquire the SAME shared reservation (owner `'provider'`)
2745:  } catch (error) {
2746:    console.warn('[composition] stopping the test-loop provider failed', error);
2747:  }
2748:}
2749:
2750:/**
2751: * The moment lap 1 closes (P5d-FIX1 H1 + H3). Everything below is ONE
2752: * `lifecycleLock` section, and every step is awaited: the driver is only told
2753: * the track was learned once it is genuinely persisted, registered, selected
2754: * and being timed. A throw anywhere here surfaces as the controller's `error`
2755: * phase, with a retry -- never as a session quietly running on the previously
2756: * selected circuit.
2757: */
2758:async function adoptLearnedCircuit(circuit: TestLoopCircuit): Promise<void> {
2759:  const provider = testLoopProvider;
2760:  // From here on the learner is done, but every fix still arrives and is
2761:  // still kept -- the controller receives them after it is armed, in order.
2762:  testLoopHandoverActive = true;
2763:  provider?.beginHandover();
2764:  // P5d-FIX2 N7: the out-lap trace is the QUALIFIED lap the geometry was built
2765:  // from, not everything the buffer happens to hold by now.
2766:  const learningSamples = circuit.lapSamples;
2767:
2768:  await lifecycleLock.run(async () => {
2769:    // P5d-FIX2 N2 (Codex P5d-REV2 HIGH 2): every step below is recorded in a
2770:    // ledger, and every step is skipped when the ledger says it already
2771:    // happened. A retry after a failure half-way through therefore RESUMES --
2772:    // it never inserts a second circuit, starts a second session, or writes
2773:    // the out-lap trace twice.
2774:    const ledger = (adoptionProgress ??= { circuitId: circuit.profile.circuitId });
2775:    // P5d-FIX3 F10: the journal is on disk BEFORE the first side effect, so a
2776:    // kill at any point below leaves a state the next launch can finish or undo.
2777:    await stageAdoptionJournal({ circuitId: ledger.circuitId, stage: 'staged' });
2778:
2779:    // 1. Keep it. A learned circuit that cannot be stored cannot be analysed
2780:    //    after a restart, so this failing is a real failure, not a warning.
2781:    if (ledger.storedProfile === undefined) {
2782:      if (learnedCircuitStore !== null) {
2783:        const stored = await putLearnedCircuitWithFreshId(circuit);
2784:        ledger.storedProfile = stored.profile;
2785:      } else {
2786:        // Web dev preview: no database, so the learned circuit lives for this
2787:        // process only. It is still a real circuit while it lasts.
2788:        const profile = circuit.profile;
2789:        memoryLearnedCircuits = [
2790:          ...memoryLearnedCircuits.filter(
2791:            (entry) => entry.circuit.profile.circuitId !== profile.circuitId,
2792:          ),
2793:          {
2794:            circuit: { profile, runtime: circuit.runtime, corners: circuit.corners },
2795:            // Ticket D3: listed from the moment it is adopted, exactly like
2796:            // the stored path below.
2797:            listed: true,
2798:          },
2799:        ];
2800:        ledger.storedProfile = profile;
2801:      }
2802:      publishLearnedCircuits();
2803:      await stageAdoptionJournal({ circuitId: ledger.storedProfile.circuitId, stage: 'stored' });
2804:    }
2805:    const profile = ledger.storedProfile;
2806:
2807:    // 2. Select it, and rebuild the production controller for it. Both go
2808:    //    through the SAME unlocked routine `selectCircuit()` uses -- this
2809:    //    section already holds the lock, so the public wrapper would deadlock.
2810:    if (!ledger.selected) {
2811:      const entry = circuitCatalog.get(profile.circuitId);
2812:      if (entry === null) {
2813:        throw new Error(`the learned circuit ${profile.circuitId} could not be registered`);
2814:      }
2815:      await unlockedApplySelection(entry);
2816:      if (productionControllerCircuitId !== profile.circuitId) {
2817:        await unlockedRebuildProductionController();
2818:      }
2819:      ledger.selected = true;
2820:      await stageAdoptionJournal({ circuitId: profile.circuitId, stage: 'selected' });
2821:    }
2822:    const ctrl = controller;
2823:    if (ctrl === null) throw new Error('no production controller to run the learned circuit on');
2824:
2825:    // 3. Start the session on it and arm it. `start('session')` deliberately
2826:    //    skips calibration (the same path `resumeRecovery()` uses): the
2827:    //    geometry came from this driver, on this road, minutes ago -- there is
2828:    //    nothing a recognition lap could add to it.
2829:    if (ledger.sessionId === undefined) {
2830:      await ctrl.start('session');
2831:      ctrl.arm();
2832:      const startedSessionId = ctrl.diagnostics().sessionId;
2833:      if (startedSessionId === null) throw new Error('the learned-circuit session did not start');
2834:      ledger.sessionId = startedSessionId;
2835:      await stageAdoptionJournal({
2836:        circuitId: profile.circuitId,
2837:        stage: 'session-started',
2838:        sessionId: startedSessionId,
2839:      });
2840:    }
2841:    const sessionId = ledger.sessionId;
2842:
2843:    // 4. The recording pipeline the learn phase was already running now
2844:    //    belongs to a session: same hooks a normal start goes through.
2845:    //
2846:    //    P5d-FIX2 N3: the learn-phase subscriptions are detached in the SAME
2847:    //    synchronous step that attaches the session's recorder -- no await
2848:    //    between them, so no sample can ever reach both and be written twice.
2849:    if (!ledger.recordingStarted) {
2850:      detachTestLoopRecording();
2851:      startTelemetryRecording(sessionId);
2852:      const startedAtUtc = new Date().toISOString();
2853:      initializeSessionStage(sessionId, startedAtUtc);
2854:      ledger.recordingStarted = true;
2855:      if (db !== null) {
2856:        await setActiveSession(db, { sessionId, circuitId: profile.circuitId, startedAtUtc });
2857:      }
2858:      await flushTestLoopTelemetry();
2859:      await releaseTestLoopGForce();
2860:      await stageAdoptionJournal({ circuitId: profile.circuitId, stage: 'recording', sessionId });
2861:    }
2862:
2863:    // 5. The learning lap is STORED as the session's out-lap trace (lap 0),
2864:    //    so the drive that defined the track is not lost.
2865:    if (!ledger.outLapSaved) {
2866:      if (learningSamples.length > 0 && repository !== null) {
2867:        await repository.saveTelemetry(sessionId, 0, learningSamples);
2868:      }
2869:      ledger.outLapSaved = true;
2870:      await stageAdoptionJournal({ circuitId: profile.circuitId, stage: 'out-lap', sessionId });
2871:    }
2872:
2873:    // 6. Finally, hand the whole backlog to the controller in order -- the
2874:    //    learning lap first, then every fix that arrived while this ran. From
2875:    //    the next fix onwards the session is simply live.
2876:    provider?.flushBuffered();
2877:    // P5d-FIX2 N5: the handover is over. The module lets go of the buffering
2878:    // provider (the running controller keeps its own reference) so every later
2879:    // rebuild takes the ORDINARY provider-cleanup path again.
2880:    testLoopProvider = null;
2881:    testLoopHandoverActive = false;
2882:    adoptionProgress = null;
2883:    // The adoption is complete: nothing is left for a later launch to repair.
2884:    await clearOwnAdoptionJournal();
2885:  });
2886:}
2887:
2888:/**
2889: * P5d-FIX1 item 10: a plain INSERT, with a fresh id on the (vanishingly
2890: * unlikely) collision, instead of an INSERT OR REPLACE that would silently
3000:      if (activeCircuitId === circuitId) {
3001:        return { ok: false as const, reason: 'active-session' as const };
3002:      }
3003:    }
3004:    const result = await store.remove(circuitId);
3005:    if (result.ok) {
3006:      publishLearnedCircuits();
3007:      // The deleted circuit may have been the selected one; fall back to the
3008:      // catalog's own default rather than leaving a dangling selection.
3009:      if (settingsStore.getSettings().selectedCircuitId === circuitId) {
3010:        const fallback = circuitCatalog.get(TMR_CIRCUIT_PROFILE.circuitId);
3011:        if (fallback !== null) await unlockedApplySelection(fallback);
3012:      }
3013:    }
3014:    return result;
3015:  });
3016:}
3017:
3018:
3019:/**
3020: * Runs the full bootstrap sequence: opens the on-device SQLite database,
3021: * builds the real `SessionController` + `GnssLocationProvider`, checks for a
3022: * recoverable checkpoint, and swaps every wrapper above from its in-memory
3023: * placeholder to the real implementation. Named (not an anonymous IIFE) and
3024: * re-invocable -- F3 fix's `retryBootstrap()` below calls it again from a
3025: * clean slate after a failed attempt.
3026: */
3027:async function runBootstrap(): Promise<void> {
3028:  try {
3029:    // F3 fix: clean slate -- discard anything a previous, failed attempt may
3030:    // have partially built, so a retry never reuses a possibly-inconsistent
3031:    // db/controller/facade left over from that attempt.
3032:    db = null;
3033:    repository = null;
3034:    controller = null;
3035:    productionFacade = null;
3036:    gnssProvider = null;
3037:    historyStore = null;
3038:    activeController = null;
3039:    // Ticket P5d: a retry re-reads the learned circuits from the database it
3040:    // is about to reopen -- never from a half-built previous attempt.
3041:    learnedCircuitStore = null;
3042:
3043:    if (IS_WEB_RUNTIME) {
3044:      // Web preview is a development surface only: expo-sqlite's wasm backend
3045:      // (wa-sqlite/OPFS) throws 'disk I/O error' in embedded browsers. Fall
3046:      // back to the in-memory repository -- no persistence across reloads,
3047:      // which is acceptable for the dev preview and keeps every session flow
3048:      // (timing, PB, history) fully functional. Native builds are unaffected.
3049:      console.warn('[composition] web preview: using in-memory storage (expo-sqlite web backend unavailable)');
3050:      repository = new InMemorySessionRepository();
3051:    } else {
3052:      const opened = await openAppDatabase(DB_NAME);
3053:      db = opened.db;
3054:      // P10B H5-B: the gate itself is no longer wired anywhere by hand --
3055:      // `opened.db` acquires it around every call made through it, which is
3056:      // what stops a standalone write from executing inside (and being
3057:      // rolled back by) an unrelated transaction.
3058:      repository = opened.repository;
3059:    }
3060:
3061:    // P4l-FIX3 J5 (binding): primes the vehicle-profile bindings cache as
3062:    // soon as `db` is available, so a binding confirmed by an EARLIER app
3063:    // run is already live for the very first `telemetryProvider.start()` of
3064:    // THIS run, not just ones after the next Signal Finder confirm. Never
3065:    // awaited -- `refreshVehicleProfileBindingsCache()` never throws, and
3066:    // bootstrap must never wait on it.
3067:    void refreshVehicleProfileBindingsCache();
3068:
3069:    gnssProvider = new GnssLocationProvider();
3070:
3071:    // Settings are hydrated BEFORE the production controller is built
3072:    // (reordered ahead of its historical position further down) so
3073:    // `createProductionController()`'s read of `settingsStore.getSettings()
3074:    // .coachingEnabled` (Phase 3 coaching addendum) reflects the user's
3075:    // actually-persisted preference on the very first controller this
3076:    // process builds -- not the in-memory placeholder's default. `facade`
3077:    // itself still stays on the inert `PendingFacade` regardless (that gate
3078:    // is `activateProductionFacade()` below), so this reorder changes
3079:    // nothing about when commands become live.
3080:    if (db !== null) {
3081:      const settings = await SqlSettingsStore.create(db);
3082:      settingsWrapper.setInner(settings);
3083:      // Ticket P4p G1 (binding): the ONE-TIME initial-profile migration, run
3084:      // here because it needs BOTH the hydrated settings (did the row ever
3085:      // carry a choice?) and the persisted bindings. Awaited -- the cache
3086:      // refresh right after it must read the profile this decided, and the
3087:      // whole thing is bounded by two small SQLite reads. Never throws.
3088:      await applyInitialActiveVehicleProfile(settings);
3089:      // The prime above ran before settings existed, so it read the DEFAULT
3090:      // profile; re-read now that the ACTIVE one is known.
3091:      await refreshVehicleProfileBindingsCache();
3092:      // Ticket P7R E2: which stored sessions were run on matching the
3093:      // calibration gate rejected. Hydrated here so `SessionHistoryScreen`
3094:      // (a synchronous render, like every other list it draws) can label
3095:      // them without an effect of its own. Never throws.
3096:      unvalidatedMatchingSessionIds = new Set(await readUnvalidatedMatchingSessionIds(db));
3097:    }
3098:
3099:    // Ticket P5d T6 (binding): learned circuits are registered in the catalog
3100:    // BEFORE the production controller and the history store are built, so a
3650:
3651:export async function deleteAllStoredUserData(): Promise<AggregatedDeleteUserDataResult> {
3652:  // Lifecycle lock amendment (binding): delete-all wipes the very data the
3653:  // recovery/selection/rebuild operations read and write, so it runs as one
3654:  // section of the SAME lock they do -- never interleaved with a resume, a
3655:  // selection change, or a controller rebuild.
3656:  return lifecycleLock.run(unlockedDeleteAllStoredUserData);
3657:}
3658:
3659:async function unlockedDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDataResult> {
3660:  const { repository: repo } = await ready();
3661:  // C fix (contracts.md's facade boundary amendment, binding, ticket
3662:  // CN-FIX4): durability. Two things had to change for "delete-all means
3663:  // deleted" to actually hold.
3664:  //
3665:  // 1. REFUSE while a session is genuinely mid-drive. A live controller goes
3666:  //    on writing checkpoints/laps/PBs by design, so a wipe underneath it
3667:  //    would be re-populated moments later -- and silently tearing the
3668:  //    driver's session down instead is not this action's job.
3669:  if (activeController !== null && MID_SESSION_STATES.has(currentControllerState(activeController))) {
3670:    return {
3671:      ok: false,
3672:      remainingSessionCount: 0,
3673:      referenceLapCleared: false,
3674:      failedCircuitIds: [],
3675:      reason: 'SESSION_ACTIVE',
3676:      errorText: 'a session is currently active -- end it before deleting all data',
3677:    };
3678:  }
3679:  // 1b. REFUSE while a `__DEV__` DevReplay controller is installed (CN-FIX5
3680:  //    item 3, closing amendment, binding). A replay controller sits in
3681:  //    `calibrating`/`calibrationReview`/`armed` -- none of them
3682:  //    `MID_SESSION_STATES` -- and step 2's rebuild deliberately no-ops
3683:  //    while it owns `facade`, so the wipe would leave a live controller
3684:  //    holding a session id that persists its session/checkpoint right back
3685:  //    over the deleted data. Durability wins over convenience on the dev
3686:  //    path: leave the replay, refuse the delete.
3687:  if (controller !== null && activeController !== controller) {
3688:    return {
3689:      ok: false,
3690:      remainingSessionCount: 0,
3691:      referenceLapCleared: false,
3692:      failedCircuitIds: [],
3693:      reason: 'DEV_REPLAY_ACTIVE',
3694:      errorText: 'a developer replay or mock session is active -- leave the Dev Replay screen before deleting all data',
3695:    };
3696:  }
3697:  // 2. Otherwise: drop any pending recovery and REPLACE the production
3698:  //    controller with a fresh, idle one BEFORE deleting anything. A
3699:  //    terminal (`sessionComplete`) controller still holds a `sessionId`
3700:  //    and a full lap set, so anything that later touches it --
3701:  //    `checkpointNow()` from a background transition, a straggler
3702:  //    `endSession()` persistence path -- would re-create rows this wipe
3703:  //    just removed. A fresh controller has no session identity at all, so
3704:  //    there is nothing left to re-persist. (`endSession()` itself is now
3705:  //    serialized on this same lock, so an in-flight one has already
3706:  //    completed by the time this section runs.)
3707:  setPendingRecovery(null);
3708:  setRecoveryNotice(null);
3709:  await unlockedRebuildProductionController();
3710:  // Multi-circuit selection addendum (ticket CN-W3): delete-all spans EVERY
3711:  // bundled circuit, not just the currently selected one -- `deleteUserData`
3712:  // itself is a single per-userId wipe (not circuit-scoped), so this loop's
3713:  // real job is the per-circuit VERIFY-EMPTY check; success requires every
3714:  // bundled circuit to come back empty.
3715:  //
3716:  // M3 fix (binding): each circuit's call is its own try/catch -- a REJECTED
3717:  // per-circuit `deleteAllUserData()` (e.g. a transient SQLite error) no
3718:  // longer aborts the loop; every remaining bundled circuit is still
3719:  // attempted, and the rejection is reflected in the aggregate `ok: false`
3720:  // plus `failedCircuitIds`/`errorText` instead of silently stopping partway
3721:  // through (previously leaving later circuits' data untouched with no
3722:  // indication why).
3723:  const perCircuitResults: DeleteUserDataResult[] = [];
3724:  const failedCircuitIds: string[] = [];
3725:  for (const summary of circuitCatalog.list()) {
3726:    try {
3727:      perCircuitResults.push(
3728:        await deleteAllUserData(repo, LOCAL_USER_ID, summary.circuitId, summary.layoutId, summary.layoutVersion),
3729:      );
3730:    } catch (error) {
3731:      console.warn(`[composition] deleteAllStoredUserData: circuit "${summary.circuitId}" failed`, error);
3732:      failedCircuitIds.push(summary.circuitId);
3733:    }
3734:  }
3735:  const perCircuitOk = failedCircuitIds.length === 0 && perCircuitResults.every((r) => r.ok);
3736:  const result: DeleteUserDataResult = {
3737:    ok: perCircuitOk,
3738:    remainingSessionCount: perCircuitResults.reduce((sum, r) => sum + r.remainingSessionCount, 0),
3739:    referenceLapCleared: perCircuitResults.length > 0 && perCircuitResults.every((r) => r.referenceLapCleared),
3740:  };
3741:  // F7 fix (WPT3, binding): `telemetry_samples` (Telemetry addendum) is a
3742:  // mobile-owned table (`persistence/telemetrySchema.ts`) that
3743:  // `@circuit/core`'s `deleteAllUserData`/`LocalSessionRepository.deleteUserData`
3744:  // (packages/core, out of this ticket's write set) has no knowledge of at
3745:  // all -- delete it here, in the SAME flow, and verify it actually landed
3746:  // empty before ever reporting success, so the UI's "All stored data
3747:  // deleted" banner is never shown while up to 200,000 rows/session remain.
3748:  //
3749:  // N4 fix (lifecycle lock amendment, binding, ticket CN-FIX3): this step is
3750:  // NO LONGER gated on the per-circuit aggregate. A rejected circuit delete
3751:  // used to suppress the telemetry DELETE + verify-empty entirely, leaving up
3752:  // to 200,000 rows/session on disk after the user asked for a full wipe. The
3753:  // telemetry step always runs; the aggregate `ok` is `circuitsOk &&
3754:  // telemetryOk`, and `errorText` names whichever half failed.
3755:  let telemetryOk = true;
3756:  if (db !== null) {
3757:    // N1-confirm residue fix (LEAD): a session's final telemetry flush can
3758:    // still be in flight when the user reaches delete-all -- await any
3759:    // in-progress shutdown first, then run DELETE + verify while HOLDING the
3760:    // shared write gate, so no straggler batch INSERT can land between (or
3761:    // after) them. `stopTelemetryRecording()` is an idempotent no-op when
3762:    // nothing is recording.
3763:    await stopTelemetryRecording();
3764:    const database = db;
3765:    try {
3766:      // P10B H5-B: expressed as a TRANSACTION rather than as a gate hold.
3767:      // The gated database now acquires the gate around every call made
3768:      // through it, so holding it here as well would deadlock -- and the
3769:      // DELETE + verify pair needs to be indivisible anyway, which is what
3770:      // `withTransactionAsync` (and only it) actually provides.
3771:      let remainingRows = 1;
3772:      await database.withTransactionAsync(async (tx) => {
3773:        await tx.runAsync('DELETE FROM telemetry_samples');
3774:        const remaining = await tx.getAllAsync<{ count: number }>(
3775:          'SELECT COUNT(*) AS count FROM telemetry_samples',
3776:        );
3777:        remainingRows = remaining[0]?.count ?? 0;
3778:      });
3779:      telemetryOk = remainingRows === 0;
3780:    } catch (error) {
3781:      // N4 (binding): symmetric with the per-circuit try/catch above -- a
3782:      // rejected telemetry delete/verify is reported as `ok: false` plus the
3783:      // fixed "telemetry" phrase in `errorText`, never as raw exception text
3784:      // and never as a rejection escaping into the UI's delete handler.
3785:      console.warn('[composition] deleteAllStoredUserData: telemetry deletion failed', error);
3786:      telemetryOk = false;
3787:    }
3788:  }
3789:  // HANDOFF release blocker: the VIN, the vehicle bindings and sweep
3790:  // records, and the learned circuits live outside every table the steps
3791:  // above touch. Always attempted, like the telemetry step; a learned circuit
3792:  // a surviving session still needs is kept and reported as remaining.
3793:  let deviceDataOk = true;
3794:  if (db !== null) {
3795:    try {
3796:      const wipe = await wipeDeviceUserData(db);
3797:      deviceDataOk = wipe.ok;
3798:      if (!wipe.ok) {
3799:        console.warn('[composition] deleteAllStoredUserData: device data remained', wipe.remaining);
3800:      }
3801:    } catch (error) {
3802:      console.warn('[composition] deleteAllStoredUserData: device data deletion failed', error);
3803:      deviceDataOk = false;
3804:    }
3805:  }
3806:  await forgetDeviceUserDataInMemory();
3807:  const finalResult: DeleteUserDataResult = { ...result, ok: result.ok && telemetryOk && deviceDataOk };
3808:  // M4 fix (binding): a leftover active-session pointer is meaningless once
3809:  // all stored data is gone -- cleared together, the SAME way session
3810:  // end/discard/vanished-checkpoint do (`setActiveSession(db, null)`).
3811:  if (finalResult.ok && db !== null) await setActiveSession(db, null);
3812:  // Ticket P7R E2: the unvalidated-matching log is a list of facts about
3813:  // sessions that no longer exist -- cleared with them, for the same reason
3814:  // the active-session pointer above is. Best-effort, exactly like the log's
3815:  // own writes: a stale label on a deleted session must never turn a
3816:  // successful wipe into a failed one.
3817:  if (finalResult.ok) {
3818:    unvalidatedMatchingSessionIds = new Set();
3819:    if (db !== null && !(await clearUnvalidatedMatchingSessionIds(db))) {
3820:      console.warn('[composition] could not clear the unvalidated-matching log');
3821:    }
3822:  }
3823:  if (finalResult.ok && historyStore !== null) await historyStore.refresh();
3824:
3825:  const errorText = finalResult.ok
3826:    ? null
3827:    : [
3828:        failedCircuitIds.length > 0 ? `circuits rejected: ${failedCircuitIds.join(', ')}` : null,
3829:        !perCircuitOk && failedCircuitIds.length === 0 ? 'one or more circuits did not verify empty' : null,
3830:        !telemetryOk ? 'telemetry rows remained or could not be deleted' : null,
3831:        !deviceDataOk ? 'vehicle or learned-circuit data remained or could not be deleted' : null,
3832:      ]
3833:        .filter((part): part is string => part !== null)
3834:        .join('; ') || 'delete-all failed';
3835:
3836:  return { ...finalResult, failedCircuitIds, errorText };
3837:}
3838:
3839:// ---------------------------------------------------------------------------
3840:// DevReplayScreen support (MUST DO #6, __DEV__ only).
3841://
3842:// F4 fix (C6 residue): `startDevReplaySession` / `restoreProductionFacade` /
3843:// `useMockFacadeForDevReplay` all mutate the SAME shared state
3844:// (`replayController`/`activeController`/`facadeWrapper`'s inner) but
3845:// previously ran independently -- a rapid start/restore/start "storm"
3846:// (double-tapping a fixture row, `DevReplayScreen` unmounting mid-transition)
3847:// could interleave them.
3848://
3849:// N5 fix (lifecycle lock amendment, binding, ticket CN-FIX3): the separate
3850:// `withDevReplayLock` queue is GONE -- every replay transition is a section of
3851:// the one `lifecycleLock`, and the screen's whole restore -> select -> start
3852:// sequence is ONE section (`runDevReplayScenario`) rather than three
3853:// independently-locked steps a concurrent unmount cleanup could slot between.
3854:// Cleanup that fires mid-scenario therefore runs strictly AFTER the scenario,
3855:// and the scenario's own `isCancelled()` checks keep it from installing a
3856:// replay (or reporting success) once the screen it belonged to is gone.
3857:// ---------------------------------------------------------------------------
3858:
3859:/** Flushes a stale replay controller's pending persistence (F4 fix: a completed lap's telemetry/checkpoint/PB write) BEFORE disposing it, so switching away mid-replay can never drop a just-finished lap. A flush failure is logged, not thrown -- it must never block the transition it's guarding (`restoreProductionFacade()` runs unconditionally, including from `DevReplayScreen`'s fire-and-forget unmount cleanup). */
3860:async function flushAndDisposeReplay(stale: SessionController): Promise<void> {
3861:  await stale.flush().catch((error: unknown) => {
3862:    console.warn('[composition] replay flush before transition failed', error);
3863:  });
3864:  await stale.dispose();
3865:}
3866:
3867:/**
3868: * Restores `facade` to the production controller (C6 fix): disposes any
3869: * active DevReplay controller first (so its provider/watchdog cannot keep
3870: * running after the swap), then re-points `facadeWrapper` back at the
/**
 * N1 fix (WPT3 re-verify, LEAD takeover), extended by ticket P10B H5-B:
 * mutual exclusion for database work on the single on-device SQLite
 * connection.
 *
 * The predecessor design (`serializeSqlDatabase`, removed) queued EVERY call
 * -- including the statements a transaction callback itself issues -- onto one
 * FIFO tail. `withTransactionAsync` then occupied the queue while its callback
 * awaited `runAsync`, which was queued BEHIND it: a guaranteed self-deadlock
 * for every repository transaction (session save, PB replacement, delete-all).
 * The reaction to that was to gate ONLY whole transactions and let every
 * standalone statement through ungated -- which is the defect ticket P10B
 * H5-B is about.
 *
 * WHAT WENT WRONG WITH "STANDALONE STATEMENTS ARE ATOMIC ON THEIR OWN".
 * They are, in isolation. But SQLite has no statement-level isolation from an
 * ALREADY OPEN transaction on the SAME connection: a `runAsync` issued while
 * some other unit sits between BEGIN and COMMIT simply becomes part of that
 * transaction, and is undone with it. The P10B reviewer reproduced it: hold
 * the initial session transaction at `DELETE FROM laps`, flush ten trace
 * fixes (nine chunk rows written and reported persisted), then fail the
 * session transaction -- rollback took every one of those chunk rows with it,
 * while the recorder still reported ten fixes persisted and zero unwritten.
 *
 * THE FIX IS TRANSACTION-SCOPED ACCESS, not "gate less". Every call made
 * through the wrapper -- standalone statements included -- now acquires the
 * gate, so nothing can execute inside another unit's open transaction. The
 * statements a transaction ITSELF issues no longer come through the wrapper
 * at all: `withTransactionAsync` hands its callback a `tx` handle bound
 * straight to the connection (see {@link SqlDatabase.withTransactionAsync}),
 * so the holder's own critical section never queues behind itself. That is
 * what makes full serialization deadlock-free where the original attempt was
 * not.
 *
 * Callers must therefore never reach for the outer handle inside a
 * transaction callback, and never wrap a statement issued through the gated
 * handle in `gate.exclusive(...)` of their own -- both re-enter the gate and
 * deadlock. Multi-statement atomicity is expressed with
 * `withTransactionAsync`, which is the only thing that ever needed it.
 */
import type { SqlDatabase } from '@circuit/core/src/persistence-sql';

export interface SqlWriteGate {
  /** Runs `op` with the gate held; queued FIFO behind any current holder. Never swallows `op`'s rejection. */
  exclusive<T>(op: () => Promise<T>): Promise<T>;
}

export function createSqlWriteGate(): SqlWriteGate {
  let tail: Promise<unknown> = Promise.resolve();
  return {
    exclusive<T>(op: () => Promise<T>): Promise<T> {
      const result = tail.then(op, op);
      tail = result.then(
        () => undefined,
        () => undefined,
      );
      return result;
    },
  };
}

/** No-op gate for tests / in-memory paths that have no shared connection to protect. */
export const PASSTHROUGH_WRITE_GATE: SqlWriteGate = { exclusive: (op) => op() };

/**
 * Ticket P10B H5-B: wraps a `SqlDatabase` so EVERY unit of work -- a whole
 * transaction, or one standalone statement -- acquires `gate` for its whole
 * duration. A standalone write can therefore no longer execute inside (and be
 * rolled back by) an unrelated transaction.
 *
 * The statements a transaction issues do not pass through here: they go
 * through the `tx` handle `inner.withTransactionAsync` supplies, straight to
 * the connection the gate is already held for. See the module doc comment.
 */
export function gateSqlTransactions(inner: SqlDatabase, gate: SqlWriteGate): SqlDatabase {
  return {
    execAsync: (sql) => gate.exclusive(() => inner.execAsync(sql)),
    runAsync: (sql, params) => gate.exclusive(() => inner.runAsync(sql, params)),
    getAllAsync: (sql, params) => gate.exclusive(() => inner.getAllAsync(sql, params)),
    withTransactionAsync: (fn) => gate.exclusive(() => inner.withTransactionAsync(fn)),
  };
}
11: * The journal is one additive `settings` row -- the same table the
28:export const ADOPTION_JOURNAL_KEY = 'testLoopAdoption';
88:/** Writes (or overwrites) the journal for the adoption in progress, returning the row it wrote. */
89:export async function writeAdoptionJournal(
94:  await db.runAsync('INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)', [
95:    ADOPTION_JOURNAL_KEY,
107:    'SELECT value FROM settings WHERE key = ? LIMIT 1',
108:    [ADOPTION_JOURNAL_KEY],
124:    'SELECT value FROM settings WHERE key = ? LIMIT 1',
125:    [ADOPTION_JOURNAL_KEY],
139:    'UPDATE settings SET value = ? WHERE key = ? AND value = ?',
140:    [serialized, ADOPTION_JOURNAL_KEY, raw],
155:  const result = await db.runAsync('DELETE FROM settings WHERE key = ? AND value = ?', [
156:    ADOPTION_JOURNAL_KEY,
177:export const ACTIVE_SESSION_SETTINGS_KEYS = [
196:      'SELECT value FROM settings WHERE key = ? LIMIT 1',
197:      [ACTIVE_SESSION_SETTINGS_KEYS[0]],
200:      for (const key of ACTIVE_SESSION_SETTINGS_KEYS) {
201:        await tx.runAsync('DELETE FROM settings WHERE key = ?', [key]);


Count    : 132
Average  : 
Sum      : 
Maximum  : 
Minimum  : 
Property : 




codex
The database gate serializes the wipe transaction, but it doesn’t cancel an in-flight VIN read or cover the later settings reset. I’m tracing whether that can restore the VIN after verification, and checking adoption retries and settings rows omitted from the wipe.

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -n 'INSERT|runAsync|CREATE TABLE' apps/mobile/src packages/core/src --glob '*.tsx'; rg -n 'unlockedApplySelection|unlockedRebuildProductionController|publishLearnedCircuits|MID_SESSION_STATES|ACTIVE_SESSION.*KEY' apps/mobile/src/session/composition.ts; "'$s=Get-Content apps/mobile/src/session/composition.ts; foreach($r in @(@(1750,1860),@(3038,3125),@(2350,2448),@(2640,2748))) {for($i=$r[0];$i -le $r[1];$i++){'"'{0}:{1}' -f "'$i,$s[$i-1]}}; Get-Content apps/mobile/src/session/adoptionJournal.ts -TotalCount 100; Get-Content apps/mobile/src/session/learnedCircuitStore.ts -TotalCount 125; rg -n '"'"'^[ ]+[a-zA-Z].*:|DEFAULT_SETTINGS'"' apps/mobile/src/session/settingsStore.ts" in D:\CODE\APLICTIE_Circuit
 succeeded in 252ms:
159:const ACTIVE_SESSION_SETTINGS_KEY = 'activeSessionId';
160:/** M4 fix (contracts.md's "Multi-circuit selection — recovery amendment", binding): the circuit the session named by `ACTIVE_SESSION_SETTINGS_KEY` actually started on -- written/cleared in the SAME transaction as the session id (`setActiveSession` below), so bootstrap recovery never has to guess which bundled circuit a crashed, never-`endSession()`'d checkpoint belongs to. */
161:const ACTIVE_SESSION_CIRCUIT_SETTINGS_KEY = 'activeSessionCircuitId';
162:/** Ticket P5c-FIX1 E7: the instant the session named by `ACTIVE_SESSION_SETTINGS_KEY` started, written/cleared in the SAME transaction as the id -- so a RECOVERED outing can head its pit view with its own start date instead of the moment it was resumed. */
163:const ACTIVE_SESSION_STARTED_SETTINGS_KEY = 'activeSessionStartedAtUtc';
200: * comment for the mutex semantics, and `unlockedRebuildProductionController()`
1575:    if (!MID_SESSION_STATES.has(currentControllerState(ctrl))) return;
1584:    ACTIVE_SESSION_SETTINGS_KEY,
1589:/** Ticket P5c-FIX1 E7: companion read for `ACTIVE_SESSION_STARTED_SETTINGS_KEY` -- `null` for a session started before this key existed. */
1592:    ACTIVE_SESSION_STARTED_SETTINGS_KEY,
1597:/** M4 fix: companion read for `ACTIVE_SESSION_CIRCUIT_SETTINGS_KEY` -- `null` when absent (a legacy checkpoint written before this key existed, or nothing active). */
1600:    ACTIVE_SESSION_CIRCUIT_SETTINGS_KEY,
1626:      await tx.runAsync('DELETE FROM settings WHERE key = ?', [ACTIVE_SESSION_SETTINGS_KEY]);
1627:      await tx.runAsync('DELETE FROM settings WHERE key = ?', [ACTIVE_SESSION_CIRCUIT_SETTINGS_KEY]);
1628:      await tx.runAsync('DELETE FROM settings WHERE key = ?', [ACTIVE_SESSION_STARTED_SETTINGS_KEY]);
1631:        ACTIVE_SESSION_SETTINGS_KEY,
1635:        ACTIVE_SESSION_CIRCUIT_SETTINGS_KEY,
1640:          ACTIVE_SESSION_STARTED_SETTINGS_KEY,
1893:async function unlockedRebuildProductionController(): Promise<void> {
1997:      await unlockedRebuildProductionController();
2084:const MID_SESSION_STATES = new Set<SessionState>(['outLap', 'timing', 'inPit', 'paused']);
2114:async function unlockedApplySelection(entry: BundledCircuit): Promise<void> {
2137:    await unlockedRebuildProductionController();
2173: * be observed disagreeing. See `unlockedApplySelection()`.
2196:    await unlockedApplySelection(entry);
2318:      await unlockedRebuildProductionController();
2395:function publishLearnedCircuits(): void {
2593:      publishLearnedCircuits();
2802:      publishLearnedCircuits();
2815:      await unlockedApplySelection(entry);
2817:        await unlockedRebuildProductionController();
2896: * `publishLearnedCircuits` maps to `listed: false` and `circuitCatalog.list()`
2971:  publishLearnedCircuits();
3006:      publishLearnedCircuits();
3011:        if (fallback !== null) await unlockedApplySelection(fallback);
3116:    publishLearnedCircuits();
3143:      // complementary half of N3 lives in `unlockedApplySelection()`: a
3172:        if (terminal || circuitChanged) await unlockedRebuildProductionController();
3428:        await unlockedApplySelection(recoveryEntry);
3436:        await unlockedRebuildProductionController();
3638:  publishLearnedCircuits();
3643:      if (fallback !== null) await unlockedApplySelection(fallback);
3669:  if (activeController !== null && MID_SESSION_STATES.has(currentControllerState(activeController))) {
3682:  //    `MID_SESSION_STATES` -- and step 2's rebuild deliberately no-ops
3709:  await unlockedRebuildProductionController();
4057: * immediately before `unlockedApplySelection()`. Once that write has begun it
4099:    await unlockedApplySelection(entry);
1750:  // Ticket P5c-B: "one change per corner per STINT" is scoped to the outing,
1751:  // and so is everything the pit view reads -- a session starts from a clean
1752:  // journal and an empty stint cache.
1753:  suggestionJournal.clear(sessionId);
1754:  // Ticket P14 H6: and this process is now the one recording for it, so a
1755:  // later empty read of the journal is a fact about the session rather than a
1756:  // fact about the journal being in memory only.
1757:  suggestionJournal.markObserved(sessionId);
1758:  // Ticket P14 H7: the car this session is being driven with, captured NOW.
1759:  void snapshotSessionVehicleProfile(sessionId);
1760:  stintRunner?.clear();
1761:  stintTraceCache.clear();
1762:  // P5c-FIX2 L16 wiring (LEAD): a batch queued by a PREVIOUS session must not
1763:  // survive into this one -- discard it loudly through the coach's own path.
1764:  stintCoach?.cancelPending(`session-started:${sessionId}`);
1765:  lastStintLapCount = 0;
1766:  stintBoundaries?.reset();
1767:}
1768:
1769:/**
1770: * Callbacks shared by every production `RealSessionFacade` (initial
1771: * bootstrap AND any later C1 terminal-state rebuild), so the
1772: * active-session-pointer/history-refresh wiring can never drift between
1773: * them. `circuitId` (M4 fix) is the circuit THIS specific controller was
1774: * built for -- captured by the caller (`buildProductionController()`) at the
1775: * exact moment it's known, so `onSessionStarted` persists the RIGHT circuit
1776: * alongside the session id it starts, even if the selection changes again
1777: * later (never mid-session, per H2, but defense in depth regardless).
1778: */
1779:function productionFacadeCallbacks(circuitId: string): RealSessionFacadeCallbacks {
1780:  return {
1781:    onSessionStarted: (sessionId) => {
1782:      const startedAtUtc = new Date().toISOString();
1783:      if (db !== null) void setActiveSession(db, { sessionId, circuitId, startedAtUtc });
1784:      initializeSessionStage(sessionId, startedAtUtc);
1785:      startTelemetryRecording(sessionId);
1786:    },
1787:    onSessionEnded: () => {
1788:      // P5c-FIX2 L16 wiring (LEAD): the session is over -- a queued cue-update
1789:      // batch may never be applied later; discard it loudly, never silently.
1790:      stintCoach?.cancelPending('session-ended');
1791:      // F2 fix (WPT3, binding): both kicked off independently (neither
1792:      // awaits/depends on the other), then joined via `Promise.allSettled`
1793:      // so an error in either is isolated -- a rejected `controllerPersistence`
1794:      // (the active-session-pointer clear + history refresh) can never
1795:      // suppress the telemetry flush, and vice versa. `telemetryFinalFlush`
1796:      // is ALSO already independently guaranteed to run by
1797:      // `SwappableFacade`'s `endSessionSideEffect` hook (fires the instant
1798:      // `facade.endSession()` was called, before this success-only callback
1799:      // even exists to be scheduled) -- calling `stopTelemetryRecording()`
1800:      // again here is a defensive, idempotent no-op in the common case, and
1801:      // matters only if that hook were ever bypassed.
1802:      const controllerPersistence = (async () => {
1803:        if (db !== null) await setActiveSession(db, null);
1804:        await historyStore?.refresh();
1805:      })();
1806:      const telemetryFinalFlush = stopTelemetryRecording();
1807:      void Promise.allSettled([controllerPersistence, telemetryFinalFlush]).then((results) => {
1808:        for (const result of results) {
1809:          if (result.status === 'rejected') {
1810:            console.warn('[composition] session-end persistence step failed', result.reason);
1811:          }
1812:        }
1813:      });
1814:    },
1815:  };
1816:}
1817:
1818:/**
1819: * Builds a fresh production `SessionController` + `RealSessionFacade` and
1820: * assigns them to `controller`/`activeController`/`productionFacade` --
1821: * WITHOUT swapping `facadeWrapper`'s inner (F3 fix: that swap is
1822: * `activateProductionFacade()` below, done separately so callers that need
1823: * a staging window -- initial bootstrap -- can build first and activate
1824: * only once everything else is actually ready).
1825: */
1826:function buildProductionController(): void {
1827:  const freshController = createProductionController();
1828:  controller = freshController;
1829:  activeController = freshController;
1830:  // M4 fix: `createProductionController()` (just above, inside this same
1831:  // call) always sets `productionControllerCircuitId` before returning --
1832:  // capture it NOW so `onSessionStarted` persists the circuit THIS
1833:  // controller actually runs, not whatever `selectedCircuitId` reads at the
1834:  // moment a session happens to start (which -- while never reachable
1835:  // mid-session, per H2 -- is a strictly weaker guarantee than closing over
1836:  // the value already fixed for this controller's whole lifetime).
1837:  const circuitId = productionControllerCircuitId;
1838:  if (circuitId === null) {
1839:    throw new Error('composition: productionControllerCircuitId not set after createProductionController()');
1840:  }
1841:  productionFacade = new RealSessionFacade(freshController, productionFacadeCallbacks(circuitId));
1842:}
1843:
1844:/**
1845: * Swaps `facadeWrapper`'s inner to the current `productionFacade` -- the
1846: * ONLY place the real production facade is ever exposed to `apps/mobile/src/ui/**`
1847: * (F3 fix). Until this runs, every command still reaches the inert
1848: * `PendingFacade`/whatever `facadeWrapper` was last pointed at, no matter
1849: * how far `buildProductionController()`/the rest of bootstrap has otherwise
1850: * progressed.
1851: */
1852:function activateProductionFacade(): void {
1853:  if (productionFacade !== null) facadeWrapper.setInner(productionFacade);
1854:}
1855:
1856:/** Builds AND immediately activates a fresh production controller in one step -- used by the preflight gate's terminal-state rebuild (`setPreflightGate` below), where bootstrap is already `'ready'` and there is no staging window to preserve. */
1857:function installProductionController(): void {
1858:  buildProductionController();
1859:  activateProductionFacade();
1860:}
3038:    activeController = null;
3039:    // Ticket P5d: a retry re-reads the learned circuits from the database it
3040:    // is about to reopen -- never from a half-built previous attempt.
3041:    learnedCircuitStore = null;
3042:
3043:    if (IS_WEB_RUNTIME) {
3044:      // Web preview is a development surface only: expo-sqlite's wasm backend
3045:      // (wa-sqlite/OPFS) throws 'disk I/O error' in embedded browsers. Fall
3046:      // back to the in-memory repository -- no persistence across reloads,
3047:      // which is acceptable for the dev preview and keeps every session flow
3048:      // (timing, PB, history) fully functional. Native builds are unaffected.
3049:      console.warn('[composition] web preview: using in-memory storage (expo-sqlite web backend unavailable)');
3050:      repository = new InMemorySessionRepository();
3051:    } else {
3052:      const opened = await openAppDatabase(DB_NAME);
3053:      db = opened.db;
3054:      // P10B H5-B: the gate itself is no longer wired anywhere by hand --
3055:      // `opened.db` acquires it around every call made through it, which is
3056:      // what stops a standalone write from executing inside (and being
3057:      // rolled back by) an unrelated transaction.
3058:      repository = opened.repository;
3059:    }
3060:
3061:    // P4l-FIX3 J5 (binding): primes the vehicle-profile bindings cache as
3062:    // soon as `db` is available, so a binding confirmed by an EARLIER app
3063:    // run is already live for the very first `telemetryProvider.start()` of
3064:    // THIS run, not just ones after the next Signal Finder confirm. Never
3065:    // awaited -- `refreshVehicleProfileBindingsCache()` never throws, and
3066:    // bootstrap must never wait on it.
3067:    void refreshVehicleProfileBindingsCache();
3068:
3069:    gnssProvider = new GnssLocationProvider();
3070:
3071:    // Settings are hydrated BEFORE the production controller is built
3072:    // (reordered ahead of its historical position further down) so
3073:    // `createProductionController()`'s read of `settingsStore.getSettings()
3074:    // .coachingEnabled` (Phase 3 coaching addendum) reflects the user's
3075:    // actually-persisted preference on the very first controller this
3076:    // process builds -- not the in-memory placeholder's default. `facade`
3077:    // itself still stays on the inert `PendingFacade` regardless (that gate
3078:    // is `activateProductionFacade()` below), so this reorder changes
3079:    // nothing about when commands become live.
3080:    if (db !== null) {
3081:      const settings = await SqlSettingsStore.create(db);
3082:      settingsWrapper.setInner(settings);
3083:      // Ticket P4p G1 (binding): the ONE-TIME initial-profile migration, run
3084:      // here because it needs BOTH the hydrated settings (did the row ever
3085:      // carry a choice?) and the persisted bindings. Awaited -- the cache
3086:      // refresh right after it must read the profile this decided, and the
3087:      // whole thing is bounded by two small SQLite reads. Never throws.
3088:      await applyInitialActiveVehicleProfile(settings);
3089:      // The prime above ran before settings existed, so it read the DEFAULT
3090:      // profile; re-read now that the ACTIVE one is known.
3091:      await refreshVehicleProfileBindingsCache();
3092:      // Ticket P7R E2: which stored sessions were run on matching the
3093:      // calibration gate rejected. Hydrated here so `SessionHistoryScreen`
3094:      // (a synchronous render, like every other list it draws) can label
3095:      // them without an effect of its own. Never throws.
3096:      unvalidatedMatchingSessionIds = new Set(await readUnvalidatedMatchingSessionIds(db));
3097:    }
3098:
3099:    // Ticket P5d T6 (binding): learned circuits are registered in the catalog
3100:    // BEFORE the production controller and the history store are built, so a
3101:    // persisted `selectedCircuitId` naming a learned circuit resolves to its
3102:    // real geometry instead of falling back to the default. A failure here is
3103:    // warned about and survived: one unreadable learned circuit must never
3104:    // stop the app from starting.
3105:    if (db !== null) {
3106:      try {
3107:        await migrateLearnedCircuitSchema(db);
3108:        const learned = new SqlLearnedCircuitStore(db);
3109:        await learned.refresh();
3110:        learnedCircuitStore = learned;
3111:      } catch (error) {
3112:        learnedCircuitStore = null;
3113:        console.warn('[composition] learned circuits unavailable this launch', error);
3114:      }
3115:    }
3116:    publishLearnedCircuits();
3117:    // P5d-FIX3 F10: BEFORE the controller and history store are built, so a
3118:    // repaired selection is the one they are built for.
3119:    if (db !== null) await repairInterruptedAdoption(db);
3120:
3121:    // F3 fix: build the production controller/facade now (so `controller`
3122:    // exists for the preflight gate registered just below), but do NOT
3123:    // activate it yet -- `facade` stays on the inert placeholder until
3124:    // `activateProductionFacade()` runs, immediately before `'ready'`, once
3125:    // history/settings/recovery have ALL actually finished initializing.
2350:/** The learned circuits on this device -- `null` until bootstrap opens the database (and on web, where there is none). */
2351:let learnedCircuitStore: SqlLearnedCircuitStore | null = null;
2352:/** Learned circuits held in memory only (the web dev preview has no database to persist them to). */
2353:let memoryLearnedCircuits: LearnedCatalogEntry[] = [];
2354:/**
2355: * P5d-FIX1 H1: the buffering provider that spans the learn phase and the
2356: * session that grows out of it. Non-null for exactly that window; every
2357: * controller built while it is set is built ON it (see
2358: * `createProductionController`), which is what makes the handover seamless.
2359: */
2360:let testLoopProvider: TestLoopLocationProvider | null = null;
2361:/**
2362: * P5d-FIX2 N5: TRUE only for the handover window (closure -> replay). The two
2363: * provider-cleanup exceptions are scoped to this flag rather than to the
2364: * provider reference, so a rebuild AFTER the handover cleans up normally.
2365: */
2366:let testLoopHandoverActive = false;
2367:/**
2368: * P5d-FIX2 N2: which adoption steps have already happened, so a retry resumes
2369: * instead of repeating them. Cleared when the adoption completes.
2370: */
2371:interface AdoptionProgress {
2372:  circuitId: string;
2373:  storedProfile?: CircuitProfile;
2374:  selected?: boolean;
2375:  sessionId?: string;
2376:  recordingStarted?: boolean;
2377:  outLapSaved?: boolean;
2378:}
2379:let adoptionProgress: AdoptionProgress | null = null;
2380:/** P5d-FIX3 F11: bumped by every new learn phase, so a teardown queued for an older one is a no-op. */
2381:let teardownGeneration = 0;
2382:/** The teardown currently running -- a second caller awaits this one instead of starting another. */
2383:let teardownInFlight: Promise<void> | null = null;
2384:/** OBD/IMU samples recorded during the learn phase, before a session id exists to tag them with. */
2385:let testLoopTelemetryBuffer: TelemetrySample[] = [];
2386:let unsubscribeTestLoopTelemetry: (() => void) | null = null;
2387:let unsubscribeTestLoopGForce: (() => void) | null = null;
2388:/** Whether the learn phase holds its own G-force reference (released when the session takes its own). */
2389:let testLoopHoldsGForce = false;
2390:/** Bound on the learn-phase telemetry backlog -- an hour of 10 Hz channels. */
2391:const MAX_TEST_LOOP_TELEMETRY_SAMPLES = 360_000;
2392:let testLoopTelemetryDropped = 0;
2393:
2394:/** Republishes the catalog's learned registry from the store (plus any memory-only entries). */
2395:function publishLearnedCircuits(): void {
2396:  const stored: LearnedCatalogEntry[] =
2397:    learnedCircuitStore === null
2398:      ? []
2399:      : learnedCircuitStore.entries().map((entry) => ({
2400:          circuit: { profile: entry.profile, runtime: entry.runtime, corners: entry.corners },
2401:          listed: entry.record.saved,
2402:        }));
2403:  setLearnedCircuits([...stored, ...memoryLearnedCircuits]);
2404:}
2405:
2406:/**
2407: * The learn phase's state machine. Created once at module load: it holds no
2408: * database and no provider, so it is safe to exist before bootstrap.
2409: *
2410: * `makeCircuitId` is a UUID (P5d-FIX1 item 10) -- learned circuits are keyed
2411: * by it in the database, and a timestamp-plus-random id invites exactly the
2412: * collision that an `INSERT OR REPLACE` would silently resolve by destroying
2413: * somebody's saved circuit.
2414: */
2415:const testLoopController = new TestLoopController({
2416:  nowUtc: () => new Date().toISOString(),
2417:  makeCircuitId: () => `learned-${newLearnedCircuitId()}`,
2418:  makeDisplayName: (createdAtUtc) =>
2419:    defaultLearnedCircuitName(settingsStore.getSettings().language, createdAtUtc),
2420:  onLearned: (circuit) => adoptLearnedCircuit(circuit),
2421:});
2422:
2423:/** RFC-4122 v4 where the runtime offers one, and a random-hex fallback where it does not. */
2424:function newLearnedCircuitId(): string {
2425:  const cryptoApi = (globalThis as { crypto?: { randomUUID?: () => string } }).crypto;
2426:  if (typeof cryptoApi?.randomUUID === 'function') return cryptoApi.randomUUID();
2427:  const hex = (length: number): string =>
2428:    Array.from({ length }, () => Math.floor(Math.random() * 16).toString(16)).join('');
2429:  return `${hex(8)}-${hex(4)}-4${hex(3)}-a${hex(3)}-${hex(12)}`;
2430:}
2431:
2432:/**
2433: * P5d-FIX2 N6 (Codex P5d-REV2): a learn phase that gives up on its own -- the
2434: * sample cap, most of all -- must not leave the GNSS watcher and the channel
2435: * buffers running behind a screen that now says it failed. The teardown is the
2436: * same one `stopTestLoop()` performs, and it is idempotent, so the two paths
2437: * cannot fight.
2438: */
2439:testLoopController.subscribe((snapshot) => {
2440:  if (snapshot.phase !== 'failed') return;
2441:  if (testLoopProvider === null && !testLoopHoldsGForce) return;
2442:  void tearDownLearnPhase().catch((error) => {
2443:    console.warn('[composition] tearing down a failed learn phase failed', error);
2444:  });
2445:});
2446:
2447:/**
2448: * The ONE teardown of a learn phase (P5d-FIX3 F11, Codex P5d-REV3): detach the
2640:  testLoopTelemetryBuffer = [];
2641:  testLoopTelemetryDropped = 0;
2642:  if (!settingsStore.getSettings().telemetryEnabled) return;
2643:
2644:  const buffer = (sample: TelemetrySample): void => {
2645:    if (testLoopTelemetryBuffer.length >= MAX_TEST_LOOP_TELEMETRY_SAMPLES) {
2646:      testLoopTelemetryDropped += 1;
2647:      return;
2648:    }
2649:    testLoopTelemetryBuffer.push(sample);
2650:  };
2651:  unsubscribeTestLoopTelemetry = telemetryProvider.onSample(buffer);
2652:  unsubscribeTestLoopGForce = gForceProvider.onSample(buffer);
2653:  try {
2654:    telemetryProvider.start();
2655:  } catch (error) {
2656:    console.warn('[composition] telemetryProvider.start() threw synchronously (test loop)', error);
2657:  }
2658:  if (!testLoopHoldsGForce) {
2659:    acquireGForce();
2660:    testLoopHoldsGForce = true;
2661:  }
2662:}
2663:
2664:/** Detaches the learn-phase buffers. The providers themselves keep running -- the session owns them now. */
2665:function detachTestLoopRecording(): void {
2666:  unsubscribeTestLoopTelemetry?.();
2667:  unsubscribeTestLoopTelemetry = null;
2668:  unsubscribeTestLoopGForce?.();
2669:  unsubscribeTestLoopGForce = null;
2670:}
2671:
2672:/** Releases the learn phase's own G-force hold (the session takes its own in `startTelemetryRecording`). */
2673:async function releaseTestLoopGForce(): Promise<void> {
2674:  if (!testLoopHoldsGForce) return;
2675:  testLoopHoldsGForce = false;
2676:  try {
2677:    await releaseGForce();
2678:  } catch (error) {
2679:    console.warn('[composition] releasing the test-loop G-force hold failed', error);
2680:  }
2681:}
2682:
2683:/**
2684: * Begins a learn phase: starts the GNSS provider through the buffering
2685: * wrapper, starts the OBD/IMU recording, and feeds every fix to
2686: * `TestLoopController`. Refuses while a session is live -- learning a track
2687: * and timing laps are two different uses of the same provider.
2688: */
2689:export async function startTestLoop(): Promise<
2690:  { ok: true } | { ok: false; reason: 'not-ready' | 'session-active' }
2691:> {
2692:  await ready();
2693:  return lifecycleLock.run(async () => {
2694:    if (gnssProvider === null) return { ok: false as const, reason: 'not-ready' as const };
2695:    if (controller !== null && sessionIsActive(currentControllerState(controller))) {
2696:      return { ok: false as const, reason: 'session-active' as const };
2697:    }
2698:    await disposeTestLoopProvider();
2699:    // A fresh learn phase: any teardown still queued for the previous one must
2700:    // not reach in and stop the providers this one is about to use.
2701:    teardownGeneration += 1;
2702:    const provider = new TestLoopLocationProvider(gnssProvider, (sample) =>
2703:      testLoopController.feed(sample),
2704:    );
2705:    testLoopProvider = provider;
2706:    testLoopController.start();
2707:    await provider.start();
2708:    startTestLoopRecording();
2709:    return { ok: true as const };
2710:  });
2711:}
2712:
2713:/**
2714: * Ends a learn phase that never closed a loop. A loop that WAS learned is
2715: * already a running session by the time this can be called, and is ended
2716: * through the ordinary `facade.endSession()` instead.
2717: */
2718:export async function stopTestLoop(): Promise<TestLoopSnapshot> {
2719:  const snapshot = testLoopController.stop();
2720:  if (snapshot.phase !== 'learned' && snapshot.phase !== 'adopting') {
2721:    await tearDownLearnPhase();
2722:  }
2723:  return snapshot;
2724:}
2725:
2726:/** Leaves Test Loop mode entirely (screen dismissed without a learned track). */
2727:export async function resetTestLoop(): Promise<void> {
2728:  await stopTestLoop();
2729:  testLoopController.reset();
2730:}
2731:
2732:/** P5d-FIX1 H3: retries a handover that failed, on the SAME learned geometry. */
2733:export function retryTestLoopAdoption(): void {
2734:  testLoopController.retryAdopt();
2735:}
2736:
2737:/** Stops and clears the buffering provider, handing the GNSS watcher back to the plain singleton. */
2738:async function disposeTestLoopProvider(): Promise<void> {
2739:  const provider = testLoopProvider;
2740:  testLoopProvider = null;
2741:  testLoopHandoverActive = false;
2742:  if (provider === null) return;
2743:  try {
2744:    await provider.stop();
2745:  } catch (error) {
2746:    console.warn('[composition] stopping the test-loop provider failed', error);
2747:  }
2748:}
import type { SqlDatabase } from '@circuit/core';

/**
 * Ticket P5d-FIX3 F10 / P5d-FIX4 G1 -- the Test Loop adoption journal.
 *
 * Keeping a learned track touches durable state in several steps (a circuit
 * row, the selected circuit, a started session). A process killed in the
 * middle of that leaves a half-adopted state, and the next launch has to be
 * able to tell what landed and either finish the job or undo it.
 *
 * The journal is one additive `settings` row -- the same table the
 * active-session pointer lives in -- written BEFORE the first side effect and
 * advanced per completed step. Two rules make it safe when more than one
 * launch is alive at once (P5d-FIX4 G1):
 *
 *   * REPAIR IS A CLAIM. `claimAdoptionJournal` is a compare-and-swap on the
 *     journal's own UUID: exactly one launch wins it, and a launch that lost
 *     does nothing at all.
 *   * NOBODY CLEARS SOMEONE ELSE'S JOURNAL. `clearClaimedJournal` is a
 *     compare-and-delete against the exact row the caller claimed, so a slow
 *     launch can never wipe a journal a newer one has since written.
 *
 * The journal is cleared only after a repair fully succeeded; a failed repair
 * leaves it in place with its attempt counted, and after
 * {@link MAX_ADOPTION_REPAIR_ATTEMPTS} the app stops retrying and says so.
 */

export const ADOPTION_JOURNAL_KEY = 'testLoopAdoption';
/** After this many failed repairs the journal is abandoned, with a notice -- never retried forever. */
export const MAX_ADOPTION_REPAIR_ATTEMPTS = 3;

export type AdoptionStage =
  | 'staged'
  | 'stored'
  | 'selected'
  | 'session-started'
  | 'recording'
  | 'out-lap';

export interface AdoptionJournal {
  /** Identity of THIS adoption -- what a claim and a clear are checked against. */
  id: string;
  circuitId: string;
  stage: AdoptionStage;
  sessionId?: string;
  /** How many launches have tried to repair it. */
  attempts: number;
  /** The launch that currently owns the repair. */
  claimedBy?: string;
}

export interface ClaimedAdoptionJournal {
  journal: AdoptionJournal;
  /** The exact row value this claim wrote -- the compare half of the compare-and-delete. */
  serialized: string;
  /** True when the attempt budget is spent: the caller abandons the journal instead of repairing. */
  exhausted: boolean;
}

/** A fresh journal/launch identity. Uses the platform UUID where there is one. */
export function newJournalId(): string {
  const cryptoApi = (globalThis as { crypto?: { randomUUID?: () => string } }).crypto;
  if (typeof cryptoApi?.randomUUID === 'function') return cryptoApi.randomUUID();
  const hex = (length: number): string =>
    Array.from({ length }, () => Math.floor(Math.random() * 16).toString(16)).join('');
  return `${hex(8)}-${hex(4)}-4${hex(3)}-a${hex(3)}-${hex(12)}`;
}

function parse(raw: string): AdoptionJournal | null {
  try {
    const parsed: unknown = JSON.parse(raw);
    if (typeof parsed !== 'object' || parsed === null) return null;
    const journal = parsed as Partial<AdoptionJournal>;
    if (typeof journal.circuitId !== 'string' || typeof journal.stage !== 'string') return null;
    return {
      id: typeof journal.id === 'string' ? journal.id : newJournalId(),
      circuitId: journal.circuitId,
      stage: journal.stage,
      attempts: typeof journal.attempts === 'number' ? journal.attempts : 0,
      ...(typeof journal.sessionId === 'string' ? { sessionId: journal.sessionId } : {}),
      ...(typeof journal.claimedBy === 'string' ? { claimedBy: journal.claimedBy } : {}),
    };
  } catch {
    return null;
  }
}

/** Writes (or overwrites) the journal for the adoption in progress, returning the row it wrote. */
export async function writeAdoptionJournal(
  db: SqlDatabase,
  journal: AdoptionJournal,
): Promise<ClaimedAdoptionJournal> {
  const serialized = JSON.stringify(journal);
  await db.runAsync('INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)', [
    ADOPTION_JOURNAL_KEY,
    serialized,
  ]);
  // P5d-FIX5 H1: the caller keeps the EXACT row it wrote, so it can later
  // clear that row and only that row (compare-and-delete) instead of deleting
  // whatever journal happens to occupy the key by then.
import {
  decodeLearnedCircuit,
  encodeLearnedCircuit,
  polylineLength,
  type CircuitProfile,
  type Corner,
  type SqlBindValue,
  type SqlDatabase,
} from '@circuit/core';

import type { BundledCircuit } from './circuitCatalog';

/**
 * Ticket P5d T4/T6 -- the learned circuits on this device.
 *
 * Same shape as `SqlSessionHistoryStore`: an async `refresh()` fills an
 * in-memory cache, and every read a screen or the catalog performs is
 * synchronous. Writes are single statements, so they need no
 * `SqlWriteGate` (see `sqlWriteGate.ts`: only multi-statement units do).
 *
 * A row that cannot be decoded is SKIPPED and counted, never thrown: one
 * corrupt learned circuit must not take the circuit list -- or the app's
 * bootstrap -- down with it.
 */

export interface LearnedCircuitRecord {
  circuitId: string;
  displayName: string;
  lengthM: number;
  cornerCount: number;
  createdAtUtc: string;
  /** `true` once the driver saved it as a reusable, listed circuit (T6). */
  saved: boolean;
}

export interface LearnedCircuitEntry extends BundledCircuit {
  record: LearnedCircuitRecord;
  /** True when the stored corner set was unusable and had to be re-derived from the geometry. */
  cornersRecovered: boolean;
}

export type RemoveLearnedCircuitResult =
  | { ok: true }
  | { ok: false; reason: 'not-found' }
  | { ok: false; reason: 'has-sessions'; sessionCount: number }
  /** P5d-FIX1 item 9: the circuit a session is being driven on right now. */
  | { ok: false; reason: 'active-session' };

interface LearnedCircuitRow {
  circuit_id: string;
  display_name: string;
  payload: string;
  length_m: number;
  corner_count: number;
  created_at_utc: string;
  saved: number;
}

export class SqlLearnedCircuitStore {
  private cache = new Map<string, LearnedCircuitEntry>();
  private invalidRows = 0;

  constructor(private readonly db: SqlDatabase) {}

  /** Reloads every learned circuit from the database, validating each one. */
  async refresh(): Promise<void> {
    const rows = await this.db.getAllAsync<LearnedCircuitRow>(
      'SELECT circuit_id, display_name, payload, length_m, corner_count, created_at_utc, saved FROM learned_circuits ORDER BY created_at_utc DESC',
    );
    const cache = new Map<string, LearnedCircuitEntry>();
    let invalid = 0;
    for (const row of rows) {
      const decoded = decodeLearnedCircuit(row.payload);
      if (!decoded.ok) {
        invalid += 1;
        console.warn(
          `[learnedCircuitStore] skipping unreadable learned circuit "${row.circuit_id}": ${decoded.errors.join(', ')}`,
        );
        continue;
      }
      cache.set(row.circuit_id, {
        profile: decoded.profile,
        runtime: decoded.runtime,
        corners: decoded.corners,
        cornersRecovered: decoded.cornersRecovered,
        record: {
          circuitId: row.circuit_id,
          displayName: decoded.profile.displayName,
          lengthM: row.length_m,
          cornerCount: decoded.corners.length,
          createdAtUtc: row.created_at_utc,
          saved: row.saved !== 0,
        },
      });
    }
    this.cache = cache;
    this.invalidRows = invalid;
  }

  /** Every learned circuit, saved or not -- what the catalog resolves session geometry from. */
  entries(): LearnedCircuitEntry[] {
    return [...this.cache.values()];
  }

  /** Only the circuits the driver actually saved -- what the selection list shows. */
  saved(): LearnedCircuitRecord[] {
    return this.entries()
      .filter((entry) => entry.record.saved)
      .map((entry) => entry.record);
  }

  get(circuitId: string): LearnedCircuitEntry | null {
    return this.cache.get(circuitId) ?? null;
  }

  /** How many stored rows could not be read back (diagnostics; never silently zero). */
  invalidRowCount(): number {
    return this.invalidRows;
  }

  /**
   * P5d-FIX1 item 10 (Codex P5d-REV1 LOW): writes a NEW learned circuit with a
   * plain INSERT, so an id that is somehow already taken is REPORTED rather
   * than silently overwriting the circuit that holds it -- and, with it, the
   * geometry of every session recorded on that circuit. The caller regenerates
38:  return primary === 'ro' ? 'ro' : 'en';
58:  thresholds: readonly number[];
62:  units: SpeedUnits;
64:  deltaDeadbandMs: number;
65:  coverageBins: CoverageBinsSetting;
76:  coachingEnabled: boolean;
85:  voiceCoachEnabled: boolean;
97:  suggestionsEnabled: boolean;
107:  telemetryEnabled: boolean;
115:  telemetrySimulate: boolean;
117:  adapterHost: string;
119:  adapterPort: number;
131:  transOilPidHex: string;
137:   * Ring (`DEFAULT_SETTINGS`, below) -- existing installs with no persisted
143:  selectedCircuitId: string;
149:  adapterType: AdapterType;
159:  enetHost: string;
161:  enetPort: number;
163:  enetTesterAddress: number;
165:  enetTargetAddress: number;
182:  enetChannelSpecsJson: string;
192:  enetHostProvenance: string;
202:  enetAutoDiscover: boolean;
214:  developerModeEnabled: boolean;
224:  language: AppLanguage;
245:  activeVehicleProfileId: string;
258:  activeVehicleProfileSource: 'user' | 'vin' | 'default';
268:  lastSeenVin: string | null;
283:  imuFusionEnabled: boolean;
302:  imuGyroCaptureEnabled: boolean;
316:  analysisSmoothingEnabled: boolean;
326:  profileId: string;
327:  channel: string;
328:  status: string;
356:  bindings: readonly VehicleProfileBindingSummary[],
369:  const ranked = [...overlapByProfile.entries()].sort((a, b) => (b[1] - a[1]) || (a[0] < b[0] ? -1 : a[0] > b[0] ? 1 : 0));
393:  count: number;
394:  firstTapAtMs: number;
401:  state: DevTapState | null,
402:  nowMs: number,
403:  threshold: number = DEV_TAP_THRESHOLD,
404:  windowMs: number = DEV_TAP_WINDOW_MS,
409:    return { state: { count: toggled ? 0 : 1, firstTapAtMs: nowMs }, toggled };
414:  return { state: { count: toggled ? 0 : count, firstTapAtMs: toggled ? nowMs : state2.firstTapAtMs }, toggled };
417:export const DEFAULT_SETTINGS: AppSettings = {
418:  units: 'kmh',
419:  deltaDeadbandMs: 100,
420:  coverageBins: { thresholds: [0.25, 0.5, 0.75, 1] },
421:  coachingEnabled: true,
422:  voiceCoachEnabled: false,
423:  suggestionsEnabled: false,
424:  telemetryEnabled: false,
425:  telemetrySimulate: false,
426:  adapterHost: '192.168.0.10',
427:  adapterPort: 35_000,
428:  transOilPidHex: '',
429:  selectedCircuitId: TMR_CIRCUIT_PROFILE.circuitId,
430:  adapterType: 'elm327',
431:  enetHost: '',
432:  enetPort: 6_801,
433:  enetTesterAddress: 0xf4,
434:  enetTargetAddress: 0x12,
435:  enetChannelSpecsJson: '',
436:  enetHostProvenance: '',
437:  enetAutoDiscover: true,
438:  developerModeEnabled: false,
439:  language: 'en',
440:  activeVehicleProfileId: GENERIC_VEHICLE_PROFILE_ID,
441:  activeVehicleProfileSource: 'default',
442:  lastSeenVin: null,
445:  imuFusionEnabled: false,
449:  imuGyroCaptureEnabled: true,
450:  analysisSmoothingEnabled: false,
460:  getSettings(): AppSettings;
461:  subscribe(cb: (s: AppSettings) => void): () => void;
462:  update(patch: Partial<AppSettings>): void;
467:  private settings: AppSettings = { ...DEFAULT_SETTINGS };
468:  private readonly listeners = new Set<(s: AppSettings) => void>();
470:  getSettings(): AppSettings {
474:  subscribe(cb: (s: AppSettings) => void): () => void {
482:  update(patch: Partial<AppSettings>): void {

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command '$s=Get-Content packages/core/src/telemetry/signalFinder/runner.ts; foreach($r in @(@(60,130),@(300,365),@(440,610))) {for($i=$r[0];$i -le $r[1];$i++){'"'{0}:{1}' -f "'$i,$s[$i-1]}}; git diff dd84864..8c495b6 -- packages/core/test/telemetry/signalFinder/runner.test.ts; rg -n '"'waitMs|paused|endAtMs' packages/core/src/telemetry/signalFinder/runner.ts; rg -n 'deleteAllStoredUserData|result.ok' apps/mobile/src/screens/SettingsScreen.tsx; rg -n 'CREATE TABLE|INSERT INTO|INSERT OR REPLACE|INSERT OR IGNORE' packages/core/src --glob '*.ts'" in D:\CODE\APLICTIE_Circuit
 succeeded in 69ms:

561:        if (!answeredKeys.has(key)) {
562:          answeredKeys.add(key);
563:          answered.push({ ecu: entry.ecu, did: entry.did });
564:        }
565:        answeredCount += 1;
566:        answeredElapsedMs += elapsedMs;
567:        if (outcome.kind === 'positive') {
568:          samples.push({ ecu: entry.ecu, did: entry.did, tMs: input.clock.now() - startedAtMs, raw: outcome.raw });
569:        }
570:      }
571:      // Yield to the event loop between entries: a channel that answers from
572:      // its own queue resolves synchronously, and a pure microtask loop would
573:      // starve the screen's metronome ticker for the whole round.
574:      await waitMs(0);
575:    }
576:
577:    if (polledThisPass === 0) {
578:      // Y3: "every entry is cooling down" is TEMPORARY -- each of them gets its
579:      // full allowance back at the next evidence window, so the round sleeps to
580:      // that boundary instead of ending. Only a pass with nothing cooling
581:      // (every entry unrouted) is genuinely finished.
582:      if (coolingThisPass === 0) break;
583:      const boundaryMs = startedAtMs + (windowAt(input.clock.now()) + 1) * windowMs;
584:      // A timer can fire before `input.clock` reaches its target (the two are
585:      // separate clocks), so keep sleeping until the clock itself crosses the
586:      // boundary. Ending the round on an early wake-up emptied every later
587:      // window, the Y3 failure all over again.
588:      const wakeAtMs = Math.min(boundaryMs, endAtMs);
589:      while (input.clock.now() < wakeAtMs && !input.control.stopped) {
590:        await waitMs(Math.max(1, wakeAtMs - input.clock.now()));
591:      }
592:      if (input.clock.now() < boundaryMs) break; // the round's own duration ran out first.
593:      continue;
594:    }
595:
596:    // Z3: pace the PASS, not the entry. Whatever the pass did not spend on
597:    // exchanges is slept off here; a pass that already took longer than its
598:    // interval starts the next one immediately, carrying no debt forward.
599:    if (passIntervalMs > 0) {
600:      const remainingMs = passIntervalMs - (input.clock.now() - passStartedAtMs);
601:      if (remainingMs > 0) await waitMs(remainingMs);
602:    }
603:  }
604:
605:  return {
606:    startedAtMs,
607:    elapsedMs: Math.max(0, input.clock.now() - startedAtMs),
608:    samples,
609:    attempted,
610:    answered,
diff --git a/packages/core/test/telemetry/signalFinder/runner.test.ts b/packages/core/test/telemetry/signalFinder/runner.test.ts
index 7c95c47..fb38dbc 100644
--- a/packages/core/test/telemetry/signalFinder/runner.test.ts
+++ b/packages/core/test/telemetry/signalFinder/runner.test.ts
@@ -394,6 +394,32 @@ describe('runFinderRound -- P4m-FIX2 probe completeness, honest rate, bounded co
     expect(result.respondingEcus).toEqual([0x12]);
   });
 
+  it('Y3: a cooldown sleep that wakes before the clock reaches the window boundary keeps waiting instead of ending the round', async () => {
+    // Timers and `Date.now()` are separate clocks: a timer can fire a
+    // millisecond before `Date.now()` reaches its target. A clock that runs
+    // 2 % slow makes every wake-up early, so the round must keep waiting
+    // until the clock itself crosses the boundary.
+    const origin = Date.now();
+    const slowClock = { now: (): number => origin + Math.floor((Date.now() - origin) * 0.98) };
+    let silent = true;
+    setTimeout(() => {
+      silent = false;
+    }, 150);
+    const { channels } = harness(() => (silent ? 'silent' : Uint8Array.from([0x07])), [0x12]);
+    const result = await runFinderRound({
+      entries: [{ ecu: 0x12, did: 0x4002 }],
+      channels,
+      clock: slowClock,
+      control: { paused: false, stopped: false },
+      durationMs: 600,
+      requestTimeoutMs: 10,
+      windowMs: 100,
+      missesBeforeBackoff: 3,
+    });
+    expect(result.elapsedMs).toBeGreaterThanOrEqual(600);
+    expect(result.samples.length).toBeGreaterThan(0);
+  });
+
   it('Y4: a send that throws leaves the key UNATTEMPTED and uncounted', async () => {
     const channels = new Map<number, SweepTransport>([
       [
285:function waitMs(ms: number): Promise<void> {
444:  const endAtMs = startedAtMs + Math.max(0, input.durationMs);
484:  const done = (): boolean => input.control.stopped || (!completePass && input.clock.now() >= endAtMs);
574:      await waitMs(0);
588:      const wakeAtMs = Math.min(boundaryMs, endAtMs);
590:        await waitMs(Math.max(1, wakeAtMs - input.clock.now()));
601:      if (remainingMs > 0) await waitMs(remainingMs);
rg: apps/mobile/src/screens/SettingsScreen.tsx: IO error for operation on apps/mobile/src/screens/SettingsScreen.tsx: The system cannot find the path specified. (os error 3)
packages/core/src\persistence-sql\schema.ts:65:CREATE TABLE IF NOT EXISTS schema_migrations (
packages/core/src\persistence-sql\schema.ts:69:CREATE TABLE IF NOT EXISTS sessions (
packages/core/src\persistence-sql\schema.ts:84:CREATE TABLE IF NOT EXISTS laps (
packages/core/src\persistence-sql\schema.ts:91:CREATE TABLE IF NOT EXISTS checkpoints (
packages/core/src\persistence-sql\schema.ts:97:CREATE TABLE IF NOT EXISTS telemetry (
packages/core/src\persistence-sql\schema.ts:104:CREATE TABLE IF NOT EXISTS reference_laps (
packages/core/src\persistence-sql\schema.ts:121:CREATE TABLE IF NOT EXISTS settings (
packages/core/src\persistence-sql\schema.ts:129: * `CREATE TABLE IF NOT EXISTS` above cannot add a column to a table it did
packages/core/src\persistence-sql\schema.ts:165: * `CREATE TABLE IF NOT EXISTS` is all the idempotence they need -- applied on
packages/core/src\persistence-sql\schema.ts:177:CREATE TABLE IF NOT EXISTS lap_verdicts (
packages/core/src\persistence-sql\schema.ts:184:CREATE TABLE IF NOT EXISTS calibration_attempts (
packages/core/src\persistence-sql\sqlSessionRepository.ts:144:   * has already been migrated re-applies only `CREATE TABLE IF NOT EXISTS`
packages/core/src\persistence-sql\sqlSessionRepository.ts:165:    // v2: the settings key-value table. `CREATE TABLE IF NOT EXISTS` makes
packages/core/src\persistence-sql\sqlSessionRepository.ts:170:    // tables. Both are `CREATE TABLE IF NOT EXISTS`, so like `SQL_DDL_V2`
packages/core/src\persistence-sql\sqlSessionRepository.ts:209:      await this.db.runAsync('INSERT INTO schema_migrations (version) VALUES (?)', [SQL_SCHEMA_VERSION]);
packages/core/src\persistence-sql\sqlSessionRepository.ts:235:    await this.db.runAsync('INSERT OR REPLACE INTO checkpoints (sessionId, payload, lapCount) VALUES (?, ?, ?)', [
packages/core/src\persistence-sql\sqlSessionRepository.ts:302:          'INSERT OR REPLACE INTO telemetry (sessionId, lapNumber, payload) VALUES (?, ?, ?)',
packages/core/src\persistence-sql\sqlSessionRepository.ts:307:        `INSERT INTO checkpoints (sessionId, payload, lapCount) VALUES (?, ?, ?)
packages/core/src\persistence-sql\sqlSessionRepository.ts:332:        `INSERT OR REPLACE INTO sessions
packages/core/src\persistence-sql\sqlSessionRepository.ts:359:        await tx.runAsync('INSERT INTO laps (sessionId, lapNumber, payload) VALUES (?, ?, ?)', [
packages/core/src\persistence-sql\sqlSessionRepository.ts:434:    await this.db.runAsync('INSERT OR REPLACE INTO telemetry (sessionId, lapNumber, payload) VALUES (?, ?, ?)', [
packages/core/src\persistence-sql\sqlSessionRepository.ts:457:          'INSERT OR REPLACE INTO telemetry (sessionId, lapNumber, payload) VALUES (?, ?, ?)',
packages/core/src\persistence-sql\sqlSessionRepository.ts:480:   * Ticket P12 item A. `INSERT OR REPLACE` keyed `(sessionId, lapNumber)` --
packages/core/src\persistence-sql\sqlSessionRepository.ts:488:      'INSERT OR REPLACE INTO lap_verdicts (sessionId, lapNumber, payload) VALUES (?, ?, ?)',
packages/core/src\persistence-sql\sqlSessionRepository.ts:521:      'INSERT OR REPLACE INTO calibration_attempts (attemptId, sessionId, startedAtUtc, payload) VALUES (?, ?, ?, ?)',
packages/core/src\persistence-sql\sqlSessionRepository.ts:574:    // ticket's explicit instruction (rather than a single INSERT OR REPLACE)
packages/core/src\persistence-sql\sqlSessionRepository.ts:584:        'INSERT INTO reference_laps (userId, circuitId, layoutId, layoutVersion, payload) VALUES (?, ?, ?, ?, ?)',

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command '$s=Get-Content apps/mobile/src/session/composition.ts; foreach($r in @(@(1893,1999),@(2114,2155),@(2415,2455),@(2650,2744))) {for($i=$r[0];$i -le $r[1];$i++){'"'{0}:{1}' -f "'$i,$s[$i-1]}}; Get-Content apps/mobile/src/session/adoptionJournal.ts -TotalCount 100; rg -n '"'INSERT|runAsync|execute|\\.run\\(' apps/mobile/src/telemetry packages/core/src/telemetry; rg -n 'deleteAllStoredUserData|result.ok' apps/mobile/src/ui/SettingsScreen.tsx; Get-Content apps/mobile/src/session/settingsStore.ts | Select-Object -First 220" in D:\CODE\APLICTIE_Circuit
 succeeded in 97ms:
1929:  // if any -- is serialized by the provider ahead of this stop, and every
1930:  // command that starts a session is itself a locked section queued BEHIND
1931:  // this one, so it restarts the provider through `ensureProviderRunning()`
1932:  // as usual. Idempotent: stopping an already-stopped provider is a no-op.
1933:  //
1934:  // Failure is logged, never thrown: a provider that cannot be stopped (a
1935:  // revoked permission, an OS hiccup) must not fail the selection/recovery/
1936:  // delete-all operation that drove this rebuild.
1937:  try {
1938:    // P5d-FIX1 H1 (binding, Codex P5d-REV1 HIGH 1): the ONE exception to the
1939:    // ownership stop. While a Test Loop is being learned or handed over, the
1940:    // watcher is NOT session-less -- the learn phase owns it, its fixes are
1941:    // being recorded, and the session about to start on the just-learned
1942:    // circuit is what this very rebuild exists to install. Stopping it here
1943:    // would blind the recording for the length of the rebuild, which is
1944:    // exactly the gap this fix wave removed. Everything the amendment guards
1945:    // against (a lit indicator with nothing behind it) still holds: the
1946:    // Test Loop path stops the provider itself when a learn phase ends
1947:    // without a track (`stopTestLoop`), and the session's own end still runs
1948:    // the ordinary teardown.
1949:    if (!testLoopHandoverActive) await gnssProvider?.stop();
1950:  } catch (error) {
1951:    console.warn('[composition] stopping the GNSS provider after a controller rebuild failed', error);
1952:  }
1953:}
1954:
1955:/**
1956: * F3 fix: `coachingConfig()` is baked into a `SessionController` for its own
1957: * whole lifetime (`SessionControllerDeps.coaching`'s own doc comment) --
1958: * previously, flipping the coaching toggle before the first session (or
1959: * while idle between sessions) had NO effect until some UNRELATED
1960: * terminal-state rebuild happened to fire later; the UI showed a fresh
1961: * coaching slot immediately, but it stayed empty. Rebuild immediately
1962: * whenever `coachingEnabled` actually CHANGES while the controller is
1963: * genuinely not in an active session (idle/sessionComplete/error). A
1964: * mid-active-session change is deliberately left alone here -- tearing down
1965: * live timing/GNSS state under a driver mid-lap would be far worse than a
1966: * stale coaching setting for one more lap -- `SettingsScreen` shows a note
1967: * that it takes effect next session instead.
1968: *
1969: * N2 fix (lifecycle lock amendment, binding): the rebuild runs inside
1970: * `lifecycleLock`, and the "is the controller genuinely idle/terminal?"
1971: * decision is re-taken INSIDE that section -- so a coaching toggle flipped
1972: * while `resumeRecovery()` is awaiting its checkpoint read now queues behind
1973: * the resume and then correctly SKIPS (the controller is mid-session by
1974: * then), instead of disposing the controller recovery had already captured.
1975: * The cheap synchronous pre-check below is kept as-is so a toggle observed
1976: * before any controller exists (bootstrap's own settings hydration) stays the
1977: * exact no-op it always was. Registered ONCE at module load (not inside `runBootstrap()`,
1978: * which re-runs on `retryBootstrap()`) so a failed-then-retried bootstrap
1979: * never accumulates duplicate subscriptions -- this callback reads the
1980: * live module-level `controller`/`activeController` on every invocation
1981: * regardless of how many times bootstrap itself has (re)run, and is a
1982: * harmless no-op before `controller` exists yet.
1983: */
1984:let lastKnownCoachingEnabled = settingsStore.getSettings().coachingEnabled;
1985:const COACHING_REBUILD_STATES = new Set<SessionState>(['idle', 'sessionComplete', 'error']);
1986:settingsStore.subscribe((settings) => {
1987:  const changed = settings.coachingEnabled !== lastKnownCoachingEnabled;
1988:  lastKnownCoachingEnabled = settings.coachingEnabled;
1989:  if (!changed) return;
1990:  if (controller === null || activeController !== controller) return;
1991:  void lifecycleLock
1992:    .run(async () => {
1993:      // Re-checked with the lock HELD: whatever was true when the toggle
1994:      // fired may no longer be true by the time this section actually runs.
1995:      if (controller === null || activeController !== controller) return;
1996:      if (!COACHING_REBUILD_STATES.has(currentControllerState(controller))) return;
1997:      await unlockedRebuildProductionController();
1998:    })
1999:    .catch((error: unknown) => {
2114:async function unlockedApplySelection(entry: BundledCircuit): Promise<void> {
2115:  applySelectedCircuit(entry);
2116:  await rebuildHistoryForSelection(entry);
2117:  // N3 fix (lifecycle lock amendment, binding, ticket CN-FIX3): the
2118:  // production controller is rebuilt for the new circuit HERE, in the same
2119:  // critical section as the settings/history writes, so the invariant "an
2120:  // IDLE production controller is always built for the resolved selection"
2121:  // holds continuously. Previously the rebuild happened lazily, in the
2122:  // preflight gate only -- and `RealSessionFacade.startPreflight()` is a
2123:  // no-op controller-side (the state machine first moves at
2124:  // `beginCalibration()`), so a selection committed after the gate had
2125:  // already run left the NEXT session starting on the previous circuit's
2126:  // geometry while settings/history said otherwise. Restricted to `idle`
2127:  // exactly like the gate's own circuit-change trigger: a terminal
2128:  // (`sessionComplete`/`error`) controller is still the gate's to replace,
2129:  // and a mid-session one is unreachable here (the selection would have been
2130:  // refused before this point).
2131:  if (
2132:    controller !== null &&
2133:    activeController === controller &&
2134:    currentControllerState(controller) === 'idle' &&
2135:    productionControllerCircuitId !== resolveSelectedCircuit(settingsStore.getSettings()).profile.circuitId
2136:  ) {
2137:    await unlockedRebuildProductionController();
2138:  }
2139:}
2140:
2141:/** Shared refusal check for every locked selection change -- `null` when the change may proceed. */
2142:function refuseSelectionIfSessionActive(): SelectCircuitResult | null {
2143:  if (activeController !== null && !SELECTABLE_STATES.has(currentControllerState(activeController))) {
2144:    return { ok: false, reason: 'SESSION_ACTIVE' };
2145:  }
2146:  return null;
2147:}
2148:
2149:/**
2150: * Selects the app's ONE active circuit (contracts.md's Multi-circuit
2151: * selection addendum, ticket CN-W3, extended by the "recovery amendment",
2152: * ticket CN-FIX2): validates `circuitId` against the bundled catalog (an
2153: * unknown id is a warned no-op, never a crash/rejection), then --
2154: *
2155: *  - H1 fix: awaits `ready()` (bootstrap) FIRST, so a selection tapped
2415:const testLoopController = new TestLoopController({
2416:  nowUtc: () => new Date().toISOString(),
2417:  makeCircuitId: () => `learned-${newLearnedCircuitId()}`,
2418:  makeDisplayName: (createdAtUtc) =>
2419:    defaultLearnedCircuitName(settingsStore.getSettings().language, createdAtUtc),
2420:  onLearned: (circuit) => adoptLearnedCircuit(circuit),
2421:});
2422:
2423:/** RFC-4122 v4 where the runtime offers one, and a random-hex fallback where it does not. */
2424:function newLearnedCircuitId(): string {
2425:  const cryptoApi = (globalThis as { crypto?: { randomUUID?: () => string } }).crypto;
2426:  if (typeof cryptoApi?.randomUUID === 'function') return cryptoApi.randomUUID();
2427:  const hex = (length: number): string =>
2428:    Array.from({ length }, () => Math.floor(Math.random() * 16).toString(16)).join('');
2429:  return `${hex(8)}-${hex(4)}-4${hex(3)}-a${hex(3)}-${hex(12)}`;
2430:}
2431:
2432:/**
2433: * P5d-FIX2 N6 (Codex P5d-REV2): a learn phase that gives up on its own -- the
2434: * sample cap, most of all -- must not leave the GNSS watcher and the channel
2435: * buffers running behind a screen that now says it failed. The teardown is the
2436: * same one `stopTestLoop()` performs, and it is idempotent, so the two paths
2437: * cannot fight.
2438: */
2439:testLoopController.subscribe((snapshot) => {
2440:  if (snapshot.phase !== 'failed') return;
2441:  if (testLoopProvider === null && !testLoopHoldsGForce) return;
2442:  void tearDownLearnPhase().catch((error) => {
2443:    console.warn('[composition] tearing down a failed learn phase failed', error);
2444:  });
2445:});
2446:
2447:/**
2448: * The ONE teardown of a learn phase (P5d-FIX3 F11, Codex P5d-REV3): detach the
2449: * learn-phase recording, release its holds, stop the buffering provider.
2450: *
2451: * Memoized and generation-guarded, and it takes `lifecycleLock` itself. Two
2452: * paths reach it for the same failure -- `stopTestLoop()` and the auto-teardown
2453: * subscription that the failed phase fires -- and before this they could both
2454: * be shutting the GLOBAL providers down at once. Now the second caller awaits
2455: * the first, and a teardown queued behind a learn phase that has meanwhile
2650:  };
2651:  unsubscribeTestLoopTelemetry = telemetryProvider.onSample(buffer);
2652:  unsubscribeTestLoopGForce = gForceProvider.onSample(buffer);
2653:  try {
2654:    telemetryProvider.start();
2655:  } catch (error) {
2656:    console.warn('[composition] telemetryProvider.start() threw synchronously (test loop)', error);
2657:  }
2658:  if (!testLoopHoldsGForce) {
2659:    acquireGForce();
2660:    testLoopHoldsGForce = true;
2661:  }
2662:}
2663:
2664:/** Detaches the learn-phase buffers. The providers themselves keep running -- the session owns them now. */
2665:function detachTestLoopRecording(): void {
2666:  unsubscribeTestLoopTelemetry?.();
2667:  unsubscribeTestLoopTelemetry = null;
2668:  unsubscribeTestLoopGForce?.();
2669:  unsubscribeTestLoopGForce = null;
2670:}
2671:
2672:/** Releases the learn phase's own G-force hold (the session takes its own in `startTelemetryRecording`). */
2673:async function releaseTestLoopGForce(): Promise<void> {
2674:  if (!testLoopHoldsGForce) return;
2675:  testLoopHoldsGForce = false;
2676:  try {
2677:    await releaseGForce();
2678:  } catch (error) {
2679:    console.warn('[composition] releasing the test-loop G-force hold failed', error);
2680:  }
2681:}
2682:
2683:/**
2684: * Begins a learn phase: starts the GNSS provider through the buffering
2685: * wrapper, starts the OBD/IMU recording, and feeds every fix to
2686: * `TestLoopController`. Refuses while a session is live -- learning a track
2687: * and timing laps are two different uses of the same provider.
2688: */
2689:export async function startTestLoop(): Promise<
2690:  { ok: true } | { ok: false; reason: 'not-ready' | 'session-active' }
2691:> {
2692:  await ready();
2693:  return lifecycleLock.run(async () => {
2694:    if (gnssProvider === null) return { ok: false as const, reason: 'not-ready' as const };
2695:    if (controller !== null && sessionIsActive(currentControllerState(controller))) {
2696:      return { ok: false as const, reason: 'session-active' as const };
2697:    }
2698:    await disposeTestLoopProvider();
2699:    // A fresh learn phase: any teardown still queued for the previous one must
2700:    // not reach in and stop the providers this one is about to use.
2701:    teardownGeneration += 1;
2702:    const provider = new TestLoopLocationProvider(gnssProvider, (sample) =>
2703:      testLoopController.feed(sample),
2704:    );
2705:    testLoopProvider = provider;
2706:    testLoopController.start();
2707:    await provider.start();
2708:    startTestLoopRecording();
2709:    return { ok: true as const };
2710:  });
2711:}
2712:
2713:/**
2714: * Ends a learn phase that never closed a loop. A loop that WAS learned is
2715: * already a running session by the time this can be called, and is ended
2716: * through the ordinary `facade.endSession()` instead.
2717: */
2718:export async function stopTestLoop(): Promise<TestLoopSnapshot> {
2719:  const snapshot = testLoopController.stop();
2720:  if (snapshot.phase !== 'learned' && snapshot.phase !== 'adopting') {
2721:    await tearDownLearnPhase();
2722:  }
2723:  return snapshot;
2724:}
2725:
2726:/** Leaves Test Loop mode entirely (screen dismissed without a learned track). */
2727:export async function resetTestLoop(): Promise<void> {
2728:  await stopTestLoop();
2729:  testLoopController.reset();
2730:}
2731:
2732:/** P5d-FIX1 H3: retries a handover that failed, on the SAME learned geometry. */
2733:export function retryTestLoopAdoption(): void {
2734:  testLoopController.retryAdopt();
2735:}
2736:
2737:/** Stops and clears the buffering provider, handing the GNSS watcher back to the plain singleton. */
2738:async function disposeTestLoopProvider(): Promise<void> {
2739:  const provider = testLoopProvider;
2740:  testLoopProvider = null;
2741:  testLoopHandoverActive = false;
2742:  if (provider === null) return;
2743:  try {
2744:    await provider.stop();
import type { SqlDatabase } from '@circuit/core';

/**
 * Ticket P5d-FIX3 F10 / P5d-FIX4 G1 -- the Test Loop adoption journal.
 *
 * Keeping a learned track touches durable state in several steps (a circuit
 * row, the selected circuit, a started session). A process killed in the
 * middle of that leaves a half-adopted state, and the next launch has to be
 * able to tell what landed and either finish the job or undo it.
 *
 * The journal is one additive `settings` row -- the same table the
 * active-session pointer lives in -- written BEFORE the first side effect and
 * advanced per completed step. Two rules make it safe when more than one
 * launch is alive at once (P5d-FIX4 G1):
 *
 *   * REPAIR IS A CLAIM. `claimAdoptionJournal` is a compare-and-swap on the
 *     journal's own UUID: exactly one launch wins it, and a launch that lost
 *     does nothing at all.
 *   * NOBODY CLEARS SOMEONE ELSE'S JOURNAL. `clearClaimedJournal` is a
 *     compare-and-delete against the exact row the caller claimed, so a slow
 *     launch can never wipe a journal a newer one has since written.
 *
 * The journal is cleared only after a repair fully succeeded; a failed repair
 * leaves it in place with its attempt counted, and after
 * {@link MAX_ADOPTION_REPAIR_ATTEMPTS} the app stops retrying and says so.
 */

export const ADOPTION_JOURNAL_KEY = 'testLoopAdoption';
/** After this many failed repairs the journal is abandoned, with a notice -- never retried forever. */
export const MAX_ADOPTION_REPAIR_ATTEMPTS = 3;

export type AdoptionStage =
  | 'staged'
  | 'stored'
  | 'selected'
  | 'session-started'
  | 'recording'
  | 'out-lap';

export interface AdoptionJournal {
  /** Identity of THIS adoption -- what a claim and a clear are checked against. */
  id: string;
  circuitId: string;
  stage: AdoptionStage;
  sessionId?: string;
  /** How many launches have tried to repair it. */
  attempts: number;
  /** The launch that currently owns the repair. */
  claimedBy?: string;
}

export interface ClaimedAdoptionJournal {
  journal: AdoptionJournal;
  /** The exact row value this claim wrote -- the compare half of the compare-and-delete. */
  serialized: string;
  /** True when the attempt budget is spent: the caller abandons the journal instead of repairing. */
  exhausted: boolean;
}

/** A fresh journal/launch identity. Uses the platform UUID where there is one. */
export function newJournalId(): string {
  const cryptoApi = (globalThis as { crypto?: { randomUUID?: () => string } }).crypto;
  if (typeof cryptoApi?.randomUUID === 'function') return cryptoApi.randomUUID();
  const hex = (length: number): string =>
    Array.from({ length }, () => Math.floor(Math.random() * 16).toString(16)).join('');
  return `${hex(8)}-${hex(4)}-4${hex(3)}-a${hex(3)}-${hex(12)}`;
}

function parse(raw: string): AdoptionJournal | null {
  try {
    const parsed: unknown = JSON.parse(raw);
    if (typeof parsed !== 'object' || parsed === null) return null;
    const journal = parsed as Partial<AdoptionJournal>;
    if (typeof journal.circuitId !== 'string' || typeof journal.stage !== 'string') return null;
    return {
      id: typeof journal.id === 'string' ? journal.id : newJournalId(),
      circuitId: journal.circuitId,
      stage: journal.stage,
      attempts: typeof journal.attempts === 'number' ? journal.attempts : 0,
      ...(typeof journal.sessionId === 'string' ? { sessionId: journal.sessionId } : {}),
      ...(typeof journal.claimedBy === 'string' ? { claimedBy: journal.claimedBy } : {}),
    };
  } catch {
    return null;
  }
}

/** Writes (or overwrites) the journal for the adoption in progress, returning the row it wrote. */
export async function writeAdoptionJournal(
  db: SqlDatabase,
  journal: AdoptionJournal,
): Promise<ClaimedAdoptionJournal> {
  const serialized = JSON.stringify(journal);
  await db.runAsync('INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)', [
    ADOPTION_JOURNAL_KEY,
    serialized,
  ]);
  // P5d-FIX5 H1: the caller keeps the EXACT row it wrote, so it can later
  // clear that row and only that row (compare-and-delete) instead of deleting
  // whatever journal happens to occupy the key by then.
rg: apps/mobile/src/telemetry: The system cannot find the file specified. (os error 2)
packages/core/src/telemetry\elm327Session.ts:168:    this.runPromise = this.run();
packages/core/src/telemetry\elm327Session.ts:227:        const result = await this.executeCommand(command, this.config.initTimeoutMs);
packages/core/src/telemetry\elm327Session.ts:273:        result = await this.executeCommand(command, this.config.commandTimeoutMs);
packages/core/src/telemetry\elm327Session.ts:333:  private executeCommand(command: string, timeoutMs: number): Promise<CommandResult> {
packages/core/src/telemetry\enet\enetSession.ts:305:    this.runPromise = this.run();
packages/core/src/telemetry\enet\enetSession.ts:460:      outcome = await this.executeDiagnosticRequest(
packages/core/src/telemetry\enet\enetSession.ts:628:  private executeDiagnosticRequest(
rg: apps/mobile/src/ui/SettingsScreen.tsx: IO error for operation on apps/mobile/src/ui/SettingsScreen.tsx: The system cannot find the file specified. (os error 2)
import { TMR_CIRCUIT_PROFILE } from './tmrProfile';

export type SpeedUnits = 'kmh' | 'mph';

/**
 * ENET telemetry addendum (contracts.md, 2026-08-27, binding, Phase 4e). The
 * second OBD transport, next to ELM327: BMW ENET (HSFZ framing over TCP
 * carrying UDS PDUs). Defaults to `'elm327'` -- every existing install (and
 * every field below this one) hydrates unchanged, and the ELM327 path stays
 * byte-identical in behavior regardless of this addendum (`telemetryProvider.ts`
 * only ever builds ENET machinery when this is `'enet'`).
 */
export type AdapterType = 'elm327' | 'enet';

/**
 * Ticket P4l-FIX1 F2 (binding, the P4l worker's own concern 4): the app's ONE
 * language. The exportable Signal Finder summary was already written in both
 * RO and EN (`signalFinderExport.ts`), but the screen hard-coded `'en'`
 * because no language setting existed. Same two-value vocabulary as
 * `@circuit/core`'s coaching `ReportLanguage`, so one setting drives every
 * user-facing text this app renders in more than one language.
 */
export type AppLanguage = 'ro' | 'en';

/**
 * The DEFAULT language for a device locale (BCP-47 tag or a legacy
 * underscore form): Romanian for a `ro` PRIMARY subtag -- `ro`, `ro-RO`,
 * `ro_RO`, `ro-MD` -- English for everything else, including an absent or
 * unreadable locale. Matched on the primary subtag alone, never a prefix, so
 * `roa`/`rom` (other languages that merely start with "ro") stay English,
 * and `hu-RO` (Hungarian spoken in Romania) is Hungarian, not Romanian.
 * Pure: takes the locale rather than reading one, so the rule can be pinned
 * by a test without any native locale API.
 */
export function defaultLanguageForLocale(locale: string | null | undefined): AppLanguage {
  if (typeof locale !== 'string') return 'en';
  const primary = locale.trim().toLowerCase().split(/[-_]/)[0] ?? '';
  return primary === 'ro' ? 'ro' : 'en';
}

/**
 * The device's own locale, read through the `Intl` global (present in Hermes
 * and in Node/vitest -- no new native dependency, and no `react-native`
 * import, so this module stays directly importable by vitest). Any failure
 * -- a runtime built without full ICU, a throwing resolver -- degrades to
 * `null`, which `defaultLanguageForLocale` maps to `'en'`.
 */
export function readDeviceLocale(): string | null {
  try {
    return new Intl.DateTimeFormat().resolvedOptions().locale ?? null;
  } catch {
    return null;
  }
}

export interface CoverageBinsSetting {
  /** Fraction thresholds, ascending, in (0,1), used to bucket calibration coverage for display. */
  thresholds: readonly number[];
}

export interface AppSettings {
  units: SpeedUnits;
  /** Live delta magnitude (ms) below which the dashboard shows neutral/gray instead of faster/slower. */
  deltaDeadbandMs: number;
  coverageBins: CoverageBinsSetting;
  /**
   * Phase 3 coaching addendum toggle. When `false`, `composition.ts` builds
   * every `SessionController` (production AND DevReplay) with coaching
   * disabled -- no `CoachEngine` is instantiated and `FacadeState.coachCue`
   * stays `null` for the whole session, regardless of the TMR corner data
   * being available. Advisory-only either way (contracts.md's Coaching
   * addendum); this only controls whether cues are computed/surfaced at all.
   * Defaults to `true` -- UI rendering of the cue itself is a later work
   * package, this ticket only plumbs the data.
   */
  coachingEnabled: boolean;
  /**
   * Phase 3 coaching addendum sub-toggle (voice cues via `session/voiceCoach.ts`
   * + expo-speech). Only meaningful while `coachingEnabled` is also `true` --
   * `voiceCoach.ts` gates on BOTH fields, never speaks with `coachingEnabled`
   * `false` regardless of this value, and `SettingsScreen` only shows this
   * row when `coachingEnabled` is on. Defaults to `false`: voice is opt-in,
   * unlike the on-screen coach cues themselves.
   */
  voiceCoachEnabled: boolean;
  /**
   * Ticket P5c-B (contracts.md "Phase 5 REVISION 2" R2-3, user-ratified): the
   * opt-in gate for the whole trackday suggestion stage â€” the between-stint
   * pit view, its bounded per-corner suggestions, and the live brake-cue
   * updates between laps. Defaults to `false`, and OFF means OFF everywhere:
   * no lap boundary reads the recording, no cue ever moves, no suggestion is
   * computed or exported, and the pit-view entry point is not shown. Turning
   * it on never lifts a bound â€” a cue may still only move to a point a clean
   * lap of the SAME outing already demonstrated, by at most 10 m / +3 km/h,
   * once per corner per stint. Advice is never shown while driving.
   */
  suggestionsEnabled: boolean;
  /**
   * Telemetry addendum (Phase 4 / P4a, docs/architecture/contracts.md).
   * Advisory/experimental: reads vehicle telemetry (RPM, speed, throttle,
   * coolant temperature) from a local WiFi OBD-II adapter, strictly
   * read-only on the vehicle bus. Defaults to `false` -- opt-in, and never
   * required for lap timing (`composition.ts`'s session integration MUST
   * leave every session 100% functional with this on and the adapter
   * dead/absent).
   */
  telemetryEnabled: boolean;
  /**
   * Dev-only: when `true` (and `telemetryEnabled`), `telemetryProvider.ts`
   * builds a `SimulatedElm327Transport` (`@circuit/core`) instead of the real
   * `TcpObdTransport`, so telemetry can be exercised without a physical
   * adapter. `SettingsScreen` only shows this toggle in `__DEV__`. Defaults
   * to `false`.
   */
  telemetrySimulate: boolean;
  /** WiFi OBD-II adapter host (Telemetry addendum: adapter is a local WiFi AP). */
  adapterHost: string;
  /** WiFi OBD-II adapter TCP port. */
  adapterPort: number;
  /**
   * Telemetry addendum â€” channel revision (2026-08-11, binding). Advanced,
   * vehicle-specific setting: the raw hex OBD/mode-22 request for
   * transmission oil temperature, sent verbatim (no standard mode-01 PID
   * exists for this channel). Decoded as the last response data byte minus
   * 40, same as every other OBD temperature channel. Empty string (the
   * default) means "not configured" -- `telemetryProvider.ts`'s poll plan
   * then omits `transOilC` entirely rather than polling a request nobody
   * supplied. Hex characters and spaces only; validated on blur by
   * `SettingsScreen`'s `parseHexPidDraft()`.
   */
  transOilPidHex: string;
  /**
   * Multi-circuit selection addendum (contracts.md, 2026-08-26, ticket
   * CN-W3): the ONE circuit every production `SessionController`, the
   * session-history store, coaching corners, the calibration track map, and
   * the circuit/PB screens are built for. Defaults to Transilvania Motor
   * Ring (`DEFAULT_SETTINGS`, below) -- existing installs with no persisted
   * value hydrate to this exact default, so TMR's behavior is unchanged.
   * An id absent from the bundled catalog resolves back to this default with
   * a `console.warn` (`circuitCatalog.ts`'s `resolveSelectedCircuit`) --
   * never a crash, never a fetch.
   */
  selectedCircuitId: string;
  /**
   * ENET telemetry addendum (binding, Phase 4e): which OBD transport
   * `telemetryProvider.ts` builds. Defaults to `'elm327'` -- existing behavior
   * unchanged for every install with no persisted value.
   */
  adapterType: AdapterType;
  /**
   * ENET adapter IP (its own local WiFi AP, same "adapter is a local WiFi
   * AP" model as the ELM327 `adapterHost` above, but a SEPARATE field -- the
   * two adapter types are never conflated, so switching `adapterType` back
   * and forth never clobbers either adapter's own remembered address).
   * Defaults to `''`: unlike the ELM327 adapter (a fixed default AP address),
   * the user reads the ENET adapter's IP from its own web UI -- `SettingsScreen`'s
   * copy explains this, there is no sane universal default to pre-fill.
   */
  enetHost: string;
  /** ENET adapter TCP port (contracts.md addendum: HSFZ over TCP 6801, verified). */
  enetPort: number;
  /** UDS tester (source) address ENET frames are sent from. EMPIRICAL (addendum: default 0xF4, alt 0xF1) -- never hardcoded without this override. */
  enetTesterAddress: number;
  /** UDS target (destination) address ENET frames are sent to. EMPIRICAL (addendum: default 0x12 = DME) -- never hardcoded without this override. */
  enetTargetAddress: number;
  /**
   * Advanced, vehicle-specific: a JSON array of `EnetChannelSpec` (`@circuit/core`),
   * each entry a `mode: 'obd01' | 'did'` request plus (for `did`) a decode
   * formula and REQUIRED provenance -- see the ENET addendum's own
   * `EnetChannelSpec` doc comment. Empty string (the default) means "use the
   * built-in defaults" (`DEFAULT_ENET_CHANNEL_SPECS`: obd01 rpm/speedKph/
   * throttlePct/coolantC/engineOilC, all EMPIRICAL on the ENET path, no
   * default `did` specs). Validated through `@circuit/core`'s
   * `validateEnetChannelSpecs` (`enetSettingsValidation.ts`'s
   * `resolveEnetChannelSpecs`/`validateEnetChannelSpecsJson`) both by
   * `SettingsScreen` (inline warnings on blur) and by `telemetryProvider.ts`
   * (re-validated on every `start()`, same "never trust a persisted value
   * blindly" rule as `transOilPidHex`/`buildCustomPids` above) -- malformed
   * JSON falls back to the built-in defaults with a `console.warn`, never a
   * crash.
   */
  enetChannelSpecsJson: string;
  /**
   * ENET auto-discovery & DID sweep addendum (binding, Phase 4f): where
   * `enetHost`/`enetPort` came from -- `''` (the default) means "entered
   * manually" (or never set); `discovered <date>` (ISO date) means a "Find
   * adapter" tap or the provider's own auto-discovery-on-connect-failure
   * detour applied a discovery hit. Display-only (SettingsScreen shows it
   * next to the host/port fields) -- never read by `telemetryProvider.ts`'s
   * connection logic itself.
   */
  enetHostProvenance: string;
  /**
   * ENET auto-discovery addendum (binding, Phase 4f): when `true` (the
   * default) and `adapterType === 'enet'`, `telemetryProvider.ts` runs
   * discovery ONCE per `start()` -- immediately when no host is configured,
   * or after the first connect attempt to a configured host fails -- and
   * applies a level-2 hit automatically. `false` reverts to the pre-addendum
   * behavior: a configured host is dialed directly, and a failure gets the
   * plain single reconnect retry, with no discovery involved at all.
   */
  enetAutoDiscover: boolean;
  /**
   * Field revision (2026-08-27, binding, "hidden developer mode"): when
   * `true` (or the build is `__DEV__`), `SettingsScreen` shows "Dev: DID
   * Probe (ENET)"/"Dev: DID Sweep (ENET)" -- the dev-only ENET diagnostic
   * tools stay REGISTERED as routes in every build, release included (App
   * Store review sees one binary), only their Settings-screen entry points
   * are hidden until this is on. Toggled by 7 taps on the About section's
   * version text within a short window (`registerDevTap`, below) -- never
   * shown as its own settings row. `DevReplay` is UNAFFECTED: it stays
   * `__DEV__`-only regardless of this setting. Defaults to `false`.
   */
  developerModeEnabled: boolean;
  /**
   * Ticket P4l-FIX1 F2 (binding): the app language (`'ro' | 'en'`). The
   * static default below is `'en'`; the DEVICE-LOCALE default
   * (`defaultLanguageForLocale(readDeviceLocale())`) is applied once, at
   * hydration, ONLY when nothing was ever persisted -- so a user who chose a
   * language keeps it even when they travel, and an existing install with no

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg --files apps/mobile/src | rg 'Store|Screen|signalFinder' ; rg -n 'INSERT|REPLACE|CREATE TABLE|settings' apps/mobile/src --glob '*Store*'; rg -n 'provenance|interface EnetChannelSpec' packages/core/src/telemetry/enet* -g '*.ts'; rg -n '"'^  [a-zA-Z]+[?:]:'"' apps/mobile/src/session/settingsStore.ts; Get-Content apps/mobile/src/session/adoptionJournal.ts | Select-Object -Skip 25 -First 35; Get-Content apps/mobile/src/session/learnedCircuitStore.ts | Select-Object -Skip 65 -First 55; Get-Content packages/core/src/telemetry/signalFinder/runner.ts | Select-Object -Skip 275 -First 16; rg -n 'paused|clock:|now:' apps/mobile/src/session/signalFinder*" in D:\CODE\APLICTIE_Circuit
 exited 1 in 74ms:
apps/mobile/src\session\learnedCircuitStore.ts:123:   * plain INSERT, so an id that is somehow already taken is REPORTED rather
apps/mobile/src\session\learnedCircuitStore.ts:136:        `INSERT INTO learned_circuits
apps/mobile/src\session\learnedCircuitStore.ts:157:      `INSERT OR REPLACE INTO learned_circuits
apps/mobile/src\session\settingsStore.ts:211:   * shown as its own settings row. `DevReplay` is UNAFFECTED: it stays
apps/mobile/src\session\settingsStore.ts:295:   * against offline. Fusion REPLACES the estimator behind those two channels
apps/mobile/src\session\settingsStore.ts:323: * settings vocabulary and must not depend on the persistence layer.
apps/mobile/src\session\settingsStore.ts:337: * settings row never carried a choice of its own -- see
apps/mobile/src\session\settingsStore.ts:454: * Persistence-agnostic settings surface consumed by `SettingsScreen` (S12).
apps/mobile/src\session\settingsStore.ts:465:/** In-memory settings store — DEV-ONLY, resets on app restart. */
apps/mobile/src\session\settingsStore.ts:467:  private settings: AppSettings = { ...DEFAULT_SETTINGS };
apps/mobile/src\session\settingsStore.ts:471:    return this.settings;
apps/mobile/src\session\settingsStore.ts:476:    cb(this.settings);
apps/mobile/src\session\settingsStore.ts:483:    this.settings = { ...this.settings, ...patch };
apps/mobile/src\session\settingsStore.ts:484:    for (const listener of this.listeners) listener(this.settings);
apps/mobile/src\persistence\sqlSettingsStore.ts:8:} from '../session/settingsStore';
apps/mobile/src\persistence\sqlSettingsStore.ts:11:export const SETTINGS_KEY = 'app-settings';
apps/mobile/src\persistence\sqlSettingsStore.ts:18: * `SettingsStore` backed by the `settings` key-value table added in the
apps/mobile/src\persistence\sqlSettingsStore.ts:28:  private settings: AppSettings;
apps/mobile/src\persistence\sqlSettingsStore.ts:45:    this.settings = initial;
apps/mobile/src\persistence\sqlSettingsStore.ts:57:    const rows = await db.getAllAsync<{ value: string }>('SELECT value FROM settings WHERE key = ?', [
apps/mobile/src\persistence\sqlSettingsStore.ts:83:        // settings are non-critical and never block app startup.
apps/mobile/src\persistence\sqlSettingsStore.ts:126:    // boolean settings above are, and NOT counted as a stored choice (the
apps/mobile/src\persistence\sqlSettingsStore.ts:156:    // install from before these settings existed carries no key at all and
apps/mobile/src\persistence\sqlSettingsStore.ts:179:    return this.settings;
apps/mobile/src\persistence\sqlSettingsStore.ts:184:    cb(this.settings);
apps/mobile/src\persistence\sqlSettingsStore.ts:191:    this.settings = { ...this.settings, ...patch };
apps/mobile/src\persistence\sqlSettingsStore.ts:192:    for (const listener of this.listeners) listener(this.settings);
apps/mobile/src\persistence\sqlSettingsStore.ts:198:      await this.db.runAsync('INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)', [
apps/mobile/src\persistence\sqlSettingsStore.ts:200:        JSON.stringify(this.settings),
apps/mobile/src\persistence\sqlSettingsStore.ts:216: * The `settings` key this log lives under. A SEPARATE key from
apps/mobile/src\persistence\sqlSettingsStore.ts:264:    const rows = await db.getAllAsync<{ value: string }>('SELECT value FROM settings WHERE key = ?', [
apps/mobile/src\persistence\sqlSettingsStore.ts:290:    await db.runAsync('INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)', [
apps/mobile/src\persistence\sqlSettingsStore.ts:307:    await db.runAsync('DELETE FROM settings WHERE key = ?', [UNVALIDATED_MATCHING_KEY]);
rg: packages/core/src/telemetry/enet*: IO error for operation on packages/core/src/telemetry/enet*: The filename, directory name, or volume label syntax is incorrect. (os error 123)
 */

export const ADOPTION_JOURNAL_KEY = 'testLoopAdoption';
/** After this many failed repairs the journal is abandoned, with a notice -- never retried forever. */
export const MAX_ADOPTION_REPAIR_ATTEMPTS = 3;

export type AdoptionStage =
  | 'staged'
  | 'stored'
  | 'selected'
  | 'session-started'
  | 'recording'
  | 'out-lap';

export interface AdoptionJournal {
  /** Identity of THIS adoption -- what a claim and a clear are checked against. */
  id: string;
  circuitId: string;
  stage: AdoptionStage;
  sessionId?: string;
  /** How many launches have tried to repair it. */
  attempts: number;
  /** The launch that currently owns the repair. */
  claimedBy?: string;
}

export interface ClaimedAdoptionJournal {
  journal: AdoptionJournal;
  /** The exact row value this claim wrote -- the compare half of the compare-and-delete. */
  serialized: string;
  /** True when the attempt budget is spent: the caller abandons the journal instead of repairing. */
  exhausted: boolean;
}

/** A fresh journal/launch identity. Uses the platform UUID where there is one. */
  async refresh(): Promise<void> {
    const rows = await this.db.getAllAsync<LearnedCircuitRow>(
      'SELECT circuit_id, display_name, payload, length_m, corner_count, created_at_utc, saved FROM learned_circuits ORDER BY created_at_utc DESC',
    );
    const cache = new Map<string, LearnedCircuitEntry>();
    let invalid = 0;
    for (const row of rows) {
      const decoded = decodeLearnedCircuit(row.payload);
      if (!decoded.ok) {
        invalid += 1;
        console.warn(
          `[learnedCircuitStore] skipping unreadable learned circuit "${row.circuit_id}": ${decoded.errors.join(', ')}`,
        );
        continue;
      }
      cache.set(row.circuit_id, {
        profile: decoded.profile,
        runtime: decoded.runtime,
        corners: decoded.corners,
        cornersRecovered: decoded.cornersRecovered,
        record: {
          circuitId: row.circuit_id,
          displayName: decoded.profile.displayName,
          lengthM: row.length_m,
          cornerCount: decoded.corners.length,
          createdAtUtc: row.created_at_utc,
          saved: row.saved !== 0,
        },
      });
    }
    this.cache = cache;
    this.invalidRows = invalid;
  }

  /** Every learned circuit, saved or not -- what the catalog resolves session geometry from. */
  entries(): LearnedCircuitEntry[] {
    return [...this.cache.values()];
  }

  /** Only the circuits the driver actually saved -- what the selection list shows. */
  saved(): LearnedCircuitRecord[] {
    return this.entries()
      .filter((entry) => entry.record.saved)
      .map((entry) => entry.record);
  }

  get(circuitId: string): LearnedCircuitEntry | null {
    return this.cache.get(circuitId) ?? null;
  }

  /** How many stored rows could not be read back (diagnostics; never silently zero). */
  invalidRowCount(): number {
    return this.invalidRows;
  }

    answered,
    requestCount: first.requestCount + second.requestCount,
    answeredCount: first.answeredCount + second.answeredCount,
    answeredElapsedMs: first.answeredElapsedMs + second.answeredElapsedMs,
    respondingEcus: [...new Set([...first.respondingEcus, ...second.respondingEcus])].sort((a, b) => a - b),
    backedOff,
  };
}

function waitMs(ms: number): Promise<void> {
  return new Promise((resolve) => setTimeout(resolve, Math.max(0, ms)));
}

/** Never lets a channel implementation's throw/rejection/hang escape or exceed its own budget. */
function guarded<T>(fn: () => Promise<T>, timeoutMs: number): Promise<{ ok: true; value: T } | { ok: false }> {
  return new Promise((resolve) => {
rg: apps/mobile/src/session/signalFinder*: IO error for operation on apps/mobile/src/session/signalFinder*: The filename, directory name, or volume label syntax is incorrect. (os error 123)

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "Get-Content apps/mobile/test/persistence/deviceDataWipe.test.ts -TotalCount 100; Get-Content apps/mobile/src/persistence/didSweepStore.ts -TotalCount 100; rg -n 'provenance|note' packages/core/src/telemetry/enet/channelSpec*; rg -n 'paused|clock|now:' apps/mobile/src/session/signalFinderController.ts; rg -n 'deleteAllStoredUserData|result.ok|deleteStatus' apps/mobile/src/ui/screens/SettingsScreen.tsx; rg -n '"'^  '"\\w+[?]?: ' apps/mobile/src/session/settingsStore.ts; rg -n 'list\\(|listed' apps/mobile/src/session/circuitCatalog.ts; rg -n 'INSERT|runAsync|CREATE TABLE' apps/mobile/src packages/core/src -i --glob '*.ts' | Select-Object -Last 20; Get-Content apps/mobile/src/session/testLoopController.ts | Select-String -Pattern 'retry|adopt|error' -Context 2,3" in D:\CODE\APLICTIE_Circuit
 succeeded in 114ms:
341:  clock: MonotonicClock;
342:  /** Wall-clock ISO string source (defaults to `new Date().toISOString()`) — injected so tests are deterministic. */
662:  let control: DidSweepControl = { paused: false, stopped: false };
706:      const elapsedMs = deps.clock.now() - anchorMs;
1283:          clock: deps.clock,
1338:            clock: deps.clock,
1389:            clock: deps.clock,
1528:      control = { paused: false, stopped: false };
1578:        sessionId: `signal-finder-${deps.clock.now()}-${Math.random().toString(36).slice(2, 8)}`,
1632:      control = { paused: false, stopped: false };
9:  deleteAllStoredUserData,
91:  return result.ok ? result.value : null;
168:  // M3 fix (ticket CN-FIX2): `deleteAllStoredUserData()`'s aggregate error
247:    if (result.ok) {
343:    if (result.ok) {
467:      const result = await deleteAllStoredUserData();
468:      setDeleteBanner(result.ok ? 'success' : 'error');
469:      setDeleteErrorText(result.ok ? null : result.errorText);
470:      setDeleteRefused(!result.ok && result.reason !== undefined);
58:  thresholds: readonly number[];
62:  units: SpeedUnits;
64:  deltaDeadbandMs: number;
65:  coverageBins: CoverageBinsSetting;
76:  coachingEnabled: boolean;
85:  voiceCoachEnabled: boolean;
97:  suggestionsEnabled: boolean;
107:  telemetryEnabled: boolean;
115:  telemetrySimulate: boolean;
117:  adapterHost: string;
119:  adapterPort: number;
131:  transOilPidHex: string;
143:  selectedCircuitId: string;
149:  adapterType: AdapterType;
159:  enetHost: string;
161:  enetPort: number;
163:  enetTesterAddress: number;
165:  enetTargetAddress: number;
182:  enetChannelSpecsJson: string;
192:  enetHostProvenance: string;
202:  enetAutoDiscover: boolean;
214:  developerModeEnabled: boolean;
224:  language: AppLanguage;
245:  activeVehicleProfileId: string;
258:  activeVehicleProfileSource: 'user' | 'vin' | 'default';
268:  lastSeenVin: string | null;
283:  imuFusionEnabled: boolean;
302:  imuGyroCaptureEnabled: boolean;
316:  analysisSmoothingEnabled: boolean;
326:  profileId: string;
327:  channel: string;
328:  status: string;
356:  bindings: readonly VehicleProfileBindingSummary[],
393:  count: number;
394:  firstTapAtMs: number;
401:  state: DevTapState | null,
402:  nowMs: number,
403:  threshold: number = DEV_TAP_THRESHOLD,
404:  windowMs: number = DEV_TAP_WINDOW_MS,
418:  units: 'kmh',
419:  deltaDeadbandMs: 100,
420:  coverageBins: { thresholds: [0.25, 0.5, 0.75, 1] },
421:  coachingEnabled: true,
422:  voiceCoachEnabled: false,
423:  suggestionsEnabled: false,
424:  telemetryEnabled: false,
425:  telemetrySimulate: false,
426:  adapterHost: '192.168.0.10',
427:  adapterPort: 35_000,
428:  transOilPidHex: '',
429:  selectedCircuitId: TMR_CIRCUIT_PROFILE.circuitId,
430:  adapterType: 'elm327',
431:  enetHost: '',
432:  enetPort: 6_801,
433:  enetTesterAddress: 0xf4,
434:  enetTargetAddress: 0x12,
435:  enetChannelSpecsJson: '',
436:  enetHostProvenance: '',
437:  enetAutoDiscover: true,
438:  developerModeEnabled: false,
439:  language: 'en',
440:  activeVehicleProfileId: GENERIC_VEHICLE_PROFILE_ID,
441:  activeVehicleProfileSource: 'default',
442:  lastSeenVin: null,
445:  imuFusionEnabled: false,
449:  imuGyroCaptureEnabled: true,
450:  analysisSmoothingEnabled: false,
51:  list(): CircuitSummary[];
117: *  - `listed: true`  -- the driver saved and named it, so it belongs in the
119: *  - `listed: false` -- a one-off test loop, kept ONLY so its own session can
121: * `get()` resolves BOTH; `list()` shows only the first kind.
130:  listed: boolean;
166:  list(): CircuitSummary[] {
172:      .filter((entry) => entry.listed)
apps/mobile/src\session\learnedCircuitStore.ts:156:    await this.db.runAsync(
apps/mobile/src\session\learnedCircuitStore.ts:157:      `INSERT OR REPLACE INTO learned_circuits
apps/mobile/src\session\learnedCircuitStore.ts:165:  /** The one row shape both writers use, so an insert and an update can never disagree. */
apps/mobile/src\session\learnedCircuitStore.ts:217:    await this.db.runAsync('DELETE FROM learned_circuits WHERE circuit_id = ?', [circuitId]);
apps/mobile/src\session\trackMapModel.ts:183: * already at or under the target is left untouched (0 interior points inserted) -- a
apps/mobile/src\session\trackMapModel.ts:186: * consecutive-point distances) is preserved exactly, since every inserted point sits on
apps/mobile/src\session\telemetryProvider.ts:882:   * THIS session's poll plan actually inserted has neither a persisted
apps/mobile/src\session\telemetryProvider.ts:1091:   * session's poll plan actually inserted (see `insertedBrakeBindings` in
apps/mobile/src\session\telemetryProvider.ts:1553:   * bindings actually ENTERED the plan (`inserted`) alongside the specs
apps/mobile/src\session\telemetryProvider.ts:1557:   * (`brakeBindingsSignature(inserted)`, read by both `buildEnetConfig()` and
apps/mobile/src\session\telemetryProvider.ts:1565:  ): { specs: EnetChannelSpec[]; inserted: ResolvedBrakeBinding[] } {
apps/mobile/src\session\telemetryProvider.ts:1568:    const inserted: ResolvedBrakeBinding[] = [];
apps/mobile/src\session\telemetryProvider.ts:1573:      if (valid.length > 0) inserted.push(brakeBinding);
apps/mobile/src\session\telemetryProvider.ts:1576:    return { specs, inserted };
apps/mobile/src\session\telemetryProvider.ts:1591:    const { specs: brakeSpecs, inserted: insertedBrakeBindings } = computeBrakePollAdditions(baseChannelSpecs, {
apps/mobile/src\session\telemetryProvider.ts:1595:    activeBrakeBindingsSignature = brakeBindingsSignature(insertedBrakeBindings);
apps/mobile/src\session\telemetryProvider.ts:1596:    activeBrakeSwitchCoarse = insertedBrakeBindings.some((binding) => binding.channel === 'brakeSwitch' && binding.coarse);
apps/mobile/src\session\telemetryProvider.ts:2535:        const freshInsertedBrakeBindings = computeBrakePollAdditions(resolveBaseEnetChannelSpecs(), { warn: false }).inserted;
apps/mobile/src\session\telemetryProvider.ts:2538:          brakeBindingsSignature(freshInsertedBrakeBindings) !== activeBrakeBindingsSignature;
apps/mobile/src\session\signalFinderController.ts:1262:        // channel of its OWN (never inserted into `channels`, so the round's

   * P5d-FIX1 H3 (Codex P5d-REV1 HIGH 3): `learned` is reached ONLY after the
   * learned circuit has been persisted, registered and selected. While that is
>  * in flight the phase is `adopting`; if it fails, the phase is `error` (with a
>  * retry), never `learned` -- a driver told the track was learned while the app
   * quietly kept the previously selected circuit would then be timed, and
   * coached, on the wrong geometry.
   */
> export type TestLoopPhase = 'idle' | 'learning' | 'adopting' | 'learned' | 'failed' | 'error';
  
  export interface LearnedTrackSummary {
    circuitId: string;
    failure: TestLoopFailure | null;
    /** P5d-FIX1 H3: why keeping the just-learned track failed, in the driver's words. */
>   adoptError: string | null;
  }
  
  export interface TestLoopControllerDeps {
     * it does (persist, register, select, roll the session into timing) is the
     * composition layer's business, not this module's -- but it is AWAITED
>    * (P5d-FIX1 H3): the phase stays `adopting` until it resolves, and a
>    * rejection becomes `error`, never `learned`.
     */
    onLearned: (circuit: TestLoopCircuit) => void | Promise<void>;
    /** Safety valve: a trace longer than this stops learning with an honest failure. */
    /** P5d-FIX1 H3: the learned circuit waiting to be kept -- retried, never re-derived. */
    private pendingCircuit: TestLoopCircuit | null = null;
>   private adoptError: string | null = null;
    /** Minted once per learn phase, not once per closure ATTEMPT -- the id belongs to this drive. */
    private circuitId: string | null = null;
    private readonly listeners = new Set<(snapshot: TestLoopSnapshot) => void>();
        learned: this.learned,
        failure: this.failure,
>       adoptError: this.adoptError,
      };
    }
  
      this.failure = null;
      this.pendingCircuit = null;
>     this.adoptError = null;
      this.circuitId = null;
      this.phase = 'learning';
      this.emit();
      this.failure = null;
      this.pendingCircuit = null;
>     this.adoptError = null;
      this.circuitId = null;
      this.emit();
    }
      }
      this.lapSamples = result.lapSamples;
>     this.adopt(result);
    }
  
    /** The QUALIFIED fixes of the learned lap -- what the session stores as its out-lap trace. */
    /**
     * P5d-FIX1 H3: the learned track is handed over and only THEN announced.
>    * `adopting` is a real, visible phase -- the driver is not told the track was
     * learned while the app is still deciding whether it could keep it.
     */
>   private adopt(circuit: TestLoopCircuit): void {
      this.pendingCircuit = circuit;
>     this.phase = 'adopting';
>     this.adoptError = null;
      this.emit();
      void Promise.resolve()
        .then(() => this.deps.onLearned(circuit))
            cornerCount: circuit.corners.length,
          };
>         this.adoptError = null;
          this.emit();
        })
>       .catch((error: unknown) => {
          // Never `learned`: the track exists in memory but nothing else knows
          // about it, so timing it would be timing the wrong circuit.
>         this.phase = 'error';
          this.learned = null;
>         this.adoptError = error instanceof Error ? error.message : String(error);
          this.emit();
        });
    }
     * (P5d-FIX1 H3). The geometry is not re-derived -- it is the same lap.
     */
>   retryAdopt(): void {
      const circuit = this.pendingCircuit;
>     if (this.phase !== 'error' || circuit === null) return;
>     this.adopt(circuit);
    }
  
    private fail(sampleCap?: number): void {



exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -n 'insert|INSERT|runAsync|require|import\\(' apps/mobile/src/persistence/didSweepStore.ts; rg --files apps/mobile/src/persistence; rg -n 'enetChannelSpecsJson|provenance' packages/core/src/telemetry/enet apps/mobile/src/ui/screens/SettingsScreen.tsx | Select-Object -First 30; rg -n 'paused|clock|Date.now' apps/mobile/src/session/signalFinderController.ts | Select-Object -First 30; rg -n 'deleteAllStoredUserData|result.ok|deleteResult' apps/mobile/src/ui/screens/SettingsScreen.tsx; git diff dd84864..ae12a10 -- docs/legal/privacy-policy.ro.md; Get-Content apps/mobile/src/persistence/didSweepStore.ts | Select-Object -Last 30; Get-Content apps/mobile/test/support/sqlJsDatabase.ts -TotalCount 100" in D:\CODE\APLICTIE_Circuit
 succeeded in 91ms:

apps/mobile/src/ui/screens/SettingsScreen.tsx:337:      setEnetChannelSpecsDraft(settings.enetChannelSpecsJson);
apps/mobile/src/ui/screens/SettingsScreen.tsx:339:  }, [settings.enetChannelSpecsJson]);
apps/mobile/src/ui/screens/SettingsScreen.tsx:345:      settingsStore.update({ enetChannelSpecsJson: enetChannelSpecsDraft });
apps/mobile/src/ui/screens/SettingsScreen.tsx:843:                        // A manual edit invalidates whatever provenance the
apps/mobile/src/ui/screens/SettingsScreen.tsx:846:                        // manually" (empty provenance).
apps/mobile/src/ui/screens/SettingsScreen.tsx:947:                    provenance for any DID entry). Leave blank to use the built-in defaults.
packages/core/src/telemetry/enet\enetChannelSpecs.ts:105:  provenance: string;
packages/core/src/telemetry/enet\enetChannelSpecs.ts:114: * its own `provenance`.
packages/core/src/telemetry/enet\enetChannelSpecs.ts:121:    provenance: 'standard mode-01 PID 0x0C; EMPIRICAL whether the DME answers it over ENET (see enet-protocol-research.md #3)',
packages/core/src/telemetry/enet\enetChannelSpecs.ts:127:    provenance: 'standard mode-01 PID 0x0D; EMPIRICAL whether the DME answers it over ENET (see enet-protocol-research.md #3)',
packages/core/src/telemetry/enet\enetChannelSpecs.ts:133:    provenance: 'standard mode-01 PID 0x11; EMPIRICAL whether the DME answers it over ENET (see enet-protocol-research.md #3)',
packages/core/src/telemetry/enet\enetChannelSpecs.ts:139:    provenance:
packages/core/src/telemetry/enet\enetChannelSpecs.ts:146:    provenance: 'standard mode-01 PID 0x05; EMPIRICAL whether the DME answers it over ENET (see enet-protocol-research.md #3)',
packages/core/src/telemetry/enet\enetChannelSpecs.ts:152:    provenance: 'standard mode-01 PID 0x5C; EMPIRICAL whether the DME answers it over ENET (see enet-protocol-research.md #3)',
packages/core/src/telemetry/enet\enetChannelSpecs.ts:162: * raw `requestHex`/`provenance` pair itself; the resulting raw pedal
packages/core/src/telemetry/enet\enetChannelSpecs.ts:171:  provenance:
packages/core/src/telemetry/enet\enetChannelSpecs.ts:184: * `provenance`. A channel repeated across specs keeps only the LAST entry
packages/core/src/telemetry/enet\enetChannelSpecs.ts:249:    return spec.decodeValue === undefined ? 'did spec is missing a decode definition' : provenanceRejection(spec);
packages/core/src/telemetry/enet\enetChannelSpecs.ts:260:  return provenanceRejection(spec);
packages/core/src/telemetry/enet\enetChannelSpecs.ts:263:/** The last did-spec check, shared by both decode shapes: provenance is REQUIRED -- no public DID table exists, so every did spec is an empirical finding that must say where it came from. */
packages/core/src/telemetry/enet\enetChannelSpecs.ts:264:function provenanceRejection(spec: EnetChannelSpec): string | null {
packages/core/src/telemetry/enet\enetChannelSpecs.ts:265:  return spec.provenance.trim() === '' ? 'did spec is missing provenance' : null;
packages/core/src/telemetry/enet\didHeuristics.ts:452: * provenance `in-car sweep <date>, DID <hex>, decode <...>` into the channel
packages/core/src/telemetry/enet\didHeuristics.ts:480:    provenance: `in-car sweep ${date}, DID ${didHex}, decode ${suggestion.decode}`,
341:  clock: MonotonicClock;
342:  /** Wall-clock ISO string source (defaults to `new Date().toISOString()`) — injected so tests are deterministic. */
662:  let control: DidSweepControl = { paused: false, stopped: false };
706:      const elapsedMs = deps.clock.now() - anchorMs;
1283:          clock: deps.clock,
1338:            clock: deps.clock,
1389:            clock: deps.clock,
1528:      control = { paused: false, stopped: false };
1578:        sessionId: `signal-finder-${deps.clock.now()}-${Math.random().toString(36).slice(2, 8)}`,
1632:      control = { paused: false, stopped: false };
9:  deleteAllStoredUserData,
91:  return result.ok ? result.value : null;
168:  // M3 fix (ticket CN-FIX2): `deleteAllStoredUserData()`'s aggregate error
247:    if (result.ok) {
343:    if (result.ok) {
467:      const result = await deleteAllStoredUserData();
468:      setDeleteBanner(result.ok ? 'success' : 'error');
469:      setDeleteErrorText(result.ok ? null : result.errorText);
470:      setDeleteRefused(!result.ok && result.reason !== undefined);
diff --git a/docs/legal/privacy-policy.ro.md b/docs/legal/privacy-policy.ro.md
index ab88e47..c78fadb 100644
--- a/docs/legal/privacy-policy.ro.md
+++ b/docs/legal/privacy-policy.ro.md
@@ -121,11 +121,9 @@ tratează ca atare:
   și ultimele două caractere, de exemplu `WBA************12`.
 - Este afișat integral pe ecranul Signal Finder, adică exact acolo unde ai avea nevoie să îl citești.
 
-> **Limitare cunoscută, spusă cinstit.** În versiunea actuală, „Delete all my data" **nu** șterge
-> VIN-ul stocat, pentru că acea valoare se află în tabelul de setări al aplicației, iar rutina de
-> ștergere acoperă tabelele de sesiune. Până la remediere, singura cale de a elimina un VIN stocat
-> este dezinstalarea aplicației. Acest lucru este consemnat ca blocant de lansare în
-> `docs/legal/compliance-checklist.md`, iar politica trebuie actualizată în momentul remedierii.
+- „Delete all my data" (§7) șterge VIN-ul stocat și, odată cu el, un profil de vehicul pe care
+  aplicația l-a ales pe baza VIN-ului. Aplicația citește din nou VIN-ul data viitoare când este
+  conectat un adaptor de tip BMW ENET.
 
 ### 3.4 Senzorii de mișcare ai dispozitivului
 
@@ -270,17 +268,24 @@ verdict de tur, încercare de calibrare și turul tău de referință — pentru
 pentru cel selectat — și verifică efectiv că ștergerea a avut loc înainte de a raporta succes. Este
 ireversibilă.
 
+Șterge de asemenea VIN-ul stocat, un profil de vehicul ales de aplicație pe baza VIN-ului,
+legăturile de semnal confirmate pentru vehicul, semnalele excluse, înregistrările de baleiaj din
+diagnosticare, evidența profilului de vehicul folosit în fiecare sesiune și geometria circuitelor pe
+care le-ai învățat aplicației.
+
+Păstrează doar **preferințele** tale: unitățile de măsură, limba, adresa și portul adaptorului,
+comutatoarele pentru coaching, voce și sugestii, circuitul selectat și un profil de vehicul ales chiar
+de tine. Niciuna dintre ele nu te identifică pe tine sau mașina ta. Dezinstalarea aplicației le
+elimină și pe acestea.
+
 Două precizări oneste:
 
 1. Comanda este **refuzată cât timp o sesiune este în desfășurare**; încheie mai întâi sesiunea.
    Această regulă există ca ștergerea să nu se poată suprapune peste o înregistrare vie și să lase
    date în urmă.
-2. În această versiune, ștergerea **nu elimină** setările aplicației (inclusiv VIN-ul stocat),
-   legăturile de semnal confirmate pentru vehicul, înregistrările de baleiaj din diagnosticare și
-   geometria circuitelor pe care le-ai învățat aplicației. Circuitele învățate pot fi șterse
-   individual din ecranul de detaliu al circuitului respectiv. Pentru restul, **dezinstalarea
-   aplicației** elimină întreaga bază de date. Remedierea este un blocant de lansare consemnat în
-   `docs/legal/compliance-checklist.md`.
+2. Dacă o parte a ștergerii eșuează, aplicația spune asta în loc să raporteze succes. În acest caz,
+   un circuit învățat pe care îl folosește încă o sesiune rămasă este păstrat, ca sesiunea să poată fi
+   analizată în continuare; rulează din nou ștergerea.
 
 ### Dreptul la restricționare (art. 18) — „opriți-vă deocamdată"
 
             ruled_out_at_utc = excluded.ruled_out_at_utc`,
          [record.profileId, record.targetId, record.ecu, record.did, record.verdict, record.sessionId, record.ruledOutAtUtc],
        );
      }
    },
    async listRuledOut(profileId, targetId): Promise<SignalFinderRuledOutRecord[]> {
      const rows = await db.getAllAsync<RuledOutRow>(
        'SELECT * FROM signal_finder_ruled_out WHERE profile_id = ? AND target_id = ? ORDER BY ecu ASC, did ASC',
        [profileId, targetId],
      );
      return rows.map((row) => ({
        profileId: row.profile_id,
        targetId: row.target_id,
        ecu: row.ecu,
        did: row.did,
        verdict: row.verdict,
        sessionId: row.session_id,
        ruledOutAtUtc: row.ruled_out_at_utc,
      }));
    },
    async clearRuledOut(profileId, targetId): Promise<void> {
      await db.runAsync('DELETE FROM signal_finder_ruled_out WHERE profile_id = ? AND target_id = ?', [profileId, targetId]);
    },
  };
}

/** Same `db !== null` ternary convention as {@link createDidSweepStore} -- the web preview gets the in-memory implementation. */
export function createSignalFinderRuledOutStore(db: SqlDatabase | null): SignalFinderRuledOutStore {
  return db === null ? createInMemorySignalFinderRuledOutStore() : createSqlSignalFinderRuledOutStore(db);
}
import initSqlJs, { type Database } from 'sql.js';
import type { SqlBindValue, SqlDatabase, SqlRunResult } from '@circuit/core';

/**
 * Local copy of packages/core/test/persistence-sql/sqlJsDatabase.ts's sql.js
 * `SqlDatabase` adapter -- kept local for the same cross-tsconfig reason as
 * `coreTestDoubles.ts` (see its top comment). Reuses the `sql.js` PACKAGE
 * itself via workspace-root hoisting (ticket CONSTRAINTS: "sql.js reuse via
 * workspace root") -- it is core's devDependency, not re-declared here.
 */

let sqlJsModulePromise: ReturnType<typeof initSqlJs> | null = null;
function loadSqlJs(): ReturnType<typeof initSqlJs> {
  sqlJsModulePromise ??= initSqlJs();
  return sqlJsModulePromise;
}

export function wrapSqlJsDatabase(db: Database): SqlDatabase {
  // Ticket P10A: the SAME serialization the real connection has.
  // `openAppDatabase()` wraps its `SqlDatabase` in `gateSqlTransactions(...,
  // createSqlWriteGate())`, so two transactions on the one on-device
  // connection can never overlap. The composition tests hand this double
  // straight to a mocked `openAppDatabase` and so had no gate at all --
  // which stayed invisible only while nothing wrote a transaction
  // concurrently with the repository's. P10A H2's session record (written at
  // recording start, alongside the active-session pointer write) does, and
  // sql.js answers an overlapping BEGIN with "cannot start a transaction
  // within a transaction". Gating here makes the double faithful to
  // production rather than papering over the collision.
  let transactionTail: Promise<unknown> = Promise.resolve();
  const handle: SqlDatabase = {
    async execAsync(sql: string): Promise<void> {
      db.exec(sql);
    },

    async runAsync(sql: string, params: readonly SqlBindValue[] = []): Promise<SqlRunResult> {
      db.run(sql, params as SqlBindValue[]);
      return { changes: db.getRowsModified() };
    },

    async getAllAsync<T>(sql: string, params: readonly SqlBindValue[] = []): Promise<T[]> {
      const stmt = db.prepare(sql);
      try {
        if (params.length > 0) stmt.bind(params as SqlBindValue[]);
        const rows: T[] = [];
        while (stmt.step()) {
          rows.push(stmt.getAsObject() as T);
        }
        return rows;
      } finally {
        stmt.free();
      }
    },

    // Ticket P10B H5-B: hands the callback the transaction-scoped handle --
    // this same adapter, which is the ungated connection. In production the
    // gate sits ABOVE this wrapper (`gateSqlTransactions`), so a callback
    // that used the outer handle instead would queue behind its own
    // transaction.
    withTransactionAsync(fn: (tx: SqlDatabase) => Promise<void>): Promise<void> {
      const run = transactionTail.then(async () => {
        db.run('BEGIN');
        try {
          await fn(handle);
          db.run('COMMIT');
        } catch (err) {
          db.run('ROLLBACK');
          throw err;
        }
      });
      transactionTail = run.then(
        () => undefined,
        () => undefined,
      );
      return run;
    },
  };
  return handle;
}

/** Creates a brand-new in-memory sql.js-backed `SqlDatabase` for a test. */
export async function createSqlJsDatabase(): Promise<SqlDatabase> {
  const SQL = await loadSqlJs();
  return wrapSqlJsDatabase(new SQL.Database());
}

/** Wraps a `SqlDatabase` over the SAME underlying sql.js `Database` instance passed in, rather than a fresh one -- used to simulate re-opening the app's on-device database across a restart. */
export function wrapExistingSqlJsDatabase(db: Database): SqlDatabase {
  return wrapSqlJsDatabase(db);
}

export async function createRawSqlJsDatabase(): Promise<Database> {
  const SQL = await loadSqlJs();
  return new SQL.Database();
}

codex
The settings inventory includes custom ENET channel definitions with free-text provenance, which survive the wipe. The adoption journal and retry state also survive. Separately, the new policy promises a VIN read on the next ENET connection, but the detector remains marked `done`. I’m checking these failure scenarios and the runner’s timing bounds.

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -a -n 'INSERT|runAsync' apps/mobile/src/persistence/didSweepStore.ts; rg -a -n 'INSERT.*settings|CREATE TABLE' apps/mobile/src packages/core/src; Get-Content apps/mobile/src/session/signalFinderController.ts | Select-Object -Skip 1650 -First 100; Get-Content packages/core/src/telemetry/signalFinder/runner.ts | Select-Object -Skip 402 -First 40; Get-Content apps/mobile/src/ui/screens/SettingsScreen.tsx | Select-Object -Skip 460 -First 22; Get-Content apps/mobile/src/session/composition.ts | Select-Object -Skip 2724 -First 21; rg -n 'reads.*again|connect|VIN' docs/legal/privacy-policy.en.md | Select-Object -First 15; Get-Content packages/core/src/telemetry/enet/enetChannelSpecs.ts | Select-Object -Skip 85 -First 22" in D:\CODE\APLICTIE_Circuit
 succeeded in 86ms:
      // resets `pendingReplace`, but a driver who stops mid-run and then just
      // taps Confirm again (never a fresh find) used to keep whatever was
      // armed from before the stop â€” a tap the screen shows as a plain
      // confirm could silently commit as a REPLACE instead.
      if (snapshot.pendingReplace !== null) emit({ pendingReplace: null });
    },

    getSamples(): readonly SignalFinderSample[] {
      return samples;
    },

    async ruledOutDidCount(targetId): Promise<number> {
      const target = findSignalTarget(catalog, targetId);
      if (target === null) return 0;
      return (await readRuledOutKeys(target)).size;
    },

    async clearRuledOut(targetId): Promise<void> {
      const store = deps.ruledOutStore;
      if (store === undefined) return;
      const target = findSignalTarget(catalog, targetId);
      if (target === null) return;
      try {
        await store.clearRuledOut(profileId, target.id);
      } catch (error) {
        console.warn('[signalFinderController] could not clear the ruled-out DIDs', error);
        return;
      }
      // The pools of a find already in progress are not rebuilt here (that
      // would change what the CURRENT script is reading mid-run); the next
      // find picks the restored DIDs up, which is what the control promises.
      if (currentTarget?.id === target.id || snapshot.targetId === target.id || snapshot.targetId === null) {
        ruledOutKeys = new Set();
        emit({ ruledOutCount: 0 });
      }
    },

    async confirmBinding(channel, score): Promise<VehicleProfileBinding | null> {
      if (deps.bindingStore === undefined) return null;
      /**
       * Ticket P4o O3 (binding, field test 8): "when a target already has a
       * field-confirmed binding on a different (ecu,did), the confirm button
       * reads 'Replace 0x58B7 â†’ 0x4002' and requires a second tap (inline
       * confirm) ... the export lists the replaced binding in a `replaced`
       * entry." Field bug this closes: a plain re-confirm used to overwrite
       * the engine-running-confirmed 0x58B7 with the generic profile's
       * two-level 0x4002 silently â€” the monitor's brake pressure became
       * 0/100 % with no warning anywhere.
       *
       * The state machine lives HERE (not in the screen) so it is testable
       * without rendering anything: a FIRST call for a differing binding
       * arms {@link SignalFinderSnapshot.pendingReplace} and writes nothing;
       * only a SECOND call naming the SAME (channel, ecu, did, byteOffset)
       * actually commits the replacement.
       */
      const byteOffset = score.byteOffset ?? null;
      const existing = await deps.bindingStore.getBinding(profileId, channel);
      const isReplace =
        existing !== null &&
        existing.status === 'field-confirmed' &&
        (existing.ecu !== score.ecu || existing.did !== score.did);
      if (isReplace) {
        const pending = snapshot.pendingReplace;
        const armed =
          pending !== null &&
          pending.channel === channel &&
          pending.ecu === score.ecu &&
          pending.did === score.did &&
          pending.byteOffset === byteOffset;
        if (!armed) {
          emit({ pendingReplace: { channel, ecu: score.ecu, did: score.did, byteOffset, existing } });
          return null;
        }
      }
      const target = findSignalTarget(catalog, channel);
      const hypothesis = target?.hypotheses.find((h) => h.ecu === score.ecu && h.did === score.did) ?? null;
      const binding: VehicleProfileBinding = {
        profileId,
        channel,
        ecu: score.ecu,
        did: score.did,
        length: score.length,
        // The hypothesis' own decode note when the winner IS one of them,
        // else the honest observation: which byte offset moved, between which
        // raw levels.
        decode:
          hypothesis?.decode ??
          `${score.byteOffset === null ? 'whole response' : `byte ${score.byteOffset}`}: ${score.restValueHex ?? '?'} at rest, ${score.min ?? '?'}..${score.max ?? '?'} observed`,
        status: 'field-confirmed',
        evidenceJson: JSON.stringify({
          sessionId: snapshot.sessionId,
          verdict: score.verdict,
          sparse: score.sparse ?? false,
          matchedEdges: score.matchedEdges,
          windowMatchedEdges: score.windowMatchedEdges ?? null,
          expectedEdges: score.expectedEdges,
          baselineChanges: score.baselineChanges,
          responseBaselineChanges: score.responseBaselineChanges,
          sampleCount: score.sampleCount,
          byteOffset: score.byteOffset,
export async function runFinderRound(input: RunFinderRoundInput): Promise<FinderRoundResult> {
  const requestTimeoutMs =
    Number.isFinite(input.requestTimeoutMs) && (input.requestTimeoutMs ?? 0) > 0
      ? (input.requestTimeoutMs as number)
      : FINDER_REQUEST_TIMEOUT_MS;
  const missesBeforeBackoff =
    Number.isFinite(input.missesBeforeBackoff) && (input.missesBeforeBackoff ?? 0) > 0
      ? Math.floor(input.missesBeforeBackoff as number)
      : FINDER_MISSES_BEFORE_BACKOFF;
  const keepAliveIntervalMs =
    Number.isFinite(input.keepAliveIntervalMs) && (input.keepAliveIntervalMs ?? 0) > 0
      ? (input.keepAliveIntervalMs as number)
      : FINDER_KEEPALIVE_INTERVAL_MS;

  const windowMs =
    Number.isFinite(input.windowMs) && (input.windowMs ?? 0) > 0 ? (input.windowMs as number) : FINDER_BACKOFF_WINDOW_MS;
  const completePass = input.completePass === true;
  /**
   * Y8: one pass over `entries` must take at least `entries / targetReqPerSec`
   * seconds. Without it an in-memory/loopback channel that answers inside one
   * tick turns the round into a hot loop (the LEAD's own E2E observation),
   * over-polling the adapter far past the rate the budget was sized from.
   *
   * P4m-FIX3 Z3 (Codex P4m-REV3 finding 9, HIGH): the budget is PER PASS, and
   * so is the pacing. Build 8 gave each ENTRY a slot of `1000 / rate` ms and
   * advanced the schedule with `max(nextSlot, now) + interval`, so every slow
   * exchange pushed the schedule permanently forward: a pass paid
   * `Î£ max(exchange, interval)` instead of `max(Î£ exchange, Î£ interval)`, and
   * the six live entries behind two 300 ms timeouts lost a whole sample per
   * window. Now a pass runs at whatever speed the adapter allows and only the
   * REMAINDER of `entries Ã— 1000 / rate` is slept off at the end of it â€” a
   * timeout inside the pass is absorbed by that remainder instead of being
   * added to it, and nothing is ever carried into the next pass.
   */
  const passIntervalMs =
    Number.isFinite(input.targetReqPerSec) && (input.targetReqPerSec ?? 0) > 0
      ? (input.entries.length * 1_000) / (input.targetReqPerSec as number)
      : 0;

  const startedAtMs = input.clock.now();
    setConfirmingDelete(false);
  }

  async function confirmDelete(): Promise<void> {
    setDeleting(true);
    try {
      const result = await deleteAllStoredUserData();
      setDeleteBanner(result.ok ? 'success' : 'error');
      setDeleteErrorText(result.ok ? null : result.errorText);
      setDeleteRefused(!result.ok && result.reason !== undefined);
    } catch {
      setDeleteBanner('error');
      setDeleteErrorText(null);
      setDeleteRefused(false);
    } finally {
      setDeleting(false);
      setConfirmingDelete(false);
    }
  }

  return (
    <SafeAreaView style={styles.safe} edges={['top', 'left', 'right']}>

/** Leaves Test Loop mode entirely (screen dismissed without a learned track). */
export async function resetTestLoop(): Promise<void> {
  await stopTestLoop();
  testLoopController.reset();
}

/** P5d-FIX1 H3: retries a handover that failed, on the SAME learned geometry. */
export function retryTestLoopAdoption(): void {
  testLoopController.retryAdopt();
}

/** Stops and clears the buffering provider, handing the GNSS watcher back to the plain singleton. */
async function disposeTestLoopProvider(): Promise<void> {
  const provider = testLoopProvider;
  testLoopProvider = null;
  testLoopHandoverActive = false;
  if (provider === null) return;
  try {
    await provider.stop();
  } catch (error) {
22:is, how fast it is going, and — if you connect an OBD-II adapter — what your car's own sensors
77:Board's *Guidelines 01/2020 on processing personal data in the context of connected vehicles and
87:| When | Only if you turn the "Telemetry" setting on **and** connect an adapter. It is off by default |
89:| Legal basis | **Consent** (Art. 6(1)(a)), given by enabling the setting and connecting the adapter |
90:| Sent anywhere | **No.** The connection is to an adapter on your own local Wi-Fi, not to the internet |
96:### 3.3 The vehicle identification number (VIN)
100:| What | Your car's 17-character VIN, read once per app run from the engine control unit (ISO 14229-1 data identifier `0xF190`) when a BMW-ENET-type adapter is connected |
107:**A VIN is personal data.** It identifies a specific vehicle, and through registration records a
108:specific keeper — the EDPB's connected-vehicles guidelines say so directly. TRACE handles it as
113:- Where the app writes the VIN to a developer log, it is **masked** — first three and last two
117:- "Delete all my data" (§7) removes the stored VIN, and with it a vehicle profile the app selected
118:  from the VIN. The app reads the VIN again the next time a BMW-ENET-type adapter is connected.
152:> responses from your car's control units. If you sweep a range that happens to include the VIN
154:> — may contain your VIN. Check a sweep export before sending it to anyone.
203:Exported reports and analyses **do not contain your VIN**. A raw session export contains your full
  targetAddress?: number;
  /** did only: how to decode the ReadDataByIdentifier response's data bytes. */
  decode?: EnetChannelDecodeSpec;
  /**
   * Ticket P4l-FIX1 F1 (binding): a decode rule that is NOT expressible as
   * `decode`'s byte-slice x scale + offset -- today, exactly the brake rules
   * a confirmed Signal Finder binding carries (`brakeSwitch`: "any byte other
   * than the recorded rest byte reads pressed"; `brakePct`: the finder's
   * observed min..max mapped onto 0..100). Returns `null` when THIS response
   * cannot be decoded (e.g. it is shorter than the binding's own byte offset,
   * because the ECU changed what it answers) -- the session then records a
   * channel error instead of ever emitting a fabricated reading.
   *
   * Takes precedence over `decode` when both are present. Can only ever be
   * set programmatically: specs parsed from the user's settings JSON come
   * from `JSON.parse`, which cannot produce a function.
   */
  decodeValue?: (dataBytes: Uint8Array) => number | null;
  /** REQUIRED for did specs: where the DID/decode formula came from (no public DID table exists -- every did spec is a user-supplied empirical finding). */
  provenance: string;
}


exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "@'
const fs=require('fs'),ts=require('typescript'),vm=require('vm');
const defaults={lastSeenVin:null,activeVehicleProfileId:'generic',activeVehicleProfileSource:'default',language:'en',enetChannelSpecsJson:''};
const modules={};
function load(file){if(modules[file])return modules[file];const m={exports:{}}; const req=(id)=>{if(id.endsWith('settingsStore'))return {DEFAULT_SETTINGS:defaults,defaultLanguageForLocale:()=> 'en',readDeviceLocale:()=> 'en'};if(id.endsWith('enetSettingsValidation'))return {repairPersistedEnetSettings:s=>s};if(id.endsWith('sessionVehicleSnapshot'))return {SESSION_VEHICLE_SNAPSHOT_KEY_PREFIX:'vehicle-profile-snapshot:'};if(id.endsWith('sqlSettingsStore'))return load('apps/mobile/src/persistence/sqlSettingsStore.ts');return require(id);};vm.runInThisContext('(function(require,module,exports){'+ts.transpileModule(fs.readFileSync(file,'utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2020}}).outputText+'\\n})',{filename:file})(req,m,m.exports);return modules[file]=m.exports;}
(async()=>{const SQL=await require('sql.js')();const raw=new SQL.Database();const wipe=load('apps/mobile/src/persistence/deviceDataWipe.ts');const {SqlSettingsStore}=load('apps/mobile/src/persistence/sqlSettingsStore.ts');const {gateSqlTransactions,createSqlWriteGate}=load('apps/mobile/src/persistence/sqlWriteGate.ts');for(const t of [...wipe.DEVICE_USER_DATA_TABLES,'learned_circuits']) raw.run('CREATE TABLE '+t+' (circuit_id TEXT)');raw.run('CREATE TABLE sessions(circuitId TEXT NOT NULL); CREATE TABLE settings(key TEXT PRIMARY KEY,value TEXT)');let inject=null,failReset=false;const handle={runAsync:async(sql,p=[])=>{if(failReset&&sql.startsWith('INSERT OR REPLACE INTO settings')&&JSON.parse(p[1]).lastSeenVin===null)throw Error('injected reset write failure');raw.run(sql,p);return {changes:raw.getRowsModified()};},getAllAsync:async(sql,p=[])=>{if(inject&&sql.includes('COUNT(*)')){const f=inject;inject=null;f();}const st=raw.prepare(sql);st.bind(p);const rows=[];while(st.step())rows.push(st.getAsObject());st.free();return rows;},withTransactionAsync:async(fn)=>{raw.run('BEGIN');try{await fn(handle);raw.run('COMMIT');}catch(e){raw.run('ROLLBACK');throw e;}}};const db=gateSqlTransactions(handle,createSqlWriteGate());await db.runAsync('INSERT INTO settings VALUES (?,?)',['app-settings',JSON.stringify({...defaults,lastSeenVin:'WZ1DB0C04LW000001',enetChannelSpecsJson:JSON.stringify([{provenance:'measured on VIN WZ1DB0C04LW000001'}])})]);const store=await SqlSettingsStore.create(db);inject=()=>store.update({units:'mph'});const result=await wipe.wipeDeviceUserData(db);failReset=true;store.update(wipe.vehicleIdentityResetPatch(store.getSettings()));const disk=JSON.parse((await db.getAllAsync('SELECT value FROM settings WHERE key = ?',['app-settings']))[0].value);console.log(JSON.stringify({wipeResult:result,memoryVin:store.getSettings().lastSeenVin,diskVin:disk.lastSeenVin,retainedCustomSpecs:disk.enetChannelSpecsJson}));failReset=false;store.update(wipe.vehicleIdentityResetPatch(store.getSettings()));await db.getAllAsync('SELECT 1');store.update({lastSeenVin:'WZ1DB0C04LW000001'});console.log('late VIN completion disk:',(await db.getAllAsync('SELECT value FROM settings WHERE key = ?',['app-settings']))[0].value);})();
'@ | node" in D:\CODE\APLICTIE_Circuit
 succeeded in 229ms:
{"wipeResult":{"ok":true,"remaining":{}},"memoryVin":null,"diskVin":"WZ1DB0C04LW000001","retainedCustomSpecs":"[{\"provenance\":\"measured on VIN WZ1DB0C04LW000001\"}]"}
late VIN completion disk: {"lastSeenVin":"WZ1DB0C04LW000001","activeVehicleProfileId":"generic","activeVehicleProfileSource":"default","language":"en","enetChannelSpecsJson":"[{\"provenance\":\"measured on VIN WZ1DB0C04LW000001\"}]","developerModeEnabled":false,"suggestionsEnabled":false,"imuFusionEnabled":false,"analysisSmoothingEnabled":false,"units":"mph"}

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "@'
const fs=require('fs'),ts=require('typescript'),Module=require('module');require.extensions['.ts']=(m,f)=>m._compile(ts.transpileModule(fs.readFileSync(f,'utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2020}}).outputText,f);
const {runFinderRound}=require('./packages/core/src/telemetry/signalFinder/runner.ts');
async function check(name,{frozen=false,paused=false,durationMs=120,stopAfter,slow=false}={}){const start=Date.now();const control={stopped:false,paused};let sends=0;const channel={send:async()=>{sends++;},nextResponse:async()=> 'timeout',keepAlive:async()=>{}};let timer;if(stopAfter)timer=setTimeout(()=>control.stopped=true,stopAfter);const result=await runFinderRound({entries:[{ecu:18,did:16386}],channels:new Map([[18,channel]]),clock:{now:()=>frozen?0:Math.floor((Date.now()-start)*(slow?.98:1))},control,durationMs,windowMs:50,requestTimeoutMs:2,missesBeforeBackoff:1});clearTimeout(timer);console.log(JSON.stringify({name,wallMs:Date.now()-start,elapsedMs:result.elapsedMs,sends,stopped:control.stopped}));}
(async()=>{await check('slow clock',{slow:true});await check('duration before boundary',{durationMs:20});await check('stop mid sleep',{stopAfter:15});await check('paused input',{paused:true});await check('frozen clock stopped by external watchdog',{frozen:true,stopAfter:160});})();
'@ | node; rg -n 'onBlur|validateEnetChannelSpecsJson|parse' apps/mobile/src/session/enetSettingsValidation.ts; git diff dd84864..edb7bd5 --name-only; git status --porcelain -uno" in D:\CODE\APLICTIE_Circuit
 succeeded in 802ms:
{"name":"slow clock","wallMs":131,"elapsedMs":128,"sends":3,"stopped":false}
{"name":"duration before boundary","wallMs":31,"elapsedMs":31,"sends":1,"stopped":false}
{"name":"stop mid sleep","wallMs":62,"elapsedMs":62,"sends":1,"stopped":true}
{"name":"paused input","wallMs":126,"elapsedMs":126,"sends":3,"stopped":false}
{"name":"frozen clock stopped by external watchdog","wallMs":205,"elapsedMs":0,"sends":1,"stopped":true}
34: * `parsePortDraft`/`parseHexPidDraft`). Accepts an optional leading `0x`/`0X`
38:export function parseHexByteDraft(text: string): number | null {
41:  const value = Number.parseInt(compact, 16);
46:/** `true` for any value that is a byte (0-255) -- used both by UI draft parsing above (through `parseHexByteDraft`) and by `repairPersistedEnetSettings` below to check an already-hydrated (not necessarily string) persisted value. */
86: * H1(b): structurally validates ONE array member from the parsed JSON --
161: * defaults" -- see `resolveEnetChannelSpecs`). A non-empty draft must parse as
172: * silently running on a half-parsed list the user never actually asked for).
180:export function validateEnetChannelSpecsJson(text: string): EnetChannelSpecsJsonValidation {
184:  let parsed: unknown;
186:    parsed = JSON.parse(trimmed);
190:  if (!Array.isArray(parsed)) {
196:  parsed.forEach((entry: unknown, index: number) => {
231:    const result = validateEnetChannelSpecsJson(channelSpecsJson);
271: * not a string, or a string that fails `validateEnetChannelSpecsJson`
291:    typeof enetChannelSpecsJsonRaw === 'string' && validateEnetChannelSpecsJson(enetChannelSpecsJsonRaw).ok
350: * way the Settings screen's own draft validation does (`validateEnetChannelSpecsJson`,
359:  const parsedExisting = validateEnetChannelSpecsJson(existingJson);
360:  const baseSpecs = parsedExisting.ok ? parsedExisting.specs : [];
.foreman/scratch/ticket-p18-rev1-codex.md
apps/mobile/src/persistence/deviceDataWipe.ts
apps/mobile/src/persistence/sessionVehicleSnapshot.ts
apps/mobile/src/persistence/sqlSettingsStore.ts
apps/mobile/src/session/composition.ts
apps/mobile/test/persistence/deviceDataWipe.test.ts
apps/mobile/test/session/composition.circuitSelection.test.ts
apps/mobile/test/session/composition.facadeBoundary.test.ts
apps/mobile/test/session/composition.gForceOwnership.test.ts
apps/mobile/test/session/composition.telemetryRecording.test.ts
docs/HANDOFF-2026-09-23-cloud.md
docs/HANDOFF.md
docs/legal/compliance-checklist.md
docs/legal/privacy-policy.en.md
docs/legal/privacy-policy.ro.md
packages/core/src/telemetry/signalFinder/runner.ts
packages/core/test/telemetry/signalFinder/runner.test.ts
 M hardware/kicad/trace-dongle/trace-dongle.kicad_prl
 M hardware/kicad/trace-dongle/trace-dongle.kicad_pro

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "Get-Content apps/mobile/src/session/enetSettingsValidation.ts | Select-Object -Skip 90 -First 65; Get-Content apps/mobile/src/session/circuitCatalog.ts | Select-Object -Skip 160 -First 23; Get-Content packages/core/src/persistence-sql/sqlSessionRepository.ts | Select-Object -Skip 594 -First 96; rg -n 'createSignalFinderController|clock:|Date.now|performance' apps/mobile/src/ui/screens/SignalFinderScreen.tsx; rg -n 'migrateDidSweepSchema|wipe|remaining|missing' apps/mobile/test/persistence/deviceDataWipe.test.ts; rg -n 'VIN|read again|citește din nou|Păstrează|What it keeps' docs/legal/privacy-policy.ro.md docs/legal/privacy-policy.en.md | Select-Object -Last 15" in D:\CODE\APLICTIE_Circuit
 succeeded in 44ms:
40:  createSignalFinderController,
181:   * whatever `performance.now()` happened to read at the LAST unrelated
213:      // from (`platform/clock.ts`'s `PerformanceNowClock`) — never `Date.now()`.
214:      nowMs: performance.now(),
257:  const controllerRef = React.useRef<ReturnType<typeof createSignalFinderController> | null>(null);
259:  function ensureController(): ReturnType<typeof createSignalFinderController> {
263:    const controller = createSignalFinderController({
268:              monotonicNow: () => Date.now(),
276:      clock: { now: () => Date.now() },
4:import { migrateDidSweepSchema } from '../../src/persistence/didSweepSchema';
9:  wipeDeviceUserData,
25:  await migrateDidSweepSchema(db);
106:describe('wipeDeviceUserData', () => {
111:    const result = await wipeDeviceUserData(db);
113:    expect(result).toEqual({ ok: true, remaining: {} });
129:    await wipeDeviceUserData(db);
147:    const result = await wipeDeviceUserData(db);
150:    expect(result.remaining).toEqual({ learned_circuits: 1 });
158:    expect(await wipeDeviceUserData(db)).toEqual({ ok: true, remaining: {} });
166:    expect(await wipeDeviceUserData(db)).toEqual({ ok: true, remaining: {} });
170:  it('rolls back and rejects when a table is missing, so the caller reports failure', async () => {
175:    await expect(wipeDeviceUserData(db)).rejects.toThrow(/vehicle_profile_bindings/);
docs/legal/privacy-policy.en.md:154:> — may contain your VIN. Check a sweep export before sending it to anyone.
docs/legal/privacy-policy.en.md:203:Exported reports and analyses **do not contain your VIN**. A raw session export contains your full
docs/legal/privacy-policy.en.md:257:It also removes the stored VIN, a vehicle profile the app selected from the VIN, confirmed
docs/legal/privacy-policy.en.md:261:What it keeps are your **preferences**: units, language, adapter address and port, coaching, voice
docs/legal/privacy-policy.ro.md:103:### 3.3 Seria de identificare a vehiculului (VIN)
docs/legal/privacy-policy.ro.md:107:| Ce | VIN-ul de 17 caractere al mașinii tale, citit o singură dată pe rulare a aplicației din calculatorul de motor (identificatorul de date `0xF190` din ISO 14229-1), atunci când este conectat un adaptor de tip BMW ENET |
docs/legal/privacy-policy.ro.md:114:**VIN-ul este dată cu caracter personal.** Identifică un vehicul anume și, prin evidențele de
docs/legal/privacy-policy.ro.md:120:- Acolo unde aplicația scrie VIN-ul într-un jurnal tehnic, acesta este **mascat** — doar primele trei
docs/legal/privacy-policy.ro.md:124:- „Delete all my data" (§7) șterge VIN-ul stocat și, odată cu el, un profil de vehicul pe care
docs/legal/privacy-policy.ro.md:125:  aplicația l-a ales pe baza VIN-ului. Aplicația citește din nou VIN-ul data viitoare când este
docs/legal/privacy-policy.ro.md:161:> identificatorul de date al VIN-ului, hexazecimalul brut din acea înregistrare — și din orice export
docs/legal/privacy-policy.ro.md:162:> al ei pe care alegi să îl trimiți — poate conține VIN-ul tău. Verifică un export de sweep înainte
docs/legal/privacy-policy.ro.md:215:Rapoartele și analizele exportate **nu conțin VIN-ul tău**. Un export brut de sesiune conține traseul
docs/legal/privacy-policy.ro.md:271:Șterge de asemenea VIN-ul stocat, un profil de vehicul ales de aplicație pe baza VIN-ului,
docs/legal/privacy-policy.ro.md:276:Păstrează doar **preferințele** tale: unitățile de măsură, limba, adresa și portul adaptorului,

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "@'
const fs=require('fs'),ts=require('typescript');require.extensions['.ts']=(m,f)=>m._compile(ts.transpileModule(fs.readFileSync(f,'utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2020}}).outputText,f);
const {validateEnetChannelSpecsJson}=require('./apps/mobile/src/session/enetSettingsValidation.ts');
const text=JSON.stringify([{channel:'transOilC',mode:'did',requestHex:'22 40 02',decode:{byteOffset:0,byteLength:1,scale:1,offset:-40},provenance:'Measured on VIN WZ1DB0C04LW000001'}]);console.log(JSON.stringify(validateEnetChannelSpecsJson(text)));
'@ | node; git show dd84864:packages/core/src/persistence-sql/schema.ts | Select-String -Pattern 'circuitId TEXT|ALTER TABLE'; rg -n 'schema|refresh|fallback' apps/mobile/test/session/composition.circuitSelection.test.ts | Select-Object -Last 15; rg -n 'enetChannelSpecsJson|update' apps/mobile/src/ui/screens/DidSweepScreen.tsx | Select-Object -Last 15; Get-Content apps/mobile/src/session/composition.ts | Select-Object -Skip 2480 -First 105" in D:\CODE\APLICTIE_Circuit
 succeeded in 795ms:
D:\CODE\APLICTIE_Circuit\apps\mobile\src\session\tmrProfile.ts:36
        throw new Error(`Bundled Transilvania Motor Ring profile failed validation: ${result.errors.join(', ')}`);
        ^

Error: Bundled Transilvania Motor Ring profile failed validation: INVALID_JSON
    at load (D:\CODE\APLICTIE_Circuit\apps\mobile\src\session\tmrProfile.ts:36:15)
    at Object.<anonymous> (D:\CODE\APLICTIE_Circuit\apps\mobile\src\session\tmrProfile.ts:43:16)
    at Module._compile (node:internal/modules/cjs/loader:1871:14)
    at require.extensions..ts ([stdin]:1:84)
    at Module.load (node:internal/modules/cjs/loader:1594:32)
    at Module._load (node:internal/modules/cjs/loader:1396:12)
    at wrapModuleLoad (node:internal/modules/cjs/loader:255:19)
    at Module.require (node:internal/modules/cjs/loader:1617:12)
    at require (node:internal/modules/helpers:153:16)
    at Object.<anonymous> (D:\CODE\APLICTIE_Circuit\apps\mobile\src\session\settingsStore.ts:8:22)

Node.js v24.18.0

  circuitId TEXT NOT NULL,
  circuitId TEXT NOT NULL,
  'ALTER TABLE sessions ADD COLUMN calibrationStatus TEXT',
  'ALTER TABLE sessions ADD COLUMN traceUnwritten INTEGER',
  'ALTER TABLE sessions ADD COLUMN traceFailedWrites INTEGER',
export const SQL_ALTERS_V4: readonly string[] = ['ALTER TABLE checkpoints ADD COLUMN lapCount INTEGER'];
export const SQL_ALTERS_V6: readonly string[] = ['ALTER TABLE sessions ADD COLUMN traceFinalized INTEGER'];
261:  it("M1 fix (ticket CN-FIX2): two rapid selectCircuit calls apply IN ORDER -- the LAST call wins for both settings and the history store, even when the FIRST call's history refresh resolves slower", async () => {
288:        // Simulate the FIRST call's (MotorPark) history refresh being slow.
109: * ("Tag as <channel>"), writing the resulting spec into `enetChannelSpecsJson`.
143:    if (settingsStore.getSettings().enetTargetAddress !== ecu) settingsStore.update({ enetTargetAddress: ecu });
500:    const merged = mergeEnetChannelSpecJson(settingsStore.getSettings().enetChannelSpecsJson, spec);
501:    settingsStore.update({ enetChannelSpecsJson: merged });
620:              lastDid < rangeEnd) -- never the most recently updated run
 */

/** The journal identity of the adoption currently in flight. */
let adoptionJournalId: string | null = null;
/** P5d-FIX5 H1: the exact journal row this adoption last wrote -- what it is allowed to clear. */
let adoptionJournalOwned: ClaimedAdoptionJournal | null = null;

async function stageAdoptionJournal(journal: {
  circuitId: string;
  stage: AdoptionStage;
  sessionId?: string;
}): Promise<void> {
  if (db === null) return;
  adoptionJournalId ??= newJournalId();
  // P5d-FIX5 H1: the journal key holds ONE adoption. If another flow (a second
  // launch's repair claim, a newer adoption) has taken it since this one last
  // wrote, it is not ours to overwrite -- this adoption simply proceeds without
  // a journal rather than destroying somebody else's.
  const current = await readAdoptionJournal(db);
  if (current !== null && current.id !== adoptionJournalId) {
    adoptionJournalOwned = null;
    console.warn(
      '[composition] the adoption journal key is held by another flow -- continuing without journalling this adoption',
    );
    return;
  }
  adoptionJournalOwned = await writeAdoptionJournal(db, {
    id: adoptionJournalId,
    attempts: 0,
    circuitId: journal.circuitId,
    stage: journal.stage,
    ...(journal.sessionId === undefined ? {} : { sessionId: journal.sessionId }),
  });
}

/**
 * Clears the journal of a COMPLETED adoption -- and ONLY the row this adoption
 * itself wrote (P5d-FIX5 H1). A blind delete by key would take out whatever
 * journal occupies it by then: a second launch's repair claim, or a newer
 * adoption already in flight. If the row has moved on, it belongs to somebody
 * else and is left exactly where it is.
 */
async function clearOwnAdoptionJournal(): Promise<void> {
  const owned = adoptionJournalOwned;
  adoptionJournalId = null;
  adoptionJournalOwned = null;
  if (db === null || owned === null) return;
  const cleared = await clearClaimedJournal(db, owned);
  if (!cleared) {
    console.warn(
      '[composition] the adoption journal changed while an adoption was finishing -- leaving it to its owner',
    );
  }
}

/**
 * Bootstrap repair: finish a half-adopted Test Loop, or undo it.
 *
 * P5d-FIX4 G1: the journal is CLAIMED first (a compare-and-swap on its own
 * UUID), so of several launches racing over the same database exactly one
 * repairs and the others do nothing. The journal is cleared only once the
 * repair has fully succeeded, and only if it is still the row this launch
 * claimed -- a slow launch can never wipe a newer adoption's journal. A repair
 * that fails leaves the journal in place with its attempt counted; after
 * `MAX_ADOPTION_REPAIR_ATTEMPTS` the app stops trying and says so.
 *
 *  - the learned geometry IS on disk -> the irreversible half succeeded, so the
 *    adoption is COMPLETED here (registered by the caller, selected below);
 *  - the geometry is NOT there -> the orphans are DELETED outright (P5d-FIX4
 *    G3): the half-written circuit row, and the session that was started for
 *    it, with its checkpoint, laps and telemetry, in one transaction.
 *
 * Runs before the production controller and the history store are built, so
 * the selection it settles is the one they are built for. Never throws.
 */
async function repairInterruptedAdoption(database: SqlDatabase): Promise<void> {
  const claim = await claimAdoptionJournal(database, newJournalId());
  if (claim === null) return;
  const journal = claim.journal;

  if (claim.exhausted) {
    // Three launches have now failed to repair this. Stop retrying, say so,
    // and let the driver deal with a device that keeps refusing writes.
    setRecoveryNotice(
      'A test loop could not be repaired after several attempts and has been abandoned.',
    );
    await clearClaimedJournal(database, claim);
    return;
  }

  try {
    const entry = learnedCircuitStore?.get(journal.circuitId) ?? null;
    if (entry !== null) {
      applySelectedCircuit({
        profile: entry.profile,
        runtime: entry.runtime,
        corners: entry.corners,
      });
      setRecoveryNotice(
        `The test loop "${entry.profile.displayName}" was kept, but the session it started was interrupted.`,
      );
    } else {
      // P5d-FIX5 M2: the session and its pointer rows go together, in one
      // transaction -- there is no window where the pointer names a session
      // that has already been deleted.



exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "@'
const fs=require('fs'),ts=require('typescript');require.extensions['.ts']=(m,f)=>m._compile(ts.transpileModule(fs.readFileSync(f,'utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2020,esModuleInterop:true}}).outputText,f);
const {validateEnetChannelSpecsJson}=require('./apps/mobile/src/session/enetSettingsValidation.ts');
const text=JSON.stringify([{channel:'transOilC',mode:'did',requestHex:'22 40 02',decode:{byteOffset:0,byteLength:1,scale:1,offset:-40},provenance:'Measured on VIN WZ1DB0C04LW000001'}]);console.log(JSON.stringify(validateEnetChannelSpecsJson(text)));
'@ | node; Get-Content apps/mobile/src/ui/screens/DidSweepScreen.tsx | Select-Object -Skip 479 -First 25; Get-Content apps/mobile/src/ui/screens/TestLoopScreen.tsx | Select-String -Pattern 'resetTestLoop|useEffect|beforeRemove' -Context 3,5; Get-Content apps/mobile/src/persistence/deviceDataWipe.ts | Select-Object -First 50" in D:\CODE\APLICTIE_Circuit
 succeeded in 775ms:
{"ok":true,"specs":[],"warnings":["Ignoring ENET channel spec for transOilC: did requestHex must be exactly 4 hex characters (one DID), got 6"],"error":null}
      }
      const result = await shareDidSweepExport(doc);
      setShareBanner(
        result.shared
          ? 'Shared.'
          : `Export ready (${result.jsonLength} bytes) -- sharing isn't available on this platform; see the console log.`,
      );
    } finally {
      setSharing(false);
    }
  }

  function confirmTag(did: number, channel: TelemetryChannelId): void {
    const controller = controllerRef.current;
    if (controller === null) return;
    const spec = controller.buildTaggedSpec(did, channel, new Date().toISOString().slice(0, 10));
    if (spec === null) {
      setTagBanner(`Could not build a spec for ${formatHexDid(did)}.`);
      return;
    }
    const merged = mergeEnetChannelSpecJson(settingsStore.getSettings().enetChannelSpecsJson, spec);
    settingsStore.update({ enetChannelSpecsJson: merged });
    setTagBanner(`Tagged ${formatHexDid(did)} as ${channel}.`);
    setTagPickerDid(null);
  }

> import React, { useEffect, useState } from 'react';
  import { ActivityIndicator, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native';
  import { SafeAreaView } from 'react-native-safe-area-context';
  import type { NativeStackScreenProps } from '@react-navigation/native-stack';
  import type { RootStackParamList } from '../navigation/types';
  import { colors, fontFamily, radii, spacing, typography } from '../theme';
  import {
>   resetTestLoop,
    retryTestLoopAdoption,
    saveLearnedCircuit,
    settingsStore,
    startTestLoop,
    stopTestLoop,
    const [name, setName] = useState('');
    const [notice, setNotice] = useState<string | null>(null);
  
>   useEffect(() => subscribeTestLoop(setSnapshot), []);
  
    /**
>    * Ticket D3 (second one-liner): `resetTestLoop()` had no callers anywhere in
     * the repo, so `TestLoopController`'s terminal snapshot outlived the screen
     * -- re-entering Test Loop after a learn phase that gave up showed the
     * failure card from the PREVIOUS attempt, as though this visit had already
     * failed before the driver pressed anything.
     *
     * (`retryTestLoopAdoption`), `'learning'`/`'adopting'` are live, and
     * `'learned'` is a running session this screen is still the face of.
     */
>   useEffect(() => {
      if (testLoopSnapshot().phase !== 'failed') return;
>     void resetTestLoop().catch((error: unknown) => {
        console.warn('[TestLoopScreen] clearing the previous learn phase failed', error);
      });
    }, []);
  
    const onStart = (): void => {
import type { SqlDatabase } from '@circuit/core';

import { DEFAULT_SETTINGS, type AppSettings } from '../session/settingsStore';
import { SESSION_VEHICLE_SNAPSHOT_KEY_PREFIX } from './sessionVehicleSnapshot';
import { SETTINGS_KEY } from './sqlSettingsStore';

/**
 * The half of "Delete all my data" that `@circuit/core`'s
 * `deleteUserData` cannot see: everything the mobile app stores about the
 * driver's CAR and the driver's own CIRCUITS, outside the session tables.
 *
 * A VIN is personal data in the EU, and so is what hangs off it -- the DID
 * bindings and sweep results are fingerprints of one specific vehicle, and a
 * learned circuit is a trace of where the driver drove. Preferences (units,
 * language, adapter address, coaching toggles, a profile the USER picked) are
 * not data about the driver and survive the wipe.
 */

/** Mobile-owned tables whose every row is vehicle- or driver-specific. */
export const DEVICE_USER_DATA_TABLES = [
  'vehicle_profile_bindings',
  'signal_finder_ruled_out',
  'did_sweep_runs',
  'did_sweep_responders',
  'did_sweep_observation_samples',
  'did_sweep_observation_summaries',
] as const;

export interface DeviceDataWipeResult {
  ok: boolean;
  /** Rows still present per table (and per settings concern) after the wipe; empty when `ok`. */
  remaining: Record<string, number>;
}

/**
 * The settings patch that forgets the vehicle's identity. The VIN goes; a
 * profile the VIN auto-selected goes with it, because keeping it would keep
 * the make and model the VIN resolved to. A profile the user chose is a
 * preference and stays.
 */
export function vehicleIdentityResetPatch(settings: AppSettings): Partial<AppSettings> {
  if (settings.activeVehicleProfileSource !== 'vin') return { lastSeenVin: null };
  return {
    lastSeenVin: null,
    activeVehicleProfileId: DEFAULT_SETTINGS.activeVehicleProfileId,
    activeVehicleProfileSource: DEFAULT_SETTINGS.activeVehicleProfileSource,
  };
}

function parseSettingsRow(raw: string | undefined): Partial<AppSettings> | null {



exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "@'
const fs=require('fs'),ts=require('typescript');require.extensions['.ts']=(m,f)=>m._compile(ts.transpileModule(fs.readFileSync(f,'utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2020,esModuleInterop:true}}).outputText,f);
const {validateEnetChannelSpecsJson}=require('./apps/mobile/src/session/enetSettingsValidation.ts');console.log(JSON.stringify(validateEnetChannelSpecsJson(JSON.stringify([{channel:'transOilC',mode:'did',requestHex:'4002',decode:{byteOffset:0,byteLength:1,scale:1,offset:-40},provenance:'Measured on VIN WZ1DB0C04LW000001'}]))));
'@ | node; rg -n 'withTransaction|gateSql' apps/mobile/test/session/composition.circuitSelection.test.ts apps/mobile/test/support/sqlJsDatabase.ts; Get-Content apps/mobile/src/ui/screens/SignalFinderScreen.tsx | Select-String -Pattern 'maybeDetectVehicleFromVin|lastSeenVin' -Context 2,4; git log --all --oneline -- packages/core/src/persistence-sql/schema.ts | Select-Object -Last 5" in D:\CODE\APLICTIE_Circuit
 succeeded in 796ms:
{"ok":true,"specs":[{"channel":"transOilC","mode":"did","requestHex":"4002","decode":{"byteOffset":0,"byteLength":1,"scale":1,"offset":-40},"provenance":"Measured on VIN WZ1DB0C04LW000001"}],"warnings":[],"error":null}
apps/mobile/test/support/sqlJsDatabase.ts:20:  // `openAppDatabase()` wraps its `SqlDatabase` in `gateSqlTransactions(...,
apps/mobile/test/support/sqlJsDatabase.ts:57:    // gate sits ABOVE this wrapper (`gateSqlTransactions`), so a callback
apps/mobile/test/support/sqlJsDatabase.ts:60:    withTransactionAsync(fn: (tx: SqlDatabase) => Promise<void>): Promise<void> {

    dismissVinAutoDetectNotice,
    getTelemetryReadDb,
>   maybeDetectVehicleFromVin,
    refreshVehicleProfileBindingsCache,
    setActiveVehicleProfileIdExplicit,
    settingsStore,
    subscribeVinAutoDetectNotice,
     * "Detected from VIN â€” <label>" banner auto-select raises. The one-shot
     * read itself is triggered here too ("when the Signal Finder screen
>    * opens") -- `maybeDetectVehicleFromVin` is idempotent per app run and
     * never blocks/steals the adapter (see its own doc comment).
     */
    const [vinNotice, setVinNotice] = React.useState<VinAutoDetectNotice | null>(null);
    React.useEffect(() => {
      const unsubscribe = subscribeVinAutoDetectNotice(setVinNotice);
>     void maybeDetectVehicleFromVin();
      return unsubscribe;
    }, []);
    const vinNoticeProfileLabel =
      vinNotice === null ? '' : resolveSignalTargetCatalogLabel(resolveSignalTargetCatalog(vinNotice.profileId), 
settings.language);
                has found one -- persisted, so it still shows on a later app
                run even before that run has read one itself. */}
>           {settings.lastSeenVin !== null ? <Text 
style={styles.caption}>{strings.vinLabel(settings.lastSeenVin)}</Text> : null}
            <Pressable
              style={[styles.secondaryButton, sharing || bindings.length === 0 ? styles.buttonDisabled : null]}
              disabled={sharing || bindings.length === 0}
              onPress={() => void handleShareProfile()}
c3e018a P13A: the data layer for a data-collection build
039db39 P12: nothing deletes a lap boundary any more, and a retried commit cannot rewind a checkpoint
21f601b P10A: a durable session record, retryable trace writes, and provenance that cannot vanish
4f39338 WP14: full integration — SessionController, real facade, recovery, watchdog, SQL settings v2, static TMR bundle proof, offline audit, eas.json (484 tests)
d813aeb WP11b: SQL session repository + expo-sqlite binding; shared contract suite proves parity with in-memory reference (64 tests)



codex
FAIL

Reviewed `dd84864..edb7bd5`. Local HEAD is `6d6f90b`, containing both merges. `git pull` was not run because this environment prohibits filesystem writes. No files changed; no agents used.

**HIGH findings**

1. **A — VIN can return after successful verification.** [composition.ts:3806](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3806), [composition.ts:1038](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:1038), [sqlSettingsStore.ts:190](D:/CODE/APLICTIE_Circuit/apps/mobile/src/persistence/sqlSettingsStore.ts:190).

   Two concrete paths:
   - Start VIN detection, then delete-all. The wipe and memory reset finish before the pending read/transport close completes. Detection subsequently assigns `cachedDetectedVin`, persists `lastSeenVin`, and invokes VIN auto-selection. Detection takes no lifecycle lock and has no deletion-generation check.
   - An `update()` during the wipe serializes the old VIN into a whole-blob write queued behind the transaction. That write restores the VIN after verification. Ordinarily, the subsequent reset write clears it again; however, that write is unawaited and failures are swallowed. If it fails, delete-all can return success with memory cleared and disk containing the VIN.

   **Evidence:** A read-only Node/sql.js reproduction using the wipe, settings-store and production FIFO-gate implementations returned `{"ok":true,"remaining":{}}`, with `memoryVin:null` and `diskVin:"WZ1DB0C04LW000001"` after an injected reset-write failure. A later VIN completion also restored the persisted value. The transaction prevents interleaving *inside* verification, not writes afterwards.

2. **A — Custom vehicle signal definitions, including VIN-bearing provenance, survive.** [deviceDataWipe.ts:75](D:/CODE/APLICTIE_Circuit/apps/mobile/src/persistence/deviceDataWipe.ts:75), [DidSweepScreen.tsx:500](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/DidSweepScreen.tsx:500).

   “Tag as channel” stores a sweep-derived DID/decode definition in `app-settings.enetChannelSpecsJson`. The wipe preserves this field verbatim while removing the equivalent vehicle-specific bindings from `vehicle_profile_bindings`.

   Concrete scenario: tag a DID for the current car, then delete-all. Its request, decode and provenance remain available for subsequent telemetry. Additionally, the Settings editor accepts provenance containing the full VIN.

   **Evidence:** The production validator accepted a `transOilC` DID `4002` definition with provenance `Measured on VIN WZ1DB0C04LW000001`, with no warnings. The wipe’s spread-and-patch preserves that string. Thus §7’s retained-“preferences” description omits retained diagnostic findings, and the VIN-erasure promise is too broad.

3. **A — A failed adoption can resume using a deleted circuit’s progress against the fallback controller.** [composition.ts:3625](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3625), [composition.ts:2819](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:2819).

   Concrete sequence:
   1. Adoption stores and selects the learned circuit.
   2. `ledger.selected = true` executes, but the following `stageAdoptionJournal()` fails before session start.
   3. Delete-all is permitted because the production controller is idle. It deletes the learned circuit and selects TMR.
   4. Retry adoption. `adoptionProgress.storedProfile` and `.selected` survived, so retry skips storage and selection and starts the **current TMR controller**, while continuing bookkeeping and out-lap persistence for the old learned circuit.

   **Evidence:** Retry skips completed steps at lines 2781 and 2810, then uses `controller` at 2822. The reset does not clear `adoptionProgress`, the Test Loop controller’s pending geometry, provider/handover state, or adoption journal. The lifecycle lock prevents simultaneous execution, but does not invalidate stale work between attempts. This can recreate deleted driving data and associate it with inconsistent circuit geometry.

**MEDIUM findings**

4. **A — Successful disk deletion can leave learned geometry published in memory.** [composition.ts:3632](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3632), [learnedCircuitStore.ts:66](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/learnedCircuitStore.ts:66).

   Scenario: the wipe commits successfully, but the following learned-circuit refresh rejects. `refresh()` replaces its cache only after a successful read. The catch retains the old cache, and `publishLearnedCircuits()` republishes it. The selected deleted circuit consequently still resolves, so fallback is skipped.

   **Evidence:** This failure does not change `deviceDataOk`; delete-all can report success while the deleted geometry remains selectable and usable until a later refresh/restart.

5. **A — VIN detection remains disabled until restart, contrary to the changed policy.** [composition.ts:3627](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3627), [composition.ts:999](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:999).

   Scenario: complete VIN detection, delete-all, then reconnect ENET or reopen Signal Finder. `cachedDetectedVin` is null, but `vinDetectionState` remains `'done'`. Detection immediately returns null; VIN auto-selection does not run again.

   **Evidence:** English policy §3.3, line 118, and Romanian §3.3, lines 125–126, promise another read on the next connection. Restart is actually required. Any reset of this state must also invalidate pending attempts to avoid finding 1.

**LOW finding**

6. **A — Session-linked settings are not uniformly erased or verified.** [deviceDataWipe.ts:101](D:/CODE/APLICTIE_Circuit/apps/mobile/src/persistence/deviceDataWipe.ts:101), [composition.ts:3819](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3819).

   `testLoopAdoption` always survives this wipe, retaining circuit/session identifiers and adoption state. `unvalidated-matching-sessions` is removed only best-effort: a failed delete logs a warning while overall success remains true.

   These are records about activity, not merely preferences. The journal is also relevant to finding 3. Eventual startup repair does not establish deletion at the time success is reported.

**Persistence inventory**

The source search included `rg -a`: `didSweepStore.ts` contains a NUL byte, so ordinary `rg` otherwise reports only a binary-file match.

| Tables written | Identifying/activity content | Delete-all treatment |
|---|---|---|
| `sessions`, `laps`, `checkpoints`, `telemetry`, `reference_laps`, `lap_verdicts`, `calibration_attempts` | Session identity, driving traces, results and assessments | Core per-user deletion; additional orphan sweeps for checkpoints, telemetry, verdicts and calibration attempts |
| `telemetry_samples` | Vehicle/motion samples linked to sessions | Separate delete and count |
| `vehicle_profile_bindings`, `signal_finder_ruled_out` | Vehicle signal findings and supporting evidence | Deleted and counted |
| `did_sweep_runs`, `did_sweep_responders`, `did_sweep_observation_samples`, `did_sweep_observation_summaries` | Diagnostic activity and raw responses, potentially VIN-bearing | Deleted and counted |
| `learned_circuits` | Driver-created location geometry | Deleted unless referenced by surviving sessions; all remaining rows counted |
| `settings` | Mixed preferences, vehicle identity and session metadata | Partial deletion; exceptions above |
| `schema_migrations`, `telemetry_schema_migrations`, `did_sweep_schema_migrations`, `learned_circuit_schema_migrations` | Schema versions | Retained; no driver/vehicle payload |

Every written `settings` key/pattern:

- `app-settings`: partially scrubbed.
- `vehicle-profile-snapshot:<sessionId>`: deleted and counted.
- `activeSessionId`, `activeSessionCircuitId`, `activeSessionStartedAtUtc`: cleared after aggregate success.
- `unvalidated-matching-sessions`: best-effort removal after success.
- `testLoopAdoption`: retained.

Every declared persisted `app-settings` field:

- Identity/selection: `lastSeenVin`, `activeVehicleProfileId`, `activeVehicleProfileSource`, `selectedCircuitId`.
- Vehicle/adapter configuration: `adapterType`, `adapterHost`, `adapterPort`, `transOilPidHex`, `enetHost`, `enetPort`, `enetTesterAddress`, `enetTargetAddress`, `enetChannelSpecsJson`, `enetHostProvenance`, `enetAutoDiscover`.
- Other preferences: `units`, `deltaDeadbandMs`, `coverageBins`, `coachingEnabled`, `voiceCoachEnabled`, `suggestionsEnabled`, `telemetryEnabled`, `telemetrySimulate`, `developerModeEnabled`, `language`, `imuFusionEnabled`, `imuGyroCaptureEnabled`, `analysisSmoothingEnabled`.

Only `lastSeenVin` is unconditionally reset. Profile ID/source reset only for source `'vin'`; selection can change through fallback. Everything else survives. Besides finding 2, `transOilPidHex` retains vehicle-specific configuration, and a user-selected profile deliberately retains make/model information. Neither alone establishes a unique vehicle identity.

**Clean list / bounded observations**

- **A — NULL poisoning:** Supported schema creation declares `sessions.circuitId TEXT NOT NULL`; migrations do not relax it. No supported migration path producing nullable circuit IDs was found. Even an externally malformed nullable schema would leave learned rows counted and make the wipe fail, rather than falsely pass.
- **A — Transaction rollback:** Missing-table rejection rolls back this device-data transaction and sets `deviceDataOk = false`. Clearing memory afterwards does **not**, by itself, convert that failure into success. `SettingsScreen.tsx:467` displays an error for either `ok:false` or a rejection.
- **A — Production migrations:** Native `openAppDatabase()` awaits DID-sweep migration before returning; first launch is covered. Web uses `db === null` and skips SQL wiping. Learned-circuit migration failure is tolerated at bootstrap, so a missing learned table can still make delete-all fail—but that failure is reported. Adding the four test migrations matches the normal native path.
- **A — Test limitation:** The sql.js test adapter serializes transactions, but its standalone statements do not implement the production all-operation FIFO gate. Existing happy-path composition coverage does not establish safety against queued settings writes.
- **A — Selection lock:** Calling `unlockedApplySelection()` while holding `lifecycleLock` avoids recursive-lock deadlock.
- **A — Catalog ordering:** The per-circuit verification loop runs **before** the new registry refresh/publication. `list()` contains bundled circuits and listed/saved learned circuits, excluding unlisted learned circuits. Core deletion itself is per-user, not limited to that list.
- **A — Verification scope:** Counts can detect retained rows, including retained learned circuits. Settings verification checks only `lastSeenVin`; it does not verify VIN-selected profile removal. The malformed-settings branch deletes without rereading. None of these checks establishes immunity to later writes.
- **A — Bindings subscription:** A profile reset triggers an asynchronous bindings refresh. On the normal successful SQL path it reads after the wipe through the gate; no independent durable write-back defect was found in that subscription.
- **B — Early wake:** With an advancing clock, the loop retries correctly. A past `wakeAtMs` skips sleeping; `endAtMs < boundaryMs` ends at the duration target; stop is observed after the outstanding timer resolves and prevents further polling.
- **B — Delay and overshoot:** Each iteration awaits a real `setTimeout`, so this is not a synchronous busy-spin. The 1 ms minimum requests a delay; it does not guarantee exact elapsed time. Timer scheduling can overshoot the deadline.
- **B — Pause/frozen clocks:** `paused` is ignored throughout `runFinderRound`, pre-existing behavior. A permanently frozen clock now waits indefinitely in this cooldown branch unless stopped; advancing timers alone cannot finish it. Production supplies `Date.now()`, so backward wall-clock adjustments can extend the wait. The change supplies no independent elapsed-time bound.
- **B — Regression test:** The 2%-slow clock exercises the intended early-wake condition. It is not deterministic proof: sufficiently late timer delivery can cross the boundary even without the patch.
- No additional executable behavior changes were found in the small constant exports or remaining diff.

Validation used read-only Node transpilation, sql.js fault injection, and direct runner probes. Full repository gates and device tests were not run.
tokens used
103,522
FAIL

Reviewed `dd84864..edb7bd5`. Local HEAD is `6d6f90b`, containing both merges. `git pull` was not run because this environment prohibits filesystem writes. No files changed; no agents used.

**HIGH findings**

1. **A — VIN can return after successful verification.** [composition.ts:3806](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3806), [composition.ts:1038](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:1038), [sqlSettingsStore.ts:190](D:/CODE/APLICTIE_Circuit/apps/mobile/src/persistence/sqlSettingsStore.ts:190).

   Two concrete paths:
   - Start VIN detection, then delete-all. The wipe and memory reset finish before the pending read/transport close completes. Detection subsequently assigns `cachedDetectedVin`, persists `lastSeenVin`, and invokes VIN auto-selection. Detection takes no lifecycle lock and has no deletion-generation check.
   - An `update()` during the wipe serializes the old VIN into a whole-blob write queued behind the transaction. That write restores the VIN after verification. Ordinarily, the subsequent reset write clears it again; however, that write is unawaited and failures are swallowed. If it fails, delete-all can return success with memory cleared and disk containing the VIN.

   **Evidence:** A read-only Node/sql.js reproduction using the wipe, settings-store and production FIFO-gate implementations returned `{"ok":true,"remaining":{}}`, with `memoryVin:null` and `diskVin:"WZ1DB0C04LW000001"` after an injected reset-write failure. A later VIN completion also restored the persisted value. The transaction prevents interleaving *inside* verification, not writes afterwards.

2. **A — Custom vehicle signal definitions, including VIN-bearing provenance, survive.** [deviceDataWipe.ts:75](D:/CODE/APLICTIE_Circuit/apps/mobile/src/persistence/deviceDataWipe.ts:75), [DidSweepScreen.tsx:500](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/DidSweepScreen.tsx:500).

   “Tag as channel” stores a sweep-derived DID/decode definition in `app-settings.enetChannelSpecsJson`. The wipe preserves this field verbatim while removing the equivalent vehicle-specific bindings from `vehicle_profile_bindings`.

   Concrete scenario: tag a DID for the current car, then delete-all. Its request, decode and provenance remain available for subsequent telemetry. Additionally, the Settings editor accepts provenance containing the full VIN.

   **Evidence:** The production validator accepted a `transOilC` DID `4002` definition with provenance `Measured on VIN WZ1DB0C04LW000001`, with no warnings. The wipe’s spread-and-patch preserves that string. Thus §7’s retained-“preferences” description omits retained diagnostic findings, and the VIN-erasure promise is too broad.

3. **A — A failed adoption can resume using a deleted circuit’s progress against the fallback controller.** [composition.ts:3625](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3625), [composition.ts:2819](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:2819).

   Concrete sequence:
   1. Adoption stores and selects the learned circuit.
   2. `ledger.selected = true` executes, but the following `stageAdoptionJournal()` fails before session start.
   3. Delete-all is permitted because the production controller is idle. It deletes the learned circuit and selects TMR.
   4. Retry adoption. `adoptionProgress.storedProfile` and `.selected` survived, so retry skips storage and selection and starts the **current TMR controller**, while continuing bookkeeping and out-lap persistence for the old learned circuit.

   **Evidence:** Retry skips completed steps at lines 2781 and 2810, then uses `controller` at 2822. The reset does not clear `adoptionProgress`, the Test Loop controller’s pending geometry, provider/handover state, or adoption journal. The lifecycle lock prevents simultaneous execution, but does not invalidate stale work between attempts. This can recreate deleted driving data and associate it with inconsistent circuit geometry.

**MEDIUM findings**

4. **A — Successful disk deletion can leave learned geometry published in memory.** [composition.ts:3632](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3632), [learnedCircuitStore.ts:66](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/learnedCircuitStore.ts:66).

   Scenario: the wipe commits successfully, but the following learned-circuit refresh rejects. `refresh()` replaces its cache only after a successful read. The catch retains the old cache, and `publishLearnedCircuits()` republishes it. The selected deleted circuit consequently still resolves, so fallback is skipped.

   **Evidence:** This failure does not change `deviceDataOk`; delete-all can report success while the deleted geometry remains selectable and usable until a later refresh/restart.

5. **A — VIN detection remains disabled until restart, contrary to the changed policy.** [composition.ts:3627](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3627), [composition.ts:999](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:999).

   Scenario: complete VIN detection, delete-all, then reconnect ENET or reopen Signal Finder. `cachedDetectedVin` is null, but `vinDetectionState` remains `'done'`. Detection immediately returns null; VIN auto-selection does not run again.

   **Evidence:** English policy §3.3, line 118, and Romanian §3.3, lines 125–126, promise another read on the next connection. Restart is actually required. Any reset of this state must also invalidate pending attempts to avoid finding 1.

**LOW finding**

6. **A — Session-linked settings are not uniformly erased or verified.** [deviceDataWipe.ts:101](D:/CODE/APLICTIE_Circuit/apps/mobile/src/persistence/deviceDataWipe.ts:101), [composition.ts:3819](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3819).

   `testLoopAdoption` always survives this wipe, retaining circuit/session identifiers and adoption state. `unvalidated-matching-sessions` is removed only best-effort: a failed delete logs a warning while overall success remains true.

   These are records about activity, not merely preferences. The journal is also relevant to finding 3. Eventual startup repair does not establish deletion at the time success is reported.

**Persistence inventory**

The source search included `rg -a`: `didSweepStore.ts` contains a NUL byte, so ordinary `rg` otherwise reports only a binary-file match.

| Tables written | Identifying/activity content | Delete-all treatment |
|---|---|---|
| `sessions`, `laps`, `checkpoints`, `telemetry`, `reference_laps`, `lap_verdicts`, `calibration_attempts` | Session identity, driving traces, results and assessments | Core per-user deletion; additional orphan sweeps for checkpoints, telemetry, verdicts and calibration attempts |
| `telemetry_samples` | Vehicle/motion samples linked to sessions | Separate delete and count |
| `vehicle_profile_bindings`, `signal_finder_ruled_out` | Vehicle signal findings and supporting evidence | Deleted and counted |
| `did_sweep_runs`, `did_sweep_responders`, `did_sweep_observation_samples`, `did_sweep_observation_summaries` | Diagnostic activity and raw responses, potentially VIN-bearing | Deleted and counted |
| `learned_circuits` | Driver-created location geometry | Deleted unless referenced by surviving sessions; all remaining rows counted |
| `settings` | Mixed preferences, vehicle identity and session metadata | Partial deletion; exceptions above |
| `schema_migrations`, `telemetry_schema_migrations`, `did_sweep_schema_migrations`, `learned_circuit_schema_migrations` | Schema versions | Retained; no driver/vehicle payload |

Every written `settings` key/pattern:

- `app-settings`: partially scrubbed.
- `vehicle-profile-snapshot:<sessionId>`: deleted and counted.
- `activeSessionId`, `activeSessionCircuitId`, `activeSessionStartedAtUtc`: cleared after aggregate success.
- `unvalidated-matching-sessions`: best-effort removal after success.
- `testLoopAdoption`: retained.

Every declared persisted `app-settings` field:

- Identity/selection: `lastSeenVin`, `activeVehicleProfileId`, `activeVehicleProfileSource`, `selectedCircuitId`.
- Vehicle/adapter configuration: `adapterType`, `adapterHost`, `adapterPort`, `transOilPidHex`, `enetHost`, `enetPort`, `enetTesterAddress`, `enetTargetAddress`, `enetChannelSpecsJson`, `enetHostProvenance`, `enetAutoDiscover`.
- Other preferences: `units`, `deltaDeadbandMs`, `coverageBins`, `coachingEnabled`, `voiceCoachEnabled`, `suggestionsEnabled`, `telemetryEnabled`, `telemetrySimulate`, `developerModeEnabled`, `language`, `imuFusionEnabled`, `imuGyroCaptureEnabled`, `analysisSmoothingEnabled`.

Only `lastSeenVin` is unconditionally reset. Profile ID/source reset only for source `'vin'`; selection can change through fallback. Everything else survives. Besides finding 2, `transOilPidHex` retains vehicle-specific configuration, and a user-selected profile deliberately retains make/model information. Neither alone establishes a unique vehicle identity.

**Clean list / bounded observations**

- **A — NULL poisoning:** Supported schema creation declares `sessions.circuitId TEXT NOT NULL`; migrations do not relax it. No supported migration path producing nullable circuit IDs was found. Even an externally malformed nullable schema would leave learned rows counted and make the wipe fail, rather than falsely pass.
- **A — Transaction rollback:** Missing-table rejection rolls back this device-data transaction and sets `deviceDataOk = false`. Clearing memory afterwards does **not**, by itself, convert that failure into success. `SettingsScreen.tsx:467` displays an error for either `ok:false` or a rejection.
- **A — Production migrations:** Native `openAppDatabase()` awaits DID-sweep migration before returning; first launch is covered. Web uses `db === null` and skips SQL wiping. Learned-circuit migration failure is tolerated at bootstrap, so a missing learned table can still make delete-all fail—but that failure is reported. Adding the four test migrations matches the normal native path.
- **A — Test limitation:** The sql.js test adapter serializes transactions, but its standalone statements do not implement the production all-operation FIFO gate. Existing happy-path composition coverage does not establish safety against queued settings writes.
- **A — Selection lock:** Calling `unlockedApplySelection()` while holding `lifecycleLock` avoids recursive-lock deadlock.
- **A — Catalog ordering:** The per-circuit verification loop runs **before** the new registry refresh/publication. `list()` contains bundled circuits and listed/saved learned circuits, excluding unlisted learned circuits. Core deletion itself is per-user, not limited to that list.
- **A — Verification scope:** Counts can detect retained rows, including retained learned circuits. Settings verification checks only `lastSeenVin`; it does not verify VIN-selected profile removal. The malformed-settings branch deletes without rereading. None of these checks establishes immunity to later writes.
- **A — Bindings subscription:** A profile reset triggers an asynchronous bindings refresh. On the normal successful SQL path it reads after the wipe through the gate; no independent durable write-back defect was found in that subscription.
- **B — Early wake:** With an advancing clock, the loop retries correctly. A past `wakeAtMs` skips sleeping; `endAtMs < boundaryMs` ends at the duration target; stop is observed after the outstanding timer resolves and prevents further polling.
- **B — Delay and overshoot:** Each iteration awaits a real `setTimeout`, so this is not a synchronous busy-spin. The 1 ms minimum requests a delay; it does not guarantee exact elapsed time. Timer scheduling can overshoot the deadline.
- **B — Pause/frozen clocks:** `paused` is ignored throughout `runFinderRound`, pre-existing behavior. A permanently frozen clock now waits indefinitely in this cooldown branch unless stopped; advancing timers alone cannot finish it. Production supplies `Date.now()`, so backward wall-clock adjustments can extend the wait. The change supplies no independent elapsed-time bound.
- **B — Regression test:** The 2%-slow clock exercises the intended early-wake condition. It is not deterministic proof: sufficiently late timer delivery can cross the boundary even without the patch.
- No additional executable behavior changes were found in the small constant exports or remaining diff.

Validation used read-only Node transpilation, sql.js fault injection, and direct runner probes. Full repository gates and device tests were not run.
