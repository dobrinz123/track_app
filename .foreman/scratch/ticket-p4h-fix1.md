# Ticket P4h-FIX1 — fix wave for Codex P4h-REV1 (6 HIGH + 4 MEDIUM), tests first
Source of truth: `.foreman/scratch/p4hrev1-codex-output.log` (sections HIGH/MEDIUM/Test gaps — read it fully). HEAD 0009720.
Another worker is editing DID-sweep files (didSweepController, DidSweepScreen, persistence/did*, packages/core/src/telemetry/enet/didCandidates*, package.json) at the same time — DO NOT touch those; if `npm test` shows failures only in those files, report them as "foreign" and continue.
Required fixes (each with a failing test first):
H1 ActiveCalibrationScreen `beforeRemove`: allow-once ref set by confirmCancelExit before re-dispatching the intercepted action; test: confirm → navigation proceeds, no re-intercept loop.
H2+H3 Preflight distance guard: ONE proximity watcher, started only after permission is granted (sequenced with the existing collector, not concurrent), rejection handled (no unhandled rejection on web/no-GNSS), stopped on Continue/Continue-anyway navigation AND on unmount (never runs during calibration). Never silently pass: with no fix the card shows "Distance to circuit unknown" and Continue stays disabled until a fix arrives (Continue-anyway remains available). Keep accuracy/age of the fix (stale > 30 s or accuracy > 200 m → unknown).
H4 Remove the process-global accel PID source from `pidCodec.ts` (`setAccelPedalPidSource/getAccelPedalPidSource`): the decoder takes the PID as a parameter and `elm327Session` receives it in its config, frozen for the session. DevReplay/ENET paths unaffected. Test: two sessions with different PIDs decode independently.
H5 telemetryProvider: lifecycle generation counter; public `stop()` bumps it; the fallback relaunch continuation runs only if its captured generation is current AND no stop was requested; concurrent `doStop()` calls share one promise (no latch overwrite). Test: Stop during fallback teardown → provider stays stopped, no relaunch.
H6 G-provider ownership reference-counted in composition (`acquireGForce()/releaseGForce()`; monitor and driving session each hold one; stop only at zero). Test: monitor Start → session start/end → G still running for the monitor; monitor Stop → stops.
M1 ENET pedal fallback: detect `accelPedalPct` unsupported from the unsupported-channel event/first poll cycle, not only inside a later sample callback; provider-level ENET test (simulated transport with 0x5A NRC).
M2 Calibration cancel vs async start: the controller's `rejectCalibration()` (or a `cancelCalibrationStart`) must cancel an in-flight `beginCalibration()` under the same lifecycleLock (startup finishes → tears down, ends idle, no orphan watchdog/provider). Test at controller level.
M3 pedalNormalization: offset ≥ 95 → invalid (keep raw, diagnostics `49-raw`); non-finite inputs never emitted (test FF at rest).
M4 Learning at rest uses speed < 1 km/h (not === 0); if no offset learned when the window expires, diagnostics/monitor label say "49 raw", not "normalized". Tests.
Gates (real exit codes): `npm run typecheck`, `npm test`, `npm run lint`, `cd apps/mobile && npx expo export --platform ios` all 0.
WRITE SET: apps/mobile/src/ui/screens/{ActiveCalibrationScreen,CalibrationInstructionsScreen,PreflightScreen,TelemetryScreen}.tsx, apps/mobile/src/session/{telemetryProvider.ts,composition.ts,pedalNormalization.ts,circuitProximity.ts}, packages/core/src/telemetry/{pidCodec.ts,elm327Session.ts}, packages/core/src/controller/sessionController.ts, matching tests. No commit, no agents, leave the Expo server alone. Report DONE/DONE_WITH_CONCERNS/BLOCKED with per-finding evidence and totals.
