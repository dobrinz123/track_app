TASK: Phase 4b work package B — recorded-telemetry read API + LapDetail bucket charts, exactly per the "Telemetry addendum — P4b amendments (binding)" section at the END of docs/architecture/contracts.md (read it first).

EXPECTED OUTCOME:
(1) apps/mobile/src/persistence/telemetryRead.ts (new): readLapTelemetry(db, sessionId, lapNumber) -> rows { tMonoMs, channel, value } ordered by t_mono_ms (parameterized SQL over telemetry_samples; lapNumber is the 1-based lap as stored — NULL-lap rows are NOT part of any lap and never returned), plus a pure function bucketTelemetry(rows, channel, bucketCount = 80): averages value per bucket across the lap's [minT, maxT] span, returns { buckets: Array<number | null>, min, max } with null for empty buckets.
(2) LapDetailScreen.tsx: a "TELEMETRY" section rendered ONLY when readLapTelemetry returns rows for this lap (find how the screen knows its sessionId + lapNumber — study its route params and how it loads existing lap data; if sessionId is genuinely unavailable there, report NEEDS_CONTEXT with what you found instead of guessing). Charts for up to three channels present in the data, in this order: speedKph, rpm, throttlePct. Each chart: View-based bar sparkline (NO svg, NO new deps) — a fixed-height row of thin bars (one per bucket, flex), amber (existing accent token) for speedKph, muted variants for the others, with min/max labels and the channel name in the section's row header, styled consistently with the screen's existing sections. Empty/absent data changes nothing else on the screen. Loading state: render nothing until the read resolves (no spinner).

CONTEXT: Repo D:\CODE\APLICTIE_Circuit. P4a landed the telemetry_samples table (apps/mobile/src/persistence/telemetrySchema.ts — read it for exact column names) and TelemetryScreen.tsx (style reference). composition.ts exposes the shared db via its module state — study how existing screens/read paths get a db handle (e.g. how history/lap detail load their data today) and reuse that pattern; if no clean read-path exists to reach the db from LapDetailScreen, add a minimal exported read function to composition.ts (single function, no refactor). Tests: vitest node env, sql.js via test/support/sqlJsDatabase.ts; UI components stay thin/untested, chart bucket math and the read query get real tests.

CONSTRAINTS: No new dependencies. No packages/core changes. English copy. Surgical diffs.

MUST DO:
1. Tests in apps/mobile/test/persistence/ (new dir ok): readLapTelemetry filters by session AND lap (rows from other sessions/laps and NULL-lap rows excluded; ordering pinned); bucketTelemetry: averaging correctness on a hand-computed vector, empty-bucket null handling, single-sample lap, bucketCount honored, min/max correctness.
2. Gates from repo root with REAL exit codes (cmd > log 2>&1; ec=$?): npm run typecheck, npm test, npm run lint, cd apps/mobile && npx expo export --platform ios. Report all four.
MUST NOT: No subagents. No changes outside WRITE SET. Do not touch telemetryRecorder.ts, telemetrySchema.ts, realFacade.ts, ActiveDashboardScreen, TelemetryStrip, settings, navigation types (the screen already exists in the stack).
WRITE SET: apps/mobile/src/persistence/telemetryRead.ts (new), apps/mobile/src/ui/screens/LapDetailScreen.tsx, apps/mobile/src/session/composition.ts (ONLY if a minimal exported db read accessor is genuinely needed — single function, additive), apps/mobile/test/persistence/**.
OUTPUT FORMAT: First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED. Then: how LapDetail resolves sessionId+lap; files changed; the four gate exit codes + test counts; deviations/concerns.
