# Ticket P4i — DID sweep results: persistence, resume, export, candidate filtering (core + mobile)
Binding: "DID sweep — results persistence, export & candidate filtering addendum" (contracts.md, end). Field context: `.foreman/ledger.md` + `.foreman/scratch/field3/responders.csv` (transcribed from the user's screenshots; may be partial).
Tests first (fail on HEAD), then implement, one wave:
1. CORE (`packages/core/src/telemetry/enet/`): `didCandidates.ts` — `filterSweepCandidates(responders, {maxLen=8, asciiThreshold=0.6})`, `isAsciiLike(bytes)`, `selectChangingCandidates(pairs)` (two samples per DID → changed or plausible-range); `runDidObservation` gains an optional `preFilter` step or the caller passes the filtered set (keep runner unchanged if possible). Tests with the field shapes: 0x4098 ASCII blob excluded, 0x4097 200+ bytes excluded, 0x1002 (6 bytes) kept, changing vs static.
2. MOBILE persistence: SQLite tables `did_sweep_runs` / `did_sweep_responders` in `apps/mobile/src/persistence/` (schema + migration pattern like telemetrySchema.ts; web preview → in-memory fallback); the sweep controller writes incrementally (batched every 2 s or 50 responders) and can `resume(runId)` from `lastDid` with the accumulator restored; keep last 5 runs. Tests at store level + controller resume test.
3. MOBILE export: `npx expo install expo-file-system expo-sharing` (SDK 57 — read the versioned docs per apps/mobile/AGENTS.md); `didSweepExport.ts` builds the JSON (schema in the addendum) and shares it; "Copy summary" via `expo-clipboard` only if already available, else omit. Web preview: export falls back to showing the JSON length + a console log (never throws).
4. MOBILE UI (DidSweepScreen): Resume button when a persisted run exists; responders collapsed with count + expand; candidates section; "Share results"; range presets (Full / Resume / 0x1000–0x1FFF / 0x4000–0x4FFF); the observation phase uses the filtered candidate set and shows "N candidates of M responders". 360pt.
Gates (real exit codes): `npm run typecheck` 0, `npm test` 0, `npm run lint` 0, `cd apps/mobile && npx expo export --platform ios` 0 (confirm new native modules are linked: strings "ExpoFileSystem"/"ExpoSharing" would appear in the native binary only after prebuild — report the export result and the package versions).
WRITE SET: packages/core/src/telemetry/enet/** + tests; apps/mobile/package.json + root package-lock.json (two deps); apps/mobile/src/persistence/**, apps/mobile/src/session/{didSweepController.ts, didSweepExport.ts (new)}, apps/mobile/src/ui/screens/DidSweepScreen.tsx, apps/mobile/test/**. No commit, no agents. Report DONE/DONE_WITH_CONCERNS/BLOCKED with per-item evidence, versions, totals.
