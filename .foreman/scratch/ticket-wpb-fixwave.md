TASK: Fix all 5 findings from the Codex cross-review of P4b (report: .foreman/scratch/p4b-verify-out.log — read it first; commit under review 50f6125). Designs below are BINDING.

BINDING DESIGNS:
H1 dashboard overflow: TelemetryStrip must contribute ZERO normal-flow height. Remove the reserved slot row (and its gap) from ActiveDashboardScreen entirely. Render TelemetryStrip as an absolutely-positioned overlay pinned to the TOP of the delta zone container (the flex-growing region around the delta figure — it has large dead space; study the layout to anchor it there without touching the delta's own centering). The strip renders NOTHING (null) unless visible (this also implements M1). Timing elements' layout must be byte-identical to pre-P4b when the strip is hidden. After the change, reason through the 360x640 @1.3-font-scale minimum-height budget the reviewer computed (their numbers are in the report) and state the new minimum in your report.
M1 visibility: with H1's overlay approach there is no reservation at all — the styled card, border, background, and the "Telemetry not connected" accessibility node exist ONLY when settings.telemetryEnabled AND provider state === 'polling'. Update the viewmodel/test accordingly (isTelemetryStripVisible stays the single source of truth).
M2 stale shutdown across DevReplay: clear the composition-level telemetryShutdown reference at every facade swap-in point (DevReplay/mock installation and restoreProductionFacade) so a stale promise from a previous production session can never feed a later replay's sessionComplete barrier. Keep the existing clear in startTelemetryRecording. Test: simulate prior session leaving a settled telemetryShutdown, install replay facade, replay session completes -> barrier takes the synchronous timer-free path (reuse the existing barrier test harness).
M3 LapDetail read rejection: attach a rejection handler to the readLapTelemetry call — if not cancelled/unmounted, set rows to [] (section hidden) and console.warn once. No retry, no spinner. Extract the load logic into a small pure/testable helper if that is what it takes to pin it with a test; otherwise pin via the helper you extract.
L1 bucketTelemetry guard: bucketCount must be a positive integer -> otherwise throw a TypeError with a clear message. Tests: 0, -1, 2.5 throw; 1 works.

CONTEXT: Repo D:\CODE\APLICTIE_Circuit. Files: apps/mobile/src/ui/screens/ActiveDashboardScreen.tsx, apps/mobile/src/ui/components/TelemetryStrip.tsx, apps/mobile/src/session/{composition.ts,telemetryProvider.ts}, apps/mobile/src/ui/screens/LapDetailScreen.tsx, apps/mobile/src/persistence/telemetryRead.ts. The binding spec is docs/architecture/contracts.md "Telemetry addendum — P4b amendments"; H1's overlay approach SUPERSEDES that section's "fixed slot" phrasing — after implementing, update that one contracts bullet to describe the overlay rule (zero normal-flow height, renders only when enabled AND polling).

MUST DO:
1. Every fix pinned by a test where the house rules allow (viewmodel/composition/persistence logic yes; raw .tsx layout no — for H1 the evidence is the removed slot + your height-budget arithmetic in the report).
2. Gates from repo root with REAL exit codes (cmd > log 2>&1; ec=$?): npm run typecheck, npm test, npm run lint, cd apps/mobile && npx expo export --platform ios. Report all four.
MUST NOT: No subagents. No packages/core changes. No new deps. No changes outside WRITE SET.
WRITE SET: the six files listed in CONTEXT, docs/architecture/contracts.md (that one bullet only), apps/mobile/test/session/**, apps/mobile/test/persistence/**.
OUTPUT FORMAT: First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED. Then per-finding fix + pinning evidence; new 360x640@1.3 height budget; four gate exit codes + test counts; deviations/concerns.
