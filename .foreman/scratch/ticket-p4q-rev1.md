You are a read-only reviewer. Repo: D:\CODE\APLICTIE_Circuit (npm workspaces). If shell exec is blocked, read files directly. Review the latest commit on main ("P4q: VIN-based vehicle auto-detection…") — files: packages/core/src/telemetry/enet/vinRead.ts (+ test), packages/core/src/telemetry/signalFinder/targets.ts (vinPatterns/vinMatchesPattern/matchVehicleProfilesByVin + tests), apps/mobile/src/session/composition.ts (maybeDetectVehicleFromVin, decideVinAutoSelect, setActiveVehicleProfileIdExplicit + composition.vinDetection.test.ts), apps/mobile/src/session/settingsStore.ts + persistence/sqlSettingsStore.ts (lastSeenVin), apps/mobile/src/ui/screens/{SignalFinderScreen.tsx,signalFinderStrings.ts}. Questions: (1) can the one-shot VIN read ever steal or dead-lock the shared adapter reservation, or fire while a sweep/finder/telemetry session is mid-flight; is the transport closed on every path? (2) auto-select precedence — can it override an explicit user choice (this run or persisted), or flip the profile mid-session while bindings are polled? (3) VIN decode — length/charset validation (17 chars? padding, 0xFF/0x00 fills), multi-frame responses; a garbage VIN persisted forever? (4) vinMatchesPattern glob — pathological patterns, case sensitivity; (5) privacy: the VIN is personal data — is it only stored locally and shown, never put into shared exports without the user seeing it? Output: numbered findings (HIGH/MEDIUM/LOW, file:line, one-line fix), then "NEW: n HIGH, m MEDIUM". No edits.
