You are a read-only reviewer. Repo: D:\CODE\APLICTIE_Circuit (npm workspaces). If shell exec is blocked, read files directly. Review the latest commit on main ("P4o: Signal Finder guardrails…") — files: packages/core/src/telemetry/signalFinder/{targets,scoring,plan}.ts (+ tests incl. fieldFixtures.test.ts), apps/mobile/src/session/{signalFinderController,signalFinderExport}.ts (+ tests), apps/mobile/src/ui/screens/{SignalFinderScreen.tsx,signalFinderStrings.ts}. Field context: data/field/signal-finder/2026-08-30-brakePressure.json (0x12/0x58B7 graded 0x1A..0x40 in press windows — must stay `found` for the analog target) and …-brakePressure-2.json (0x12/0x4002 two-level 0x83/0x9B — must be capped 'two-level' for the analog target, but still `found` for the boolean brakeSwitch target). Questions: (1) countDistinctLevels with the noise floor — can a graded 8-bit series with small steps (e.g. 0..12 hPa) collapse to ≤2 levels and be wrongly capped; can a noisy two-level series exceed 2 levels and escape the cap? (2) the two-tap Replace: can the pending replace be applied to a different row after the list re-sorts; is pendingReplace cleared on a new find/stop/profile change; does the export's replaced[] reflect only actual replacements? (3) confirmed DIDs first: are they read even on a different profile whose catalog lacks the ECU, and is the budget still respected? (4) engineNotDetectedRunning: stale rpm sample age; ELM327 vs ENET; false warnings when telemetry is simply not running (should say 'not detected', not 'off')? (5) any regression to the boolean brakeSwitch path (0x500C found, confirm enabled). Output: numbered findings (HIGH/MEDIUM/LOW, file:line, one-line fix), then "NEW: n HIGH, m MEDIUM". No edits.
