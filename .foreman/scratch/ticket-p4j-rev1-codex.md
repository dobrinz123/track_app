# Review ticket P4j-REV1 — Codex read-only cross-review: batched guided observation
Commit 6c38985 (diff vs de8020a). Read-only; do not run tests (the sandbox may block exec — read files directly). Binding: DID-sweep addenda in docs/architecture/contracts.md (read-only whitelist {0x01,0x22,0x3E}, adapter reservation, keep-alive ≤ 2 s, persistence rules, hard bounds). Field context: data/field/sweeps/2026-08-29-supra-dme12-0x4000-0x4FFF.json (128 candidates got 1–2 samples/phase at ~9 req/s).
Review: packages/core/src/telemetry/enet/{didObservationBatches.ts,didObservationPhases.ts,didCandidates.ts}, apps/mobile/src/session/{didSweepController.ts,didSweepExport.ts}, apps/mobile/src/ui/screens/DidSweepScreen.tsx. Focus: (1) batch planning math — does every DID really get ≥ 5 samples per phase at the measured rate, incl. NRC/timeout slow paths; (2) margin-based change rule — false positives from idle noise (values like 297→305→295) and false negatives for 0/1 switches; (3) per-byte block diffing correctness (offset reporting, variable-length responses, ASCII exclusion); (4) lifecycle: stop/pause/unmount mid-batch, keep-alive continuity across batches, socket/reservation release, persistence of batched samples + export batchIndex; (5) focused shortlist input validation (hex parsing, duplicates, > 16 DIDs); (6) regressions in the legacy guided flow; (7) 360pt layout risks. Output: HIGH / MEDIUM / Test gaps / Clean with file:line and concrete failure scenarios.
