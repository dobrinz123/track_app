TASK: Premium pre-generated voice pack for the coach via ElevenLabs, replacing on-device TTS as primary (expo-speech stays as fallback). Two deliverables: (1) a generation script the foreman will run once the user provides their API key, (2) the app-side clip player integrated into voiceCoach with full fallback.

EXPECTED OUTCOME: gates green from root (typecheck, tests, lint, export:ios). The script is runnable-but-unrun (no key in repo); the player works with an EMPTY clip pack today (falls back to expo-speech transparently — current behavior unchanged until clips land).

CONTEXT: Read first: apps/mobile/src/session/voiceCoach.ts (phraseForCue, speaker abstraction, serialization chain, background gating, dedupe — ALL preserved), packages/core/src/corners+coach APIs (corner ids 1..12, kinds BRAKE/CORNER_AHEAD, severities, advisory speeds, units setting), ADR-0004 (offline mandate — the reason for pre-generation), apps/mobile/src/session/tmrProfile.ts (TMR_CORNERS source of truth).

MUST DO:
1. Phrase manifest generator (single source of truth): `apps/mobile/scripts/generate-voice-pack.mjs` — derives the exact phrase list FROM the same corner data the app ships (import the built corner table via a small JSON dump step or re-derive with node from the assets — document approach): for each corner × kind × units(kmh,mph): short fixed phrase, e.g. BRAKE: "Brake for corner {id}. {severity word}, {advisory speed}." CORNER_AHEAD: "Corner {id} ahead, {left|right}, {advisory speed}." Numbers spoken naturally (the TTS handles digits fine). Writes `apps/mobile/assets/voice/manifest.json` (phrase text + expected filename + hash of phrase so regeneration skips unchanged clips).
2. ElevenLabs generation in the same script: reads ELEVENLABS_API_KEY strictly from env (exit with a clear message if absent; NEVER read from a file in-repo, never log it); voice id configurable via --voice flag with a sensible professional-English default; model eleven_turbo_v2_5 (cheap+fast) — verify the current API path/params against docs at runtime failure and document; output mp3_44100_64 (small); writes `apps/mobile/assets/voice/{units}/{cornerId}-{kind}.mp3`; idempotent via manifest hashes (re-run only generates missing/changed); prints total characters consumed.
3. App playback: extend voiceCoach's speaker layer with a ClipSpeaker: static require map for bundled clips (Metro needs static requires — generate `apps/mobile/src/session/voiceClips.gen.ts` from the manifest as part of the script, with a checked-in EMPTY default `voiceClips.gen.ts` exporting {} so the app builds before clips exist); playback via `npx expo install expo-audio` (the ONLY new dep — SDK 57 module, Expo Go compatible; verify against installed types); on clip present → play mp3 (respecting the existing serialization chain: stop current playback before next); on clip missing OR any playback error → transparent fallback to expo-speech with the existing dynamic phrase (which keeps live distance for BRAKE — document the deliberate asymmetry). Background gating + dedupe + toggle stop apply identically to clips (stop() must halt an in-flight clip).
4. Settings: no new toggle (voiceCoachEnabled covers it); Settings voice row subtitle mentions "premium voice pack when installed".
5. Tests: ClipSpeaker selection logic (clip present vs missing → fallback), stop-halts-clip, serialization with mixed clip/TTS utterances (fake audio module via vi.mock('expo-audio')); manifest generator unit-testable pure part (phrase derivation) with a pinned snapshot for corner 5 kmh+mph.
6. Docs: short section in docs/testing-and-replay.md or a new docs/voice-pack.md: how to run the generator (env var, one command), cost estimate, regeneration rules, license note (generated audio usage per user's own ElevenLabs account).

MUST NOT: commit any API key or call ElevenLabs during tests/build; no network in the app runtime (clips are bundled); don't break the existing TTS path; no subagents; no git commit.

OUTPUT FORMAT: status first line; files; the exact command the foreman must run to generate (with env var placeholder); commands + pasted results.

WRITE SET: apps/mobile/scripts/**, apps/mobile/assets/voice/**, apps/mobile/src/session/{voiceCoach.ts,voiceClips.gen.ts}, apps/mobile/src/ui/screens/SettingsScreen.tsx (subtitle only), apps/mobile/package.json, package-lock.json, apps/mobile/test/session/**, docs/voice-pack.md.
