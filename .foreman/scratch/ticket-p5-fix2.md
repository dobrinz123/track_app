# Ticket P5-FIX2 (QUEUED — dispatch after P5c-B lands) — Codex P5-REV findings 12/14/15/16
W1 (H12) analysisViewModel/controller: generation-scoped runner promises — a run superseded by active→inactive (or any state change) is invalidated, never rejoined/published/cached; the next eligible view starts fresh. Tests with a fake facade flipping states mid-run.
W2 (M14) Chunk the FINAL pass too: finishAssembly/analyzeSession yield per lap or per corner (extend the chunked path into the engine call site — the engine itself is pure; chunk by analysing lap-by-lap where its API allows, else slice corners); test that the event loop turns during the final pass on a large synthetic session.
W3 (M15) Export DTO v3: add structured fields — per-lap labels (HEAVY_BRAKING/ABS_SUSPECTED/SLIDE_ROTATION with values), per-lap channel coverage/exclusions — alongside the prose; bump schemaVersion to 3 with a key-snapshot test.
W4 (M16) Expanded corner cards: compact VISUAL detail — a small inline bar/mark row (brake/lift positions per lap relative to the corner, v_min/exit as compact figures), engine prose NOT rendered in-app (export-only), per R2-2. Reuse the existing sparkline/bar patterns in the codebase if any; keep it testable in the view model (marks as data).
Gates: all four, real exit codes. WRITE SET: apps/mobile/src/session/{analysisViewModel,analysisAssembly,analysisExport}.ts, apps/mobile/src/ui/screens/{AnalysisScreen.tsx,analysisStrings.ts}, tests.
