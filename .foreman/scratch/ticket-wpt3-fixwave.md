TASK: Fix ALL 11 findings from the Codex cross-review of the P4a mobile telemetry wiring (verdict FAIL: 2 HIGH, 8 MEDIUM, 1 LOW). The full findings list with file:line references is at /tmp/wpt2-verify.log (read it first — the tail contains the complete final report). The designs below are BINDING — implement them as written, do not redesign.

BINDING DESIGNS (foreman-decided, one per finding):
F1 HIGH nested transactions: TelemetryRecorder must NEVER open its own transaction. Remove withTransactionAsync from the recorder entirely; each flush becomes ONE parameterized multi-row INSERT statement (single statement = atomic in SQLite). ADDITIONALLY, serialize every telemetry flush through the SAME persistence tail chain the controller uses for lap persistence (composition.ts lapPersistenceTail pattern): flushes append to that chain so a telemetry write can never interleave with an open controller transaction. Test (revert-detecting): a fake DB that records BEGIN/COMMIT ordering proves no telemetry write occurs between a controller transaction's BEGIN and COMMIT, and that the recorder issues zero BEGINs.
F2 HIGH fire-and-forget end flush: endSession path must AWAIT the final telemetry flush with error isolation. In composition: stopTelemetryRecording() returns a promise; the session-end flow awaits Promise.allSettled([controllerPersistence..., telemetryFinalFlush]) semantics such that (a) the final buffered batch is durably written before session completion is reported, (b) a REJECTED telemetry flush never breaks session end or lap persistence (allSettled/caught), (c) stopTelemetryRecording runs in a finally so it executes even when controller persistence rejects. Tests: kill-after-end scenario shows last batch persisted; telemetry flush rejection leaves session end + lap persistence fully successful; controller persistence rejection still stops telemetry.
F3 MED provider stop/start race: introduce a per-start generation token; stop() captures ITS generation's unsubscribe handles locally (not from shared fields) and a start() during a pending stop gets a fresh generation whose listeners the old stop cannot detach. State events from a stale generation are dropped. Test: start, stop (unresolved), start again, resolve old stop -> new session still forwards samples and state.
F4 MED lap 0 tagging: in the recorder wiring, map facade lapNumber === 0 to NULL lap_number. Test: samples during pre-lap state store NULL.
F5 MED cap exactness: set capReached whenever rowsWritten >= rowCap at the end of any flush (including exactly-at-cap), and drop (do not buffer) incoming samples once capReached. Test: land a batch exactly on 200000 (use the rowCap test seam) -> capReached true, subsequent samples not buffered.
F6 MED recovery bypass: resumeRecovery() must call the same startTelemetryRecording(sessionId) hook used by normal start (telemetryEnabled gating unchanged). Test: recovery flow with telemetryEnabled starts recording for the recovered session id.
F7 MED delete-all leaves telemetry: the mobile delete-all flow must also DELETE FROM telemetry_samples (all rows) and verify the table is empty afterward, in the same flow that delegates to the core repository. Test: insert rows, run delete-all, assert zero rows.
F8 MED simulate honored in release: telemetryProvider gates simulation on __DEV__: in non-dev builds treat telemetrySimulate as false regardless of persisted value (do NOT mutate the stored setting; just ignore it). Test: with __DEV__ false (mock the global the way existing tests mock RN globals, or inject an isDev flag defaulting to __DEV__ — prefer injected flag for testability, default wired to __DEV__ in composition), simulate=true still builds the TCP transport path (assert via a transport-factory spy).
F9 MED close-during-import race: TcpObdTransport keeps a closed flag/generation; connect() re-checks it AFTER the lazy import resolves and before socket creation — if closed, abort without creating a socket. Test: call connect(), close() while import pending (inject a controllable import), resolve import -> no socket created, connect() rejects cleanly.
F10 MED stale monitor state: provider onStateChange subscription REPLAYS the current state immediately on subscribe (mirroring facade.subscribe semantics). TelemetryScreen unchanged beyond removing any workaround. Test: subscribe after state is 'polling' -> callback fires synchronously with 'polling'.
F11 LOW port field UX: SettingsScreen keeps a local string draft for the port; validate on blur/submit: full-string integer 1-65535 or revert to last valid; reject partial-numeric garbage ("123abc" invalid, not 123).

CONTEXT: Repo D:\CODE\APLICTIE_Circuit, commit 6e83281. Binding spec: docs/architecture/contracts.md "Telemetry addendum" — the rule "telemetry NEVER gates lap timing" is the reason F1/F2 are HIGH; your fixes must preserve it (telemetry failures isolated, telemetry durability awaited but never propagating rejection). Study composition.ts flush/tail discipline before touching it.

MUST DO:
1. Every fix gets a revert-detecting test (the tests named above are mandatory; keep them in the existing test files where natural, new files only if needed).
2. Gates from repo root with REAL exit codes (cmd > log 2>&1; ec=$?): npm run typecheck, npm test, npm run lint, cd apps/mobile && npx expo export --platform ios. Report all four.
3. Keep diffs surgical — no refactors beyond the designs above.
MUST NOT: No subagents. No packages/core/src changes (if F7 seems to need core repository changes, do the deletion mobile-side as designed). No new dependencies. Do not weaken any existing test.
WRITE SET: apps/mobile/src/session/{telemetryProvider.ts,tcpObdTransport.ts,composition.ts}, apps/mobile/src/persistence/{telemetryRecorder.ts,telemetrySchema.ts,expoSqlDatabase.ts}, apps/mobile/src/ui/screens/{TelemetryScreen.tsx,SettingsScreen.tsx}, apps/mobile/test/session/**, apps/mobile/test/persistence/** (new dir allowed).
OUTPUT FORMAT: First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED. Then per-finding: fix summary + test that pins it. Then the four gate exit codes + test counts. Then deviations/concerns.
