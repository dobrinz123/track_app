# Ticket P4o-FIX3 — graded-evidence strength + bipolar excursions (Codex P4o-REV3 1 HIGH + 1 MEDIUM), tests first — Sonnet
Source: .foreman/scratch/p4orev3-codex-output.log (findings 6, 7). HEAD = P4o-FIX2 commit. Keep the window-consistent rule; do not add clustering.
T1 (H6) Evidence strength: `graded` gets a strength — `strong` when at least one press window holds ≥ 2 samples inside its (rest+floor, windowMax−floor) interval OR the intermediate values across the qualifying windows are not all within `floor` of each other (≥ 2 distinct intermediate levels); otherwise `weak` (a single mid-transition sample per window cannot be told apart from a slewing two-state signal). Result row and export: "found (graded)" for strong, "found (graded — weak evidence: 1 intermediate sample per press)" for weak; confirm stays ENABLED for both (the user sees the raw range and can judge), but the export/summary carries `gradedEvidence: 'strong'|'weak'`. Tests: field 0x58B7 → strong (P1 has 1A,40,36,30 → ≥ 2 inside); synthetic single-intermediate-per-window → weak; two-state → two-level cap unchanged; 0x4002 → two-level.
T2 (M7) Bipolar targets (`analog-bipolar`): evaluate excursions in BOTH directions — a window qualifies if it has an intermediate strictly inside (rest+floor, windowMax−floor) OR strictly inside (windowMin+floor, rest−floor), with the amplitude condition on the corresponding side; a steering series with rest in the middle and left/right sweeps is graded; a monotone target is unaffected. Tests for both directions and for a bipolar two-state.
Gates (real exit codes, repo root): `npm run typecheck`, `npm run test`, `npm run lint`, `npx expo export --platform ios` in apps/mobile — all 0. Report totals + red→green per item + changed strings (EN/RO).
WRITE SET: packages/core/src/telemetry/signalFinder/scoring.ts, apps/mobile/src/session/signalFinderExport.ts (one field + summary text), apps/mobile/src/ui/screens/{SignalFinderScreen.tsx,signalFinderStrings.ts} (label only), tests. No commit, no agents, no Expo dev server.
