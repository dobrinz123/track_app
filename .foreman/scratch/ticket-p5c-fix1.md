# Ticket P5c-FIX1 — Codex P5c-REV1 (4 HIGH + 5 MEDIUM + 3 LOW), safety-first, tests first — Opus
Source: .foreman/scratch/p5crev1-codex-output.log (final list 1–12). Dispatch AFTER P5-FIX2 lands (shared files). The safety invariants are the contract: a cue may never move beyond what a clean lap of the SAME outing demonstrated, bounds 10 m / +3 km/h / one change per corner per stint, suggestionsEnabled OFF = zero change, honesty gates block suggestions.
E1 (H1) The async apply path RE-CHECKS at apply time: suggestionsEnabled, same sessionId, same controller generation, and applies only AT a lap boundary (queue until the next boundary if the analysis finishes mid-lap). Tests: toggle OFF mid-analysis → nothing applies; session restart mid-analysis → nothing applies; mid-lap completion → applied at the next boundary.
E2 (H2) `applyCueUpdates` validates against CONTROLLER-OWNED state: the current cue position (fromM must match within ε) and a controller-side clamp to ≤ 10 m and to the demonstrated value passed through a signed evidence structure recomputed, not trusted — reject mismatches (log). Tests incl. a forged/stale update.
E3 (H3) Voiced-as-"Lift" cues (severity 1–4 brake cues): a cue whose voice is "Lift" must be validated against the demonstrated LIFT envelope (earliest lift), not latestBrakeStartM; if the engine cannot distinguish, such cues are EXCLUDED from updates entirely (safe default). Tests.
E4 (H4) Honesty gates: suggestionsFromInsights consumes geometryValidated and the limitations set — GEOMETRY_UNVALIDATED, UNVERIFIED_LAPS, insufficient coverage on the corner's channels → no suggestion and no cue update for the affected corners (geometry → none at all). Tests: MotorPark profile (community-derived) → zero suggestions.
E5 (M5) The lap event is consumed only after its telemetry persistence settles (await/subscribe to the flush) — analysis never reads a lap with missing trace. Test.
E6 (M6) Boundary coalescing: a lap completing during an in-flight pass retains the pending count and runs ONE follow-up pass after settle. Test.
E7 (M7) Session recovery initializes the stint stage (mostRecentSessionId/start metadata) so a recovered outing can compute cues and open the Pit view. Test at composition level.
E8 (M8) The journal records only what was SHOWN (top-3 + cues-moved list), not every generated suggestion. Test.
E9 (M9) Export: the trackday block only when suggestionsEnabled at export time AND the journal is non-empty; the "Observations only" line replaced by an honest header when suggestions are present ("Observations + the suggestions you were shown"). RO/EN. Tests.
E10 (L10) Stint boundaries: detect pit/stationary (reuse the existing stationary detection if present; else speed < 5 km/h for > 60 s inside the pit area or a Pit-view open) → re-arms the one-change-per-corner allowance per STINT; document the chosen detector. Tests.
E11 (L11) Boundary pass cost: reuse the shared analysis runner's cache (incremental: only the new lap loaded/projected; the rest cached) — bound the per-boundary work to O(new lap). Test with a counter on loads.
E12 (L12) Duplicate the key safety/integration tests on a MotorPark fixture (corner-ID alignment + the geometry gate).
Gates (real exit codes, repo root): `npm run typecheck`, `npm run test`, `npm run lint`, `npx expo export --platform ios` in apps/mobile — all 0. Report totals + red→green per item + changed strings (RO/EN).
WRITE SET: packages/core/src/coaching/suggestions.ts, packages/core/src/controller/sessionController.ts, apps/mobile/src/session/{stintCoaching,pitViewModel,composition,analysisExport}.ts, apps/mobile/src/ui/screens/{PitViewScreen.tsx,trackdayStrings.ts,AnalysisScreen.tsx (E9 header only)}, tests. Coach engine/voice files read-only (E3 is solved at the update-validation layer). No commit, no agents, no Expo dev server.
