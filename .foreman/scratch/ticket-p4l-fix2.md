# Ticket P4l-FIX2 — Codex P4l-REV1 scoring findings (core only), tests first — Opus
Source: .foreman/scratch/p4lrev1-codex-output.log (final section). Spec: contracts.md "Signal Finder (Phase 4l)" item 3.
Scope is ONLY packages/core/src/telemetry/signalFinder/** and packages/core/test/telemetry/signalFinder/** (another worker owns telemetryProvider/controller/export/screen right now).
G1 (H1) A DID whose whole response has baseline changes (`responseBaselineChanges > 0`, e.g. a block with a rolling counter byte + one action byte) is capped at `unrelated` — the final verdict must use DID-level baseline changes, not only the winning byte's. Test: counter byte + brake byte block → unrelated.
G2 (H2) `analog-bipolar` must require significant excursions on BOTH sides of rest (left and right steps of the metronome, or ±) before ranking `found`/`probable`; one-sided excursion → at most `probable` with a stated reason, never `found`. Test: rest 100, press 120, release 100 (never below rest) → not found.
G3 (H4, scoring part) Multi-byte values: scoring compares the SAME big-endian scalar for 1–4-byte responses (0x500B 0002→0006 must count as an edge); for blocks, the selected offset's min/max are persisted in the summary (never null). Test with the 0x500B field series.
G4 (M6) Sufficiency gate per non-empty metronome step (press, hold, release each need ≥ 2 samples when the script defines them); aggregate only after the gate. Test: 0 press samples + 2 hold samples → insufficient.
G5 (M10) Edge matching = ordered transitions: rest→active observed inside the shifted press window and active→rest inside the shifted release window; extra/random transitions inside baseline or hold windows count against the DID (a quiet-baseline random toggler press [00,01] / release [01,00] must NOT be `found`). Keep: field 0x500C → found; 1 Hz samples in 2 s windows shifted 500 ms → sufficient; free-running counters and battery dips → unrelated.
Gates (real exit codes, repo root): `npm run typecheck`, `npm run test`, `npm run lint` — all 0 (skip expo export; the other worker runs it). Report core totals.
No commit, no agents, no Expo dev server. Do not touch apps/mobile. Report DONE / DONE_WITH_CONCERNS / BLOCKED with red→green evidence.
