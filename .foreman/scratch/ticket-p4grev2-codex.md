# Review ticket P4g-REV2 — Codex read-only bounded re-verify of P4g fix wave 1
Prior: `.foreman/scratch/p4grev1-codex-output.log`. Diff: `git diff d56c6bc..d992353`. Read-only Node/Git path if spawning is denied.
TASK (bounded): verdict FIXED/PARTIAL/NOT FIXED + file:line + failing scenario for: H1 unified teardown (both adapter types share `stopping`; Start awaits it; concurrent Starts coalesce; stopped emission generation-guarded); H2 settings changes during auto-discovery (abort + discard stale hits by fingerprint; reservation released); M1 fingerprint incl. telemetrySimulate; M2 field-repro test quality; L1 dev-tap window across the sequence. Then ONLY new defects in the touched functions: ELM byte-identical path for a user who never touches ENET (the worker rewrote two prior tests — was existing ELM behavior changed beyond the mandated unification? state exactly what changed), any deadlock between `starting`/`stopping`/discovery abort, reservation release for the ELM kind.
OUTPUT: first line PASS / FAIL / PASS_WITH_NOTES; table; NEW findings; Clean list. Stdout only. No agents.
