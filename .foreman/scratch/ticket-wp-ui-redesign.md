TASK: Premium redesign of the entire mobile UI: new design system (palette + typography + layout language), multi-circuit-ready selection screen, and relocation of legal/advisory text. The current UI reads "cheap/AI-generated" (generic blue accent, default system font, centered text) — replace it with a restrained, technical, motorsport-telemetry aesthetic. Dark theme stays.

EXPECTED OUTCOME: `npm run typecheck`, `npm run lint` pass from repo root; `npm run export:ios` exits 0; web dev server renders the redesign (a foreman will review visually — make the web build work: `npx expo start --web` is already configured; do NOT leave it running). Paste decisive output.

CONTEXT: Read first: apps/mobile/src/ui/theme/index.ts (current tokens), all screens in apps/mobile/src/ui/screens/, components in ui/components/, docs/decisions/ADR-0002 (attribution obligations). The app is portrait-first, iOS-primary. A concurrent Codex worker is adding `createCircuitCatalog` to @circuit/core — do NOT import it yet; instead create `apps/mobile/src/session/circuitCatalog.ts` implementing EXACTLY this interface (the foreman swaps the backing implementation at integration):
  interface CircuitSummary { circuitId: string; displayName: string; country: string; locality: string; lengthM: number; layoutId: string; layoutVersion: number; geometryStatus: string; sectorStatus: string }
  interface AppCircuitCatalog { list(): CircuitSummary[]; get(circuitId: string): { profile: CircuitProfile; runtime: RuntimeProfile } | null }
Back it today with the existing single TMR entry from session/tmrProfile.ts.

DESIGN DIRECTION (binding):
- Palette (replace theme tokens): layered near-blacks — background #0A0A0C, surface #121216, surfaceRaised #1A1A20; hairline borders rgba(255,255,255,0.08); textPrimary #F2F2F4, textSecondary #9A9AA3, textMuted #5C5C66. Accent: racing amber #FFB300 (interactive elements, highlights, active states) with pressed/dim variants. Semantic colors RESERVED for telemetry meaning only: delta-faster green #00E676, delta-slower red #FF3B30, warning amber distinct from accent where confusion possible, quality pill colors kept. No blue anywhere.
- Typography: bundle fonts via `npx expo install expo-font @expo-google-fonts/space-grotesk @expo-google-fonts/inter @expo-google-fonts/jetbrains-mono` (ONLY these). Display/headings: Space Grotesk (600/700). Body/UI: Inter (400/500/600). ALL timing digits: JetBrains Mono with tabular feel (fontVariant: ['tabular-nums'] where supported). Uppercase micro-labels ("SECTOR", "LAP", "PB") in Inter 600, letterSpacing ~1.2, textMuted. Load fonts at app root with a splash-gate (useFonts + keep splash until loaded); system-font fallback must never flash mid-session.
- Layout language: LEFT-ALIGNED everything (no centered headings/cards content); generous vertical rhythm; screens open with a small uppercase kicker (e.g. "CIRCUITS") + large display heading; cards are full-bleed-width rows with hairline separators rather than floating bordered boxes where a list is shown.
- S1 CircuitSelectionScreen: rebuild as a multi-circuit LIST driven by AppCircuitCatalog.list(): each row = circuit name (display font), locality · country, length + layout chip, chevron; supports N circuits from day one (TMR is the only row today — it must still look intentional, not empty: header + row + a subtle "More circuits coming" muted row is acceptable). REMOVE the ODbL footer and the advisory disclaimer from this screen entirely.
- Legal relocation (compliance, non-negotiable): ODbL attribution ("© OpenStreetMap contributors, ODbL 1.0") + geometry-provenance note + the advisory "recreational timing aid" disclaimer move to (a) SettingsScreen → About section (full text, always reachable) and (b) CircuitDetailScreen as one small muted line. Verify both render.
- All 13 screens + all shared components restyled to the new tokens (no hardcoded old colors left — grep for the old hex values and eliminate). ActiveDashboardScreen keeps its information hierarchy (delta dominant ~30% height, then lap time, sectors, pills) — restyle only: JetBrains Mono digits, new palette, left-aligned labels with uppercase kickers; delta colors stay green/red/gray semantics.
- App name: centralize the user-visible app name in ONE constant `APP_NAME` in apps/mobile/src/ui/branding.ts (used by S1 header etc.); keep value "Circuit Timer" for now (rename lands separately).
- Accessibility preserved: all existing accessibilityLabel/maxFontSizeMultiplier props kept or improved.

MUST NOT: touch packages/core/** ; change navigation structure or facade contracts; reintroduce modals during timing; add deps beyond the 4 font packages; leave any screen on old tokens; no subagents; no git commit.

OUTPUT FORMAT: First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED. Then files changed, screens restyled checklist (S1–S13 each ✓), commands + pasted results, limitations.

WRITE SET: apps/mobile/src/ui/**, apps/mobile/src/session/circuitCatalog.ts, apps/mobile/App.tsx (font loading), apps/mobile/package.json, package-lock.json.
