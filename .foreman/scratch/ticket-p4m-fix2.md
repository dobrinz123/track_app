# Ticket P4m-FIX2 — Codex P4m-REV2 (2 HIGH + 4 MEDIUM + M9 partial), tests first — Opus
Source: .foreman/scratch/p4mrev2-codex-output.log (final list, items 9, 11–16). HEAD = P4m-FIX1 commit. No build until this lands with 0 HIGH.
Y1 (H11) Probe = one attempted request per planned (ecu,did), NOT a 2 s deadline: the probe runs until every entry has been attempted once (each attempt bounded by the 300 ms timeout, so ≤ 12 × 0.3 s = 3.6 s worst case) — an ECU is `silent` only if EVERY one of its probe attempts was actually made and missed. Unattempted entries are never classified. Test: 3 ECUs, first ECU's 4 DIDs all time out → later ECUs still attempted; silent list correct.
Y2 (H12) Measured rate INCLUDES timeout cost (rate = attempts / wall time incl. misses), and the plan drops individual silent DIDs after the probe (a DID that missed in the probe goes to `notRead` with reason `silent` unless it is a hypothesis, which gets one retry in the script); ECUs with ≥ 1 answering DID stay. Re-plan/budget uses the timeout-inclusive rate. Test: 1 answering DID + 10 silent DIDs on 0x12 → budget refilled from the pools, script windows keep ≥ 3 samples for the live DID.
Y3 (M13) Back-off is a bounded cooldown, not round-long removal: after 3 consecutive misses a DID is skipped for the rest of the CURRENT evidence window and retried once at the start of every following window; a recovering DID keeps sampling. Test: DID silent in windows 1–2, answering from window 3 → samples in windows 3+ and not `insufficient` on that account (windows 1–2 still count as empty for that DID — honest).
Y4 (M14) `attempted` and `requestCount` are recorded only after `channel.send()` resolved successfully (exchange() returns `sent: boolean`); a thrown/rejected/timed-out send leaves the key unattempted (→ notRead). Test with a throwing send.
Y5 (M15) `stop()` awaits the WHOLE find, including pool collection: register the run promise before any async work; a stop during pool collection must prevent acquire/connect. Test: stop during collection → no transportFactory call.
Y6 (M16) `close()` is bounded (e.g., 2 s) with a transport abort/destroy fallback, and `release()` runs from a nested `finally` so a hanging close never blocks stop or the reservation. Test with a never-resolving close.
Y7 (M9 partial) RO output: pass the localized target label into the export (controller stores targetId; label resolved by language at export/screen time), replace the hard-coded `more` with `s.moreItems`, localize profile labels (catalog data gets `labelRo`) and map runtime/share errors to localized messages (unknown error → generic localized line + the raw message in parentheses). Test: RO summary for the brake target contains no `Brake switch` / `more` literals.
Y8 (LOW, LEAD E2E) pace the runner to the planned rate: sleep so one pass over the entries takes ≥ entries / targetHz (no over-polling; prevents the hot loop seen on the in-memory simulator). Test: fake instant channel → passes per second ≤ targetHz × 1.2.
Gates (real exit codes, repo root): `npm run typecheck`, `npm run test`, `npm run lint`, `npx expo export --platform ios` in apps/mobile — all 0. Field fixtures must stay green (0x4002 found, 0x4007 found, 0x1701/0x4659 not found). Report totals + red→green per item.
WRITE SET: packages/core/src/telemetry/signalFinder/**, apps/mobile/src/session/{signalFinderController,signalFinderExport}.ts, apps/mobile/src/ui/screens/{SignalFinderScreen.tsx,signalFinderStrings.ts}, tests. Do not touch sweep/observation flows, the reservation module, telemetryProvider. No commit, no agents, no Expo dev server.
