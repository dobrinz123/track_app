# Ticket P5b-FIX1 — Codex P5b-REV1 (1 HIGH + 8 MEDIUM + 2 LOW) + user F2 (interactive report), tests first — Opus
Sources: .foreman/scratch/p5brev1-codex-output.log (final list 1–11) and contracts "Phase 5 REVISION 2" R2-2 (user F2). HEAD = P5b commit + REVISION 2. A concurrent worker owns packages/core/src/coaching/** (clean-lap revision) — do NOT touch core; if its in-flight changes break a type you consume, report and stop that item.
C1 (H1) Session-active correctness: the view model observes the session facade's state; an analysis is cancelled/hidden when a session starts mid-run; a session ending while the screen is open makes analysis available without re-entering; recheck after every await before publishing. Tests with a fake facade.
C2 (M2) Channel coverage: computed per lap (a channel enters a lap's inputs only with coverage on THAT lap), the 50 % boundary conservative (<= excluded), and the limitation reported whenever any analysed lap lacked the channel. Tests incl. exactly-50 %.
C3 (M3) The loader validates circuitId AND layoutId (+ persisted layout version if stored) against the catalog; mismatch → a named "layout incompatible" state, never a silent projection onto different geometry. Test.
C4 (M4) `measured` derives from EVERY observable per-corner metric (lift, exit speed, peak decel, throttle-on, latG included). Test: a corner with only latG → measured.
C5 (M5) Chunked analysis: the projection/analysis loop yields periodically (e.g. per lap) so the UI stays responsive; test asserts the event loop turns between laps (fake timers / tick counter), not just total duration.
C6 (M6) One shared runner + cache per sessionId hoisted to composition; re-entering the screen joins the in-flight run; leaving does not spawn a second runner. Tests.
C7 (M7+M8) Export DTO: a standalone, versioned trace-analysis-report v2 DTO explicitly mapped from engine outputs; advisory/suggestion-derived fields (corners[].advisorySpeedKph etc.) OMITTED in V1; compatibility test pins the DTO shape (snapshot of keys).
C8 (M9) One-tap share hands over BOTH files in one action where the platform allows (expo-sharing: share the .md, and if multi-file share is unavailable, the button label says the JSON is written alongside AND a second control shares it — mirror the Signal Finder pattern; state clearly in the report which behaviour shipped).
C9 (L10+L11) Filename: every dynamic segment through one sanitizer (date normalized YYYY-MM-DD); the Analysis route header title from the RO/EN string table.
C10 (F2, user-ratified) INTERACTIVE report screen: corner LIST rows show only name + badges (time lost/gained, consistency when available, v_min → exit); tapping a row expands (or navigates to) the corner detail: per-lap values (brake/lift with ±uncertainty, v_min, exit, peak decel, latG), the demonstrated-envelope line, and the engine's observation sentence for that corner. Minimal prose on the main screen; the engine's full prose stays ONLY in the export. Keep the limitations/notes section compact (chips/one-liners). Strings RO/EN in analysisStrings. View-model tests for the row/detail data; screens stay thin.
Gates (real exit codes, repo root): `npm run typecheck`, `npm run test`, `npm run lint`, `npx expo export --platform ios` in apps/mobile — all 0. Report totals + red→green per item + changed strings (RO/EN).
WRITE SET: apps/mobile/src/session/{analysisAssembly,analysisSessionLoader,analysisViewModel,analysisExport}.ts, apps/mobile/src/session/composition.ts (C6 hoist only), apps/mobile/src/ui/screens/{AnalysisScreen.tsx,analysisStrings.ts}, apps/mobile/src/ui/navigation/RootNavigator.tsx (C9 title only), tests. packages/core read-only. No commit, no agents, no Expo dev server.
