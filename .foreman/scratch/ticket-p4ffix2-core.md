# Ticket P4f-FIX2-core — REV2 Part A residuals + E2E finding (core)
Binding: "sweep transport interface & lifecycle amendment" (contracts.md, end). Review: `.foreman/scratch/p4frev2-codex-output.log` Part A.
Tests first (fail on HEAD 91c23d0), then fix:
1. `runDiscovery`: budgetMs = min(cfg, 8000) hard; concurrency sanitized (NaN/≤0 → 16; cap 16). Tests: budget 60000 with a pending connect → returns ≤ 8 s truncated; concurrency NaN → 16 workers.
2. `runDidSweep`: NEW interface `{send, nextResponse(timeoutMs), keepAlive}` (BREAKING — document); 0x78 → await nextResponse again without re-send (bounded, default 5); unmatched → count + keep awaiting within the remaining timeout (bounded, default 3); TesterPresent via keepAlive every 2 s; synchronous throws contained; per-request timeout preserved. Replace the "same pdu re-sent" test with "0x78 then final without re-send"; add sync-throw, unmatched-then-correct-in-sequence, keep-alive cadence tests.
3. E2E finding: on the simulator, a sweep over unknown DIDs showed NRC/timeouts/unmatched all 0. Verify `SimulatedEnetTransport` answers unknown 0x22 DIDs with 0x7F 22 31 and that `runDidSweep` counts them in `nrcCounts` — add an end-to-end core test (simulator + runner + real codec) asserting non-zero NRC counts and the 3 scenario responders found within their range; fix whatever is wrong (simulator or runner).
Gates (real exit codes): typecheck 0, core tests 0, lint 0. WRITE SET: packages/core/src/telemetry/enet/**, packages/core/test/telemetry/enet/**. No commit, no agents, never touch apps/mobile (parallel worker). Report the exact new interface.
