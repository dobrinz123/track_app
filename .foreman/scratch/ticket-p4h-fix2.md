# Ticket P4h-FIX2 — Codex REV2 partials (telemetry/composition/controller), tests first
Source: `.foreman/scratch/p4hrev2c-codex-output.log` (final section "Cross-review result", HIGH items 1–2, MEDIUM item 1, table rows H5/H6/M2). HEAD 74a21e9. Another worker edits DID-sweep files concurrently (didSweep*, DidSweepScreen, didCandidates, didObservationPhases, eslint config) — do not touch them.
F1 (HIGH) telemetryProvider: Start during fallback teardown → two launches. Design: one lifecycle "intent" state machine — fallback relaunch and public Start both go through a single `launchAfterStop(generation)` continuation; a Start that arrives while a fallback teardown is in flight supersedes the fallback relaunch (the fallback continuation sees its generation stale and exits) and launches exactly once, with the source resolved from settings (5A) — never two sessions/transports. Test: 0x5A unsupported → teardown → Start tapped during teardown → exactly one session constructed, one transport, no overwrite.
F2 (HIGH) composition G-force refcount pairing: `releaseGForce()` only when this session actually acquired (track `gForceAcquiredForSession` per recorder/session start; early returns for SQLite-unavailable / telemetry-disabled / web never acquire and never release). Cover recovery, rebuild/error, delete-all paths. Test: monitor holds the only ref; a disabled/web session starts+ends, rebuild, delete-all → monitor ref intact, provider still running.
F3 (MEDIUM) queued calibration cancel: `rejectCalibration()` must also cancel a `beginCalibration()` that is still QUEUED behind the lifecycle lock (composition-level cancel token passed into the queued command; when the lock is acquired and the token is cancelled, the command returns without starting). Test at composition level with a held lock.
Gates (real exit codes): typecheck, test, lint, expo export iOS all 0 (if lint fails ONLY on DidSweepScreen.tsx it is the other worker's — report as foreign).
WRITE SET: apps/mobile/src/session/{telemetryProvider.ts,composition.ts,lifecycleLock.ts}, packages/core/src/controller/sessionController.ts, apps/mobile/src/ui/screens/{TelemetryScreen,CalibrationInstructionsScreen,ActiveCalibrationScreen}.tsx, matching tests. No commit, no agents, leave the Expo server alone. Report DONE/DONE_WITH_CONCERNS/BLOCKED with evidence + totals.
