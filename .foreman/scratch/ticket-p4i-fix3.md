# Ticket P4i-FIX3 — Codex P4i-REV3 partials (R1/R3/R6), tests first, bounded, small
Source: `.foreman/scratch/p4irev3-codex-output.log` (final "### Table"). HEAD 9b4d643.
X1 (R1) Terminal flush failure must be VISIBLE: `stop()/pause()` reject (or resolve with `{persisted:false, error}`) when the terminal `flushRunProgress` fails; screen shows "Save failed — results kept in memory, share now" (export still works from memory). Natural completion: emit `sweepComplete` only AFTER the terminal flush settles (or emit a `saving` state first); Share is enabled only once persisted or explicitly failed. Tests: rejecting store on stop/pause/complete.
X2 (R3) Rollback must be slice-aware: track claimed slices per flush; on failure, mark THAT slice as unpersisted and re-queue it (retry once) instead of `Math.min` on the shared marker; never double-write slices claimed by later flushes. Test: A claims 0–1, B claims 1–2, A fails, B succeeds → next flush re-sends only slice A, sampleCount of B's responder stays 1.
X3 (R6) Pre-pass/phase elapsed advances on a wall-clock ticker (250 ms) independent of `onSample`; countdown reaches the advertised duration even with zero samples. Test with fake timers and a transport that never answers.
Gates (real exit codes): typecheck, test, lint, expo export iOS all 0.
WRITE SET: apps/mobile/src/session/{didSweepController.ts,didSweepExport.ts}, apps/mobile/src/ui/screens/DidSweepScreen.tsx, apps/mobile/src/persistence/didSweepStore.ts, matching tests. No commit, no agents, leave the Expo server (8082) alone. Report DONE/DONE_WITH_CONCERNS/BLOCKED with evidence + totals.
