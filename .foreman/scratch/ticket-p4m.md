# Ticket P4m — Signal Finder v2: one human metronome, DID budget, hypotheses first, honest "not read" — Opus, tests first
Binding: docs/architecture/contracts.md "Signal Finder REVISION (2026-08-29, after field test 5)" items 9–13. Field data: data/field/signal-finder/2026-08-29-{brakeSwitch,accelPedal}.json (+ .md), ledger entry "field test 5". Build 5 behaviour to replace: apps/mobile/src/session/signalFinderController.ts runs ONE FULL METRONOME PER PASS over 91 passes; hypotheses queued last; unread DIDs exported as noResponse.
M1 core `packages/core/src/telemetry/signalFinder/`: metronome defaults → baseline 3000, press 3000, release 3000, repetitions 3 (max 5), settle stays; `planFinderRun(rate, hypotheses, changedDids, cachedDids, budgetOpts)` → ordered DID list within the budget (items 10a–c) + `notRead` remainder; scoring item 11 (`sparse` flag, found rule, insufficient only on empty windows). Tests from the field fixtures (item 13): replay the exported samples of 0x4002 and 0x4007 through the scorer with the export's metronome → `found`; 0x1701 → unrelated; 0x4659 → not found. Keep every existing scoring test green or update it with a one-line reason.
M2 controller: a Find = one run on ONE transport session polling all budgeted DIDs across ECUs (per-entry target address — see createRawUdsChannel / enet targetAddress per request; if the raw channel is single-target, open one channel per ECU on the same transport and round-robin), one metronome, then score. `nextRound()` runs the next budget slice on a new explicit call only. Snapshot: `round`, `readDids`, `notReadCount`, `notReadDids`. Cached DIDs prioritised by prior CHANGE evidence (read did_sweep observation summaries: rank other than static/insufficient) before plain responders.
M3 export/summary: "Read N DIDs across E ECUs in R round(s)"; "Not read: N (+ Next round)"; never list unread DIDs under "No response". schemaVersion → 2 (adds rounds/notRead); .md budget unchanged.
M4 screen: metronome prompt uses the target's verbs ("APASĂ frâna" / "PRESS the brake", RO/EN by the language setting), big countdown, 3 reps; result: "Next round (N DIDs, ≈ 25 s)" button; `sparse` shown as "found (sparse)". Remove the per-pass loop entirely.
M5 vehicle profile data: add the DME hypotheses 0x12/0x4002 (brakeSwitch, boolean 0x01→0x19) and 0x12/0x4007 (accelPedal idle flag bit0) to the Supra catalog in targets.ts with provenance (field test 5), keep 0x29/0x500C/0x500B as secondary; brakeSwitch binding decode must handle "0x01 rest / 0x19 active" (rest value vs. active value, already generic) — add a provider test with 0x4002.
Gates (real exit codes, repo root, npm workspaces): `npm run typecheck`, `npm run test`, `npm run lint`, `npx expo export --platform ios` in apps/mobile — all 0. Report totals.
WRITE SET: packages/core/src/telemetry/signalFinder/**, apps/mobile/src/session/{signalFinderController,signalFinderExport,telemetryProvider}.ts, apps/mobile/src/ui/screens/SignalFinderScreen.tsx, tests (fixtures may be read from data/field/signal-finder/ via fs in core tests). Do NOT touch the sweep/observation flows or the reservation. No commit, no agents, no Expo dev server. Report DONE / DONE_WITH_CONCERNS / BLOCKED / NEEDS_INPUT with red→green evidence + totals + exact new on-screen strings.
