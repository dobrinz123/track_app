Read-only final design review of the TRACE OBD dongle hardware+firmware package (rev A2). You are the second engineering family reviewing before the user sends this to fabrication. Be adversarial; the deliverable is a go/no-go with findings.

READ IN ORDER: hardware/DESIGN.md (binding, incl. section 7 amendments), hardware/schematic/generate_schematic.py (netlist as drawn), hardware/kicad/trace-dongle/generate_board.py (board as built, incl. pin maps and net assignments), hardware/kicad/trace-dongle/production/bom.csv + cpl.csv, firmware/src/** (esp. can_obd.*, read_only_guard.*, elm_server_core.*, wifi_ap.*), firmware/platformio.ini.

HUNT:
1. Electrical: netlist-vs-board cross-check (sample 15 connections from DESIGN.md section 3 against generate_board.py's net assignments — any mismatch is HIGH); buck design sanity (TPS54202: FB divider values for 3.3V vs datasheet 0.596V reference — verify the math!, BST cap, L value, UVLO divider math for ~8V threshold); protection chain order (PTC->Schottky->TVS placement electrically sound? TVS AFTER the Schottky protects the buck but not the diode — acceptable? state it); TJA1051T/3 VIO/VCC both on 3V3 correct for the /3 variant; PESD2CAN orientation; ESP32-C3 strapping pins (IO9 boot, EN RC) correct for auto-flash via J2 with SW1.
2. BOM/CPL: every BOM row has an LCSC part; CPL rotations plausible for polarized parts (diodes, ICs); DNP handling of R120/R9 consistent between BOM and board.
3. Firmware: read-only guard chokepoint actually the ONLY transmit path (grep for twai_transmit calls); guard whitelist matches app-side 21/22 rule + mode 01; ELM responses match the app's parser expectations (cross-check 3 vectors against packages/core/test/telemetry/pidCodec.test.ts); TCP single-client handling; AP credentials documented.
4. Anything that would brick/damage: pin conflicts on strapping pins (IO8 LED on a strapping pin? IO8 is a C3 strapping pin! check level at boot), TWAI vs pin choice conflicts, buck feedback wrong-value risk (would overvolt the 3.3V rail -> kills U1/U2).
OUTPUT: First line GO / NO-GO / GO_WITH_CHANGES. Then findings (severity HIGH/MED/LOW, file:line, concrete consequence), then the buck math shown explicitly, then a checked-clean list.
