TASK: Phase 4b work package A — (1) the sessionComplete telemetry barrier and (2) the dashboard TelemetryStrip, exactly per the "Telemetry addendum — P4b amendments (binding)" section at the END of docs/architecture/contracts.md (read it first).

EXPECTED OUTCOME:
(1) Barrier: the mobile facade layer holds the relay of state.sessionState === 'sessionComplete' to subscribers until the telemetry shutdown promise (composition.ts's telemetryShutdown, produced by stopTelemetryRecording()) settles, capped at 2000 ms. Implementation location is your choice between realFacade.ts's state relay or the SwappableFacade wrapper in composition.ts — pick the one with the smallest, clearest diff and justify in one sentence. Binding semantics: allSettled (a rejected/hung telemetry promise NEVER blocks past the cap, never rejects the relay); all OTHER states relay untouched and immediately; when telemetry never ran (disabled/web), zero added latency (no timer, straight through); the held state must still arrive exactly once, in order (no state emitted after it may arrive before it).
(2) TelemetryStrip: new component apps/mobile/src/ui/components/TelemetryStrip.tsx rendered on ActiveDashboardScreen. Visible ONLY while settings.telemetryEnabled AND telemetryProvider state is 'polling' (subscribe via telemetryProvider.onStateChange — it replays current state on subscribe). Content, max three values, one compact row: RPM (integer), throttle % (integer), coolant °C tinted amber >= 98, red >= 105 (thresholds as named constants). Muted/premium-dark styling consistent with CoachStrip.tsx tokens; FIXED height so appearing/disappearing never reflows the timing elements — reserve the slot like CoachStrip does, or overlay in dead space; it must never occlude timer/delta/sectors (study ActiveDashboardScreen's layout first). Values update from telemetryProvider.onSample; throttle updates at most 5 Hz are fine as-is — no animation work.

CONTEXT: Repo D:\CODE\APLICTIE_Circuit. P4a landed: apps/mobile/src/session/telemetryProvider.ts (onSample/onStateChange with replay, getDiagnostics), composition.ts (telemetryShutdown promise, stopTelemetryRecording, endSessionSideEffect hook, facadeWrapper), realFacade.ts, TelemetryScreen.tsx (style reference for value rows). Dashboard: apps/mobile/src/ui/screens/ActiveDashboardScreen.tsx + components/CoachStrip.tsx. Mobile tests: vitest node env, logic modules only — the barrier and any strip view-model logic must live in plain-TS testable functions/modules; the .tsx component itself stays thin and untested (house rule).

CONSTRAINTS: No new dependencies. No packages/core changes. Do not touch telemetryRecorder/persistence (another worker's write set). English copy. Keep diffs surgical.

MUST DO:
1. Tests in apps/mobile/test/session/: barrier holds sessionComplete until shutdown settles (fake pending promise -> resolve -> state arrives); 2000 ms cap (fake timers: hung promise -> state arrives at cap); zero added latency when telemetry never ran; ordering (states after sessionComplete never overtake it); strip view-model: visibility rule (enabled+polling only) and coolant threshold tinting boundaries (97/98/104/105).
2. Gates from repo root with REAL exit codes (cmd > log 2>&1; ec=$?): npm run typecheck, npm test, npm run lint, cd apps/mobile && npx expo export --platform ios. Report all four.
MUST NOT: No subagents. No changes outside WRITE SET. Do not modify TelemetryScreen, SettingsScreen, recorder, schema, or navigation.
WRITE SET: apps/mobile/src/session/realFacade.ts, apps/mobile/src/session/composition.ts, apps/mobile/src/ui/components/TelemetryStrip.tsx (new), apps/mobile/src/ui/screens/ActiveDashboardScreen.tsx, apps/mobile/src/session/telemetryProvider.ts (only if the strip needs a tiny accessor that does not exist), apps/mobile/test/session/**.
OUTPUT FORMAT: First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED. Then: barrier location choice + one-sentence justification; files changed; the four gate exit codes + test counts; deviations/concerns.
