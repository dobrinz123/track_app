# Ticket P4n — both brake bindings in telemetry (switch AND pressure), monitor labels, apply-after-confirm — tests first, Sonnet
Field fact (test 7, 2026-08-30): the user confirmed brakeSwitch = 0x29/0x500C and brakePressure = 0x12/0x58B7 (u8, 0 → up to 64 at idle with a firm press). The Telemetry monitor shows "brakeSwitch 100/0" but never the pressure: `apps/mobile/src/session/telemetryProvider.ts` `resolveBrakeBindingFromProfile` resolves ONE binding (preference pressure > switch), and a running monitor session keeps its old poll plan.
N1 Provider: resolve and poll BOTH field-confirmed brake bindings — brakeSwitch → channel `brakeSwitch` (boolean 0/100), brakePressure → channel `brakePct` (scaled 0–100 from observedMin..observedMax, clamp; fall back to raw when no observed range). Each is its own poll entry on its own ECU; the reservation/session logic is unchanged (per-entry target address already works on hardware — test 7). Tests: both bindings present → two DID entries and both samples emitted; only one present → one.
N2 Monitor screen (`apps/mobile/src/ui/screens/TelemetryScreen.tsx`): rows use labels from the channel table, never the raw id — "Brake switch" (unit: on/off shown as 100/0 → display "ON"/"OFF") and "Brake pressure" (unit "%", plus the raw value in small text e.g. "raw 37 / 64"); RO/EN via the language setting if the screen already localises, else EN. Test the label mapping as a pure function.
N3 Apply after confirm: when the Signal Finder confirms a binding while a telemetry session is running, the provider must pick it up: either restart the ENET poll plan in place (preferred: rebuild the plan from the refreshed bindings cache without dropping the socket) or, if that is unsafe, show a one-line hint on the Signal Finder result ("Confirmed — restart Telemetry (Stop → Start) to apply") and on the monitor ("New binding confirmed — Stop → Start to apply"). Test: confirm during a running session → next plan includes the new entry (or the hint state is set).
N4 Recording + analysis sanity: brakePct samples land in the session recording and `packages/core/src/coaching` prefers brakePct over brakeSwitch for brake onset/"how hard" (already the detectBrake order) — add one test with both channels present on a fixture lap.
N5 Vehicle-profile data: the Supra catalog's brakeSwitch hypothesis 0x12/0x4002 is NOT a pure switch (rest byte changes with the engine: 0x01 off / 0x83 running, bit7 = engine flag) — mark it `expectedShape: 'analog-monotone'`/label "brake pedal travel" and keep 0x29/0x500C as the boolean hypothesis first; a boolean binding must never be confirmable on 0x4002 by default (the finder may still report it found as analog).
Gates (real exit codes, repo root): `npm run typecheck`, `npm run test`, `npm run lint`, `npx expo export --platform ios` in apps/mobile — all 0. Report totals + red→green per item + changed strings.
WRITE SET: apps/mobile/src/session/{telemetryProvider,composition,signalFinderController}.ts (N3 hooks only), apps/mobile/src/ui/screens/{TelemetryScreen,SignalFinderScreen}.tsx (+ strings module), packages/core/src/telemetry/signalFinder/targets.ts (N5 data), packages/core/src/coaching/** (N4 test only unless a one-line fix is needed), tests. Do not touch the sweep/observation flows or the reservation module. No commit, no agents, no Expo dev server.
