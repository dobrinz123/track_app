# Review ticket P4f-REV3 — Codex read-only bounded re-verify of P4f fix wave 2 + T3
Prior: `.foreman/scratch/p4frev2-codex-output.log`. Diff: `git diff 91c23d0..ff92597`. Binding: "sweep transport interface & lifecycle amendment" (contracts.md, end). Read-only Node/Git path if spawning is denied.
TASK (bounded): per prior item — Part A residuals (budget hard cap / concurrency NaN; 0x78 re-await without re-send; sync-throw containment; simulator NRC on unknown DIDs), Part B (H1/H2 lifecycle order reserve→fresh transport→run→close→release; H3 raw channel + core-owned correlation after T3; M1 abortable auto-discovery on stop; M2 observation from paused; M3 cadence + GNSS context; reentrant start; simulated discovery under simulate; 360pt) — FIXED/PARTIAL/NOT FIXED with file:line + failing scenario. Then ONLY new defects in touched functions (e.g. T3 delegation: does the controller still duplicate any wire logic? one-DID-plan observation cost; keep-alive during observation; accumulator misuse; ELM path untouched). Nothing else.
OUTPUT: first line PASS / FAIL / PASS_WITH_NOTES; table; NEW findings; Clean list. Stdout only. No agents.
