TASK: Full integration — replace the mock session facade with the real production pipeline, wire persistence/recovery/watchdog, prove offline standalone readiness, and wire the dev replay screen to real fixtures. This makes the app functionally complete.

EXPECTED OUTCOME (all pasted with real output): `npm run typecheck`, `npm test`, `npm run lint` pass from repo root; `npm run export:ios` and `export:android` exit 0; a NEW integration test suite in apps/mobile is NOT required (no RN test runner) — instead core-side controller tests (see MUST DO 8) run in packages/core. Static-bundle proof and offline audit results included in your report (MUST DO 9, 10).

CONTEXT: Read first: docs/architecture/contracts.md; packages/core/src/{replay,fixtures,reference,matching,calibration,timing,statemachine,profile,persistence,persistence-sql}/index.ts (all exist, tested); apps/mobile/src/session/{facade.ts,composition.ts,mockFacade.ts} (swap point); apps/mobile/src/platform/ (GnssLocationProvider, PerformanceNowClock, ReplayLocationProvider, preflight, lifecycle); apps/mobile/src/persistence/expoSqlDatabase.ts; docs/decisions/ADR-0003 (§1 watchdog mandate) and ADR-0004 (offline/static-bundle mandates); packages/core/assets/circuits/transilvania-motor-ring.v1.json.

CONSTRAINTS: TypeScript strict everywhere. Core additions go in packages/core/src/controller/ (pure; no RN imports — lint enforces). App code in apps/mobile/src/session/ + minimal edits to screens ONLY where the facade contract genuinely needs extending (add speedKph to FacadeState — the dashboard placeholder awaits it). You own packages/core/src/index.ts now: wire ALL module re-exports (geometry, profile, matching, calibration, timing, statemachine, persistence, persistence-sql, reference, fixtures, replay, controller). No new dependencies except creating eas.json (a config file, not a dep).

MUST DO:
1. `SessionController` in packages/core/src/controller/: the production orchestrator — consumes a LocationProvider + MonotonicClock + LocalSessionRepository + RuntimeProfile + user/session IDs; internally composes the SAME pipeline pieces as runSessionPipeline (reuse shared wiring — refactor runSessionPipeline to build on the controller or extract shared composition; no duplicated wiring logic); exposes: start(phase: 'calibration'|'session'), acceptCalibration/rejectCalibration, arm, endSession, pause/resume, subscribe(state) emitting the full FacadeState shape (define a core-side FacadeStateCore type the app maps 1:1), and diagnostics(). Handles: calibration flow → CalibrationResult; armed → outLap → timing via crossings; lap completion → repository.saveTelemetry + checkpoint; PB evaluation via shouldReplacePb + buildReferenceLap + putReferenceLap on session end AND immediately when a lap beats PB (atomic, spec-compliant); delta engine fed from stored reference lap (loaded at session start); sessionId format `${userId}--<random>` (WP11b convention); checkpoint saveCheckpoint after every completed lap and on pause; speedKph from latest sample.
2. Watchdog (ADR-0003 §1 amendment, binding): inside SessionController (or a small core helper), if no sample arrives for > watchdogTimeoutMs (default 5000, config) while active and not paused, invoke a provided `restartProvider` callback (the app passes GnssLocationProvider stop/start) and increment `watchRestarts` diagnostic. Testable in core with a fake clock/provider.
3. Recovery (ADR-0003 §3, binding): on app launch, if a checkpoint exists for an incomplete session, offer recovery — restored laps are historical; the in-flight lap is marked invalid ('RECOVERY'); tMono never compared across launches. Core-side: SessionController.restoreFromCheckpoint(snapshot, laps). App-side: a small recovery prompt on CircuitDetailScreen (inline banner, not modal).
4. `RealSessionFacade` in apps/mobile/src/session/realFacade.ts: adapts SessionController to the existing SessionFacade interface; composition.ts swaps to it (MockSessionFacade stays available behind __DEV__ toggle on DevReplayScreen only). SessionHistoryStore backed by SqlSessionRepository (real listSessions/laps); settings store persisted via the same SQLite db (simple key-value table — extend schema via a v2 migration in persistence-sql, exercising the migration path for real).
5. TMR profile loaded via STATIC import: `import tmrProfile from '@circuit/core/assets/circuits/transilvania-motor-ring.v1.json'` (or require) so Metro inlines it — then loadProfileFromJson(JSON.stringify(tmrProfile)) or a loadProfileFromObject variant you add with the same validation path. NO fetch, NO fs at runtime.
6. DevReplayScreen: list the real fixture scenarios from @circuit/core fixtures; on selection, run the session through RealSessionFacade with ReplayLocationProvider at accelerated pace (10×) — the ACTUAL production controller, driving the ACTUAL dashboard UI live; result lands in real SQLite history. This is the on-device end-to-end demo.
7. Preflight integration: PreflightScreen calls real collectors; failures human-readable; PRECISE_LOCATION_OFF instructions shown (WP12b provided copy).
8. Tests (packages/core/test/controller/): full-session controller test driving fixtures through SessionController with in-memory repository + fake clock — asserts: calibration accept flow, 3 laps timed, checkpoint after each lap, PB created then improved (verify repository contents), delta emitted while timing, watchdog fires on a gap fixture (provider restart callback invoked), recovery restores laps and invalidates in-flight lap, pause/resume marks PAUSE_GAP. Session-end summary saved (listSessions returns it).
9. Static-bundle proof: run `npm run export:ios` with sourcemaps (`npx expo export --platform ios --source-maps --output-dir dist-export`), then verify (script or node one-liner) that the bundle's sources include the TMR asset module AND that searching the .hbc/map for "transilvania-motor-ring" finds the embedded profile data; paste evidence.
10. Offline audit: grep apps/mobile/src and packages/core/src for fetch(, XMLHttpRequest, axios, WebSocket — must be zero hits in session-critical paths (report any hits with justification); confirm no import of expo-updates/analytics/network libs.
11. eas.json at apps/mobile (or root per EAS convention): profiles development (dev client), preview (internal distribution, ios), production (store) — matching ADR-0004; do not run eas.
12. Update docs/architecture/current-state.md status section: integration complete, module map, controller description.

MUST NOT: duplicate pipeline wiring between controller and replay harness; introduce network calls; weaken any test; modal dialogs during timing; spawn subagents; git commit.

OUTPUT FORMAT: First line exactly one of DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED. Then files changed, assumptions, commands + pasted results (incl. static-bundle proof + offline audit), limitations, integration notes.

WRITE SET: packages/core/src/controller/**, packages/core/src/index.ts, packages/core/src/persistence-sql/** (v2 migration only), packages/core/test/controller/**, packages/core/src/replay/** (refactor to shared composition only), apps/mobile/src/session/**, apps/mobile/src/persistence/**, apps/mobile/src/ui/screens/{DevReplayScreen,PreflightScreen,CircuitDetailScreen,ActiveDashboardScreen}.tsx (minimal), apps/mobile/eas.json, docs/architecture/current-state.md.
