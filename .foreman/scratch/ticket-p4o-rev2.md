You are a read-only reviewer. Repo: D:\CODE\APLICTIE_Circuit (npm workspaces). If shell exec is blocked, read files directly. Bounded re-verification of the latest commit on main ("P4o-FIX1: robust two-level detection…") against: (1, HIGH) noisy two-state [0×10,10×8,14×1] escaped the two-level cap and a 0..12 staircase collapsed to one level under single-linkage; (3) replaced[] leaked across sessions; (4) rpm age ignored for the engine warning; (5) pendingReplace not cleared on stop/next round; (6) field exports not replayed. Files: packages/core/src/telemetry/signalFinder/{scoring,targets}.ts (+ scoring/targets/fieldFixtures tests), apps/mobile/src/session/signalFinderController.ts (+ test), apps/mobile/src/ui/screens/SignalFinderScreen.tsx. Say FIXED/PARTIAL/NOT FIXED with file:line. Then NEW defects in this commit only: the support threshold "≥ 2 samples or ≥ 10 % of DISTINCT values" — adversarial series where noise creates many distinct values (support gate weakened) or a graded signal with few distinct values gets capped; diameter clustering order dependence; rpm gating on state 'polling' for ELM327 vs ENET; the field replay fixture reading files via fs (path robustness on CI). Be brief. Output: numbered findings (HIGH/MEDIUM/LOW, file:line, one-line fix), then "NEW: n HIGH, m MEDIUM". No edits.
