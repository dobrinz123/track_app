# Ticket P4k-FIX1 — Codex P4k-REV1 residuals (1 MEDIUM + 1 LOW), tests first, tiny
Source: .foreman/scratch/p4krev1-codex-output.log (final section). HEAD cdb975e.
K1 (MEDIUM) apps/mobile/src/session/didSweepController.ts ~line 1702 (runCountGuaranteedPhase): the 3× hard cap is checked only before launching a full slice → the final slice can overrun the cap, and settling can leave DIDs short near the deadline. Fix: clamp each slice to `hardCapMs - elapsedMs` (skip when ≤ 0) so the phase never exceeds the cap; a DID still short at the cap ends `insufficient` (existing tri-state). Red-first test: with a slow responder and settle window, total phase time ≤ hard cap (assert on fake timers) and the short DID is `insufficient`.
K2 (LOW) packages/core/test/telemetry/enet/didObservationPhases.test.ts ~line 743: the "0x5422-like" fixture uses 0x00 everywhere, so it is static even if settling exclusion breaks. Fix: early steering sample = 0x00, all others 0x01 (as in the field data); assert `steeringCandidate` without settle and `static` with settle.
Gates (real exit codes, from the repo root, npm workspaces): `npm run typecheck`, `npm run test`, `npm run lint`, and `npx expo export --platform ios` inside apps/mobile — all 0. Report totals.
WRITE SET: apps/mobile/src/session/didSweepController.ts, apps/mobile/test/session/** (settle/count-guarantee tests), packages/core/test/telemetry/enet/didObservationPhases.test.ts. Nothing else (another worker is concurrently adding NEW files under packages/core/src/telemetry/signalFinder/ and apps/mobile/src/session/signalFinderController.ts — do not touch them, and ignore their test failures if any appear mid-flight; report them separately). No commit, no agents, no Expo dev server. Report DONE / DONE_WITH_CONCERNS / BLOCKED with red→green evidence + totals.
