# Review ticket P4h-REV1 — Codex read-only cross-review: field revision 2
Adversarial READ-ONLY reviewer. Diff: `git diff 6b5079c..0009720`. Binding: "Field revision 2" (contracts.md, end). Read-only Node/Git path if spawning is denied.
Attack list (file:line + concrete failing scenario):
1. Distance guard (`circuitProximity.ts`, PreflightScreen): haversine correctness, S/F midpoint from the RuntimeProfile/CircuitProfile (which coordinate system), stale/old GNSS fixes, accuracy-poor fixes, "Continue anyway" path still runs the existing checks, the dedicated GnssLocationProvider watcher — does it conflict with the controller's provider (double start/stop of the shared native watcher, orphaned watcher on unmount — the very battery issue closed earlier), web preview (no GNSS).
2. Calibration escape (ActiveCalibrationScreen): `beforeRemove` interception — can the confirm card loop or block navigation after cancel completes; double-tap Cancel; gesture back during a lifecycleLock section; sticky footer at 360×640 with 1.3× fonts (does the map still fit / scroll); web preview behavior.
3. Pedal (core + provider): `setAccelPedalPidSource` is MUTABLE GLOBAL STATE in @circuit/core — shared across provider generations, tests, and the DevReplay/ELM/ENET paths; races between two generations; reset on stop/start; is it reset when the adapter changes; determinism of tests that flip it. Fallback trigger reuses doStop()+relaunch — interaction with the fingerprint/latch/starting logic from P4g (can it loop: 0x5A unsupported → fallback → relaunch → 0x5A again?), the 8 s ELM grace window vs real ELM "NO DATA", ENET `unsupportedChannels` reactive path (untested at provider level per the worker), normalization math (offset learned only while speed==0; what if speed never 0; offset ≥ 100; negative), diagnostics `pedalSource`.
4. G provider with the monitor: start/stop ownership vs a driving session (Stop while a session is active must not stop it; session end while monitor open), idempotency, web.
5. Tests: substantive? which of the above have none?
OUTPUT: first line PASS / FAIL / PASS_WITH_NOTES; findings by severity with file:line + scenario + evidence; Clean list. Stdout only. No agents.
