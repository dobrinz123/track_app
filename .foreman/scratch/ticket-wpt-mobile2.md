TASK: Mobile side of the telemetry channel revision, exactly per docs/architecture/contracts.md section "Telemetry addendum — channel revision (2026-08-11, binding)" (read it first). Core support landed in commit 056e0d9 (engineOilC PID, customPids config in Elm327Config, latG/longG channel ids).

EXPECTED OUTCOME:
(a) Poll plan revision in telemetryProvider.ts: rpm 5 Hz, speedKph 5, throttlePct 5, engineOilC 0.5, transOilC 0.5 ONLY when the user configured a custom PID (settings below), coolantC 0.2. Pass customPids to Elm327Config from settings.
(b) Settings: new advanced field transOilPidHex (string, default '' = disabled) in settingsStore + Settings screen row under the TELEMETRY section: text input, accepts only hex characters/spaces (validate full string on blur like the port field pattern; empty = disabled), helper copy stating it is vehicle-specific and advisory (English). No other new settings.
(c) GForceProvider: apps/mobile/src/session/gforceProvider.ts built ON the existing apps/mobile/src/platform/motionCapture.ts (study it first — reuse its accelerometer plumbing/idioms; extend that module additively only if reuse genuinely requires it). Semantics per addendum: ~25 Hz, gravity via low-pass alpha 0.8, linear acceleration = raw minus gravity estimate, latG = component along the device X axis projected off the gravity vector, longG = component along the device Y axis projected off the gravity vector, unit g (divide by 9.81 m/s^2), portrait mount assumption documented in the module doc comment. Emits TelemetrySample ({channel:'latG'|'longG', value, tMonoMs from the SAME injected monotonic clock}). Session-scoped: started/stopped by startTelemetryRecording/stopTelemetryRecording in composition.ts alongside the OBD provider — but INDEPENDENT of it: G recording works even when the adapter is absent/failed, and vice versa; G samples flow into the SAME TelemetryRecorder. Lazy-import expo-sensors exactly like motionCapture does (vitest must never load it).
(d) TelemetryStrip revision: slots THR | ENG OIL | TRANS OIL, third slot shows COOLANT when transOilPidHex is unset. RPM removed from the strip. Tint constants per addendum: engineOil 120/130, transOil 110/125, coolant keeps 98/105. Update the strip viewmodel + its tests; visibility rule unchanged.
(e) TelemetryScreen: rows for engineOilC and (when configured) transOilC added; rpm row stays here (monitor screen is not the strip). latG/longG NOT shown here (analysis-only).
(f) LapDetailScreen charts: order speedKph, rpm, throttlePct, latG, longG, engineOilC, transOilC — render-only-when-rows-exist rule unchanged (verify the existing code just needs the channel list extended).

CONTEXT: Repo D:\CODE\APLICTIE_Circuit. Study: telemetryProvider.ts, composition.ts (startTelemetryRecording/stopTelemetryRecording/telemetryShutdown discipline — preserve it exactly; G provider teardown joins the same allSettled), settingsStore.ts (port-draft validation pattern from SettingsScreen), TelemetryStrip.tsx + telemetryStripViewModel tests, motionCapture.ts, LapDetailScreen.tsx, TelemetryScreen.tsx. Tests: vitest node env; viewmodel/provider/composition logic tested, thin .tsx not.

CONSTRAINTS: No new dependencies. No packages/core changes. English copy. Surgical diffs. G provider must never gate lap timing (same binding as OBD telemetry).

MUST DO:
1. Tests: poll plan built from settings (with/without transOilPidHex, pinned exact plans + customPids passthrough); hex validation (valid/invalid/empty); GForce math on hand-computed vectors (gravity convergence from a constant-gravity stream -> latG/longG near 0; a step lateral acceleration -> expected latG in g within tolerance; unit conversion pinned); G provider lifecycle (start/stop, samples into recorder, independence: OBD 'failed' does not stop G and G failure does not affect OBD, pinned); strip viewmodel slot content incl. coolant fallback + all six threshold boundaries; chart channel-list extension pinned.
2. Gates from repo root with REAL exit codes (cmd > log 2>&1; ec=$?): npm run typecheck, npm test, npm run lint, cd apps/mobile && npx expo export --platform ios. Report all four.
MUST NOT: No subagents. No changes outside WRITE SET. Do not alter the sessionComplete barrier, the write gate, or the recorder's batching/cap logic.
WRITE SET: apps/mobile/src/session/{telemetryProvider.ts,composition.ts,settingsStore.ts,gforceProvider.ts(new)}, apps/mobile/src/platform/motionCapture.ts (additive only), apps/mobile/src/ui/components/TelemetryStrip.tsx, apps/mobile/src/ui/screens/{SettingsScreen.tsx,TelemetryScreen.tsx,LapDetailScreen.tsx}, apps/mobile/test/session/**, apps/mobile/test/persistence/** (only if chart-list test lives there).
OUTPUT FORMAT: First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED. Then: files changed; the four gate exit codes + test counts; G math approach summary (3 sentences max); deviations/concerns.
