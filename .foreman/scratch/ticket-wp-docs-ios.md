TASK: Write the iOS-first, Windows-host developer and validation documentation for the track-session app.

EXPECTED OUTCOME: Three deliverables exist, accurate against the actual repo state, cross-linked, iOS/Windows-first: (1) docs/ios-no-mac-workflow.md, (2) updated README.md, (3) docs/verification/real-track-validation-checklist.md. `npm run lint` still passes (markdown not linted, but README references must not break). No code changes.

CONTEXT: Read first (binding sources — do not contradict them): docs/decisions/ADR-0003-ios-primary-target.md, docs/decisions/ADR-0004-no-mac-ios-workflow.md, docs/decisions/ADR-0001-stack.md, docs/decisions/ADR-0002-circuit-geometry-source.md, README.md (current), .foreman/scratch/platform-research.md (battery/thermal + iOS sections), docs/research/transilvania-motor-ring.md, apps/mobile/package.json (scripts), package.json (root scripts). The app: Expo SDK 57, all deps Expo Go-compatible, foreground-only location, monorepo (@circuit/core + apps/mobile).

MUST DO:
1. docs/ios-no-mac-workflow.md — the canonical guide: prerequisites on Windows (Node 24, npm), install, start Metro (`npx expo start`, `--tunnel` variant), connect iPhone via Expo Go (QR from camera app), what works in Expo Go vs what differs (Info.plist caveat from ADR-0004), running the dev replay screen on-device, EAS Build flow verbatim from ADR-0004 (login, configure, `eas build --platform ios`), Apple Developer $99/yr requirement clearly flagged as the ONLY paid dependency, TestFlight install steps, troubleshooting (same-network issues → tunnel; Expo Go version vs SDK mismatch).
2. README.md — restructure top-down: what the app is (advisory recreational lap timer, Transilvania Motor Ring, not an official timing system), quickstart (Windows + iPhone via Expo Go, 5 commands max, link to the workflow doc), monorepo layout, verification commands (typecheck/test/lint/export ios+android), docs index (link every doc in docs/), license/attribution note (OSM ODbL for circuit geometry). Keep existing accurate content; fix anything stale.
3. docs/verification/real-track-validation-checklist.md — iOS-first physical validation protocol at Transilvania Motor Ring. Structure as a printable checklist with result fields (Device model/iOS version, build type [TestFlight standalone REQUIRED for the reference pass — cite ADR-0004 §3], phone mounting position). Items (each with expected outcome + pass/fail field): observed GNSS update frequency (from diagnostics screen); horizontal-accuracy distribution p50/p95; thermal behavior over a 30-min session; recognition-lap acceptance on first attempt; calibration failure messaging when aborting mid-lap; pit-lane transit → lap invalidated + pit states visited; start/finish detection across ≥10 laps (zero missed, zero duplicate); sector crossing detection consistency; stop-on-line no-duplicate test (stop ON the S/F line 30 s); comparison against an independent timer (hand-timed or organizer timing) with expected agreement window ~±0.3 s and a note that GNSS interpolation at ~1 Hz bounds precision; live-delta stability (no flicker >±0.5 s between consecutive updates on steady laps); PB replacement after a genuinely faster lap; app backgrounding mid-lap → documented recovery behavior (lap invalidated, session recoverable); battery % consumed per 30 min; diagnostics export screenshot attached. Footer: configurable-thresholds note (do not tune to one device — cite mission constraint) and a sign-off block.

MUST NOT: modify any code, app.json, package.json scripts, or ADRs; contradict the ADRs; invent facts about the circuit or platform behavior not present in the source docs; spawn subagents; git commit.

OUTPUT FORMAT: First line exactly one of DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED. Then files changed, assumptions, verification notes (link-check of README references), limitations.

WRITE SET: docs/ios-no-mac-workflow.md, README.md, docs/verification/real-track-validation-checklist.md.
