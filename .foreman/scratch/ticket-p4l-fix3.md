# Ticket P4l-FIX3 — Codex P4l-REV1 mobile findings M5 M7 M8 M9, tests first — Sonnet
Source: .foreman/scratch/p4lrev1-codex-output.log (findings 5, 7, 8, 9). Runs AFTER P4l-FIX1 landed (same files).
J1 (M5) signalFinderController: an ECU's hypothesis + cached-responder list longer than 16 is partitioned into consecutive passes of ≤ 16 (all DIDs get read); the snapshot exposes pass index/total so the screen can say "pass 2/3". Test: 40 eligible DIDs → 3 passes, all 40 read.
J2 (M7) SignalFinderScreen: the on-screen summary/export document is built from `controller.getSnapshot()` after `find()` resolves (or in an effect keyed on the result snapshot), never from the closure that started the run. Test at the controller/document level if a render test is impossible; explain.
J3 (M8) signalFinderExport .md summary: total budget (≤ ~60 lines / ~4 KB): cap no-response DID lists and decode strings, truncate every variable-length section with "(+N more)". Test: 200 no-response DIDs and a 500-char decode → summary within budget and marker present.
J4 (M9) Profile export: add `buildVehicleProfileDocument(profileId, bindings)` that merges persisted bindings into the canonical vehicle-profile shape (same keys as data/vehicle-profiles/toyota-supra-b58.draft.json: profileId, make/model if known, transport, ecus, channels[] with status/provenance/evidence) and a "Share profile" button on the Signal Finder screen (JSON file `trace-vehicle-profile-<profileId>-<date>.json`). Test: two confirmed bindings → channels[] with status field-confirmed and evidence.
Gates (real exit codes, repo root): `npm run typecheck`, `npm run test`, `npm run lint`, `npx expo export --platform ios` in apps/mobile — all 0. Report totals.
WRITE SET: apps/mobile/src/session/{signalFinderController,signalFinderExport}.ts, apps/mobile/src/ui/screens/SignalFinderScreen.tsx, apps/mobile/src/persistence/didSweepStore.ts (read helpers only), tests. No commit, no agents, no Expo dev server. Report DONE / DONE_WITH_CONCERNS / BLOCKED with evidence + totals.
J5 (FIX1 concern 1 — production wiring) apps/mobile/src/session/composition.ts: pass `readVehicleProfileBindings` to `createTelemetryProvider` from a cached `createVehicleProfileBindingStore(getTelemetryReadDb()).listBindings(profileId)` snapshot (profileId = the Signal Finder's selected/generic profile; document the choice), refreshed after a Signal Finder confirm (and on provider (re)start). Test: after confirmBinding(0x29/0x500C) the provider's ENET config contains the brake did entry. composition.ts is ADDED to the write set for this item only.
