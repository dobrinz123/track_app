# Ticket P5a (REVISED) — Phase 5 core: deterministic corner analysis engine + per-corner report (packages/core ONLY)
Binding: contracts.md "Phase 5 REVISION" (end of file; it supersedes the LLM framing — NO LLM schema, NO prompt builder, NO validator for LLM output) + docs/roadmap/phase-5-llm-corner-analysis.md (REVISION section). Read them fully, plus the existing corner analysis (CORNER_ANALYSIS_VERSION, catalog corners), replay fixtures for BOTH circuits (TMR + motorpark-scenarios), session/lap data model and the telemetry contracts (channels, unsupportedChannels).
Deliver in `packages/core/src/coaching/` (pure, deterministic, no I/O, no React), tests first:
1. `cornerMetrics.ts` — `computeCornerMetrics(lapSamples, corners, options)` → per corner: liftPointM (throttle drop if throttle channel, else decel onset), brakeStartM (sustained longitudinal decel below a threshold, from GPS speed derivative and/or IMU longG), peakDecelG, minSpeedKph + positionM, exitSpeedKph, maxLatG, sectorMs, quality flags (gnss accuracy, gaps). GPS-only works; OBD/IMU channels enrich when present. Tests on synthetic laps + fixture laps of both circuits.
2. `cleanLap.ts` — `classifyLap(...)` → clean | anomalous(offTrack | yawSpike | decelSpike | gnssPoor | incomplete). Tests.
3. `envelope.ts` — `buildDemonstratedEnvelope(cleanLaps)` per corner: latest clean brake start, highest clean min speed, earliest lift, evidence lap ids (used for consistency + future bounded suggestions). Tests.
4. `sessionInsights.ts` — `analyzeSession(laps, corners, availability)` → ranked findings: (1) time loss per corner/sector vs the best clean lap (with the lap ids and numbers), (2) consistency per corner (std-dev / spread of brake point, min speed, sector time; a simple 0–100 consistency score), (3) brake/lift points per corner per lap table, (4) min/exit speed per corner. Honesty gates: < 2 clean laps → observations limited to per-lap facts, with an explicit `limitations[]` list (missing channels, poor GNSS, unvalidated geometry flag passed by caller). Deterministic ordering. Tests on both circuits.
5. `reportText.ts` — template-based report in RO and EN (`renderReport(insights, 'ro'|'en')`): per-corner sections, every sentence carries its numbers and lap ids, coach tone, no suggestions in V1 (only observations), a fixed disclaimer line. Snapshot-style tests for both languages; no string may contain "undefined"/"NaN".
Gates (real exit codes, repo root): `npm run typecheck`, `npm test`, `npm run lint` all 0. Export via `packages/core/src/index.ts` only if that is the existing pattern.
WRITE SET: packages/core/src/coaching/**, packages/core/test/coaching/**, packages/core/src/index.ts (export line only). Do NOT touch apps/mobile, telemetry/enet, or docs. No commit, no agents. Report DONE/DONE_WITH_CONCERNS/BLOCKED with evidence + totals.
