# Ticket P4m-FIX3 — Codex P4m-REV3 (2 HIGH + 3 MEDIUM + partials) and the LEAD E2E rate defect, tests first — Opus
Source: .foreman/scratch/p4mrev3-codex-output.log (final list) + ledger "E2E (web, simulator, FIX2 tree)". HEAD = P4m-FIX2 commit. No build until this lands with 0 HIGH. Prefer SIMPLER mechanisms over clever ones — every clever rule so far produced a new finding.
Z1 (LEAD E2E, HIGH) Rate basis: the rate used for the budget and for every duration estimate (next-step sweep minutes, round length) is the LIVE rate of the RETAINED entries (answers / the wall time spent on those entries), never the probe's overall rate including timeouts of entries that were dropped. Keep the timeout-inclusive figure only as diagnostics. Test: probe with 1 instant answer + 2 silent (dropped) → budget rate ≈ the answering entry's rate, next-step minutes ≈ 6413 / 15 / 60 at 15/s, not 56 min.
Z2 (H8 + partial 13) Cooldown: at every new evidence window a cooled DID gets the FULL miss allowance again (misses reset to 0), so a recovering DID is sampled; a DID that misses 3× in a window cools for the rest of that window only. Test: misses in window 1, answers from window 2 → ≥ 3 samples in window 2.
Z3 (H9) Pacing without drift: pace per PASS, not per entry — after a full pass over the entries, sleep max(0, passIntervalMs − passElapsedMs) where passIntervalMs = entries × 1000 / targetReqPerSec; never shift a schedule after slow exchanges. Test: mixed 300 ms misses + fast responders → live entries keep ≥ 3 samples per 3 s window at the planned rate.
Z4 (M10 + partial 12) Silent hypothesis: exactly ONE explicit retry after the probe (a single request), before the script; still silent → excluded from the round and listed as "not read — silent" (never polled during the script). Test.
Z5 (M11) Probe visible: snapshot phase `probing` with progress `probed/total` and the screen shows "Probing the ECUs… n/N" (RO: "Sondez ECU-urile… n/N") before the first metronome prompt; the probe is bounded by entries (≤ 12 × 300 ms) and that bound is stated in the string ("up to 4 s"). Test at controller level: phase sequence probing → running → result.
Z6 (M12) Teardown: release the reservation only after close settled; if close does not settle within 2 s and the transport has no destroy/abort, wait up to 5 s total, then release with `adapterTeardownPending: true` in the snapshot and a warning line, and refuse a new Find until the pending close settles (subscribe to it). Test: never-resolving close → release after the 5 s bound, snapshot flag set, second Find refused with the localized reason.
Z7 (partial 9) RO/EN: user-facing runtime/share messages never contain the raw error text; the raw text goes to the export's diagnostics section only. Test: RO banner for a thrown English error contains no English words from the error.
Gates (real exit codes, repo root): `npm run typecheck`, `npm run test`, `npm run lint`, `npx expo export --platform ios` in apps/mobile — all 0. Field fixtures must stay green. Report totals + red→green per item + changed strings (RO/EN).
WRITE SET: packages/core/src/telemetry/signalFinder/**, apps/mobile/src/session/{signalFinderController,signalFinderExport}.ts, apps/mobile/src/ui/screens/{SignalFinderScreen.tsx,signalFinderStrings.ts}, tests. Do not touch sweep/observation flows, the reservation module, telemetryProvider. No commit, no agents, no Expo dev server.
