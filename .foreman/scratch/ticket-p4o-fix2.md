# Ticket P4o-FIX2 — replace level clustering with a window-consistent "graded" rule; rpm warning re-render; ESM fixture path — tests first — Sonnet
Source: .foreman/scratch/p4orev2-codex-output.log (findings 3 partial, 6 HIGH, 7, 8, 9). HEAD = P4o-FIX1 commit. No build until this lands with 0 HIGH. Principle: no more clustering heuristics — the metronome gives us press windows, so use them.
U1 (H6 + M7 + M8) Delete the distinct-level clustering (`countDistinctLevels` and its support gate) as the basis of the two-level cap. New rule, symmetric and order-independent: for an ANALOG target the scored series is `graded` iff in at least 2 press windows there exists a sample whose value lies strictly inside the open interval (rest + floor, windowMax − floor) — an ordered intermediate excursion on the way from rest to that window's maximum — AND windowMax − rest > 2 × floor in those windows. Otherwise the series is `two-level` (cap: `found` → `probable`, reason 'two-level', confirm disabled). Noise singletons scattered across windows do not create intermediates unless they fall inside the interval of their own window; a two-state signal (0x83 → 0x9B) never has one. Tests: field 0x58B7 (press values 0x1A/0x40/0x36/0x30 …) → graded; field 0x4002 → two-level; noisy two-state with four one-off outliers far from the ramp → two-level; a genuine three-level signal with a single intermediate sample per press window → graded; reflected/reversed sample order → same result; boolean targets untouched. Keep `countDistinctLevels` only if still used elsewhere; otherwise remove it and its tests.
U2 (3 partial) The engine warning re-evaluates on a 1 s ticker while any running-required target is visible (or while a find is idle), so a positive rpm sample crossing the 5 s age flips the warning on without a new sample. Test the predicate over time with a fake clock at the screen-model level (pure function + timer wiring).
U3 (M9) fieldFixtures.test.ts resolves the export paths with `new URL('../../../../data/field/signal-finder/…', import.meta.url)` (no `__dirname`); verify the test runs under vitest's ESM mode.
Gates (real exit codes, repo root): `npm run typecheck`, `npm run test`, `npm run lint`, `npx expo export --platform ios` in apps/mobile — all 0. Report totals + red→green per item.
WRITE SET: packages/core/src/telemetry/signalFinder/{scoring,targets}.ts, apps/mobile/src/ui/screens/SignalFinderScreen.tsx (+ strings), tests. No other files. No commit, no agents, no Expo dev server.
