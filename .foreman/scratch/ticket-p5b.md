# Ticket P5b — post-session Analysis screen (per-corner report RO/EN, exportable), tests first — Opus
Binding: docs/architecture/analysis-engine.md (whole file), docs/architecture/contracts.md "Phase 5 REVISION" (deterministic engine, observations only, RO+EN, both circuits, session-only) and "Signal Finder REVISION" item 8-style exportable-reports rule (contracts: every result screen shares one-tap JSON + ≤1-page .md). Engine: packages/core/src/coaching/** (distanceDomain, cornerMetrics, cleanLap, envelope, sessionInsights, reportText, types — read index.ts for the public API). Persistence: find where finished sessions/laps + telemetry samples are stored (apps/mobile/src/persistence/** — the lap history and LapDetailScreen already read them). Language: the settings store's `language`.
B1 Entry points: an "Analysis" button on the session summary (post-session) and on the lap-history/session views for a past session of EITHER circuit. Dev-independent (not behind the dev gate). No analysis during an active session.
B2 Data assembly: build the engine inputs from the stored session — laps with GPS samples, the circuit catalog's corners (both circuits, from data, no hardcoding), channel availability derived from what the recording actually contains (gps always; accelPedalPct/brakeSwitch/brakePct/rpm when present with coverage). The engine decides clean/unverified/anomalous and availability — the screen never invents data.
B3 Screen: session overview (insights: time loss vs best clean lap, consistency with basis, limitations honestly — UNVERIFIED_LAPS, TIME_INTEGRATION_DRIFT, partial coverage "insufficient data (covers X %)"), then per-corner rows (corner id/name, time loss, brake/lift observations with ±uncertainty when the engine provides it, min/exit speed) in the app language (RO/EN via reportText — the engine already renders localized text; the screen adds only chrome labels, put THOSE in a strings module RO/EN). Observations only — no advice (suggestions stay OFF).
B4 Export (binding): one-tap Share = the full report as JSON (engine outputs, schemaVersion 1, kind trace-analysis-report) + a ≤1-page .md (the same text the screen shows, session header + per-corner table + limitations), named trace-analysis-<circuit>-<date>. Reuse the existing share path.
B5 Performance: analysis runs off the UI thread path as feasible (async, spinner state); a session with 20 laps × 1 Hz must not freeze the screen; memoize per session id.
B6 Review pre-item: read .foreman/scratch/p5arev3-codex-output.log if present (P5a REV3) and verify in code that the FIX3 items (metre-span coverage union, per-interval yaw availability, overview wording) are still as fixed; report their state — do NOT modify core unless a one-line bug blocks the screen (then report it).
Gates (real exit codes, repo root): `npm run typecheck`, `npm run test`, `npm run lint`, `npx expo export --platform ios` in apps/mobile — all 0. Report totals + red→green per item + the exact screen strings (RO/EN).
WRITE SET: apps/mobile/src/ui/screens/** (new AnalysisScreen + strings + navigation entry), apps/mobile/src/session/** (new analysis assembly module + export), apps/mobile/src/persistence/** (read-only helpers, additive), tests. packages/core: read-only (B6 exception). No sweep/finder/reservation changes. No commit, no agents, no Expo dev server.
