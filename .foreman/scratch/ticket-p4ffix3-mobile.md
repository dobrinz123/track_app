# Ticket P4f-FIX3-mobile — lifecycle race, abortable network-info, observation via core runner (mobile)
Binding: "observation runner & lifecycle race amendment" (contracts.md, end). Review: `.foreman/scratch/p4frev3-codex-output.log` (NEW-1, NEW-2, NEW-3).
A parallel core ticket adds `runDidObservation({responders, transport, clock, durationMs, targetHz, pacing?, control, onSample, maxConsecutiveErrors?}) → {series, errors, cadenceDegraded}`; build against that shape (if not in the tree when you integrate, code to the description and note it — do not re-implement the loop locally).
Tests first (fail on HEAD ff92597), then fix:
H1 `didSweepController.ts`: retain the transport reference until close settles; `stop()` awaits in-flight teardown before release; one lifecycle try/finally around channel creation + run + observation (close then release on every path incl. sync throws); run-generation guard so `stopped` is never overwritten by `sweepComplete`. Tests: the REV3 race (slow close + Stop during completion → release only after close; final state 'stopped'); sync throw in channel creation → transport closed + claim released.
M1 `telemetryProvider.ts`: race `readNetworkInfo()` against the abort signal and a 1500 ms timeout; `stop()` returns promptly with the token released. Test: never-settling getNetworkInfo + stop → resolves, token null, next start proceeds.
M2 observation phase uses core `runDidObservation` (single run; pass `deps.pacing`); delete the per-DID loop. Tests: keep-alive observed during a fast-response window (through the simulated transport), error budget honored, cadenceDegraded surfaced.
Gates (real exit codes): `npm run typecheck` 0, `npm test` 0, `npm run lint` 0, `cd apps/mobile && npx expo export --platform ios` 0.
WRITE SET: apps/mobile/src/session/{didSweepController.ts, telemetryProvider.ts}, apps/mobile/src/ui/screens/DidSweepScreen.tsx (only if API changes), apps/mobile/test/**. No commit, no agents, never touch packages/core (parallel worker).
