# Ticket P4i-FIX1 — Codex P4i-REV1 findings (DID sweep persistence/observation/export), tests first
Source: `.foreman/scratch/p4hrev2c-codex-output.log` (final section "Cross-review result": HIGH items 3–5, MEDIUM items 2–6, Test gaps). HEAD 74a21e9. Another worker edits telemetryProvider/composition/sessionController/calibration screens concurrently — do not touch them.
F1 (HIGH) persistence: flushes are queued (serialized promise chain), run-scoped (each flush captures its runId + index and is discarded if the controller moved to another run), forced stop/complete flush is NEVER dropped (awaited, chained after the active flush), responders + progress written in ONE transaction (sql.js/expo-sqlite `withTransaction`/BEGIN..COMMIT; in-memory fallback atomic), batch window ≤ 1 s. Resume after kill must not re-send visited DIDs beyond the last committed checkpoint — document the bound. Tests: stop during in-flight flush; kill (no stop) inside batch window then resume; immediate new Start while old flush pending → no cross-write; partial-batch atomicity.
F2 (HIGH) integrate the binding two-sample changing-value pre-pass: before the guided phases, read every filtered candidate twice (~2 s apart, user prompted "blip throttle / press brake / turn wheel") and keep changed-or-plausible DIDs (`selectChangingCandidates` — now with a production caller). Guided phases: round-robin must CONTINUE from where the previous phase stopped (persistent cursor) and each phase must cover the candidate set — if the set is larger than ~rate×phaseSeconds, raise the phase length automatically (show it) so every candidate is sampled ≥ 2× per phase. Test: 300 candidates at 15 req/s → phase length grows, every DID sampled ≥ 2× per phase; cursor continuity across phases.
F3 (HIGH) export includes guided series: controller exposes `getGuidedSamples()` (per DID, per phase, relative timestamps, raw hex); the screen passes them to `buildDidSweepExportDocument`. Test at screen/controller handoff level (not just the builder).
F4 (MEDIUM) keep-alive continuity: one TesterPresent ticker across the pre-pass and all phases (or a controller-owned keep-alive around the sequence) — never > 2 s between keep-alives at phase boundaries. Test with fake clock.
F5 (MEDIUM) ranking identity: a DID that changed in exactly one active phase is labelled by THAT phase ("BRAKE?", "STEERING?", "THROTTLE?"), not a merged "BRAKE/STEERING?". Tests.
F6 (MEDIUM) Resume picks the most recent RESUMABLE run (status paused/stopped/interrupted with lastDid < rangeEnd), not the most recent run; show its progress.
F7 (MEDIUM) unmount must not overwrite a naturally completed run with status `stopped` (stop() is a no-op on completed; status transitions are monotone).
F8 (MEDIUM) remove the `react-hooks/exhaustive-deps` disable comment (rule not configured) or configure the rule properly — lint must be 0 without unknown-rule references.
Gates (real exit codes): typecheck, test, lint, expo export iOS all 0.
WRITE SET: apps/mobile/src/persistence/didSweep*.ts, apps/mobile/src/session/{didSweepController.ts,didSweepExport.ts}, apps/mobile/src/ui/screens/DidSweepScreen.tsx, packages/core/src/telemetry/enet/{didCandidates.ts,didObservationPhases.ts,didSweep.ts,simulatedEnetTransport.ts}, eslint config (only if F8 needs it), matching tests. No commit, no agents, leave the Expo server alone. Report DONE/DONE_WITH_CONCERNS/BLOCKED with evidence + totals.
