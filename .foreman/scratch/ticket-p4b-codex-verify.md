You are a read-only cross-family verifier. Commit 50f6125 lands Phase 4b (two Claude workers): (A) a sessionComplete telemetry barrier + dashboard TelemetryStrip, (B) readLapTelemetry/bucketTelemetry + LapDetail sparkline charts. Binding spec: docs/architecture/contracts.md, section "Telemetry addendum — P4b amendments (binding)" (read first). Original tickets: .foreman/scratch/ticket-wpb-a-session.md and ticket-wpb-b-lapdetail.md. Assume broken until proven otherwise; find what unit tests missed.

DIFF SCOPE (50f6125 vs parent): apps/mobile/src/session/{composition.ts,telemetryProvider.ts}, apps/mobile/src/ui/components/TelemetryStrip.tsx, apps/mobile/src/ui/screens/{ActiveDashboardScreen.tsx,LapDetailScreen.tsx}, apps/mobile/src/persistence/telemetryRead.ts, tests.

HUNT SPECIFICALLY FOR:
1. Barrier correctness in SwappableFacade: (a) state ORDERING — can any state emitted after sessionComplete overtake it (queue semantics during the hold, including a rapid new session's states while the hold is pending)? (b) DevReplay facade swap mid-hold — leak or wrong-facade delivery? (c) does the barrier hold apply per-subscriber or once globally (double-hold if two subscribers)? (d) zero-latency path genuinely timer-free when telemetry never ran? (e) can the held sessionComplete be LOST (never delivered) on any path?
2. TelemetryStrip: subscription leaks on unmount; setState-after-unmount; visibility rule matches binding (enabled AND polling only); fixed slot really can't reflow/occlude timing elements (check the layout math); per-sample re-render cost at 20 samples/s — any obviously pathological pattern (new object per render breaking memo, etc.).
3. telemetryRead: SQL correctness incl. lap filtering and NULL-lap exclusion; bucketTelemetry math — bucket index at t === maxT (off-by-one overflow?), single-bucket case, bucketCount <= 0 guard, division by zero when minT === maxT; does LapDetail call the read with the lap numbering scheme ACTUALLY stored by the recorder (recorder tags facade lapNumber -> is LapDetail's route lapNumber the same 1-based scheme, or an array index +/-1)? Trace both sides to the source and verify they agree.
4. LapDetailScreen: async read raced with unmount; renders-nothing-while-loading rule; section hidden when zero rows; sparkline flex math with 80 bars on narrow screens (bar width < 1px issues are LOW).
5. New defects introduced into previously-working flows (composition diff especially — the barrier refactor renamed broadcast->commit; check every call path still relays states, including DevReplay swap and error-terminal states).

RULES: read-only; cite file:line; severity HIGH (malfunction on track / data wrong / state machine stuck), MEDIUM (degraded edge), LOW (style/perf nit); no speculative findings without a concrete failure path.
OUTPUT FORMAT: First line PASS / FAIL / PASS_WITH_NOTES (FAIL if any HIGH). Then findings (severity, file:line, concrete failure scenario, fix direction), then "checked and clean" list.
