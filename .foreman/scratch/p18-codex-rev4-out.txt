OpenAI Codex v0.153.4
--------
workdir: D:\CODE\APLICTIE_Circuit
model: gpt-6-astra
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a0cf1d-58a6-7b53-8f42-05d419dda088
--------
user
# Review ticket P18-REV4 — Codex read-only cross-review of P18-FIX3
Adversarial READ-ONLY reviewer. The fix is commit `939b93f` on branch `p18-fix1-delete-all`: `git diff 1dee7b2..939b93f` (small). Cumulative delete-all rework: `git diff 6d6f90b..939b93f`.
It answers your P18-REV3 review (`.foreman/scratch/p18-codex-rev3-out.txt`, final section after the last `codex` line): MEDIUM 1 (fence-only return strands VIN detection at 'attempting' after a refused delete), LOW 2 (Settings draft not reset when delete-all rejects), the history-freshness qualification, and the policy qualifications (selected circuit, adopting-phase "stop").
Binding: HANDOFF.md item 1 (a VIN is personal data; delete-all must remove it), docs/legal/privacy-policy.*.md §3.3 and §7.
1. For EACH REV3 finding/qualification: CLOSED / PARTIAL / OPEN with file:line and a concrete failing sequence if not closed.
2. Attack the new `maybeDetectVehicleFromVin` branch (`deviceWipeInProgress` with unchanged generation -> `vinDetectionState = 'idle'`): can it let delete-all return `ok: true` with the VIN in memory or on disk, allow two concurrent reads, or re-arm when it must not?
3. The history refresh now keyed on `result.ok` (per-circuit wipe) — any stale or wrong history shown?
4. SettingsScreen `finally` draft reset — any state bug (unmounted component, stale closure)?
5. Does the new test in `composition.deleteAllVinFence.test.ts` pin M1 (it fails on 1dee7b2)?
6. A final sweep over the cumulative diff for any remaining HIGH.
OUTPUT: first line PASS / FAIL / PASS_WITH_NOTES; per-finding status; new findings by severity with file:line + scenario + evidence; Clean list. Stdout only. No agents.

codex
I’ll inspect the fix and cumulative diff, map each REV3 item to the code, and check the VIN fence test. I’ll keep the review read-only and report to stdout.

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'Get-Content HANDOFF.md; Get-Content .foreman/scratch/p18-codex-rev3-out.txt; git diff 1dee7b2..939b93f; git diff --stat 6d6f90b..939b93f' in D:\CODE\APLICTIE_Circuit
 succeeded in 223ms:
Get-Content : Cannot find path 'D:\CODE\APLICTIE_Circuit\HANDOFF.md' because it does not exist.
At line:2 char:1
+ Get-Content HANDOFF.md; Get-Content .foreman/scratch/p18-codex-rev3-o ...
+ ~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : ObjectNotFound: (D:\CODE\APLICTIE_Circuit\HANDOFF.md:String) [Get-Content], ItemNotFound 
   Exception
    + FullyQualifiedErrorId : PathNotFound,Microsoft.PowerShell.Commands.GetContentCommand
 
OpenAI Codex v0.153.4
--------
workdir: D:\CODE\APLICTIE_Circuit
model: gpt-6-astra
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a0cf17-4b89-7871-b022-ecfbf9910178
--------
user
# Review ticket P18-REV3 â€” Codex read-only cross-review of the P18-FIX2 fix wave
Adversarial READ-ONLY reviewer. The fix is commit `1dee7b2` on branch `p18-fix1-delete-all`: `git diff d2b26f6..1dee7b2` (the whole delete-all rework since the cloud handoff: `git diff 6d6f90b..1dee7b2`).
It answers your P18-REV2 review (`.foreman/scratch/p18-codex-rev2-out.txt`, final section after the last `codex` line): HIGH 1 (VIN read started after the second reset outlives the final check), MEDIUM 2 (`error` phase refusal with an instruction the UI cannot follow), MEDIUM 3 (terminal Test Loop state keeps geometry/coordinates), MEDIUM 4 (Settings channel-specs draft survives), plus the history-refresh and policy qualifications.
Binding: HANDOFF.md item 1 (a VIN is personal data; delete-all must remove it), docs/legal/privacy-policy.*.md Â§3.3 and Â§7 as changed.
For EACH REV2 finding and qualification: CLOSED / PARTIAL / OPEN with file:line, and a concrete failing sequence if not closed. Then attack the new code:
1. `deviceWipeInProgress` fence (`unlockedDeleteAllStoredUserData` try/finally, `maybeDetectVehicleFromVin`): any path where delete-all returns `ok: true` with the VIN in memory or on disk; any path that leaves detection stuck or permanently disabled; refusal paths.
2. Test Loop abandonment in delete-all (step 1d): `testLoopController.reset()` + `unlockedTearDownLearnPhase()` under `lifecycleLock` for phases `learned`/`failed`/`error`, before `unlockedRebuildProductionController()`. Can it stop providers a live or just-ended session still needs, race the auto-teardown subscription on `failed`, or leave `TestLoopScreen` in a state it cannot recover from? Is clearing the in-memory ledger while keeping the journal row until success correct against bootstrap repair?
3. SettingsScreen draft reset: correct after both success and failure? any stale ref interplay?
4. Tests: does `composition.deleteAllVinFence.test.ts` pin the HIGH (it fails on d2b26f6 with 3 reads started â€” is asserting "no read starts" the right property, and is the VIN/disk assertion meaningful)? Do the Test Loop tests pin M3?
5. Policy EN/RO vs code: over- or under-claims.
6. Anything else in the diff that changes behaviour the commit messages do not mention.
OUTPUT: first line PASS / FAIL / PASS_WITH_NOTES; per-REV2-finding status; new findings by severity with file:line + scenario + evidence; Clean list. Stdout only. No agents.

codex
Iâ€™ll check the REV2 findings against the fix, trace the lifecycle and VIN races, and review the tests and policy claims. Iâ€™ll keep the review read-only and use no agents.

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'git status --short; git diff d2b26f6..1dee7b2 --stat; Get-Content .foreman/scratch/p18-codex-rev2-out.txt -Tail 220' in D:\CODE\APLICTIE_Circuit
 succeeded in 55ms:

  async refresh(): Promise<void> {
    const summaries = await this.repository.listSessions(this.userId, this.circuitId);
    this.cache = summaries
      .filter((s) => s.layoutId === this.layoutId && s.layoutVersion === this.layoutVersion)
      .map((s) => ({
        sessionId: s.sessionId,
        circuitId: s.circuitId,
        layoutId: s.layoutId,
        displayDateUtc: s.startedAtUtc,
        laps: s.laps,
        // Ticket P10A H6/H7: carried straight through from the stored row.
        // An absent value stays absent here -- the readers turn that into
        // `'unknown'`, never into `'validated'`.
        ...(s.calibrationStatus === undefined ? {} : { calibrationStatus: s.calibrationStatus }),
        // Ticket P12 item C: BOTH trace figures, not just the first. The
        // session report has to be able to say "storage misbehaved N times
        // and nothing was lost" as well as "N fixes never landed".
        ...(s.trace === undefined
          ? {}
          : {
              unwrittenSampleCount: s.trace.unwrittenSampleCount,
              failedWriteCount: s.trace.failedWriteCount,
              // Ticket P14 H3: absent stays ABSENT (= unknown). A row written
              // before the column existed must not read back as finalised.
              ...(s.trace.recordingFinalized === undefined
                ? {}
                : { recordingFinalized: s.trace.recordingFinalized }),
            }),
      }));

    const ref = await this.repository.getReferenceLap(this.userId, this.circuitId, this.layoutId, this.layoutVersion);
    if (ref === null) {
      this.pb = null;
      return;
    }
    const session = this.cache.find((s) => s.sessionId === ref.sessionId);
    const lap = session?.laps.find((l) => l.lapNumber === ref.lapNumber) ?? syntheticLapFromReference(ref);
    this.pb = { lap, sessionId: ref.sessionId, recordedAtUtc: ref.recordedAtUtc };
  }

  listSessions(): StoredSession[] {
    return [...this.cache];
  }

  getSession(sessionId: string): StoredSession | null {
    return this.cache.find((s) => s.sessionId === sessionId) ?? null;
  }

  getPersonalBest(): PersonalBestEntry | null {

codex
FAIL

Reviewed `6d6f90b..d2b26f6`; HEAD matches the fix. No files changed; no agents used.

**REV1 dispositions**

| Finding | Status | Evidence |
|---|---|---|
| H1 Ã¢â‚¬â€ VIN resurrection | **PARTIAL** | [composition.ts:1048](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:1048) rejects reads from earlier generations, and the initial memory reset fixes stale whole-blob writes. However, detection started after the **second** reset can restore VIN after verification. Failing sequence below. |
| H2 Ã¢â‚¬â€ persisted tagged channels survive | **CLOSED** | [deviceDataWipe.ts:45](D:/CODE/APLICTIE_Circuit/apps/mobile/src/persistence/deviceDataWipe.ts:45) clears `enetChannelSpecsJson`; verification counts remaining nonempty strings. The original persisted-channel scenario is fixed. An editor-memory gap remains below. |
| H3 Ã¢â‚¬â€ stale adoption resumes against fallback controller | **CLOSED** | [composition.ts:3777](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3777) refuses the failed-adoption `error` phase. [composition.ts:3711](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3711) clears an abandoned ledger before success. The prescribed UI escape is broken, as detailed below. |
| M4 Ã¢â‚¬â€ learned refresh failure reported as success | **CLOSED** | [composition.ts:3673](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3673) records refresh failure; line 3898 incorporates it into the result. Stale geometry can remain, but success is withheld. |
| M5 Ã¢â‚¬â€ detection disabled until restart | **CLOSED** | [composition.ts:3650](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3650) resets detection to `idle`; obsolete completions cannot set it back to `done`. This restores eligibility at the next trigger, subject to adapter availability. |
| L6 Ã¢â‚¬â€ session-linked settings survive silently | **CLOSED** | [composition.ts:3717](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3717) deletes the adoption journal; [composition.ts:3915](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3915) turns an unvalidated-log deletion failure into aggregate failure. |

**New and residual findings**

1. **HIGH Ã¢â‚¬â€ The second reset permits a fresh VIN read to outlive the final check.**  
   [composition.ts:3668](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3668), [composition.ts:3695](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3695), [composition.ts:1057](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:1057).

   Sequence:
   - Delete-all performs its second identity reset, setting detection to `idle`.
   - While deletion awaits subsequent work, opening Signal Finder starts detection with the **current** generation.
   - Identity read-back returns zero.
   - Detection completes while deletion awaits journal, active-pointer, log, or history operations. Its generation matches, so it restores both `cachedDetectedVin` and `lastSeenVin`.
   - Those remaining operations succeed; delete-all returns `ok: true`.

   **Evidence:** A read-only, in-memory reproduction using the source-extracted reset/detection functions, actual `SqlSettingsStore`, actual FIFO gate and sql.js produced:
   ```
   identityCountAtFinalCheck: 0
   memoryVin: WZ1DB0C04LW000001
   diskVin: WZ1DB0C04LW000001
   vinDetectionState: done
   ```
   This is a component reproduction, not a full composition integration test. The remaining success path contains no further generation change or identity check.

   A generation fence must cover the whole deletion interval; two isolated resets do not provide that fence.

2. **MEDIUM Ã¢â‚¬â€ The refusal tells users to leave Test Loop, but leaving does not abandon it.**  
   [composition.ts:3777](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3777), [TestLoopScreen.tsx:56](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/TestLoopScreen.tsx:56).

   Sequence: adoption fails at the selected-journal write before session start Ã¢â€ â€™ phase becomes `error` Ã¢â€ â€™ leave Test Loop Ã¢â€ â€™ request delete-all. It refuses. Following its instruction and leaving again does not help.

   **Evidence:** The screen has no unmount/blur reset. Its mount effect resets **only `failed`**, expressly preserving `error`. The error UI offers retry, without abandonment. Thus a repeatedly failing adoption can block deletion for the rest of the run despite following the advertised remedy.

   The new test directly calls `resetTestLoop()`, bypassing the missing UI path.

3. **MEDIUM Ã¢â‚¬â€ Allowed terminal Test Loop phases retain personal location data after success.**  
   [composition.ts:3777](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3777), [composition.ts:3712](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3712), [testLoopController.ts:286](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/testLoopController.ts:286).

   Sequence: learn a circuit Ã¢â€ â€™ finish its session Ã¢â€ â€™ delete-all while the Test Loop singleton remains `learned`. Disk deletion and catalog refresh succeed, but nothing resets that singleton. It retains `pendingCircuit`, including geometry, and the learning samples.

   `failed` also retains `lastQualified` and `qualifiedStart`; its failure cleanup clears arrays but not those coordinates. A read-only reproduction confirmed both coordinates remain after entering `failed`.

   These phases cannot retry adoption directly, so this is **retained personal data**, not the original wrong-controller retry. Clear terminal Test Loop state as part of successful deletion.

4. **MEDIUM Ã¢â‚¬â€ VIN-bearing Settings drafts can survive deletion.**  
   [SettingsScreen.tsx:330](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/SettingsScreen.tsx:330), [SettingsScreen.tsx:334](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/SettingsScreen.tsx:334).

   Sequence: persisted channel JSON is empty Ã¢â€ â€™ enter an invalid JSON draft containing VIN-bearing channel provenance Ã¢â€ â€™ blur, which fails validation Ã¢â€ â€™ delete-all.

   Both resets leave persisted `enetChannelSpecsJson` equal to `''`. The draft-reset effect sees no field change, so delete-all can return success while the editor still holds and displays the VIN. Correcting the draft and blurring subsequently persists it.

   **Evidence:** Draft state is independent of the settings store; reset depends exclusively on the stored field changing. There is no deletion-generation reset for the editor. This is distinct from the fixed persisted-channel case.

**Tests and verification**

- The changed patch expectations, tagged-channel assertions, journal assertion and learning-phase refusal test would reject the corresponding pre-fix behavior.
- The new read-back test proves the helper detects an explicitly awaited stale write. It does **not** prove composition ordering or protection against writes after read-back.
- The composition database double serializes transactions, but its standalone reads/writes bypass that transaction queue. These tests therefore do not exercise the production gateÃ¢â‚¬â„¢s complete ordering behavior.
- Untested commit claims include pending VIN/bindings invalidation, detection rearming, failed reset persistence, learned-refresh failure propagation, adoption-journal deletion failure, unvalidated-log deletion failure, and error/adopting refusal. Editor reset and terminal Test Loop cleanup are also uncovered.
- I attempted the three changed test files with caching disabled. Vitest could not start tests because its temporary-directory creation was rejected by the read-only environment. **No test-suite pass is claimed.**

**Clean list and remaining qualifications**

- **FIFO claim:** correct for writes already submitted before the read-back. `SqlSettingsStore.persist()` serializes and submits synchronously before its first `await`; the production gate queues calls FIFO. Ordinary preference updates after reset serialize the cleared identity. This does not cover future writes.
- **Active-profile subscription:** its pre-wipe bindings read can run under the first resetÃ¢â‚¬â„¢s generation, but the second reset invalidates that generation. The subsequent refresh reads after the committed wipe.
- **Generation state:** only deletion increments `deviceDataGeneration`. I found no newly introduced result-dropping path outside deletion, or path where the mismatch return itself leaves detection stuck at `attempting`.
- **Abandoned adoption:** `adoptionProgress` can remain non-null after `resetTestLoop()` makes the phase idle. Clearing it is necessary. In one runtime, `ready()` places bootstrap repair before deletion, and the lifecycle lock serializes newer adoption writes. The unconditional journal delete is not a cross-runtime ownership guarantee.
- **Failure reporting:** learned-refresh, identity-read, journal-delete and log-delete failures all prevent success. Verification failure can conservatively report failure even when disk happens to be empty; that is not demonstrated corruption or a false-success bug.
- **Result mutation:** the log failure changes `finalResult.ok` **after active-pointer clearing but before history refresh**, not after history refresh. Consequently history refresh is skipped and may leave stale cached history on this reported-failure path.
- **Policy:** EN/RO broadly agree. Their deletion promises remain too strong given findings 1, 3 and 4. Their instruction to leave Test Loop is unsupported by the UI, and their refusal description omits the persistent `error` phase. Ã¢â‚¬Å“Next connectionÃ¢â‚¬Â also describes eligibility too strongly: detection can skip while the adapter reservation is held.
- **Other behavior changes:** all custom channel JSON is cleared, including manually entered definitions, while policy specifically mentions sweep-tagged channels. The wipe diagnostic key changes from `last_seen_vin` to `vehicle_identity_settings`. Neither is separately identified in the commit message.
tokens used
78,249
FAIL

Reviewed `6d6f90b..d2b26f6`; HEAD matches the fix. No files changed; no agents used.

**REV1 dispositions**

| Finding | Status | Evidence |
|---|---|---|
| H1 Ã¢â‚¬â€ VIN resurrection | **PARTIAL** | [composition.ts:1048](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:1048) rejects reads from earlier generations, and the initial memory reset fixes stale whole-blob writes. However, detection started after the **second** reset can restore VIN after verification. Failing sequence below. |
| H2 Ã¢â‚¬â€ persisted tagged channels survive | **CLOSED** | [deviceDataWipe.ts:45](D:/CODE/APLICTIE_Circuit/apps/mobile/src/persistence/deviceDataWipe.ts:45) clears `enetChannelSpecsJson`; verification counts remaining nonempty strings. The original persisted-channel scenario is fixed. An editor-memory gap remains below. |
| H3 Ã¢â‚¬â€ stale adoption resumes against fallback controller | **CLOSED** | [composition.ts:3777](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3777) refuses the failed-adoption `error` phase. [composition.ts:3711](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3711) clears an abandoned ledger before success. The prescribed UI escape is broken, as detailed below. |
| M4 Ã¢â‚¬â€ learned refresh failure reported as success | **CLOSED** | [composition.ts:3673](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3673) records refresh failure; line 3898 incorporates it into the result. Stale geometry can remain, but success is withheld. |
| M5 Ã¢â‚¬â€ detection disabled until restart | **CLOSED** | [composition.ts:3650](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3650) resets detection to `idle`; obsolete completions cannot set it back to `done`. This restores eligibility at the next trigger, subject to adapter availability. |
| L6 Ã¢â‚¬â€ session-linked settings survive silently | **CLOSED** | [composition.ts:3717](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3717) deletes the adoption journal; [composition.ts:3915](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3915) turns an unvalidated-log deletion failure into aggregate failure. |

**New and residual findings**

1. **HIGH Ã¢â‚¬â€ The second reset permits a fresh VIN read to outlive the final check.**  
   [composition.ts:3668](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3668), [composition.ts:3695](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3695), [composition.ts:1057](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:1057).

   Sequence:
   - Delete-all performs its second identity reset, setting detection to `idle`.
   - While deletion awaits subsequent work, opening Signal Finder starts detection with the **current** generation.
   - Identity read-back returns zero.
   - Detection completes while deletion awaits journal, active-pointer, log, or history operations. Its generation matches, so it restores both `cachedDetectedVin` and `lastSeenVin`.
   - Those remaining operations succeed; delete-all returns `ok: true`.

   **Evidence:** A read-only, in-memory reproduction using the source-extracted reset/detection functions, actual `SqlSettingsStore`, actual FIFO gate and sql.js produced:
   ```
   identityCountAtFinalCheck: 0
   memoryVin: WZ1DB0C04LW000001
   diskVin: WZ1DB0C04LW000001
   vinDetectionState: done
   ```
   This is a component reproduction, not a full composition integration test. The remaining success path contains no further generation change or identity check.

   A generation fence must cover the whole deletion interval; two isolated resets do not provide that fence.

2. **MEDIUM Ã¢â‚¬â€ The refusal tells users to leave Test Loop, but leaving does not abandon it.**  
   [composition.ts:3777](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3777), [TestLoopScreen.tsx:56](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/TestLoopScreen.tsx:56).

   Sequence: adoption fails at the selected-journal write before session start Ã¢â€ â€™ phase becomes `error` Ã¢â€ â€™ leave Test Loop Ã¢â€ â€™ request delete-all. It refuses. Following its instruction and leaving again does not help.

   **Evidence:** The screen has no unmount/blur reset. Its mount effect resets **only `failed`**, expressly preserving `error`. The error UI offers retry, without abandonment. Thus a repeatedly failing adoption can block deletion for the rest of the run despite following the advertised remedy.

   The new test directly calls `resetTestLoop()`, bypassing the missing UI path.

3. **MEDIUM Ã¢â‚¬â€ Allowed terminal Test Loop phases retain personal location data after success.**  
   [composition.ts:3777](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3777), [composition.ts:3712](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3712), [testLoopController.ts:286](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/testLoopController.ts:286).

   Sequence: learn a circuit Ã¢â€ â€™ finish its session Ã¢â€ â€™ delete-all while the Test Loop singleton remains `learned`. Disk deletion and catalog refresh succeed, but nothing resets that singleton. It retains `pendingCircuit`, including geometry, and the learning samples.

   `failed` also retains `lastQualified` and `qualifiedStart`; its failure cleanup clears arrays but not those coordinates. A read-only reproduction confirmed both coordinates remain after entering `failed`.

   These phases cannot retry adoption directly, so this is **retained personal data**, not the original wrong-controller retry. Clear terminal Test Loop state as part of successful deletion.

4. **MEDIUM Ã¢â‚¬â€ VIN-bearing Settings drafts can survive deletion.**  
   [SettingsScreen.tsx:330](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/SettingsScreen.tsx:330), [SettingsScreen.tsx:334](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/SettingsScreen.tsx:334).

   Sequence: persisted channel JSON is empty Ã¢â€ â€™ enter an invalid JSON draft containing VIN-bearing channel provenance Ã¢â€ â€™ blur, which fails validation Ã¢â€ â€™ delete-all.

   Both resets leave persisted `enetChannelSpecsJson` equal to `''`. The draft-reset effect sees no field change, so delete-all can return success while the editor still holds and displays the VIN. Correcting the draft and blurring subsequently persists it.

   **Evidence:** Draft state is independent of the settings store; reset depends exclusively on the stored field changing. There is no deletion-generation reset for the editor. This is distinct from the fixed persisted-channel case.

**Tests and verification**

- The changed patch expectations, tagged-channel assertions, journal assertion and learning-phase refusal test would reject the corresponding pre-fix behavior.
- The new read-back test proves the helper detects an explicitly awaited stale write. It does **not** prove composition ordering or protection against writes after read-back.
- The composition database double serializes transactions, but its standalone reads/writes bypass that transaction queue. These tests therefore do not exercise the production gateÃ¢â‚¬â„¢s complete ordering behavior.
- Untested commit claims include pending VIN/bindings invalidation, detection rearming, failed reset persistence, learned-refresh failure propagation, adoption-journal deletion failure, unvalidated-log deletion failure, and error/adopting refusal. Editor reset and terminal Test Loop cleanup are also uncovered.
- I attempted the three changed test files with caching disabled. Vitest could not start tests because its temporary-directory creation was rejected by the read-only environment. **No test-suite pass is claimed.**

**Clean list and remaining qualifications**

- **FIFO claim:** correct for writes already submitted before the read-back. `SqlSettingsStore.persist()` serializes and submits synchronously before its first `await`; the production gate queues calls FIFO. Ordinary preference updates after reset serialize the cleared identity. This does not cover future writes.
- **Active-profile subscription:** its pre-wipe bindings read can run under the first resetÃ¢â‚¬â„¢s generation, but the second reset invalidates that generation. The subsequent refresh reads after the committed wipe.
- **Generation state:** only deletion increments `deviceDataGeneration`. I found no newly introduced result-dropping path outside deletion, or path where the mismatch return itself leaves detection stuck at `attempting`.
- **Abandoned adoption:** `adoptionProgress` can remain non-null after `resetTestLoop()` makes the phase idle. Clearing it is necessary. In one runtime, `ready()` places bootstrap repair before deletion, and the lifecycle lock serializes newer adoption writes. The unconditional journal delete is not a cross-runtime ownership guarantee.
- **Failure reporting:** learned-refresh, identity-read, journal-delete and log-delete failures all prevent success. Verification failure can conservatively report failure even when disk happens to be empty; that is not demonstrated corruption or a false-success bug.
- **Result mutation:** the log failure changes `finalResult.ok` **after active-pointer clearing but before history refresh**, not after history refresh. Consequently history refresh is skipped and may leave stale cached history on this reported-failure path.
- **Policy:** EN/RO broadly agree. Their deletion promises remain too strong given findings 1, 3 and 4. Their instruction to leave Test Loop is unsupported by the UI, and their refusal description omits the persistent `error` phase. Ã¢â‚¬Å“Next connectionÃ¢â‚¬Â also describes eligibility too strongly: detection can skip while the adapter reservation is held.
- **Other behavior changes:** all custom channel JSON is cleared, including manually entered definitions, while policy specifically mentions sweep-tagged channels. The wipe diagnostic key changes from `last_seen_vin` to `vehicle_identity_settings`. Neither is separately identified in the commit message.

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'rg --files -g AGENTS.md -g HANDOFF.md; git rev-parse HEAD; git diff d2b26f6..1dee7b2 -- apps docs; git diff 6d6f90b..1dee7b2 --stat' in D:\CODE\APLICTIE_Circuit
 succeeded in 24ms:
 .foreman/scratch/p18-codex-rev1-out.txt            | 5148 ++++++++++++
 .foreman/scratch/p18-codex-rev2-out.txt            | 8672 ++++++++++++++++++++
 .foreman/scratch/ticket-p18-rev2-codex.md          |   13 +
 apps/mobile/src/persistence/deviceDataWipe.ts      |   48 +-
 apps/mobile/src/session/composition.ts             |  176 +-
 apps/mobile/src/ui/screens/SettingsScreen.tsx      |    7 +
 .../mobile/test/persistence/deviceDataWipe.test.ts |   29 +-
 .../session/composition.circuitSelection.test.ts   |   11 +
 .../session/composition.deleteAllVinFence.test.ts  |  134 +
 .../test/session/composition.testLoop.test.ts      |   47 +
 docs/legal/privacy-policy.en.md                    |   14 +-
 docs/legal/privacy-policy.ro.md                    |   16 +-
 12 files changed, 14270 insertions(+), 45 deletions(-)

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "Get-Content apps/mobile/AGENTS.md; Get-Content docs/HANDOFF.md -TotalCount 75; rg -n 'unlockedRebuildProductionController|teardownGeneration|tearDownLearnPhase|disposeTestLoopProvider|releaseTestLoopGForce|stopTelemetryRecording|resetTestLoop|testLoopController.subscribe|lifecycleLock|maybeDetectVehicleFromVin' apps/mobile/src/session/composition.ts; "'$p='"'apps/mobile/src/session/composition.ts'; "'$s=Get-Content $p; $s[3630..3980]; $s[1015..1110]' in D:\CODE\APLICTIE_Circuit
 succeeded in 36ms:
// verify-empty logic lives in `@circuit/core`'s `deleteAllUserData` (unit
// tested there; the mobile UI itself is not unit-testable in this repo).
// Refreshes the shared `sessionHistoryStore` cache on success so
// `SessionHistoryScreen` reflects the deletion without a manual reload.
// ---------------------------------------------------------------------------

/** M3 fix: `deleteAllStoredUserData()`'s own return shape, layered on top of `@circuit/core`'s `DeleteUserDataResult` -- `failedCircuitIds`/`errorText` cannot live on that type itself (`packages/core/**` is out of this ticket's write set). `errorText` is `null` whenever `ok` is `true`. */
export interface AggregatedDeleteUserDataResult extends DeleteUserDataResult {
  /** Bundled circuit ids whose `deleteAllUserData()` call REJECTED (not just returned `ok: false`) -- empty unless a per-circuit delete actually threw. */
  failedCircuitIds: string[];
  errorText: string | null;
  /**
   * Set when the wipe was REFUSED outright -- nothing was read, deleted, or
   * verified on those paths. `'SESSION_ACTIVE'`: a session is genuinely
   * mid-drive (CN-FIX4, facade boundary amendment). `'DEV_REPLAY_ACTIVE'`: a
   * `__DEV__` replay controller is installed (CN-FIX5, closing amendment).
   * `'TEST_LOOP_ACTIVE'`: a Test Loop is learning a track or saving one.
   */
  reason?: 'SESSION_ACTIVE' | 'DEV_REPLAY_ACTIVE' | 'TEST_LOOP_ACTIVE';
}

/**
 * The in-memory half of {@link wipeDeviceUserData}, run BEFORE the disk wipe:
 * the settings store rewrites its whole blob on every `update()`, so the VIN
 * has to leave memory first. Any settings write queued before this lands
 * ahead of the wipe's transaction and is scrubbed by it; every write after it
 * carries no VIN. The generation bump makes a VIN read or a bindings read
 * already in flight drop its result instead of restoring it.
 */
function forgetVehicleIdentityInMemory(): void {
  deviceDataGeneration += 1;
  vinDetectionState = 'idle';
  cachedDetectedVin = null;
  setVinAutoDetectNotice(null);
  settingsStore.update(vehicleIdentityResetPatch(settingsStore.getSettings()));
  vehicleProfileBindingsCache = [];
  sessionVehicleSnapshots.clear();
}

/**
 * Brings every in-memory copy of the learned circuits and the bindings back in
 * line with the disk after the wipe. Resolves `false` when the learned
 * circuits could not be re-read (the deleted geometry may then still be
 * selectable) or the stored settings still carry the vehicle's identity:
 * either way delete-all must not report success.
 */
async function resyncAfterDeviceWipe(): Promise<boolean> {
  // Again, now the wipe has committed: a VIN or bindings read that finished
  // while it ran may have put the old values back in memory.
  forgetVehicleIdentityInMemory();
  let learnedOk = true;
  memoryLearnedCircuits = [];
  if (learnedCircuitStore !== null) {
    try {
      await learnedCircuitStore.refresh();
    } catch (error) {
      console.warn('[composition] could not refresh learned circuits after delete-all', error);
      learnedOk = false;
    }
  }
  publishLearnedCircuits();
  const selectedCircuitId = settingsStore.getSettings().selectedCircuitId;
  if (circuitCatalog.get(selectedCircuitId) === null) {
    const fallback = circuitCatalog.get(TMR_CIRCUIT_PROFILE.circuitId);
    try {
      if (fallback !== null) await unlockedApplySelection(fallback);
    } catch (error) {
      console.warn('[composition] could not fall back to the default circuit after delete-all', error);
    }
  }
  await refreshVehicleProfileBindingsCache();
  // The last word on the VIN: read back after every settings write queued so
  // far, so a write that carried the old blob cannot hide behind the wipe.
  let identityOk = true;
  if (db !== null) {
    try {
      identityOk = (await countStoredVehicleIdentity(db)) === 0;
    } catch (error) {
      console.warn('[composition] could not verify the vehicle identity was deleted', error);
      identityOk = false;
    }
  }
  return learnedOk && identityOk;
}

/**
 * Drops what an abandoned Test Loop adoption left behind: its in-memory
 * ledger and its journal row. Only called once the sessions are gone -- the
 * journal names a session and a circuit, and a retry must never resume onto a
 * circuit this wipe deleted. Resolves `false` when the row could not be
 * removed.
 */
async function forgetAbandonedAdoption(): Promise<boolean> {
  adoptionProgress = null;
  adoptionJournalId = null;
  adoptionJournalOwned = null;
  if (db === null) return true;
  try {
    await db.runAsync('DELETE FROM settings WHERE key = ?', [ADOPTION_JOURNAL_KEY]);
    return true;
  } catch (error) {
    console.warn('[composition] could not clear the test-loop adoption journal', error);
    return false;
  }
}

export async function deleteAllStoredUserData(): Promise<AggregatedDeleteUserDataResult> {
  // Lifecycle lock amendment (binding): delete-all wipes the very data the
  // recovery/selection/rebuild operations read and write, so it runs as one
  // section of the SAME lock they do -- never interleaved with a resume, a
  // selection change, or a controller rebuild.
  return lifecycleLock.run(unlockedDeleteAllStoredUserData);
}

async function unlockedDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDataResult> {
  deviceWipeInProgress = true;
  try {
    return await runDeleteAllStoredUserData();
  } finally {
    deviceWipeInProgress = false;
  }
}

async function runDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDataResult> {
  const { repository: repo } = await ready();
  // C fix (contracts.md's facade boundary amendment, binding, ticket
  // CN-FIX4): durability. Two things had to change for "delete-all means
  // deleted" to actually hold.
  //
  // 1. REFUSE while a session is genuinely mid-drive. A live controller goes
  //    on writing checkpoints/laps/PBs by design, so a wipe underneath it
  //    would be re-populated moments later -- and silently tearing the
  //    driver's session down instead is not this action's job.
  if (activeController !== null && MID_SESSION_STATES.has(currentControllerState(activeController))) {
    return {
      ok: false,
      remainingSessionCount: 0,
      referenceLapCleared: false,
      failedCircuitIds: [],
      reason: 'SESSION_ACTIVE',
      errorText: 'a session is currently active -- end it before deleting all data',
    };
  }
  // 1b. REFUSE while a `__DEV__` DevReplay controller is installed (CN-FIX5
  //    item 3, closing amendment, binding). A replay controller sits in
  //    `calibrating`/`calibrationReview`/`armed` -- none of them
  //    `MID_SESSION_STATES` -- and step 2's rebuild deliberately no-ops
  //    while it owns `facade`, so the wipe would leave a live controller
  //    holding a session id that persists its session/checkpoint right back
  //    over the deleted data. Durability wins over convenience on the dev
  //    path: leave the replay, refuse the delete.
  if (controller !== null && activeController !== controller) {
    return {
      ok: false,
      remainingSessionCount: 0,
      referenceLapCleared: false,
      failedCircuitIds: [],
      reason: 'DEV_REPLAY_ACTIVE',
      errorText: 'a developer replay or mock session is active -- leave the Dev Replay screen before deleting all data',
    };
  }
  // 1c. REFUSE while a Test Loop is learning or saving a track: a wipe
  //    underneath would let the adoption store the circuit again, or resume
  //    onto the fallback circuit's controller while writing the out-lap for
  //    the one just deleted. Stopping the learn phase on the Test Loop screen
  //    ends the first; the second finishes on its own.
  const testLoopPhase = testLoopController.snapshot().phase;
  if (testLoopPhase === 'learning' || testLoopPhase === 'adopting') {
    return {
      ok: false,
      remainingSessionCount: 0,
      referenceLapCleared: false,
      failedCircuitIds: [],
      reason: 'TEST_LOOP_ACTIVE',
      errorText: 'a test loop is learning or saving a track -- stop it before deleting all data',
    };
  }
  // 1d. Every other Test Loop phase is ABANDONED instead. `error` holds a
  //    learned track waiting for a Retry that must not run onto a circuit
  //    this wipe deletes; `learned` and `failed` still hold the traced
  //    geometry and coordinates of where the driver drove. The learn phase is
  //    torn down before step 2 rebuilds the controller, so the new controller
  //    is never built on a provider that is about to stop.
  if (testLoopPhase !== 'idle') {
    testLoopController.reset();
    teardownGeneration += 1;
    await unlockedTearDownLearnPhase();
    // Its learned track is gone, so its ledger can never be resumed. The
    // journal row stays until the wipe succeeds: until then it is what lets
    // the next launch repair the half-adopted state.
    adoptionProgress = null;
    adoptionJournalId = null;
    adoptionJournalOwned = null;
  }
  // 2. Otherwise: drop any pending recovery and REPLACE the production
  //    controller with a fresh, idle one BEFORE deleting anything. A
  //    terminal (`sessionComplete`) controller still holds a `sessionId`
  //    and a full lap set, so anything that later touches it --
  //    `checkpointNow()` from a background transition, a straggler
  //    `endSession()` persistence path -- would re-create rows this wipe
  //    just removed. A fresh controller has no session identity at all, so
  //    there is nothing left to re-persist. (`endSession()` itself is now
  //    serialized on this same lock, so an in-flight one has already
  //    completed by the time this section runs.)
  setPendingRecovery(null);
  setRecoveryNotice(null);
  await unlockedRebuildProductionController();
  // Multi-circuit selection addendum (ticket CN-W3): delete-all spans EVERY
  // bundled circuit, not just the currently selected one -- `deleteUserData`
  // itself is a single per-userId wipe (not circuit-scoped), so this loop's
  // real job is the per-circuit VERIFY-EMPTY check; success requires every
  // bundled circuit to come back empty.
  //
  // M3 fix (binding): each circuit's call is its own try/catch -- a REJECTED
  // per-circuit `deleteAllUserData()` (e.g. a transient SQLite error) no
  // longer aborts the loop; every remaining bundled circuit is still
  // attempted, and the rejection is reflected in the aggregate `ok: false`
  // plus `failedCircuitIds`/`errorText` instead of silently stopping partway
  // through (previously leaving later circuits' data untouched with no
  // indication why).
  const perCircuitResults: DeleteUserDataResult[] = [];
  const failedCircuitIds: string[] = [];
  for (const summary of circuitCatalog.list()) {
    try {
      perCircuitResults.push(
        await deleteAllUserData(repo, LOCAL_USER_ID, summary.circuitId, summary.layoutId, summary.layoutVersion),
      );
    } catch (error) {
      console.warn(`[composition] deleteAllStoredUserData: circuit "${summary.circuitId}" failed`, error);
      failedCircuitIds.push(summary.circuitId);
    }
  }
  const perCircuitOk = failedCircuitIds.length === 0 && perCircuitResults.every((r) => r.ok);
  const result: DeleteUserDataResult = {
    ok: perCircuitOk,
    remainingSessionCount: perCircuitResults.reduce((sum, r) => sum + r.remainingSessionCount, 0),
    referenceLapCleared: perCircuitResults.length > 0 && perCircuitResults.every((r) => r.referenceLapCleared),
  };
  // F7 fix (WPT3, binding): `telemetry_samples` (Telemetry addendum) is a
  // mobile-owned table (`persistence/telemetrySchema.ts`) that
  // `@circuit/core`'s `deleteAllUserData`/`LocalSessionRepository.deleteUserData`
  // (packages/core, out of this ticket's write set) has no knowledge of at
  // all -- delete it here, in the SAME flow, and verify it actually landed
  // empty before ever reporting success, so the UI's "All stored data
  // deleted" banner is never shown while up to 200,000 rows/session remain.
  //
  // N4 fix (lifecycle lock amendment, binding, ticket CN-FIX3): this step is
  // NO LONGER gated on the per-circuit aggregate. A rejected circuit delete
  // used to suppress the telemetry DELETE + verify-empty entirely, leaving up
  // to 200,000 rows/session on disk after the user asked for a full wipe. The
  // telemetry step always runs; the aggregate `ok` is `circuitsOk &&
  // telemetryOk`, and `errorText` names whichever half failed.
  let telemetryOk = true;
  if (db !== null) {
    // N1-confirm residue fix (LEAD): a session's final telemetry flush can
    // still be in flight when the user reaches delete-all -- await any
    // in-progress shutdown first, then run DELETE + verify while HOLDING the
    // shared write gate, so no straggler batch INSERT can land between (or
    // after) them. `stopTelemetryRecording()` is an idempotent no-op when
    // nothing is recording.
    await stopTelemetryRecording();
    const database = db;
    try {
      // P10B H5-B: expressed as a TRANSACTION rather than as a gate hold.
      // The gated database now acquires the gate around every call made
      // through it, so holding it here as well would deadlock -- and the
      // DELETE + verify pair needs to be indivisible anyway, which is what
      // `withTransactionAsync` (and only it) actually provides.
      let remainingRows = 1;
      await database.withTransactionAsync(async (tx) => {
        await tx.runAsync('DELETE FROM telemetry_samples');
        const remaining = await tx.getAllAsync<{ count: number }>(
          'SELECT COUNT(*) AS count FROM telemetry_samples',
        );
        remainingRows = remaining[0]?.count ?? 0;
      });
      telemetryOk = remainingRows === 0;
    } catch (error) {
      // N4 (binding): symmetric with the per-circuit try/catch above -- a
      // rejected telemetry delete/verify is reported as `ok: false` plus the
      // fixed "telemetry" phrase in `errorText`, never as raw exception text
      // and never as a rejection escaping into the UI's delete handler.
      console.warn('[composition] deleteAllStoredUserData: telemetry deletion failed', error);
      telemetryOk = false;
    }
  }
  // HANDOFF release blocker: the VIN, the vehicle bindings and sweep
  // records, and the learned circuits live outside every table the steps
  // above touch. Always attempted, like the telemetry step; a learned circuit
  // a surviving session still needs is kept and reported as remaining.
  forgetVehicleIdentityInMemory();
  let deviceDataOk = true;
  if (db !== null) {
    try {
      const wipe = await wipeDeviceUserData(db);
      deviceDataOk = wipe.ok;
      if (!wipe.ok) {
        console.warn('[composition] deleteAllStoredUserData: device data remained', wipe.remaining);
      }
    } catch (error) {
      console.warn('[composition] deleteAllStoredUserData: device data deletion failed', error);
      deviceDataOk = false;
    }
  }
  if (!(await resyncAfterDeviceWipe())) deviceDataOk = false;
  const sessionDataOk = result.ok && telemetryOk && deviceDataOk;
  // Records ABOUT the sessions go only once the sessions themselves are gone:
  // while one survives, its label and any adoption journal still describe it.
  let sessionRecordsOk = true;
  if (sessionDataOk && !(await forgetAbandonedAdoption())) sessionRecordsOk = false;
  const finalResult: DeleteUserDataResult = { ...result, ok: sessionDataOk && sessionRecordsOk };
  // M4 fix (binding): a leftover active-session pointer is meaningless once
  // all stored data is gone -- cleared together, the SAME way session
  // end/discard/vanished-checkpoint do (`setActiveSession(db, null)`).
  if (finalResult.ok && db !== null) await setActiveSession(db, null);
  // Ticket P7R E2: the unvalidated-matching log is a list of facts about
  // sessions that no longer exist -- cleared with them, for the same reason
  // the active-session pointer above is. A row that cannot be removed is
  // reported: delete-all promises the records are gone, not that it tried.
  if (finalResult.ok) {
    unvalidatedMatchingSessionIds = new Set();
    if (db !== null && !(await clearUnvalidatedMatchingSessionIds(db))) {
      console.warn('[composition] could not clear the unvalidated-matching log');
      sessionRecordsOk = false;
      finalResult.ok = false;
    }
  }
  if (sessionDataOk && historyStore !== null) await historyStore.refresh();

  const errorText = finalResult.ok
    ? null
    : [
        failedCircuitIds.length > 0 ? `circuits rejected: ${failedCircuitIds.join(', ')}` : null,
        !perCircuitOk && failedCircuitIds.length === 0 ? 'one or more circuits did not verify empty' : null,
        !telemetryOk ? 'telemetry rows remained or could not be deleted' : null,
        !deviceDataOk ? 'vehicle or learned-circuit data remained or could not be deleted' : null,
        !sessionRecordsOk ? 'session records remained or could not be deleted' : null,
      ]
        .filter((part): part is string => part !== null)
        .join('; ') || 'delete-all failed';

  return { ...finalResult, failedCircuitIds, errorText };
}

// ---------------------------------------------------------------------------
// DevReplayScreen support (MUST DO #6, __DEV__ only).
//
// F4 fix (C6 residue): `startDevReplaySession` / `restoreProductionFacade` /
// `useMockFacadeForDevReplay` all mutate the SAME shared state
// (`replayController`/`activeController`/`facadeWrapper`'s inner) but
// previously ran independently -- a rapid start/restore/start "storm"
 * `Promise.race` itself attaches a handler to every promise it is given, so
 * it can never surface as an unhandled rejection, and this function has
 * already returned by then.
 */
export async function maybeDetectVehicleFromVin(): Promise<string | null> {
  if (deviceWipeInProgress) return null; // not marked done: the next trigger after delete-all reads again.
  if (vinDetectionState !== 'idle') return cachedDetectedVin;
  const settings = settingsStore.getSettings();
  // Q1 (binding): ENET only -- the ELM327 session has no mode-09 support.
  if (settings.adapterType !== 'enet') return null;
  if (settings.enetHost.trim() === '') return null; // nothing configured to connect to.
  if (sharedEnetAdapterReservation.holder() !== null) return null; // never steals a running flow's adapter.
  const token = sharedEnetAdapterReservation.tryAcquire('signalFinder');
  if (token === null) return null; // lost the race to acquire -- another trigger point retries later.

  vinDetectionState = 'attempting';
  const generation = deviceDataGeneration;
  const transport = new EnetTcpTransport({ host: settings.enetHost, port: settings.enetPort });
  let vin: string | null = null;
  try {
    const attempt = (async (): Promise<string | null> => {
      await transport.connect();
      const channel = createRawUdsChannel(transport, settings.enetTesterAddress, VIN_READ_ECU);
      return readVinFromChannel(channel);
    })();
    const timedOut = new Promise<'timeout'>((resolve) => {
      setTimeout(() => resolve('timeout'), VIN_READ_OVERALL_TIMEOUT_MS);
    });
    const raced = await Promise.race([attempt, timedOut]);
    vin = raced === 'timeout' ? null : raced;
  } catch (error) {
    console.warn('[composition] the one-shot VIN read failed', error);
    vin = null;
  } finally {
    await transport.close().catch(() => undefined);
    sharedEnetAdapterReservation.release(token);
  }

  // Delete-all ran (or is running) while this read was in flight: it has
  // already put detection back to 'idle', so the next connection reads again.
  if (generation !== deviceDataGeneration || deviceWipeInProgress) return null;
  vinDetectionState = 'done';
  cachedDetectedVin = vin;
  if (vin !== null) {
    // Q2 (binding): logged so a pattern can be added LATER from the real
    // car -- never invented here. R4 fix (binding, LOW): MASKED -- a VIN is
    // personal data, and the full value is never written to a log line; the
    // driver reads it in full only on the Signal Finder screen / `lastSeenVin`.
    console.info(`[composition] VIN detected: ${maskVin(vin)}`);
    settingsStore.update({ lastSeenVin: vin });
    applyVinAutoSelect(vin);
  }
  return vin;
}

const baseTelemetryProvider: TelemetryProvider = createTelemetryProvider({
  settingsStore,
  monotonicNow: () => telemetryClock.now(),
  isDev: telemetryIsDev,
  readVehicleProfileBindings: () => vehicleProfileBindingsCache,
});

/**
 * Thin wrapper, not a second implementation: every method delegates straight
 * to {@link baseTelemetryProvider}, EXCEPT `start()`, which also fires a
 * defensive cache refresh (fire-and-forget -- see (c) above) before
 * delegating. Both of this app's `start()` call sites --
 * `startTelemetryRecording()` below and `TelemetryScreen.tsx`'s manual
 * monitor -- go through this SAME exported singleton, so neither needs its
 * own refresh call.
 */
export const telemetryProvider: TelemetryProvider = {
  ...baseTelemetryProvider,
  start(): void {
    void refreshVehicleProfileBindingsCache();
    baseTelemetryProvider.start();
    // Ticket P4q (binding): one of this app's two VIN-detection trigger
    // points ("when ... telemetry connects") -- fire-and-forget, and called
    // AFTER `baseTelemetryProvider.start()` on purpose: that call may itself
    // synchronously acquire the SAME shared reservation (owner `'provider'`)
    // before yielding, and this must never race it for the claim -- "never
    // blocks or steals the adapter from a running flow". `holder()` is
    // therefore whatever telemetry's own start just left it as; a no-op
    // (skipped, not marked done, so a LATER trigger still gets its own
    // chance) whenever that reservation is held.
    void maybeDetectVehicleFromVin();
  },
};

// ---------------------------------------------------------------------------
// G-force telemetry (Telemetry addendum Ã¢â‚¬â€ channel revision, binding):
// `gforceProvider.ts`'s own module doc comment covers its math/portrait-mount
// assumption/unit handling in full. SAME `telemetryClock` as the OBD
// `telemetryProvider` above, so every telemetry channel (OBD + accelerometer)
// shares one time base. Session-scoped, started/stopped alongside the OBD
// provider by `startTelemetryRecording()`/`stopTelemetryRecording()` below --

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command '$s=Get-Content apps/mobile/src/session/composition.ts; $s[1919..1987]; $s[2390..2497]; $s[2685..2940]; $s=Get-Content apps/mobile/src/ui/screens/SettingsScreen.tsx; $s[315..365]; $s[445..500]; Get-Content apps/mobile/src/ui/screens/TestLoopScreen.tsx; rg -n '"'reset\\(|retry|pendingCircuit|lastQualified|qualifiedStart' apps/mobile/src/session/testLoopController.ts; Get-Content apps/mobile/src/session/lifecycleLock.ts" in D:\CODE\APLICTIE_Circuit
 succeeded in 80ms:
  // before the controller it was recording alongside is disposed -- in the
  // normal flow `onSessionEnded` (above) already did this; this guards the
  // 'error' terminal-state path, which does not run through `endSession()`.
  // Awaited (F2 fix) so the final flush actually lands before disposal
  // proceeds, instead of racing ahead of it.
  //
  // P5d-FIX1 H1: skipped during a Test Loop handover. Nothing is recording
  // through a `TelemetryRecorder` at that moment (the learn phase BUFFERS,
  // and the recorder is created a few lines later, for the session this
  // rebuild is installing), so the only thing this call would do is stop the
  // OBD provider that is mid-stream -- a blink in the very continuity this
  // rebuild exists to preserve.
  if (!testLoopHandoverActive) await stopTelemetryRecording();
  await staleController.dispose();
  staleFacade?.dispose();
  installProductionController();
  // CN-FIX6 (contracts.md's "Multi-circuit selection Ã¢â‚¬â€ provider ownership
  // amendment", binding, user finding): THIS layer owns the GNSS provider
  // singleton, so the provider is stopped here, after the fresh controller
  // is installed.
  //
  // Why it is needed: `SessionController.dispose()` stops only a provider
  // the disposed controller itself had running (`providerRunning`), and a
  // `start()` aborted by disposal deliberately never stops a possibly-shared
  // provider (CN-FIX5 item 2 -- core cannot know who owns it). Both are
  // right core-side, and both can leave the OS watcher running with no
  // session behind it: a lit location indicator and a battery drain the user
  // can see, which is exactly the residual this closes.
  //
  // Why it is safe: the controller just installed is idle BY CONSTRUCTION
  // (nothing has started it), the previous controller's pending `start()` --
  // if any -- is serialized by the provider ahead of this stop, and every
  // command that starts a session is itself a locked section queued BEHIND
  // this one, so it restarts the provider through `ensureProviderRunning()`
  // as usual. Idempotent: stopping an already-stopped provider is a no-op.
  //
  // Failure is logged, never thrown: a provider that cannot be stopped (a
  // revoked permission, an OS hiccup) must not fail the selection/recovery/
  // delete-all operation that drove this rebuild.
  try {
    // P5d-FIX1 H1 (binding, Codex P5d-REV1 HIGH 1): the ONE exception to the
    // ownership stop. While a Test Loop is being learned or handed over, the
    // watcher is NOT session-less -- the learn phase owns it, its fixes are
    // being recorded, and the session about to start on the just-learned
    // circuit is what this very rebuild exists to install. Stopping it here
    // would blind the recording for the length of the rebuild, which is
    // exactly the gap this fix wave removed. Everything the amendment guards
    // against (a lit indicator with nothing behind it) still holds: the
    // Test Loop path stops the provider itself when a learn phase ends
    // without a track (`stopTestLoop`), and the session's own end still runs
    // the ordinary teardown.
    if (!testLoopHandoverActive) await gnssProvider?.stop();
  } catch (error) {
    console.warn('[composition] stopping the GNSS provider after a controller rebuild failed', error);
  }
}

/**
 * F3 fix: `coachingConfig()` is baked into a `SessionController` for its own
 * whole lifetime (`SessionControllerDeps.coaching`'s own doc comment) --
 * previously, flipping the coaching toggle before the first session (or
 * while idle between sessions) had NO effect until some UNRELATED
 * terminal-state rebuild happened to fire later; the UI showed a fresh
 * coaching slot immediately, but it stayed empty. Rebuild immediately
 * provider reference, so a rebuild AFTER the handover cleans up normally.
 */
let testLoopHandoverActive = false;
/**
 * P5d-FIX2 N2: which adoption steps have already happened, so a retry resumes
 * instead of repeating them. Cleared when the adoption completes.
 */
interface AdoptionProgress {
  circuitId: string;
  storedProfile?: CircuitProfile;
  selected?: boolean;
  sessionId?: string;
  recordingStarted?: boolean;
  outLapSaved?: boolean;
}
let adoptionProgress: AdoptionProgress | null = null;
/** P5d-FIX3 F11: bumped by every new learn phase, so a teardown queued for an older one is a no-op. */
let teardownGeneration = 0;
/** The teardown currently running -- a second caller awaits this one instead of starting another. */
let teardownInFlight: Promise<void> | null = null;
/** OBD/IMU samples recorded during the learn phase, before a session id exists to tag them with. */
let testLoopTelemetryBuffer: TelemetrySample[] = [];
let unsubscribeTestLoopTelemetry: (() => void) | null = null;
let unsubscribeTestLoopGForce: (() => void) | null = null;
/** Whether the learn phase holds its own G-force reference (released when the session takes its own). */
let testLoopHoldsGForce = false;
/** Bound on the learn-phase telemetry backlog -- an hour of 10 Hz channels. */
const MAX_TEST_LOOP_TELEMETRY_SAMPLES = 360_000;
let testLoopTelemetryDropped = 0;

/** Republishes the catalog's learned registry from the store (plus any memory-only entries). */
function publishLearnedCircuits(): void {
  const stored: LearnedCatalogEntry[] =
    learnedCircuitStore === null
      ? []
      : learnedCircuitStore.entries().map((entry) => ({
          circuit: { profile: entry.profile, runtime: entry.runtime, corners: entry.corners },
          listed: entry.record.saved,
        }));
  setLearnedCircuits([...stored, ...memoryLearnedCircuits]);
}

/**
 * The learn phase's state machine. Created once at module load: it holds no
 * database and no provider, so it is safe to exist before bootstrap.
 *
 * `makeCircuitId` is a UUID (P5d-FIX1 item 10) -- learned circuits are keyed
 * by it in the database, and a timestamp-plus-random id invites exactly the
 * collision that an `INSERT OR REPLACE` would silently resolve by destroying
 * somebody's saved circuit.
 */
const testLoopController = new TestLoopController({
  nowUtc: () => new Date().toISOString(),
  makeCircuitId: () => `learned-${newLearnedCircuitId()}`,
  makeDisplayName: (createdAtUtc) =>
    defaultLearnedCircuitName(settingsStore.getSettings().language, createdAtUtc),
  onLearned: (circuit) => adoptLearnedCircuit(circuit),
});

/** RFC-4122 v4 where the runtime offers one, and a random-hex fallback where it does not. */
function newLearnedCircuitId(): string {
  const cryptoApi = (globalThis as { crypto?: { randomUUID?: () => string } }).crypto;
  if (typeof cryptoApi?.randomUUID === 'function') return cryptoApi.randomUUID();
  const hex = (length: number): string =>
    Array.from({ length }, () => Math.floor(Math.random() * 16).toString(16)).join('');
  return `${hex(8)}-${hex(4)}-4${hex(3)}-a${hex(3)}-${hex(12)}`;
}

/**
 * P5d-FIX2 N6 (Codex P5d-REV2): a learn phase that gives up on its own -- the
 * sample cap, most of all -- must not leave the GNSS watcher and the channel
 * buffers running behind a screen that now says it failed. The teardown is the
 * same one `stopTestLoop()` performs, and it is idempotent, so the two paths
 * cannot fight.
 */
testLoopController.subscribe((snapshot) => {
  if (snapshot.phase !== 'failed') return;
  if (testLoopProvider === null && !testLoopHoldsGForce) return;
  void tearDownLearnPhase().catch((error) => {
    console.warn('[composition] tearing down a failed learn phase failed', error);
  });
});

/**
 * The ONE teardown of a learn phase (P5d-FIX3 F11, Codex P5d-REV3): detach the
 * learn-phase recording, release its holds, stop the buffering provider.
 *
 * Memoized and generation-guarded, and it takes `lifecycleLock` itself. Two
 * paths reach it for the same failure -- `stopTestLoop()` and the auto-teardown
 * subscription that the failed phase fires -- and before this they could both
 * be shutting the GLOBAL providers down at once. Now the second caller awaits
 * the first, and a teardown queued behind a learn phase that has meanwhile
 * been restarted does nothing at all (the generation moved on).
 */
function tearDownLearnPhase(): Promise<void> {
  if (teardownInFlight !== null) return teardownInFlight;
  const generation = teardownGeneration;
  const run = lifecycleLock
    .run(async () => {
      if (generation !== teardownGeneration) return;
      await unlockedTearDownLearnPhase();
    })
    .finally(() => {
      if (teardownInFlight === run) teardownInFlight = null;
    });
  teardownInFlight = run;
  return run;
}
    telemetryProvider.start();
  } catch (error) {
    console.warn('[composition] telemetryProvider.start() threw synchronously (test loop)', error);
  }
  if (!testLoopHoldsGForce) {
    acquireGForce();
    testLoopHoldsGForce = true;
  }
}

/** Detaches the learn-phase buffers. The providers themselves keep running -- the session owns them now. */
function detachTestLoopRecording(): void {
  unsubscribeTestLoopTelemetry?.();
  unsubscribeTestLoopTelemetry = null;
  unsubscribeTestLoopGForce?.();
  unsubscribeTestLoopGForce = null;
}

/** Releases the learn phase's own G-force hold (the session takes its own in `startTelemetryRecording`). */
async function releaseTestLoopGForce(): Promise<void> {
  if (!testLoopHoldsGForce) return;
  testLoopHoldsGForce = false;
  try {
    await releaseGForce();
  } catch (error) {
    console.warn('[composition] releasing the test-loop G-force hold failed', error);
  }
}

/**
 * Begins a learn phase: starts the GNSS provider through the buffering
 * wrapper, starts the OBD/IMU recording, and feeds every fix to
 * `TestLoopController`. Refuses while a session is live -- learning a track
 * and timing laps are two different uses of the same provider.
 */
export async function startTestLoop(): Promise<
  { ok: true } | { ok: false; reason: 'not-ready' | 'session-active' }
> {
  await ready();
  return lifecycleLock.run(async () => {
    if (gnssProvider === null) return { ok: false as const, reason: 'not-ready' as const };
    if (controller !== null && sessionIsActive(currentControllerState(controller))) {
      return { ok: false as const, reason: 'session-active' as const };
    }
    await disposeTestLoopProvider();
    // A fresh learn phase: any teardown still queued for the previous one must
    // not reach in and stop the providers this one is about to use.
    teardownGeneration += 1;
    const provider = new TestLoopLocationProvider(gnssProvider, (sample) =>
      testLoopController.feed(sample),
    );
    testLoopProvider = provider;
    testLoopController.start();
    await provider.start();
    startTestLoopRecording();
    return { ok: true as const };
  });
}

/**
 * Ends a learn phase that never closed a loop. A loop that WAS learned is
 * already a running session by the time this can be called, and is ended
 * through the ordinary `facade.endSession()` instead.
 */
export async function stopTestLoop(): Promise<TestLoopSnapshot> {
  const snapshot = testLoopController.stop();
  if (snapshot.phase !== 'learned' && snapshot.phase !== 'adopting') {
    await tearDownLearnPhase();
  }
  return snapshot;
}

/** Leaves Test Loop mode entirely (screen dismissed without a learned track). */
export async function resetTestLoop(): Promise<void> {
  await stopTestLoop();
  testLoopController.reset();
}

/** P5d-FIX1 H3: retries a handover that failed, on the SAME learned geometry. */
export function retryTestLoopAdoption(): void {
  testLoopController.retryAdopt();
}

/** Stops and clears the buffering provider, handing the GNSS watcher back to the plain singleton. */
async function disposeTestLoopProvider(): Promise<void> {
  const provider = testLoopProvider;
  testLoopProvider = null;
  testLoopHandoverActive = false;
  if (provider === null) return;
  try {
    await provider.stop();
  } catch (error) {
    console.warn('[composition] stopping the test-loop provider failed', error);
  }
}

/**
 * The moment lap 1 closes (P5d-FIX1 H1 + H3). Everything below is ONE
 * `lifecycleLock` section, and every step is awaited: the driver is only told
 * the track was learned once it is genuinely persisted, registered, selected
 * and being timed. A throw anywhere here surfaces as the controller's `error`
 * phase, with a retry -- never as a session quietly running on the previously
 * selected circuit.
 */
async function adoptLearnedCircuit(circuit: TestLoopCircuit): Promise<void> {
  const provider = testLoopProvider;
  // From here on the learner is done, but every fix still arrives and is
  // still kept -- the controller receives them after it is armed, in order.
  testLoopHandoverActive = true;
  provider?.beginHandover();
  // P5d-FIX2 N7: the out-lap trace is the QUALIFIED lap the geometry was built
  // from, not everything the buffer happens to hold by now.
  const learningSamples = circuit.lapSamples;

  await lifecycleLock.run(async () => {
    // P5d-FIX2 N2 (Codex P5d-REV2 HIGH 2): every step below is recorded in a
    // ledger, and every step is skipped when the ledger says it already
    // happened. A retry after a failure half-way through therefore RESUMES --
    // it never inserts a second circuit, starts a second session, or writes
    // the out-lap trace twice.
    const ledger = (adoptionProgress ??= { circuitId: circuit.profile.circuitId });
    // P5d-FIX3 F10: the journal is on disk BEFORE the first side effect, so a
    // kill at any point below leaves a state the next launch can finish or undo.
    await stageAdoptionJournal({ circuitId: ledger.circuitId, stage: 'staged' });

    // 1. Keep it. A learned circuit that cannot be stored cannot be analysed
    //    after a restart, so this failing is a real failure, not a warning.
    if (ledger.storedProfile === undefined) {
      if (learnedCircuitStore !== null) {
        const stored = await putLearnedCircuitWithFreshId(circuit);
        ledger.storedProfile = stored.profile;
      } else {
        // Web dev preview: no database, so the learned circuit lives for this
        // process only. It is still a real circuit while it lasts.
        const profile = circuit.profile;
        memoryLearnedCircuits = [
          ...memoryLearnedCircuits.filter(
            (entry) => entry.circuit.profile.circuitId !== profile.circuitId,
          ),
          {
            circuit: { profile, runtime: circuit.runtime, corners: circuit.corners },
            // Ticket D3: listed from the moment it is adopted, exactly like
            // the stored path below.
            listed: true,
          },
        ];
        ledger.storedProfile = profile;
      }
      publishLearnedCircuits();
      await stageAdoptionJournal({ circuitId: ledger.storedProfile.circuitId, stage: 'stored' });
    }
    const profile = ledger.storedProfile;

    // 2. Select it, and rebuild the production controller for it. Both go
    //    through the SAME unlocked routine `selectCircuit()` uses -- this
    //    section already holds the lock, so the public wrapper would deadlock.
    if (!ledger.selected) {
      const entry = circuitCatalog.get(profile.circuitId);
      if (entry === null) {
        throw new Error(`the learned circuit ${profile.circuitId} could not be registered`);
      }
      await unlockedApplySelection(entry);
      if (productionControllerCircuitId !== profile.circuitId) {
        await unlockedRebuildProductionController();
      }
      ledger.selected = true;
      await stageAdoptionJournal({ circuitId: profile.circuitId, stage: 'selected' });
    }
    const ctrl = controller;
    if (ctrl === null) throw new Error('no production controller to run the learned circuit on');

    // 3. Start the session on it and arm it. `start('session')` deliberately
    //    skips calibration (the same path `resumeRecovery()` uses): the
    //    geometry came from this driver, on this road, minutes ago -- there is
    //    nothing a recognition lap could add to it.
    if (ledger.sessionId === undefined) {
      await ctrl.start('session');
      ctrl.arm();
      const startedSessionId = ctrl.diagnostics().sessionId;
      if (startedSessionId === null) throw new Error('the learned-circuit session did not start');
      ledger.sessionId = startedSessionId;
      await stageAdoptionJournal({
        circuitId: profile.circuitId,
        stage: 'session-started',
        sessionId: startedSessionId,
      });
    }
    const sessionId = ledger.sessionId;

    // 4. The recording pipeline the learn phase was already running now
    //    belongs to a session: same hooks a normal start goes through.
    //
    //    P5d-FIX2 N3: the learn-phase subscriptions are detached in the SAME
    //    synchronous step that attaches the session's recorder -- no await
    //    between them, so no sample can ever reach both and be written twice.
    if (!ledger.recordingStarted) {
      detachTestLoopRecording();
      startTelemetryRecording(sessionId);
      const startedAtUtc = new Date().toISOString();
      initializeSessionStage(sessionId, startedAtUtc);
      ledger.recordingStarted = true;
      if (db !== null) {
        await setActiveSession(db, { sessionId, circuitId: profile.circuitId, startedAtUtc });
      }
      await flushTestLoopTelemetry();
      await releaseTestLoopGForce();
      await stageAdoptionJournal({ circuitId: profile.circuitId, stage: 'recording', sessionId });
    }

    // 5. The learning lap is STORED as the session's out-lap trace (lap 0),
    //    so the drive that defined the track is not lost.
    if (!ledger.outLapSaved) {
      if (learningSamples.length > 0 && repository !== null) {
        await repository.saveTelemetry(sessionId, 0, learningSamples);
      }
      ledger.outLapSaved = true;
      await stageAdoptionJournal({ circuitId: profile.circuitId, stage: 'out-lap', sessionId });
    }

    // 6. Finally, hand the whole backlog to the controller in order -- the
    //    learning lap first, then every fix that arrived while this ran. From
    //    the next fix onwards the session is simply live.
    provider?.flushBuffered();
    // P5d-FIX2 N5: the handover is over. The module lets go of the buffering
    // provider (the running controller keeps its own reference) so every later
    // rebuild takes the ORDINARY provider-cleanup path again.
    testLoopProvider = null;
    testLoopHandoverActive = false;
    adoptionProgress = null;
    // The adoption is complete: nothing is left for a later launch to repair.
    await clearOwnAdoptionJournal();
  });
}

/**
 * P5d-FIX1 item 10: a plain INSERT, with a fresh id on the (vanishingly
 * unlikely) collision, instead of an INSERT OR REPLACE that would silently
 * overwrite an existing learned circuit -- along with the sessions recorded
 * on it.
 *
 * Ticket D3 (flow review F4) -- A CIRCUIT THE DRIVER HAS ACTUALLY DRIVEN
 * MUST NOT BE ABLE TO VANISH. This used to store `saved: false`, which
 * `publishLearnedCircuits` maps to `listed: false` and `circuitCatalog.list()`
 * filters out. The only control that could ever flip it was the name field
 * on `TestLoopScreen`, rendered only while the in-memory learn phase is still
 * `'learned'` -- so a driver who tapped "Open dashboard" (the primary action,
 * ABOVE that field), drove, and later selected another circuit lost the
 * circuit, and with it every session, personal best, analysis and export
 * recorded against it. The data stayed on disk and became unreachable.
 *
 * It is therefore stored as a listed circuit immediately, under the automatic
 * name `TestLoopController.makeDisplayName` already gives it
 * (`defaultLearnedCircuitName`, RO/EN, dated). Naming it is now a RENAME of a
 * circuit that is already in the list (`saveLearnedCircuit`), which is a
 * convenience rather than the only thing standing between the driver and
 * losing their laps.
      lastCommittedEnetTargetAddress.current = parsed;
      settingsStore.update({ enetTargetAddress: parsed });
      setEnetTargetAddressDraft(formatHexByte(parsed));
    } else {
      setEnetTargetAddressDraft(formatHexByte(lastCommittedEnetTargetAddress.current));
    }
  }

  // Channel-specs JSON: same "invalid text stays in place, inline error below
  // the field" pattern as `transOilPidDraft` above -- a syntactically valid
  // draft is committed as-is (verbatim, not re-serialized) even when
  // `validateEnetChannelSpecsJson` reports per-entry warnings (those are
  // shown too, but never block the commit -- matching `validateEnetChannelSpecs`'s
  // own "drop bad entries, don't reject the whole list" model).
  const [enetChannelSpecsDraft, setEnetChannelSpecsDraft] = React.useState(settings.enetChannelSpecsJson);
  const lastCommittedEnetChannelSpecs = React.useRef(settings.enetChannelSpecsJson);
  const [enetChannelSpecsError, setEnetChannelSpecsError] = React.useState<string | null>(null);
  const [enetChannelSpecsWarnings, setEnetChannelSpecsWarnings] = React.useState<readonly string[]>([]);
  React.useEffect(() => {
    if (settings.enetChannelSpecsJson !== lastCommittedEnetChannelSpecs.current) {
      lastCommittedEnetChannelSpecs.current = settings.enetChannelSpecsJson;
      setEnetChannelSpecsDraft(settings.enetChannelSpecsJson);
    }
  }, [settings.enetChannelSpecsJson]);

  function commitEnetChannelSpecsDraft(): void {
    const result = validateEnetChannelSpecsJson(enetChannelSpecsDraft);
    if (result.ok) {
      lastCommittedEnetChannelSpecs.current = enetChannelSpecsDraft;
      settingsStore.update({ enetChannelSpecsJson: enetChannelSpecsDraft });
      setEnetChannelSpecsError(null);
      setEnetChannelSpecsWarnings(result.warnings);
    } else {
      setEnetChannelSpecsError(result.error);
      setEnetChannelSpecsWarnings([]);
    }
  }

  // ENET auto-discovery addendum (binding, Phase 4f): "Find adapter" --
  // acquires the shared reservation under the 'discovery' owner (refused
  // while the provider/probe hold it, same single-client rule as everything
  // else that touches the adapter), runs `runDiscovery` against a REAL probe
  // factory, and lets the user apply one result with a tap. `discoveryAbort`
  // is a plain mutable object (not a real `AbortController` -- `runDiscovery`
  // only ever reads `.aborted`) so the Cancel button can bound an in-flight
  // run early.
  const [discoveryRunning, setDiscoveryRunning] = React.useState(false);
  const [discoveryResults, setDiscoveryResults] = React.useState<readonly DiscoveryProbeResult[] | null>(null);
  const [discoveryError, setDiscoveryError] = React.useState<string | null>(null);
  const discoveryAbortRef = React.useRef<{ aborted: boolean } | null>(null);
  const discoveryTokenRef = React.useRef<ReturnType<typeof enetAdapterReservation.tryAcquire>>(null);
  const activeBinPreset =
    Object.entries(COVERAGE_BIN_PRESETS).find(
      ([, thresholds]) => thresholds.length === settings.coverageBins.thresholds.length,
    )?.[0] ?? '4 bins';

  const sessionCount = sessionHistoryStore.listSessions().length;
  const sessionActive = ACTIVE_SESSION_STATES.has(facadeState.sessionState);
  const sessionCountLabel = `${sessionCount} stored session${sessionCount === 1 ? '' : 's'}`;

  function startDeleteConfirm(): void {
    setDeleteBanner(null);
    setConfirmingDelete(true);
  }

  function cancelDeleteConfirm(): void {
    setConfirmingDelete(false);
  }

  async function confirmDelete(): Promise<void> {
    setDeleting(true);
    try {
      const result = await deleteAllStoredUserData();
      // An uncommitted channel draft can hold VIN-bearing provenance and is
      // not in the settings store, so delete-all cannot reach it from there.
      const channelSpecs = settingsStore.getSettings().enetChannelSpecsJson;
      lastCommittedEnetChannelSpecs.current = channelSpecs;
      setEnetChannelSpecsDraft(channelSpecs);
      setEnetChannelSpecsError(null);
      setEnetChannelSpecsWarnings([]);
      setDeleteBanner(result.ok ? 'success' : 'error');
      setDeleteErrorText(result.ok ? null : result.errorText);
      setDeleteRefused(!result.ok && result.reason !== undefined);
    } catch {
      setDeleteBanner('error');
      setDeleteErrorText(null);
      setDeleteRefused(false);
    } finally {
      setDeleting(false);
      setConfirmingDelete(false);
    }
  }

  return (
    <SafeAreaView style={styles.safe} edges={['top', 'left', 'right']}>
      <ScrollView contentContainerStyle={styles.container}>
        <Text style={styles.title} maxFontSizeMultiplier={1.3}>
          Settings
        </Text>

        {/* Ticket P4l-FIX1 F2 (binding): the app language. Defaulted from the
            device locale at first hydration (`sqlSettingsStore.ts`); this row
            is where the user overrides it. Drives the Signal Finder's
            exportable RO/EN summary today. */}
        <View style={styles.section}>
          <Text style={styles.sectionLabel} maxFontSizeMultiplier={1.3}>
            LANGUAGE
import React, { useEffect, useState } from 'react';
import { ActivityIndicator, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import type { NativeStackScreenProps } from '@react-navigation/native-stack';
import type { RootStackParamList } from '../navigation/types';
import { colors, fontFamily, radii, spacing, typography } from '../theme';
import {
  resetTestLoop,
  retryTestLoopAdoption,
  saveLearnedCircuit,
  settingsStore,
  startTestLoop,
  stopTestLoop,
  subscribeTestLoop,
  testLoopSnapshot,
} from '../../session/composition';
import type { TestLoopSnapshot } from '../../session/testLoopController';
import { useSettings } from '../hooks/useSettings';
import { resolveTestLoopStrings } from './testLoopStrings';

type Props = NativeStackScreenProps<RootStackParamList, 'TestLoop'>;

/**
 * Ticket P5d T2/T6 -- Test Loop mode, reached from the circuit selection
 * screen (NOT from a developer section: learning a track is how an
 * unregistered circuit becomes usable, which is a product feature).
 *
 * The screen is a thin renderer over `TestLoopController`'s snapshot: it
 * starts and stops the learn phase, shows what has been driven so far, shows
 * the banner when lap 1 closes, offers to save the track as a reusable
 * circuit, and hands over to the ordinary session flow. Every sentence it
 * shows comes from `testLoopStrings.ts`.
 */
export function TestLoopScreen({ navigation }: Props): React.JSX.Element {
  const settings = useSettings(settingsStore);
  const strings = resolveTestLoopStrings(settings.language);
  const [snapshot, setSnapshot] = useState<TestLoopSnapshot>(() => testLoopSnapshot());
  const [busy, setBusy] = useState(false);
  const [name, setName] = useState('');
  const [notice, setNotice] = useState<string | null>(null);

  useEffect(() => subscribeTestLoop(setSnapshot), []);

  /**
   * Ticket D3 (second one-liner): `resetTestLoop()` had no callers anywhere in
   * the repo, so `TestLoopController`'s terminal snapshot outlived the screen
   * -- re-entering Test Loop after a learn phase that gave up showed the
   * failure card from the PREVIOUS attempt, as though this visit had already
   * failed before the driver pressed anything.
   *
   * Cleared on mount, and ONLY for `'failed'`: `'error'` is an adoption that
   * failed with geometry still in hand and a Retry that must survive
   * (`retryTestLoopAdoption`), `'learning'`/`'adopting'` are live, and
   * `'learned'` is a running session this screen is still the face of.
   */
  useEffect(() => {
    if (testLoopSnapshot().phase !== 'failed') return;
    void resetTestLoop().catch((error: unknown) => {
      console.warn('[TestLoopScreen] clearing the previous learn phase failed', error);
    });
  }, []);

  const onStart = (): void => {
    setNotice(null);
    setBusy(true);
    void (async () => {
      try {
        const result = await startTestLoop();
        if (!result.ok) {
          setNotice(result.reason === 'session-active' ? strings.sessionActive : strings.notReady);
        }
      } finally {
        setBusy(false);
      }
    })();
  };

  const onStop = (): void => {
    setBusy(true);
    void (async () => {
      try {
        await stopTestLoop();
      } finally {
        setBusy(false);
      }
    })();
  };

  const onSave = (): void => {
    const learned = snapshot.learned;
    if (learned === null) return;
    if (name.trim().length === 0) {
      setNotice(strings.saveEmptyName);
      return;
    }
    setBusy(true);
    void (async () => {
      try {
        const result = await saveLearnedCircuit(learned.circuitId, name.trim());
        setNotice(result.ok ? strings.saved(name.trim()) : strings.saveFailed);
      } finally {
        setBusy(false);
      }
    })();
  };

  const metres = (value: number): string => Math.round(value).toString();

  return (
    <SafeAreaView style={styles.safe} edges={['top', 'left', 'right']}>
      <ScrollView contentContainerStyle={styles.container}>
        <Text style={styles.title} maxFontSizeMultiplier={1.3}>
          {strings.screenTitle}
        </Text>
        <View style={styles.warnCard}>
          <Text style={styles.warnText} maxFontSizeMultiplier={1.3}>
            {strings.intro}
          </Text>
          {
            // P5d-FIX6: the public-road case is a scoped, secondary note under
            // the racetrack framing -- never the headline.
          }
          <Text style={styles.muted} maxFontSizeMultiplier={1.3}>
            {strings.streetNote}
          </Text>
        </View>
        <Text style={styles.body} maxFontSizeMultiplier={1.3}>
          {strings.howItWorks}
        </Text>
        <Text style={styles.muted} maxFontSizeMultiplier={1.3}>
          {strings.cuesOff}
        </Text>

        {snapshot.phase === 'learning' ? (
          <View style={styles.card}>
            <View style={styles.rowCentered}>
              <ActivityIndicator color={colors.accent} />
              <Text style={styles.cardTitle} maxFontSizeMultiplier={1.3}>
                {strings.learningTitle}
              </Text>
            </View>
            <Text style={styles.mono} maxFontSizeMultiplier={1.3}>
              {strings.travelled(metres(snapshot.travelledM))}
            </Text>
            <Text style={styles.muted} maxFontSizeMultiplier={1.3}>
              {strings.learningHint}
            </Text>
          </View>
        ) : null}

        {snapshot.phase === 'learned' && snapshot.learned !== null ? (
          <View style={styles.successCard}>
            <Text style={styles.cardTitle} maxFontSizeMultiplier={1.3}>
              {strings.learnedBanner(snapshot.learned.cornerCount, metres(snapshot.learned.lengthM))}
            </Text>
            <Text style={styles.muted} maxFontSizeMultiplier={1.3}>
              {strings.learnedHint}
            </Text>
            <Text style={styles.muted} maxFontSizeMultiplier={1.3}>
              {strings.timingStarted}
            </Text>
            <Text style={styles.muted} maxFontSizeMultiplier={1.3}>
              {strings.adHocNote}
            </Text>
          </View>
        ) : null}

        {snapshot.phase === 'adopting' ? (
          <View style={styles.card}>
            <View style={styles.rowCentered}>
              <ActivityIndicator color={colors.accent} />
              <Text style={styles.cardTitle} maxFontSizeMultiplier={1.3}>
                {strings.adopting}
              </Text>
            </View>
          </View>
        ) : null}

        {snapshot.phase === 'error' ? (
          <View style={styles.failCard}>
            <Text style={styles.cardTitle} maxFontSizeMultiplier={1.3}>
              {strings.adoptFailedTitle}
            </Text>
            <Text style={styles.body} maxFontSizeMultiplier={1.3}>
              {strings.adoptFailed(snapshot.adoptError ?? '')}
            </Text>
            <Pressable
              style={[styles.button, styles.primaryButton]}
              onPress={() => retryTestLoopAdoption()}
              accessibilityRole="button"
              accessibilityLabel={strings.retryAdoptA11y}
            >
              <Text style={styles.primaryButtonText} maxFontSizeMultiplier={1.3}>
                {strings.retryAdopt}
              </Text>
            </Pressable>
          </View>
        ) : null}

        {snapshot.phase === 'failed' && snapshot.failure !== null ? (
          <View style={styles.failCard}>
            <Text style={styles.cardTitle} maxFontSizeMultiplier={1.3}>
              {strings.failureTitle}
            </Text>
            <Text style={styles.body} maxFontSizeMultiplier={1.3}>
              {strings.failure[snapshot.failure.reason]}
            </Text>
            <Text style={styles.mono} maxFontSizeMultiplier={1.3}>
              {strings.travelled(metres(snapshot.failure.travelledM))}
            </Text>
            {snapshot.failure.sampleCap === undefined ? null : (
              <Text style={styles.muted} maxFontSizeMultiplier={1.3}>
                {strings.capDetail(snapshot.failure.sampleCap)}
              </Text>
            )}
          </View>
        ) : null}

        {notice !== null ? (
          <Text style={styles.notice} maxFontSizeMultiplier={1.3}>
            {notice}
          </Text>
        ) : null}

        {snapshot.phase === 'learning' ? (
          <Pressable
            style={[styles.button, styles.secondaryButton, busy && styles.buttonDisabled]}
            onPress={onStop}
            disabled={busy}
            accessibilityRole="button"
            accessibilityLabel={strings.stopA11y}
          >
            <Text style={styles.secondaryButtonText} maxFontSizeMultiplier={1.3}>
              {strings.stop}
            </Text>
          </Pressable>
        ) : snapshot.phase === 'idle' || snapshot.phase === 'failed' ? (
          <Pressable
            style={[styles.button, styles.primaryButton, busy && styles.buttonDisabled]}
            onPress={onStart}
            disabled={busy}
            accessibilityRole="button"
            accessibilityLabel={strings.startA11y}
          >
            <Text style={styles.primaryButtonText} maxFontSizeMultiplier={1.3}>
              {snapshot.phase === 'failed' ? strings.tryAgain : strings.start}
            </Text>
          </Pressable>
        ) : null}

        {snapshot.phase === 'learned' ? (
          <>
            <Pressable
              style={[styles.button, styles.primaryButton]}
              onPress={() => navigation.navigate('ActiveDashboard')}
              accessibilityRole="button"
              accessibilityLabel={strings.openDashboardA11y}
            >
              <Text style={styles.primaryButtonText} maxFontSizeMultiplier={1.3}>
                {strings.openDashboard}
              </Text>
            </Pressable>

            <View style={styles.card}>
              <Text style={styles.cardTitle} maxFontSizeMultiplier={1.3}>
                {strings.saveTitle}
              </Text>
              <Text style={styles.muted} maxFontSizeMultiplier={1.3}>
                {strings.saveHint}
              </Text>
              <TextInput
                style={styles.input}
                value={name}
                onChangeText={setName}
                editable={!busy}
                autoCorrect={false}
                placeholder={strings.namePlaceholder}
                placeholderTextColor={colors.textMuted}
                accessibilityLabel={strings.namePlaceholder}
              />
              <Pressable
                style={[styles.button, styles.secondaryButton, busy && styles.buttonDisabled]}
                onPress={onSave}
                disabled={busy}
                accessibilityRole="button"
                accessibilityLabel={strings.saveA11y}
              >
                <Text style={styles.secondaryButtonText} maxFontSizeMultiplier={1.3}>
                  {strings.save}
                </Text>
              </Pressable>
            </View>
          </>
        ) : null}
      </ScrollView>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  safe: { flex: 1, backgroundColor: colors.background },
  container: { padding: spacing.lg, gap: spacing.md },
  title: { ...typography.title, color: colors.textPrimary },
  body: { ...typography.body, color: colors.textPrimary },
  muted: { ...typography.caption, color: colors.textSecondary },
  mono: { fontFamily: fontFamily.monoMedium, fontSize: 15, color: colors.textPrimary },
  notice: { ...typography.body, color: colors.accent },
  card: {
    backgroundColor: colors.surface,
    borderRadius: radii.md,
    borderWidth: 1,
    borderColor: colors.border,
    padding: spacing.md,
    gap: spacing.sm,
  },
  successCard: {
    backgroundColor: colors.surface,
    borderRadius: radii.md,
    borderWidth: 1,
    borderColor: colors.accent,
    padding: spacing.md,
    gap: spacing.sm,
  },
  failCard: {
    backgroundColor: colors.surface,
    borderRadius: radii.md,
    borderWidth: 1,
    borderColor: colors.border,
    padding: spacing.md,
    gap: spacing.sm,
  },
  warnCard: {
    backgroundColor: colors.surface,
    borderRadius: radii.md,
    borderWidth: 1,
    borderColor: colors.border,
    padding: spacing.md,
    gap: spacing.sm,
  },
  warnText: { ...typography.body, color: colors.textPrimary },
  cardTitle: { ...typography.subtitle, color: colors.textPrimary },
  rowCentered: { flexDirection: 'row', alignItems: 'center', gap: spacing.sm },
  input: {
    borderWidth: 1,
    borderColor: colors.border,
    borderRadius: radii.sm,
    paddingHorizontal: spacing.sm,
    paddingVertical: spacing.sm,
    color: colors.textPrimary,
    fontFamily: fontFamily.bodyRegular,
    fontSize: 16,
  },
  button: { borderRadius: radii.lg, paddingVertical: spacing.md, alignItems: 'center' },
  buttonDisabled: { opacity: 0.5 },
  primaryButton: { backgroundColor: colors.accent },
  primaryButtonText: { ...typography.subtitle, color: colors.onAccent },
  secondaryButton: { borderWidth: 1, borderColor: colors.border },
  secondaryButtonText: { ...typography.subtitle, color: colors.textPrimary },
});
32: * retry), never `learned` -- a driver told the track was learned while the app
111:  private lastQualified: LocationSample | null = null;
113:  private qualifiedStart: LocationSample | null = null;
119:  private pendingCircuit: TestLoopCircuit | null = null;
154:    this.lastQualified = null;
155:    this.qualifiedStart = null;
159:    this.pendingCircuit = null;
180:    const start = this.qualifiedStart;
186:    const verdict = qualifyStep(sample, this.lastQualified, DEFAULT_TEST_LOOP_CONFIG);
187:    if (verdict.reanchor && !verdict.accepted) this.lastQualified = sample;
189:      const previous = this.lastQualified;
191:      this.lastQualified = sample;
192:      this.qualifiedStart ??= sample;
241:  reset(): void {
248:    this.lastQualified = null;
249:    this.qualifiedStart = null;
253:    this.pendingCircuit = null;
286:    this.pendingCircuit = circuit;
317:  retryAdopt(): void {
318:    const circuit = this.pendingCircuit;
/**
 * The ONE ordering boundary for session-lifecycle work (contracts.md's
 * "Multi-circuit selection Ã¢â‚¬â€ lifecycle lock amendment", binding, ticket
 * CN-FIX3).
 *
 * `composition.ts` previously had THREE independent serialization mechanisms
 * -- `selectionChain` (settings/history writes), `rebuildInFlight`
 * (controller dispose+install), and `withDevReplayLock` (replay transitions).
 * Each was individually correct, but they did not form a single ordering
 * boundary: a rebuild could dispose the controller another operation had
 * already captured, and the preflight gate could forward START_PREFLIGHT to a
 * controller built for a circuit a queued selection was about to replace
 * (Codex CN-REV3 findings H3/N2/N3). All three are replaced by ONE mutex:
 * every operation that reads or replaces the production controller runs
 * entirely inside `run()`.
 *
 * Semantics:
 *  - FIFO: sections execute in the order `run()` was called, never
 *    overlapping. A section "holds" the lock across its own `await`s, so a
 *    controller it captured cannot be disposed underneath it.
 *  - Errors propagate to that section's caller only, and always release the
 *    lock (including a synchronous throw before the section's first `await`),
 *    so one failed operation can never wedge every later one.
 *  - Re-entrancy is NOT supported, by design: a section that calls `run()`
 *    again would queue behind ITSELF and deadlock forever. Composition-level
 *    code therefore calls `unlocked*` inner routines from inside a section,
 *    never the locked public wrapper. The synchronously-detectable form of
 *    that mistake (a `run()` issued from a section's own synchronous body) is
 *    REFUSED with `LifecycleLockReentry` instead of hanging.
 *
 * Detection limit (documented deliberately): JavaScript gives no way to tell
 * a `run()` issued from a holder's post-`await` continuation apart from a
 * legitimate concurrent call that must queue -- both see `isHeld() === true`
 * from an unrelated task. Refusing on `isHeld()` alone would break the
 * queueing this lock exists for, so the assert covers the synchronous case
 * only; the structural rule ("inside a section, call `unlocked*`") is what
 * covers the rest, and every call site in `composition.ts` follows it.
 */

/** Thrown by `run()` when a section re-enters the lock from its own synchronous body -- a coding error that would otherwise self-deadlock. */
export class LifecycleLockReentry extends Error {
  constructor(message = 'lifecycleLock: re-entrant run() from inside a held section -- call the unlocked* inner routine instead') {
    super(message);
    this.name = 'LifecycleLockReentry';
  }
}

export interface LifecycleLock {
  /** Queues `fn` behind every earlier call and runs it with the lock held for its whole duration (awaits included). */
  run<T>(fn: () => Promise<T>): Promise<T>;
  /** `true` while a section is executing -- diagnostics/tests only; never a substitute for queueing through `run()`. */
  isHeld(): boolean;
}

export function createLifecycleLock(): LifecycleLock {
  let tail: Promise<unknown> = Promise.resolve();
  let held = false;
  /** `> 0` only while a section's SYNCHRONOUS body is on the stack -- the one re-entrancy case that is precisely detectable (see the module doc comment). */
  let syncDepth = 0;

  return {
    isHeld(): boolean {
      return held;
    },
    run<T>(fn: () => Promise<T>): Promise<T> {
      if (syncDepth > 0) return Promise.reject(new LifecycleLockReentry());
      const result = tail.then(async () => {
        held = true;
        try {
          let started: Promise<T>;
          syncDepth += 1;
          try {
            started = fn();
          } finally {
            syncDepth -= 1;
          }
          return await started;
        } finally {
          held = false;
        }
      });
      // The chain tail must survive a rejected section (mirrors
      // `SqlWriteGate`'s own tail discipline) -- otherwise one failure would
      // leave every later section queued behind a never-settling link.
      tail = result.then(
        () => undefined,
        () => undefined,
      );
      return result;
    },
  };
}

codex
The fence appears to close the reported VIN resurrection window, and terminal Test Loop states now reset before controller replacement. Iâ€™m checking an edge case in the fence: it starts before the refusal checks, so a rejected deletion may affect a VIN read already in flight.

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command '$s=Get-Content apps/mobile/src/ui/screens/SettingsScreen.tsx; $s[320..367]; $s[457..492]; rg -n '"'enetChannelSpecsDraft|lastCommittedEnetChannelSpecs|commitEnet|deleting' apps/mobile/src/ui/screens/SettingsScreen.tsx; "'$s=Get-Content apps/mobile/src/session/testLoopController.ts; $s[225..335]; rg -n '"'function ready|bootstrapPromise|MID_SESSION_STATES' apps/mobile/src/session/composition.ts; Get-Content apps/mobile/test/support/sqlJsDatabase.ts; Get-Content package.json; Get-Content vitest.config.ts; git show -s --format=%B 1dee7b2; git diff 6d6f90b..d2b26f6 -- apps/mobile/src/persistence/deviceDataWipe.ts apps/mobile/src/session/composition.ts" in D:\CODE\APLICTIE_Circuit
 succeeded in 151ms:



  /**
   * Ends the learn phase. A loop that was learned stays learned; one that was
   * not ends in `failed` with the reason the closure test itself gives -- the
   * driver is told which of the three conditions did not hold, never just
   * "something went wrong".
   */
  stop(): TestLoopSnapshot {
    if (this.phase === 'learning') this.fail();
    return this.snapshot();
  }

  /** Drops everything and returns to idle (leaving the screen). */
  reset(): void {
    this.phase = 'idle';
    this.samples = [];
    this.travelled = 0;
    this.distanceToStart = null;
    this.departed = false;
    this.wasInside = true;
    this.lastQualified = null;
    this.qualifiedStart = null;
    this.lapSamples = [];
    this.learned = null;
    this.failure = null;
    this.pendingCircuit = null;
    this.adoptError = null;
    this.circuitId = null;
    this.emit();
  }

  private tryClose(): void {
    this.circuitId ??= this.deps.makeCircuitId();
    const result = buildTestLoopCircuit(this.samples, {
      circuitId: this.circuitId,
      displayName: this.deps.makeDisplayName(this.deps.nowUtc()),
      createdAtUtc: this.deps.nowUtc(),
    });
    if (!result.ok) {
      // Not a loop YET: keep driving. Only `stop()` (or the sample cap) turns
      // a not-yet into a failure.
      return;
    }
    this.lapSamples = result.lapSamples;
    this.adopt(result);
  }

  /** The QUALIFIED fixes of the learned lap -- what the session stores as its out-lap trace. */
  learnedLapSamples(): LocationSample[] {
    return [...this.lapSamples];
  }

  /**
   * P5d-FIX1 H3: the learned track is handed over and only THEN announced.
   * `adopting` is a real, visible phase -- the driver is not told the track was
   * learned while the app is still deciding whether it could keep it.
   */
  private adopt(circuit: TestLoopCircuit): void {
    this.pendingCircuit = circuit;
    this.phase = 'adopting';
    this.adoptError = null;
    this.emit();
    void Promise.resolve()
      .then(() => this.deps.onLearned(circuit))
      .then(() => {
        this.phase = 'learned';
        this.learned = {
          circuitId: circuit.profile.circuitId,
          displayName: circuit.profile.displayName,
          lengthM: polylineLength(circuit.runtime.centerline),
          cornerCount: circuit.corners.length,
        };
        this.adoptError = null;
        this.emit();
      })
      .catch((error: unknown) => {
        // Never `learned`: the track exists in memory but nothing else knows
        // about it, so timing it would be timing the wrong circuit.
        this.phase = 'error';
        this.learned = null;
        this.adoptError = error instanceof Error ? error.message : String(error);
        this.emit();
      });
  }

  /**
   * Retries the handover of a track that WAS learned but could not be kept
   * (P5d-FIX1 H3). The geometry is not re-derived -- it is the same lap.
   */
  retryAdopt(): void {
    const circuit = this.pendingCircuit;
    if (this.phase !== 'error' || circuit === null) return;
    this.adopt(circuit);
  }

  private fail(sampleCap?: number): void {
    const closure = detectLoopClosure(this.samples);
    this.failure = {
      reason: closure.closed ? 'not-returned' : closure.reason,
      travelledM: closure.closed ? closure.closure.lapLengthM : closure.travelledM,
      ...(sampleCap === undefined ? {} : { sampleCap }),
    };
    this.phase = 'failed';
    // P5d-FIX2 N6: a learn phase that gave up holds no trace. The buffers are
    // released here, not left to grow until the screen happens to be dismissed.
    this.samples = [];
    this.lapSamples = [];
    this.emit();
  }
1569:// independent of `bootstrapPromise` below -- so a background transition
1602:    if (!MID_SESSION_STATES.has(currentControllerState(ctrl))) return;
1717: * factory) -- the SAME deps `bootstrapPromise` used to construct it inline
2111:const MID_SESSION_STATES = new Set<SessionState>(['outLap', 'timing', 'inPit', 'paused']);
2113:/** Applies a validated circuit selection to the persisted settings ONLY -- no bootstrap-await, no session-active refusal, no serialization. Shared by the public `selectCircuit()` below and `runBootstrap()`'s own recovery-circuit switch, which CANNOT go through `selectCircuit()` itself: that function awaits `ready()` (i.e. `bootstrapPromise`), and calling it from inside `runBootstrap()` -- the very function that promise is for -- would deadlock forever. */
3271:          // `ready()`/`bootstrapPromise` -- itself still in flight here,
3333:let bootstrapPromise: Promise<void> = runBootstrap();
3338:bootstrapPromise.catch(() => undefined);
3354:  bootstrapPromise = attempt;
3364:async function ready(): Promise<{ db: SqlDatabase | null; repository: LocalSessionRepository; controller: SessionController }> {
3365:  await bootstrapPromise;
3764:  if (activeController !== null && MID_SESSION_STATES.has(currentControllerState(activeController))) {
3777:  //    `MID_SESSION_STATES` -- and step 2's rebuild deliberately no-ops
import initSqlJs, { type Database } from 'sql.js';
import type { SqlBindValue, SqlDatabase, SqlRunResult } from '@circuit/core';

/**
 * Local copy of packages/core/test/persistence-sql/sqlJsDatabase.ts's sql.js
 * `SqlDatabase` adapter -- kept local for the same cross-tsconfig reason as
 * `coreTestDoubles.ts` (see its top comment). Reuses the `sql.js` PACKAGE
 * itself via workspace-root hoisting (ticket CONSTRAINTS: "sql.js reuse via
 * workspace root") -- it is core's devDependency, not re-declared here.
 */

let sqlJsModulePromise: ReturnType<typeof initSqlJs> | null = null;
function loadSqlJs(): ReturnType<typeof initSqlJs> {
  sqlJsModulePromise ??= initSqlJs();
  return sqlJsModulePromise;
}

export function wrapSqlJsDatabase(db: Database): SqlDatabase {
  // Ticket P10A: the SAME serialization the real connection has.
  // `openAppDatabase()` wraps its `SqlDatabase` in `gateSqlTransactions(...,
  // createSqlWriteGate())`, so two transactions on the one on-device
  // connection can never overlap. The composition tests hand this double
  // straight to a mocked `openAppDatabase` and so had no gate at all --
  // which stayed invisible only while nothing wrote a transaction
  // concurrently with the repository's. P10A H2's session record (written at
  // recording start, alongside the active-session pointer write) does, and
  // sql.js answers an overlapping BEGIN with "cannot start a transaction
  // within a transaction". Gating here makes the double faithful to
  // production rather than papering over the collision.
  let transactionTail: Promise<unknown> = Promise.resolve();
  const handle: SqlDatabase = {
    async execAsync(sql: string): Promise<void> {
      db.exec(sql);
    },

    async runAsync(sql: string, params: readonly SqlBindValue[] = []): Promise<SqlRunResult> {
      db.run(sql, params as SqlBindValue[]);
      return { changes: db.getRowsModified() };
    },

    async getAllAsync<T>(sql: string, params: readonly SqlBindValue[] = []): Promise<T[]> {
      const stmt = db.prepare(sql);
      try {
        if (params.length > 0) stmt.bind(params as SqlBindValue[]);
        const rows: T[] = [];
        while (stmt.step()) {
          rows.push(stmt.getAsObject() as T);
        }
        return rows;
      } finally {
        stmt.free();
      }
    },

    // Ticket P10B H5-B: hands the callback the transaction-scoped handle --
    // this same adapter, which is the ungated connection. In production the
    // gate sits ABOVE this wrapper (`gateSqlTransactions`), so a callback
    // that used the outer handle instead would queue behind its own
    // transaction.
    withTransactionAsync(fn: (tx: SqlDatabase) => Promise<void>): Promise<void> {
      const run = transactionTail.then(async () => {
        db.run('BEGIN');
        try {
          await fn(handle);
          db.run('COMMIT');
        } catch (err) {
          db.run('ROLLBACK');
          throw err;
        }
      });
      transactionTail = run.then(
        () => undefined,
        () => undefined,
      );
      return run;
    },
  };
  return handle;
}

/** Creates a brand-new in-memory sql.js-backed `SqlDatabase` for a test. */
export async function createSqlJsDatabase(): Promise<SqlDatabase> {
  const SQL = await loadSqlJs();
  return wrapSqlJsDatabase(new SQL.Database());
}

/** Wraps a `SqlDatabase` over the SAME underlying sql.js `Database` instance passed in, rather than a fresh one -- used to simulate re-opening the app's on-device database across a restart. */
export function wrapExistingSqlJsDatabase(db: Database): SqlDatabase {
  return wrapSqlJsDatabase(db);
}

export async function createRawSqlJsDatabase(): Promise<Database> {
  const SQL = await loadSqlJs();
  return new SQL.Database();
}
{
  "name": "circuit-timer",
  "private": true,
  "version": "0.0.0",
  "description": "GNSS track lap-timing monorepo (npm workspaces)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "eslint .",
    "format": "prettier --write .",
    "generate:tmr": "node --experimental-strip-types packages/core/scripts/generate-tmr-profile.ts",
    "generate:motorpark": "node --experimental-strip-types packages/core/scripts/generate-motorpark-profile.ts"
  },
  "devDependencies": {
    "@eslint/js": "^9.13.0",
    "@onebeyond/license-checker": "^2.2.0",
    "@typescript-eslint/eslint-plugin": "^8.15.0",
    "@typescript-eslint/parser": "^8.15.0",
    "eslint": "^9.13.0",
    "eslint-config-prettier": "^9.1.0",
    "globals": "^15.11.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  }
}
Get-Content : Cannot find path 'D:\CODE\APLICTIE_Circuit\vitest.config.ts' because it does not exist.
At line:2 char:474
+ ... se.ts; Get-Content package.json; Get-Content vitest.config.ts; git sh ...
+                                      ~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : ObjectNotFound: (D:\CODE\APLICTI...itest.config.ts:String) [Get-Content], ItemNotFoundEx 
   ception
    + FullyQualifiedErrorId : PathNotFound,Microsoft.PowerShell.Commands.GetContentCommand
 
P18-FIX2: close Codex P18-REV2's delete-all findings

- H1: VIN detection is fenced for the whole of delete-all: no read starts
  while it runs, and none that was running completes into it. Detection is
  re-armed afterwards. New test drives reads from every settings change
  during the delete (fails on FIX1: 3 reads started).
- M2: a failed Test Loop adoption ('error') no longer blocks delete-all
  with an instruction the UI cannot follow; delete-all abandons it.
- M3: 'learned', 'failed' and 'error' Test Loop state (geometry, anchor
  coordinates) is reset and the learn phase torn down, before the
  controller rebuild; only 'learning'/'adopting' are refused.
- M4: Settings drops an uncommitted channel-specs draft after delete-all.
- History refresh now follows the session wipe, not the record cleanup.
- Policy EN/RO: all custom channel definitions (tagged or typed), the
  discarded unsaved Test Loop track, "stop the Test Loop", and VIN re-read
  only when the adapter is free.

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>

diff --git a/apps/mobile/src/persistence/deviceDataWipe.ts b/apps/mobile/src/persistence/deviceDataWipe.ts
index 971e758..a1c5c18 100644
--- a/apps/mobile/src/persistence/deviceDataWipe.ts
+++ b/apps/mobile/src/persistence/deviceDataWipe.ts
@@ -11,9 +11,11 @@ import { SETTINGS_KEY } from './sqlSettingsStore';
  *
  * A VIN is personal data in the EU, and so is what hangs off it -- the DID
  * bindings and sweep results are fingerprints of one specific vehicle, and a
- * learned circuit is a trace of where the driver drove. Preferences (units,
- * language, adapter address, coaching toggles, a profile the USER picked) are
- * not data about the driver and survive the wipe.
+ * learned circuit is a trace of where the driver drove. So are the custom
+ * channel definitions tagged from a sweep ("Tag as channel"): they are the
+ * same findings as the bindings, and their provenance text can hold the VIN.
+ * Preferences (units, language, adapter address, coaching toggles, a profile
+ * the USER picked) are not data about the driver and survive the wipe.
  */
 
 /** Mobile-owned tables whose every row is vehicle- or driver-specific. */
@@ -33,20 +35,31 @@ export interface DeviceDataWipeResult {
 }
 
 /**
- * The settings patch that forgets the vehicle's identity. The VIN goes; a
- * profile the VIN auto-selected goes with it, because keeping it would keep
- * the make and model the VIN resolved to. A profile the user chose is a
- * preference and stays.
+ * The settings patch that forgets the vehicle's identity. The VIN goes, and
+ * so do the channel definitions tagged from this car's sweeps; a profile the
+ * VIN auto-selected goes with them, because keeping it would keep the make
+ * and model the VIN resolved to. A profile the user chose is a preference and
+ * stays.
  */
 export function vehicleIdentityResetPatch(settings: AppSettings): Partial<AppSettings> {
-  if (settings.activeVehicleProfileSource !== 'vin') return { lastSeenVin: null };
+  const vehicleFindings = { lastSeenVin: null, enetChannelSpecsJson: DEFAULT_SETTINGS.enetChannelSpecsJson };
+  if (settings.activeVehicleProfileSource !== 'vin') return vehicleFindings;
   return {
-    lastSeenVin: null,
+    ...vehicleFindings,
     activeVehicleProfileId: DEFAULT_SETTINGS.activeVehicleProfileId,
     activeVehicleProfileSource: DEFAULT_SETTINGS.activeVehicleProfileSource,
   };
 }
 
+/** How many identity fields a stored settings blob still carries -- 0 when {@link vehicleIdentityResetPatch} has been applied. */
+function identityFieldsLeft(stored: Partial<AppSettings>): number {
+  let left = 0;
+  if (stored.lastSeenVin !== null && stored.lastSeenVin !== undefined) left += 1;
+  if (typeof stored.enetChannelSpecsJson === 'string' && stored.enetChannelSpecsJson !== '') left += 1;
+  if (stored.activeVehicleProfileSource === 'vin') left += 1;
+  return left;
+}
+
 function parseSettingsRow(raw: string | undefined): Partial<AppSettings> | null {
   if (raw === undefined) return null;
   try {
@@ -75,7 +88,20 @@ async function scrubStoredSettings(tx: SqlDatabase): Promise<number> {
   await tx.runAsync('UPDATE settings SET value = ? WHERE key = ?', [JSON.stringify(scrubbed), SETTINGS_KEY]);
   const after = await tx.getAllAsync<{ value: string }>('SELECT value FROM settings WHERE key = ?', [SETTINGS_KEY]);
   const verified = parseSettingsRow(after[0]?.value);
-  return verified === null || verified.lastSeenVin === null || verified.lastSeenVin === undefined ? 0 : 1;
+  return verified === null ? 1 : identityFieldsLeft(verified);
+}
+
+/**
+ * How many identity fields the stored settings row carries right now. Run
+ * after the wipe, through the same serialized handle as every settings
+ * write, it sees the row as it is once every write queued before it landed.
+ */
+export async function countStoredVehicleIdentity(db: SqlDatabase): Promise<number> {
+  const rows = await db.getAllAsync<{ value: string }>('SELECT value FROM settings WHERE key = ?', [SETTINGS_KEY]);
+  const raw = rows[0]?.value;
+  if (raw === undefined) return 0;
+  const stored = parseSettingsRow(raw);
+  return stored === null ? 1 : identityFieldsLeft(stored);
 }
 
 /**
@@ -105,7 +131,7 @@ export async function wipeDeviceUserData(db: SqlDatabase): Promise<DeviceDataWip
       [prefix, prefix],
     );
 
-    remaining.last_seen_vin = await scrubStoredSettings(tx);
+    remaining.vehicle_identity_settings = await scrubStoredSettings(tx);
   });
 
   const leftovers = Object.fromEntries(Object.entries(remaining).filter(([, count]) => count > 0));
diff --git a/apps/mobile/src/session/composition.ts b/apps/mobile/src/session/composition.ts
index 89380cb..46414e1 100644
--- a/apps/mobile/src/session/composition.ts
+++ b/apps/mobile/src/session/composition.ts
@@ -101,6 +101,7 @@ import {
   type RemoveLearnedCircuitResult,
 } from './learnedCircuitStore';
 import {
+  ADOPTION_JOURNAL_KEY,
   claimAdoptionJournal,
   clearClaimedJournal,
   deleteOrphanSession,
@@ -127,7 +128,11 @@ import {
   writeSessionVehicleSnapshot,
   type SessionVehicleSnapshot,
 } from '../persistence/sessionVehicleSnapshot';
-import { vehicleIdentityResetPatch, wipeDeviceUserData } from '../persistence/deviceDataWipe';
+import {
+  countStoredVehicleIdentity,
+  vehicleIdentityResetPatch,
+  wipeDeviceUserData,
+} from '../persistence/deviceDataWipe';
 import { createRawUdsChannel } from './didSweepController';
 import { EnetTcpTransport } from './enetTcpTransport';
 import { enetAdapterReservation as sharedEnetAdapterReservation } from './enetAdapterReservation';
@@ -722,9 +727,13 @@ let vehicleProfileBindingsCache: readonly VehicleProfileBindingLike[] = [];
  * "telemetry must never fail to start" discipline in this module.
  */
 export async function refreshVehicleProfileBindingsCache(): Promise<void> {
+  const generation = deviceDataGeneration;
   try {
     const store = createVehicleProfileBindingStore(getTelemetryReadDb());
-    vehicleProfileBindingsCache = await store.listBindings(getActiveVehicleProfileId());
+    const bindings = await store.listBindings(getActiveVehicleProfileId());
+    // A read that raced delete-all may hold rows the wipe removed.
+    if (generation !== deviceDataGeneration) return;
+    vehicleProfileBindingsCache = bindings;
   } catch (error) {
     console.warn('[composition] could not refresh the vehicle profile bindings cache', error);
   }
@@ -862,6 +871,12 @@ type VinDetectionState = 'idle' | 'attempting' | 'done';
 let vinDetectionState: VinDetectionState = 'idle';
 /** The result of the ONE completed attempt this app run has made (or will make) -- `null` before `vinDetectionState === 'done'`, and forever after if the read found nothing usable. */
 let cachedDetectedVin: string | null = null;
+/**
+ * Bumped by delete-all. A VIN read that started before the wipe and finishes
+ * after it belongs to data the driver asked to erase, so it is dropped rather
+ * than cached, persisted or used for auto-select.
+ */
+let deviceDataGeneration = 0;
 
 /**
  * Codex R2 fix (ticket P4q follow-up, binding, MEDIUM): "the explicit-choice
@@ -1006,6 +1021,7 @@ export async function maybeDetectVehicleFromVin(): Promise<string | null> {
   if (token === null) return null; // lost the race to acquire -- another trigger point retries later.
 
   vinDetectionState = 'attempting';
+  const generation = deviceDataGeneration;
   const transport = new EnetTcpTransport({ host: settings.enetHost, port: settings.enetPort });
   let vin: string | null = null;
   try {
@@ -1027,6 +1043,9 @@ export async function maybeDetectVehicleFromVin(): Promise<string | null> {
     sharedEnetAdapterReservation.release(token);
   }
 
+  // Delete-all ran while this read was in flight: it has already put
+  // detection back to 'idle', so the next connection reads again.
+  if (generation !== deviceDataGeneration) return null;
   vinDetectionState = 'done';
   cachedDetectedVin = vin;
   if (vin !== null) {
@@ -3612,27 +3631,49 @@ export interface AggregatedDeleteUserDataResult extends DeleteUserDataResult {
    * verified on those paths. `'SESSION_ACTIVE'`: a session is genuinely
    * mid-drive (CN-FIX4, facade boundary amendment). `'DEV_REPLAY_ACTIVE'`: a
    * `__DEV__` replay controller is installed (CN-FIX5, closing amendment).
+   * `'TEST_LOOP_ACTIVE'`: a Test Loop is learning, adopting, or waiting for a
+   * retry of its adoption.
    */
-  reason?: 'SESSION_ACTIVE' | 'DEV_REPLAY_ACTIVE';
+  reason?: 'SESSION_ACTIVE' | 'DEV_REPLAY_ACTIVE' | 'TEST_LOOP_ACTIVE';
 }
 
 /**
- * The in-memory mirror of {@link wipeDeviceUserData}: drops every cached copy
- * of what that wipe deleted, so nothing on screen or in the next session
- * start still carries it. Runs whether or not the on-disk wipe verified clean
- * -- a cache must never outlive the rows it was read from.
+ * The in-memory half of {@link wipeDeviceUserData}, run BEFORE the disk wipe:
+ * the settings store rewrites its whole blob on every `update()`, so the VIN
+ * has to leave memory first. Any settings write queued before this lands
+ * ahead of the wipe's transaction and is scrubbed by it; every write after it
+ * carries no VIN. The generation bump makes a VIN read or a bindings read
+ * already in flight drop its result instead of restoring it.
  */
-async function forgetDeviceUserDataInMemory(): Promise<void> {
-  settingsStore.update(vehicleIdentityResetPatch(settingsStore.getSettings()));
+function forgetVehicleIdentityInMemory(): void {
+  deviceDataGeneration += 1;
+  vinDetectionState = 'idle';
   cachedDetectedVin = null;
+  setVinAutoDetectNotice(null);
+  settingsStore.update(vehicleIdentityResetPatch(settingsStore.getSettings()));
   vehicleProfileBindingsCache = [];
   sessionVehicleSnapshots.clear();
+}
+
+/**
+ * Brings every in-memory copy of the learned circuits and the bindings back in
+ * line with the disk after the wipe. Resolves `false` when the learned
+ * circuits could not be re-read (the deleted geometry may then still be
+ * selectable) or the stored settings still carry the vehicle's identity:
+ * either way delete-all must not report success.
+ */
+async function resyncAfterDeviceWipe(): Promise<boolean> {
+  // Again, now the wipe has committed: a VIN or bindings read that finished
+  // while it ran may have put the old values back in memory.
+  forgetVehicleIdentityInMemory();
+  let learnedOk = true;
   memoryLearnedCircuits = [];
   if (learnedCircuitStore !== null) {
     try {
       await learnedCircuitStore.refresh();
     } catch (error) {
       console.warn('[composition] could not refresh learned circuits after delete-all', error);
+      learnedOk = false;
     }
   }
   publishLearnedCircuits();
@@ -3646,6 +3687,39 @@ async function forgetDeviceUserDataInMemory(): Promise<void> {
     }
   }
   await refreshVehicleProfileBindingsCache();
+  // The last word on the VIN: read back after every settings write queued so
+  // far, so a write that carried the old blob cannot hide behind the wipe.
+  let identityOk = true;
+  if (db !== null) {
+    try {
+      identityOk = (await countStoredVehicleIdentity(db)) === 0;
+    } catch (error) {
+      console.warn('[composition] could not verify the vehicle identity was deleted', error);
+      identityOk = false;
+    }
+  }
+  return learnedOk && identityOk;
+}
+
+/**
+ * Drops what an abandoned Test Loop adoption left behind: its in-memory
+ * ledger and its journal row. Only called once the sessions are gone -- the
+ * journal names a session and a circuit, and a retry must never resume onto a
+ * circuit this wipe deleted. Resolves `false` when the row could not be
+ * removed.
+ */
+async function forgetAbandonedAdoption(): Promise<boolean> {
+  adoptionProgress = null;
+  adoptionJournalId = null;
+  adoptionJournalOwned = null;
+  if (db === null) return true;
+  try {
+    await db.runAsync('DELETE FROM settings WHERE key = ?', [ADOPTION_JOURNAL_KEY]);
+    return true;
+  } catch (error) {
+    console.warn('[composition] could not clear the test-loop adoption journal', error);
+    return false;
+  }
 }
 
 export async function deleteAllStoredUserData(): Promise<AggregatedDeleteUserDataResult> {
@@ -3694,6 +3768,23 @@ async function unlockedDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDa
       errorText: 'a developer replay or mock session is active -- leave the Dev Replay screen before deleting all data',
     };
   }
+  // 1c. REFUSE while a Test Loop is learning, adopting, or holding a failed
+  //    adoption for retry. Its geometry and its adoption ledger live in
+  //    memory: a wipe underneath would let the adoption (or its retry) store
+  //    the circuit again, or resume onto the fallback circuit's controller
+  //    while writing the out-lap for the one just deleted. Leaving the Test
+  //    Loop screen ends all three.
+  const testLoopPhase = testLoopController.snapshot().phase;
+  if (testLoopPhase === 'learning' || testLoopPhase === 'adopting' || testLoopPhase === 'error') {
+    return {
+      ok: false,
+      remainingSessionCount: 0,
+      referenceLapCleared: false,
+      failedCircuitIds: [],
+      reason: 'TEST_LOOP_ACTIVE',
+      errorText: 'a test loop is in progress -- leave the Test Loop screen before deleting all data',
+    };
+  }
   // 2. Otherwise: drop any pending recovery and REPLACE the production
   //    controller with a fresh, idle one BEFORE deleting anything. A
   //    terminal (`sessionComplete`) controller still holds a `sessionId`
@@ -3790,6 +3881,7 @@ async function unlockedDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDa
   // records, and the learned circuits live outside every table the steps
   // above touch. Always attempted, like the telemetry step; a learned circuit
   // a surviving session still needs is kept and reported as remaining.
+  forgetVehicleIdentityInMemory();
   let deviceDataOk = true;
   if (db !== null) {
     try {
@@ -3803,21 +3895,27 @@ async function unlockedDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDa
       deviceDataOk = false;
     }
   }
-  await forgetDeviceUserDataInMemory();
-  const finalResult: DeleteUserDataResult = { ...result, ok: result.ok && telemetryOk && deviceDataOk };
+  if (!(await resyncAfterDeviceWipe())) deviceDataOk = false;
+  const sessionDataOk = result.ok && telemetryOk && deviceDataOk;
+  // Records ABOUT the sessions go only once the sessions themselves are gone:
+  // while one survives, its label and any adoption journal still describe it.
+  let sessionRecordsOk = true;
+  if (sessionDataOk && !(await forgetAbandonedAdoption())) sessionRecordsOk = false;
+  const finalResult: DeleteUserDataResult = { ...result, ok: sessionDataOk && sessionRecordsOk };
   // M4 fix (binding): a leftover active-session pointer is meaningless once
   // all stored data is gone -- cleared together, the SAME way session
   // end/discard/vanished-checkpoint do (`setActiveSession(db, null)`).
   if (finalResult.ok && db !== null) await setActiveSession(db, null);
   // Ticket P7R E2: the unvalidated-matching log is a list of facts about
   // sessions that no longer exist -- cleared with them, for the same reason
-  // the active-session pointer above is. Best-effort, exactly like the log's
-  // own writes: a stale label on a deleted session must never turn a
-  // successful wipe into a failed one.
+  // the active-session pointer above is. A row that cannot be removed is
+  // reported: delete-all promises the records are gone, not that it tried.
   if (finalResult.ok) {
     unvalidatedMatchingSessionIds = new Set();
     if (db !== null && !(await clearUnvalidatedMatchingSessionIds(db))) {
       console.warn('[composition] could not clear the unvalidated-matching log');
+      sessionRecordsOk = false;
+      finalResult.ok = false;
     }
   }
   if (finalResult.ok && historyStore !== null) await historyStore.refresh();
@@ -3829,6 +3927,7 @@ async function unlockedDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDa
         !perCircuitOk && failedCircuitIds.length === 0 ? 'one or more circuits did not verify empty' : null,
         !telemetryOk ? 'telemetry rows remained or could not be deleted' : null,
         !deviceDataOk ? 'vehicle or learned-circuit data remained or could not be deleted' : null,
+        !sessionRecordsOk ? 'session records remained or could not be deleted' : null,
       ]
         .filter((part): part is string => part !== null)
         .join('; ') || 'delete-all failed';

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'Get-Content apps/mobile/vitest.config.ts; Get-Content apps/mobile/package.json; $s=Get-Content apps/mobile/src/session/composition.ts; $s[1239..1289]; $s[2920..2949]; $s[3160..3230]; $s[3360..3385]; Get-Content apps/mobile/src/persistence/activeSession.ts -TotalCount 140; rg -n '"'repair|stage|deleteOrphan|clearClaimed' apps/mobile/src/session/adoptionJournal.ts" in D:\CODE\APLICTIE_Circuit
 succeeded in 56ms:
Get-Content : Cannot find path 'D:\CODE\APLICTIE_Circuit\apps\mobile\src\persistence\activeSession.ts' because it does 
not exist.
At line:2 char:200
+ ... 360..3385]; Get-Content apps/mobile/src/persistence/activeSession.ts  ...
+                 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : ObjectNotFound: (D:\CODE\APLICTI...ctiveSession.ts:String) [Get-Content], ItemNotFoundEx 
   ception
    + FullyQualifiedErrorId : PathNotFound,Microsoft.PowerShell.Commands.GetContentCommand
 
19: *   * NOBODY CLEARS SOMEONE ELSE'S JOURNAL. `clearClaimedJournal` is a
23: * The journal is cleared only after a repair fully succeeded; a failed repair
29:/** After this many failed repairs the journal is abandoned, with a notice -- never retried forever. */
33:  | 'staged'
44:  stage: AdoptionStage;
46:  /** How many launches have tried to repair it. */
48:  /** The launch that currently owns the repair. */
56:  /** True when the attempt budget is spent: the caller abandons the journal instead of repairing. */
74:    if (typeof journal.circuitId !== 'string' || typeof journal.stage !== 'string') return null;
78:      stage: journal.stage,
117: * there is nothing to repair or another launch got there first.
151:export async function clearClaimedJournal(
183:export async function deleteOrphanSession(db: SqlDatabase, sessionId: string): Promise<void> {

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "@'
const fs=require('fs'), ts=require('typescript'), vm=require('vm');
const src=fs.readFileSync('apps/mobile/src/session/composition.ts','utf8');
const ast=ts.createSourceFile('composition.ts',src,ts.ScriptTarget.Latest,true);
const names=new Set(['maybeDetectVehicleFromVin','unlockedDeleteAllStoredUserData','runDeleteAllStoredUserData','ready']);
const functions=ast.statements.filter(n=>ts.isFunctionDeclaration(n)&&names.has(n.name?.text)).map(n=>n.getText(ast).replace(/"'^export /,'"'')).join('\\n');
let finishClose, reachedClose; const inClose=new Promise(r=>reachedClose=r); const closing=new Promise(r=>finishClose=r);
const c={console, setTimeout:()=>0, deviceWipeInProgress:false,vinDetectionState:'idle',cachedDetectedVin:null,deviceDataGeneration:0,settingsStore:{getSettings:()=>({adapterType:'enet',enetHost:'host'})},sharedEnetAdapterReservation:{holder:()=>null,tryAcquire:()=>1,release:()=>{}},EnetTcpTransport:class {async connect(){} close(){reachedClose();return closing;}},createRawUdsChannel:()=>({}),readVinFromChannel:async()=> 'WZ1DB0C04LW000001',VIN_READ_ECU:1,VIN_READ_OVERALL_TIMEOUT_MS:5000,bootstrapPromise:Promise.resolve(),repository:{},controller:{},db:null,activeController:{},MID_SESSION_STATES:new Set(['timing']),currentControllerState:()=> 'timing'};
vm.createContext(c); vm.runInContext(ts.transpileModule(functions,{compilerOptions:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.None}}).outputText,c);
const locksrc=fs.readFileSync('apps/mobile/src/session/lifecycleLock.ts','utf8');const mod={exports:{}};vm.runInNewContext(ts.transpileModule(locksrc,{compilerOptions:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.CommonJS}}).outputText,{exports:mod.exports});const lock=mod.exports.createLifecycleLock();
(async()=>{const read=c.maybeDetectVehicleFromVin();await inClose;finishClose();const deletion=lock.run(c.unlockedDeleteAllStoredUserData);console.log(JSON.stringify({read:await read,deletion:await deletion,state:c.vinDetectionState,fence:c.deviceWipeInProgress,generation:c.deviceDataGeneration}));console.log(JSON.stringify({nextTrigger:await c.maybeDetectVehicleFromVin(),state:c.vinDetectionState}));})().catch(console.error);
'@ | node" in D:\CODE\APLICTIE_Circuit
 succeeded in 198ms:
{"read":null,"deletion":{"ok":false,"remainingSessionCount":0,"referenceLapCleared":false,"failedCircuitIds":[],"reason":"SESSION_ACTIVE","errorText":"a session is currently active -- end it before deleting all data"},"state":"attempting","fence":false,"generation":0}
{"nextTrigger":null,"state":"attempting"}

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "node --input-type=module -e 'import { startVitest } from \"vitest/node\"; const ctx=await startVitest(\"test\",[\"test/session/composition.deleteAllVinFence.test.ts\",\"test/session/composition.testLoop.test.ts\"],{root:process.cwd(),config:false,cache:false,watch:false}); if(ctx) await ctx.close();'" in D:\CODE\APLICTIE_Circuit\apps\mobile
 exited 1 in 0ms:
file:///D:/CODE/APLICTIE_Circuit/apps/mobile/[eval1]:1
import { startVitest } from vitest/node; const ctx=await startVitest(test,[test/session/composition.deleteAllVinFence.test.ts,test/session/composition.testLoop.test.ts],{root:process.cwd(),config:false,cache:false,watch:false}); if(ctx) await ctx.close();
                            ^^^^^^

SyntaxError: Unexpected identifier 'vitest'
    at compileSourceTextModule (node:internal/modules/esm/utils:318:16)
    at ModuleLoader.createModuleWrap (node:internal/modules/esm/loader:210:12)
    at ModuleLoader.eval (node:internal/modules/esm/loader:248:23)
    at node:internal/process/execution:77:24
    at asyncRunEntryPointWithESMLoader (node:internal/modules/run_main:101:11)
    at Object.runEntryPointWithESMLoader (node:internal/modules/run_main:123:19)
    at evalModuleEntryPoint (node:internal/process/execution:76:47)
    at node:internal/main/eval_string:37:3

Node.js v24.18.0

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "@'
import { startVitest } from 'vitest/node';
const ctx=await startVitest('test',['test/session/composition.deleteAllVinFence.test.ts','test/session/composition.testLoop.test.ts'],{root:process.cwd(),config:false,cache:false,watch:false}); if(ctx) await ctx.close();
'@ | node --input-type=module" in D:\CODE\APLICTIE_Circuit\apps\mobile
 exited 1 in 606ms:

 RUN  v2.1.9 D:/CODE/APLICTIE_Circuit/apps/mobile


âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯ Unhandled Errors âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯

Vitest caught 2 unhandled errors during the test run.
This might cause false positive tests. Resolve unhandled errors to make sure your tests are not affected.

âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯ Unhandled Error âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯
Error: EPERM: operation not permitted, mkdir 'C:\Users\dobri\AppData\Local\Temp\lcO7swLgtWgDlIDXCICEN'
 â¯ mkdir node:internal/fs/promises:859:10
 â¯ Proxy.fetch ../../node_modules/vitest/dist/chunks/resolveConfig.rBxzbVsl.js:6636:9
 â¯ EventEmitter.onMessage ../../node_modules/vitest/dist/chunks/index.68735LiX.js:91:20


âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯ Unhandled Error âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯
Error: EPERM: operation not permitted, mkdir 'C:\Users\dobri\AppData\Local\Temp\lcO7swLgtWgDlIDXCICEN'
 â¯ mkdir node:internal/fs/promises:859:10
 â¯ Proxy.fetch ../../node_modules/vitest/dist/chunks/resolveConfig.rBxzbVsl.js:6636:9
 â¯ EventEmitter.onMessage ../../node_modules/vitest/dist/chunks/index.68735LiX.js:91:20

âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯âŽ¯

 Test Files  no tests
      Tests  no tests
     Errors  2 errors
   Start at  19:28:24
   Duration  181ms (transform 11ms, setup 0ms, collect 0ms, tests 0ms, environment 0ms, prepare 0ms)


codex
I reproduced a new refusal-path bug: if a VIN read finishes while delete-all is checking whether to refuse, the fence discards it but leaves detection at `attempting`. Later triggers then skip detection until restart or a subsequent wipe resets it. Vitest also failed before collecting tests because the read-only sandbox denied its temporary-directory creation.

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -n 'setActiveSession|repair.*Adoption|repairInterrupted|claimAdoptionJournal|adoptionJournalOwned' apps/mobile/src/session/composition.ts; rg -n 'function setActiveSession' apps/mobile/src; "'$s=Get-Content apps/mobile/src/session/composition.ts; $s[2520..2640]; $s[3060..3125]; $s=Get-Content apps/mobile/src/ui/screens/SettingsScreen.tsx; $s[918..941]; Get-Content apps/mobile/src/persistence/sqlSettingsStore.ts -TotalCount 170; rg -n '"'VIN|data|export|preferences' docs/legal/privacy-policy.en.md | Select-Object -Last 25; rg -n 'error|delete-all|selected.*fail|journal' apps/mobile/test/session/composition.testLoop.test.ts" in D:\CODE\APLICTIE_Circuit
 succeeded in 71ms:
                    />
                  </View>
                  <View style={styles.fieldColumn}>
                    <Text style={styles.fieldLabel} maxFontSizeMultiplier={1.3}>
                      Channel specs (JSON, advanced)
                    </Text>
                    <TextInput
                      style={styles.multilineInput}
                      value={enetChannelSpecsDraft}
                      onChangeText={(text) => {
                        setEnetChannelSpecsDraft(text);
                        if (enetChannelSpecsError !== null) setEnetChannelSpecsError(null);
                      }}
                      onBlur={commitEnetChannelSpecsDraft}
                      placeholder="blank = built-in defaults"
                      placeholderTextColor={colors.textMuted}
                      autoCapitalize="none"
                      autoCorrect={false}
                      multiline
                      numberOfLines={4}
                      accessibilityLabel="ENET channel specs, JSON"
                    />
                  </View>
                  {enetChannelSpecsError === null ? null : (
import type { SqlDatabase } from '@circuit/core';
import {
  DEFAULT_SETTINGS,
  defaultLanguageForLocale,
  readDeviceLocale,
  type AppSettings,
  type SettingsStore,
} from '../session/settingsStore';
import { repairPersistedEnetSettings } from '../session/enetSettingsValidation';

export const SETTINGS_KEY = 'app-settings';

function isPartialAppSettings(value: unknown): value is Partial<AppSettings> {
  return typeof value === 'object' && value !== null;
}

/**
 * `SettingsStore` backed by the `settings` key-value table added in the
 * persistence-sql v2 migration (MUST DO #4) -- same on-device SQLite
 * database as session/lap/checkpoint/reference-lap data, not a separate
 * store. `getSettings()`/`subscribe()` stay synchronous (matching
 * `SettingsStore`'s existing contract, which `SettingsScreen` calls
 * directly): the current value is cached in memory and hydrated once from
 * disk in `create()`; every `update()` applies to the cache immediately and
 * persists in the background.
 */
export class SqlSettingsStore implements SettingsStore {
  private settings: AppSettings;
  private readonly listeners = new Set<(s: AppSettings) => void>();

  private constructor(
    private readonly db: SqlDatabase,
    initial: AppSettings,
    /**
     * Ticket P4p G1 (binding): did the persisted ROW itself carry a usable
     * `activeVehicleProfileId`? Same question (and same reason) as N7's
     * `rowHasLanguage` above: `DEFAULT_SETTINGS.activeVehicleProfileId` is a
     * perfectly valid `'generic'`, so after the merge a row written before
     * this setting existed is indistinguishable from a deliberate "generic"
     * choice. `composition.ts`'s one-time migration heuristic runs only while
     * this is `false`, so a profile the USER chose is never overridden.
     */
    readonly activeVehicleProfileIdWasStored: boolean,
  ) {
    this.settings = initial;
  }

  /**
   * `readLocale` is injectable ONLY so the language-default rule can be
   * pinned by a test without a native locale API; production always uses the
   * device's own `readDeviceLocale`.
   */
  static async create(
    db: SqlDatabase,
    readLocale: () => string | null = readDeviceLocale,
  ): Promise<SqlSettingsStore> {
    const rows = await db.getAllAsync<{ value: string }>('SELECT value FROM settings WHERE key = ?', [
      SETTINGS_KEY,
    ]);
    let initial = DEFAULT_SETTINGS;
    // Ticket P4l-FIX4 N7 (binding, Codex P4l-REV2b finding 11): whether the
    // USER ever chose a language is a fact about the ROW, and it has to be
    // captured BEFORE the merge below -- `DEFAULT_SETTINGS.language` is a
    // perfectly valid `'en'`, so after merging, a row written by any build
    // that predates the setting is indistinguishable from a deliberate
    // English choice, and the device-locale default was never reachable.
    let rowHasLanguage = false;
    // Ticket P4p G1: the same ROW-level question for the active vehicle
    // profile -- captured here, before the merge, for the reason N7 documents.
    let rowHasActiveVehicleProfileId = false;
    const raw = rows[0]?.value;
    if (raw !== undefined) {
      try {
        const parsed: unknown = JSON.parse(raw);
        if (isPartialAppSettings(parsed)) {
          rowHasLanguage = parsed.language === 'ro' || parsed.language === 'en';
          rowHasActiveVehicleProfileId =
            typeof parsed.activeVehicleProfileId === 'string' && parsed.activeVehicleProfileId.trim() !== '';
          initial = { ...DEFAULT_SETTINGS, ...parsed };
        }
      } catch {
        // Corrupt/legacy row: fall back to defaults rather than throw --
        // settings are non-critical and never block app startup.
      }
    }
    // P4e-FIX2 L1 fix (binding, review finding): `isPartialAppSettings` only
    // proves the persisted JSON was "some object" -- a PRESENT but malformed
    // ENET field (e.g. `enetPort: 70000`, `enetTesterAddress: -1`) would
    // otherwise overwrite `DEFAULT_SETTINGS` unchecked. `repairPersistedEnetSettings`
    // resets exactly those fields back to their defaults when structurally
    // invalid; every other (including every ELM327) field is untouched.
    // Applied unconditionally (a no-op when `initial` is already
    // `DEFAULT_SETTINGS`, i.e. no row / a corrupt row) so there is only one
    // hydration path to reason about.
    initial = repairPersistedEnetSettings(initial);
    // Field revision (2026-08-27, binding, "hidden developer mode"): a
    // present-but-malformed persisted value (e.g. from a corrupt row, or a
    // future schema change) must never leave dev-only ENET tools visible in
    // a release build by accident -- repaired back to `false` (never
    // trusted as truthy) the same defensive way `repairPersistedEnetSettings`
    // above handles the ENET fields.
    if (typeof initial.developerModeEnabled !== 'boolean') {
      initial = { ...initial, developerModeEnabled: false };
    }
    // Ticket P5c-B (contracts.md R2-3): the trackday suggestion stage is
    // opt-in, so a present-but-malformed persisted value must never be read
    // as truthy and silently switch it on -- repaired back to `false` exactly
    // like `developerModeEnabled` above. An install from before this setting
    // existed carries no key at all and takes `DEFAULT_SETTINGS`' `false`.
    if (typeof initial.suggestionsEnabled !== 'boolean') {
      initial = { ...initial, suggestionsEnabled: false };
    }
    // Ticket P4l-FIX1 F2 (binding), corrected by P4l-FIX4 N7: the language
    // DEFAULT comes from the device locale, applied here and only here --
    // whenever the persisted ROW did not itself carry a valid choice (no row
    // at all, a row from before the setting existed, or a value outside the
    // two-value vocabulary, repaired the same defensive way
    // `developerModeEnabled` above is). A user's own stored choice always
    // wins and is never re-derived on later launches.
    if (!rowHasLanguage) {
      initial = { ...initial, language: defaultLanguageForLocale(readLocale()) };
    }
    // Ticket P4p G1 (binding): a present-but-malformed profile id must never
    // reach the binding cache -- an unresolvable id would silently poll
    // nothing at all. Repaired back to `'generic'` the same defensive way the
    // boolean settings above are, and NOT counted as a stored choice (the
    // migration heuristic then still gets its one chance).
    if (!rowHasActiveVehicleProfileId) {
      initial = { ...initial, activeVehicleProfileId: DEFAULT_SETTINGS.activeVehicleProfileId };
    }
    // Codex R2 fix (ticket P4q follow-up, binding): a present-but-malformed
    // `activeVehicleProfileSource` (outside the three-value vocabulary) must
    // never be read as `'user'` by accident -- that would either wrongly
    // block a real VIN auto-select, or (worse, if it decoded to something
    // else entirely) let one silently overwrite a choice the repair itself
    // cannot prove was ever explicit. Repaired back to the default, the same
    // defensive discipline every other enum-like field above follows.
    if (
      initial.activeVehicleProfileSource !== 'user' &&
      initial.activeVehicleProfileSource !== 'vin' &&
      initial.activeVehicleProfileSource !== 'default'
    ) {
      initial = { ...initial, activeVehicleProfileSource: DEFAULT_SETTINGS.activeVehicleProfileSource };
    }
    // Ticket P4q (binding): a present-but-malformed persisted VIN (wrong
    // type from a corrupt/legacy row) must never reach the Signal Finder
    // screen or the VIN-matching logic -- repaired back to `null` (never read
    // yet), the same defensive way the boolean/ENET fields above are.
    if (typeof initial.lastSeenVin !== 'string' && initial.lastSeenVin !== null) {
      initial = { ...initial, lastSeenVin: null };
    }
    // Ticket P6a (binding): both new flags are opt-in and both change how a
    // field-confirmed signal path behaves, so a present-but-malformed
    // persisted value must never be read as truthy and silently switch one on
    // -- repaired back to `false` exactly like `suggestionsEnabled` above. An
    // install from before these settings existed carries no key at all and
    // takes `DEFAULT_SETTINGS`' `false`.
    if (typeof initial.imuFusionEnabled !== 'boolean') {
      initial = { ...initial, imuFusionEnabled: false };
    }
    // Ticket P7R E3: the same defensive repair for the gyroscope-CAPTURE
    // flag, but back to its OWN default (`true`) rather than to `false`. The
    // rule being applied is "a malformed value is never trusted, the declared
    // default wins" -- not "a malformed value means off". Capture is additive
    // and cannot alter latG/longG, so its default is the safe answer here the
    // same way `false` is the safe answer for the flags that replace a
    // field-proven path. An install from before this setting existed carries
    // no key at all and likewise takes `DEFAULT_SETTINGS`' `true`.
    if (typeof initial.imuGyroCaptureEnabled !== 'boolean') {
      initial = { ...initial, imuGyroCaptureEnabled: DEFAULT_SETTINGS.imuGyroCaptureEnabled };
197:| **Signal Finder export**, **DID Sweep export** | The respective diagnostic screens | The guided-observation or sweep record, including raw responses from your car |
204:Exported reports and analyses **do not contain your VIN**. A raw session export contains your full
205:position trace, which is precise location data â€” treat it as you would a photograph of where you
210:## 6. How long the data is kept
223:Uninstalling TRACE removes its private database along with it, in the normal way your operating
224:system removes an app's data.
226:**If you have exported files**, those files are yours and have their own life: this policy cannot
233:Under GDPR Articles 15â€“22 you have the rights listed below. Because your data is on your own device
237:### Right of access (Art. 15) â€” "give me a copy of my data"
240:or **raw session export** for everything on disk without interpretation. The analysis screen's
241:**Share report / Share the JSON file** gives you the corner-by-corner analysis. These exports are
253:**In the app:** **Settings â†’ DATA â†’ "Delete all my data"**, then confirm. This permanently removes
258:It also removes the stored VIN, a vehicle profile the app selected from the VIN, confirmed
264:What it keeps are your **preferences**: units, language, adapter address and port, coaching, voice
272:   cannot race a live recording and leave data behind.
282:### Right to data portability (Art. 20) â€” "give it to me in a usable format"
284:**In the app:** the same exports as under access. They are JSON â€” open, documented, machine-readable
302:copy of your data**, so we cannot send you an export, and we cannot delete anything from your phone.
314:> Email: anspdcp@dataprotection.ro
315:> Web: https://www.dataprotection.ro/
323:- The database lives in the app's **private sandbox directory**, which the operating system protects
328:- **We do not encrypt the database separately** with a key of the app's own. If someone has your
335:- Files you export sit in the app's cache and then wherever you send them. The share sheet's
338:**There is no backup.** If you lose or reset your phone, your sessions are gone unless you exported
345:None. No data is transferred outside your device, and therefore none is transferred outside the
264:  it('Codex P18-REV1 H3: delete-all is refused while a Test Loop is learning, and runs once the driver leaves it', async () => {
274:    expect(refused.errorText).toMatch(/test loop/i);
281:  it('Codex P18-REV2 M3: delete-all clears a failed learn phase instead of keeping where the driver drove', async () => {
294:  it('Codex P18-REV2 M3: delete-all clears a learned loop once its session has ended', async () => {

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command '$s=Get-Content apps/mobile/src/session/composition.ts; $s[1640..1678]; $s[2910..2922]; rg -n '"'async persist|JSON.stringify|this.persist' apps/mobile/src/persistence/sqlSettingsStore.ts; rg -n 'gateSqlTransactions|gate.run' apps/mobile/src/persistence/*.ts; git diff --name-only; "'$s=Get-Content docs/legal/privacy-policy.en.md; $s[250..280]; $s=Get-Content apps/mobile/src/session/composition.ts; $s[2114..2168]' in D:\CODE\APLICTIE_Circuit
 succeeded in 101ms:
 * (`openAppDatabase`'s own doc comment) -- this never interleaves with a
 * concurrent `SqlSessionRepository` transaction or a `TelemetryRecorder`
 * batch either.
 */
async function setActiveSession(
  database: SqlDatabase,
  session: { sessionId: string; circuitId: string; startedAtUtc?: string } | null,
): Promise<void> {
  // Ticket P10B H5-B: statements go through the transaction-scoped `tx`
  // handle, never through the (gated) outer `database` handle.
  await database.withTransactionAsync(async (tx) => {
    if (session === null) {
      await tx.runAsync('DELETE FROM settings WHERE key = ?', [ACTIVE_SESSION_SETTINGS_KEY]);
      await tx.runAsync('DELETE FROM settings WHERE key = ?', [ACTIVE_SESSION_CIRCUIT_SETTINGS_KEY]);
      await tx.runAsync('DELETE FROM settings WHERE key = ?', [ACTIVE_SESSION_STARTED_SETTINGS_KEY]);
    } else {
      await tx.runAsync('INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)', [
        ACTIVE_SESSION_SETTINGS_KEY,
        session.sessionId,
      ]);
      await tx.runAsync('INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)', [
        ACTIVE_SESSION_CIRCUIT_SETTINGS_KEY,
        session.circuitId,
      ]);
      if (session.startedAtUtc !== undefined) {
        await tx.runAsync('INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)', [
          ACTIVE_SESSION_STARTED_SETTINGS_KEY,
          session.startedAtUtc,
        ]);
      }
    }
  });
}

function midSessionState(snapshot: SessionMachineSnapshot): boolean {
  const midStates = new Set(['outLap', 'timing', 'inPit']);
  if (midStates.has(snapshot.state)) return true;
  const priorState = snapshot.context.priorState;
  return snapshot.state === 'paused' && typeof priorState === 'string' && midStates.has(priorState);
    // rebuild takes the ORDINARY provider-cleanup path again.
    testLoopProvider = null;
    testLoopHandoverActive = false;
    adoptionProgress = null;
    // The adoption is complete: nothing is left for a later launch to repair.
    await clearOwnAdoptionJournal();
  });
}

/**
 * P5d-FIX1 item 10: a plain INSERT, with a fresh id on the (vanishingly
 * unlikely) collision, instead of an INSERT OR REPLACE that would silently
 * overwrite an existing learned circuit -- along with the sessions recorded
193:    void this.persist();
196:  private async persist(): Promise<void> {
200:        JSON.stringify(this.settings),
292:      JSON.stringify(next),
rg: apps/mobile/src/persistence/*.ts: IO error for operation on apps/mobile/src/persistence/*.ts: The filename, directory name, or volume label syntax is incorrect. (os error 123)
hardware/kicad/trace-dongle/trace-dongle.kicad_prl
hardware/kicad/trace-dongle/trace-dongle.kicad_pro
### Right to erasure (Art. 17) Ã¢â‚¬â€ "delete it"

**In the app:** **Settings Ã¢â€ â€™ DATA Ã¢â€ â€™ "Delete all my data"**, then confirm. This permanently removes
every session, lap, checkpoint, GNSS trace, vehicle and motion sample, lap verdict, calibration
attempt and your personal-best reference lap Ã¢â‚¬â€ for every circuit, not just the one selected Ã¢â‚¬â€ and
verifies the deletion actually landed before reporting success. It cannot be undone.

It also removes the stored VIN, a vehicle profile the app selected from the VIN, confirmed
per-vehicle signal bindings, ruled-out signals, diagnostic sweep records, custom signal channel
definitions (whether tagged from a sweep with "Tag as channel" or typed in Settings), the record of
which vehicle profile each session used, the note of an interrupted Test Loop, a track the Test Loop
learned but could not save, and the geometry of circuits you taught the app.

What it keeps are your **preferences**: units, language, adapter address and port, coaching, voice
and suggestion toggles, the selected circuit, and a vehicle profile you chose yourself. None of these
identify you or your car. Uninstalling the app removes them too.

Two honest caveats:

1. The control is **refused while a session is running, or while a Test Loop is learning a track or
   saving one**; end the session or stop the Test Loop first. This exists so the deletion
   cannot race a live recording and leave data behind.
2. If any part of the deletion fails, the app says so instead of reporting success. In that case a
   learned circuit that a surviving session still uses is kept, so that session can still be
   analysed; run the deletion again.

### Right to restriction (Art. 18) Ã¢â‚¬â€ "stop using it for now"

**In the app:** turn off telemetry, coaching and suggestions in Settings; do not start a session. The
app records nothing when no session is active.

  settingsStore.update({ selectedCircuitId: entry.profile.circuitId });
}

/** Rebuilds/refreshes `SqlSessionHistoryStore` for `entry` and swaps it into `historyWrapper` -- the same no-op-if-bootstrap-incomplete guard `runBootstrap()`'s own initial build and `selectCircuit()` share. Split out from `applySelectedCircuit()` (rather than always paired) so `runBootstrap()`'s recovery block can call both in sequence exactly once, without a second, redundant history rebuild layered on top via a full `selectCircuit()` call. */
async function rebuildHistoryForSelection(entry: BundledCircuit): Promise<void> {
  if (repository === null) return;
  const history = new SqlSessionHistoryStore(
    repository,
    LOCAL_USER_ID,
    entry.profile.circuitId,
    entry.profile.layoutId,
    entry.profile.layoutVersion,
  );
  await history.refresh();
  historyWrapper.setInner(history);
  historyStore = history;
}

/**
 * The selection change itself -- settings + history store, in that order.
 * MUST be called with `lifecycleLock` held (M1 fix's ordering guarantee now
 * comes from the lock, which every other lifecycle operation shares, rather
 * than from a selection-only chain). Callers are the locked `selectCircuit()`
 * below, `resumeRecovery()`'s recovery-circuit switch, and
 * `runDevReplayScenario()`.
 */
async function unlockedApplySelection(entry: BundledCircuit): Promise<void> {
  applySelectedCircuit(entry);
  await rebuildHistoryForSelection(entry);
  // N3 fix (lifecycle lock amendment, binding, ticket CN-FIX3): the
  // production controller is rebuilt for the new circuit HERE, in the same
  // critical section as the settings/history writes, so the invariant "an
  // IDLE production controller is always built for the resolved selection"
  // holds continuously. Previously the rebuild happened lazily, in the
  // preflight gate only -- and `RealSessionFacade.startPreflight()` is a
  // no-op controller-side (the state machine first moves at
  // `beginCalibration()`), so a selection committed after the gate had
  // already run left the NEXT session starting on the previous circuit's
  // geometry while settings/history said otherwise. Restricted to `idle`
  // exactly like the gate's own circuit-change trigger: a terminal
  // (`sessionComplete`/`error`) controller is still the gate's to replace,
  // and a mid-session one is unreachable here (the selection would have been
  // refused before this point).
  if (
    controller !== null &&
    activeController === controller &&
    currentControllerState(controller) === 'idle' &&
    productionControllerCircuitId !== resolveSelectedCircuit(settingsStore.getSettings()).profile.circuitId
  ) {
    await unlockedRebuildProductionController();
  }
}

/** Shared refusal check for every locked selection change -- `null` when the change may proceed. */
function refuseSelectionIfSessionActive(): SelectCircuitResult | null {

codex
FAIL

Reviewed `d2b26f6..1dee7b2` and the cumulative rework `6d6f90b..1dee7b2`; HEAD matches `1dee7b2`. No files changed; no agents used.

**REV2 dispositions**

Paths below are relative to `apps/mobile/` unless otherwise stated.

| REV2 finding | Status | Evidence |
|---|---|---|
| HIGH 1 â€” VIN read after second reset survives verification | **CLOSED** | `src/session/composition.ts:3746` holds the fence through the operation; `:1021` blocks new detection and `:1056` rejects obsolete completions. No surviving success-path VIN resurrection found in these paths. |
| MEDIUM 2 â€” `error` refusal gives an impossible UI remedy | **CLOSED** | `composition.ts:3798` refuses only learning/adopting; `:3814` resets and abandons terminal states, including `error`. A genuinely running session still correctly refuses first. |
| MEDIUM 3 â€” terminal Test Loop retains geometry/coordinates | **CLOSED** | `composition.ts:3815` calls `reset()`; `src/session/testLoopController.ts:241` clears samples, both coordinate anchors, learned metadata and `pendingCircuit`. |
| MEDIUM 4 â€” Settings channel draft survives | **PARTIAL** | `src/ui/screens/SettingsScreen.tsx:468` resets draft, committed ref and validation state after **resolved** success/failure/refusal. Rejected operations bypass this cleanup at `:478`; failing sequence below. |

The previously closed REV1 items remain closed: persisted channels (`src/persistence/deviceDataWipe.ts:45`), stale adoption retry (`composition.ts:3815`), learned-refresh failure reporting (`:3689`), and journal/log failure reporting (`:3939`, `:3952`). Detection rearming now has a new refusal-path regression.

**New and residual findings**

1. **MEDIUM â€” A refused deletion can permanently strand VIN detection at `attempting`.**

   Locations: [composition.ts:3746](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3746), [composition.ts:1056](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:1056), [composition.ts:3764](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3764).

   Sequence:

   - VIN detection starts and reaches its awaited transport close.
   - Delete-all acquires the lifecycle lock and sets `deviceWipeInProgress = true`.
   - While deletion awaits `ready()`, detection finishes. Its generation still matches, but the fence makes it return before changing `vinDetectionState`.
   - Deletion refuses because a session is active. Neither identity reset runs.
   - `finally` releases the fence, but detection remains `attempting`. Every subsequent trigger returns the cached null without attempting another read.

   **Observed evidence:** A read-only, in-memory reproduction used the source-extracted detection, delete wrapper, delete body and `ready()` functions, plus the actual lifecycle lock. Transport and surrounding dependencies were stubbed:

   ```text
   read: null
   deletion.reason: SESSION_ACTIVE
   state: attempting
   fence: false
   generation: 0
   nextTrigger: null
   state after next trigger: attempting
   ```

   This reproduces with an already-resolved bootstrap promise; no prolonged bootstrap is required. Restart or a later deletion reaching the identity reset recovers detection. The fence itself releases correctly; the detection state does not.

2. **LOW â€” Settings draft cleanup misses rejected deletion failures.**

   Locations: [SettingsScreen.tsx:468](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/SettingsScreen.tsx:468), [SettingsScreen.tsx:478](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/SettingsScreen.tsx:478), [composition.ts:3945](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3945).

   Sequence: stored channel JSON is empty â†’ enter invalid VIN-bearing JSON â†’ delete-all clears the database â†’ active-pointer clearing throws, or the final history refresh rejects â†’ `confirmDelete()` enters `catch`.

   The draft/ref/error cleanup is skipped. Because the stored channel field stayed empty, its field-change effect does not clear the draft either. It remains editable and can subsequently be corrected and persisted.

   This is **not false success**: the UI reports failure. It does mean â€œdraft reset after both success and failureâ€ is incomplete. Cleanup should also cover rejection, using the current store value.

**REV2 qualifications**

| Qualification | Status | Assessment |
|---|---|---|
| FIFO protects already-submitted writes, not future writes | **CLOSED for the reported VIN race** | `src/persistence/sqlSettingsStore.ts:196` submits the serialized blob; `composition.ts:3701` verifies it. The new detection fence closes the previously identified future-writer window. FIFO alone still provides no such guarantee. |
| Active-profile subscription/bindings generation | **CLOSED** | `composition.ts:729`, `:3659`, `:3680`: earlier reads are invalidated; post-wipe refresh uses the new generation. |
| Generation mismatch does not strand detection | **OPEN â€” regression** | The added fence-only return at `composition.ts:1056` can strand detection without a generation change. See finding 1. |
| Abandoned ledger and bootstrap repair | **CLOSED within one runtime** | `composition.ts:3815` removes retryable geometry before clearing its ledger; `:3151` repairs during bootstrap, before `ready()` permits deletion. A retained journal describes surviving disk state after failure. |
| Cross-runtime journal ownership | **OPEN, unchanged qualification** | `composition.ts:3729` still deletes by key unconditionally. Another runtime claiming/writing that key during deletion is outside this lifecycle lockâ€™s protection. |
| Failure reporting | **CLOSED for the named cases** | Learned refresh, identity verification, journal deletion and log deletion prevent success (`composition.ts:3689`, `:3704`, `:3939`, `:3952`). Other awaited failures can reject rather than return the aggregate; Settings reports an error. |
| History skipped after log failure | **CLOSED** | `composition.ts:3958` now refreshes when `sessionDataOk`, even if journal/log cleanup fails. |
| Broader history freshness on failure | **PARTIAL** | `sessionDataOk` still includes device-data success (`composition.ts:3935`). Sessions can be deleted, identity verification can fail, and history refresh is then skipped. An active-pointer rejection also bypasses it. |
| Tests omit important paths | **PARTIAL** | Successful rearming and failed/learned abandonment now have coverage; refusal-time completion, rejection cleanup, late deferred VIN completion and several injected storage failures remain uncovered. |
| Policy alignment | **PARTIAL** | Changed EN/RO wording fixes the cited channel scope, terminal adoption and adapter-availability descriptions. Remaining qualifications below. |
| Unmentioned behavior changes | **CLOSED for custom-channel scope; diagnostic rename remains** | Policies now explicitly include typed definitions. The cumulative diff still changes the diagnostic key to `vehicle_identity_settings` (`deviceDataWipe.ts:134`). |

**Test assessment**

- `test/session/composition.deleteAllVinFence.test.ts:116` asserts that settings-change triggers start no reads during deletion. That is a valid, stronger property of this chosen fence implementation. It is not the only possible implementation of the privacy requirement.
- The test seeds a real VIN and checks the actual settings row after deletion (`:110`, `:127`), so its memory/disk assertions are meaningful.
- It does **not** directly schedule a read after the second reset and complete it after final verification. Reads resolve immediately, and triggers occur only on settings notifications. It also does not inspect private `cachedDetectedVin` or exercise refusal-time completion.
- The Test Loop tests at `test/session/composition.testLoop.test.ts:281` and `:294` reject the old terminal-state behavior. They pin invocation of abandonment through observable snapshots. They do not directly inspect private anchors/geometry, test `error` abandonment, verify restart after deletion, or exercise queued auto-teardown ordering.
- The SQL double still gates transactions only; standalone operations bypass that queue (`test/support/sqlJsDatabase.ts:35`). Production FIFO ordering is not fully represented.
- Attempted both changed composition test files with configuration/cache writing disabled. Vitest failed before collection because temporary-directory creation returned `EPERM`. **No suite pass claimed.**

**Clean list**

- No demonstrated `ok: true` VIN resurrection through the reviewed detection paths.
- Live sessions are refused before Test Loop teardown. Just-ended session commands and deletion share the lifecycle lock; teardown awaits the existing telemetry shutdown.
- Failed auto-teardown cannot overlap the new teardown under that lock. Incrementing `teardownGeneration` invalidates an older queued teardown.
- Reset publishes `idle`; `TestLoopScreen` subscribes to snapshots and exposes Start for idle. No newly introduced unrecoverable screen state found.
- Retaining the journal after an unsuccessful wipe is appropriate for bootstrap repair. Repair uses surviving disk geometry or removes orphaned state; it does not reconstruct erased geometry from the cleared in-memory ledger.
- Settingsâ€™ explicit committed-ref update handles the original unchanged-empty-store case correctly on resolved operations. No stale-ref resurrection demonstrated there.
- EN/RO agree on the changed scope. One pre-existing overclaim remains: Â§7 says the selected circuit is kept, but deleting the selected learned circuit selects the fallback (`composition.ts:3693`). The adopting-phase instruction to â€œstopâ€ is also imprecise: that phase offers no Stop control and must settle.
- Additional behavior: even an outright refusal now discards an uncommitted Settings draft, and terminal Test Loop abandonment occurs before deletion success is known. Neither is a false-success bug, but both are observable consequences of this patch.
tokens used
82,388
FAIL

Reviewed `d2b26f6..1dee7b2` and the cumulative rework `6d6f90b..1dee7b2`; HEAD matches `1dee7b2`. No files changed; no agents used.

**REV2 dispositions**

Paths below are relative to `apps/mobile/` unless otherwise stated.

| REV2 finding | Status | Evidence |
|---|---|---|
| HIGH 1 â€” VIN read after second reset survives verification | **CLOSED** | `src/session/composition.ts:3746` holds the fence through the operation; `:1021` blocks new detection and `:1056` rejects obsolete completions. No surviving success-path VIN resurrection found in these paths. |
| MEDIUM 2 â€” `error` refusal gives an impossible UI remedy | **CLOSED** | `composition.ts:3798` refuses only learning/adopting; `:3814` resets and abandons terminal states, including `error`. A genuinely running session still correctly refuses first. |
| MEDIUM 3 â€” terminal Test Loop retains geometry/coordinates | **CLOSED** | `composition.ts:3815` calls `reset()`; `src/session/testLoopController.ts:241` clears samples, both coordinate anchors, learned metadata and `pendingCircuit`. |
| MEDIUM 4 â€” Settings channel draft survives | **PARTIAL** | `src/ui/screens/SettingsScreen.tsx:468` resets draft, committed ref and validation state after **resolved** success/failure/refusal. Rejected operations bypass this cleanup at `:478`; failing sequence below. |

The previously closed REV1 items remain closed: persisted channels (`src/persistence/deviceDataWipe.ts:45`), stale adoption retry (`composition.ts:3815`), learned-refresh failure reporting (`:3689`), and journal/log failure reporting (`:3939`, `:3952`). Detection rearming now has a new refusal-path regression.

**New and residual findings**

1. **MEDIUM â€” A refused deletion can permanently strand VIN detection at `attempting`.**

   Locations: [composition.ts:3746](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3746), [composition.ts:1056](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:1056), [composition.ts:3764](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3764).

   Sequence:

   - VIN detection starts and reaches its awaited transport close.
   - Delete-all acquires the lifecycle lock and sets `deviceWipeInProgress = true`.
   - While deletion awaits `ready()`, detection finishes. Its generation still matches, but the fence makes it return before changing `vinDetectionState`.
   - Deletion refuses because a session is active. Neither identity reset runs.
   - `finally` releases the fence, but detection remains `attempting`. Every subsequent trigger returns the cached null without attempting another read.

   **Observed evidence:** A read-only, in-memory reproduction used the source-extracted detection, delete wrapper, delete body and `ready()` functions, plus the actual lifecycle lock. Transport and surrounding dependencies were stubbed:

   ```text
   read: null
   deletion.reason: SESSION_ACTIVE
   state: attempting
   fence: false
   generation: 0
   nextTrigger: null
   state after next trigger: attempting
   ```

   This reproduces with an already-resolved bootstrap promise; no prolonged bootstrap is required. Restart or a later deletion reaching the identity reset recovers detection. The fence itself releases correctly; the detection state does not.

2. **LOW â€” Settings draft cleanup misses rejected deletion failures.**

   Locations: [SettingsScreen.tsx:468](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/SettingsScreen.tsx:468), [SettingsScreen.tsx:478](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/SettingsScreen.tsx:478), [composition.ts:3945](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3945).

   Sequence: stored channel JSON is empty â†’ enter invalid VIN-bearing JSON â†’ delete-all clears the database â†’ active-pointer clearing throws, or the final history refresh rejects â†’ `confirmDelete()` enters `catch`.

   The draft/ref/error cleanup is skipped. Because the stored channel field stayed empty, its field-change effect does not clear the draft either. It remains editable and can subsequently be corrected and persisted.

   This is **not false success**: the UI reports failure. It does mean â€œdraft reset after both success and failureâ€ is incomplete. Cleanup should also cover rejection, using the current store value.

**REV2 qualifications**

| Qualification | Status | Assessment |
|---|---|---|
| FIFO protects already-submitted writes, not future writes | **CLOSED for the reported VIN race** | `src/persistence/sqlSettingsStore.ts:196` submits the serialized blob; `composition.ts:3701` verifies it. The new detection fence closes the previously identified future-writer window. FIFO alone still provides no such guarantee. |
| Active-profile subscription/bindings generation | **CLOSED** | `composition.ts:729`, `:3659`, `:3680`: earlier reads are invalidated; post-wipe refresh uses the new generation. |
| Generation mismatch does not strand detection | **OPEN â€” regression** | The added fence-only return at `composition.ts:1056` can strand detection without a generation change. See finding 1. |
| Abandoned ledger and bootstrap repair | **CLOSED within one runtime** | `composition.ts:3815` removes retryable geometry before clearing its ledger; `:3151` repairs during bootstrap, before `ready()` permits deletion. A retained journal describes surviving disk state after failure. |
| Cross-runtime journal ownership | **OPEN, unchanged qualification** | `composition.ts:3729` still deletes by key unconditionally. Another runtime claiming/writing that key during deletion is outside this lifecycle lockâ€™s protection. |
| Failure reporting | **CLOSED for the named cases** | Learned refresh, identity verification, journal deletion and log deletion prevent success (`composition.ts:3689`, `:3704`, `:3939`, `:3952`). Other awaited failures can reject rather than return the aggregate; Settings reports an error. |
| History skipped after log failure | **CLOSED** | `composition.ts:3958` now refreshes when `sessionDataOk`, even if journal/log cleanup fails. |
| Broader history freshness on failure | **PARTIAL** | `sessionDataOk` still includes device-data success (`composition.ts:3935`). Sessions can be deleted, identity verification can fail, and history refresh is then skipped. An active-pointer rejection also bypasses it. |
| Tests omit important paths | **PARTIAL** | Successful rearming and failed/learned abandonment now have coverage; refusal-time completion, rejection cleanup, late deferred VIN completion and several injected storage failures remain uncovered. |
| Policy alignment | **PARTIAL** | Changed EN/RO wording fixes the cited channel scope, terminal adoption and adapter-availability descriptions. Remaining qualifications below. |
| Unmentioned behavior changes | **CLOSED for custom-channel scope; diagnostic rename remains** | Policies now explicitly include typed definitions. The cumulative diff still changes the diagnostic key to `vehicle_identity_settings` (`deviceDataWipe.ts:134`). |

**Test assessment**

- `test/session/composition.deleteAllVinFence.test.ts:116` asserts that settings-change triggers start no reads during deletion. That is a valid, stronger property of this chosen fence implementation. It is not the only possible implementation of the privacy requirement.
- The test seeds a real VIN and checks the actual settings row after deletion (`:110`, `:127`), so its memory/disk assertions are meaningful.
- It does **not** directly schedule a read after the second reset and complete it after final verification. Reads resolve immediately, and triggers occur only on settings notifications. It also does not inspect private `cachedDetectedVin` or exercise refusal-time completion.
- The Test Loop tests at `test/session/composition.testLoop.test.ts:281` and `:294` reject the old terminal-state behavior. They pin invocation of abandonment through observable snapshots. They do not directly inspect private anchors/geometry, test `error` abandonment, verify restart after deletion, or exercise queued auto-teardown ordering.
- The SQL double still gates transactions only; standalone operations bypass that queue (`test/support/sqlJsDatabase.ts:35`). Production FIFO ordering is not fully represented.
- Attempted both changed composition test files with configuration/cache writing disabled. Vitest failed before collection because temporary-directory creation returned `EPERM`. **No suite pass claimed.**

**Clean list**

- No demonstrated `ok: true` VIN resurrection through the reviewed detection paths.
- Live sessions are refused before Test Loop teardown. Just-ended session commands and deletion share the lifecycle lock; teardown awaits the existing telemetry shutdown.
- Failed auto-teardown cannot overlap the new teardown under that lock. Incrementing `teardownGeneration` invalidates an older queued teardown.
- Reset publishes `idle`; `TestLoopScreen` subscribes to snapshots and exposes Start for idle. No newly introduced unrecoverable screen state found.
- Retaining the journal after an unsuccessful wipe is appropriate for bootstrap repair. Repair uses surviving disk geometry or removes orphaned state; it does not reconstruct erased geometry from the cleared in-memory ledger.
- Settingsâ€™ explicit committed-ref update handles the original unchanged-empty-store case correctly on resolved operations. No stale-ref resurrection demonstrated there.
- EN/RO agree on the changed scope. One pre-existing overclaim remains: Â§7 says the selected circuit is kept, but deleting the selected learned circuit selects the fallback (`composition.ts:3693`). The adopting-phase instruction to â€œstopâ€ is also imprecise: that phase offers no Stop control and must settle.
- Additional behavior: even an outright refusal now discards an uncommitted Settings draft, and terminal Test Loop abandonment occurs before deletion success is known. Neither is a false-success bug, but both are observable consequences of this patch.
diff --git a/.foreman/scratch/p18-codex-rev3-out.txt b/.foreman/scratch/p18-codex-rev3-out.txt
new file mode 100644
index 0000000..8812554
--- /dev/null
+++ b/.foreman/scratch/p18-codex-rev3-out.txt
@@ -0,0 +1,3002 @@
+OpenAI Codex v0.153.4
+--------
+workdir: D:\CODE\APLICTIE_Circuit
+model: gpt-6-astra
+provider: openai
+approval: never
+sandbox: read-only
+reasoning effort: none
+reasoning summaries: none
+session id: 01a0cf17-4b89-7871-b022-ecfbf9910178
+--------
+user
+# Review ticket P18-REV3 — Codex read-only cross-review of the P18-FIX2 fix wave
+Adversarial READ-ONLY reviewer. The fix is commit `1dee7b2` on branch `p18-fix1-delete-all`: `git diff d2b26f6..1dee7b2` (the whole delete-all rework since the cloud handoff: `git diff 6d6f90b..1dee7b2`).
+It answers your P18-REV2 review (`.foreman/scratch/p18-codex-rev2-out.txt`, final section after the last `codex` line): HIGH 1 (VIN read started after the second reset outlives the final check), MEDIUM 2 (`error` phase refusal with an instruction the UI cannot follow), MEDIUM 3 (terminal Test Loop state keeps geometry/coordinates), MEDIUM 4 (Settings channel-specs draft survives), plus the history-refresh and policy qualifications.
+Binding: HANDOFF.md item 1 (a VIN is personal data; delete-all must remove it), docs/legal/privacy-policy.*.md §3.3 and §7 as changed.
+For EACH REV2 finding and qualification: CLOSED / PARTIAL / OPEN with file:line, and a concrete failing sequence if not closed. Then attack the new code:
+1. `deviceWipeInProgress` fence (`unlockedDeleteAllStoredUserData` try/finally, `maybeDetectVehicleFromVin`): any path where delete-all returns `ok: true` with the VIN in memory or on disk; any path that leaves detection stuck or permanently disabled; refusal paths.
+2. Test Loop abandonment in delete-all (step 1d): `testLoopController.reset()` + `unlockedTearDownLearnPhase()` under `lifecycleLock` for phases `learned`/`failed`/`error`, before `unlockedRebuildProductionController()`. Can it stop providers a live or just-ended session still needs, race the auto-teardown subscription on `failed`, or leave `TestLoopScreen` in a state it cannot recover from? Is clearing the in-memory ledger while keeping the journal row until success correct against bootstrap repair?
+3. SettingsScreen draft reset: correct after both success and failure? any stale ref interplay?
+4. Tests: does `composition.deleteAllVinFence.test.ts` pin the HIGH (it fails on d2b26f6 with 3 reads started — is asserting "no read starts" the right property, and is the VIN/disk assertion meaningful)? Do the Test Loop tests pin M3?
+5. Policy EN/RO vs code: over- or under-claims.
+6. Anything else in the diff that changes behaviour the commit messages do not mention.
+OUTPUT: first line PASS / FAIL / PASS_WITH_NOTES; per-REV2-finding status; new findings by severity with file:line + scenario + evidence; Clean list. Stdout only. No agents.
+
+codex
+I’ll check the REV2 findings against the fix, trace the lifecycle and VIN races, and review the tests and policy claims. I’ll keep the review read-only and use no agents.
+
+exec
+"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'git status --short; git diff d2b26f6..1dee7b2 --stat; Get-Content .foreman/scratch/p18-codex-rev2-out.txt -Tail 220' in D:\CODE\APLICTIE_Circuit
+ succeeded in 55ms:
+
+  async refresh(): Promise<void> {
+    const summaries = await this.repository.listSessions(this.userId, this.circuitId);
+    this.cache = summaries
+      .filter((s) => s.layoutId === this.layoutId && s.layoutVersion === this.layoutVersion)
+      .map((s) => ({
+        sessionId: s.sessionId,
+        circuitId: s.circuitId,
+        layoutId: s.layoutId,
+        displayDateUtc: s.startedAtUtc,
+        laps: s.laps,
+        // Ticket P10A H6/H7: carried straight through from the stored row.
+        // An absent value stays absent here -- the readers turn that into
+        // `'unknown'`, never into `'validated'`.
+        ...(s.calibrationStatus === undefined ? {} : { calibrationStatus: s.calibrationStatus }),
+        // Ticket P12 item C: BOTH trace figures, not just the first. The
+        // session report has to be able to say "storage misbehaved N times
+        // and nothing was lost" as well as "N fixes never landed".
+        ...(s.trace === undefined
+          ? {}
+          : {
+              unwrittenSampleCount: s.trace.unwrittenSampleCount,
+              failedWriteCount: s.trace.failedWriteCount,
+              // Ticket P14 H3: absent stays ABSENT (= unknown). A row written
+              // before the column existed must not read back as finalised.
+              ...(s.trace.recordingFinalized === undefined
+                ? {}
+                : { recordingFinalized: s.trace.recordingFinalized }),
+            }),
+      }));
+
+    const ref = await this.repository.getReferenceLap(this.userId, this.circuitId, this.layoutId, this.layoutVersion);
+    if (ref === null) {
+      this.pb = null;
+      return;
+    }
+    const session = this.cache.find((s) => s.sessionId === ref.sessionId);
+    const lap = session?.laps.find((l) => l.lapNumber === ref.lapNumber) ?? syntheticLapFromReference(ref);
+    this.pb = { lap, sessionId: ref.sessionId, recordedAtUtc: ref.recordedAtUtc };
+  }
+
+  listSessions(): StoredSession[] {
+    return [...this.cache];
+  }
+
+  getSession(sessionId: string): StoredSession | null {
+    return this.cache.find((s) => s.sessionId === sessionId) ?? null;
+  }
+
+  getPersonalBest(): PersonalBestEntry | null {
+
+codex
+FAIL
+
+Reviewed `6d6f90b..d2b26f6`; HEAD matches the fix. No files changed; no agents used.
+
+**REV1 dispositions**
+
+| Finding | Status | Evidence |
+|---|---|---|
+| H1 â€” VIN resurrection | **PARTIAL** | [composition.ts:1048](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:1048) rejects reads from earlier generations, and the initial memory reset fixes stale whole-blob writes. However, detection started after the **second** reset can restore VIN after verification. Failing sequence below. |
+| H2 â€” persisted tagged channels survive | **CLOSED** | [deviceDataWipe.ts:45](D:/CODE/APLICTIE_Circuit/apps/mobile/src/persistence/deviceDataWipe.ts:45) clears `enetChannelSpecsJson`; verification counts remaining nonempty strings. The original persisted-channel scenario is fixed. An editor-memory gap remains below. |
+| H3 â€” stale adoption resumes against fallback controller | **CLOSED** | [composition.ts:3777](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3777) refuses the failed-adoption `error` phase. [composition.ts:3711](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3711) clears an abandoned ledger before success. The prescribed UI escape is broken, as detailed below. |
+| M4 â€” learned refresh failure reported as success | **CLOSED** | [composition.ts:3673](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3673) records refresh failure; line 3898 incorporates it into the result. Stale geometry can remain, but success is withheld. |
+| M5 â€” detection disabled until restart | **CLOSED** | [composition.ts:3650](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3650) resets detection to `idle`; obsolete completions cannot set it back to `done`. This restores eligibility at the next trigger, subject to adapter availability. |
+| L6 â€” session-linked settings survive silently | **CLOSED** | [composition.ts:3717](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3717) deletes the adoption journal; [composition.ts:3915](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3915) turns an unvalidated-log deletion failure into aggregate failure. |
+
+**New and residual findings**
+
+1. **HIGH â€” The second reset permits a fresh VIN read to outlive the final check.**  
+   [composition.ts:3668](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3668), [composition.ts:3695](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3695), [composition.ts:1057](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:1057).
+
+   Sequence:
+   - Delete-all performs its second identity reset, setting detection to `idle`.
+   - While deletion awaits subsequent work, opening Signal Finder starts detection with the **current** generation.
+   - Identity read-back returns zero.
+   - Detection completes while deletion awaits journal, active-pointer, log, or history operations. Its generation matches, so it restores both `cachedDetectedVin` and `lastSeenVin`.
+   - Those remaining operations succeed; delete-all returns `ok: true`.
+
+   **Evidence:** A read-only, in-memory reproduction using the source-extracted reset/detection functions, actual `SqlSettingsStore`, actual FIFO gate and sql.js produced:
+   ```
+   identityCountAtFinalCheck: 0
+   memoryVin: WZ1DB0C04LW000001
+   diskVin: WZ1DB0C04LW000001
+   vinDetectionState: done
+   ```
+   This is a component reproduction, not a full composition integration test. The remaining success path contains no further generation change or identity check.
+
+   A generation fence must cover the whole deletion interval; two isolated resets do not provide that fence.
+
+2. **MEDIUM â€” The refusal tells users to leave Test Loop, but leaving does not abandon it.**  
+   [composition.ts:3777](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3777), [TestLoopScreen.tsx:56](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/TestLoopScreen.tsx:56).
+
+   Sequence: adoption fails at the selected-journal write before session start â†’ phase becomes `error` â†’ leave Test Loop â†’ request delete-all. It refuses. Following its instruction and leaving again does not help.
+
+   **Evidence:** The screen has no unmount/blur reset. Its mount effect resets **only `failed`**, expressly preserving `error`. The error UI offers retry, without abandonment. Thus a repeatedly failing adoption can block deletion for the rest of the run despite following the advertised remedy.
+
+   The new test directly calls `resetTestLoop()`, bypassing the missing UI path.
+
+3. **MEDIUM â€” Allowed terminal Test Loop phases retain personal location data after success.**  
+   [composition.ts:3777](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3777), [composition.ts:3712](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3712), [testLoopController.ts:286](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/testLoopController.ts:286).
+
+   Sequence: learn a circuit â†’ finish its session â†’ delete-all while the Test Loop singleton remains `learned`. Disk deletion and catalog refresh succeed, but nothing resets that singleton. It retains `pendingCircuit`, including geometry, and the learning samples.
+
+   `failed` also retains `lastQualified` and `qualifiedStart`; its failure cleanup clears arrays but not those coordinates. A read-only reproduction confirmed both coordinates remain after entering `failed`.
+
+   These phases cannot retry adoption directly, so this is **retained personal data**, not the original wrong-controller retry. Clear terminal Test Loop state as part of successful deletion.
+
+4. **MEDIUM â€” VIN-bearing Settings drafts can survive deletion.**  
+   [SettingsScreen.tsx:330](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/SettingsScreen.tsx:330), [SettingsScreen.tsx:334](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/SettingsScreen.tsx:334).
+
+   Sequence: persisted channel JSON is empty â†’ enter an invalid JSON draft containing VIN-bearing channel provenance â†’ blur, which fails validation â†’ delete-all.
+
+   Both resets leave persisted `enetChannelSpecsJson` equal to `''`. The draft-reset effect sees no field change, so delete-all can return success while the editor still holds and displays the VIN. Correcting the draft and blurring subsequently persists it.
+
+   **Evidence:** Draft state is independent of the settings store; reset depends exclusively on the stored field changing. There is no deletion-generation reset for the editor. This is distinct from the fixed persisted-channel case.
+
+**Tests and verification**
+
+- The changed patch expectations, tagged-channel assertions, journal assertion and learning-phase refusal test would reject the corresponding pre-fix behavior.
+- The new read-back test proves the helper detects an explicitly awaited stale write. It does **not** prove composition ordering or protection against writes after read-back.
+- The composition database double serializes transactions, but its standalone reads/writes bypass that transaction queue. These tests therefore do not exercise the production gateâ€™s complete ordering behavior.
+- Untested commit claims include pending VIN/bindings invalidation, detection rearming, failed reset persistence, learned-refresh failure propagation, adoption-journal deletion failure, unvalidated-log deletion failure, and error/adopting refusal. Editor reset and terminal Test Loop cleanup are also uncovered.
+- I attempted the three changed test files with caching disabled. Vitest could not start tests because its temporary-directory creation was rejected by the read-only environment. **No test-suite pass is claimed.**
+
+**Clean list and remaining qualifications**
+
+- **FIFO claim:** correct for writes already submitted before the read-back. `SqlSettingsStore.persist()` serializes and submits synchronously before its first `await`; the production gate queues calls FIFO. Ordinary preference updates after reset serialize the cleared identity. This does not cover future writes.
+- **Active-profile subscription:** its pre-wipe bindings read can run under the first resetâ€™s generation, but the second reset invalidates that generation. The subsequent refresh reads after the committed wipe.
+- **Generation state:** only deletion increments `deviceDataGeneration`. I found no newly introduced result-dropping path outside deletion, or path where the mismatch return itself leaves detection stuck at `attempting`.
+- **Abandoned adoption:** `adoptionProgress` can remain non-null after `resetTestLoop()` makes the phase idle. Clearing it is necessary. In one runtime, `ready()` places bootstrap repair before deletion, and the lifecycle lock serializes newer adoption writes. The unconditional journal delete is not a cross-runtime ownership guarantee.
+- **Failure reporting:** learned-refresh, identity-read, journal-delete and log-delete failures all prevent success. Verification failure can conservatively report failure even when disk happens to be empty; that is not demonstrated corruption or a false-success bug.
+- **Result mutation:** the log failure changes `finalResult.ok` **after active-pointer clearing but before history refresh**, not after history refresh. Consequently history refresh is skipped and may leave stale cached history on this reported-failure path.
+- **Policy:** EN/RO broadly agree. Their deletion promises remain too strong given findings 1, 3 and 4. Their instruction to leave Test Loop is unsupported by the UI, and their refusal description omits the persistent `error` phase. â€œNext connectionâ€ also describes eligibility too strongly: detection can skip while the adapter reservation is held.
+- **Other behavior changes:** all custom channel JSON is cleared, including manually entered definitions, while policy specifically mentions sweep-tagged channels. The wipe diagnostic key changes from `last_seen_vin` to `vehicle_identity_settings`. Neither is separately identified in the commit message.
+tokens used
+78,249
+FAIL
+
+Reviewed `6d6f90b..d2b26f6`; HEAD matches the fix. No files changed; no agents used.
+
+**REV1 dispositions**
+
+| Finding | Status | Evidence |
+|---|---|---|
+| H1 â€” VIN resurrection | **PARTIAL** | [composition.ts:1048](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:1048) rejects reads from earlier generations, and the initial memory reset fixes stale whole-blob writes. However, detection started after the **second** reset can restore VIN after verification. Failing sequence below. |
+| H2 â€” persisted tagged channels survive | **CLOSED** | [deviceDataWipe.ts:45](D:/CODE/APLICTIE_Circuit/apps/mobile/src/persistence/deviceDataWipe.ts:45) clears `enetChannelSpecsJson`; verification counts remaining nonempty strings. The original persisted-channel scenario is fixed. An editor-memory gap remains below. |
+| H3 â€” stale adoption resumes against fallback controller | **CLOSED** | [composition.ts:3777](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3777) refuses the failed-adoption `error` phase. [composition.ts:3711](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3711) clears an abandoned ledger before success. The prescribed UI escape is broken, as detailed below. |
+| M4 â€” learned refresh failure reported as success | **CLOSED** | [composition.ts:3673](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3673) records refresh failure; line 3898 incorporates it into the result. Stale geometry can remain, but success is withheld. |
+| M5 â€” detection disabled until restart | **CLOSED** | [composition.ts:3650](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3650) resets detection to `idle`; obsolete completions cannot set it back to `done`. This restores eligibility at the next trigger, subject to adapter availability. |
+| L6 â€” session-linked settings survive silently | **CLOSED** | [composition.ts:3717](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3717) deletes the adoption journal; [composition.ts:3915](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3915) turns an unvalidated-log deletion failure into aggregate failure. |
+
+**New and residual findings**
+
+1. **HIGH â€” The second reset permits a fresh VIN read to outlive the final check.**  
+   [composition.ts:3668](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3668), [composition.ts:3695](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3695), [composition.ts:1057](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:1057).
+
+   Sequence:
+   - Delete-all performs its second identity reset, setting detection to `idle`.
+   - While deletion awaits subsequent work, opening Signal Finder starts detection with the **current** generation.
+   - Identity read-back returns zero.
+   - Detection completes while deletion awaits journal, active-pointer, log, or history operations. Its generation matches, so it restores both `cachedDetectedVin` and `lastSeenVin`.
+   - Those remaining operations succeed; delete-all returns `ok: true`.
+
+   **Evidence:** A read-only, in-memory reproduction using the source-extracted reset/detection functions, actual `SqlSettingsStore`, actual FIFO gate and sql.js produced:
+   ```
+   identityCountAtFinalCheck: 0
+   memoryVin: WZ1DB0C04LW000001
+   diskVin: WZ1DB0C04LW000001
+   vinDetectionState: done
+   ```
+   This is a component reproduction, not a full composition integration test. The remaining success path contains no further generation change or identity check.
+
+   A generation fence must cover the whole deletion interval; two isolated resets do not provide that fence.
+
+2. **MEDIUM â€” The refusal tells users to leave Test Loop, but leaving does not abandon it.**  
+   [composition.ts:3777](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3777), [TestLoopScreen.tsx:56](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/TestLoopScreen.tsx:56).
+
+   Sequence: adoption fails at the selected-journal write before session start â†’ phase becomes `error` â†’ leave Test Loop â†’ request delete-all. It refuses. Following its instruction and leaving again does not help.
+
+   **Evidence:** The screen has no unmount/blur reset. Its mount effect resets **only `failed`**, expressly preserving `error`. The error UI offers retry, without abandonment. Thus a repeatedly failing adoption can block deletion for the rest of the run despite following the advertised remedy.
+
+   The new test directly calls `resetTestLoop()`, bypassing the missing UI path.
+
+3. **MEDIUM â€” Allowed terminal Test Loop phases retain personal location data after success.**  
+   [composition.ts:3777](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3777), [composition.ts:3712](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3712), [testLoopController.ts:286](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/testLoopController.ts:286).
+
+   Sequence: learn a circuit â†’ finish its session â†’ delete-all while the Test Loop singleton remains `learned`. Disk deletion and catalog refresh succeed, but nothing resets that singleton. It retains `pendingCircuit`, including geometry, and the learning samples.
+
+   `failed` also retains `lastQualified` and `qualifiedStart`; its failure cleanup clears arrays but not those coordinates. A read-only reproduction confirmed both coordinates remain after entering `failed`.
+
+   These phases cannot retry adoption directly, so this is **retained personal data**, not the original wrong-controller retry. Clear terminal Test Loop state as part of successful deletion.
+
+4. **MEDIUM â€” VIN-bearing Settings drafts can survive deletion.**  
+   [SettingsScreen.tsx:330](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/SettingsScreen.tsx:330), [SettingsScreen.tsx:334](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/SettingsScreen.tsx:334).
+
+   Sequence: persisted channel JSON is empty â†’ enter an invalid JSON draft containing VIN-bearing channel provenance â†’ blur, which fails validation â†’ delete-all.
+
+   Both resets leave persisted `enetChannelSpecsJson` equal to `''`. The draft-reset effect sees no field change, so delete-all can return success while the editor still holds and displays the VIN. Correcting the draft and blurring subsequently persists it.
+
+   **Evidence:** Draft state is independent of the settings store; reset depends exclusively on the stored field changing. There is no deletion-generation reset for the editor. This is distinct from the fixed persisted-channel case.
+
+**Tests and verification**
+
+- The changed patch expectations, tagged-channel assertions, journal assertion and learning-phase refusal test would reject the corresponding pre-fix behavior.
+- The new read-back test proves the helper detects an explicitly awaited stale write. It does **not** prove composition ordering or protection against writes after read-back.
+- The composition database double serializes transactions, but its standalone reads/writes bypass that transaction queue. These tests therefore do not exercise the production gateâ€™s complete ordering behavior.
+- Untested commit claims include pending VIN/bindings invalidation, detection rearming, failed reset persistence, learned-refresh failure propagation, adoption-journal deletion failure, unvalidated-log deletion failure, and error/adopting refusal. Editor reset and terminal Test Loop cleanup are also uncovered.
+- I attempted the three changed test files with caching disabled. Vitest could not start tests because its temporary-directory creation was rejected by the read-only environment. **No test-suite pass is claimed.**
+
+**Clean list and remaining qualifications**
+
+- **FIFO claim:** correct for writes already submitted before the read-back. `SqlSettingsStore.persist()` serializes and submits synchronously before its first `await`; the production gate queues calls FIFO. Ordinary preference updates after reset serialize the cleared identity. This does not cover future writes.
+- **Active-profile subscription:** its pre-wipe bindings read can run under the first resetâ€™s generation, but the second reset invalidates that generation. The subsequent refresh reads after the committed wipe.
+- **Generation state:** only deletion increments `deviceDataGeneration`. I found no newly introduced result-dropping path outside deletion, or path where the mismatch return itself leaves detection stuck at `attempting`.
+- **Abandoned adoption:** `adoptionProgress` can remain non-null after `resetTestLoop()` makes the phase idle. Clearing it is necessary. In one runtime, `ready()` places bootstrap repair before deletion, and the lifecycle lock serializes newer adoption writes. The unconditional journal delete is not a cross-runtime ownership guarantee.
+- **Failure reporting:** learned-refresh, identity-read, journal-delete and log-delete failures all prevent success. Verification failure can conservatively report failure even when disk happens to be empty; that is not demonstrated corruption or a false-success bug.
+- **Result mutation:** the log failure changes `finalResult.ok` **after active-pointer clearing but before history refresh**, not after history refresh. Consequently history refresh is skipped and may leave stale cached history on this reported-failure path.
+- **Policy:** EN/RO broadly agree. Their deletion promises remain too strong given findings 1, 3 and 4. Their instruction to leave Test Loop is unsupported by the UI, and their refusal description omits the persistent `error` phase. â€œNext connectionâ€ also describes eligibility too strongly: detection can skip while the adapter reservation is held.
+- **Other behavior changes:** all custom channel JSON is cleared, including manually entered definitions, while policy specifically mentions sweep-tagged channels. The wipe diagnostic key changes from `last_seen_vin` to `vehicle_identity_settings`. Neither is separately identified in the commit message.
+
+exec
+"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'rg --files -g AGENTS.md -g HANDOFF.md; git rev-parse HEAD; git diff d2b26f6..1dee7b2 -- apps docs; git diff 6d6f90b..1dee7b2 --stat' in D:\CODE\APLICTIE_Circuit
+ succeeded in 24ms:
+ .foreman/scratch/p18-codex-rev1-out.txt            | 5148 ++++++++++++
+ .foreman/scratch/p18-codex-rev2-out.txt            | 8672 ++++++++++++++++++++
+ .foreman/scratch/ticket-p18-rev2-codex.md          |   13 +
+ apps/mobile/src/persistence/deviceDataWipe.ts      |   48 +-
+ apps/mobile/src/session/composition.ts             |  176 +-
+ apps/mobile/src/ui/screens/SettingsScreen.tsx      |    7 +
+ .../mobile/test/persistence/deviceDataWipe.test.ts |   29 +-
+ .../session/composition.circuitSelection.test.ts   |   11 +
+ .../session/composition.deleteAllVinFence.test.ts  |  134 +
+ .../test/session/composition.testLoop.test.ts      |   47 +
+ docs/legal/privacy-policy.en.md                    |   14 +-
+ docs/legal/privacy-policy.ro.md                    |   16 +-
+ 12 files changed, 14270 insertions(+), 45 deletions(-)
+
+exec
+"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "Get-Content apps/mobile/AGENTS.md; Get-Content docs/HANDOFF.md -TotalCount 75; rg -n 'unlockedRebuildProductionController|teardownGeneration|tearDownLearnPhase|disposeTestLoopProvider|releaseTestLoopGForce|stopTelemetryRecording|resetTestLoop|testLoopController.subscribe|lifecycleLock|maybeDetectVehicleFromVin' apps/mobile/src/session/composition.ts; "'$p='"'apps/mobile/src/session/composition.ts'; "'$s=Get-Content $p; $s[3630..3980]; $s[1015..1110]' in D:\CODE\APLICTIE_Circuit
+ succeeded in 36ms:
+// verify-empty logic lives in `@circuit/core`'s `deleteAllUserData` (unit
+// tested there; the mobile UI itself is not unit-testable in this repo).
+// Refreshes the shared `sessionHistoryStore` cache on success so
+// `SessionHistoryScreen` reflects the deletion without a manual reload.
+// ---------------------------------------------------------------------------
+
+/** M3 fix: `deleteAllStoredUserData()`'s own return shape, layered on top of `@circuit/core`'s `DeleteUserDataResult` -- `failedCircuitIds`/`errorText` cannot live on that type itself (`packages/core/**` is out of this ticket's write set). `errorText` is `null` whenever `ok` is `true`. */
+export interface AggregatedDeleteUserDataResult extends DeleteUserDataResult {
+  /** Bundled circuit ids whose `deleteAllUserData()` call REJECTED (not just returned `ok: false`) -- empty unless a per-circuit delete actually threw. */
+  failedCircuitIds: string[];
+  errorText: string | null;
+  /**
+   * Set when the wipe was REFUSED outright -- nothing was read, deleted, or
+   * verified on those paths. `'SESSION_ACTIVE'`: a session is genuinely
+   * mid-drive (CN-FIX4, facade boundary amendment). `'DEV_REPLAY_ACTIVE'`: a
+   * `__DEV__` replay controller is installed (CN-FIX5, closing amendment).
+   * `'TEST_LOOP_ACTIVE'`: a Test Loop is learning a track or saving one.
+   */
+  reason?: 'SESSION_ACTIVE' | 'DEV_REPLAY_ACTIVE' | 'TEST_LOOP_ACTIVE';
+}
+
+/**
+ * The in-memory half of {@link wipeDeviceUserData}, run BEFORE the disk wipe:
+ * the settings store rewrites its whole blob on every `update()`, so the VIN
+ * has to leave memory first. Any settings write queued before this lands
+ * ahead of the wipe's transaction and is scrubbed by it; every write after it
+ * carries no VIN. The generation bump makes a VIN read or a bindings read
+ * already in flight drop its result instead of restoring it.
+ */
+function forgetVehicleIdentityInMemory(): void {
+  deviceDataGeneration += 1;
+  vinDetectionState = 'idle';
+  cachedDetectedVin = null;
+  setVinAutoDetectNotice(null);
+  settingsStore.update(vehicleIdentityResetPatch(settingsStore.getSettings()));
+  vehicleProfileBindingsCache = [];
+  sessionVehicleSnapshots.clear();
+}
+
+/**
+ * Brings every in-memory copy of the learned circuits and the bindings back in
+ * line with the disk after the wipe. Resolves `false` when the learned
+ * circuits could not be re-read (the deleted geometry may then still be
+ * selectable) or the stored settings still carry the vehicle's identity:
+ * either way delete-all must not report success.
+ */
+async function resyncAfterDeviceWipe(): Promise<boolean> {
+  // Again, now the wipe has committed: a VIN or bindings read that finished
+  // while it ran may have put the old values back in memory.
+  forgetVehicleIdentityInMemory();
+  let learnedOk = true;
+  memoryLearnedCircuits = [];
+  if (learnedCircuitStore !== null) {
+    try {
+      await learnedCircuitStore.refresh();
+    } catch (error) {
+      console.warn('[composition] could not refresh learned circuits after delete-all', error);
+      learnedOk = false;
+    }
+  }
+  publishLearnedCircuits();
+  const selectedCircuitId = settingsStore.getSettings().selectedCircuitId;
+  if (circuitCatalog.get(selectedCircuitId) === null) {
+    const fallback = circuitCatalog.get(TMR_CIRCUIT_PROFILE.circuitId);
+    try {
+      if (fallback !== null) await unlockedApplySelection(fallback);
+    } catch (error) {
+      console.warn('[composition] could not fall back to the default circuit after delete-all', error);
+    }
+  }
+  await refreshVehicleProfileBindingsCache();
+  // The last word on the VIN: read back after every settings write queued so
+  // far, so a write that carried the old blob cannot hide behind the wipe.
+  let identityOk = true;
+  if (db !== null) {
+    try {
+      identityOk = (await countStoredVehicleIdentity(db)) === 0;
+    } catch (error) {
+      console.warn('[composition] could not verify the vehicle identity was deleted', error);
+      identityOk = false;
+    }
+  }
+  return learnedOk && identityOk;
+}
+
+/**
+ * Drops what an abandoned Test Loop adoption left behind: its in-memory
+ * ledger and its journal row. Only called once the sessions are gone -- the
+ * journal names a session and a circuit, and a retry must never resume onto a
+ * circuit this wipe deleted. Resolves `false` when the row could not be
+ * removed.
+ */
+async function forgetAbandonedAdoption(): Promise<boolean> {
+  adoptionProgress = null;
+  adoptionJournalId = null;
+  adoptionJournalOwned = null;
+  if (db === null) return true;
+  try {
+    await db.runAsync('DELETE FROM settings WHERE key = ?', [ADOPTION_JOURNAL_KEY]);
+    return true;
+  } catch (error) {
+    console.warn('[composition] could not clear the test-loop adoption journal', error);
+    return false;
+  }
+}
+
+export async function deleteAllStoredUserData(): Promise<AggregatedDeleteUserDataResult> {
+  // Lifecycle lock amendment (binding): delete-all wipes the very data the
+  // recovery/selection/rebuild operations read and write, so it runs as one
+  // section of the SAME lock they do -- never interleaved with a resume, a
+  // selection change, or a controller rebuild.
+  return lifecycleLock.run(unlockedDeleteAllStoredUserData);
+}
+
+async function unlockedDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDataResult> {
+  deviceWipeInProgress = true;
+  try {
+    return await runDeleteAllStoredUserData();
+  } finally {
+    deviceWipeInProgress = false;
+  }
+}
+
+async function runDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDataResult> {
+  const { repository: repo } = await ready();
+  // C fix (contracts.md's facade boundary amendment, binding, ticket
+  // CN-FIX4): durability. Two things had to change for "delete-all means
+  // deleted" to actually hold.
+  //
+  // 1. REFUSE while a session is genuinely mid-drive. A live controller goes
+  //    on writing checkpoints/laps/PBs by design, so a wipe underneath it
+  //    would be re-populated moments later -- and silently tearing the
+  //    driver's session down instead is not this action's job.
+  if (activeController !== null && MID_SESSION_STATES.has(currentControllerState(activeController))) {
+    return {
+      ok: false,
+      remainingSessionCount: 0,
+      referenceLapCleared: false,
+      failedCircuitIds: [],
+      reason: 'SESSION_ACTIVE',
+      errorText: 'a session is currently active -- end it before deleting all data',
+    };
+  }
+  // 1b. REFUSE while a `__DEV__` DevReplay controller is installed (CN-FIX5
+  //    item 3, closing amendment, binding). A replay controller sits in
+  //    `calibrating`/`calibrationReview`/`armed` -- none of them
+  //    `MID_SESSION_STATES` -- and step 2's rebuild deliberately no-ops
+  //    while it owns `facade`, so the wipe would leave a live controller
+  //    holding a session id that persists its session/checkpoint right back
+  //    over the deleted data. Durability wins over convenience on the dev
+  //    path: leave the replay, refuse the delete.
+  if (controller !== null && activeController !== controller) {
+    return {
+      ok: false,
+      remainingSessionCount: 0,
+      referenceLapCleared: false,
+      failedCircuitIds: [],
+      reason: 'DEV_REPLAY_ACTIVE',
+      errorText: 'a developer replay or mock session is active -- leave the Dev Replay screen before deleting all data',
+    };
+  }
+  // 1c. REFUSE while a Test Loop is learning or saving a track: a wipe
+  //    underneath would let the adoption store the circuit again, or resume
+  //    onto the fallback circuit's controller while writing the out-lap for
+  //    the one just deleted. Stopping the learn phase on the Test Loop screen
+  //    ends the first; the second finishes on its own.
+  const testLoopPhase = testLoopController.snapshot().phase;
+  if (testLoopPhase === 'learning' || testLoopPhase === 'adopting') {
+    return {
+      ok: false,
+      remainingSessionCount: 0,
+      referenceLapCleared: false,
+      failedCircuitIds: [],
+      reason: 'TEST_LOOP_ACTIVE',
+      errorText: 'a test loop is learning or saving a track -- stop it before deleting all data',
+    };
+  }
+  // 1d. Every other Test Loop phase is ABANDONED instead. `error` holds a
+  //    learned track waiting for a Retry that must not run onto a circuit
+  //    this wipe deletes; `learned` and `failed` still hold the traced
+  //    geometry and coordinates of where the driver drove. The learn phase is
+  //    torn down before step 2 rebuilds the controller, so the new controller
+  //    is never built on a provider that is about to stop.
+  if (testLoopPhase !== 'idle') {
+    testLoopController.reset();
+    teardownGeneration += 1;
+    await unlockedTearDownLearnPhase();
+    // Its learned track is gone, so its ledger can never be resumed. The
+    // journal row stays until the wipe succeeds: until then it is what lets
+    // the next launch repair the half-adopted state.
+    adoptionProgress = null;
+    adoptionJournalId = null;
+    adoptionJournalOwned = null;
+  }
+  // 2. Otherwise: drop any pending recovery and REPLACE the production
+  //    controller with a fresh, idle one BEFORE deleting anything. A
+  //    terminal (`sessionComplete`) controller still holds a `sessionId`
+  //    and a full lap set, so anything that later touches it --
+  //    `checkpointNow()` from a background transition, a straggler
+  //    `endSession()` persistence path -- would re-create rows this wipe
+  //    just removed. A fresh controller has no session identity at all, so
+  //    there is nothing left to re-persist. (`endSession()` itself is now
+  //    serialized on this same lock, so an in-flight one has already
+  //    completed by the time this section runs.)
+  setPendingRecovery(null);
+  setRecoveryNotice(null);
+  await unlockedRebuildProductionController();
+  // Multi-circuit selection addendum (ticket CN-W3): delete-all spans EVERY
+  // bundled circuit, not just the currently selected one -- `deleteUserData`
+  // itself is a single per-userId wipe (not circuit-scoped), so this loop's
+  // real job is the per-circuit VERIFY-EMPTY check; success requires every
+  // bundled circuit to come back empty.
+  //
+  // M3 fix (binding): each circuit's call is its own try/catch -- a REJECTED
+  // per-circuit `deleteAllUserData()` (e.g. a transient SQLite error) no
+  // longer aborts the loop; every remaining bundled circuit is still
+  // attempted, and the rejection is reflected in the aggregate `ok: false`
+  // plus `failedCircuitIds`/`errorText` instead of silently stopping partway
+  // through (previously leaving later circuits' data untouched with no
+  // indication why).
+  const perCircuitResults: DeleteUserDataResult[] = [];
+  const failedCircuitIds: string[] = [];
+  for (const summary of circuitCatalog.list()) {
+    try {
+      perCircuitResults.push(
+        await deleteAllUserData(repo, LOCAL_USER_ID, summary.circuitId, summary.layoutId, summary.layoutVersion),
+      );
+    } catch (error) {
+      console.warn(`[composition] deleteAllStoredUserData: circuit "${summary.circuitId}" failed`, error);
+      failedCircuitIds.push(summary.circuitId);
+    }
+  }
+  const perCircuitOk = failedCircuitIds.length === 0 && perCircuitResults.every((r) => r.ok);
+  const result: DeleteUserDataResult = {
+    ok: perCircuitOk,
+    remainingSessionCount: perCircuitResults.reduce((sum, r) => sum + r.remainingSessionCount, 0),
+    referenceLapCleared: perCircuitResults.length > 0 && perCircuitResults.every((r) => r.referenceLapCleared),
+  };
+  // F7 fix (WPT3, binding): `telemetry_samples` (Telemetry addendum) is a
+  // mobile-owned table (`persistence/telemetrySchema.ts`) that
+  // `@circuit/core`'s `deleteAllUserData`/`LocalSessionRepository.deleteUserData`
+  // (packages/core, out of this ticket's write set) has no knowledge of at
+  // all -- delete it here, in the SAME flow, and verify it actually landed
+  // empty before ever reporting success, so the UI's "All stored data
+  // deleted" banner is never shown while up to 200,000 rows/session remain.
+  //
+  // N4 fix (lifecycle lock amendment, binding, ticket CN-FIX3): this step is
+  // NO LONGER gated on the per-circuit aggregate. A rejected circuit delete
+  // used to suppress the telemetry DELETE + verify-empty entirely, leaving up
+  // to 200,000 rows/session on disk after the user asked for a full wipe. The
+  // telemetry step always runs; the aggregate `ok` is `circuitsOk &&
+  // telemetryOk`, and `errorText` names whichever half failed.
+  let telemetryOk = true;
+  if (db !== null) {
+    // N1-confirm residue fix (LEAD): a session's final telemetry flush can
+    // still be in flight when the user reaches delete-all -- await any
+    // in-progress shutdown first, then run DELETE + verify while HOLDING the
+    // shared write gate, so no straggler batch INSERT can land between (or
+    // after) them. `stopTelemetryRecording()` is an idempotent no-op when
+    // nothing is recording.
+    await stopTelemetryRecording();
+    const database = db;
+    try {
+      // P10B H5-B: expressed as a TRANSACTION rather than as a gate hold.
+      // The gated database now acquires the gate around every call made
+      // through it, so holding it here as well would deadlock -- and the
+      // DELETE + verify pair needs to be indivisible anyway, which is what
+      // `withTransactionAsync` (and only it) actually provides.
+      let remainingRows = 1;
+      await database.withTransactionAsync(async (tx) => {
+        await tx.runAsync('DELETE FROM telemetry_samples');
+        const remaining = await tx.getAllAsync<{ count: number }>(
+          'SELECT COUNT(*) AS count FROM telemetry_samples',
+        );
+        remainingRows = remaining[0]?.count ?? 0;
+      });
+      telemetryOk = remainingRows === 0;
+    } catch (error) {
+      // N4 (binding): symmetric with the per-circuit try/catch above -- a
+      // rejected telemetry delete/verify is reported as `ok: false` plus the
+      // fixed "telemetry" phrase in `errorText`, never as raw exception text
+      // and never as a rejection escaping into the UI's delete handler.
+      console.warn('[composition] deleteAllStoredUserData: telemetry deletion failed', error);
+      telemetryOk = false;
+    }
+  }
+  // HANDOFF release blocker: the VIN, the vehicle bindings and sweep
+  // records, and the learned circuits live outside every table the steps
+  // above touch. Always attempted, like the telemetry step; a learned circuit
+  // a surviving session still needs is kept and reported as remaining.
+  forgetVehicleIdentityInMemory();
+  let deviceDataOk = true;
+  if (db !== null) {
+    try {
+      const wipe = await wipeDeviceUserData(db);
+      deviceDataOk = wipe.ok;
+      if (!wipe.ok) {
+        console.warn('[composition] deleteAllStoredUserData: device data remained', wipe.remaining);
+      }
+    } catch (error) {
+      console.warn('[composition] deleteAllStoredUserData: device data deletion failed', error);
+      deviceDataOk = false;
+    }
+  }
+  if (!(await resyncAfterDeviceWipe())) deviceDataOk = false;
+  const sessionDataOk = result.ok && telemetryOk && deviceDataOk;
+  // Records ABOUT the sessions go only once the sessions themselves are gone:
+  // while one survives, its label and any adoption journal still describe it.
+  let sessionRecordsOk = true;
+  if (sessionDataOk && !(await forgetAbandonedAdoption())) sessionRecordsOk = false;
+  const finalResult: DeleteUserDataResult = { ...result, ok: sessionDataOk && sessionRecordsOk };
+  // M4 fix (binding): a leftover active-session pointer is meaningless once
+  // all stored data is gone -- cleared together, the SAME way session
+  // end/discard/vanished-checkpoint do (`setActiveSession(db, null)`).
+  if (finalResult.ok && db !== null) await setActiveSession(db, null);
+  // Ticket P7R E2: the unvalidated-matching log is a list of facts about
+  // sessions that no longer exist -- cleared with them, for the same reason
+  // the active-session pointer above is. A row that cannot be removed is
+  // reported: delete-all promises the records are gone, not that it tried.
+  if (finalResult.ok) {
+    unvalidatedMatchingSessionIds = new Set();
+    if (db !== null && !(await clearUnvalidatedMatchingSessionIds(db))) {
+      console.warn('[composition] could not clear the unvalidated-matching log');
+      sessionRecordsOk = false;
+      finalResult.ok = false;
+    }
+  }
+  if (sessionDataOk && historyStore !== null) await historyStore.refresh();
+
+  const errorText = finalResult.ok
+    ? null
+    : [
+        failedCircuitIds.length > 0 ? `circuits rejected: ${failedCircuitIds.join(', ')}` : null,
+        !perCircuitOk && failedCircuitIds.length === 0 ? 'one or more circuits did not verify empty' : null,
+        !telemetryOk ? 'telemetry rows remained or could not be deleted' : null,
+        !deviceDataOk ? 'vehicle or learned-circuit data remained or could not be deleted' : null,
+        !sessionRecordsOk ? 'session records remained or could not be deleted' : null,
+      ]
+        .filter((part): part is string => part !== null)
+        .join('; ') || 'delete-all failed';
+
+  return { ...finalResult, failedCircuitIds, errorText };
+}
+
+// ---------------------------------------------------------------------------
+// DevReplayScreen support (MUST DO #6, __DEV__ only).
+//
+// F4 fix (C6 residue): `startDevReplaySession` / `restoreProductionFacade` /
+// `useMockFacadeForDevReplay` all mutate the SAME shared state
+// (`replayController`/`activeController`/`facadeWrapper`'s inner) but
+// previously ran independently -- a rapid start/restore/start "storm"
+ * `Promise.race` itself attaches a handler to every promise it is given, so
+ * it can never surface as an unhandled rejection, and this function has
+ * already returned by then.
+ */
+export async function maybeDetectVehicleFromVin(): Promise<string | null> {
+  if (deviceWipeInProgress) return null; // not marked done: the next trigger after delete-all reads again.
+  if (vinDetectionState !== 'idle') return cachedDetectedVin;
+  const settings = settingsStore.getSettings();
+  // Q1 (binding): ENET only -- the ELM327 session has no mode-09 support.
+  if (settings.adapterType !== 'enet') return null;
+  if (settings.enetHost.trim() === '') return null; // nothing configured to connect to.
+  if (sharedEnetAdapterReservation.holder() !== null) return null; // never steals a running flow's adapter.
+  const token = sharedEnetAdapterReservation.tryAcquire('signalFinder');
+  if (token === null) return null; // lost the race to acquire -- another trigger point retries later.
+
+  vinDetectionState = 'attempting';
+  const generation = deviceDataGeneration;
+  const transport = new EnetTcpTransport({ host: settings.enetHost, port: settings.enetPort });
+  let vin: string | null = null;
+  try {
+    const attempt = (async (): Promise<string | null> => {
+      await transport.connect();
+      const channel = createRawUdsChannel(transport, settings.enetTesterAddress, VIN_READ_ECU);
+      return readVinFromChannel(channel);
+    })();
+    const timedOut = new Promise<'timeout'>((resolve) => {
+      setTimeout(() => resolve('timeout'), VIN_READ_OVERALL_TIMEOUT_MS);
+    });
+    const raced = await Promise.race([attempt, timedOut]);
+    vin = raced === 'timeout' ? null : raced;
+  } catch (error) {
+    console.warn('[composition] the one-shot VIN read failed', error);
+    vin = null;
+  } finally {
+    await transport.close().catch(() => undefined);
+    sharedEnetAdapterReservation.release(token);
+  }
+
+  // Delete-all ran (or is running) while this read was in flight: it has
+  // already put detection back to 'idle', so the next connection reads again.
+  if (generation !== deviceDataGeneration || deviceWipeInProgress) return null;
+  vinDetectionState = 'done';
+  cachedDetectedVin = vin;
+  if (vin !== null) {
+    // Q2 (binding): logged so a pattern can be added LATER from the real
+    // car -- never invented here. R4 fix (binding, LOW): MASKED -- a VIN is
+    // personal data, and the full value is never written to a log line; the
+    // driver reads it in full only on the Signal Finder screen / `lastSeenVin`.
+    console.info(`[composition] VIN detected: ${maskVin(vin)}`);
+    settingsStore.update({ lastSeenVin: vin });
+    applyVinAutoSelect(vin);
+  }
+  return vin;
+}
+
+const baseTelemetryProvider: TelemetryProvider = createTelemetryProvider({
+  settingsStore,
+  monotonicNow: () => telemetryClock.now(),
+  isDev: telemetryIsDev,
+  readVehicleProfileBindings: () => vehicleProfileBindingsCache,
+});
+
+/**
+ * Thin wrapper, not a second implementation: every method delegates straight
+ * to {@link baseTelemetryProvider}, EXCEPT `start()`, which also fires a
+ * defensive cache refresh (fire-and-forget -- see (c) above) before
+ * delegating. Both of this app's `start()` call sites --
+ * `startTelemetryRecording()` below and `TelemetryScreen.tsx`'s manual
+ * monitor -- go through this SAME exported singleton, so neither needs its
+ * own refresh call.
+ */
+export const telemetryProvider: TelemetryProvider = {
+  ...baseTelemetryProvider,
+  start(): void {
+    void refreshVehicleProfileBindingsCache();
+    baseTelemetryProvider.start();
+    // Ticket P4q (binding): one of this app's two VIN-detection trigger
+    // points ("when ... telemetry connects") -- fire-and-forget, and called
+    // AFTER `baseTelemetryProvider.start()` on purpose: that call may itself
+    // synchronously acquire the SAME shared reservation (owner `'provider'`)
+    // before yielding, and this must never race it for the claim -- "never
+    // blocks or steals the adapter from a running flow". `holder()` is
+    // therefore whatever telemetry's own start just left it as; a no-op
+    // (skipped, not marked done, so a LATER trigger still gets its own
+    // chance) whenever that reservation is held.
+    void maybeDetectVehicleFromVin();
+  },
+};
+
+// ---------------------------------------------------------------------------
+// G-force telemetry (Telemetry addendum â€” channel revision, binding):
+// `gforceProvider.ts`'s own module doc comment covers its math/portrait-mount
+// assumption/unit handling in full. SAME `telemetryClock` as the OBD
+// `telemetryProvider` above, so every telemetry channel (OBD + accelerometer)
+// shares one time base. Session-scoped, started/stopped alongside the OBD
+// provider by `startTelemetryRecording()`/`stopTelemetryRecording()` below --
+
+exec
+"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command '$s=Get-Content apps/mobile/src/session/composition.ts; $s[1919..1987]; $s[2390..2497]; $s[2685..2940]; $s=Get-Content apps/mobile/src/ui/screens/SettingsScreen.tsx; $s[315..365]; $s[445..500]; Get-Content apps/mobile/src/ui/screens/TestLoopScreen.tsx; rg -n '"'reset\\(|retry|pendingCircuit|lastQualified|qualifiedStart' apps/mobile/src/session/testLoopController.ts; Get-Content apps/mobile/src/session/lifecycleLock.ts" in D:\CODE\APLICTIE_Circuit
+ succeeded in 80ms:
+  // before the controller it was recording alongside is disposed -- in the
+  // normal flow `onSessionEnded` (above) already did this; this guards the
+  // 'error' terminal-state path, which does not run through `endSession()`.
+  // Awaited (F2 fix) so the final flush actually lands before disposal
+  // proceeds, instead of racing ahead of it.
+  //
+  // P5d-FIX1 H1: skipped during a Test Loop handover. Nothing is recording
+  // through a `TelemetryRecorder` at that moment (the learn phase BUFFERS,
+  // and the recorder is created a few lines later, for the session this
+  // rebuild is installing), so the only thing this call would do is stop the
+  // OBD provider that is mid-stream -- a blink in the very continuity this
+  // rebuild exists to preserve.
+  if (!testLoopHandoverActive) await stopTelemetryRecording();
+  await staleController.dispose();
+  staleFacade?.dispose();
+  installProductionController();
+  // CN-FIX6 (contracts.md's "Multi-circuit selection â€” provider ownership
+  // amendment", binding, user finding): THIS layer owns the GNSS provider
+  // singleton, so the provider is stopped here, after the fresh controller
+  // is installed.
+  //
+  // Why it is needed: `SessionController.dispose()` stops only a provider
+  // the disposed controller itself had running (`providerRunning`), and a
+  // `start()` aborted by disposal deliberately never stops a possibly-shared
+  // provider (CN-FIX5 item 2 -- core cannot know who owns it). Both are
+  // right core-side, and both can leave the OS watcher running with no
+  // session behind it: a lit location indicator and a battery drain the user
+  // can see, which is exactly the residual this closes.
+  //
+  // Why it is safe: the controller just installed is idle BY CONSTRUCTION
+  // (nothing has started it), the previous controller's pending `start()` --
+  // if any -- is serialized by the provider ahead of this stop, and every
+  // command that starts a session is itself a locked section queued BEHIND
+  // this one, so it restarts the provider through `ensureProviderRunning()`
+  // as usual. Idempotent: stopping an already-stopped provider is a no-op.
+  //
+  // Failure is logged, never thrown: a provider that cannot be stopped (a
+  // revoked permission, an OS hiccup) must not fail the selection/recovery/
+  // delete-all operation that drove this rebuild.
+  try {
+    // P5d-FIX1 H1 (binding, Codex P5d-REV1 HIGH 1): the ONE exception to the
+    // ownership stop. While a Test Loop is being learned or handed over, the
+    // watcher is NOT session-less -- the learn phase owns it, its fixes are
+    // being recorded, and the session about to start on the just-learned
+    // circuit is what this very rebuild exists to install. Stopping it here
+    // would blind the recording for the length of the rebuild, which is
+    // exactly the gap this fix wave removed. Everything the amendment guards
+    // against (a lit indicator with nothing behind it) still holds: the
+    // Test Loop path stops the provider itself when a learn phase ends
+    // without a track (`stopTestLoop`), and the session's own end still runs
+    // the ordinary teardown.
+    if (!testLoopHandoverActive) await gnssProvider?.stop();
+  } catch (error) {
+    console.warn('[composition] stopping the GNSS provider after a controller rebuild failed', error);
+  }
+}
+
+/**
+ * F3 fix: `coachingConfig()` is baked into a `SessionController` for its own
+ * whole lifetime (`SessionControllerDeps.coaching`'s own doc comment) --
+ * previously, flipping the coaching toggle before the first session (or
+ * while idle between sessions) had NO effect until some UNRELATED
+ * terminal-state rebuild happened to fire later; the UI showed a fresh
+ * coaching slot immediately, but it stayed empty. Rebuild immediately
+ * provider reference, so a rebuild AFTER the handover cleans up normally.
+ */
+let testLoopHandoverActive = false;
+/**
+ * P5d-FIX2 N2: which adoption steps have already happened, so a retry resumes
+ * instead of repeating them. Cleared when the adoption completes.
+ */
+interface AdoptionProgress {
+  circuitId: string;
+  storedProfile?: CircuitProfile;
+  selected?: boolean;
+  sessionId?: string;
+  recordingStarted?: boolean;
+  outLapSaved?: boolean;
+}
+let adoptionProgress: AdoptionProgress | null = null;
+/** P5d-FIX3 F11: bumped by every new learn phase, so a teardown queued for an older one is a no-op. */
+let teardownGeneration = 0;
+/** The teardown currently running -- a second caller awaits this one instead of starting another. */
+let teardownInFlight: Promise<void> | null = null;
+/** OBD/IMU samples recorded during the learn phase, before a session id exists to tag them with. */
+let testLoopTelemetryBuffer: TelemetrySample[] = [];
+let unsubscribeTestLoopTelemetry: (() => void) | null = null;
+let unsubscribeTestLoopGForce: (() => void) | null = null;
+/** Whether the learn phase holds its own G-force reference (released when the session takes its own). */
+let testLoopHoldsGForce = false;
+/** Bound on the learn-phase telemetry backlog -- an hour of 10 Hz channels. */
+const MAX_TEST_LOOP_TELEMETRY_SAMPLES = 360_000;
+let testLoopTelemetryDropped = 0;
+
+/** Republishes the catalog's learned registry from the store (plus any memory-only entries). */
+function publishLearnedCircuits(): void {
+  const stored: LearnedCatalogEntry[] =
+    learnedCircuitStore === null
+      ? []
+      : learnedCircuitStore.entries().map((entry) => ({
+          circuit: { profile: entry.profile, runtime: entry.runtime, corners: entry.corners },
+          listed: entry.record.saved,
+        }));
+  setLearnedCircuits([...stored, ...memoryLearnedCircuits]);
+}
+
+/**
+ * The learn phase's state machine. Created once at module load: it holds no
+ * database and no provider, so it is safe to exist before bootstrap.
+ *
+ * `makeCircuitId` is a UUID (P5d-FIX1 item 10) -- learned circuits are keyed
+ * by it in the database, and a timestamp-plus-random id invites exactly the
+ * collision that an `INSERT OR REPLACE` would silently resolve by destroying
+ * somebody's saved circuit.
+ */
+const testLoopController = new TestLoopController({
+  nowUtc: () => new Date().toISOString(),
+  makeCircuitId: () => `learned-${newLearnedCircuitId()}`,
+  makeDisplayName: (createdAtUtc) =>
+    defaultLearnedCircuitName(settingsStore.getSettings().language, createdAtUtc),
+  onLearned: (circuit) => adoptLearnedCircuit(circuit),
+});
+
+/** RFC-4122 v4 where the runtime offers one, and a random-hex fallback where it does not. */
+function newLearnedCircuitId(): string {
+  const cryptoApi = (globalThis as { crypto?: { randomUUID?: () => string } }).crypto;
+  if (typeof cryptoApi?.randomUUID === 'function') return cryptoApi.randomUUID();
+  const hex = (length: number): string =>
+    Array.from({ length }, () => Math.floor(Math.random() * 16).toString(16)).join('');
+  return `${hex(8)}-${hex(4)}-4${hex(3)}-a${hex(3)}-${hex(12)}`;
+}
+
+/**
+ * P5d-FIX2 N6 (Codex P5d-REV2): a learn phase that gives up on its own -- the
+ * sample cap, most of all -- must not leave the GNSS watcher and the channel
+ * buffers running behind a screen that now says it failed. The teardown is the
+ * same one `stopTestLoop()` performs, and it is idempotent, so the two paths
+ * cannot fight.
+ */
+testLoopController.subscribe((snapshot) => {
+  if (snapshot.phase !== 'failed') return;
+  if (testLoopProvider === null && !testLoopHoldsGForce) return;
+  void tearDownLearnPhase().catch((error) => {
+    console.warn('[composition] tearing down a failed learn phase failed', error);
+  });
+});
+
+/**
+ * The ONE teardown of a learn phase (P5d-FIX3 F11, Codex P5d-REV3): detach the
+ * learn-phase recording, release its holds, stop the buffering provider.
+ *
+ * Memoized and generation-guarded, and it takes `lifecycleLock` itself. Two
+ * paths reach it for the same failure -- `stopTestLoop()` and the auto-teardown
+ * subscription that the failed phase fires -- and before this they could both
+ * be shutting the GLOBAL providers down at once. Now the second caller awaits
+ * the first, and a teardown queued behind a learn phase that has meanwhile
+ * been restarted does nothing at all (the generation moved on).
+ */
+function tearDownLearnPhase(): Promise<void> {
+  if (teardownInFlight !== null) return teardownInFlight;
+  const generation = teardownGeneration;
+  const run = lifecycleLock
+    .run(async () => {
+      if (generation !== teardownGeneration) return;
+      await unlockedTearDownLearnPhase();
+    })
+    .finally(() => {
+      if (teardownInFlight === run) teardownInFlight = null;
+    });
+  teardownInFlight = run;
+  return run;
+}
+    telemetryProvider.start();
+  } catch (error) {
+    console.warn('[composition] telemetryProvider.start() threw synchronously (test loop)', error);
+  }
+  if (!testLoopHoldsGForce) {
+    acquireGForce();
+    testLoopHoldsGForce = true;
+  }
+}
+
+/** Detaches the learn-phase buffers. The providers themselves keep running -- the session owns them now. */
+function detachTestLoopRecording(): void {
+  unsubscribeTestLoopTelemetry?.();
+  unsubscribeTestLoopTelemetry = null;
+  unsubscribeTestLoopGForce?.();
+  unsubscribeTestLoopGForce = null;
+}
+
+/** Releases the learn phase's own G-force hold (the session takes its own in `startTelemetryRecording`). */
+async function releaseTestLoopGForce(): Promise<void> {
+  if (!testLoopHoldsGForce) return;
+  testLoopHoldsGForce = false;
+  try {
+    await releaseGForce();
+  } catch (error) {
+    console.warn('[composition] releasing the test-loop G-force hold failed', error);
+  }
+}
+
+/**
+ * Begins a learn phase: starts the GNSS provider through the buffering
+ * wrapper, starts the OBD/IMU recording, and feeds every fix to
+ * `TestLoopController`. Refuses while a session is live -- learning a track
+ * and timing laps are two different uses of the same provider.
+ */
+export async function startTestLoop(): Promise<
+  { ok: true } | { ok: false; reason: 'not-ready' | 'session-active' }
+> {
+  await ready();
+  return lifecycleLock.run(async () => {
+    if (gnssProvider === null) return { ok: false as const, reason: 'not-ready' as const };
+    if (controller !== null && sessionIsActive(currentControllerState(controller))) {
+      return { ok: false as const, reason: 'session-active' as const };
+    }
+    await disposeTestLoopProvider();
+    // A fresh learn phase: any teardown still queued for the previous one must
+    // not reach in and stop the providers this one is about to use.
+    teardownGeneration += 1;
+    const provider = new TestLoopLocationProvider(gnssProvider, (sample) =>
+      testLoopController.feed(sample),
+    );
+    testLoopProvider = provider;
+    testLoopController.start();
+    await provider.start();
+    startTestLoopRecording();
+    return { ok: true as const };
+  });
+}
+
+/**
+ * Ends a learn phase that never closed a loop. A loop that WAS learned is
+ * already a running session by the time this can be called, and is ended
+ * through the ordinary `facade.endSession()` instead.
+ */
+export async function stopTestLoop(): Promise<TestLoopSnapshot> {
+  const snapshot = testLoopController.stop();
+  if (snapshot.phase !== 'learned' && snapshot.phase !== 'adopting') {
+    await tearDownLearnPhase();
+  }
+  return snapshot;
+}
+
+/** Leaves Test Loop mode entirely (screen dismissed without a learned track). */
+export async function resetTestLoop(): Promise<void> {
+  await stopTestLoop();
+  testLoopController.reset();
+}
+
+/** P5d-FIX1 H3: retries a handover that failed, on the SAME learned geometry. */
+export function retryTestLoopAdoption(): void {
+  testLoopController.retryAdopt();
+}
+
+/** Stops and clears the buffering provider, handing the GNSS watcher back to the plain singleton. */
+async function disposeTestLoopProvider(): Promise<void> {
+  const provider = testLoopProvider;
+  testLoopProvider = null;
+  testLoopHandoverActive = false;
+  if (provider === null) return;
+  try {
+    await provider.stop();
+  } catch (error) {
+    console.warn('[composition] stopping the test-loop provider failed', error);
+  }
+}
+
+/**
+ * The moment lap 1 closes (P5d-FIX1 H1 + H3). Everything below is ONE
+ * `lifecycleLock` section, and every step is awaited: the driver is only told
+ * the track was learned once it is genuinely persisted, registered, selected
+ * and being timed. A throw anywhere here surfaces as the controller's `error`
+ * phase, with a retry -- never as a session quietly running on the previously
+ * selected circuit.
+ */
+async function adoptLearnedCircuit(circuit: TestLoopCircuit): Promise<void> {
+  const provider = testLoopProvider;
+  // From here on the learner is done, but every fix still arrives and is
+  // still kept -- the controller receives them after it is armed, in order.
+  testLoopHandoverActive = true;
+  provider?.beginHandover();
+  // P5d-FIX2 N7: the out-lap trace is the QUALIFIED lap the geometry was built
+  // from, not everything the buffer happens to hold by now.
+  const learningSamples = circuit.lapSamples;
+
+  await lifecycleLock.run(async () => {
+    // P5d-FIX2 N2 (Codex P5d-REV2 HIGH 2): every step below is recorded in a
+    // ledger, and every step is skipped when the ledger says it already
+    // happened. A retry after a failure half-way through therefore RESUMES --
+    // it never inserts a second circuit, starts a second session, or writes
+    // the out-lap trace twice.
+    const ledger = (adoptionProgress ??= { circuitId: circuit.profile.circuitId });
+    // P5d-FIX3 F10: the journal is on disk BEFORE the first side effect, so a
+    // kill at any point below leaves a state the next launch can finish or undo.
+    await stageAdoptionJournal({ circuitId: ledger.circuitId, stage: 'staged' });
+
+    // 1. Keep it. A learned circuit that cannot be stored cannot be analysed
+    //    after a restart, so this failing is a real failure, not a warning.
+    if (ledger.storedProfile === undefined) {
+      if (learnedCircuitStore !== null) {
+        const stored = await putLearnedCircuitWithFreshId(circuit);
+        ledger.storedProfile = stored.profile;
+      } else {
+        // Web dev preview: no database, so the learned circuit lives for this
+        // process only. It is still a real circuit while it lasts.
+        const profile = circuit.profile;
+        memoryLearnedCircuits = [
+          ...memoryLearnedCircuits.filter(
+            (entry) => entry.circuit.profile.circuitId !== profile.circuitId,
+          ),
+          {
+            circuit: { profile, runtime: circuit.runtime, corners: circuit.corners },
+            // Ticket D3: listed from the moment it is adopted, exactly like
+            // the stored path below.
+            listed: true,
+          },
+        ];
+        ledger.storedProfile = profile;
+      }
+      publishLearnedCircuits();
+      await stageAdoptionJournal({ circuitId: ledger.storedProfile.circuitId, stage: 'stored' });
+    }
+    const profile = ledger.storedProfile;
+
+    // 2. Select it, and rebuild the production controller for it. Both go
+    //    through the SAME unlocked routine `selectCircuit()` uses -- this
+    //    section already holds the lock, so the public wrapper would deadlock.
+    if (!ledger.selected) {
+      const entry = circuitCatalog.get(profile.circuitId);
+      if (entry === null) {
+        throw new Error(`the learned circuit ${profile.circuitId} could not be registered`);
+      }
+      await unlockedApplySelection(entry);
+      if (productionControllerCircuitId !== profile.circuitId) {
+        await unlockedRebuildProductionController();
+      }
+      ledger.selected = true;
+      await stageAdoptionJournal({ circuitId: profile.circuitId, stage: 'selected' });
+    }
+    const ctrl = controller;
+    if (ctrl === null) throw new Error('no production controller to run the learned circuit on');
+
+    // 3. Start the session on it and arm it. `start('session')` deliberately
+    //    skips calibration (the same path `resumeRecovery()` uses): the
+    //    geometry came from this driver, on this road, minutes ago -- there is
+    //    nothing a recognition lap could add to it.
+    if (ledger.sessionId === undefined) {
+      await ctrl.start('session');
+      ctrl.arm();
+      const startedSessionId = ctrl.diagnostics().sessionId;
+      if (startedSessionId === null) throw new Error('the learned-circuit session did not start');
+      ledger.sessionId = startedSessionId;
+      await stageAdoptionJournal({
+        circuitId: profile.circuitId,
+        stage: 'session-started',
+        sessionId: startedSessionId,
+      });
+    }
+    const sessionId = ledger.sessionId;
+
+    // 4. The recording pipeline the learn phase was already running now
+    //    belongs to a session: same hooks a normal start goes through.
+    //
+    //    P5d-FIX2 N3: the learn-phase subscriptions are detached in the SAME
+    //    synchronous step that attaches the session's recorder -- no await
+    //    between them, so no sample can ever reach both and be written twice.
+    if (!ledger.recordingStarted) {
+      detachTestLoopRecording();
+      startTelemetryRecording(sessionId);
+      const startedAtUtc = new Date().toISOString();
+      initializeSessionStage(sessionId, startedAtUtc);
+      ledger.recordingStarted = true;
+      if (db !== null) {
+        await setActiveSession(db, { sessionId, circuitId: profile.circuitId, startedAtUtc });
+      }
+      await flushTestLoopTelemetry();
+      await releaseTestLoopGForce();
+      await stageAdoptionJournal({ circuitId: profile.circuitId, stage: 'recording', sessionId });
+    }
+
+    // 5. The learning lap is STORED as the session's out-lap trace (lap 0),
+    //    so the drive that defined the track is not lost.
+    if (!ledger.outLapSaved) {
+      if (learningSamples.length > 0 && repository !== null) {
+        await repository.saveTelemetry(sessionId, 0, learningSamples);
+      }
+      ledger.outLapSaved = true;
+      await stageAdoptionJournal({ circuitId: profile.circuitId, stage: 'out-lap', sessionId });
+    }
+
+    // 6. Finally, hand the whole backlog to the controller in order -- the
+    //    learning lap first, then every fix that arrived while this ran. From
+    //    the next fix onwards the session is simply live.
+    provider?.flushBuffered();
+    // P5d-FIX2 N5: the handover is over. The module lets go of the buffering
+    // provider (the running controller keeps its own reference) so every later
+    // rebuild takes the ORDINARY provider-cleanup path again.
+    testLoopProvider = null;
+    testLoopHandoverActive = false;
+    adoptionProgress = null;
+    // The adoption is complete: nothing is left for a later launch to repair.
+    await clearOwnAdoptionJournal();
+  });
+}
+
+/**
+ * P5d-FIX1 item 10: a plain INSERT, with a fresh id on the (vanishingly
+ * unlikely) collision, instead of an INSERT OR REPLACE that would silently
+ * overwrite an existing learned circuit -- along with the sessions recorded
+ * on it.
+ *
+ * Ticket D3 (flow review F4) -- A CIRCUIT THE DRIVER HAS ACTUALLY DRIVEN
+ * MUST NOT BE ABLE TO VANISH. This used to store `saved: false`, which
+ * `publishLearnedCircuits` maps to `listed: false` and `circuitCatalog.list()`
+ * filters out. The only control that could ever flip it was the name field
+ * on `TestLoopScreen`, rendered only while the in-memory learn phase is still
+ * `'learned'` -- so a driver who tapped "Open dashboard" (the primary action,
+ * ABOVE that field), drove, and later selected another circuit lost the
+ * circuit, and with it every session, personal best, analysis and export
+ * recorded against it. The data stayed on disk and became unreachable.
+ *
+ * It is therefore stored as a listed circuit immediately, under the automatic
+ * name `TestLoopController.makeDisplayName` already gives it
+ * (`defaultLearnedCircuitName`, RO/EN, dated). Naming it is now a RENAME of a
+ * circuit that is already in the list (`saveLearnedCircuit`), which is a
+ * convenience rather than the only thing standing between the driver and
+ * losing their laps.
+      lastCommittedEnetTargetAddress.current = parsed;
+      settingsStore.update({ enetTargetAddress: parsed });
+      setEnetTargetAddressDraft(formatHexByte(parsed));
+    } else {
+      setEnetTargetAddressDraft(formatHexByte(lastCommittedEnetTargetAddress.current));
+    }
+  }
+
+  // Channel-specs JSON: same "invalid text stays in place, inline error below
+  // the field" pattern as `transOilPidDraft` above -- a syntactically valid
+  // draft is committed as-is (verbatim, not re-serialized) even when
+  // `validateEnetChannelSpecsJson` reports per-entry warnings (those are
+  // shown too, but never block the commit -- matching `validateEnetChannelSpecs`'s
+  // own "drop bad entries, don't reject the whole list" model).
+  const [enetChannelSpecsDraft, setEnetChannelSpecsDraft] = React.useState(settings.enetChannelSpecsJson);
+  const lastCommittedEnetChannelSpecs = React.useRef(settings.enetChannelSpecsJson);
+  const [enetChannelSpecsError, setEnetChannelSpecsError] = React.useState<string | null>(null);
+  const [enetChannelSpecsWarnings, setEnetChannelSpecsWarnings] = React.useState<readonly string[]>([]);
+  React.useEffect(() => {
+    if (settings.enetChannelSpecsJson !== lastCommittedEnetChannelSpecs.current) {
+      lastCommittedEnetChannelSpecs.current = settings.enetChannelSpecsJson;
+      setEnetChannelSpecsDraft(settings.enetChannelSpecsJson);
+    }
+  }, [settings.enetChannelSpecsJson]);
+
+  function commitEnetChannelSpecsDraft(): void {
+    const result = validateEnetChannelSpecsJson(enetChannelSpecsDraft);
+    if (result.ok) {
+      lastCommittedEnetChannelSpecs.current = enetChannelSpecsDraft;
+      settingsStore.update({ enetChannelSpecsJson: enetChannelSpecsDraft });
+      setEnetChannelSpecsError(null);
+      setEnetChannelSpecsWarnings(result.warnings);
+    } else {
+      setEnetChannelSpecsError(result.error);
+      setEnetChannelSpecsWarnings([]);
+    }
+  }
+
+  // ENET auto-discovery addendum (binding, Phase 4f): "Find adapter" --
+  // acquires the shared reservation under the 'discovery' owner (refused
+  // while the provider/probe hold it, same single-client rule as everything
+  // else that touches the adapter), runs `runDiscovery` against a REAL probe
+  // factory, and lets the user apply one result with a tap. `discoveryAbort`
+  // is a plain mutable object (not a real `AbortController` -- `runDiscovery`
+  // only ever reads `.aborted`) so the Cancel button can bound an in-flight
+  // run early.
+  const [discoveryRunning, setDiscoveryRunning] = React.useState(false);
+  const [discoveryResults, setDiscoveryResults] = React.useState<readonly DiscoveryProbeResult[] | null>(null);
+  const [discoveryError, setDiscoveryError] = React.useState<string | null>(null);
+  const discoveryAbortRef = React.useRef<{ aborted: boolean } | null>(null);
+  const discoveryTokenRef = React.useRef<ReturnType<typeof enetAdapterReservation.tryAcquire>>(null);
+  const activeBinPreset =
+    Object.entries(COVERAGE_BIN_PRESETS).find(
+      ([, thresholds]) => thresholds.length === settings.coverageBins.thresholds.length,
+    )?.[0] ?? '4 bins';
+
+  const sessionCount = sessionHistoryStore.listSessions().length;
+  const sessionActive = ACTIVE_SESSION_STATES.has(facadeState.sessionState);
+  const sessionCountLabel = `${sessionCount} stored session${sessionCount === 1 ? '' : 's'}`;
+
+  function startDeleteConfirm(): void {
+    setDeleteBanner(null);
+    setConfirmingDelete(true);
+  }
+
+  function cancelDeleteConfirm(): void {
+    setConfirmingDelete(false);
+  }
+
+  async function confirmDelete(): Promise<void> {
+    setDeleting(true);
+    try {
+      const result = await deleteAllStoredUserData();
+      // An uncommitted channel draft can hold VIN-bearing provenance and is
+      // not in the settings store, so delete-all cannot reach it from there.
+      const channelSpecs = settingsStore.getSettings().enetChannelSpecsJson;
+      lastCommittedEnetChannelSpecs.current = channelSpecs;
+      setEnetChannelSpecsDraft(channelSpecs);
+      setEnetChannelSpecsError(null);
+      setEnetChannelSpecsWarnings([]);
+      setDeleteBanner(result.ok ? 'success' : 'error');
+      setDeleteErrorText(result.ok ? null : result.errorText);
+      setDeleteRefused(!result.ok && result.reason !== undefined);
+    } catch {
+      setDeleteBanner('error');
+      setDeleteErrorText(null);
+      setDeleteRefused(false);
+    } finally {
+      setDeleting(false);
+      setConfirmingDelete(false);
+    }
+  }
+
+  return (
+    <SafeAreaView style={styles.safe} edges={['top', 'left', 'right']}>
+      <ScrollView contentContainerStyle={styles.container}>
+        <Text style={styles.title} maxFontSizeMultiplier={1.3}>
+          Settings
+        </Text>
+
+        {/* Ticket P4l-FIX1 F2 (binding): the app language. Defaulted from the
+            device locale at first hydration (`sqlSettingsStore.ts`); this row
+            is where the user overrides it. Drives the Signal Finder's
+            exportable RO/EN summary today. */}
+        <View style={styles.section}>
+          <Text style={styles.sectionLabel} maxFontSizeMultiplier={1.3}>
+            LANGUAGE
+import React, { useEffect, useState } from 'react';
+import { ActivityIndicator, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native';
+import { SafeAreaView } from 'react-native-safe-area-context';
+import type { NativeStackScreenProps } from '@react-navigation/native-stack';
+import type { RootStackParamList } from '../navigation/types';
+import { colors, fontFamily, radii, spacing, typography } from '../theme';
+import {
+  resetTestLoop,
+  retryTestLoopAdoption,
+  saveLearnedCircuit,
+  settingsStore,
+  startTestLoop,
+  stopTestLoop,
+  subscribeTestLoop,
+  testLoopSnapshot,
+} from '../../session/composition';
+import type { TestLoopSnapshot } from '../../session/testLoopController';
+import { useSettings } from '../hooks/useSettings';
+import { resolveTestLoopStrings } from './testLoopStrings';
+
+type Props = NativeStackScreenProps<RootStackParamList, 'TestLoop'>;
+
+/**
+ * Ticket P5d T2/T6 -- Test Loop mode, reached from the circuit selection
+ * screen (NOT from a developer section: learning a track is how an
+ * unregistered circuit becomes usable, which is a product feature).
+ *
+ * The screen is a thin renderer over `TestLoopController`'s snapshot: it
+ * starts and stops the learn phase, shows what has been driven so far, shows
+ * the banner when lap 1 closes, offers to save the track as a reusable
+ * circuit, and hands over to the ordinary session flow. Every sentence it
+ * shows comes from `testLoopStrings.ts`.
+ */
+export function TestLoopScreen({ navigation }: Props): React.JSX.Element {
+  const settings = useSettings(settingsStore);
+  const strings = resolveTestLoopStrings(settings.language);
+  const [snapshot, setSnapshot] = useState<TestLoopSnapshot>(() => testLoopSnapshot());
+  const [busy, setBusy] = useState(false);
+  const [name, setName] = useState('');
+  const [notice, setNotice] = useState<string | null>(null);
+
+  useEffect(() => subscribeTestLoop(setSnapshot), []);
+
+  /**
+   * Ticket D3 (second one-liner): `resetTestLoop()` had no callers anywhere in
+   * the repo, so `TestLoopController`'s terminal snapshot outlived the screen
+   * -- re-entering Test Loop after a learn phase that gave up showed the
+   * failure card from the PREVIOUS attempt, as though this visit had already
+   * failed before the driver pressed anything.
+   *
+   * Cleared on mount, and ONLY for `'failed'`: `'error'` is an adoption that
+   * failed with geometry still in hand and a Retry that must survive
+   * (`retryTestLoopAdoption`), `'learning'`/`'adopting'` are live, and
+   * `'learned'` is a running session this screen is still the face of.
+   */
+  useEffect(() => {
+    if (testLoopSnapshot().phase !== 'failed') return;
+    void resetTestLoop().catch((error: unknown) => {
+      console.warn('[TestLoopScreen] clearing the previous learn phase failed', error);
+    });
+  }, []);
+
+  const onStart = (): void => {
+    setNotice(null);
+    setBusy(true);
+    void (async () => {
+      try {
+        const result = await startTestLoop();
+        if (!result.ok) {
+          setNotice(result.reason === 'session-active' ? strings.sessionActive : strings.notReady);
+        }
+      } finally {
+        setBusy(false);
+      }
+    })();
+  };
+
+  const onStop = (): void => {
+    setBusy(true);
+    void (async () => {
+      try {
+        await stopTestLoop();
+      } finally {
+        setBusy(false);
+      }
+    })();
+  };
+
+  const onSave = (): void => {
+    const learned = snapshot.learned;
+    if (learned === null) return;
+    if (name.trim().length === 0) {
+      setNotice(strings.saveEmptyName);
+      return;
+    }
+    setBusy(true);
+    void (async () => {
+      try {
+        const result = await saveLearnedCircuit(learned.circuitId, name.trim());
+        setNotice(result.ok ? strings.saved(name.trim()) : strings.saveFailed);
+      } finally {
+        setBusy(false);
+      }
+    })();
+  };
+
+  const metres = (value: number): string => Math.round(value).toString();
+
+  return (
+    <SafeAreaView style={styles.safe} edges={['top', 'left', 'right']}>
+      <ScrollView contentContainerStyle={styles.container}>
+        <Text style={styles.title} maxFontSizeMultiplier={1.3}>
+          {strings.screenTitle}
+        </Text>
+        <View style={styles.warnCard}>
+          <Text style={styles.warnText} maxFontSizeMultiplier={1.3}>
+            {strings.intro}
+          </Text>
+          {
+            // P5d-FIX6: the public-road case is a scoped, secondary note under
+            // the racetrack framing -- never the headline.
+          }
+          <Text style={styles.muted} maxFontSizeMultiplier={1.3}>
+            {strings.streetNote}
+          </Text>
+        </View>
+        <Text style={styles.body} maxFontSizeMultiplier={1.3}>
+          {strings.howItWorks}
+        </Text>
+        <Text style={styles.muted} maxFontSizeMultiplier={1.3}>
+          {strings.cuesOff}
+        </Text>
+
+        {snapshot.phase === 'learning' ? (
+          <View style={styles.card}>
+            <View style={styles.rowCentered}>
+              <ActivityIndicator color={colors.accent} />
+              <Text style={styles.cardTitle} maxFontSizeMultiplier={1.3}>
+                {strings.learningTitle}
+              </Text>
+            </View>
+            <Text style={styles.mono} maxFontSizeMultiplier={1.3}>
+              {strings.travelled(metres(snapshot.travelledM))}
+            </Text>
+            <Text style={styles.muted} maxFontSizeMultiplier={1.3}>
+              {strings.learningHint}
+            </Text>
+          </View>
+        ) : null}
+
+        {snapshot.phase === 'learned' && snapshot.learned !== null ? (
+          <View style={styles.successCard}>
+            <Text style={styles.cardTitle} maxFontSizeMultiplier={1.3}>
+              {strings.learnedBanner(snapshot.learned.cornerCount, metres(snapshot.learned.lengthM))}
+            </Text>
+            <Text style={styles.muted} maxFontSizeMultiplier={1.3}>
+              {strings.learnedHint}
+            </Text>
+            <Text style={styles.muted} maxFontSizeMultiplier={1.3}>
+              {strings.timingStarted}
+            </Text>
+            <Text style={styles.muted} maxFontSizeMultiplier={1.3}>
+              {strings.adHocNote}
+            </Text>
+          </View>
+        ) : null}
+
+        {snapshot.phase === 'adopting' ? (
+          <View style={styles.card}>
+            <View style={styles.rowCentered}>
+              <ActivityIndicator color={colors.accent} />
+              <Text style={styles.cardTitle} maxFontSizeMultiplier={1.3}>
+                {strings.adopting}
+              </Text>
+            </View>
+          </View>
+        ) : null}
+
+        {snapshot.phase === 'error' ? (
+          <View style={styles.failCard}>
+            <Text style={styles.cardTitle} maxFontSizeMultiplier={1.3}>
+              {strings.adoptFailedTitle}
+            </Text>
+            <Text style={styles.body} maxFontSizeMultiplier={1.3}>
+              {strings.adoptFailed(snapshot.adoptError ?? '')}
+            </Text>
+            <Pressable
+              style={[styles.button, styles.primaryButton]}
+              onPress={() => retryTestLoopAdoption()}
+              accessibilityRole="button"
+              accessibilityLabel={strings.retryAdoptA11y}
+            >
+              <Text style={styles.primaryButtonText} maxFontSizeMultiplier={1.3}>
+                {strings.retryAdopt}
+              </Text>
+            </Pressable>
+          </View>
+        ) : null}
+
+        {snapshot.phase === 'failed' && snapshot.failure !== null ? (
+          <View style={styles.failCard}>
+            <Text style={styles.cardTitle} maxFontSizeMultiplier={1.3}>
+              {strings.failureTitle}
+            </Text>
+            <Text style={styles.body} maxFontSizeMultiplier={1.3}>
+              {strings.failure[snapshot.failure.reason]}
+            </Text>
+            <Text style={styles.mono} maxFontSizeMultiplier={1.3}>
+              {strings.travelled(metres(snapshot.failure.travelledM))}
+            </Text>
+            {snapshot.failure.sampleCap === undefined ? null : (
+              <Text style={styles.muted} maxFontSizeMultiplier={1.3}>
+                {strings.capDetail(snapshot.failure.sampleCap)}
+              </Text>
+            )}
+          </View>
+        ) : null}
+
+        {notice !== null ? (
+          <Text style={styles.notice} maxFontSizeMultiplier={1.3}>
+            {notice}
+          </Text>
+        ) : null}
+
+        {snapshot.phase === 'learning' ? (
+          <Pressable
+            style={[styles.button, styles.secondaryButton, busy && styles.buttonDisabled]}
+            onPress={onStop}
+            disabled={busy}
+            accessibilityRole="button"
+            accessibilityLabel={strings.stopA11y}
+          >
+            <Text style={styles.secondaryButtonText} maxFontSizeMultiplier={1.3}>
+              {strings.stop}
+            </Text>
+          </Pressable>
+        ) : snapshot.phase === 'idle' || snapshot.phase === 'failed' ? (
+          <Pressable
+            style={[styles.button, styles.primaryButton, busy && styles.buttonDisabled]}
+            onPress={onStart}
+            disabled={busy}
+            accessibilityRole="button"
+            accessibilityLabel={strings.startA11y}
+          >
+            <Text style={styles.primaryButtonText} maxFontSizeMultiplier={1.3}>
+              {snapshot.phase === 'failed' ? strings.tryAgain : strings.start}
+            </Text>
+          </Pressable>
+        ) : null}
+
+        {snapshot.phase === 'learned' ? (
+          <>
+            <Pressable
+              style={[styles.button, styles.primaryButton]}
+              onPress={() => navigation.navigate('ActiveDashboard')}
+              accessibilityRole="button"
+              accessibilityLabel={strings.openDashboardA11y}
+            >
+              <Text style={styles.primaryButtonText} maxFontSizeMultiplier={1.3}>
+                {strings.openDashboard}
+              </Text>
+            </Pressable>
+
+            <View style={styles.card}>
+              <Text style={styles.cardTitle} maxFontSizeMultiplier={1.3}>
+                {strings.saveTitle}
+              </Text>
+              <Text style={styles.muted} maxFontSizeMultiplier={1.3}>
+                {strings.saveHint}
+              </Text>
+              <TextInput
+                style={styles.input}
+                value={name}
+                onChangeText={setName}
+                editable={!busy}
+                autoCorrect={false}
+                placeholder={strings.namePlaceholder}
+                placeholderTextColor={colors.textMuted}
+                accessibilityLabel={strings.namePlaceholder}
+              />
+              <Pressable
+                style={[styles.button, styles.secondaryButton, busy && styles.buttonDisabled]}
+                onPress={onSave}
+                disabled={busy}
+                accessibilityRole="button"
+                accessibilityLabel={strings.saveA11y}
+              >
+                <Text style={styles.secondaryButtonText} maxFontSizeMultiplier={1.3}>
+                  {strings.save}
+                </Text>
+              </Pressable>
+            </View>
+          </>
+        ) : null}
+      </ScrollView>
+    </SafeAreaView>
+  );
+}
+
+const styles = StyleSheet.create({
+  safe: { flex: 1, backgroundColor: colors.background },
+  container: { padding: spacing.lg, gap: spacing.md },
+  title: { ...typography.title, color: colors.textPrimary },
+  body: { ...typography.body, color: colors.textPrimary },
+  muted: { ...typography.caption, color: colors.textSecondary },
+  mono: { fontFamily: fontFamily.monoMedium, fontSize: 15, color: colors.textPrimary },
+  notice: { ...typography.body, color: colors.accent },
+  card: {
+    backgroundColor: colors.surface,
+    borderRadius: radii.md,
+    borderWidth: 1,
+    borderColor: colors.border,
+    padding: spacing.md,
+    gap: spacing.sm,
+  },
+  successCard: {
+    backgroundColor: colors.surface,
+    borderRadius: radii.md,
+    borderWidth: 1,
+    borderColor: colors.accent,
+    padding: spacing.md,
+    gap: spacing.sm,
+  },
+  failCard: {
+    backgroundColor: colors.surface,
+    borderRadius: radii.md,
+    borderWidth: 1,
+    borderColor: colors.border,
+    padding: spacing.md,
+    gap: spacing.sm,
+  },
+  warnCard: {
+    backgroundColor: colors.surface,
+    borderRadius: radii.md,
+    borderWidth: 1,
+    borderColor: colors.border,
+    padding: spacing.md,
+    gap: spacing.sm,
+  },
+  warnText: { ...typography.body, color: colors.textPrimary },
+  cardTitle: { ...typography.subtitle, color: colors.textPrimary },
+  rowCentered: { flexDirection: 'row', alignItems: 'center', gap: spacing.sm },
+  input: {
+    borderWidth: 1,
+    borderColor: colors.border,
+    borderRadius: radii.sm,
+    paddingHorizontal: spacing.sm,
+    paddingVertical: spacing.sm,
+    color: colors.textPrimary,
+    fontFamily: fontFamily.bodyRegular,
+    fontSize: 16,
+  },
+  button: { borderRadius: radii.lg, paddingVertical: spacing.md, alignItems: 'center' },
+  buttonDisabled: { opacity: 0.5 },
+  primaryButton: { backgroundColor: colors.accent },
+  primaryButtonText: { ...typography.subtitle, color: colors.onAccent },
+  secondaryButton: { borderWidth: 1, borderColor: colors.border },
+  secondaryButtonText: { ...typography.subtitle, color: colors.textPrimary },
+});
+32: * retry), never `learned` -- a driver told the track was learned while the app
+111:  private lastQualified: LocationSample | null = null;
+113:  private qualifiedStart: LocationSample | null = null;
+119:  private pendingCircuit: TestLoopCircuit | null = null;
+154:    this.lastQualified = null;
+155:    this.qualifiedStart = null;
+159:    this.pendingCircuit = null;
+180:    const start = this.qualifiedStart;
+186:    const verdict = qualifyStep(sample, this.lastQualified, DEFAULT_TEST_LOOP_CONFIG);
+187:    if (verdict.reanchor && !verdict.accepted) this.lastQualified = sample;
+189:      const previous = this.lastQualified;
+191:      this.lastQualified = sample;
+192:      this.qualifiedStart ??= sample;
+241:  reset(): void {
+248:    this.lastQualified = null;
+249:    this.qualifiedStart = null;
+253:    this.pendingCircuit = null;
+286:    this.pendingCircuit = circuit;
+317:  retryAdopt(): void {
+318:    const circuit = this.pendingCircuit;
+/**
+ * The ONE ordering boundary for session-lifecycle work (contracts.md's
+ * "Multi-circuit selection â€” lifecycle lock amendment", binding, ticket
+ * CN-FIX3).
+ *
+ * `composition.ts` previously had THREE independent serialization mechanisms
+ * -- `selectionChain` (settings/history writes), `rebuildInFlight`
+ * (controller dispose+install), and `withDevReplayLock` (replay transitions).
+ * Each was individually correct, but they did not form a single ordering
+ * boundary: a rebuild could dispose the controller another operation had
+ * already captured, and the preflight gate could forward START_PREFLIGHT to a
+ * controller built for a circuit a queued selection was about to replace
+ * (Codex CN-REV3 findings H3/N2/N3). All three are replaced by ONE mutex:
+ * every operation that reads or replaces the production controller runs
+ * entirely inside `run()`.
+ *
+ * Semantics:
+ *  - FIFO: sections execute in the order `run()` was called, never
+ *    overlapping. A section "holds" the lock across its own `await`s, so a
+ *    controller it captured cannot be disposed underneath it.
+ *  - Errors propagate to that section's caller only, and always release the
+ *    lock (including a synchronous throw before the section's first `await`),
+ *    so one failed operation can never wedge every later one.
+ *  - Re-entrancy is NOT supported, by design: a section that calls `run()`
+ *    again would queue behind ITSELF and deadlock forever. Composition-level
+ *    code therefore calls `unlocked*` inner routines from inside a section,
+ *    never the locked public wrapper. The synchronously-detectable form of
+ *    that mistake (a `run()` issued from a section's own synchronous body) is
+ *    REFUSED with `LifecycleLockReentry` instead of hanging.
+ *
+ * Detection limit (documented deliberately): JavaScript gives no way to tell
+ * a `run()` issued from a holder's post-`await` continuation apart from a
+ * legitimate concurrent call that must queue -- both see `isHeld() === true`
+ * from an unrelated task. Refusing on `isHeld()` alone would break the
+ * queueing this lock exists for, so the assert covers the synchronous case
+ * only; the structural rule ("inside a section, call `unlocked*`") is what
+ * covers the rest, and every call site in `composition.ts` follows it.
+ */
+
+/** Thrown by `run()` when a section re-enters the lock from its own synchronous body -- a coding error that would otherwise self-deadlock. */
+export class LifecycleLockReentry extends Error {
+  constructor(message = 'lifecycleLock: re-entrant run() from inside a held section -- call the unlocked* inner routine instead') {
+    super(message);
+    this.name = 'LifecycleLockReentry';
+  }
+}
+
+export interface LifecycleLock {
+  /** Queues `fn` behind every earlier call and runs it with the lock held for its whole duration (awaits included). */
+  run<T>(fn: () => Promise<T>): Promise<T>;
+  /** `true` while a section is executing -- diagnostics/tests only; never a substitute for queueing through `run()`. */
+  isHeld(): boolean;
+}
+
+export function createLifecycleLock(): LifecycleLock {
+  let tail: Promise<unknown> = Promise.resolve();
+  let held = false;
+  /** `> 0` only while a section's SYNCHRONOUS body is on the stack -- the one re-entrancy case that is precisely detectable (see the module doc comment). */
+  let syncDepth = 0;
+
+  return {
+    isHeld(): boolean {
+      return held;
+    },
+    run<T>(fn: () => Promise<T>): Promise<T> {
+      if (syncDepth > 0) return Promise.reject(new LifecycleLockReentry());
+      const result = tail.then(async () => {
+        held = true;
+        try {
+          let started: Promise<T>;
+          syncDepth += 1;
+          try {
+            started = fn();
+          } finally {
+            syncDepth -= 1;
+          }
+          return await started;
+        } finally {
+          held = false;
+        }
+      });
+      // The chain tail must survive a rejected section (mirrors
+      // `SqlWriteGate`'s own tail discipline) -- otherwise one failure would
+      // leave every later section queued behind a never-settling link.
+      tail = result.then(
+        () => undefined,
+        () => undefined,
+      );
+      return result;
+    },
+  };
+}
+
+codex
+The fence appears to close the reported VIN resurrection window, and terminal Test Loop states now reset before controller replacement. I’m checking an edge case in the fence: it starts before the refusal checks, so a rejected deletion may affect a VIN read already in flight.
+
+exec
+"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command '$s=Get-Content apps/mobile/src/ui/screens/SettingsScreen.tsx; $s[320..367]; $s[457..492]; rg -n '"'enetChannelSpecsDraft|lastCommittedEnetChannelSpecs|commitEnet|deleting' apps/mobile/src/ui/screens/SettingsScreen.tsx; "'$s=Get-Content apps/mobile/src/session/testLoopController.ts; $s[225..335]; rg -n '"'function ready|bootstrapPromise|MID_SESSION_STATES' apps/mobile/src/session/composition.ts; Get-Content apps/mobile/test/support/sqlJsDatabase.ts; Get-Content package.json; Get-Content vitest.config.ts; git show -s --format=%B 1dee7b2; git diff 6d6f90b..d2b26f6 -- apps/mobile/src/persistence/deviceDataWipe.ts apps/mobile/src/session/composition.ts" in D:\CODE\APLICTIE_Circuit
+ succeeded in 151ms:
+
+
+
+  /**
+   * Ends the learn phase. A loop that was learned stays learned; one that was
+   * not ends in `failed` with the reason the closure test itself gives -- the
+   * driver is told which of the three conditions did not hold, never just
+   * "something went wrong".
+   */
+  stop(): TestLoopSnapshot {
+    if (this.phase === 'learning') this.fail();
+    return this.snapshot();
+  }
+
+  /** Drops everything and returns to idle (leaving the screen). */
+  reset(): void {
+    this.phase = 'idle';
+    this.samples = [];
+    this.travelled = 0;
+    this.distanceToStart = null;
+    this.departed = false;
+    this.wasInside = true;
+    this.lastQualified = null;
+    this.qualifiedStart = null;
+    this.lapSamples = [];
+    this.learned = null;
+    this.failure = null;
+    this.pendingCircuit = null;
+    this.adoptError = null;
+    this.circuitId = null;
+    this.emit();
+  }
+
+  private tryClose(): void {
+    this.circuitId ??= this.deps.makeCircuitId();
+    const result = buildTestLoopCircuit(this.samples, {
+      circuitId: this.circuitId,
+      displayName: this.deps.makeDisplayName(this.deps.nowUtc()),
+      createdAtUtc: this.deps.nowUtc(),
+    });
+    if (!result.ok) {
+      // Not a loop YET: keep driving. Only `stop()` (or the sample cap) turns
+      // a not-yet into a failure.
+      return;
+    }
+    this.lapSamples = result.lapSamples;
+    this.adopt(result);
+  }
+
+  /** The QUALIFIED fixes of the learned lap -- what the session stores as its out-lap trace. */
+  learnedLapSamples(): LocationSample[] {
+    return [...this.lapSamples];
+  }
+
+  /**
+   * P5d-FIX1 H3: the learned track is handed over and only THEN announced.
+   * `adopting` is a real, visible phase -- the driver is not told the track was
+   * learned while the app is still deciding whether it could keep it.
+   */
+  private adopt(circuit: TestLoopCircuit): void {
+    this.pendingCircuit = circuit;
+    this.phase = 'adopting';
+    this.adoptError = null;
+    this.emit();
+    void Promise.resolve()
+      .then(() => this.deps.onLearned(circuit))
+      .then(() => {
+        this.phase = 'learned';
+        this.learned = {
+          circuitId: circuit.profile.circuitId,
+          displayName: circuit.profile.displayName,
+          lengthM: polylineLength(circuit.runtime.centerline),
+          cornerCount: circuit.corners.length,
+        };
+        this.adoptError = null;
+        this.emit();
+      })
+      .catch((error: unknown) => {
+        // Never `learned`: the track exists in memory but nothing else knows
+        // about it, so timing it would be timing the wrong circuit.
+        this.phase = 'error';
+        this.learned = null;
+        this.adoptError = error instanceof Error ? error.message : String(error);
+        this.emit();
+      });
+  }
+
+  /**
+   * Retries the handover of a track that WAS learned but could not be kept
+   * (P5d-FIX1 H3). The geometry is not re-derived -- it is the same lap.
+   */
+  retryAdopt(): void {
+    const circuit = this.pendingCircuit;
+    if (this.phase !== 'error' || circuit === null) return;
+    this.adopt(circuit);
+  }
+
+  private fail(sampleCap?: number): void {
+    const closure = detectLoopClosure(this.samples);
+    this.failure = {
+      reason: closure.closed ? 'not-returned' : closure.reason,
+      travelledM: closure.closed ? closure.closure.lapLengthM : closure.travelledM,
+      ...(sampleCap === undefined ? {} : { sampleCap }),
+    };
+    this.phase = 'failed';
+    // P5d-FIX2 N6: a learn phase that gave up holds no trace. The buffers are
+    // released here, not left to grow until the screen happens to be dismissed.
+    this.samples = [];
+    this.lapSamples = [];
+    this.emit();
+  }
+1569:// independent of `bootstrapPromise` below -- so a background transition
+1602:    if (!MID_SESSION_STATES.has(currentControllerState(ctrl))) return;
+1717: * factory) -- the SAME deps `bootstrapPromise` used to construct it inline
+2111:const MID_SESSION_STATES = new Set<SessionState>(['outLap', 'timing', 'inPit', 'paused']);
+2113:/** Applies a validated circuit selection to the persisted settings ONLY -- no bootstrap-await, no session-active refusal, no serialization. Shared by the public `selectCircuit()` below and `runBootstrap()`'s own recovery-circuit switch, which CANNOT go through `selectCircuit()` itself: that function awaits `ready()` (i.e. `bootstrapPromise`), and calling it from inside `runBootstrap()` -- the very function that promise is for -- would deadlock forever. */
+3271:          // `ready()`/`bootstrapPromise` -- itself still in flight here,
+3333:let bootstrapPromise: Promise<void> = runBootstrap();
+3338:bootstrapPromise.catch(() => undefined);
+3354:  bootstrapPromise = attempt;
+3364:async function ready(): Promise<{ db: SqlDatabase | null; repository: LocalSessionRepository; controller: SessionController }> {
+3365:  await bootstrapPromise;
+3764:  if (activeController !== null && MID_SESSION_STATES.has(currentControllerState(activeController))) {
+3777:  //    `MID_SESSION_STATES` -- and step 2's rebuild deliberately no-ops
+import initSqlJs, { type Database } from 'sql.js';
+import type { SqlBindValue, SqlDatabase, SqlRunResult } from '@circuit/core';
+
+/**
+ * Local copy of packages/core/test/persistence-sql/sqlJsDatabase.ts's sql.js
+ * `SqlDatabase` adapter -- kept local for the same cross-tsconfig reason as
+ * `coreTestDoubles.ts` (see its top comment). Reuses the `sql.js` PACKAGE
+ * itself via workspace-root hoisting (ticket CONSTRAINTS: "sql.js reuse via
+ * workspace root") -- it is core's devDependency, not re-declared here.
+ */
+
+let sqlJsModulePromise: ReturnType<typeof initSqlJs> | null = null;
+function loadSqlJs(): ReturnType<typeof initSqlJs> {
+  sqlJsModulePromise ??= initSqlJs();
+  return sqlJsModulePromise;
+}
+
+export function wrapSqlJsDatabase(db: Database): SqlDatabase {
+  // Ticket P10A: the SAME serialization the real connection has.
+  // `openAppDatabase()` wraps its `SqlDatabase` in `gateSqlTransactions(...,
+  // createSqlWriteGate())`, so two transactions on the one on-device
+  // connection can never overlap. The composition tests hand this double
+  // straight to a mocked `openAppDatabase` and so had no gate at all --
+  // which stayed invisible only while nothing wrote a transaction
+  // concurrently with the repository's. P10A H2's session record (written at
+  // recording start, alongside the active-session pointer write) does, and
+  // sql.js answers an overlapping BEGIN with "cannot start a transaction
+  // within a transaction". Gating here makes the double faithful to
+  // production rather than papering over the collision.
+  let transactionTail: Promise<unknown> = Promise.resolve();
+  const handle: SqlDatabase = {
+    async execAsync(sql: string): Promise<void> {
+      db.exec(sql);
+    },
+
+    async runAsync(sql: string, params: readonly SqlBindValue[] = []): Promise<SqlRunResult> {
+      db.run(sql, params as SqlBindValue[]);
+      return { changes: db.getRowsModified() };
+    },
+
+    async getAllAsync<T>(sql: string, params: readonly SqlBindValue[] = []): Promise<T[]> {
+      const stmt = db.prepare(sql);
+      try {
+        if (params.length > 0) stmt.bind(params as SqlBindValue[]);
+        const rows: T[] = [];
+        while (stmt.step()) {
+          rows.push(stmt.getAsObject() as T);
+        }
+        return rows;
+      } finally {
+        stmt.free();
+      }
+    },
+
+    // Ticket P10B H5-B: hands the callback the transaction-scoped handle --
+    // this same adapter, which is the ungated connection. In production the
+    // gate sits ABOVE this wrapper (`gateSqlTransactions`), so a callback
+    // that used the outer handle instead would queue behind its own
+    // transaction.
+    withTransactionAsync(fn: (tx: SqlDatabase) => Promise<void>): Promise<void> {
+      const run = transactionTail.then(async () => {
+        db.run('BEGIN');
+        try {
+          await fn(handle);
+          db.run('COMMIT');
+        } catch (err) {
+          db.run('ROLLBACK');
+          throw err;
+        }
+      });
+      transactionTail = run.then(
+        () => undefined,
+        () => undefined,
+      );
+      return run;
+    },
+  };
+  return handle;
+}
+
+/** Creates a brand-new in-memory sql.js-backed `SqlDatabase` for a test. */
+export async function createSqlJsDatabase(): Promise<SqlDatabase> {
+  const SQL = await loadSqlJs();
+  return wrapSqlJsDatabase(new SQL.Database());
+}
+
+/** Wraps a `SqlDatabase` over the SAME underlying sql.js `Database` instance passed in, rather than a fresh one -- used to simulate re-opening the app's on-device database across a restart. */
+export function wrapExistingSqlJsDatabase(db: Database): SqlDatabase {
+  return wrapSqlJsDatabase(db);
+}
+
+export async function createRawSqlJsDatabase(): Promise<Database> {
+  const SQL = await loadSqlJs();
+  return new SQL.Database();
+}
+{
+  "name": "circuit-timer",
+  "private": true,
+  "version": "0.0.0",
+  "description": "GNSS track lap-timing monorepo (npm workspaces)",
+  "workspaces": [
+    "packages/*",
+    "apps/*"
+  ],
+  "scripts": {
+    "typecheck": "npm run typecheck --workspaces --if-present",
+    "test": "npm run test --workspaces --if-present",
+    "lint": "eslint .",
+    "format": "prettier --write .",
+    "generate:tmr": "node --experimental-strip-types packages/core/scripts/generate-tmr-profile.ts",
+    "generate:motorpark": "node --experimental-strip-types packages/core/scripts/generate-motorpark-profile.ts"
+  },
+  "devDependencies": {
+    "@eslint/js": "^9.13.0",
+    "@onebeyond/license-checker": "^2.2.0",
+    "@typescript-eslint/eslint-plugin": "^8.15.0",
+    "@typescript-eslint/parser": "^8.15.0",
+    "eslint": "^9.13.0",
+    "eslint-config-prettier": "^9.1.0",
+    "globals": "^15.11.0",
+    "prettier": "^3.3.3",
+    "typescript": "^5.6.3"
+  }
+}
+Get-Content : Cannot find path 'D:\CODE\APLICTIE_Circuit\vitest.config.ts' because it does not exist.
+At line:2 char:474
++ ... se.ts; Get-Content package.json; Get-Content vitest.config.ts; git sh ...
++                                      ~~~~~~~~~~~~~~~~~~~~~~~~~~~~
+    + CategoryInfo          : ObjectNotFound: (D:\CODE\APLICTI...itest.config.ts:String) [Get-Content], ItemNotFoundEx 
+   ception
+    + FullyQualifiedErrorId : PathNotFound,Microsoft.PowerShell.Commands.GetContentCommand
+ 
+P18-FIX2: close Codex P18-REV2's delete-all findings
+
+- H1: VIN detection is fenced for the whole of delete-all: no read starts
+  while it runs, and none that was running completes into it. Detection is
+  re-armed afterwards. New test drives reads from every settings change
+  during the delete (fails on FIX1: 3 reads started).
+- M2: a failed Test Loop adoption ('error') no longer blocks delete-all
+  with an instruction the UI cannot follow; delete-all abandons it.
+- M3: 'learned', 'failed' and 'error' Test Loop state (geometry, anchor
+  coordinates) is reset and the learn phase torn down, before the
+  controller rebuild; only 'learning'/'adopting' are refused.
+- M4: Settings drops an uncommitted channel-specs draft after delete-all.
+- History refresh now follows the session wipe, not the record cleanup.
+- Policy EN/RO: all custom channel definitions (tagged or typed), the
+  discarded unsaved Test Loop track, "stop the Test Loop", and VIN re-read
+  only when the adapter is free.
+
+Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
+
+diff --git a/apps/mobile/src/persistence/deviceDataWipe.ts b/apps/mobile/src/persistence/deviceDataWipe.ts
+index 971e758..a1c5c18 100644
+--- a/apps/mobile/src/persistence/deviceDataWipe.ts
++++ b/apps/mobile/src/persistence/deviceDataWipe.ts
+@@ -11,9 +11,11 @@ import { SETTINGS_KEY } from './sqlSettingsStore';
+  *
+  * A VIN is personal data in the EU, and so is what hangs off it -- the DID
+  * bindings and sweep results are fingerprints of one specific vehicle, and a
+- * learned circuit is a trace of where the driver drove. Preferences (units,
+- * language, adapter address, coaching toggles, a profile the USER picked) are
+- * not data about the driver and survive the wipe.
++ * learned circuit is a trace of where the driver drove. So are the custom
++ * channel definitions tagged from a sweep ("Tag as channel"): they are the
++ * same findings as the bindings, and their provenance text can hold the VIN.
++ * Preferences (units, language, adapter address, coaching toggles, a profile
++ * the USER picked) are not data about the driver and survive the wipe.
+  */
+ 
+ /** Mobile-owned tables whose every row is vehicle- or driver-specific. */
+@@ -33,20 +35,31 @@ export interface DeviceDataWipeResult {
+ }
+ 
+ /**
+- * The settings patch that forgets the vehicle's identity. The VIN goes; a
+- * profile the VIN auto-selected goes with it, because keeping it would keep
+- * the make and model the VIN resolved to. A profile the user chose is a
+- * preference and stays.
++ * The settings patch that forgets the vehicle's identity. The VIN goes, and
++ * so do the channel definitions tagged from this car's sweeps; a profile the
++ * VIN auto-selected goes with them, because keeping it would keep the make
++ * and model the VIN resolved to. A profile the user chose is a preference and
++ * stays.
+  */
+ export function vehicleIdentityResetPatch(settings: AppSettings): Partial<AppSettings> {
+-  if (settings.activeVehicleProfileSource !== 'vin') return { lastSeenVin: null };
++  const vehicleFindings = { lastSeenVin: null, enetChannelSpecsJson: DEFAULT_SETTINGS.enetChannelSpecsJson };
++  if (settings.activeVehicleProfileSource !== 'vin') return vehicleFindings;
+   return {
+-    lastSeenVin: null,
++    ...vehicleFindings,
+     activeVehicleProfileId: DEFAULT_SETTINGS.activeVehicleProfileId,
+     activeVehicleProfileSource: DEFAULT_SETTINGS.activeVehicleProfileSource,
+   };
+ }
+ 
++/** How many identity fields a stored settings blob still carries -- 0 when {@link vehicleIdentityResetPatch} has been applied. */
++function identityFieldsLeft(stored: Partial<AppSettings>): number {
++  let left = 0;
++  if (stored.lastSeenVin !== null && stored.lastSeenVin !== undefined) left += 1;
++  if (typeof stored.enetChannelSpecsJson === 'string' && stored.enetChannelSpecsJson !== '') left += 1;
++  if (stored.activeVehicleProfileSource === 'vin') left += 1;
++  return left;
++}
++
+ function parseSettingsRow(raw: string | undefined): Partial<AppSettings> | null {
+   if (raw === undefined) return null;
+   try {
+@@ -75,7 +88,20 @@ async function scrubStoredSettings(tx: SqlDatabase): Promise<number> {
+   await tx.runAsync('UPDATE settings SET value = ? WHERE key = ?', [JSON.stringify(scrubbed), SETTINGS_KEY]);
+   const after = await tx.getAllAsync<{ value: string }>('SELECT value FROM settings WHERE key = ?', [SETTINGS_KEY]);
+   const verified = parseSettingsRow(after[0]?.value);
+-  return verified === null || verified.lastSeenVin === null || verified.lastSeenVin === undefined ? 0 : 1;
++  return verified === null ? 1 : identityFieldsLeft(verified);
++}
++
++/**
++ * How many identity fields the stored settings row carries right now. Run
++ * after the wipe, through the same serialized handle as every settings
++ * write, it sees the row as it is once every write queued before it landed.
++ */
++export async function countStoredVehicleIdentity(db: SqlDatabase): Promise<number> {
++  const rows = await db.getAllAsync<{ value: string }>('SELECT value FROM settings WHERE key = ?', [SETTINGS_KEY]);
++  const raw = rows[0]?.value;
++  if (raw === undefined) return 0;
++  const stored = parseSettingsRow(raw);
++  return stored === null ? 1 : identityFieldsLeft(stored);
+ }
+ 
+ /**
+@@ -105,7 +131,7 @@ export async function wipeDeviceUserData(db: SqlDatabase): Promise<DeviceDataWip
+       [prefix, prefix],
+     );
+ 
+-    remaining.last_seen_vin = await scrubStoredSettings(tx);
++    remaining.vehicle_identity_settings = await scrubStoredSettings(tx);
+   });
+ 
+   const leftovers = Object.fromEntries(Object.entries(remaining).filter(([, count]) => count > 0));
+diff --git a/apps/mobile/src/session/composition.ts b/apps/mobile/src/session/composition.ts
+index 89380cb..46414e1 100644
+--- a/apps/mobile/src/session/composition.ts
++++ b/apps/mobile/src/session/composition.ts
+@@ -101,6 +101,7 @@ import {
+   type RemoveLearnedCircuitResult,
+ } from './learnedCircuitStore';
+ import {
++  ADOPTION_JOURNAL_KEY,
+   claimAdoptionJournal,
+   clearClaimedJournal,
+   deleteOrphanSession,
+@@ -127,7 +128,11 @@ import {
+   writeSessionVehicleSnapshot,
+   type SessionVehicleSnapshot,
+ } from '../persistence/sessionVehicleSnapshot';
+-import { vehicleIdentityResetPatch, wipeDeviceUserData } from '../persistence/deviceDataWipe';
++import {
++  countStoredVehicleIdentity,
++  vehicleIdentityResetPatch,
++  wipeDeviceUserData,
++} from '../persistence/deviceDataWipe';
+ import { createRawUdsChannel } from './didSweepController';
+ import { EnetTcpTransport } from './enetTcpTransport';
+ import { enetAdapterReservation as sharedEnetAdapterReservation } from './enetAdapterReservation';
+@@ -722,9 +727,13 @@ let vehicleProfileBindingsCache: readonly VehicleProfileBindingLike[] = [];
+  * "telemetry must never fail to start" discipline in this module.
+  */
+ export async function refreshVehicleProfileBindingsCache(): Promise<void> {
++  const generation = deviceDataGeneration;
+   try {
+     const store = createVehicleProfileBindingStore(getTelemetryReadDb());
+-    vehicleProfileBindingsCache = await store.listBindings(getActiveVehicleProfileId());
++    const bindings = await store.listBindings(getActiveVehicleProfileId());
++    // A read that raced delete-all may hold rows the wipe removed.
++    if (generation !== deviceDataGeneration) return;
++    vehicleProfileBindingsCache = bindings;
+   } catch (error) {
+     console.warn('[composition] could not refresh the vehicle profile bindings cache', error);
+   }
+@@ -862,6 +871,12 @@ type VinDetectionState = 'idle' | 'attempting' | 'done';
+ let vinDetectionState: VinDetectionState = 'idle';
+ /** The result of the ONE completed attempt this app run has made (or will make) -- `null` before `vinDetectionState === 'done'`, and forever after if the read found nothing usable. */
+ let cachedDetectedVin: string | null = null;
++/**
++ * Bumped by delete-all. A VIN read that started before the wipe and finishes
++ * after it belongs to data the driver asked to erase, so it is dropped rather
++ * than cached, persisted or used for auto-select.
++ */
++let deviceDataGeneration = 0;
+ 
+ /**
+  * Codex R2 fix (ticket P4q follow-up, binding, MEDIUM): "the explicit-choice
+@@ -1006,6 +1021,7 @@ export async function maybeDetectVehicleFromVin(): Promise<string | null> {
+   if (token === null) return null; // lost the race to acquire -- another trigger point retries later.
+ 
+   vinDetectionState = 'attempting';
++  const generation = deviceDataGeneration;
+   const transport = new EnetTcpTransport({ host: settings.enetHost, port: settings.enetPort });
+   let vin: string | null = null;
+   try {
+@@ -1027,6 +1043,9 @@ export async function maybeDetectVehicleFromVin(): Promise<string | null> {
+     sharedEnetAdapterReservation.release(token);
+   }
+ 
++  // Delete-all ran while this read was in flight: it has already put
++  // detection back to 'idle', so the next connection reads again.
++  if (generation !== deviceDataGeneration) return null;
+   vinDetectionState = 'done';
+   cachedDetectedVin = vin;
+   if (vin !== null) {
+@@ -3612,27 +3631,49 @@ export interface AggregatedDeleteUserDataResult extends DeleteUserDataResult {
+    * verified on those paths. `'SESSION_ACTIVE'`: a session is genuinely
+    * mid-drive (CN-FIX4, facade boundary amendment). `'DEV_REPLAY_ACTIVE'`: a
+    * `__DEV__` replay controller is installed (CN-FIX5, closing amendment).
++   * `'TEST_LOOP_ACTIVE'`: a Test Loop is learning, adopting, or waiting for a
++   * retry of its adoption.
+    */
+-  reason?: 'SESSION_ACTIVE' | 'DEV_REPLAY_ACTIVE';
++  reason?: 'SESSION_ACTIVE' | 'DEV_REPLAY_ACTIVE' | 'TEST_LOOP_ACTIVE';
+ }
+ 
+ /**
+- * The in-memory mirror of {@link wipeDeviceUserData}: drops every cached copy
+- * of what that wipe deleted, so nothing on screen or in the next session
+- * start still carries it. Runs whether or not the on-disk wipe verified clean
+- * -- a cache must never outlive the rows it was read from.
++ * The in-memory half of {@link wipeDeviceUserData}, run BEFORE the disk wipe:
++ * the settings store rewrites its whole blob on every `update()`, so the VIN
++ * has to leave memory first. Any settings write queued before this lands
++ * ahead of the wipe's transaction and is scrubbed by it; every write after it
++ * carries no VIN. The generation bump makes a VIN read or a bindings read
++ * already in flight drop its result instead of restoring it.
+  */
+-async function forgetDeviceUserDataInMemory(): Promise<void> {
+-  settingsStore.update(vehicleIdentityResetPatch(settingsStore.getSettings()));
++function forgetVehicleIdentityInMemory(): void {
++  deviceDataGeneration += 1;
++  vinDetectionState = 'idle';
+   cachedDetectedVin = null;
++  setVinAutoDetectNotice(null);
++  settingsStore.update(vehicleIdentityResetPatch(settingsStore.getSettings()));
+   vehicleProfileBindingsCache = [];
+   sessionVehicleSnapshots.clear();
++}
++
++/**
++ * Brings every in-memory copy of the learned circuits and the bindings back in
++ * line with the disk after the wipe. Resolves `false` when the learned
++ * circuits could not be re-read (the deleted geometry may then still be
++ * selectable) or the stored settings still carry the vehicle's identity:
++ * either way delete-all must not report success.
++ */
++async function resyncAfterDeviceWipe(): Promise<boolean> {
++  // Again, now the wipe has committed: a VIN or bindings read that finished
++  // while it ran may have put the old values back in memory.
++  forgetVehicleIdentityInMemory();
++  let learnedOk = true;
+   memoryLearnedCircuits = [];
+   if (learnedCircuitStore !== null) {
+     try {
+       await learnedCircuitStore.refresh();
+     } catch (error) {
+       console.warn('[composition] could not refresh learned circuits after delete-all', error);
++      learnedOk = false;
+     }
+   }
+   publishLearnedCircuits();
+@@ -3646,6 +3687,39 @@ async function forgetDeviceUserDataInMemory(): Promise<void> {
+     }
+   }
+   await refreshVehicleProfileBindingsCache();
++  // The last word on the VIN: read back after every settings write queued so
++  // far, so a write that carried the old blob cannot hide behind the wipe.
++  let identityOk = true;
++  if (db !== null) {
++    try {
++      identityOk = (await countStoredVehicleIdentity(db)) === 0;
++    } catch (error) {
++      console.warn('[composition] could not verify the vehicle identity was deleted', error);
++      identityOk = false;
++    }
++  }
++  return learnedOk && identityOk;
++}
++
++/**
++ * Drops what an abandoned Test Loop adoption left behind: its in-memory
++ * ledger and its journal row. Only called once the sessions are gone -- the
++ * journal names a session and a circuit, and a retry must never resume onto a
++ * circuit this wipe deleted. Resolves `false` when the row could not be
++ * removed.
++ */
++async function forgetAbandonedAdoption(): Promise<boolean> {
++  adoptionProgress = null;
++  adoptionJournalId = null;
++  adoptionJournalOwned = null;
++  if (db === null) return true;
++  try {
++    await db.runAsync('DELETE FROM settings WHERE key = ?', [ADOPTION_JOURNAL_KEY]);
++    return true;
++  } catch (error) {
++    console.warn('[composition] could not clear the test-loop adoption journal', error);
++    return false;
++  }
+ }
+ 
+ export async function deleteAllStoredUserData(): Promise<AggregatedDeleteUserDataResult> {
+@@ -3694,6 +3768,23 @@ async function unlockedDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDa
+       errorText: 'a developer replay or mock session is active -- leave the Dev Replay screen before deleting all data',
+     };
+   }
++  // 1c. REFUSE while a Test Loop is learning, adopting, or holding a failed
++  //    adoption for retry. Its geometry and its adoption ledger live in
++  //    memory: a wipe underneath would let the adoption (or its retry) store
++  //    the circuit again, or resume onto the fallback circuit's controller
++  //    while writing the out-lap for the one just deleted. Leaving the Test
++  //    Loop screen ends all three.
++  const testLoopPhase = testLoopController.snapshot().phase;
++  if (testLoopPhase === 'learning' || testLoopPhase === 'adopting' || testLoopPhase === 'error') {
++    return {
++      ok: false,
++      remainingSessionCount: 0,
++      referenceLapCleared: false,
++      failedCircuitIds: [],
++      reason: 'TEST_LOOP_ACTIVE',
++      errorText: 'a test loop is in progress -- leave the Test Loop screen before deleting all data',
++    };
++  }
+   // 2. Otherwise: drop any pending recovery and REPLACE the production
+   //    controller with a fresh, idle one BEFORE deleting anything. A
+   //    terminal (`sessionComplete`) controller still holds a `sessionId`
+@@ -3790,6 +3881,7 @@ async function unlockedDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDa
+   // records, and the learned circuits live outside every table the steps
+   // above touch. Always attempted, like the telemetry step; a learned circuit
+   // a surviving session still needs is kept and reported as remaining.
++  forgetVehicleIdentityInMemory();
+   let deviceDataOk = true;
+   if (db !== null) {
+     try {
+@@ -3803,21 +3895,27 @@ async function unlockedDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDa
+       deviceDataOk = false;
+     }
+   }
+-  await forgetDeviceUserDataInMemory();
+-  const finalResult: DeleteUserDataResult = { ...result, ok: result.ok && telemetryOk && deviceDataOk };
++  if (!(await resyncAfterDeviceWipe())) deviceDataOk = false;
++  const sessionDataOk = result.ok && telemetryOk && deviceDataOk;
++  // Records ABOUT the sessions go only once the sessions themselves are gone:
++  // while one survives, its label and any adoption journal still describe it.
++  let sessionRecordsOk = true;
++  if (sessionDataOk && !(await forgetAbandonedAdoption())) sessionRecordsOk = false;
++  const finalResult: DeleteUserDataResult = { ...result, ok: sessionDataOk && sessionRecordsOk };
+   // M4 fix (binding): a leftover active-session pointer is meaningless once
+   // all stored data is gone -- cleared together, the SAME way session
+   // end/discard/vanished-checkpoint do (`setActiveSession(db, null)`).
+   if (finalResult.ok && db !== null) await setActiveSession(db, null);
+   // Ticket P7R E2: the unvalidated-matching log is a list of facts about
+   // sessions that no longer exist -- cleared with them, for the same reason
+-  // the active-session pointer above is. Best-effort, exactly like the log's
+-  // own writes: a stale label on a deleted session must never turn a
+-  // successful wipe into a failed one.
++  // the active-session pointer above is. A row that cannot be removed is
++  // reported: delete-all promises the records are gone, not that it tried.
+   if (finalResult.ok) {
+     unvalidatedMatchingSessionIds = new Set();
+     if (db !== null && !(await clearUnvalidatedMatchingSessionIds(db))) {
+       console.warn('[composition] could not clear the unvalidated-matching log');
++      sessionRecordsOk = false;
++      finalResult.ok = false;
+     }
+   }
+   if (finalResult.ok && historyStore !== null) await historyStore.refresh();
+@@ -3829,6 +3927,7 @@ async function unlockedDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDa
+         !perCircuitOk && failedCircuitIds.length === 0 ? 'one or more circuits did not verify empty' : null,
+         !telemetryOk ? 'telemetry rows remained or could not be deleted' : null,
+         !deviceDataOk ? 'vehicle or learned-circuit data remained or could not be deleted' : null,
++        !sessionRecordsOk ? 'session records remained or could not be deleted' : null,
+       ]
+         .filter((part): part is string => part !== null)
+         .join('; ') || 'delete-all failed';
+
+exec
+"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'Get-Content apps/mobile/vitest.config.ts; Get-Content apps/mobile/package.json; $s=Get-Content apps/mobile/src/session/composition.ts; $s[1239..1289]; $s[2920..2949]; $s[3160..3230]; $s[3360..3385]; Get-Content apps/mobile/src/persistence/activeSession.ts -TotalCount 140; rg -n '"'repair|stage|deleteOrphan|clearClaimed' apps/mobile/src/session/adoptionJournal.ts" in D:\CODE\APLICTIE_Circuit
+ succeeded in 56ms:
+Get-Content : Cannot find path 'D:\CODE\APLICTIE_Circuit\apps\mobile\src\persistence\activeSession.ts' because it does 
+not exist.
+At line:2 char:200
++ ... 360..3385]; Get-Content apps/mobile/src/persistence/activeSession.ts  ...
++                 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
+    + CategoryInfo          : ObjectNotFound: (D:\CODE\APLICTI...ctiveSession.ts:String) [Get-Content], ItemNotFoundEx 
+   ception
+    + FullyQualifiedErrorId : PathNotFound,Microsoft.PowerShell.Commands.GetContentCommand
+ 
+19: *   * NOBODY CLEARS SOMEONE ELSE'S JOURNAL. `clearClaimedJournal` is a
+23: * The journal is cleared only after a repair fully succeeded; a failed repair
+29:/** After this many failed repairs the journal is abandoned, with a notice -- never retried forever. */
+33:  | 'staged'
+44:  stage: AdoptionStage;
+46:  /** How many launches have tried to repair it. */
+48:  /** The launch that currently owns the repair. */
+56:  /** True when the attempt budget is spent: the caller abandons the journal instead of repairing. */
+74:    if (typeof journal.circuitId !== 'string' || typeof journal.stage !== 'string') return null;
+78:      stage: journal.stage,
+117: * there is nothing to repair or another launch got there first.
+151:export async function clearClaimedJournal(
+183:export async function deleteOrphanSession(db: SqlDatabase, sessionId: string): Promise<void> {
+
+exec
+"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "@'
+const fs=require('fs'), ts=require('typescript'), vm=require('vm');
+const src=fs.readFileSync('apps/mobile/src/session/composition.ts','utf8');
+const ast=ts.createSourceFile('composition.ts',src,ts.ScriptTarget.Latest,true);
+const names=new Set(['maybeDetectVehicleFromVin','unlockedDeleteAllStoredUserData','runDeleteAllStoredUserData','ready']);
+const functions=ast.statements.filter(n=>ts.isFunctionDeclaration(n)&&names.has(n.name?.text)).map(n=>n.getText(ast).replace(/"'^export /,'"'')).join('\\n');
+let finishClose, reachedClose; const inClose=new Promise(r=>reachedClose=r); const closing=new Promise(r=>finishClose=r);
+const c={console, setTimeout:()=>0, deviceWipeInProgress:false,vinDetectionState:'idle',cachedDetectedVin:null,deviceDataGeneration:0,settingsStore:{getSettings:()=>({adapterType:'enet',enetHost:'host'})},sharedEnetAdapterReservation:{holder:()=>null,tryAcquire:()=>1,release:()=>{}},EnetTcpTransport:class {async connect(){} close(){reachedClose();return closing;}},createRawUdsChannel:()=>({}),readVinFromChannel:async()=> 'WZ1DB0C04LW000001',VIN_READ_ECU:1,VIN_READ_OVERALL_TIMEOUT_MS:5000,bootstrapPromise:Promise.resolve(),repository:{},controller:{},db:null,activeController:{},MID_SESSION_STATES:new Set(['timing']),currentControllerState:()=> 'timing'};
+vm.createContext(c); vm.runInContext(ts.transpileModule(functions,{compilerOptions:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.None}}).outputText,c);
+const locksrc=fs.readFileSync('apps/mobile/src/session/lifecycleLock.ts','utf8');const mod={exports:{}};vm.runInNewContext(ts.transpileModule(locksrc,{compilerOptions:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.CommonJS}}).outputText,{exports:mod.exports});const lock=mod.exports.createLifecycleLock();
+(async()=>{const read=c.maybeDetectVehicleFromVin();await inClose;finishClose();const deletion=lock.run(c.unlockedDeleteAllStoredUserData);console.log(JSON.stringify({read:await read,deletion:await deletion,state:c.vinDetectionState,fence:c.deviceWipeInProgress,generation:c.deviceDataGeneration}));console.log(JSON.stringify({nextTrigger:await c.maybeDetectVehicleFromVin(),state:c.vinDetectionState}));})().catch(console.error);
+'@ | node" in D:\CODE\APLICTIE_Circuit
+ succeeded in 198ms:
+{"read":null,"deletion":{"ok":false,"remainingSessionCount":0,"referenceLapCleared":false,"failedCircuitIds":[],"reason":"SESSION_ACTIVE","errorText":"a session is currently active -- end it before deleting all data"},"state":"attempting","fence":false,"generation":0}
+{"nextTrigger":null,"state":"attempting"}
+
+exec
+"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "node --input-type=module -e 'import { startVitest } from \"vitest/node\"; const ctx=await startVitest(\"test\",[\"test/session/composition.deleteAllVinFence.test.ts\",\"test/session/composition.testLoop.test.ts\"],{root:process.cwd(),config:false,cache:false,watch:false}); if(ctx) await ctx.close();'" in D:\CODE\APLICTIE_Circuit\apps\mobile
+ exited 1 in 0ms:
+file:///D:/CODE/APLICTIE_Circuit/apps/mobile/[eval1]:1
+import { startVitest } from vitest/node; const ctx=await startVitest(test,[test/session/composition.deleteAllVinFence.test.ts,test/session/composition.testLoop.test.ts],{root:process.cwd(),config:false,cache:false,watch:false}); if(ctx) await ctx.close();
+                            ^^^^^^
+
+SyntaxError: Unexpected identifier 'vitest'
+    at compileSourceTextModule (node:internal/modules/esm/utils:318:16)
+    at ModuleLoader.createModuleWrap (node:internal/modules/esm/loader:210:12)
+    at ModuleLoader.eval (node:internal/modules/esm/loader:248:23)
+    at node:internal/process/execution:77:24
+    at asyncRunEntryPointWithESMLoader (node:internal/modules/run_main:101:11)
+    at Object.runEntryPointWithESMLoader (node:internal/modules/run_main:123:19)
+    at evalModuleEntryPoint (node:internal/process/execution:76:47)
+    at node:internal/main/eval_string:37:3
+
+Node.js v24.18.0
+
+exec
+"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "@'
+import { startVitest } from 'vitest/node';
+const ctx=await startVitest('test',['test/session/composition.deleteAllVinFence.test.ts','test/session/composition.testLoop.test.ts'],{root:process.cwd(),config:false,cache:false,watch:false}); if(ctx) await ctx.close();
+'@ | node --input-type=module" in D:\CODE\APLICTIE_Circuit\apps\mobile
+ exited 1 in 606ms:
+
+ RUN  v2.1.9 D:/CODE/APLICTIE_Circuit/apps/mobile
+
+
+⎯⎯⎯⎯⎯⎯ Unhandled Errors ⎯⎯⎯⎯⎯⎯
+
+Vitest caught 2 unhandled errors during the test run.
+This might cause false positive tests. Resolve unhandled errors to make sure your tests are not affected.
+
+⎯⎯⎯⎯⎯⎯ Unhandled Error ⎯⎯⎯⎯⎯⎯⎯
+Error: EPERM: operation not permitted, mkdir 'C:\Users\dobri\AppData\Local\Temp\lcO7swLgtWgDlIDXCICEN'
+ ❯ mkdir node:internal/fs/promises:859:10
+ ❯ Proxy.fetch ../../node_modules/vitest/dist/chunks/resolveConfig.rBxzbVsl.js:6636:9
+ ❯ EventEmitter.onMessage ../../node_modules/vitest/dist/chunks/index.68735LiX.js:91:20
+
+
+⎯⎯⎯⎯⎯⎯ Unhandled Error ⎯⎯⎯⎯⎯⎯⎯
+Error: EPERM: operation not permitted, mkdir 'C:\Users\dobri\AppData\Local\Temp\lcO7swLgtWgDlIDXCICEN'
+ ❯ mkdir node:internal/fs/promises:859:10
+ ❯ Proxy.fetch ../../node_modules/vitest/dist/chunks/resolveConfig.rBxzbVsl.js:6636:9
+ ❯ EventEmitter.onMessage ../../node_modules/vitest/dist/chunks/index.68735LiX.js:91:20
+
+⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯
+
+ Test Files  no tests
+      Tests  no tests
+     Errors  2 errors
+   Start at  19:28:24
+   Duration  181ms (transform 11ms, setup 0ms, collect 0ms, tests 0ms, environment 0ms, prepare 0ms)
+
+
+codex
+I reproduced a new refusal-path bug: if a VIN read finishes while delete-all is checking whether to refuse, the fence discards it but leaves detection at `attempting`. Later triggers then skip detection until restart or a subsequent wipe resets it. Vitest also failed before collecting tests because the read-only sandbox denied its temporary-directory creation.
+
+exec
+"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -n 'setActiveSession|repair.*Adoption|repairInterrupted|claimAdoptionJournal|adoptionJournalOwned' apps/mobile/src/session/composition.ts; rg -n 'function setActiveSession' apps/mobile/src; "'$s=Get-Content apps/mobile/src/session/composition.ts; $s[2520..2640]; $s[3060..3125]; $s=Get-Content apps/mobile/src/ui/screens/SettingsScreen.tsx; $s[918..941]; Get-Content apps/mobile/src/persistence/sqlSettingsStore.ts -TotalCount 170; rg -n '"'VIN|data|export|preferences' docs/legal/privacy-policy.en.md | Select-Object -Last 25; rg -n 'error|delete-all|selected.*fail|journal' apps/mobile/test/session/composition.testLoop.test.ts" in D:\CODE\APLICTIE_Circuit
+ succeeded in 71ms:
+                    />
+                  </View>
+                  <View style={styles.fieldColumn}>
+                    <Text style={styles.fieldLabel} maxFontSizeMultiplier={1.3}>
+                      Channel specs (JSON, advanced)
+                    </Text>
+                    <TextInput
+                      style={styles.multilineInput}
+                      value={enetChannelSpecsDraft}
+                      onChangeText={(text) => {
+                        setEnetChannelSpecsDraft(text);
+                        if (enetChannelSpecsError !== null) setEnetChannelSpecsError(null);
+                      }}
+                      onBlur={commitEnetChannelSpecsDraft}
+                      placeholder="blank = built-in defaults"
+                      placeholderTextColor={colors.textMuted}
+                      autoCapitalize="none"
+                      autoCorrect={false}
+                      multiline
+                      numberOfLines={4}
+                      accessibilityLabel="ENET channel specs, JSON"
+                    />
+                  </View>
+                  {enetChannelSpecsError === null ? null : (
+import type { SqlDatabase } from '@circuit/core';
+import {
+  DEFAULT_SETTINGS,
+  defaultLanguageForLocale,
+  readDeviceLocale,
+  type AppSettings,
+  type SettingsStore,
+} from '../session/settingsStore';
+import { repairPersistedEnetSettings } from '../session/enetSettingsValidation';
+
+export const SETTINGS_KEY = 'app-settings';
+
+function isPartialAppSettings(value: unknown): value is Partial<AppSettings> {
+  return typeof value === 'object' && value !== null;
+}
+
+/**
+ * `SettingsStore` backed by the `settings` key-value table added in the
+ * persistence-sql v2 migration (MUST DO #4) -- same on-device SQLite
+ * database as session/lap/checkpoint/reference-lap data, not a separate
+ * store. `getSettings()`/`subscribe()` stay synchronous (matching
+ * `SettingsStore`'s existing contract, which `SettingsScreen` calls
+ * directly): the current value is cached in memory and hydrated once from
+ * disk in `create()`; every `update()` applies to the cache immediately and
+ * persists in the background.
+ */
+export class SqlSettingsStore implements SettingsStore {
+  private settings: AppSettings;
+  private readonly listeners = new Set<(s: AppSettings) => void>();
+
+  private constructor(
+    private readonly db: SqlDatabase,
+    initial: AppSettings,
+    /**
+     * Ticket P4p G1 (binding): did the persisted ROW itself carry a usable
+     * `activeVehicleProfileId`? Same question (and same reason) as N7's
+     * `rowHasLanguage` above: `DEFAULT_SETTINGS.activeVehicleProfileId` is a
+     * perfectly valid `'generic'`, so after the merge a row written before
+     * this setting existed is indistinguishable from a deliberate "generic"
+     * choice. `composition.ts`'s one-time migration heuristic runs only while
+     * this is `false`, so a profile the USER chose is never overridden.
+     */
+    readonly activeVehicleProfileIdWasStored: boolean,
+  ) {
+    this.settings = initial;
+  }
+
+  /**
+   * `readLocale` is injectable ONLY so the language-default rule can be
+   * pinned by a test without a native locale API; production always uses the
+   * device's own `readDeviceLocale`.
+   */
+  static async create(
+    db: SqlDatabase,
+    readLocale: () => string | null = readDeviceLocale,
+  ): Promise<SqlSettingsStore> {
+    const rows = await db.getAllAsync<{ value: string }>('SELECT value FROM settings WHERE key = ?', [
+      SETTINGS_KEY,
+    ]);
+    let initial = DEFAULT_SETTINGS;
+    // Ticket P4l-FIX4 N7 (binding, Codex P4l-REV2b finding 11): whether the
+    // USER ever chose a language is a fact about the ROW, and it has to be
+    // captured BEFORE the merge below -- `DEFAULT_SETTINGS.language` is a
+    // perfectly valid `'en'`, so after merging, a row written by any build
+    // that predates the setting is indistinguishable from a deliberate
+    // English choice, and the device-locale default was never reachable.
+    let rowHasLanguage = false;
+    // Ticket P4p G1: the same ROW-level question for the active vehicle
+    // profile -- captured here, before the merge, for the reason N7 documents.
+    let rowHasActiveVehicleProfileId = false;
+    const raw = rows[0]?.value;
+    if (raw !== undefined) {
+      try {
+        const parsed: unknown = JSON.parse(raw);
+        if (isPartialAppSettings(parsed)) {
+          rowHasLanguage = parsed.language === 'ro' || parsed.language === 'en';
+          rowHasActiveVehicleProfileId =
+            typeof parsed.activeVehicleProfileId === 'string' && parsed.activeVehicleProfileId.trim() !== '';
+          initial = { ...DEFAULT_SETTINGS, ...parsed };
+        }
+      } catch {
+        // Corrupt/legacy row: fall back to defaults rather than throw --
+        // settings are non-critical and never block app startup.
+      }
+    }
+    // P4e-FIX2 L1 fix (binding, review finding): `isPartialAppSettings` only
+    // proves the persisted JSON was "some object" -- a PRESENT but malformed
+    // ENET field (e.g. `enetPort: 70000`, `enetTesterAddress: -1`) would
+    // otherwise overwrite `DEFAULT_SETTINGS` unchecked. `repairPersistedEnetSettings`
+    // resets exactly those fields back to their defaults when structurally
+    // invalid; every other (including every ELM327) field is untouched.
+    // Applied unconditionally (a no-op when `initial` is already
+    // `DEFAULT_SETTINGS`, i.e. no row / a corrupt row) so there is only one
+    // hydration path to reason about.
+    initial = repairPersistedEnetSettings(initial);
+    // Field revision (2026-08-27, binding, "hidden developer mode"): a
+    // present-but-malformed persisted value (e.g. from a corrupt row, or a
+    // future schema change) must never leave dev-only ENET tools visible in
+    // a release build by accident -- repaired back to `false` (never
+    // trusted as truthy) the same defensive way `repairPersistedEnetSettings`
+    // above handles the ENET fields.
+    if (typeof initial.developerModeEnabled !== 'boolean') {
+      initial = { ...initial, developerModeEnabled: false };
+    }
+    // Ticket P5c-B (contracts.md R2-3): the trackday suggestion stage is
+    // opt-in, so a present-but-malformed persisted value must never be read
+    // as truthy and silently switch it on -- repaired back to `false` exactly
+    // like `developerModeEnabled` above. An install from before this setting
+    // existed carries no key at all and takes `DEFAULT_SETTINGS`' `false`.
+    if (typeof initial.suggestionsEnabled !== 'boolean') {
+      initial = { ...initial, suggestionsEnabled: false };
+    }
+    // Ticket P4l-FIX1 F2 (binding), corrected by P4l-FIX4 N7: the language
+    // DEFAULT comes from the device locale, applied here and only here --
+    // whenever the persisted ROW did not itself carry a valid choice (no row
+    // at all, a row from before the setting existed, or a value outside the
+    // two-value vocabulary, repaired the same defensive way
+    // `developerModeEnabled` above is). A user's own stored choice always
+    // wins and is never re-derived on later launches.
+    if (!rowHasLanguage) {
+      initial = { ...initial, language: defaultLanguageForLocale(readLocale()) };
+    }
+    // Ticket P4p G1 (binding): a present-but-malformed profile id must never
+    // reach the binding cache -- an unresolvable id would silently poll
+    // nothing at all. Repaired back to `'generic'` the same defensive way the
+    // boolean settings above are, and NOT counted as a stored choice (the
+    // migration heuristic then still gets its one chance).
+    if (!rowHasActiveVehicleProfileId) {
+      initial = { ...initial, activeVehicleProfileId: DEFAULT_SETTINGS.activeVehicleProfileId };
+    }
+    // Codex R2 fix (ticket P4q follow-up, binding): a present-but-malformed
+    // `activeVehicleProfileSource` (outside the three-value vocabulary) must
+    // never be read as `'user'` by accident -- that would either wrongly
+    // block a real VIN auto-select, or (worse, if it decoded to something
+    // else entirely) let one silently overwrite a choice the repair itself
+    // cannot prove was ever explicit. Repaired back to the default, the same
+    // defensive discipline every other enum-like field above follows.
+    if (
+      initial.activeVehicleProfileSource !== 'user' &&
+      initial.activeVehicleProfileSource !== 'vin' &&
+      initial.activeVehicleProfileSource !== 'default'
+    ) {
+      initial = { ...initial, activeVehicleProfileSource: DEFAULT_SETTINGS.activeVehicleProfileSource };
+    }
+    // Ticket P4q (binding): a present-but-malformed persisted VIN (wrong
+    // type from a corrupt/legacy row) must never reach the Signal Finder
+    // screen or the VIN-matching logic -- repaired back to `null` (never read
+    // yet), the same defensive way the boolean/ENET fields above are.
+    if (typeof initial.lastSeenVin !== 'string' && initial.lastSeenVin !== null) {
+      initial = { ...initial, lastSeenVin: null };
+    }
+    // Ticket P6a (binding): both new flags are opt-in and both change how a
+    // field-confirmed signal path behaves, so a present-but-malformed
+    // persisted value must never be read as truthy and silently switch one on
+    // -- repaired back to `false` exactly like `suggestionsEnabled` above. An
+    // install from before these settings existed carries no key at all and
+    // takes `DEFAULT_SETTINGS`' `false`.
+    if (typeof initial.imuFusionEnabled !== 'boolean') {
+      initial = { ...initial, imuFusionEnabled: false };
+    }
+    // Ticket P7R E3: the same defensive repair for the gyroscope-CAPTURE
+    // flag, but back to its OWN default (`true`) rather than to `false`. The
+    // rule being applied is "a malformed value is never trusted, the declared
+    // default wins" -- not "a malformed value means off". Capture is additive
+    // and cannot alter latG/longG, so its default is the safe answer here the
+    // same way `false` is the safe answer for the flags that replace a
+    // field-proven path. An install from before this setting existed carries
+    // no key at all and likewise takes `DEFAULT_SETTINGS`' `true`.
+    if (typeof initial.imuGyroCaptureEnabled !== 'boolean') {
+      initial = { ...initial, imuGyroCaptureEnabled: DEFAULT_SETTINGS.imuGyroCaptureEnabled };
+197:| **Signal Finder export**, **DID Sweep export** | The respective diagnostic screens | The guided-observation or sweep record, including raw responses from your car |
+204:Exported reports and analyses **do not contain your VIN**. A raw session export contains your full
+205:position trace, which is precise location data — treat it as you would a photograph of where you
+210:## 6. How long the data is kept
+223:Uninstalling TRACE removes its private database along with it, in the normal way your operating
+224:system removes an app's data.
+226:**If you have exported files**, those files are yours and have their own life: this policy cannot
+233:Under GDPR Articles 15–22 you have the rights listed below. Because your data is on your own device
+237:### Right of access (Art. 15) — "give me a copy of my data"
+240:or **raw session export** for everything on disk without interpretation. The analysis screen's
+241:**Share report / Share the JSON file** gives you the corner-by-corner analysis. These exports are
+253:**In the app:** **Settings → DATA → "Delete all my data"**, then confirm. This permanently removes
+258:It also removes the stored VIN, a vehicle profile the app selected from the VIN, confirmed
+264:What it keeps are your **preferences**: units, language, adapter address and port, coaching, voice
+272:   cannot race a live recording and leave data behind.
+282:### Right to data portability (Art. 20) — "give it to me in a usable format"
+284:**In the app:** the same exports as under access. They are JSON — open, documented, machine-readable
+302:copy of your data**, so we cannot send you an export, and we cannot delete anything from your phone.
+314:> Email: anspdcp@dataprotection.ro
+315:> Web: https://www.dataprotection.ro/
+323:- The database lives in the app's **private sandbox directory**, which the operating system protects
+328:- **We do not encrypt the database separately** with a key of the app's own. If someone has your
+335:- Files you export sit in the app's cache and then wherever you send them. The share sheet's
+338:**There is no backup.** If you lose or reset your phone, your sessions are gone unless you exported
+345:None. No data is transferred outside your device, and therefore none is transferred outside the
+264:  it('Codex P18-REV1 H3: delete-all is refused while a Test Loop is learning, and runs once the driver leaves it', async () => {
+274:    expect(refused.errorText).toMatch(/test loop/i);
+281:  it('Codex P18-REV2 M3: delete-all clears a failed learn phase instead of keeping where the driver drove', async () => {
+294:  it('Codex P18-REV2 M3: delete-all clears a learned loop once its session has ended', async () => {
+
+exec
+"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command '$s=Get-Content apps/mobile/src/session/composition.ts; $s[1640..1678]; $s[2910..2922]; rg -n '"'async persist|JSON.stringify|this.persist' apps/mobile/src/persistence/sqlSettingsStore.ts; rg -n 'gateSqlTransactions|gate.run' apps/mobile/src/persistence/*.ts; git diff --name-only; "'$s=Get-Content docs/legal/privacy-policy.en.md; $s[250..280]; $s=Get-Content apps/mobile/src/session/composition.ts; $s[2114..2168]' in D:\CODE\APLICTIE_Circuit
+ succeeded in 101ms:
+ * (`openAppDatabase`'s own doc comment) -- this never interleaves with a
+ * concurrent `SqlSessionRepository` transaction or a `TelemetryRecorder`
+ * batch either.
+ */
+async function setActiveSession(
+  database: SqlDatabase,
+  session: { sessionId: string; circuitId: string; startedAtUtc?: string } | null,
+): Promise<void> {
+  // Ticket P10B H5-B: statements go through the transaction-scoped `tx`
+  // handle, never through the (gated) outer `database` handle.
+  await database.withTransactionAsync(async (tx) => {
+    if (session === null) {
+      await tx.runAsync('DELETE FROM settings WHERE key = ?', [ACTIVE_SESSION_SETTINGS_KEY]);
+      await tx.runAsync('DELETE FROM settings WHERE key = ?', [ACTIVE_SESSION_CIRCUIT_SETTINGS_KEY]);
+      await tx.runAsync('DELETE FROM settings WHERE key = ?', [ACTIVE_SESSION_STARTED_SETTINGS_KEY]);
+    } else {
+      await tx.runAsync('INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)', [
+        ACTIVE_SESSION_SETTINGS_KEY,
+        session.sessionId,
+      ]);
+      await tx.runAsync('INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)', [
+        ACTIVE_SESSION_CIRCUIT_SETTINGS_KEY,
+        session.circuitId,
+      ]);
+      if (session.startedAtUtc !== undefined) {
+        await tx.runAsync('INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)', [
+          ACTIVE_SESSION_STARTED_SETTINGS_KEY,
+          session.startedAtUtc,
+        ]);
+      }
+    }
+  });
+}
+
+function midSessionState(snapshot: SessionMachineSnapshot): boolean {
+  const midStates = new Set(['outLap', 'timing', 'inPit']);
+  if (midStates.has(snapshot.state)) return true;
+  const priorState = snapshot.context.priorState;
+  return snapshot.state === 'paused' && typeof priorState === 'string' && midStates.has(priorState);
+    // rebuild takes the ORDINARY provider-cleanup path again.
+    testLoopProvider = null;
+    testLoopHandoverActive = false;
+    adoptionProgress = null;
+    // The adoption is complete: nothing is left for a later launch to repair.
+    await clearOwnAdoptionJournal();
+  });
+}
+
+/**
+ * P5d-FIX1 item 10: a plain INSERT, with a fresh id on the (vanishingly
+ * unlikely) collision, instead of an INSERT OR REPLACE that would silently
+ * overwrite an existing learned circuit -- along with the sessions recorded
+193:    void this.persist();
+196:  private async persist(): Promise<void> {
+200:        JSON.stringify(this.settings),
+292:      JSON.stringify(next),
+rg: apps/mobile/src/persistence/*.ts: IO error for operation on apps/mobile/src/persistence/*.ts: The filename, directory name, or volume label syntax is incorrect. (os error 123)
+hardware/kicad/trace-dongle/trace-dongle.kicad_prl
+hardware/kicad/trace-dongle/trace-dongle.kicad_pro
+### Right to erasure (Art. 17) â€” "delete it"
+
+**In the app:** **Settings â†’ DATA â†’ "Delete all my data"**, then confirm. This permanently removes
+every session, lap, checkpoint, GNSS trace, vehicle and motion sample, lap verdict, calibration
+attempt and your personal-best reference lap â€” for every circuit, not just the one selected â€” and
+verifies the deletion actually landed before reporting success. It cannot be undone.
+
+It also removes the stored VIN, a vehicle profile the app selected from the VIN, confirmed
+per-vehicle signal bindings, ruled-out signals, diagnostic sweep records, custom signal channel
+definitions (whether tagged from a sweep with "Tag as channel" or typed in Settings), the record of
+which vehicle profile each session used, the note of an interrupted Test Loop, a track the Test Loop
+learned but could not save, and the geometry of circuits you taught the app.
+
+What it keeps are your **preferences**: units, language, adapter address and port, coaching, voice
+and suggestion toggles, the selected circuit, and a vehicle profile you chose yourself. None of these
+identify you or your car. Uninstalling the app removes them too.
+
+Two honest caveats:
+
+1. The control is **refused while a session is running, or while a Test Loop is learning a track or
+   saving one**; end the session or stop the Test Loop first. This exists so the deletion
+   cannot race a live recording and leave data behind.
+2. If any part of the deletion fails, the app says so instead of reporting success. In that case a
+   learned circuit that a surviving session still uses is kept, so that session can still be
+   analysed; run the deletion again.
+
+### Right to restriction (Art. 18) â€” "stop using it for now"
+
+**In the app:** turn off telemetry, coaching and suggestions in Settings; do not start a session. The
+app records nothing when no session is active.
+
+  settingsStore.update({ selectedCircuitId: entry.profile.circuitId });
+}
+
+/** Rebuilds/refreshes `SqlSessionHistoryStore` for `entry` and swaps it into `historyWrapper` -- the same no-op-if-bootstrap-incomplete guard `runBootstrap()`'s own initial build and `selectCircuit()` share. Split out from `applySelectedCircuit()` (rather than always paired) so `runBootstrap()`'s recovery block can call both in sequence exactly once, without a second, redundant history rebuild layered on top via a full `selectCircuit()` call. */
+async function rebuildHistoryForSelection(entry: BundledCircuit): Promise<void> {
+  if (repository === null) return;
+  const history = new SqlSessionHistoryStore(
+    repository,
+    LOCAL_USER_ID,
+    entry.profile.circuitId,
+    entry.profile.layoutId,
+    entry.profile.layoutVersion,
+  );
+  await history.refresh();
+  historyWrapper.setInner(history);
+  historyStore = history;
+}
+
+/**
+ * The selection change itself -- settings + history store, in that order.
+ * MUST be called with `lifecycleLock` held (M1 fix's ordering guarantee now
+ * comes from the lock, which every other lifecycle operation shares, rather
+ * than from a selection-only chain). Callers are the locked `selectCircuit()`
+ * below, `resumeRecovery()`'s recovery-circuit switch, and
+ * `runDevReplayScenario()`.
+ */
+async function unlockedApplySelection(entry: BundledCircuit): Promise<void> {
+  applySelectedCircuit(entry);
+  await rebuildHistoryForSelection(entry);
+  // N3 fix (lifecycle lock amendment, binding, ticket CN-FIX3): the
+  // production controller is rebuilt for the new circuit HERE, in the same
+  // critical section as the settings/history writes, so the invariant "an
+  // IDLE production controller is always built for the resolved selection"
+  // holds continuously. Previously the rebuild happened lazily, in the
+  // preflight gate only -- and `RealSessionFacade.startPreflight()` is a
+  // no-op controller-side (the state machine first moves at
+  // `beginCalibration()`), so a selection committed after the gate had
+  // already run left the NEXT session starting on the previous circuit's
+  // geometry while settings/history said otherwise. Restricted to `idle`
+  // exactly like the gate's own circuit-change trigger: a terminal
+  // (`sessionComplete`/`error`) controller is still the gate's to replace,
+  // and a mid-session one is unreachable here (the selection would have been
+  // refused before this point).
+  if (
+    controller !== null &&
+    activeController === controller &&
+    currentControllerState(controller) === 'idle' &&
+    productionControllerCircuitId !== resolveSelectedCircuit(settingsStore.getSettings()).profile.circuitId
+  ) {
+    await unlockedRebuildProductionController();
+  }
+}
+
+/** Shared refusal check for every locked selection change -- `null` when the change may proceed. */
+function refuseSelectionIfSessionActive(): SelectCircuitResult | null {
+
+codex
+FAIL
+
+Reviewed `d2b26f6..1dee7b2` and the cumulative rework `6d6f90b..1dee7b2`; HEAD matches `1dee7b2`. No files changed; no agents used.
+
+**REV2 dispositions**
+
+Paths below are relative to `apps/mobile/` unless otherwise stated.
+
+| REV2 finding | Status | Evidence |
+|---|---|---|
+| HIGH 1 — VIN read after second reset survives verification | **CLOSED** | `src/session/composition.ts:3746` holds the fence through the operation; `:1021` blocks new detection and `:1056` rejects obsolete completions. No surviving success-path VIN resurrection found in these paths. |
+| MEDIUM 2 — `error` refusal gives an impossible UI remedy | **CLOSED** | `composition.ts:3798` refuses only learning/adopting; `:3814` resets and abandons terminal states, including `error`. A genuinely running session still correctly refuses first. |
+| MEDIUM 3 — terminal Test Loop retains geometry/coordinates | **CLOSED** | `composition.ts:3815` calls `reset()`; `src/session/testLoopController.ts:241` clears samples, both coordinate anchors, learned metadata and `pendingCircuit`. |
+| MEDIUM 4 — Settings channel draft survives | **PARTIAL** | `src/ui/screens/SettingsScreen.tsx:468` resets draft, committed ref and validation state after **resolved** success/failure/refusal. Rejected operations bypass this cleanup at `:478`; failing sequence below. |
+
+The previously closed REV1 items remain closed: persisted channels (`src/persistence/deviceDataWipe.ts:45`), stale adoption retry (`composition.ts:3815`), learned-refresh failure reporting (`:3689`), and journal/log failure reporting (`:3939`, `:3952`). Detection rearming now has a new refusal-path regression.
+
+**New and residual findings**
+
+1. **MEDIUM — A refused deletion can permanently strand VIN detection at `attempting`.**
+
+   Locations: [composition.ts:3746](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3746), [composition.ts:1056](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:1056), [composition.ts:3764](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3764).
+
+   Sequence:
+
+   - VIN detection starts and reaches its awaited transport close.
+   - Delete-all acquires the lifecycle lock and sets `deviceWipeInProgress = true`.
+   - While deletion awaits `ready()`, detection finishes. Its generation still matches, but the fence makes it return before changing `vinDetectionState`.
+   - Deletion refuses because a session is active. Neither identity reset runs.
+   - `finally` releases the fence, but detection remains `attempting`. Every subsequent trigger returns the cached null without attempting another read.
+
+   **Observed evidence:** A read-only, in-memory reproduction used the source-extracted detection, delete wrapper, delete body and `ready()` functions, plus the actual lifecycle lock. Transport and surrounding dependencies were stubbed:
+
+   ```text
+   read: null
+   deletion.reason: SESSION_ACTIVE
+   state: attempting
+   fence: false
+   generation: 0
+   nextTrigger: null
+   state after next trigger: attempting
+   ```
+
+   This reproduces with an already-resolved bootstrap promise; no prolonged bootstrap is required. Restart or a later deletion reaching the identity reset recovers detection. The fence itself releases correctly; the detection state does not.
+
+2. **LOW — Settings draft cleanup misses rejected deletion failures.**
+
+   Locations: [SettingsScreen.tsx:468](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/SettingsScreen.tsx:468), [SettingsScreen.tsx:478](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/SettingsScreen.tsx:478), [composition.ts:3945](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3945).
+
+   Sequence: stored channel JSON is empty → enter invalid VIN-bearing JSON → delete-all clears the database → active-pointer clearing throws, or the final history refresh rejects → `confirmDelete()` enters `catch`.
+
+   The draft/ref/error cleanup is skipped. Because the stored channel field stayed empty, its field-change effect does not clear the draft either. It remains editable and can subsequently be corrected and persisted.
+
+   This is **not false success**: the UI reports failure. It does mean “draft reset after both success and failure” is incomplete. Cleanup should also cover rejection, using the current store value.
+
+**REV2 qualifications**
+
+| Qualification | Status | Assessment |
+|---|---|---|
+| FIFO protects already-submitted writes, not future writes | **CLOSED for the reported VIN race** | `src/persistence/sqlSettingsStore.ts:196` submits the serialized blob; `composition.ts:3701` verifies it. The new detection fence closes the previously identified future-writer window. FIFO alone still provides no such guarantee. |
+| Active-profile subscription/bindings generation | **CLOSED** | `composition.ts:729`, `:3659`, `:3680`: earlier reads are invalidated; post-wipe refresh uses the new generation. |
+| Generation mismatch does not strand detection | **OPEN — regression** | The added fence-only return at `composition.ts:1056` can strand detection without a generation change. See finding 1. |
+| Abandoned ledger and bootstrap repair | **CLOSED within one runtime** | `composition.ts:3815` removes retryable geometry before clearing its ledger; `:3151` repairs during bootstrap, before `ready()` permits deletion. A retained journal describes surviving disk state after failure. |
+| Cross-runtime journal ownership | **OPEN, unchanged qualification** | `composition.ts:3729` still deletes by key unconditionally. Another runtime claiming/writing that key during deletion is outside this lifecycle lock’s protection. |
+| Failure reporting | **CLOSED for the named cases** | Learned refresh, identity verification, journal deletion and log deletion prevent success (`composition.ts:3689`, `:3704`, `:3939`, `:3952`). Other awaited failures can reject rather than return the aggregate; Settings reports an error. |
+| History skipped after log failure | **CLOSED** | `composition.ts:3958` now refreshes when `sessionDataOk`, even if journal/log cleanup fails. |
+| Broader history freshness on failure | **PARTIAL** | `sessionDataOk` still includes device-data success (`composition.ts:3935`). Sessions can be deleted, identity verification can fail, and history refresh is then skipped. An active-pointer rejection also bypasses it. |
+| Tests omit important paths | **PARTIAL** | Successful rearming and failed/learned abandonment now have coverage; refusal-time completion, rejection cleanup, late deferred VIN completion and several injected storage failures remain uncovered. |
+| Policy alignment | **PARTIAL** | Changed EN/RO wording fixes the cited channel scope, terminal adoption and adapter-availability descriptions. Remaining qualifications below. |
+| Unmentioned behavior changes | **CLOSED for custom-channel scope; diagnostic rename remains** | Policies now explicitly include typed definitions. The cumulative diff still changes the diagnostic key to `vehicle_identity_settings` (`deviceDataWipe.ts:134`). |
+
+**Test assessment**
+
+- `test/session/composition.deleteAllVinFence.test.ts:116` asserts that settings-change triggers start no reads during deletion. That is a valid, stronger property of this chosen fence implementation. It is not the only possible implementation of the privacy requirement.
+- The test seeds a real VIN and checks the actual settings row after deletion (`:110`, `:127`), so its memory/disk assertions are meaningful.
+- It does **not** directly schedule a read after the second reset and complete it after final verification. Reads resolve immediately, and triggers occur only on settings notifications. It also does not inspect private `cachedDetectedVin` or exercise refusal-time completion.
+- The Test Loop tests at `test/session/composition.testLoop.test.ts:281` and `:294` reject the old terminal-state behavior. They pin invocation of abandonment through observable snapshots. They do not directly inspect private anchors/geometry, test `error` abandonment, verify restart after deletion, or exercise queued auto-teardown ordering.
+- The SQL double still gates transactions only; standalone operations bypass that queue (`test/support/sqlJsDatabase.ts:35`). Production FIFO ordering is not fully represented.
+- Attempted both changed composition test files with configuration/cache writing disabled. Vitest failed before collection because temporary-directory creation returned `EPERM`. **No suite pass claimed.**
+
+**Clean list**
+
+- No demonstrated `ok: true` VIN resurrection through the reviewed detection paths.
+- Live sessions are refused before Test Loop teardown. Just-ended session commands and deletion share the lifecycle lock; teardown awaits the existing telemetry shutdown.
+- Failed auto-teardown cannot overlap the new teardown under that lock. Incrementing `teardownGeneration` invalidates an older queued teardown.
+- Reset publishes `idle`; `TestLoopScreen` subscribes to snapshots and exposes Start for idle. No newly introduced unrecoverable screen state found.
+- Retaining the journal after an unsuccessful wipe is appropriate for bootstrap repair. Repair uses surviving disk geometry or removes orphaned state; it does not reconstruct erased geometry from the cleared in-memory ledger.
+- Settings’ explicit committed-ref update handles the original unchanged-empty-store case correctly on resolved operations. No stale-ref resurrection demonstrated there.
+- EN/RO agree on the changed scope. One pre-existing overclaim remains: §7 says the selected circuit is kept, but deleting the selected learned circuit selects the fallback (`composition.ts:3693`). The adopting-phase instruction to “stop” is also imprecise: that phase offers no Stop control and must settle.
+- Additional behavior: even an outright refusal now discards an uncommitted Settings draft, and terminal Test Loop abandonment occurs before deletion success is known. Neither is a false-success bug, but both are observable consequences of this patch.
+tokens used
+82,388
+FAIL
+
+Reviewed `d2b26f6..1dee7b2` and the cumulative rework `6d6f90b..1dee7b2`; HEAD matches `1dee7b2`. No files changed; no agents used.
+
+**REV2 dispositions**
+
+Paths below are relative to `apps/mobile/` unless otherwise stated.
+
+| REV2 finding | Status | Evidence |
+|---|---|---|
+| HIGH 1 — VIN read after second reset survives verification | **CLOSED** | `src/session/composition.ts:3746` holds the fence through the operation; `:1021` blocks new detection and `:1056` rejects obsolete completions. No surviving success-path VIN resurrection found in these paths. |
+| MEDIUM 2 — `error` refusal gives an impossible UI remedy | **CLOSED** | `composition.ts:3798` refuses only learning/adopting; `:3814` resets and abandons terminal states, including `error`. A genuinely running session still correctly refuses first. |
+| MEDIUM 3 — terminal Test Loop retains geometry/coordinates | **CLOSED** | `composition.ts:3815` calls `reset()`; `src/session/testLoopController.ts:241` clears samples, both coordinate anchors, learned metadata and `pendingCircuit`. |
+| MEDIUM 4 — Settings channel draft survives | **PARTIAL** | `src/ui/screens/SettingsScreen.tsx:468` resets draft, committed ref and validation state after **resolved** success/failure/refusal. Rejected operations bypass this cleanup at `:478`; failing sequence below. |
+
+The previously closed REV1 items remain closed: persisted channels (`src/persistence/deviceDataWipe.ts:45`), stale adoption retry (`composition.ts:3815`), learned-refresh failure reporting (`:3689`), and journal/log failure reporting (`:3939`, `:3952`). Detection rearming now has a new refusal-path regression.
+
+**New and residual findings**
+
+1. **MEDIUM — A refused deletion can permanently strand VIN detection at `attempting`.**
+
+   Locations: [composition.ts:3746](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3746), [composition.ts:1056](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:1056), [composition.ts:3764](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3764).
+
+   Sequence:
+
+   - VIN detection starts and reaches its awaited transport close.
+   - Delete-all acquires the lifecycle lock and sets `deviceWipeInProgress = true`.
+   - While deletion awaits `ready()`, detection finishes. Its generation still matches, but the fence makes it return before changing `vinDetectionState`.
+   - Deletion refuses because a session is active. Neither identity reset runs.
+   - `finally` releases the fence, but detection remains `attempting`. Every subsequent trigger returns the cached null without attempting another read.
+
+   **Observed evidence:** A read-only, in-memory reproduction used the source-extracted detection, delete wrapper, delete body and `ready()` functions, plus the actual lifecycle lock. Transport and surrounding dependencies were stubbed:
+
+   ```text
+   read: null
+   deletion.reason: SESSION_ACTIVE
+   state: attempting
+   fence: false
+   generation: 0
+   nextTrigger: null
+   state after next trigger: attempting
+   ```
+
+   This reproduces with an already-resolved bootstrap promise; no prolonged bootstrap is required. Restart or a later deletion reaching the identity reset recovers detection. The fence itself releases correctly; the detection state does not.
+
+2. **LOW — Settings draft cleanup misses rejected deletion failures.**
+
+   Locations: [SettingsScreen.tsx:468](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/SettingsScreen.tsx:468), [SettingsScreen.tsx:478](D:/CODE/APLICTIE_Circuit/apps/mobile/src/ui/screens/SettingsScreen.tsx:478), [composition.ts:3945](D:/CODE/APLICTIE_Circuit/apps/mobile/src/session/composition.ts:3945).
+
+   Sequence: stored channel JSON is empty → enter invalid VIN-bearing JSON → delete-all clears the database → active-pointer clearing throws, or the final history refresh rejects → `confirmDelete()` enters `catch`.
+
+   The draft/ref/error cleanup is skipped. Because the stored channel field stayed empty, its field-change effect does not clear the draft either. It remains editable and can subsequently be corrected and persisted.
+
+   This is **not false success**: the UI reports failure. It does mean “draft reset after both success and failure” is incomplete. Cleanup should also cover rejection, using the current store value.
+
+**REV2 qualifications**
+
+| Qualification | Status | Assessment |
+|---|---|---|
+| FIFO protects already-submitted writes, not future writes | **CLOSED for the reported VIN race** | `src/persistence/sqlSettingsStore.ts:196` submits the serialized blob; `composition.ts:3701` verifies it. The new detection fence closes the previously identified future-writer window. FIFO alone still provides no such guarantee. |
+| Active-profile subscription/bindings generation | **CLOSED** | `composition.ts:729`, `:3659`, `:3680`: earlier reads are invalidated; post-wipe refresh uses the new generation. |
+| Generation mismatch does not strand detection | **OPEN — regression** | The added fence-only return at `composition.ts:1056` can strand detection without a generation change. See finding 1. |
+| Abandoned ledger and bootstrap repair | **CLOSED within one runtime** | `composition.ts:3815` removes retryable geometry before clearing its ledger; `:3151` repairs during bootstrap, before `ready()` permits deletion. A retained journal describes surviving disk state after failure. |
+| Cross-runtime journal ownership | **OPEN, unchanged qualification** | `composition.ts:3729` still deletes by key unconditionally. Another runtime claiming/writing that key during deletion is outside this lifecycle lock’s protection. |
+| Failure reporting | **CLOSED for the named cases** | Learned refresh, identity verification, journal deletion and log deletion prevent success (`composition.ts:3689`, `:3704`, `:3939`, `:3952`). Other awaited failures can reject rather than return the aggregate; Settings reports an error. |
+| History skipped after log failure | **CLOSED** | `composition.ts:3958` now refreshes when `sessionDataOk`, even if journal/log cleanup fails. |
+| Broader history freshness on failure | **PARTIAL** | `sessionDataOk` still includes device-data success (`composition.ts:3935`). Sessions can be deleted, identity verification can fail, and history refresh is then skipped. An active-pointer rejection also bypasses it. |
+| Tests omit important paths | **PARTIAL** | Successful rearming and failed/learned abandonment now have coverage; refusal-time completion, rejection cleanup, late deferred VIN completion and several injected storage failures remain uncovered. |
+| Policy alignment | **PARTIAL** | Changed EN/RO wording fixes the cited channel scope, terminal adoption and adapter-availability descriptions. Remaining qualifications below. |
+| Unmentioned behavior changes | **CLOSED for custom-channel scope; diagnostic rename remains** | Policies now explicitly include typed definitions. The cumulative diff still changes the diagnostic key to `vehicle_identity_settings` (`deviceDataWipe.ts:134`). |
+
+**Test assessment**
+
+- `test/session/composition.deleteAllVinFence.test.ts:116` asserts that settings-change triggers start no reads during deletion. That is a valid, stronger property of this chosen fence implementation. It is not the only possible implementation of the privacy requirement.
+- The test seeds a real VIN and checks the actual settings row after deletion (`:110`, `:127`), so its memory/disk assertions are meaningful.
+- It does **not** directly schedule a read after the second reset and complete it after final verification. Reads resolve immediately, and triggers occur only on settings notifications. It also does not inspect private `cachedDetectedVin` or exercise refusal-time completion.
+- The Test Loop tests at `test/session/composition.testLoop.test.ts:281` and `:294` reject the old terminal-state behavior. They pin invocation of abandonment through observable snapshots. They do not directly inspect private anchors/geometry, test `error` abandonment, verify restart after deletion, or exercise queued auto-teardown ordering.
+- The SQL double still gates transactions only; standalone operations bypass that queue (`test/support/sqlJsDatabase.ts:35`). Production FIFO ordering is not fully represented.
+- Attempted both changed composition test files with configuration/cache writing disabled. Vitest failed before collection because temporary-directory creation returned `EPERM`. **No suite pass claimed.**
+
+**Clean list**
+
+- No demonstrated `ok: true` VIN resurrection through the reviewed detection paths.
+- Live sessions are refused before Test Loop teardown. Just-ended session commands and deletion share the lifecycle lock; teardown awaits the existing telemetry shutdown.
+- Failed auto-teardown cannot overlap the new teardown under that lock. Incrementing `teardownGeneration` invalidates an older queued teardown.
+- Reset publishes `idle`; `TestLoopScreen` subscribes to snapshots and exposes Start for idle. No newly introduced unrecoverable screen state found.
+- Retaining the journal after an unsuccessful wipe is appropriate for bootstrap repair. Repair uses surviving disk geometry or removes orphaned state; it does not reconstruct erased geometry from the cleared in-memory ledger.
+- Settings’ explicit committed-ref update handles the original unchanged-empty-store case correctly on resolved operations. No stale-ref resurrection demonstrated there.
+- EN/RO agree on the changed scope. One pre-existing overclaim remains: §7 says the selected circuit is kept, but deleting the selected learned circuit selects the fallback (`composition.ts:3693`). The adopting-phase instruction to “stop” is also imprecise: that phase offers no Stop control and must settle.
+- Additional behavior: even an outright refusal now discards an uncommitted Settings draft, and terminal Test Loop abandonment occurs before deletion success is known. Neither is a false-success bug, but both are observable consequences of this patch.
diff --git a/.foreman/scratch/ticket-p18-rev3-codex.md b/.foreman/scratch/ticket-p18-rev3-codex.md
new file mode 100644
index 0000000..645689f
--- /dev/null
+++ b/.foreman/scratch/ticket-p18-rev3-codex.md
@@ -0,0 +1,12 @@
+# Review ticket P18-REV3 — Codex read-only cross-review of the P18-FIX2 fix wave
+Adversarial READ-ONLY reviewer. The fix is commit `1dee7b2` on branch `p18-fix1-delete-all`: `git diff d2b26f6..1dee7b2` (the whole delete-all rework since the cloud handoff: `git diff 6d6f90b..1dee7b2`).
+It answers your P18-REV2 review (`.foreman/scratch/p18-codex-rev2-out.txt`, final section after the last `codex` line): HIGH 1 (VIN read started after the second reset outlives the final check), MEDIUM 2 (`error` phase refusal with an instruction the UI cannot follow), MEDIUM 3 (terminal Test Loop state keeps geometry/coordinates), MEDIUM 4 (Settings channel-specs draft survives), plus the history-refresh and policy qualifications.
+Binding: HANDOFF.md item 1 (a VIN is personal data; delete-all must remove it), docs/legal/privacy-policy.*.md §3.3 and §7 as changed.
+For EACH REV2 finding and qualification: CLOSED / PARTIAL / OPEN with file:line, and a concrete failing sequence if not closed. Then attack the new code:
+1. `deviceWipeInProgress` fence (`unlockedDeleteAllStoredUserData` try/finally, `maybeDetectVehicleFromVin`): any path where delete-all returns `ok: true` with the VIN in memory or on disk; any path that leaves detection stuck or permanently disabled; refusal paths.
+2. Test Loop abandonment in delete-all (step 1d): `testLoopController.reset()` + `unlockedTearDownLearnPhase()` under `lifecycleLock` for phases `learned`/`failed`/`error`, before `unlockedRebuildProductionController()`. Can it stop providers a live or just-ended session still needs, race the auto-teardown subscription on `failed`, or leave `TestLoopScreen` in a state it cannot recover from? Is clearing the in-memory ledger while keeping the journal row until success correct against bootstrap repair?
+3. SettingsScreen draft reset: correct after both success and failure? any stale ref interplay?
+4. Tests: does `composition.deleteAllVinFence.test.ts` pin the HIGH (it fails on d2b26f6 with 3 reads started — is asserting "no read starts" the right property, and is the VIN/disk assertion meaningful)? Do the Test Loop tests pin M3?
+5. Policy EN/RO vs code: over- or under-claims.
+6. Anything else in the diff that changes behaviour the commit messages do not mention.
+OUTPUT: first line PASS / FAIL / PASS_WITH_NOTES; per-REV2-finding status; new findings by severity with file:line + scenario + evidence; Clean list. Stdout only. No agents.
diff --git a/apps/mobile/src/session/composition.ts b/apps/mobile/src/session/composition.ts
index 785c5a8..3a6192a 100644
--- a/apps/mobile/src/session/composition.ts
+++ b/apps/mobile/src/session/composition.ts
@@ -1051,9 +1051,16 @@ export async function maybeDetectVehicleFromVin(): Promise<string | null> {
     sharedEnetAdapterReservation.release(token);
   }
 
-  // Delete-all ran (or is running) while this read was in flight: it has
-  // already put detection back to 'idle', so the next connection reads again.
-  if (generation !== deviceDataGeneration || deviceWipeInProgress) return null;
+  // Delete-all ran while this read was in flight: it has already put
+  // detection back to 'idle', so the next connection reads again.
+  if (generation !== deviceDataGeneration) return null;
+  // Delete-all is running and has not reset anything yet -- it may still be
+  // refused. Drop the result, and re-arm here rather than rely on a reset
+  // that a refusal never reaches.
+  if (deviceWipeInProgress) {
+    vinDetectionState = 'idle';
+    return null;
+  }
   vinDetectionState = 'done';
   cachedDetectedVin = vin;
   if (vin !== null) {
@@ -3955,7 +3962,9 @@ async function runDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDataRes
       finalResult.ok = false;
     }
   }
-  if (sessionDataOk && historyStore !== null) await historyStore.refresh();
+  // The history list follows the sessions: refreshed whenever they verified
+  // gone, even if a later half of the wipe failed.
+  if (result.ok && historyStore !== null) await historyStore.refresh();
 
   const errorText = finalResult.ok
     ? null
diff --git a/apps/mobile/src/ui/screens/SettingsScreen.tsx b/apps/mobile/src/ui/screens/SettingsScreen.tsx
index f482505..2dd6194 100644
--- a/apps/mobile/src/ui/screens/SettingsScreen.tsx
+++ b/apps/mobile/src/ui/screens/SettingsScreen.tsx
@@ -465,13 +465,6 @@ export function SettingsScreen({ navigation }: Props): React.JSX.Element {
     setDeleting(true);
     try {
       const result = await deleteAllStoredUserData();
-      // An uncommitted channel draft can hold VIN-bearing provenance and is
-      // not in the settings store, so delete-all cannot reach it from there.
-      const channelSpecs = settingsStore.getSettings().enetChannelSpecsJson;
-      lastCommittedEnetChannelSpecs.current = channelSpecs;
-      setEnetChannelSpecsDraft(channelSpecs);
-      setEnetChannelSpecsError(null);
-      setEnetChannelSpecsWarnings([]);
       setDeleteBanner(result.ok ? 'success' : 'error');
       setDeleteErrorText(result.ok ? null : result.errorText);
       setDeleteRefused(!result.ok && result.reason !== undefined);
@@ -480,6 +473,14 @@ export function SettingsScreen({ navigation }: Props): React.JSX.Element {
       setDeleteErrorText(null);
       setDeleteRefused(false);
     } finally {
+      // An uncommitted channel draft can hold VIN-bearing provenance and is
+      // not in the settings store, so delete-all cannot reach it from there.
+      // Dropped however delete-all ended, even when it threw.
+      const channelSpecs = settingsStore.getSettings().enetChannelSpecsJson;
+      lastCommittedEnetChannelSpecs.current = channelSpecs;
+      setEnetChannelSpecsDraft(channelSpecs);
+      setEnetChannelSpecsError(null);
+      setEnetChannelSpecsWarnings([]);
       setDeleting(false);
       setConfirmingDelete(false);
     }
diff --git a/apps/mobile/test/session/composition.deleteAllVinFence.test.ts b/apps/mobile/test/session/composition.deleteAllVinFence.test.ts
index 3036e52..7376660 100644
--- a/apps/mobile/test/session/composition.deleteAllVinFence.test.ts
+++ b/apps/mobile/test/session/composition.deleteAllVinFence.test.ts
@@ -19,6 +19,8 @@ const seeded = vi.hoisted(() => ({
   db: undefined as unknown,
   repository: undefined as unknown,
   vinReads: 0,
+  /** When set, the stub transport's close() waits on it -- holds a VIN read open across a delete-all. */
+  closeGate: null as Promise<void> | null,
 }));
 
 vi.mock('expo-constants', () => ({
@@ -66,7 +68,9 @@ vi.mock('../../src/persistence/expoSqlDatabase', () => ({
 vi.mock('../../src/session/enetTcpTransport', () => ({
   EnetTcpTransport: class {
     async connect(): Promise<void> {}
-    async close(): Promise<void> {}
+    async close(): Promise<void> {
+      if (seeded.closeGate !== null) await seeded.closeGate;
+    }
     onData(): () => void {
       return () => {};
     }
@@ -102,6 +106,7 @@ describe('composition.ts delete-all -- VIN detection fence (Codex P18-REV2 H1)',
     seeded.db = db;
     seeded.repository = await SqlSessionRepository.create(db);
     seeded.vinReads = 0;
+    seeded.closeGate = null;
     vi.resetModules();
     const composition = await import('../../src/session/composition');
     await flush();
@@ -131,4 +136,36 @@ describe('composition.ts delete-all -- VIN detection fence (Codex P18-REV2 H1)',
     // And detection is re-armed: the next connection after delete-all reads again.
     expect(await composition.maybeDetectVehicleFromVin()).toBe(VIN);
   });
+
+  it('Codex P18-REV3 M1: a VIN read that completes into a REFUSED delete-all leaves detection re-armed', async () => {
+    const db = await createSqlJsDatabase();
+    await migrateTelemetrySchema(db);
+    await migrateDidSweepSchema(db);
+    seeded.db = db;
+    seeded.repository = await SqlSessionRepository.create(db);
+    seeded.vinReads = 0;
+    let openGate: () => void = () => {};
+    seeded.closeGate = new Promise<void>((resolve) => {
+      openGate = resolve;
+    });
+    vi.resetModules();
+    const composition = await import('../../src/session/composition');
+    await flush();
+    composition.settingsStore.update({ adapterType: 'enet', enetHost: '192.168.16.254' });
+
+    // A learning Test Loop makes delete-all refuse.
+    expect((await composition.startTestLoop()).ok).toBe(true);
+    const detection = composition.maybeDetectVehicleFromVin();
+    await flush();
+    const deletion = composition.deleteAllStoredUserData();
+    openGate();
+    const [detected, refused] = await Promise.all([detection, deletion]);
+    seeded.closeGate = null;
+
+    expect(refused.reason).toBe('TEST_LOOP_ACTIVE');
+    expect(detected).toBeNull();
+    // Not stuck: the next trigger reads again.
+    expect(await composition.maybeDetectVehicleFromVin()).toBe(VIN);
+    await composition.resetTestLoop();
+  });
 });
diff --git a/docs/legal/privacy-policy.en.md b/docs/legal/privacy-policy.en.md
index 41240ba..4e3cce9 100644
--- a/docs/legal/privacy-policy.en.md
+++ b/docs/legal/privacy-policy.en.md
@@ -262,13 +262,14 @@ which vehicle profile each session used, the note of an interrupted Test Loop, a
 learned but could not save, and the geometry of circuits you taught the app.
 
 What it keeps are your **preferences**: units, language, adapter address and port, coaching, voice
-and suggestion toggles, the selected circuit, and a vehicle profile you chose yourself. None of these
-identify you or your car. Uninstalling the app removes them too.
+and suggestion toggles, the selected circuit (unless it was a learned circuit that was deleted; then
+the default circuit is selected), and a vehicle profile you chose yourself. None of these identify
+you or your car. Uninstalling the app removes them too.
 
 Two honest caveats:
 
 1. The control is **refused while a session is running, or while a Test Loop is learning a track or
-   saving one**; end the session or stop the Test Loop first. This exists so the deletion
+   saving one**; end the session, or stop the Test Loop (or let it finish saving) first. This exists so the deletion
    cannot race a live recording and leave data behind.
 2. If any part of the deletion fails, the app says so instead of reporting success. In that case a
    learned circuit that a surviving session still uses is kept, so that session can still be
diff --git a/docs/legal/privacy-policy.ro.md b/docs/legal/privacy-policy.ro.md
index 4586013..895b4bf 100644
--- a/docs/legal/privacy-policy.ro.md
+++ b/docs/legal/privacy-policy.ro.md
@@ -276,14 +276,14 @@ unui Test Loop întrerupt, un traseu pe care Test Loop l-a învățat dar nu l-a
 circuitelor pe care le-ai învățat aplicației.
 
 Păstrează doar **preferințele** tale: unitățile de măsură, limba, adresa și portul adaptorului,
-comutatoarele pentru coaching, voce și sugestii, circuitul selectat și un profil de vehicul ales chiar
-de tine. Niciuna dintre ele nu te identifică pe tine sau mașina ta. Dezinstalarea aplicației le
+comutatoarele pentru coaching, voce și sugestii, circuitul selectat (cu excepția unui circuit învățat
+care a fost șters; atunci se selectează circuitul implicit) și un profil de vehicul ales chiar de tine. Niciuna dintre ele nu te identifică pe tine sau mașina ta. Dezinstalarea aplicației le
 elimină și pe acestea.
 
 Două precizări oneste:
 
 1. Comanda este **refuzată cât timp o sesiune este în desfășurare sau cât timp un Test Loop învață
-   ori salvează un traseu**; încheie mai întâi sesiunea sau oprește Test Loop. Această regulă
+   ori salvează un traseu**; încheie mai întâi sesiunea sau oprește Test Loop (ori așteaptă să termine salvarea). Această regulă
    există ca ștergerea să nu se poată suprapune peste o înregistrare vie și să lase date în urmă.
 2. Dacă o parte a ștergerii eșuează, aplicația spune asta în loc să raporteze succes. În acest caz,
    un circuit învățat pe care îl folosește încă o sesiune rămasă este păstrat, ca sesiunea să poată fi
 .foreman/scratch/p18-codex-rev1-out.txt            | 5148 ++++++++++++
 .foreman/scratch/p18-codex-rev2-out.txt            | 8672 ++++++++++++++++++++
 .foreman/scratch/p18-codex-rev3-out.txt            | 3002 +++++++
 .foreman/scratch/ticket-p18-rev2-codex.md          |   13 +
 .foreman/scratch/ticket-p18-rev3-codex.md          |   12 +
 apps/mobile/src/persistence/deviceDataWipe.ts      |   48 +-
 apps/mobile/src/session/composition.ts             |  185 +-
 apps/mobile/src/ui/screens/SettingsScreen.tsx      |    8 +
 .../mobile/test/persistence/deviceDataWipe.test.ts |   29 +-
 .../session/composition.circuitSelection.test.ts   |   11 +
 .../session/composition.deleteAllVinFence.test.ts  |  171 +
 .../test/session/composition.testLoop.test.ts      |   47 +
 docs/legal/privacy-policy.en.md                    |   19 +-
 docs/legal/privacy-policy.ro.md                    |   20 +-
 14 files changed, 17336 insertions(+), 49 deletions(-)

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg --files -g AGENTS.md -g '*privacy-policy*' -g '*deleteAllVinFence*' -g package.json" in D:\CODE\APLICTIE_Circuit
 succeeded in 107ms:
package.json
packages\core\package.json
docs\legal\privacy-policy.ro.md
docs\legal\privacy-policy.en.md
apps\mobile\test\session\composition.deleteAllVinFence.test.ts
apps\mobile\package.json
apps\mobile\AGENTS.md

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "Get-Content apps/mobile/AGENTS.md; Get-Content .foreman/scratch/p18-codex-rev3-out.txt -Tail 135; git status --short; git rev-parse HEAD; rg --files --hidden -g '*HANDOFF*'; git diff 6d6f90b..939b93f -- apps/mobile/src docs/legal" in D:\CODE\APLICTIE_Circuit
 succeeded in 75ms:
 M hardware/kicad/trace-dongle/trace-dongle.kicad_prl
 M hardware/kicad/trace-dongle/trace-dongle.kicad_pro
?? .foreman/scratch/p-v123-expo-export.txt
?? .foreman/scratch/p-v123-lint.txt
?? .foreman/scratch/p-v123-lint2.txt
?? .foreman/scratch/p-v123-test.txt
?? .foreman/scratch/p-v123-test2.txt
?? .foreman/scratch/p-v123-typecheck.txt
?? .foreman/scratch/p-v123-typecheck2.txt
?? .foreman/scratch/p10-codex-rev2-out.txt
?? .foreman/scratch/p10-codex-rev2.md
?? .foreman/scratch/p10-fix2-lint.txt
?? .foreman/scratch/p10-fix2-test.txt
?? .foreman/scratch/p10-fix2-typecheck.txt
?? .foreman/scratch/p10a-dispositions.md
?? .foreman/scratch/p10a-expo-export.txt
?? .foreman/scratch/p10a-lint.txt
?? .foreman/scratch/p10a-test.txt
?? .foreman/scratch/p10a-ticket.md
?? .foreman/scratch/p10a-typecheck.txt
?? .foreman/scratch/p10c-lint.txt
?? .foreman/scratch/p10c-test.txt
?? .foreman/scratch/p10c-timing-evidence.txt
?? .foreman/scratch/p10c-typecheck.txt
?? .foreman/scratch/p11-codex-rev3-out.txt
?? .foreman/scratch/p11-codex-rev3.md
?? .foreman/scratch/p11-codex-rev3b-out.txt
?? .foreman/scratch/p11c-ab-without-mine.txt
?? .foreman/scratch/p11c-after-green.txt
?? .foreman/scratch/p11c-baseline-test.txt
?? .foreman/scratch/p11c-before-red.txt
?? .foreman/scratch/p11c-core-test-1.txt
?? .foreman/scratch/p11c-dense-sweep.txt
?? .foreman/scratch/p11c-evidence.txt
?? .foreman/scratch/p11c-expo-export.txt
?? .foreman/scratch/p11c-h4b-regression.txt
?? .foreman/scratch/p11c-lint.txt
?? .foreman/scratch/p11c-test.txt
?? .foreman/scratch/p11c-typecheck.txt
?? .foreman/scratch/p12-codex-rev4-out.txt
?? .foreman/scratch/p12-codex-rev4.md
?? .foreman/scratch/p13-base-lint.txt
?? .foreman/scratch/p13-base-test.txt
?? .foreman/scratch/p13-base-typecheck.txt
?? .foreman/scratch/p13-codex-out.txt
?? .foreman/scratch/p13-codex.md
?? .foreman/scratch/p13-lint.txt
?? .foreman/scratch/p13-test.txt
?? .foreman/scratch/p13-typecheck.txt
?? .foreman/scratch/p13b-expo-export.txt
?? .foreman/scratch/p13b-exports/
?? .foreman/scratch/p13b-lint.txt
?? .foreman/scratch/p13b-lint1.txt
?? .foreman/scratch/p13b-newtests1.txt
?? .foreman/scratch/p13b-test.txt
?? .foreman/scratch/p13b-test1.txt
?? .foreman/scratch/p13b-ticket-draft.md
?? .foreman/scratch/p13b-typecheck.txt
?? .foreman/scratch/p13b-typecheck1.txt
?? .foreman/scratch/p13d-after-green.txt
?? .foreman/scratch/p13d-before-red.txt
?? .foreman/scratch/p14-base-test.txt
?? .foreman/scratch/p14-codex-out.txt
?? .foreman/scratch/p14-codex.md
?? .foreman/scratch/p14-evidence-before/
?? .foreman/scratch/p14-evidence/
?? .foreman/scratch/p14-expo-export.txt
?? .foreman/scratch/p14-lint.txt
?? .foreman/scratch/p14-red-core.txt
?? .foreman/scratch/p14-test.txt
?? .foreman/scratch/p14-typecheck.txt
?? .foreman/scratch/p15-baseline-test.txt
?? .foreman/scratch/p15-evidence-gen/
?? .foreman/scratch/p15-expo-export.txt
?? .foreman/scratch/p15-lint.txt
?? .foreman/scratch/p15-test.txt
?? .foreman/scratch/p15-typecheck.txt
?? .foreman/scratch/p16-base-lint.txt
?? .foreman/scratch/p16-base-test.txt
?? .foreman/scratch/p16-base-typecheck.txt
?? .foreman/scratch/p16-c1-after-green.txt
?? .foreman/scratch/p16-c1-before-red.txt
?? .foreman/scratch/p16-c2-test.txt
?? .foreman/scratch/p16-lint.txt
?? .foreman/scratch/p16-mobile-boundary.md
?? .foreman/scratch/p16-test.txt
?? .foreman/scratch/p16-typecheck.txt
?? .foreman/scratch/p17-codex-flow-out.txt
?? .foreman/scratch/p17-codex-flow.md
?? .foreman/scratch/p18-codex-rev4-out.txt
?? .foreman/scratch/p4c-a-core-test.txt
?? .foreman/scratch/p4c-a-lint.txt
?? .foreman/scratch/p4c-a-test-all.txt
?? .foreman/scratch/p4c-a-typecheck.txt
?? .foreman/scratch/p4l-expo-export.txt
?? .foreman/scratch/p4l-fix3-expo-export.txt
?? .foreman/scratch/p4l-fix3-lint.txt
?? .foreman/scratch/p4l-fix3-test.txt
?? .foreman/scratch/p4l-fix3-typecheck.txt
?? .foreman/scratch/p4l-lint.txt
?? .foreman/scratch/p4l-test.txt
?? .foreman/scratch/p4l-typecheck.txt
?? .foreman/scratch/p4lfix1-expo-export.txt
?? .foreman/scratch/p4lfix1-lint.txt
?? .foreman/scratch/p4lfix1-test.txt
?? .foreman/scratch/p4lfix1-typecheck.txt
?? .foreman/scratch/p4m-expo-export.txt
?? .foreman/scratch/p4m-lint.txt
?? .foreman/scratch/p4m-test.txt
?? .foreman/scratch/p4m-typecheck.txt
?? .foreman/scratch/p4mfix1-expo-export.txt
?? .foreman/scratch/p4mfix1-lint.txt
?? .foreman/scratch/p4mfix1-test.txt
?? .foreman/scratch/p4mfix1-typecheck.txt
?? .foreman/scratch/p4mfix4-expo-export.txt
?? .foreman/scratch/p4mfix4-lint.txt
?? .foreman/scratch/p4mfix4-test.txt
?? .foreman/scratch/p4mfix4-typecheck.txt
?? .foreman/scratch/p4ofix1-expo-export.txt
?? .foreman/scratch/p4ofix1-lint.txt
?? .foreman/scratch/p4ofix1-test.txt
?? .foreman/scratch/p4ofix1-typecheck.txt
?? .foreman/scratch/p4ofix2-expo-export.txt
?? .foreman/scratch/p4ofix2-lint.txt
?? .foreman/scratch/p4ofix2-test.txt
?? .foreman/scratch/p4ofix3-expo-export.txt
?? .foreman/scratch/p4ofix3-lint.txt
?? .foreman/scratch/p4ofix3-test.txt
?? .foreman/scratch/p4ofix3-typecheck.txt
?? .foreman/scratch/p4p-expo-export.txt
?? .foreman/scratch/p4p-lint.txt
?? .foreman/scratch/p4p-test.txt
?? .foreman/scratch/p4p-typecheck.txt
?? .foreman/scratch/p5c-b-expo-export.txt
?? .foreman/scratch/p5c-b-lint.txt
?? .foreman/scratch/p5c-b-test.txt
?? .foreman/scratch/p5c-b-typecheck.txt
?? .foreman/scratch/p5cfix2-expo-export.txt
?? .foreman/scratch/p5cfix2-lint.txt
?? .foreman/scratch/p5cfix2-test.txt
?? .foreman/scratch/p5cfix2-typecheck.txt
?? .foreman/scratch/p5d-expo-export.txt
?? .foreman/scratch/p5d-lint.txt
?? .foreman/scratch/p5d-test.txt
?? .foreman/scratch/p5d-typecheck.txt
?? .foreman/scratch/p5dfix1-expo-export.txt
?? .foreman/scratch/p5dfix1-lint.txt
?? .foreman/scratch/p5dfix1-test.txt
?? .foreman/scratch/p5dfix1-typecheck.txt
?? .foreman/scratch/p5dfix2-expo-export.txt
?? .foreman/scratch/p5dfix2-lint.txt
?? .foreman/scratch/p5dfix2-test.txt
?? .foreman/scratch/p5dfix2-typecheck.txt
?? .foreman/scratch/p5dfix3-expo-export.txt
?? .foreman/scratch/p5dfix3-lint.txt
?? .foreman/scratch/p5dfix3-test.txt
?? .foreman/scratch/p5dfix3-typecheck.txt
?? .foreman/scratch/p5dfix4-expo-export.txt
?? .foreman/scratch/p5dfix4-lint.txt
?? .foreman/scratch/p5dfix4-test.txt
?? .foreman/scratch/p5dfix4-typecheck.txt
?? .foreman/scratch/p5dfix5-expo-export.txt
?? .foreman/scratch/p5dfix5-lint.txt
?? .foreman/scratch/p5dfix5-test.txt
?? .foreman/scratch/p5dfix5-typecheck.txt
?? .foreman/scratch/p5dfix6-expo-export.txt
?? .foreman/scratch/p5dfix6-lint.txt
?? .foreman/scratch/p5dfix6-test.txt
?? .foreman/scratch/p5dfix6-typecheck.txt
?? .foreman/scratch/p6a-baseline-lint.txt
?? .foreman/scratch/p6a-baseline-test.txt
?? .foreman/scratch/p6a-baseline-typecheck.txt
?? .foreman/scratch/p6a-expo-export.txt
?? .foreman/scratch/p6a-fix1.md
?? .foreman/scratch/p6a-fix2.md
?? .foreman/scratch/p6a-lint.txt
?? .foreman/scratch/p6a-mid-test.txt
?? .foreman/scratch/p6a-test.txt
?? .foreman/scratch/p6a-ticket.md
?? .foreman/scratch/p6a-typecheck.txt
?? .foreman/scratch/p6b-codex-out.txt
?? .foreman/scratch/p6b-codex-review.md
?? .foreman/scratch/p6b-rev2-out.txt
?? .foreman/scratch/p6b-rev2.md
?? .foreman/scratch/p6b-rev3-out.txt
?? .foreman/scratch/p6b-rev3.md
?? .foreman/scratch/p6c-ticket.md
?? .foreman/scratch/p7d-ticket.md
?? .foreman/scratch/p7g-baseline-core.txt
?? .foreman/scratch/p7g-baseline-mobile-catalog.txt
?? .foreman/scratch/p7g-baseline-mobile.txt
?? .foreman/scratch/p7g-geometry-evidence.txt
?? .foreman/scratch/p7g-lint.txt
?? .foreman/scratch/p7g-test.txt
?? .foreman/scratch/p7g-ticket.md
?? .foreman/scratch/p7g-typecheck.txt
?? .foreman/scratch/p7m-baseline-test.txt
?? .foreman/scratch/p7m-lint.txt
?? .foreman/scratch/p7m-test.txt
?? .foreman/scratch/p7m-ticket.md
?? .foreman/scratch/p7m-typecheck.txt
?? .foreman/scratch/p7r-baseline-test.txt
?? .foreman/scratch/p7r-c1c2-lint.txt
?? .foreman/scratch/p7r-c1c2-test.txt
?? .foreman/scratch/p7r-c1c2-typecheck.txt
?? .foreman/scratch/p7r-e1-zero-lap-export.json
?? .foreman/scratch/p7r-e1-zero-lap-summary.md
?? .foreman/scratch/p7r-expo-export.txt
?? .foreman/scratch/p7r-lint.txt
?? .foreman/scratch/p7r-test.txt
?? .foreman/scratch/p7r-ticket.md
?? .foreman/scratch/p7r-typecheck.txt
?? .foreman/scratch/p8-ticket.md
?? .foreman/scratch/p9-codex-out.txt
?? .foreman/scratch/p9-codex-review.md
?? .foreman/scratch/ticket-p18-rev4-codex.md
?? .foreman/scratch/ticket-p5c-rev2.md
?? .foreman/scratch/ticket-p5d-rev2.md
939b93f5a2ee64fb78f3d8bdc1f1a117e4246919
docs\HANDOFF.md
docs\HANDOFF-2026-09-23-cloud.md
diff --git a/apps/mobile/src/persistence/deviceDataWipe.ts b/apps/mobile/src/persistence/deviceDataWipe.ts
index 971e758..a1c5c18 100644
--- a/apps/mobile/src/persistence/deviceDataWipe.ts
+++ b/apps/mobile/src/persistence/deviceDataWipe.ts
@@ -11,9 +11,11 @@ import { SETTINGS_KEY } from './sqlSettingsStore';
  *
  * A VIN is personal data in the EU, and so is what hangs off it -- the DID
  * bindings and sweep results are fingerprints of one specific vehicle, and a
- * learned circuit is a trace of where the driver drove. Preferences (units,
- * language, adapter address, coaching toggles, a profile the USER picked) are
- * not data about the driver and survive the wipe.
+ * learned circuit is a trace of where the driver drove. So are the custom
+ * channel definitions tagged from a sweep ("Tag as channel"): they are the
+ * same findings as the bindings, and their provenance text can hold the VIN.
+ * Preferences (units, language, adapter address, coaching toggles, a profile
+ * the USER picked) are not data about the driver and survive the wipe.
  */
 
 /** Mobile-owned tables whose every row is vehicle- or driver-specific. */
@@ -33,20 +35,31 @@ export interface DeviceDataWipeResult {
 }
 
 /**
- * The settings patch that forgets the vehicle's identity. The VIN goes; a
- * profile the VIN auto-selected goes with it, because keeping it would keep
- * the make and model the VIN resolved to. A profile the user chose is a
- * preference and stays.
+ * The settings patch that forgets the vehicle's identity. The VIN goes, and
+ * so do the channel definitions tagged from this car's sweeps; a profile the
+ * VIN auto-selected goes with them, because keeping it would keep the make
+ * and model the VIN resolved to. A profile the user chose is a preference and
+ * stays.
  */
 export function vehicleIdentityResetPatch(settings: AppSettings): Partial<AppSettings> {
-  if (settings.activeVehicleProfileSource !== 'vin') return { lastSeenVin: null };
+  const vehicleFindings = { lastSeenVin: null, enetChannelSpecsJson: DEFAULT_SETTINGS.enetChannelSpecsJson };
+  if (settings.activeVehicleProfileSource !== 'vin') return vehicleFindings;
   return {
-    lastSeenVin: null,
+    ...vehicleFindings,
     activeVehicleProfileId: DEFAULT_SETTINGS.activeVehicleProfileId,
     activeVehicleProfileSource: DEFAULT_SETTINGS.activeVehicleProfileSource,
   };
 }
 
+/** How many identity fields a stored settings blob still carries -- 0 when {@link vehicleIdentityResetPatch} has been applied. */
+function identityFieldsLeft(stored: Partial<AppSettings>): number {
+  let left = 0;
+  if (stored.lastSeenVin !== null && stored.lastSeenVin !== undefined) left += 1;
+  if (typeof stored.enetChannelSpecsJson === 'string' && stored.enetChannelSpecsJson !== '') left += 1;
+  if (stored.activeVehicleProfileSource === 'vin') left += 1;
+  return left;
+}
+
 function parseSettingsRow(raw: string | undefined): Partial<AppSettings> | null {
   if (raw === undefined) return null;
   try {
@@ -75,7 +88,20 @@ async function scrubStoredSettings(tx: SqlDatabase): Promise<number> {
   await tx.runAsync('UPDATE settings SET value = ? WHERE key = ?', [JSON.stringify(scrubbed), SETTINGS_KEY]);
   const after = await tx.getAllAsync<{ value: string }>('SELECT value FROM settings WHERE key = ?', [SETTINGS_KEY]);
   const verified = parseSettingsRow(after[0]?.value);
-  return verified === null || verified.lastSeenVin === null || verified.lastSeenVin === undefined ? 0 : 1;
+  return verified === null ? 1 : identityFieldsLeft(verified);
+}
+
+/**
+ * How many identity fields the stored settings row carries right now. Run
+ * after the wipe, through the same serialized handle as every settings
+ * write, it sees the row as it is once every write queued before it landed.
+ */
+export async function countStoredVehicleIdentity(db: SqlDatabase): Promise<number> {
+  const rows = await db.getAllAsync<{ value: string }>('SELECT value FROM settings WHERE key = ?', [SETTINGS_KEY]);
+  const raw = rows[0]?.value;
+  if (raw === undefined) return 0;
+  const stored = parseSettingsRow(raw);
+  return stored === null ? 1 : identityFieldsLeft(stored);
 }
 
 /**
@@ -105,7 +131,7 @@ export async function wipeDeviceUserData(db: SqlDatabase): Promise<DeviceDataWip
       [prefix, prefix],
     );
 
-    remaining.last_seen_vin = await scrubStoredSettings(tx);
+    remaining.vehicle_identity_settings = await scrubStoredSettings(tx);
   });
 
   const leftovers = Object.fromEntries(Object.entries(remaining).filter(([, count]) => count > 0));
diff --git a/apps/mobile/src/session/composition.ts b/apps/mobile/src/session/composition.ts
index 89380cb..3a6192a 100644
--- a/apps/mobile/src/session/composition.ts
+++ b/apps/mobile/src/session/composition.ts
@@ -101,6 +101,7 @@ import {
   type RemoveLearnedCircuitResult,
 } from './learnedCircuitStore';
 import {
+  ADOPTION_JOURNAL_KEY,
   claimAdoptionJournal,
   clearClaimedJournal,
   deleteOrphanSession,
@@ -127,7 +128,11 @@ import {
   writeSessionVehicleSnapshot,
   type SessionVehicleSnapshot,
 } from '../persistence/sessionVehicleSnapshot';
-import { vehicleIdentityResetPatch, wipeDeviceUserData } from '../persistence/deviceDataWipe';
+import {
+  countStoredVehicleIdentity,
+  vehicleIdentityResetPatch,
+  wipeDeviceUserData,
+} from '../persistence/deviceDataWipe';
 import { createRawUdsChannel } from './didSweepController';
 import { EnetTcpTransport } from './enetTcpTransport';
 import { enetAdapterReservation as sharedEnetAdapterReservation } from './enetAdapterReservation';
@@ -722,9 +727,13 @@ let vehicleProfileBindingsCache: readonly VehicleProfileBindingLike[] = [];
  * "telemetry must never fail to start" discipline in this module.
  */
 export async function refreshVehicleProfileBindingsCache(): Promise<void> {
+  const generation = deviceDataGeneration;
   try {
     const store = createVehicleProfileBindingStore(getTelemetryReadDb());
-    vehicleProfileBindingsCache = await store.listBindings(getActiveVehicleProfileId());
+    const bindings = await store.listBindings(getActiveVehicleProfileId());
+    // A read that raced delete-all may hold rows the wipe removed.
+    if (generation !== deviceDataGeneration) return;
+    vehicleProfileBindingsCache = bindings;
   } catch (error) {
     console.warn('[composition] could not refresh the vehicle profile bindings cache', error);
   }
@@ -862,6 +871,19 @@ type VinDetectionState = 'idle' | 'attempting' | 'done';
 let vinDetectionState: VinDetectionState = 'idle';
 /** The result of the ONE completed attempt this app run has made (or will make) -- `null` before `vinDetectionState === 'done'`, and forever after if the read found nothing usable. */
 let cachedDetectedVin: string | null = null;
+/**
+ * Bumped by delete-all. A VIN read that started before the wipe and finishes
+ * after it belongs to data the driver asked to erase, so it is dropped rather
+ * than cached, persisted or used for auto-select.
+ */
+let deviceDataGeneration = 0;
+/**
+ * True for the whole of delete-all, from the first identity reset to the
+ * result. No VIN read starts inside it and none that was running completes
+ * into it: a read that finished between the final check and the result
+ * would otherwise put the VIN back behind a success banner.
+ */
+let deviceWipeInProgress = false;
 
 /**
  * Codex R2 fix (ticket P4q follow-up, binding, MEDIUM): "the explicit-choice
@@ -996,6 +1018,7 @@ export function maskVin(vin: string): string {
  * already returned by then.
  */
 export async function maybeDetectVehicleFromVin(): Promise<string | null> {
+  if (deviceWipeInProgress) return null; // not marked done: the next trigger after delete-all reads again.
   if (vinDetectionState !== 'idle') return cachedDetectedVin;
   const settings = settingsStore.getSettings();
   // Q1 (binding): ENET only -- the ELM327 session has no mode-09 support.
@@ -1006,6 +1029,7 @@ export async function maybeDetectVehicleFromVin(): Promise<string | null> {
   if (token === null) return null; // lost the race to acquire -- another trigger point retries later.
 
   vinDetectionState = 'attempting';
+  const generation = deviceDataGeneration;
   const transport = new EnetTcpTransport({ host: settings.enetHost, port: settings.enetPort });
   let vin: string | null = null;
   try {
@@ -1027,6 +1051,16 @@ export async function maybeDetectVehicleFromVin(): Promise<string | null> {
     sharedEnetAdapterReservation.release(token);
   }
 
+  // Delete-all ran while this read was in flight: it has already put
+  // detection back to 'idle', so the next connection reads again.
+  if (generation !== deviceDataGeneration) return null;
+  // Delete-all is running and has not reset anything yet -- it may still be
+  // refused. Drop the result, and re-arm here rather than rely on a reset
+  // that a refusal never reaches.
+  if (deviceWipeInProgress) {
+    vinDetectionState = 'idle';
+    return null;
+  }
   vinDetectionState = 'done';
   cachedDetectedVin = vin;
   if (vin !== null) {
@@ -2461,11 +2495,7 @@ function tearDownLearnPhase(): Promise<void> {
   const run = lifecycleLock
     .run(async () => {
       if (generation !== teardownGeneration) return;
-      detachTestLoopRecording();
-      testLoopTelemetryBuffer = [];
-      await releaseTestLoopGForce();
-      await stopTelemetryRecording();
-      await disposeTestLoopProvider();
+      await unlockedTearDownLearnPhase();
     })
     .finally(() => {
       if (teardownInFlight === run) teardownInFlight = null;
@@ -2474,6 +2504,15 @@ function tearDownLearnPhase(): Promise<void> {
   return run;
 }
 
+/** The body of {@link tearDownLearnPhase}, for a caller that already holds `lifecycleLock`. */
+async function unlockedTearDownLearnPhase(): Promise<void> {
+  detachTestLoopRecording();
+  testLoopTelemetryBuffer = [];
+  await releaseTestLoopGForce();
+  await stopTelemetryRecording();
+  await disposeTestLoopProvider();
+}
+
 /**
  * P5d-FIX3 F10 / P5d-FIX4 G1 -- the Test Loop adoption journal lives in
  * `adoptionJournal.ts` (claim, compare-and-delete, attempt budget, orphan
@@ -3612,27 +3651,48 @@ export interface AggregatedDeleteUserDataResult extends DeleteUserDataResult {
    * verified on those paths. `'SESSION_ACTIVE'`: a session is genuinely
    * mid-drive (CN-FIX4, facade boundary amendment). `'DEV_REPLAY_ACTIVE'`: a
    * `__DEV__` replay controller is installed (CN-FIX5, closing amendment).
+   * `'TEST_LOOP_ACTIVE'`: a Test Loop is learning a track or saving one.
    */
-  reason?: 'SESSION_ACTIVE' | 'DEV_REPLAY_ACTIVE';
+  reason?: 'SESSION_ACTIVE' | 'DEV_REPLAY_ACTIVE' | 'TEST_LOOP_ACTIVE';
 }
 
 /**
- * The in-memory mirror of {@link wipeDeviceUserData}: drops every cached copy
- * of what that wipe deleted, so nothing on screen or in the next session
- * start still carries it. Runs whether or not the on-disk wipe verified clean
- * -- a cache must never outlive the rows it was read from.
+ * The in-memory half of {@link wipeDeviceUserData}, run BEFORE the disk wipe:
+ * the settings store rewrites its whole blob on every `update()`, so the VIN
+ * has to leave memory first. Any settings write queued before this lands
+ * ahead of the wipe's transaction and is scrubbed by it; every write after it
+ * carries no VIN. The generation bump makes a VIN read or a bindings read
+ * already in flight drop its result instead of restoring it.
  */
-async function forgetDeviceUserDataInMemory(): Promise<void> {
-  settingsStore.update(vehicleIdentityResetPatch(settingsStore.getSettings()));
+function forgetVehicleIdentityInMemory(): void {
+  deviceDataGeneration += 1;
+  vinDetectionState = 'idle';
   cachedDetectedVin = null;
+  setVinAutoDetectNotice(null);
+  settingsStore.update(vehicleIdentityResetPatch(settingsStore.getSettings()));
   vehicleProfileBindingsCache = [];
   sessionVehicleSnapshots.clear();
+}
+
+/**
+ * Brings every in-memory copy of the learned circuits and the bindings back in
+ * line with the disk after the wipe. Resolves `false` when the learned
+ * circuits could not be re-read (the deleted geometry may then still be
+ * selectable) or the stored settings still carry the vehicle's identity:
+ * either way delete-all must not report success.
+ */
+async function resyncAfterDeviceWipe(): Promise<boolean> {
+  // Again, now the wipe has committed: a VIN or bindings read that finished
+  // while it ran may have put the old values back in memory.
+  forgetVehicleIdentityInMemory();
+  let learnedOk = true;
   memoryLearnedCircuits = [];
   if (learnedCircuitStore !== null) {
     try {
       await learnedCircuitStore.refresh();
     } catch (error) {
       console.warn('[composition] could not refresh learned circuits after delete-all', error);
+      learnedOk = false;
     }
   }
   publishLearnedCircuits();
@@ -3646,6 +3706,39 @@ async function forgetDeviceUserDataInMemory(): Promise<void> {
     }
   }
   await refreshVehicleProfileBindingsCache();
+  // The last word on the VIN: read back after every settings write queued so
+  // far, so a write that carried the old blob cannot hide behind the wipe.
+  let identityOk = true;
+  if (db !== null) {
+    try {
+      identityOk = (await countStoredVehicleIdentity(db)) === 0;
+    } catch (error) {
+      console.warn('[composition] could not verify the vehicle identity was deleted', error);
+      identityOk = false;
+    }
+  }
+  return learnedOk && identityOk;
+}
+
+/**
+ * Drops what an abandoned Test Loop adoption left behind: its in-memory
+ * ledger and its journal row. Only called once the sessions are gone -- the
+ * journal names a session and a circuit, and a retry must never resume onto a
+ * circuit this wipe deleted. Resolves `false` when the row could not be
+ * removed.
+ */
+async function forgetAbandonedAdoption(): Promise<boolean> {
+  adoptionProgress = null;
+  adoptionJournalId = null;
+  adoptionJournalOwned = null;
+  if (db === null) return true;
+  try {
+    await db.runAsync('DELETE FROM settings WHERE key = ?', [ADOPTION_JOURNAL_KEY]);
+    return true;
+  } catch (error) {
+    console.warn('[composition] could not clear the test-loop adoption journal', error);
+    return false;
+  }
 }
 
 export async function deleteAllStoredUserData(): Promise<AggregatedDeleteUserDataResult> {
@@ -3657,6 +3750,15 @@ export async function deleteAllStoredUserData(): Promise<AggregatedDeleteUserDat
 }
 
 async function unlockedDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDataResult> {
+  deviceWipeInProgress = true;
+  try {
+    return await runDeleteAllStoredUserData();
+  } finally {
+    deviceWipeInProgress = false;
+  }
+}
+
+async function runDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDataResult> {
   const { repository: repo } = await ready();
   // C fix (contracts.md's facade boundary amendment, binding, ticket
   // CN-FIX4): durability. Two things had to change for "delete-all means
@@ -3694,6 +3796,39 @@ async function unlockedDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDa
       errorText: 'a developer replay or mock session is active -- leave the Dev Replay screen before deleting all data',
     };
   }
+  // 1c. REFUSE while a Test Loop is learning or saving a track: a wipe
+  //    underneath would let the adoption store the circuit again, or resume
+  //    onto the fallback circuit's controller while writing the out-lap for
+  //    the one just deleted. Stopping the learn phase on the Test Loop screen
+  //    ends the first; the second finishes on its own.
+  const testLoopPhase = testLoopController.snapshot().phase;
+  if (testLoopPhase === 'learning' || testLoopPhase === 'adopting') {
+    return {
+      ok: false,
+      remainingSessionCount: 0,
+      referenceLapCleared: false,
+      failedCircuitIds: [],
+      reason: 'TEST_LOOP_ACTIVE',
+      errorText: 'a test loop is learning or saving a track -- stop it before deleting all data',
+    };
+  }
+  // 1d. Every other Test Loop phase is ABANDONED instead. `error` holds a
+  //    learned track waiting for a Retry that must not run onto a circuit
+  //    this wipe deletes; `learned` and `failed` still hold the traced
+  //    geometry and coordinates of where the driver drove. The learn phase is
+  //    torn down before step 2 rebuilds the controller, so the new controller
+  //    is never built on a provider that is about to stop.
+  if (testLoopPhase !== 'idle') {
+    testLoopController.reset();
+    teardownGeneration += 1;
+    await unlockedTearDownLearnPhase();
+    // Its learned track is gone, so its ledger can never be resumed. The
+    // journal row stays until the wipe succeeds: until then it is what lets
+    // the next launch repair the half-adopted state.
+    adoptionProgress = null;
+    adoptionJournalId = null;
+    adoptionJournalOwned = null;
+  }
   // 2. Otherwise: drop any pending recovery and REPLACE the production
   //    controller with a fresh, idle one BEFORE deleting anything. A
   //    terminal (`sessionComplete`) controller still holds a `sessionId`
@@ -3790,6 +3925,7 @@ async function unlockedDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDa
   // records, and the learned circuits live outside every table the steps
   // above touch. Always attempted, like the telemetry step; a learned circuit
   // a surviving session still needs is kept and reported as remaining.
+  forgetVehicleIdentityInMemory();
   let deviceDataOk = true;
   if (db !== null) {
     try {
@@ -3803,24 +3939,32 @@ async function unlockedDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDa
       deviceDataOk = false;
     }
   }
-  await forgetDeviceUserDataInMemory();
-  const finalResult: DeleteUserDataResult = { ...result, ok: result.ok && telemetryOk && deviceDataOk };
+  if (!(await resyncAfterDeviceWipe())) deviceDataOk = false;
+  const sessionDataOk = result.ok && telemetryOk && deviceDataOk;
+  // Records ABOUT the sessions go only once the sessions themselves are gone:
+  // while one survives, its label and any adoption journal still describe it.
+  let sessionRecordsOk = true;
+  if (sessionDataOk && !(await forgetAbandonedAdoption())) sessionRecordsOk = false;
+  const finalResult: DeleteUserDataResult = { ...result, ok: sessionDataOk && sessionRecordsOk };
   // M4 fix (binding): a leftover active-session pointer is meaningless once
   // all stored data is gone -- cleared together, the SAME way session
   // end/discard/vanished-checkpoint do (`setActiveSession(db, null)`).
   if (finalResult.ok && db !== null) await setActiveSession(db, null);
   // Ticket P7R E2: the unvalidated-matching log is a list of facts about
   // sessions that no longer exist -- cleared with them, for the same reason
-  // the active-session pointer above is. Best-effort, exactly like the log's
-  // own writes: a stale label on a deleted session must never turn a
-  // successful wipe into a failed one.
+  // the active-session pointer above is. A row that cannot be removed is
+  // reported: delete-all promises the records are gone, not that it tried.
   if (finalResult.ok) {
     unvalidatedMatchingSessionIds = new Set();
     if (db !== null && !(await clearUnvalidatedMatchingSessionIds(db))) {
       console.warn('[composition] could not clear the unvalidated-matching log');
+      sessionRecordsOk = false;
+      finalResult.ok = false;
     }
   }
-  if (finalResult.ok && historyStore !== null) await historyStore.refresh();
+  // The history list follows the sessions: refreshed whenever they verified
+  // gone, even if a later half of the wipe failed.
+  if (result.ok && historyStore !== null) await historyStore.refresh();
 
   const errorText = finalResult.ok
     ? null
@@ -3829,6 +3973,7 @@ async function unlockedDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDa
         !perCircuitOk && failedCircuitIds.length === 0 ? 'one or more circuits did not verify empty' : null,
         !telemetryOk ? 'telemetry rows remained or could not be deleted' : null,
         !deviceDataOk ? 'vehicle or learned-circuit data remained or could not be deleted' : null,
+        !sessionRecordsOk ? 'session records remained or could not be deleted' : null,
       ]
         .filter((part): part is string => part !== null)
         .join('; ') || 'delete-all failed';
diff --git a/apps/mobile/src/ui/screens/SettingsScreen.tsx b/apps/mobile/src/ui/screens/SettingsScreen.tsx
index 11ff413..2dd6194 100644
--- a/apps/mobile/src/ui/screens/SettingsScreen.tsx
+++ b/apps/mobile/src/ui/screens/SettingsScreen.tsx
@@ -473,6 +473,14 @@ export function SettingsScreen({ navigation }: Props): React.JSX.Element {
       setDeleteErrorText(null);
       setDeleteRefused(false);
     } finally {
+      // An uncommitted channel draft can hold VIN-bearing provenance and is
+      // not in the settings store, so delete-all cannot reach it from there.
+      // Dropped however delete-all ended, even when it threw.
+      const channelSpecs = settingsStore.getSettings().enetChannelSpecsJson;
+      lastCommittedEnetChannelSpecs.current = channelSpecs;
+      setEnetChannelSpecsDraft(channelSpecs);
+      setEnetChannelSpecsError(null);
+      setEnetChannelSpecsWarnings([]);
       setDeleting(false);
       setConfirmingDelete(false);
     }
diff --git a/docs/legal/privacy-policy.en.md b/docs/legal/privacy-policy.en.md
index 5fb376d..4e3cce9 100644
--- a/docs/legal/privacy-policy.en.md
+++ b/docs/legal/privacy-policy.en.md
@@ -115,7 +115,8 @@ such:
 - It is shown in full on the Signal Finder screen, which is where you would need to read it.
 
 - "Delete all my data" (§7) removes the stored VIN, and with it a vehicle profile the app selected
-  from the VIN. The app reads the VIN again the next time a BMW-ENET-type adapter is connected.
+  from the VIN. The app tries to read the VIN again the next time a BMW-ENET-type adapter is
+  connected and not busy with another task.
 
 ### 3.4 Device motion sensors
 
@@ -255,17 +256,21 @@ attempt and your personal-best reference lap — for every circuit, not just the
 verifies the deletion actually landed before reporting success. It cannot be undone.
 
 It also removes the stored VIN, a vehicle profile the app selected from the VIN, confirmed
-per-vehicle signal bindings, ruled-out signals, diagnostic sweep records, the record of which
-vehicle profile each session used, and the geometry of circuits you taught the app.
+per-vehicle signal bindings, ruled-out signals, diagnostic sweep records, custom signal channel
+definitions (whether tagged from a sweep with "Tag as channel" or typed in Settings), the record of
+which vehicle profile each session used, the note of an interrupted Test Loop, a track the Test Loop
+learned but could not save, and the geometry of circuits you taught the app.
 
 What it keeps are your **preferences**: units, language, adapter address and port, coaching, voice
-and suggestion toggles, the selected circuit, and a vehicle profile you chose yourself. None of these
-identify you or your car. Uninstalling the app removes them too.
+and suggestion toggles, the selected circuit (unless it was a learned circuit that was deleted; then
+the default circuit is selected), and a vehicle profile you chose yourself. None of these identify
+you or your car. Uninstalling the app removes them too.
 
 Two honest caveats:
 
-1. The control is **refused while a session is running**; end the session first. This exists so the
-   deletion cannot race a live recording and leave data behind.
+1. The control is **refused while a session is running, or while a Test Loop is learning a track or
+   saving one**; end the session, or stop the Test Loop (or let it finish saving) first. This exists so the deletion
+   cannot race a live recording and leave data behind.
 2. If any part of the deletion fails, the app says so instead of reporting success. In that case a
    learned circuit that a surviving session still uses is kept, so that session can still be
    analysed; run the deletion again.
diff --git a/docs/legal/privacy-policy.ro.md b/docs/legal/privacy-policy.ro.md
index c78fadb..895b4bf 100644
--- a/docs/legal/privacy-policy.ro.md
+++ b/docs/legal/privacy-policy.ro.md
@@ -122,8 +122,8 @@ tratează ca atare:
 - Este afișat integral pe ecranul Signal Finder, adică exact acolo unde ai avea nevoie să îl citești.
 
 - „Delete all my data" (§7) șterge VIN-ul stocat și, odată cu el, un profil de vehicul pe care
-  aplicația l-a ales pe baza VIN-ului. Aplicația citește din nou VIN-ul data viitoare când este
-  conectat un adaptor de tip BMW ENET.
+  aplicația l-a ales pe baza VIN-ului. Aplicația încearcă să citească din nou VIN-ul data viitoare
+  când este conectat un adaptor de tip BMW ENET care nu e ocupat cu altă operațiune.
 
 ### 3.4 Senzorii de mișcare ai dispozitivului
 
@@ -270,19 +270,21 @@ ireversibilă.
 
 Șterge de asemenea VIN-ul stocat, un profil de vehicul ales de aplicație pe baza VIN-ului,
 legăturile de semnal confirmate pentru vehicul, semnalele excluse, înregistrările de baleiaj din
-diagnosticare, evidența profilului de vehicul folosit în fiecare sesiune și geometria circuitelor pe
-care le-ai învățat aplicației.
+diagnosticare, definițiile de canale de semnal personalizate (fie etichetate dintr-un baleiaj cu „Tag
+as channel", fie scrise în Setări), evidența profilului de vehicul folosit în fiecare sesiune, nota
+unui Test Loop întrerupt, un traseu pe care Test Loop l-a învățat dar nu l-a putut salva și geometria
+circuitelor pe care le-ai învățat aplicației.
 
 Păstrează doar **preferințele** tale: unitățile de măsură, limba, adresa și portul adaptorului,
-comutatoarele pentru coaching, voce și sugestii, circuitul selectat și un profil de vehicul ales chiar
-de tine. Niciuna dintre ele nu te identifică pe tine sau mașina ta. Dezinstalarea aplicației le
+comutatoarele pentru coaching, voce și sugestii, circuitul selectat (cu excepția unui circuit învățat
+care a fost șters; atunci se selectează circuitul implicit) și un profil de vehicul ales chiar de tine. Niciuna dintre ele nu te identifică pe tine sau mașina ta. Dezinstalarea aplicației le
 elimină și pe acestea.
 
 Două precizări oneste:
 
-1. Comanda este **refuzată cât timp o sesiune este în desfășurare**; încheie mai întâi sesiunea.
-   Această regulă există ca ștergerea să nu se poată suprapune peste o înregistrare vie și să lase
-   date în urmă.
+1. Comanda este **refuzată cât timp o sesiune este în desfășurare sau cât timp un Test Loop învață
+   ori salvează un traseu**; încheie mai întâi sesiunea sau oprește Test Loop (ori așteaptă să termine salvarea). Această regulă
+   există ca ștergerea să nu se poată suprapune peste o înregistrare vie și să lase date în urmă.
 2. Dacă o parte a ștergerii eșuează, aplicația spune asta în loc să raporteze succes. În acest caz,
    un circuit învățat pe care îl folosește încă o sesiune rămasă este păstrat, ca sesiunea să poată fi
    analizată în continuare; rulează din nou ștergerea.

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command '$p='"'apps/mobile/src/session/composition.ts'; "'$s=Get-Content $p; foreach($r in @(@(710,750),@(995,1105),@(3650,3990))){for($i=$r[0];$i -le $r[1];$i++){ '"'{0}:{1}' -f "'$i,$s[$i-1] }}' in D:\CODE\APLICTIE_Circuit
exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "git rev-parse HEAD; rg --files --hidden -g '*HANDOFF*'; Get-Content apps/mobile/package.json; Get-Content apps/mobile/test/session/composition.deleteAllVinFence.test.ts" in D:\CODE\APLICTIE_Circuit
exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'git diff 6d6f90b..939b93f -- apps/mobile/src/persistence/deviceDataWipe.ts; Get-Content apps/mobile/src/session/enetAdapterReservation.ts; Get-Content apps/mobile/src/persistence/sqlSessionHistoryStore.ts; Get-Content apps/mobile/test/support/sqlJsDatabase.ts' in D:\CODE\APLICTIE_Circuit
 succeeded in 46ms:
710:/** The vehicle profile every binding read in this module is scoped to. */
711:export function getActiveVehicleProfileId(): string {
712:  return settingsStore.getSettings().activeVehicleProfileId;
713:}
714:
715:let vehicleProfileBindingsCache: readonly VehicleProfileBindingLike[] = [];
716:
717:/**
718: * Re-reads confirmed vehicle-profile bindings from storage into the cache
719: * above. Refreshed (a) once bootstrap resolves `db` (see `runBootstrap()`),
720: * (b) whenever the Signal Finder confirms a channel (`SignalFinderScreen.tsx`'s
721: * `handleConfirm` calls this), and (c) defensively on every
722: * `telemetryProvider.start()` below -- (c) cannot affect the START it is
723: * called from (the read is async, `readVehicleProfileBindings()` is not),
724: * only the NEXT one, so (a)/(b) are what make a freshly confirmed binding
725: * actually reach the very next start. Never throws: a read failure just
726: * leaves the previous (or empty) cache in place, matching every other
727: * "telemetry must never fail to start" discipline in this module.
728: */
729:export async function refreshVehicleProfileBindingsCache(): Promise<void> {
730:  const generation = deviceDataGeneration;
731:  try {
732:    const store = createVehicleProfileBindingStore(getTelemetryReadDb());
733:    const bindings = await store.listBindings(getActiveVehicleProfileId());
734:    // A read that raced delete-all may hold rows the wipe removed.
735:    if (generation !== deviceDataGeneration) return;
736:    vehicleProfileBindingsCache = bindings;
737:  } catch (error) {
738:    console.warn('[composition] could not refresh the vehicle profile bindings cache', error);
739:  }
740:}
741:
742:/** Test/diagnostic visibility into the cache above. */
743:export function getVehicleProfileBindingsCache(): readonly VehicleProfileBindingLike[] {
744:  return vehicleProfileBindingsCache;
745:}
746:
747:/**
748: * Ticket P14 H7 (binding) -- THE CAR THIS SESSION WAS RECORDED WITH, WRITTEN
749: * DOWN AT THE TIME.
750: *
995:}
996:
997:/**
998: * Ticket P4q Q1 (binding): the one-shot ENET VIN read -- "runs when the
999: * adapter reservation is free (e.g. when the Signal Finder screen opens or
1000: * telemetry connects)". Safe to call from multiple trigger points and
1001: * multiple times: after the FIRST attempt actually completes (found or not),
1002: * every later call returns the cached result immediately without touching
1003: * the adapter again. A call that finds the reservation held by someone else
1004: * returns `null` WITHOUT marking detection done, so a later trigger still
1005: * gets its own chance once the adapter frees up.
1006: *
1007: * R3 fix (Codex follow-up, binding, LOW): the connect-and-read attempt
1008: * (`transport.connect()` -- which itself starts with a LAZY native
1009: * `react-native-tcp-socket` import -- then the one-shot read) is raced
1010: * against {@link VIN_READ_OVERALL_TIMEOUT_MS}. A hung `connect()` (a dead
1011: * adapter, a native import that never resolves) therefore never holds the
1012: * shared reservation past that bound: `finally` below closes the transport
1013: * and releases the token EITHER way, whichever branch of the race actually
1014: * settled it first. The losing side's eventual settlement (a `connect()`
1015: * that resolves or rejects long after the timeout already won) is harmless --
1016: * `Promise.race` itself attaches a handler to every promise it is given, so
1017: * it can never surface as an unhandled rejection, and this function has
1018: * already returned by then.
1019: */
1020:export async function maybeDetectVehicleFromVin(): Promise<string | null> {
1021:  if (deviceWipeInProgress) return null; // not marked done: the next trigger after delete-all reads again.
1022:  if (vinDetectionState !== 'idle') return cachedDetectedVin;
1023:  const settings = settingsStore.getSettings();
1024:  // Q1 (binding): ENET only -- the ELM327 session has no mode-09 support.
1025:  if (settings.adapterType !== 'enet') return null;
1026:  if (settings.enetHost.trim() === '') return null; // nothing configured to connect to.
1027:  if (sharedEnetAdapterReservation.holder() !== null) return null; // never steals a running flow's adapter.
1028:  const token = sharedEnetAdapterReservation.tryAcquire('signalFinder');
1029:  if (token === null) return null; // lost the race to acquire -- another trigger point retries later.
1030:
1031:  vinDetectionState = 'attempting';
1032:  const generation = deviceDataGeneration;
1033:  const transport = new EnetTcpTransport({ host: settings.enetHost, port: settings.enetPort });
1034:  let vin: string | null = null;
1035:  try {
1036:    const attempt = (async (): Promise<string | null> => {
1037:      await transport.connect();
1038:      const channel = createRawUdsChannel(transport, settings.enetTesterAddress, VIN_READ_ECU);
1039:      return readVinFromChannel(channel);
1040:    })();
1041:    const timedOut = new Promise<'timeout'>((resolve) => {
1042:      setTimeout(() => resolve('timeout'), VIN_READ_OVERALL_TIMEOUT_MS);
1043:    });
1044:    const raced = await Promise.race([attempt, timedOut]);
1045:    vin = raced === 'timeout' ? null : raced;
1046:  } catch (error) {
1047:    console.warn('[composition] the one-shot VIN read failed', error);
1048:    vin = null;
1049:  } finally {
1050:    await transport.close().catch(() => undefined);
1051:    sharedEnetAdapterReservation.release(token);
1052:  }
1053:
1054:  // Delete-all ran while this read was in flight: it has already put
1055:  // detection back to 'idle', so the next connection reads again.
1056:  if (generation !== deviceDataGeneration) return null;
1057:  // Delete-all is running and has not reset anything yet -- it may still be
1058:  // refused. Drop the result, and re-arm here rather than rely on a reset
1059:  // that a refusal never reaches.
1060:  if (deviceWipeInProgress) {
1061:    vinDetectionState = 'idle';
1062:    return null;
1063:  }
1064:  vinDetectionState = 'done';
1065:  cachedDetectedVin = vin;
1066:  if (vin !== null) {
1067:    // Q2 (binding): logged so a pattern can be added LATER from the real
1068:    // car -- never invented here. R4 fix (binding, LOW): MASKED -- a VIN is
1069:    // personal data, and the full value is never written to a log line; the
1070:    // driver reads it in full only on the Signal Finder screen / `lastSeenVin`.
1071:    console.info(`[composition] VIN detected: ${maskVin(vin)}`);
1072:    settingsStore.update({ lastSeenVin: vin });
1073:    applyVinAutoSelect(vin);
1074:  }
1075:  return vin;
1076:}
1077:
1078:const baseTelemetryProvider: TelemetryProvider = createTelemetryProvider({
1079:  settingsStore,
1080:  monotonicNow: () => telemetryClock.now(),
1081:  isDev: telemetryIsDev,
1082:  readVehicleProfileBindings: () => vehicleProfileBindingsCache,
1083:});
1084:
1085:/**
1086: * Thin wrapper, not a second implementation: every method delegates straight
1087: * to {@link baseTelemetryProvider}, EXCEPT `start()`, which also fires a
1088: * defensive cache refresh (fire-and-forget -- see (c) above) before
1089: * delegating. Both of this app's `start()` call sites --
1090: * `startTelemetryRecording()` below and `TelemetryScreen.tsx`'s manual
1091: * monitor -- go through this SAME exported singleton, so neither needs its
1092: * own refresh call.
1093: */
1094:export const telemetryProvider: TelemetryProvider = {
1095:  ...baseTelemetryProvider,
1096:  start(): void {
1097:    void refreshVehicleProfileBindingsCache();
1098:    baseTelemetryProvider.start();
1099:    // Ticket P4q (binding): one of this app's two VIN-detection trigger
1100:    // points ("when ... telemetry connects") -- fire-and-forget, and called
1101:    // AFTER `baseTelemetryProvider.start()` on purpose: that call may itself
1102:    // synchronously acquire the SAME shared reservation (owner `'provider'`)
1103:    // before yielding, and this must never race it for the claim -- "never
1104:    // blocks or steals the adapter from a running flow". `holder()` is
1105:    // therefore whatever telemetry's own start just left it as; a no-op
3650:   * Set when the wipe was REFUSED outright -- nothing was read, deleted, or
3651:   * verified on those paths. `'SESSION_ACTIVE'`: a session is genuinely
3652:   * mid-drive (CN-FIX4, facade boundary amendment). `'DEV_REPLAY_ACTIVE'`: a
3653:   * `__DEV__` replay controller is installed (CN-FIX5, closing amendment).
3654:   * `'TEST_LOOP_ACTIVE'`: a Test Loop is learning a track or saving one.
3655:   */
3656:  reason?: 'SESSION_ACTIVE' | 'DEV_REPLAY_ACTIVE' | 'TEST_LOOP_ACTIVE';
3657:}
3658:
3659:/**
3660: * The in-memory half of {@link wipeDeviceUserData}, run BEFORE the disk wipe:
3661: * the settings store rewrites its whole blob on every `update()`, so the VIN
3662: * has to leave memory first. Any settings write queued before this lands
3663: * ahead of the wipe's transaction and is scrubbed by it; every write after it
3664: * carries no VIN. The generation bump makes a VIN read or a bindings read
3665: * already in flight drop its result instead of restoring it.
3666: */
3667:function forgetVehicleIdentityInMemory(): void {
3668:  deviceDataGeneration += 1;
3669:  vinDetectionState = 'idle';
3670:  cachedDetectedVin = null;
3671:  setVinAutoDetectNotice(null);
3672:  settingsStore.update(vehicleIdentityResetPatch(settingsStore.getSettings()));
3673:  vehicleProfileBindingsCache = [];
3674:  sessionVehicleSnapshots.clear();
3675:}
3676:
3677:/**
3678: * Brings every in-memory copy of the learned circuits and the bindings back in
3679: * line with the disk after the wipe. Resolves `false` when the learned
3680: * circuits could not be re-read (the deleted geometry may then still be
3681: * selectable) or the stored settings still carry the vehicle's identity:
3682: * either way delete-all must not report success.
3683: */
3684:async function resyncAfterDeviceWipe(): Promise<boolean> {
3685:  // Again, now the wipe has committed: a VIN or bindings read that finished
3686:  // while it ran may have put the old values back in memory.
3687:  forgetVehicleIdentityInMemory();
3688:  let learnedOk = true;
3689:  memoryLearnedCircuits = [];
3690:  if (learnedCircuitStore !== null) {
3691:    try {
3692:      await learnedCircuitStore.refresh();
3693:    } catch (error) {
3694:      console.warn('[composition] could not refresh learned circuits after delete-all', error);
3695:      learnedOk = false;
3696:    }
3697:  }
3698:  publishLearnedCircuits();
3699:  const selectedCircuitId = settingsStore.getSettings().selectedCircuitId;
3700:  if (circuitCatalog.get(selectedCircuitId) === null) {
3701:    const fallback = circuitCatalog.get(TMR_CIRCUIT_PROFILE.circuitId);
3702:    try {
3703:      if (fallback !== null) await unlockedApplySelection(fallback);
3704:    } catch (error) {
3705:      console.warn('[composition] could not fall back to the default circuit after delete-all', error);
3706:    }
3707:  }
3708:  await refreshVehicleProfileBindingsCache();
3709:  // The last word on the VIN: read back after every settings write queued so
3710:  // far, so a write that carried the old blob cannot hide behind the wipe.
3711:  let identityOk = true;
3712:  if (db !== null) {
3713:    try {
3714:      identityOk = (await countStoredVehicleIdentity(db)) === 0;
3715:    } catch (error) {
3716:      console.warn('[composition] could not verify the vehicle identity was deleted', error);
3717:      identityOk = false;
3718:    }
3719:  }
3720:  return learnedOk && identityOk;
3721:}
3722:
3723:/**
3724: * Drops what an abandoned Test Loop adoption left behind: its in-memory
3725: * ledger and its journal row. Only called once the sessions are gone -- the
3726: * journal names a session and a circuit, and a retry must never resume onto a
3727: * circuit this wipe deleted. Resolves `false` when the row could not be
3728: * removed.
3729: */
3730:async function forgetAbandonedAdoption(): Promise<boolean> {
3731:  adoptionProgress = null;
3732:  adoptionJournalId = null;
3733:  adoptionJournalOwned = null;
3734:  if (db === null) return true;
3735:  try {
3736:    await db.runAsync('DELETE FROM settings WHERE key = ?', [ADOPTION_JOURNAL_KEY]);
3737:    return true;
3738:  } catch (error) {
3739:    console.warn('[composition] could not clear the test-loop adoption journal', error);
3740:    return false;
3741:  }
3742:}
3743:
3744:export async function deleteAllStoredUserData(): Promise<AggregatedDeleteUserDataResult> {
3745:  // Lifecycle lock amendment (binding): delete-all wipes the very data the
3746:  // recovery/selection/rebuild operations read and write, so it runs as one
3747:  // section of the SAME lock they do -- never interleaved with a resume, a
3748:  // selection change, or a controller rebuild.
3749:  return lifecycleLock.run(unlockedDeleteAllStoredUserData);
3750:}
3751:
3752:async function unlockedDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDataResult> {
3753:  deviceWipeInProgress = true;
3754:  try {
3755:    return await runDeleteAllStoredUserData();
3756:  } finally {
3757:    deviceWipeInProgress = false;
3758:  }
3759:}
3760:
3761:async function runDeleteAllStoredUserData(): Promise<AggregatedDeleteUserDataResult> {
3762:  const { repository: repo } = await ready();
3763:  // C fix (contracts.md's facade boundary amendment, binding, ticket
3764:  // CN-FIX4): durability. Two things had to change for "delete-all means
3765:  // deleted" to actually hold.
3766:  //
3767:  // 1. REFUSE while a session is genuinely mid-drive. A live controller goes
3768:  //    on writing checkpoints/laps/PBs by design, so a wipe underneath it
3769:  //    would be re-populated moments later -- and silently tearing the
3770:  //    driver's session down instead is not this action's job.
3771:  if (activeController !== null && MID_SESSION_STATES.has(currentControllerState(activeController))) {
3772:    return {
3773:      ok: false,
3774:      remainingSessionCount: 0,
3775:      referenceLapCleared: false,
3776:      failedCircuitIds: [],
3777:      reason: 'SESSION_ACTIVE',
3778:      errorText: 'a session is currently active -- end it before deleting all data',
3779:    };
3780:  }
3781:  // 1b. REFUSE while a `__DEV__` DevReplay controller is installed (CN-FIX5
3782:  //    item 3, closing amendment, binding). A replay controller sits in
3783:  //    `calibrating`/`calibrationReview`/`armed` -- none of them
3784:  //    `MID_SESSION_STATES` -- and step 2's rebuild deliberately no-ops
3785:  //    while it owns `facade`, so the wipe would leave a live controller
3786:  //    holding a session id that persists its session/checkpoint right back
3787:  //    over the deleted data. Durability wins over convenience on the dev
3788:  //    path: leave the replay, refuse the delete.
3789:  if (controller !== null && activeController !== controller) {
3790:    return {
3791:      ok: false,
3792:      remainingSessionCount: 0,
3793:      referenceLapCleared: false,
3794:      failedCircuitIds: [],
3795:      reason: 'DEV_REPLAY_ACTIVE',
3796:      errorText: 'a developer replay or mock session is active -- leave the Dev Replay screen before deleting all data',
3797:    };
3798:  }
3799:  // 1c. REFUSE while a Test Loop is learning or saving a track: a wipe
3800:  //    underneath would let the adoption store the circuit again, or resume
3801:  //    onto the fallback circuit's controller while writing the out-lap for
3802:  //    the one just deleted. Stopping the learn phase on the Test Loop screen
3803:  //    ends the first; the second finishes on its own.
3804:  const testLoopPhase = testLoopController.snapshot().phase;
3805:  if (testLoopPhase === 'learning' || testLoopPhase === 'adopting') {
3806:    return {
3807:      ok: false,
3808:      remainingSessionCount: 0,
3809:      referenceLapCleared: false,
3810:      failedCircuitIds: [],
3811:      reason: 'TEST_LOOP_ACTIVE',
3812:      errorText: 'a test loop is learning or saving a track -- stop it before deleting all data',
3813:    };
3814:  }
3815:  // 1d. Every other Test Loop phase is ABANDONED instead. `error` holds a
3816:  //    learned track waiting for a Retry that must not run onto a circuit
3817:  //    this wipe deletes; `learned` and `failed` still hold the traced
3818:  //    geometry and coordinates of where the driver drove. The learn phase is
3819:  //    torn down before step 2 rebuilds the controller, so the new controller
3820:  //    is never built on a provider that is about to stop.
3821:  if (testLoopPhase !== 'idle') {
3822:    testLoopController.reset();
3823:    teardownGeneration += 1;
3824:    await unlockedTearDownLearnPhase();
3825:    // Its learned track is gone, so its ledger can never be resumed. The
3826:    // journal row stays until the wipe succeeds: until then it is what lets
3827:    // the next launch repair the half-adopted state.
3828:    adoptionProgress = null;
3829:    adoptionJournalId = null;
3830:    adoptionJournalOwned = null;
3831:  }
3832:  // 2. Otherwise: drop any pending recovery and REPLACE the production
3833:  //    controller with a fresh, idle one BEFORE deleting anything. A
3834:  //    terminal (`sessionComplete`) controller still holds a `sessionId`
3835:  //    and a full lap set, so anything that later touches it --
3836:  //    `checkpointNow()` from a background transition, a straggler
3837:  //    `endSession()` persistence path -- would re-create rows this wipe
3838:  //    just removed. A fresh controller has no session identity at all, so
3839:  //    there is nothing left to re-persist. (`endSession()` itself is now
3840:  //    serialized on this same lock, so an in-flight one has already
3841:  //    completed by the time this section runs.)
3842:  setPendingRecovery(null);
3843:  setRecoveryNotice(null);
3844:  await unlockedRebuildProductionController();
3845:  // Multi-circuit selection addendum (ticket CN-W3): delete-all spans EVERY
3846:  // bundled circuit, not just the currently selected one -- `deleteUserData`
3847:  // itself is a single per-userId wipe (not circuit-scoped), so this loop's
3848:  // real job is the per-circuit VERIFY-EMPTY check; success requires every
3849:  // bundled circuit to come back empty.
3850:  //
3851:  // M3 fix (binding): each circuit's call is its own try/catch -- a REJECTED
3852:  // per-circuit `deleteAllUserData()` (e.g. a transient SQLite error) no
3853:  // longer aborts the loop; every remaining bundled circuit is still
3854:  // attempted, and the rejection is reflected in the aggregate `ok: false`
3855:  // plus `failedCircuitIds`/`errorText` instead of silently stopping partway
3856:  // through (previously leaving later circuits' data untouched with no
3857:  // indication why).
3858:  const perCircuitResults: DeleteUserDataResult[] = [];
3859:  const failedCircuitIds: string[] = [];
3860:  for (const summary of circuitCatalog.list()) {
3861:    try {
3862:      perCircuitResults.push(
3863:        await deleteAllUserData(repo, LOCAL_USER_ID, summary.circuitId, summary.layoutId, summary.layoutVersion),
3864:      );
3865:    } catch (error) {
3866:      console.warn(`[composition] deleteAllStoredUserData: circuit "${summary.circuitId}" failed`, error);
3867:      failedCircuitIds.push(summary.circuitId);
3868:    }
3869:  }
3870:  const perCircuitOk = failedCircuitIds.length === 0 && perCircuitResults.every((r) => r.ok);
3871:  const result: DeleteUserDataResult = {
3872:    ok: perCircuitOk,
3873:    remainingSessionCount: perCircuitResults.reduce((sum, r) => sum + r.remainingSessionCount, 0),
3874:    referenceLapCleared: perCircuitResults.length > 0 && perCircuitResults.every((r) => r.referenceLapCleared),
3875:  };
3876:  // F7 fix (WPT3, binding): `telemetry_samples` (Telemetry addendum) is a
3877:  // mobile-owned table (`persistence/telemetrySchema.ts`) that
3878:  // `@circuit/core`'s `deleteAllUserData`/`LocalSessionRepository.deleteUserData`
3879:  // (packages/core, out of this ticket's write set) has no knowledge of at
3880:  // all -- delete it here, in the SAME flow, and verify it actually landed
3881:  // empty before ever reporting success, so the UI's "All stored data
3882:  // deleted" banner is never shown while up to 200,000 rows/session remain.
3883:  //
3884:  // N4 fix (lifecycle lock amendment, binding, ticket CN-FIX3): this step is
3885:  // NO LONGER gated on the per-circuit aggregate. A rejected circuit delete
3886:  // used to suppress the telemetry DELETE + verify-empty entirely, leaving up
3887:  // to 200,000 rows/session on disk after the user asked for a full wipe. The
3888:  // telemetry step always runs; the aggregate `ok` is `circuitsOk &&
3889:  // telemetryOk`, and `errorText` names whichever half failed.
3890:  let telemetryOk = true;
3891:  if (db !== null) {
3892:    // N1-confirm residue fix (LEAD): a session's final telemetry flush can
3893:    // still be in flight when the user reaches delete-all -- await any
3894:    // in-progress shutdown first, then run DELETE + verify while HOLDING the
3895:    // shared write gate, so no straggler batch INSERT can land between (or
3896:    // after) them. `stopTelemetryRecording()` is an idempotent no-op when
3897:    // nothing is recording.
3898:    await stopTelemetryRecording();
3899:    const database = db;
3900:    try {
3901:      // P10B H5-B: expressed as a TRANSACTION rather than as a gate hold.
3902:      // The gated database now acquires the gate around every call made
3903:      // through it, so holding it here as well would deadlock -- and the
3904:      // DELETE + verify pair needs to be indivisible anyway, which is what
3905:      // `withTransactionAsync` (and only it) actually provides.
3906:      let remainingRows = 1;
3907:      await database.withTransactionAsync(async (tx) => {
3908:        await tx.runAsync('DELETE FROM telemetry_samples');
3909:        const remaining = await tx.getAllAsync<{ count: number }>(
3910:          'SELECT COUNT(*) AS count FROM telemetry_samples',
3911:        );
3912:        remainingRows = remaining[0]?.count ?? 0;
3913:      });
3914:      telemetryOk = remainingRows === 0;
3915:    } catch (error) {
3916:      // N4 (binding): symmetric with the per-circuit try/catch above -- a
3917:      // rejected telemetry delete/verify is reported as `ok: false` plus the
3918:      // fixed "telemetry" phrase in `errorText`, never as raw exception text
3919:      // and never as a rejection escaping into the UI's delete handler.
3920:      console.warn('[composition] deleteAllStoredUserData: telemetry deletion failed', error);
3921:      telemetryOk = false;
3922:    }
3923:  }
3924:  // HANDOFF release blocker: the VIN, the vehicle bindings and sweep
3925:  // records, and the learned circuits live outside every table the steps
3926:  // above touch. Always attempted, like the telemetry step; a learned circuit
3927:  // a surviving session still needs is kept and reported as remaining.
3928:  forgetVehicleIdentityInMemory();
3929:  let deviceDataOk = true;
3930:  if (db !== null) {
3931:    try {
3932:      const wipe = await wipeDeviceUserData(db);
3933:      deviceDataOk = wipe.ok;
3934:      if (!wipe.ok) {
3935:        console.warn('[composition] deleteAllStoredUserData: device data remained', wipe.remaining);
3936:      }
3937:    } catch (error) {
3938:      console.warn('[composition] deleteAllStoredUserData: device data deletion failed', error);
3939:      deviceDataOk = false;
3940:    }
3941:  }
3942:  if (!(await resyncAfterDeviceWipe())) deviceDataOk = false;
3943:  const sessionDataOk = result.ok && telemetryOk && deviceDataOk;
3944:  // Records ABOUT the sessions go only once the sessions themselves are gone:
3945:  // while one survives, its label and any adoption journal still describe it.
3946:  let sessionRecordsOk = true;
3947:  if (sessionDataOk && !(await forgetAbandonedAdoption())) sessionRecordsOk = false;
3948:  const finalResult: DeleteUserDataResult = { ...result, ok: sessionDataOk && sessionRecordsOk };
3949:  // M4 fix (binding): a leftover active-session pointer is meaningless once
3950:  // all stored data is gone -- cleared together, the SAME way session
3951:  // end/discard/vanished-checkpoint do (`setActiveSession(db, null)`).
3952:  if (finalResult.ok && db !== null) await setActiveSession(db, null);
3953:  // Ticket P7R E2: the unvalidated-matching log is a list of facts about
3954:  // sessions that no longer exist -- cleared with them, for the same reason
3955:  // the active-session pointer above is. A row that cannot be removed is
3956:  // reported: delete-all promises the records are gone, not that it tried.
3957:  if (finalResult.ok) {
3958:    unvalidatedMatchingSessionIds = new Set();
3959:    if (db !== null && !(await clearUnvalidatedMatchingSessionIds(db))) {
3960:      console.warn('[composition] could not clear the unvalidated-matching log');
3961:      sessionRecordsOk = false;
3962:      finalResult.ok = false;
3963:    }
3964:  }
3965:  // The history list follows the sessions: refreshed whenever they verified
3966:  // gone, even if a later half of the wipe failed.
3967:  if (result.ok && historyStore !== null) await historyStore.refresh();
3968:
3969:  const errorText = finalResult.ok
3970:    ? null
3971:    : [
3972:        failedCircuitIds.length > 0 ? `circuits rejected: ${failedCircuitIds.join(', ')}` : null,
3973:        !perCircuitOk && failedCircuitIds.length === 0 ? 'one or more circuits did not verify empty' : null,
3974:        !telemetryOk ? 'telemetry rows remained or could not be deleted' : null,
3975:        !deviceDataOk ? 'vehicle or learned-circuit data remained or could not be deleted' : null,
3976:        !sessionRecordsOk ? 'session records remained or could not be deleted' : null,
3977:      ]
3978:        .filter((part): part is string => part !== null)
3979:        .join('; ') || 'delete-all failed';
3980:
3981:  return { ...finalResult, failedCircuitIds, errorText };
3982:}
3983:
3984:// ---------------------------------------------------------------------------
3985:// DevReplayScreen support (MUST DO #6, __DEV__ only).
3986://
3987:// F4 fix (C6 residue): `startDevReplaySession` / `restoreProductionFacade` /
3988:// `useMockFacadeForDevReplay` all mutate the SAME shared state
3989:// (`replayController`/`activeController`/`facadeWrapper`'s inner) but
3990:// previously ran independently -- a rapid start/restore/start "storm"

 succeeded in 41ms:
{
  "name": "mobile",
  "version": "1.0.0",
  "main": "index.ts",
  "dependencies": {
    "@circuit/core": "*",
    "@expo-google-fonts/inter": "^0.4.2",
    "@expo-google-fonts/jetbrains-mono": "^0.4.1",
    "@expo-google-fonts/space-grotesk": "^0.4.1",
    "@expo/metro-runtime": "~57.0.8",
    "@react-native-masked-view/masked-view": "0.3.2",
    "@react-navigation/native": "^7.3.15",
    "@react-navigation/native-stack": "^7.18.7",
    "expo": "~57.0.11",
    "expo-audio": "~57.0.3",
    "expo-dev-client": "~57.0.10",
    "expo-file-system": "~57.0.6",
    "expo-font": "~57.0.1",
    "expo-keep-awake": "~57.0.1",
    "expo-linear-gradient": "~57.0.1",
    "expo-location": "~57.0.8",
    "expo-network": "~57.0.1",
    "expo-sensors": "~57.0.2",
    "expo-sharing": "~57.0.16",
    "expo-speech": "~57.0.1",
    "expo-sqlite": "~57.0.1",
    "expo-status-bar": "~57.0.1",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "react-native": "0.86.2",
    "react-native-safe-area-context": "~5.7.0",
    "react-native-screens": "~4.26.0",
    "react-native-tcp-socket": "^6.4.2",
    "react-native-web": "^0.21.2"
  },
  "devDependencies": {
    "@types/react": "~19.2.2",
    "typescript": "~6.0.3",
    "vitest": "^2.1.4"
  },
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "export:ios": "expo export --platform ios",
    "export:android": "expo export --platform android",
    "generate:voice-pack": "node scripts/generate-voice-pack.mjs"
  },
  "private": true
}
import { describe, expect, it, vi } from 'vitest';
import { SqlSessionRepository, type LocationSample, type SqlDatabase } from '@circuit/core';

import { createSqlJsDatabase } from '../support/sqlJsDatabase';
import { migrateTelemetrySchema } from '../../src/persistence/telemetrySchema';
import { migrateDidSweepSchema } from '../../src/persistence/didSweepSchema';

/**
 * Codex P18-REV2 H1: a VIN read that STARTS while delete-all is running must
 * not put the VIN back once delete-all has reported success. The ENET
 * transport and the VIN read itself are stubbed so the read completes
 * instantly with a real-looking VIN; everything else is the production
 * composition. Mock pattern follows `composition.circuitSelection.test.ts`.
 */

const VIN = 'WZ1DB0C04LW000001';

const seeded = vi.hoisted(() => ({
  db: undefined as unknown,
  repository: undefined as unknown,
  vinReads: 0,
  /** When set, the stub transport's close() waits on it -- holds a VIN read open across a delete-all. */
  closeGate: null as Promise<void> | null,
}));

vi.mock('expo-constants', () => ({
  default: { expoConfig: { version: 'delete-all-vin-fence-test' } },
}));

vi.mock('../../src/platform', () => {
  class StubGnssLocationProvider {
    listeners = new Set<(s: LocationSample) => void>();
    async start(): Promise<void> {}
    async stop(): Promise<void> {}
    subscribe(cb: (s: LocationSample) => void): () => void {
      this.listeners.add(cb);
      return () => {
        this.listeners.delete(cb);
      };
    }
    getDiagnostics(): unknown {
      return {
        samplesEmitted: 0,
        samplesRejectedMocked: 0,
        sampleIntervalHistogramMs: [],
        accuracyDistributionM: { sampleCount: 0, minM: null, p50M: null, p95M: null },
        reducedAccuracy: false,
      };
    }
  }
  class StubClock {
    now(): number {
      return Date.now();
    }
  }
  return {
    GnssLocationProvider: StubGnssLocationProvider,
    PerformanceNowClock: StubClock,
    ReplayLocationProvider: class {},
    startLifecycleListener: () => {},
  };
});

vi.mock('../../src/persistence/expoSqlDatabase', () => ({
  openAppDatabase: async () => ({ db: seeded.db, repository: seeded.repository }),
}));

vi.mock('../../src/session/enetTcpTransport', () => ({
  EnetTcpTransport: class {
    async connect(): Promise<void> {}
    async close(): Promise<void> {
      if (seeded.closeGate !== null) await seeded.closeGate;
    }
    onData(): () => void {
      return () => {};
    }
    onClose(): () => void {
      return () => {};
    }
    async send(): Promise<void> {}
  },
}));

vi.mock('@circuit/core', async (importOriginal) => ({
  ...(await importOriginal<typeof import('@circuit/core')>()),
  readVinFromChannel: async () => {
    seeded.vinReads += 1;
    return 'WZ1DB0C04LW000001';
  },
}));

function flush(): Promise<void> {
  return new Promise((resolve) => setTimeout(resolve, 0));
}

async function storedVin(db: SqlDatabase): Promise<unknown> {
  const rows = await db.getAllAsync<{ value: string }>('SELECT value FROM settings WHERE key = ?', ['app-settings']);
  return (JSON.parse(rows[0]?.value ?? '{}') as { lastSeenVin?: unknown }).lastSeenVin;
}

describe('composition.ts delete-all -- VIN detection fence (Codex P18-REV2 H1)', () => {
  it('a VIN read triggered at any point during delete-all does not restore the VIN after it reports success', async () => {
    const db = await createSqlJsDatabase();
    await migrateTelemetrySchema(db);
    await migrateDidSweepSchema(db);
    seeded.db = db;
    seeded.repository = await SqlSessionRepository.create(db);
    seeded.vinReads = 0;
    seeded.closeGate = null;
    vi.resetModules();
    const composition = await import('../../src/session/composition');
    await flush();

    composition.settingsStore.update({ adapterType: 'enet', enetHost: '192.168.16.254' });
    expect(await composition.maybeDetectVehicleFromVin()).toBe(VIN);
    await flush();
    expect(await storedVin(db)).toBe(VIN);

    // Every settings change delete-all makes is a moment another trigger
    // (a telemetry connect, the Signal Finder opening) could start a read.
    let deleting = true;
    const unsubscribe = composition.settingsStore.subscribe(() => {
      if (deleting) void composition.maybeDetectVehicleFromVin();
    });
    const readsBefore = seeded.vinReads;
    const result = await composition.deleteAllStoredUserData();
    deleting = false;
    unsubscribe();
    for (let i = 0; i < 5; i += 1) await flush();

    expect(result.ok).toBe(true);
    expect(seeded.vinReads).toBe(readsBefore);
    expect(composition.settingsStore.getSettings().lastSeenVin).toBeNull();
    expect(await storedVin(db)).toBeNull();

    // And detection is re-armed: the next connection after delete-all reads again.
    expect(await composition.maybeDetectVehicleFromVin()).toBe(VIN);
  });

  it('Codex P18-REV3 M1: a VIN read that completes into a REFUSED delete-all leaves detection re-armed', async () => {
    const db = await createSqlJsDatabase();
    await migrateTelemetrySchema(db);
    await migrateDidSweepSchema(db);
    seeded.db = db;
    seeded.repository = await SqlSessionRepository.create(db);
    seeded.vinReads = 0;
    let openGate: () => void = () => {};
    seeded.closeGate = new Promise<void>((resolve) => {
      openGate = resolve;
    });
    vi.resetModules();
    const composition = await import('../../src/session/composition');
    await flush();
    composition.settingsStore.update({ adapterType: 'enet', enetHost: '192.168.16.254' });

    // A learning Test Loop makes delete-all refuse.
    expect((await composition.startTestLoop()).ok).toBe(true);
    const detection = composition.maybeDetectVehicleFromVin();
    await flush();
    const deletion = composition.deleteAllStoredUserData();
    openGate();
    const [detected, refused] = await Promise.all([detection, deletion]);
    seeded.closeGate = null;

    expect(refused.reason).toBe('TEST_LOOP_ACTIVE');
    expect(detected).toBeNull();
    // Not stuck: the next trigger reads again.
    expect(await composition.maybeDetectVehicleFromVin()).toBe(VIN);
    await composition.resetTestLoop();
  });
});

 succeeded in 71ms:
      // (see its own doc comment), so this call can never prevent the
      // token below from being returned to the caller that just acquired.
      notify();
      return token;
    },
    release(token: EnetAdapterToken): void {
      if (held === null || held.token !== token) return; // stale/foreign token -- never touches a newer holder's claim.
      held = null;
      releasingToken = null;
      // P4j-FIX2 V2 (binding): every `whenFree()` waiter is resolved the
      // instant `held` is ACTUALLY cleared -- before `notify()`, so a
      // re-entrant subscriber that itself calls `tryAcquire` synchronously
      // still sees waiters already released (order never matters for a
      // promise resolution observed on a later microtask either way, but
      // this keeps the "freed" signal as close to the state change as
      // possible).
      const waiters = freeWaiters;
      freeWaiters = [];
      for (const resolve of waiters) resolve();
      // Same guarantee as `tryAcquire` above: `held` is already cleared, and
      // `notify()` can never throw, so a caller's own cleanup AFTER this
      // call (e.g. `telemetryProvider.ts`'s `resolveStopping()`) always runs.
      notify();
    },
    holder(): EnetAdapterOwner | null {
      return held?.owner ?? null;
    },
    whenFree(): Promise<void> {
      if (held === null) return Promise.resolve();
      return new Promise((resolve) => freeWaiters.push(resolve));
    },
    markReleasing(token: EnetAdapterToken): void {
      if (held !== null && held.token === token) releasingToken = token;
    },
    isReleasePending(): boolean {
      return held !== null && releasingToken === held.token;
    },
    subscribe(cb: (holder: EnetAdapterOwner | null) => void): () => void {
      listeners.add(cb);
      // Same guard as `notify()` above -- a throwing subscriber must not
      // prevent `subscribe()` itself from returning its unsubscribe function.
      try {
        cb(held?.owner ?? null);
      } catch (error) {
        console.warn('[enetAdapterReservation] a subscriber threw on initial replay -- ignored', error);
      }
      return () => listeners.delete(cb);
    },
  };
}

/** The ONE production reservation shared by `telemetryProvider.ts` and `DidProbeScreen.tsx`. */
export const enetAdapterReservation: EnetAdapterReservation = createEnetAdapterReservation();
Get-Content : Cannot find path 'D:\CODE\APLICTIE_Circuit\apps\mobile\src\persistence\sqlSessionHistoryStore.ts' 
        db.run('BEGIN');
        try {
          await fn(handle);
          db.run('COMMIT');
        } catch (err) {
          db.run('ROLLBACK');
          throw err;
        }
      });
      transactionTail = run.then(
        () => undefined,
        () => undefined,
      );
      return run;
    },
  };
  return handle;
}

/** Creates a brand-new in-memory sql.js-backed `SqlDatabase` for a test. */
export async function createSqlJsDatabase(): Promise<SqlDatabase> {
  const SQL = await loadSqlJs();
  return wrapSqlJsDatabase(new SQL.Database());
}

/** Wraps a `SqlDatabase` over the SAME underlying sql.js `Database` instance passed in, rather than a fresh one -- used to simulate re-opening the app's on-device database across a restart. */
export function wrapExistingSqlJsDatabase(db: Database): SqlDatabase {
  return wrapSqlJsDatabase(db);
}

export async function createRawSqlJsDatabase(): Promise<Database> {
  const SQL = await loadSqlJs();
  return new SQL.Database();
}

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command '$s=Get-Content apps/mobile/src/session/composition.ts; for($i=3684;$i -le 3985;$i++){ '"'{0}:{1}' -f "'$i,$s[$i-1] }; rg -n '"'VIN|personal' docs/HANDOFF.md; rg --files -g '*HistoryStore*' -g '*vitest*'; rg -n 'enetChannelSpecs|confirmDelete|settingsStore' apps/mobile/src/ui/screens/SettingsScreen.tsx" in D:\CODE\APLICTIE_Circuit
 succeeded in 56ms:
3796:      errorText: 'a developer replay or mock session is active -- leave the Dev Replay screen before deleting all data',
3797:    };
3798:  }
3799:  // 1c. REFUSE while a Test Loop is learning or saving a track: a wipe
3800:  //    underneath would let the adoption store the circuit again, or resume
3801:  //    onto the fallback circuit's controller while writing the out-lap for
3802:  //    the one just deleted. Stopping the learn phase on the Test Loop screen
3803:  //    ends the first; the second finishes on its own.
3804:  const testLoopPhase = testLoopController.snapshot().phase;
3805:  if (testLoopPhase === 'learning' || testLoopPhase === 'adopting') {
3806:    return {
3807:      ok: false,
3808:      remainingSessionCount: 0,
3809:      referenceLapCleared: false,
3810:      failedCircuitIds: [],
3811:      reason: 'TEST_LOOP_ACTIVE',
3812:      errorText: 'a test loop is learning or saving a track -- stop it before deleting all data',
3813:    };
3814:  }
3815:  // 1d. Every other Test Loop phase is ABANDONED instead. `error` holds a
3816:  //    learned track waiting for a Retry that must not run onto a circuit
3817:  //    this wipe deletes; `learned` and `failed` still hold the traced
3818:  //    geometry and coordinates of where the driver drove. The learn phase is
3819:  //    torn down before step 2 rebuilds the controller, so the new controller
3820:  //    is never built on a provider that is about to stop.
3821:  if (testLoopPhase !== 'idle') {
3822:    testLoopController.reset();
3823:    teardownGeneration += 1;
3824:    await unlockedTearDownLearnPhase();
3825:    // Its learned track is gone, so its ledger can never be resumed. The
3826:    // journal row stays until the wipe succeeds: until then it is what lets
3827:    // the next launch repair the half-adopted state.
3828:    adoptionProgress = null;
3829:    adoptionJournalId = null;
3830:    adoptionJournalOwned = null;
3831:  }
3832:  // 2. Otherwise: drop any pending recovery and REPLACE the production
3833:  //    controller with a fresh, idle one BEFORE deleting anything. A
3834:  //    terminal (`sessionComplete`) controller still holds a `sessionId`
3835:  //    and a full lap set, so anything that later touches it --
3836:  //    `checkpointNow()` from a background transition, a straggler
3837:  //    `endSession()` persistence path -- would re-create rows this wipe
3838:  //    just removed. A fresh controller has no session identity at all, so
3839:  //    there is nothing left to re-persist. (`endSession()` itself is now
3840:  //    serialized on this same lock, so an in-flight one has already
3841:  //    completed by the time this section runs.)
3842:  setPendingRecovery(null);
3843:  setRecoveryNotice(null);
3844:  await unlockedRebuildProductionController();
3845:  // Multi-circuit selection addendum (ticket CN-W3): delete-all spans EVERY
3846:  // bundled circuit, not just the currently selected one -- `deleteUserData`
3847:  // itself is a single per-userId wipe (not circuit-scoped), so this loop's
3848:  // real job is the per-circuit VERIFY-EMPTY check; success requires every
3849:  // bundled circuit to come back empty.
3850:  //
3851:  // M3 fix (binding): each circuit's call is its own try/catch -- a REJECTED
3852:  // per-circuit `deleteAllUserData()` (e.g. a transient SQLite error) no
3853:  // longer aborts the loop; every remaining bundled circuit is still
3854:  // attempted, and the rejection is reflected in the aggregate `ok: false`
3855:  // plus `failedCircuitIds`/`errorText` instead of silently stopping partway
3856:  // through (previously leaving later circuits' data untouched with no
3857:  // indication why).
3858:  const perCircuitResults: DeleteUserDataResult[] = [];
3859:  const failedCircuitIds: string[] = [];
3860:  for (const summary of circuitCatalog.list()) {
3861:    try {
3862:      perCircuitResults.push(
3863:        await deleteAllUserData(repo, LOCAL_USER_ID, summary.circuitId, summary.layoutId, summary.layoutVersion),
3864:      );
3865:    } catch (error) {
3866:      console.warn(`[composition] deleteAllStoredUserData: circuit "${summary.circuitId}" failed`, error);
3867:      failedCircuitIds.push(summary.circuitId);
3868:    }
3869:  }
3870:  const perCircuitOk = failedCircuitIds.length === 0 && perCircuitResults.every((r) => r.ok);
3871:  const result: DeleteUserDataResult = {
3872:    ok: perCircuitOk,
3873:    remainingSessionCount: perCircuitResults.reduce((sum, r) => sum + r.remainingSessionCount, 0),
3874:    referenceLapCleared: perCircuitResults.length > 0 && perCircuitResults.every((r) => r.referenceLapCleared),
3875:  };
3876:  // F7 fix (WPT3, binding): `telemetry_samples` (Telemetry addendum) is a
3877:  // mobile-owned table (`persistence/telemetrySchema.ts`) that
3878:  // `@circuit/core`'s `deleteAllUserData`/`LocalSessionRepository.deleteUserData`
3879:  // (packages/core, out of this ticket's write set) has no knowledge of at
3880:  // all -- delete it here, in the SAME flow, and verify it actually landed
3881:  // empty before ever reporting success, so the UI's "All stored data
3882:  // deleted" banner is never shown while up to 200,000 rows/session remain.
3883:  //
3884:  // N4 fix (lifecycle lock amendment, binding, ticket CN-FIX3): this step is
3885:  // NO LONGER gated on the per-circuit aggregate. A rejected circuit delete
3886:  // used to suppress the telemetry DELETE + verify-empty entirely, leaving up
3887:  // to 200,000 rows/session on disk after the user asked for a full wipe. The
3888:  // telemetry step always runs; the aggregate `ok` is `circuitsOk &&
3889:  // telemetryOk`, and `errorText` names whichever half failed.
3890:  let telemetryOk = true;
3891:  if (db !== null) {
3892:    // N1-confirm residue fix (LEAD): a session's final telemetry flush can
3893:    // still be in flight when the user reaches delete-all -- await any
3894:    // in-progress shutdown first, then run DELETE + verify while HOLDING the
3895:    // shared write gate, so no straggler batch INSERT can land between (or
3896:    // after) them. `stopTelemetryRecording()` is an idempotent no-op when
3897:    // nothing is recording.
3898:    await stopTelemetryRecording();
3899:    const database = db;
3900:    try {
3901:      // P10B H5-B: expressed as a TRANSACTION rather than as a gate hold.
3902:      // The gated database now acquires the gate around every call made
3903:      // through it, so holding it here as well would deadlock -- and the
3904:      // DELETE + verify pair needs to be indivisible anyway, which is what
3905:      // `withTransactionAsync` (and only it) actually provides.
3906:      let remainingRows = 1;
3907:      await database.withTransactionAsync(async (tx) => {
3908:        await tx.runAsync('DELETE FROM telemetry_samples');
3909:        const remaining = await tx.getAllAsync<{ count: number }>(
3910:          'SELECT COUNT(*) AS count FROM telemetry_samples',
3911:        );
3912:        remainingRows = remaining[0]?.count ?? 0;
3913:      });
3914:      telemetryOk = remainingRows === 0;
3915:    } catch (error) {
3916:      // N4 (binding): symmetric with the per-circuit try/catch above -- a
3917:      // rejected telemetry delete/verify is reported as `ok: false` plus the
3918:      // fixed "telemetry" phrase in `errorText`, never as raw exception text
3919:      // and never as a rejection escaping into the UI's delete handler.
3920:      console.warn('[composition] deleteAllStoredUserData: telemetry deletion failed', error);
3921:      telemetryOk = false;
3922:    }
3923:  }
3924:  // HANDOFF release blocker: the VIN, the vehicle bindings and sweep
3925:  // records, and the learned circuits live outside every table the steps
3926:  // above touch. Always attempted, like the telemetry step; a learned circuit
3927:  // a surviving session still needs is kept and reported as remaining.
3928:  forgetVehicleIdentityInMemory();
3929:  let deviceDataOk = true;
3930:  if (db !== null) {
3931:    try {
3932:      const wipe = await wipeDeviceUserData(db);
3933:      deviceDataOk = wipe.ok;
3934:      if (!wipe.ok) {
3935:        console.warn('[composition] deleteAllStoredUserData: device data remained', wipe.remaining);
3936:      }
3937:    } catch (error) {
3938:      console.warn('[composition] deleteAllStoredUserData: device data deletion failed', error);
3939:      deviceDataOk = false;
3940:    }
3941:  }
3942:  if (!(await resyncAfterDeviceWipe())) deviceDataOk = false;
3943:  const sessionDataOk = result.ok && telemetryOk && deviceDataOk;
3944:  // Records ABOUT the sessions go only once the sessions themselves are gone:
3945:  // while one survives, its label and any adoption journal still describe it.
3946:  let sessionRecordsOk = true;
3947:  if (sessionDataOk && !(await forgetAbandonedAdoption())) sessionRecordsOk = false;
3948:  const finalResult: DeleteUserDataResult = { ...result, ok: sessionDataOk && sessionRecordsOk };
3949:  // M4 fix (binding): a leftover active-session pointer is meaningless once
3950:  // all stored data is gone -- cleared together, the SAME way session
3951:  // end/discard/vanished-checkpoint do (`setActiveSession(db, null)`).
3952:  if (finalResult.ok && db !== null) await setActiveSession(db, null);
3953:  // Ticket P7R E2: the unvalidated-matching log is a list of facts about
3954:  // sessions that no longer exist -- cleared with them, for the same reason
3955:  // the active-session pointer above is. A row that cannot be removed is
3956:  // reported: delete-all promises the records are gone, not that it tried.
3957:  if (finalResult.ok) {
3958:    unvalidatedMatchingSessionIds = new Set();
3959:    if (db !== null && !(await clearUnvalidatedMatchingSessionIds(db))) {
3960:      console.warn('[composition] could not clear the unvalidated-matching log');
3961:      sessionRecordsOk = false;
3962:      finalResult.ok = false;
3963:    }
3964:  }
3965:  // The history list follows the sessions: refreshed whenever they verified
3966:  // gone, even if a later half of the wipe failed.
3967:  if (result.ok && historyStore !== null) await historyStore.refresh();
3968:
3969:  const errorText = finalResult.ok
3970:    ? null
3971:    : [
3972:        failedCircuitIds.length > 0 ? `circuits rejected: ${failedCircuitIds.join(', ')}` : null,
3973:        !perCircuitOk && failedCircuitIds.length === 0 ? 'one or more circuits did not verify empty' : null,
3974:        !telemetryOk ? 'telemetry rows remained or could not be deleted' : null,
3975:        !deviceDataOk ? 'vehicle or learned-circuit data remained or could not be deleted' : null,
3976:        !sessionRecordsOk ? 'session records remained or could not be deleted' : null,
3977:      ]
3978:        .filter((part): part is string => part !== null)
3979:        .join('; ') || 'delete-all failed';
3980:
3981:  return { ...finalResult, failedCircuitIds, errorText };
3982:}
3983:
3984:// ---------------------------------------------------------------------------
3985:// DevReplayScreen support (MUST DO #6, __DEV__ only).
54:### 1. ~~`deleteUserData` does not delete the VIN~~ — fixed
56:"Delete all my data" now also clears the VIN, a VIN-selected vehicle profile, vehicle bindings,
77:(learned circuits always draw three sectors against an empty `sectorGates`), F11 (a personal best can link
packages\core\vitest.config.ts
apps\mobile\vitest.config.ts
apps\mobile\test\session\sqlSessionHistoryStore.test.ts
apps\mobile\src\session\sqlSessionHistoryStore.ts
14:  settingsStore,
19:import { registerDevTap, type AdapterType, type AppLanguage, type DevTapState, type SpeedUnits } from '../../session/settingsStore';
154:  const settings = useSettings(settingsStore);
178:  // `registerDevTap` (pure, `settingsStore.ts`) owns the counting/reset
195:    settingsStore.update({ developerModeEnabled: next });
220:      settingsStore.update({ adapterPort: parsed });
249:      settingsStore.update({ transOilPidHex: result.value });
277:      settingsStore.update({ enetPort: parsed });
297:      settingsStore.update({ enetTesterAddress: parsed });
317:      settingsStore.update({ enetTargetAddress: parsed });
330:  const [enetChannelSpecsDraft, setEnetChannelSpecsDraft] = React.useState(settings.enetChannelSpecsJson);
331:  const lastCommittedEnetChannelSpecs = React.useRef(settings.enetChannelSpecsJson);
332:  const [enetChannelSpecsError, setEnetChannelSpecsError] = React.useState<string | null>(null);
333:  const [enetChannelSpecsWarnings, setEnetChannelSpecsWarnings] = React.useState<readonly string[]>([]);
335:    if (settings.enetChannelSpecsJson !== lastCommittedEnetChannelSpecs.current) {
336:      lastCommittedEnetChannelSpecs.current = settings.enetChannelSpecsJson;
337:      setEnetChannelSpecsDraft(settings.enetChannelSpecsJson);
339:  }, [settings.enetChannelSpecsJson]);
342:    const result = validateEnetChannelSpecsJson(enetChannelSpecsDraft);
344:      lastCommittedEnetChannelSpecs.current = enetChannelSpecsDraft;
345:      settingsStore.update({ enetChannelSpecsJson: enetChannelSpecsDraft });
381:      const current = settingsStore.getSettings();
434:    settingsStore.update(applyDiscoveryResult(result, new Date().toISOString()));
464:  async function confirmDelete(): Promise<void> {
479:      const channelSpecs = settingsStore.getSettings().enetChannelSpecsJson;
507:            onChange={(language) => settingsStore.update({ language })}
522:            onChange={(units) => settingsStore.update({ units })}
538:                settingsStore.update({ deltaDeadbandMs: Math.max(MIN_DEADBAND_MS, settings.deltaDeadbandMs - DEADBAND_STEP_MS) })
553:                settingsStore.update({ deltaDeadbandMs: Math.min(MAX_DEADBAND_MS, settings.deltaDeadbandMs + DEADBAND_STEP_MS) })
587:              onValueChange={(value) => settingsStore.update({ coachingEnabled: value })}
608:                onValueChange={(value) => settingsStore.update({ voiceCoachEnabled: value })}
638:              onValueChange={(value) => settingsStore.update({ suggestionsEnabled: value })}
676:              onValueChange={(value) => settingsStore.update({ imuGyroCaptureEnabled: value })}
698:              onValueChange={(value) => settingsStore.update({ imuFusionEnabled: value })}
720:              onValueChange={(value) => settingsStore.update({ analysisSmoothingEnabled: value })}
747:              onValueChange={(value) => settingsStore.update({ telemetryEnabled: value })}
760:                onChange={(adapterType) => settingsStore.update({ adapterType })}
776:                      onChangeText={(text) => settingsStore.update({ adapterHost: text })}
855:                        settingsStore.update({ enetHost: text, enetHostProvenance: '' })
928:                      value={enetChannelSpecsDraft}
931:                        if (enetChannelSpecsError !== null) setEnetChannelSpecsError(null);
943:                  {enetChannelSpecsError === null ? null : (
945:                      {enetChannelSpecsError}
948:                  {enetChannelSpecsWarnings.length === 0 ? null : (
950:                      {enetChannelSpecsWarnings.join('; ')}
970:                      onValueChange={(value) => settingsStore.update({ enetAutoDiscover: value })}
1043:                    onValueChange={(value) => settingsStore.update({ telemetrySimulate: value })}
1073:            onChange={(key) => settingsStore.update({ coverageBins: { thresholds: COVERAGE_BIN_PRESETS[key]! } })}
1100:              // toggles it (`registerDevTap`, pure, `settingsStore.ts`).
1157:                    style={styles.confirmDeleteButton}
1158:                    onPress={() => void confirmDelete()}
1164:                    <Text style={styles.confirmDeleteButtonText} maxFontSizeMultiplier={1.3}>
1462:  confirmDeleteButton: {
1471:  confirmDeleteButtonText: { ...typography.body, color: colors.textPrimary, fontFamily: fontFamily.bodySemibold },

codex
The VIN branch now re-arms detection after a refusal without accepting the discarded result. The history change covers device-wipe failures, but an earlier active-pointer rejection can still bypass refresh. I’m checking the regression test and the remaining cumulative changes.

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "Get-Content apps/mobile/vitest.config.ts; Get-Content apps/mobile/src/session/sqlSessionHistoryStore.ts -TotalCount 125; Get-Content apps/mobile/src/persistence/deviceDataWipe.ts; rg -n 'historyStore|refreshHistory|unlockedApplySelection' apps/mobile/src/session/composition.ts; Get-Content apps/mobile/src/ui/screens/HistoryScreen.tsx -TotalCount 140; Get-Content docs/HANDOFF.md -TotalCount 66" in D:\CODE\APLICTIE_Circuit
 succeeded in 202ms:
import { defineConfig } from 'vitest/config';

// Mirrors packages/core/vitest.config.ts (the model this ticket follows).
// PURE-TS session/persistence modules only -- no React Native test renderer,
// no expo runtime; see apps/mobile/test/support/** for how expo-* imports
// are kept out of the modules under test. The generated voiceClips.gen.ts
// guards its Metro asset requires with safeRequire, so it loads under
// vite-node without any asset-stubbing plugin or alias here.
export default defineConfig({
  test: {
    environment: 'node',
    include: ['test/**/*.test.ts'],
  },
});
import type { LapRecord, LocalSessionRepository, ReferenceLap } from '@circuit/core';
import type { PersonalBestEntry, SessionHistoryStore, StoredSession } from './mockHistory';

function syntheticLapFromReference(ref: ReferenceLap): LapRecord {
  // Fallback only: used if the PB's originating session/lap somehow isn't
  // present in the cached session list (e.g. its session row predates this
  // store, or telemetry-only edge cases) -- still real, stored data (the
  // reference lap itself), never fabricated timing.
  return {
    lapNumber: ref.lapNumber,
    tStart: 0,
    tEnd: ref.durationMs,
    durationMs: ref.durationMs,
    sectorTimes: ref.sectorTimes,
    valid: true,
    invalidReasons: [],
    quality: ref.gnssQualitySummary.level,
  };
}

/**
 * `SessionHistoryStore` (S9-S11) backed by the real `SqlSessionRepository`
 * (MUST DO #4). `listSessions()`/`getSession()`/`getPersonalBest()` stay
 * synchronous, matching the existing interface screens call directly during
 * render -- `refresh()` (async) repopulates an in-memory cache, called once
 * at app startup and again after every session save.
 */
export class SqlSessionHistoryStore implements SessionHistoryStore {
  private cache: StoredSession[] = [];
  private pb: PersonalBestEntry | null = null;

  constructor(
    // Interface, not the concrete SQL class: the web dev preview substitutes
    // the in-memory repository (expo-sqlite's wasm backend is unreliable there).
    private readonly repository: LocalSessionRepository,
    private readonly userId: string,
    private readonly circuitId: string,
    private readonly layoutId: string,
    private readonly layoutVersion: number,
  ) {}

  async refresh(): Promise<void> {
    const summaries = await this.repository.listSessions(this.userId, this.circuitId);
    this.cache = summaries
      .filter((s) => s.layoutId === this.layoutId && s.layoutVersion === this.layoutVersion)
      .map((s) => ({
        sessionId: s.sessionId,
        circuitId: s.circuitId,
        layoutId: s.layoutId,
        displayDateUtc: s.startedAtUtc,
        laps: s.laps,
        // Ticket P10A H6/H7: carried straight through from the stored row.
        // An absent value stays absent here -- the readers turn that into
        // `'unknown'`, never into `'validated'`.
        ...(s.calibrationStatus === undefined ? {} : { calibrationStatus: s.calibrationStatus }),
        // Ticket P12 item C: BOTH trace figures, not just the first. The
        // session report has to be able to say "storage misbehaved N times
        // and nothing was lost" as well as "N fixes never landed".
        ...(s.trace === undefined
          ? {}
          : {
              unwrittenSampleCount: s.trace.unwrittenSampleCount,
              failedWriteCount: s.trace.failedWriteCount,
              // Ticket P14 H3: absent stays ABSENT (= unknown). A row written
              // before the column existed must not read back as finalised.
              ...(s.trace.recordingFinalized === undefined
                ? {}
                : { recordingFinalized: s.trace.recordingFinalized }),
            }),
      }));

    const ref = await this.repository.getReferenceLap(this.userId, this.circuitId, this.layoutId, this.layoutVersion);
    if (ref === null) {
      this.pb = null;
      return;
    }
    const session = this.cache.find((s) => s.sessionId === ref.sessionId);
    const lap = session?.laps.find((l) => l.lapNumber === ref.lapNumber) ?? syntheticLapFromReference(ref);
    this.pb = { lap, sessionId: ref.sessionId, recordedAtUtc: ref.recordedAtUtc };
  }

  listSessions(): StoredSession[] {
    return [...this.cache];
  }

  getSession(sessionId: string): StoredSession | null {
    return this.cache.find((s) => s.sessionId === sessionId) ?? null;
  }

  getPersonalBest(): PersonalBestEntry | null {
    return this.pb;
  }
}
import type { SqlDatabase } from '@circuit/core';

import { DEFAULT_SETTINGS, type AppSettings } from '../session/settingsStore';
import { SESSION_VEHICLE_SNAPSHOT_KEY_PREFIX } from './sessionVehicleSnapshot';
import { SETTINGS_KEY } from './sqlSettingsStore';

/**
 * The half of "Delete all my data" that `@circuit/core`'s
 * `deleteUserData` cannot see: everything the mobile app stores about the
 * driver's CAR and the driver's own CIRCUITS, outside the session tables.
 *
 * A VIN is personal data in the EU, and so is what hangs off it -- the DID
 * bindings and sweep results are fingerprints of one specific vehicle, and a
 * learned circuit is a trace of where the driver drove. So are the custom
 * channel definitions tagged from a sweep ("Tag as channel"): they are the
 * same findings as the bindings, and their provenance text can hold the VIN.
 * Preferences (units, language, adapter address, coaching toggles, a profile
 * the USER picked) are not data about the driver and survive the wipe.
 */

/** Mobile-owned tables whose every row is vehicle- or driver-specific. */
export const DEVICE_USER_DATA_TABLES = [
  'vehicle_profile_bindings',
  'signal_finder_ruled_out',
  'did_sweep_runs',
  'did_sweep_responders',
  'did_sweep_observation_samples',
  'did_sweep_observation_summaries',
] as const;

export interface DeviceDataWipeResult {
  ok: boolean;
  /** Rows still present per table (and per settings concern) after the wipe; empty when `ok`. */
  remaining: Record<string, number>;
}

/**
 * The settings patch that forgets the vehicle's identity. The VIN goes, and
 * so do the channel definitions tagged from this car's sweeps; a profile the
 * VIN auto-selected goes with them, because keeping it would keep the make
 * and model the VIN resolved to. A profile the user chose is a preference and
 * stays.
 */
export function vehicleIdentityResetPatch(settings: AppSettings): Partial<AppSettings> {
  const vehicleFindings = { lastSeenVin: null, enetChannelSpecsJson: DEFAULT_SETTINGS.enetChannelSpecsJson };
  if (settings.activeVehicleProfileSource !== 'vin') return vehicleFindings;
  return {
    ...vehicleFindings,
    activeVehicleProfileId: DEFAULT_SETTINGS.activeVehicleProfileId,
    activeVehicleProfileSource: DEFAULT_SETTINGS.activeVehicleProfileSource,
  };
}

/** How many identity fields a stored settings blob still carries -- 0 when {@link vehicleIdentityResetPatch} has been applied. */
function identityFieldsLeft(stored: Partial<AppSettings>): number {
  let left = 0;
  if (stored.lastSeenVin !== null && stored.lastSeenVin !== undefined) left += 1;
  if (typeof stored.enetChannelSpecsJson === 'string' && stored.enetChannelSpecsJson !== '') left += 1;
  if (stored.activeVehicleProfileSource === 'vin') left += 1;
  return left;
}

function parseSettingsRow(raw: string | undefined): Partial<AppSettings> | null {
  if (raw === undefined) return null;
  try {
    const parsed: unknown = JSON.parse(raw);
    return typeof parsed === 'object' && parsed !== null ? (parsed as Partial<AppSettings>) : null;
  } catch {
    return null;
  }
}

async function countRows(tx: SqlDatabase, sql: string, params: readonly string[] = []): Promise<number> {
  const rows = await tx.getAllAsync<{ count: number }>(sql, params);
  return rows[0]?.count ?? 0;
}

async function scrubStoredSettings(tx: SqlDatabase): Promise<number> {
  const rows = await tx.getAllAsync<{ value: string }>('SELECT value FROM settings WHERE key = ?', [SETTINGS_KEY]);
  const raw = rows[0]?.value;
  if (raw === undefined) return 0;
  const stored = parseSettingsRow(raw);
  if (stored === null) {
    await tx.runAsync('DELETE FROM settings WHERE key = ?', [SETTINGS_KEY]);
    return 0;
  }
  const scrubbed = { ...stored, ...vehicleIdentityResetPatch({ ...DEFAULT_SETTINGS, ...stored }) };
  await tx.runAsync('UPDATE settings SET value = ? WHERE key = ?', [JSON.stringify(scrubbed), SETTINGS_KEY]);
  const after = await tx.getAllAsync<{ value: string }>('SELECT value FROM settings WHERE key = ?', [SETTINGS_KEY]);
  const verified = parseSettingsRow(after[0]?.value);
  return verified === null ? 1 : identityFieldsLeft(verified);
}

/**
 * How many identity fields the stored settings row carries right now. Run
 * after the wipe, through the same serialized handle as every settings
 * write, it sees the row as it is once every write queued before it landed.
 */
export async function countStoredVehicleIdentity(db: SqlDatabase): Promise<number> {
  const rows = await db.getAllAsync<{ value: string }>('SELECT value FROM settings WHERE key = ?', [SETTINGS_KEY]);
  const raw = rows[0]?.value;
  if (raw === undefined) return 0;
  const stored = parseSettingsRow(raw);
  return stored === null ? 1 : identityFieldsLeft(stored);
}

/**
 * Deletes and verifies, in one transaction. A learned circuit that a session
 * still points at is kept (deleting its geometry would break that session's
 * analysis and replay) and counted as remaining, so a wipe whose session
 * half failed can never report this half as clean either.
 */
export async function wipeDeviceUserData(db: SqlDatabase): Promise<DeviceDataWipeResult> {
  const remaining: Record<string, number> = {};
  await db.withTransactionAsync(async (tx) => {
    for (const table of DEVICE_USER_DATA_TABLES) {
      await tx.runAsync(`DELETE FROM ${table}`);
      remaining[table] = await countRows(tx, `SELECT COUNT(*) AS count FROM ${table}`);
    }

    await tx.runAsync(
      'DELETE FROM learned_circuits WHERE circuit_id NOT IN (SELECT circuitId FROM sessions)',
    );
    remaining.learned_circuits = await countRows(tx, 'SELECT COUNT(*) AS count FROM learned_circuits');

    const prefix = SESSION_VEHICLE_SNAPSHOT_KEY_PREFIX;
    await tx.runAsync('DELETE FROM settings WHERE substr(key, 1, length(?)) = ?', [prefix, prefix]);
    remaining.session_vehicle_snapshots = await countRows(
      tx,
      'SELECT COUNT(*) AS count FROM settings WHERE substr(key, 1, length(?)) = ?',
      [prefix, prefix],
    );

    remaining.vehicle_identity_settings = await scrubStoredSettings(tx);
  });

  const leftovers = Object.fromEntries(Object.entries(remaining).filter(([, count]) => count > 0));
  return { ok: Object.keys(leftovers).length === 0, remaining: leftovers };
}
1544:let historyStore: SqlSessionHistoryStore | null = null;
1838:        await historyStore?.refresh();
2137:  historyStore = history;
2148:async function unlockedApplySelection(entry: BundledCircuit): Promise<void> {
2207: * be observed disagreeing. See `unlockedApplySelection()`.
2230:    await unlockedApplySelection(entry);
2854:      await unlockedApplySelection(entry);
3050:        if (fallback !== null) await unlockedApplySelection(fallback);
3076:    historyStore = null;
3182:      // complementary half of N3 lives in `unlockedApplySelection()`: a
3230:    historyStore = history;
3467:        await unlockedApplySelection(recoveryEntry);
3607:      await historyStore?.refresh();
3703:      if (fallback !== null) await unlockedApplySelection(fallback);
3967:  if (result.ok && historyStore !== null) await historyStore.refresh();
4179:          void historyStore?.refresh();
4202: * immediately before `unlockedApplySelection()`. Once that write has begun it
4244:    await unlockedApplySelection(entry);
4419:  const stored = historyStore?.getSession(sessionId)?.calibrationStatus;
4452:  return historyStore?.getSession(sessionId)?.unwrittenSampleCount ?? null;
4544:  const store = historyStore;
4799:  const laps = historyStore?.getSession(sessionId)?.laps ?? [];
4818:  const laps = historyStore?.getSession(sessionId)?.laps ?? [];
4840:  const lap = historyStore
4993:        const circuitId = historyStore?.getSession(sessionId)?.circuitId ?? null;
5085:  const store = historyStore;
Get-Content: 
Line |
   2 |  … osition.ts; Get-Content apps/mobile/src/ui/screens/HistoryScreen.tsx  …
     |                ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     | Cannot find path 'D:\CODE\APLICTIE_Circuit\apps\mobile\src\ui\screens\HistoryScreen.tsx' because it does not exist.
# Handoff — TRACE, 2026-09-23

Brief for a fresh session. Self-contained: you should not need the previous conversation.

---

## What this is

**TRACE** — a GNSS lap timer and driver-coaching app for racetracks. TypeScript npm-workspaces monorepo:
`packages/core` is a pure engine (one runtime dependency, `zod`), `apps/mobile` is Expo SDK 57 / React
Native 0.86 on iOS, installed by sideloading. Repo is **public**: `dobrinz123/track_app`.

State at this commit: **3397 tests green**, typecheck 0, lint 0 errors (6 pre-existing warnings in
`didSweep*` files — leave them). Build 13 delivered to the owner.

**Before changing an area, read the documents that cover it.** They are current and were written for exactly this purpose:

| Document | What it gives you |
|---|---|
| `docs/architecture/map-core-timing.md` | How a GNSS sample becomes a lap time; the invariants and why |
| `docs/architecture/map-core-analysis.md` | The analysis engine, the data model, schema v1→v6 |
| `docs/architecture/map-mobile-app.md` | The composition root, the screens, **the platform fences** |
| `docs/architecture/map-data-and-infra.md` | Assets, generators, CI, firmware, and a clean-clone runbook |
| `docs/architecture/flow-review.md` | End-to-end journeys and where they still break |
| `docs/architecture/public-release-plan.md` | What blocks App Store / Play, ordered |
| `docs/legal/` | Draft privacy policy, terms, store privacy labels, permission strings, compliance checklist |
| `.foreman/ledger.md` | 200 KB append-only history. Grep it; do not read it whole |

---

## The hard rules this project runs on

These are the owner's, learned the hard way. Breaking them is how this app lost him a track day.

1. **No build without the full chain.** Gates → independent review at **zero HIGH findings** → then build.
2. **Never report success you did not observe.** Weeks of work went into making a failed read surface as
   FAILED rather than as "there was nothing". If you cannot verify something, say so.
3. **Field facts come from the owner, never guessed.** What happened at the track, how the phone is
   mounted, what he saw on screen — ask. Technical trade-offs are yours to decide; do not quiz him on those.
4. **Honesty gates are load-bearing, not decoration.** `geometryStatus`, calibration provenance, the
   four-valued availability model. Do not widen one to make a feature appear.
5. **Never delete a lap boundary you cannot confirm — mark it.** Three attempts at deciding the pit
   question each silently deleted or invented real laps. The rule that finally worked is that nothing is
   deleted, so no threshold can be outrun.
6. Gates run from the repo root; **`npx expo export --platform ios` runs from `apps/mobile`** — from the
   root it fails on AppEntry resolution, and that is expected, not a defect.
7. There is **no React Native render harness** and adding one has been declined repeatedly. Put decisions
   in pure tested modules and keep JSX thin. Never claim render coverage you do not have.

---

## Open work, in the order I would take it

### 1. ~~`deleteUserData` does not delete the VIN~~ — fixed

"Delete all my data" now also clears the VIN, a VIN-selected vehicle profile, vehicle bindings,
ruled-out signals, DID sweep records, per-session vehicle snapshots and learned-circuit geometry
(`apps/mobile/src/persistence/deviceDataWipe.ts`). Preferences are kept. The privacy policies and
compliance checklist item 3.5 are updated.

### 2. MotorPark's corner segmentation reads wrong

Corner 1 is a 158 m "corner" with a 324 m brake distance. The numbers are true; the label is not useful.
This is the resolution limit of a coarse traced line, not a bug in the gate. `analyzeCorners` against the
densified MotorPark asset is the place to look. The owner sees this on Monday.


exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "node --input-type=module -e 'import { startVitest } from \"vitest/node\"; const ctx = await startVitest(\"test\", [\"test/session/composition.deleteAllVinFence.test.ts\", \"test/session/composition.testLoop.test.ts\", \"test/persistence/deviceDataWipe.test.ts\"], { root: \"apps/mobile\", config: false, cache: false, watch: false, environment: \"node\", maxWorkers: 1, minWorkers: 1 }); await ctx?.close();'" in D:\CODE\APLICTIE_Circuit
 exited 1 in 13ms:
exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command '$s=Get-Content apps/mobile/src/session/composition.ts; foreach($r in @(@(2080,2200),@(4525,4565))){for($i=$r[0];$i -le $r[1];$i++){ '"'{0}:{1}' -f "'$i,$s[$i-1] }}; rg -n '"'stop|adopting|deleteRefused|deleteErrorText' apps/mobile/src/ui/screens/TestLoopScreen.tsx apps/mobile/src/ui/screens/SettingsScreen.tsx; Get-Content apps/mobile/src/session/lifecycleLock.ts; Get-Content apps/mobile/src/persistence/sqlSettingsStore.ts -Tail 70" in D:\CODE\APLICTIE_Circuit
 succeeded in 26ms:
/**
 * The ONE ordering boundary for session-lifecycle work (contracts.md's
 * "Multi-circuit selection â€” lifecycle lock amendment", binding, ticket
 * CN-FIX3).
 *
 * `composition.ts` previously had THREE independent serialization mechanisms
 * -- `selectionChain` (settings/history writes), `rebuildInFlight`
 * (controller dispose+install), and `withDevReplayLock` (replay transitions).
 * Each was individually correct, but they did not form a single ordering
 * boundary: a rebuild could dispose the controller another operation had
 * already captured, and the preflight gate could forward START_PREFLIGHT to a
 * controller built for a circuit a queued selection was about to replace
 * (Codex CN-REV3 findings H3/N2/N3). All three are replaced by ONE mutex:
 * every operation that reads or replaces the production controller runs
 * entirely inside `run()`.
 *
 * Semantics:
 *  - FIFO: sections execute in the order `run()` was called, never
 *    overlapping. A section "holds" the lock across its own `await`s, so a
 *    controller it captured cannot be disposed underneath it.
 *  - Errors propagate to that section's caller only, and always release the
 *    lock (including a synchronous throw before the section's first `await`),
 *    so one failed operation can never wedge every later one.
 *  - Re-entrancy is NOT supported, by design: a section that calls `run()`
 *    again would queue behind ITSELF and deadlock forever. Composition-level
 *    code therefore calls `unlocked*` inner routines from inside a section,
 *    never the locked public wrapper. The synchronously-detectable form of
 *    that mistake (a `run()` issued from a section's own synchronous body) is
 *    REFUSED with `LifecycleLockReentry` instead of hanging.
 *
 * Detection limit (documented deliberately): JavaScript gives no way to tell
 * a `run()` issued from a holder's post-`await` continuation apart from a
 * legitimate concurrent call that must queue -- both see `isHeld() === true`
 * from an unrelated task. Refusing on `isHeld()` alone would break the
 * queueing this lock exists for, so the assert covers the synchronous case
 * only; the structural rule ("inside a section, call `unlocked*`") is what
 * covers the rest, and every call site in `composition.ts` follows it.
 */

/** Thrown by `run()` when a section re-enters the lock from its own synchronous body -- a coding error that would otherwise self-deadlock. */
export class LifecycleLockReentry extends Error {
  constructor(message = 'lifecycleLock: re-entrant run() from inside a held section -- call the unlocked* inner routine instead') {
    super(message);
    this.name = 'LifecycleLockReentry';
  }
}

export interface LifecycleLock {
  /** Queues `fn` behind every earlier call and runs it with the lock held for its whole duration (awaits included). */
  run<T>(fn: () => Promise<T>): Promise<T>;
  /** `true` while a section is executing -- diagnostics/tests only; never a substitute for queueing through `run()`. */
  isHeld(): boolean;
}

export function createLifecycleLock(): LifecycleLock {
  let tail: Promise<unknown> = Promise.resolve();
  let held = false;
  /** `> 0` only while a section's SYNCHRONOUS body is on the stack -- the one re-entrancy case that is precisely detectable (see the module doc comment). */
  let syncDepth = 0;

  return {
    isHeld(): boolean {
      return held;
    },
    run<T>(fn: () => Promise<T>): Promise<T> {
      if (syncDepth > 0) return Promise.reject(new LifecycleLockReentry());
      const result = tail.then(async () => {
        held = true;
        try {
          let started: Promise<T>;
          syncDepth += 1;
          try {
            started = fn();
          } finally {
            syncDepth -= 1;
          }
          return await started;
        } finally {
          held = false;
        }
      });
      // The chain tail must survive a rejected section (mirrors
      // `SqlWriteGate`'s own tail discipline) -- otherwise one failure would
      // leave every later section queued behind a never-settling link.
      tail = result.then(
        () => undefined,
        () => undefined,
      );
      return result;
    },
  };
}
function parseSessionIdList(raw: string | undefined): string[] {
  if (raw === undefined) return [];
  try {
    const parsed: unknown = JSON.parse(raw);
    if (!Array.isArray(parsed)) return [];
    return parsed.filter((id): id is string => typeof id === 'string' && id.length > 0);
  } catch {
    // A corrupt row reads as "nothing recorded", never as a throw: this log
    // is read on the path to EXPORTING a session's data, and losing a label
    // must not be able to lose the data.
    return [];
  }
}

/**
 * Every session id previously recorded by
 * {@link markSessionMatchingUnvalidated}, oldest first. Never throws: a
 * failed read resolves to an empty list.
 */
export async function readUnvalidatedMatchingSessionIds(db: SqlDatabase): Promise<string[]> {
  try {
    const rows = await db.getAllAsync<{ value: string }>('SELECT value FROM settings WHERE key = ?', [
      UNVALIDATED_MATCHING_KEY,
    ]);
    return parseSessionIdList(rows[0]?.value);
  } catch {
    return [];
  }
}

/**
 * Records that `sessionId` was run on matching the calibration gate rejected.
 * Idempotent (a second call for the same id is a no-op), bounded by
 * {@link UNVALIDATED_MATCHING_LOG_LIMIT}, and never throws -- a failed write
 * must never be able to stop a driver going out.
 *
 * Resolves `true` when the log now contains the id.
 */
export async function markSessionMatchingUnvalidated(
  db: SqlDatabase,
  sessionId: string,
): Promise<boolean> {
  if (sessionId.length === 0) return false;
  try {
    const existing = await readUnvalidatedMatchingSessionIds(db);
    if (existing.includes(sessionId)) return true;
    const next = [...existing, sessionId].slice(-UNVALIDATED_MATCHING_LOG_LIMIT);
    await db.runAsync('INSERT OR REPLACE INTO settings (key, value) VALUES (?, ?)', [
      UNVALIDATED_MATCHING_KEY,
      JSON.stringify(next),
    ]);
    return true;
  } catch {
    return false;
  }
}

/**
 * Drops the whole log. Used by delete-all: these are facts about sessions
 * that no longer exist. Never throws; resolves `false` when the row could
 * not be removed.
 */
export async function clearUnvalidatedMatchingSessionIds(db: SqlDatabase): Promise<boolean> {
  try {
    await db.runAsync('DELETE FROM settings WHERE key = ?', [UNVALIDATED_MATCHING_KEY]);
    return true;
  } catch {
    return false;
  }
}

exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "@'
import { startVitest } from 'vitest/node';
const ctx = await startVitest('test', ['test/session/composition.deleteAllVinFence.test.ts','test/session/composition.testLoop.test.ts','test/persistence/deviceDataWipe.test.ts'], { root: 'apps/mobile', config: false, cache: false, watch: false, environment: 'node', maxWorkers: 1, minWorkers: 1 });
await ctx?.close();
'@ | node --input-type=module" in D:\CODE\APLICTIE_Circuit
 exited 1 in 674ms:

 RUN  v2.1.9 D:/CODE/APLICTIE_Circuit/apps/mobile


⎯⎯⎯⎯⎯⎯ Unhandled Errors ⎯⎯⎯⎯⎯⎯

Vitest caught 3 unhandled errors during the test run.
This might cause false positive tests. Resolve unhandled errors to make sure your tests are not affected.

⎯⎯⎯⎯⎯⎯ Unhandled Error ⎯⎯⎯⎯⎯⎯⎯
Error: EPERM: operation not permitted, mkdir 'C:\Users\dobri\AppData\Local\Temp\cVwCbQcsP_YCURQcva5BB'
 ❯ mkdir ../../node:internal/fs/promises:859:10
 ❯ Proxy.fetch ../../node_modules/vitest/dist/chunks/resolveConfig.rBxzbVsl.js:6636:9
 ❯ EventEmitter.onMessage ../../node_modules/vitest/dist/chunks/index.68735LiX.js:91:20


⎯⎯⎯⎯⎯⎯ Unhandled Error ⎯⎯⎯⎯⎯⎯⎯
Error: EPERM: operation not permitted, mkdir 'C:\Users\dobri\AppData\Local\Temp\cVwCbQcsP_YCURQcva5BB'
 ❯ mkdir ../../node:internal/fs/promises:859:10
 ❯ Proxy.fetch ../../node_modules/vitest/dist/chunks/resolveConfig.rBxzbVsl.js:6636:9
 ❯ EventEmitter.onMessage ../../node_modules/vitest/dist/chunks/index.68735LiX.js:91:20


⎯⎯⎯⎯⎯⎯ Unhandled Error ⎯⎯⎯⎯⎯⎯⎯
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯
Error: EPERM: operation not permitted, mkdir 'C:\Users\dobri\AppData\Local\Temp\cVwCbQcsP_YCURQcva5BB'
 ❯ mkdir ../../node:internal/fs/promises:859:10
 ❯ Proxy.fetch ../../node_modules/vitest/dist/chunks/resolveConfig.rBxzbVsl.js:6636:9
 ❯ EventEmitter.onMessage ../../node_modules/vitest/dist/chunks/index.68735LiX.js:91:20


 Test Files  no tests
      Tests  no tests
     Errors  3 errors
   Start at  19:34:28
   Duration  350ms (transform 8ms, setup 0ms, collect 0ms, tests 0ms, environment 0ms, prepare 0ms)


exec
"C:\\Users\\dobri\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -n 'vinDetectionState|deviceDataGeneration|deviceWipeInProgress|async function ready|repairAdoption' apps/mobile/src/session/composition.ts; git diff 6d6f90b..939b93f -- apps/mobile/test/session/composition.testLoop.test.ts; rg -n 'reset\\(|lastQualified|qualifiedStart|pendingCircuit' apps/mobile/src/session/testLoopController.ts; rg -n 'Delete all|selected circuit|stop the Test|circuitul selectat|oprește Test|tries to read' docs/legal/privacy-policy.*.md" in D:\CODE\APLICTIE_Circuit
 exited 1 in 10ms:
codex
PASS_WITH_NOTES

Reviewed `939b93f` (HEAD), the FIX3 delta, and cumulative `6d6f90b..939b93f`. No files changed; no agents used. **No remaining HIGH demonstrated.**

Paths below are relative to `apps/mobile/` unless prefixed with `docs/`.

| REV3 finding / qualification | Status | Evidence |
|---|---|---|
| M1: refused delete strands detection at `attempting` | **CLOSED** | `src/session/composition.ts:1056–1062`: unchanged-generation completion under the fence explicitly restores `idle`; obsolete generations return without modifying newer state. |
| L2: rejected delete skips Settings draft reset | **CLOSED** | `src/ui/screens/SettingsScreen.tsx:475–486`: `finally` resets draft, committed ref, error and warnings for success, refusal, aggregate failure and rejection. |
| FIFO protects submitted writes, not future writers | **CLOSED for the reported race** | `composition.ts:1021`, `:3753`, `:3714`: entry fence, generation invalidation and final identity verification work together. FIFO alone remains insufficient. |
| Active-profile bindings freshness | **CLOSED** | `composition.ts:730–736`, `:3668`, `:3687`, `:3708`: obsolete reads are discarded; post-wipe refresh follows the second reset. |
| Generation handling / detection rearming | **CLOSED** | `composition.ts:1056–1062`, `:3667–3674`: the new branch addresses the missing no-reset refusal case. |
| Abandoned ledger / bootstrap repair | **CLOSED within one runtime** | `composition.ts:3822–3830`, `:3730–3740`: terminal state and ledger are abandoned; journal deletion remains conditional on successful preceding cleanup. |
| Cross-runtime journal ownership | **OPEN — unchanged scope qualification** | `composition.ts:3736` deletes by key without ownership comparison. Sequence: another runtime claims the journal after this runtime’s checks; this runtime deletes that newer claim. No evidence that normal deployment runs two such runtimes concurrently. |
| Named cleanup failures prevent success | **CLOSED** | `composition.ts:3942–3962`: device verification, journal and log failures prevent success. Active-pointer/history rejection propagates to Settings’ error handling. |
| History skipped after log failure | **CLOSED** | `composition.ts:3967` depends on per-circuit `result.ok`, not the subsequently modified aggregate. |
| Broader history freshness on failure | **PARTIAL** | Device/telemetry failures no longer suppress refresh. Earlier rejection and partial per-circuit failure still can; concrete sequences below. |
| Missing test coverage | **PARTIAL** | `test/session/composition.deleteAllVinFence.test.ts:140` covers refusal-time completion. Settings rejection cleanup, late successful-wipe completion and several injected storage failures remain untested by this patch. |
| Policy: selected learned circuit | **CLOSED** | `docs/legal/privacy-policy.en.md:265`, `.ro.md:279` now describe fallback selection, matching `composition.ts:3699–3703`. |
| Policy: adopting-phase “stop” | **CLOSED in policy; PARTIAL in UI wording** | EN §7 (`:272`) and RO §7 (`:286`) allow waiting for saving. The displayed refusal still says only “stop”; see below. |
| Policy: channel scope and VIN retry eligibility | **CLOSED** | EN §3.3/§7 and corresponding RO sections explicitly cover custom definitions and adapter availability. |
| Unmentioned behavior changes | **CLOSED for policy scope; unchanged observations** | `src/persistence/deviceDataWipe.ts:134` retains the diagnostic rename. Refusal discards uncommitted drafts (`SettingsScreen.tsx:475`); terminal abandonment precedes known wipe success (`composition.ts:3822`). |

**Residual findings by severity**

1. **LOW — History freshness remains incomplete on unsuccessful deletion.**  
   Locations: `src/session/composition.ts:3952`, `:3967`; `src/session/sqlSessionHistoryStore.ts:47`.

   Concrete sequences:
   - Cache history for a bundled circuit → session deletion succeeds → active-pointer clearing rejects at `:3952` → refresh at `:3967` is never reached → cached deleted sessions remain available.
   - One circuit’s delete/verification rejects → a subsequent user-wide delete removes those sessions → aggregate `result.ok` remains false → refresh is skipped despite changed disk contents.

   Evidence: the refresh remains behind both an uncaught awaited operation and the all-circuits-success predicate. These paths report failure, **not false success**. The FIX3 change correctly closes the narrower device-wipe-failure case.

2. **LOW — Adopting refusal still offers an unavailable action in the app.**  
   Locations: `src/session/composition.ts:3812`; `src/ui/screens/SettingsScreen.tsx:1182–1183`; `src/ui/screens/TestLoopScreen.tsx:168`.

   Sequence: request deletion while Test Loop remains `adopting` → Settings displays “stop it before deleting all data” → adopting UI has no Stop control. Waiting for saving to settle is the available remedy. The policies now say this; the refusal string does not.

**VIN branch attack**

No new VIN resurrection, overlapping detection attempt, or inappropriate rearming found through this branch:

- It executes after transport close and reservation release, and returns before either cache assignment or settings persistence (`composition.ts:1050–1073`).
- While the fence remains raised, every new trigger returns at `:1021`, even though state is now `idle`.
- An actual wipe increments the generation and clears identity. Older completions take the generation-mismatch return without resetting a newer attempt.
- After refusal, rearming permits a later trigger; it does not itself start another read.
- The lifecycle lock serializes deletions, so one deletion’s `finally` cannot lower another deletion’s active fence.

**Settings assessment**

The reset reads the current singleton store after the await, rather than captured render-time settings (`SettingsScreen.tsx:479`). Updating the committed ref alongside the draft avoids the original unchanged-empty-field problem.

No new unmount or stale-closure persistence bug found: this cleanup updates component-local state/ref and does not write the discarded draft back to storage. An old component’s completion does not target a newly mounted component’s state.

**Regression-test assessment**

The new test structurally pins M1:

- Holds detection at transport close.
- Starts deletion in a refusing learning phase.
- Requires the intercepted result to be null.
- Requires the next trigger to return the VIN (`composition.deleteAllVinFence.test.ts:168`).

Under the `1dee7b2` branch, that intercepted completion leaves `attempting`; the final VIN assertion should fail. Thus it tests the observable recovery property, not merely an internal state assignment.

**Independent execution was blocked:** Vitest, with configuration/cache writing disabled, failed before collection because temporary-directory creation returned `EPERM`. I did not independently observe the current pass or baseline failure.

**Clean list**

- No remaining HIGH found in the cumulative rework.
- No demonstrated successful-delete VIN restoration through the reviewed detection paths.
- Persisted custom channels and VIN-selected identity are cleared and checked.
- Terminal Test Loop reset clears retained geometry and coordinate anchors.
- Live-session refusal precedes terminal teardown.
- The history predicate change introduces no wrong-circuit selection; normal fallback rebuilds the history store for the selected circuit.
- Settings rejection now receives the same draft cleanup as resolved outcomes.
- EN/RO policy changes agree on selection fallback and waiting for adoption to finish.
tokens used
58,558
PASS_WITH_NOTES

Reviewed `939b93f` (HEAD), the FIX3 delta, and cumulative `6d6f90b..939b93f`. No files changed; no agents used. **No remaining HIGH demonstrated.**

Paths below are relative to `apps/mobile/` unless prefixed with `docs/`.

| REV3 finding / qualification | Status | Evidence |
|---|---|---|
| M1: refused delete strands detection at `attempting` | **CLOSED** | `src/session/composition.ts:1056–1062`: unchanged-generation completion under the fence explicitly restores `idle`; obsolete generations return without modifying newer state. |
| L2: rejected delete skips Settings draft reset | **CLOSED** | `src/ui/screens/SettingsScreen.tsx:475–486`: `finally` resets draft, committed ref, error and warnings for success, refusal, aggregate failure and rejection. |
| FIFO protects submitted writes, not future writers | **CLOSED for the reported race** | `composition.ts:1021`, `:3753`, `:3714`: entry fence, generation invalidation and final identity verification work together. FIFO alone remains insufficient. |
| Active-profile bindings freshness | **CLOSED** | `composition.ts:730–736`, `:3668`, `:3687`, `:3708`: obsolete reads are discarded; post-wipe refresh follows the second reset. |
| Generation handling / detection rearming | **CLOSED** | `composition.ts:1056–1062`, `:3667–3674`: the new branch addresses the missing no-reset refusal case. |
| Abandoned ledger / bootstrap repair | **CLOSED within one runtime** | `composition.ts:3822–3830`, `:3730–3740`: terminal state and ledger are abandoned; journal deletion remains conditional on successful preceding cleanup. |
| Cross-runtime journal ownership | **OPEN — unchanged scope qualification** | `composition.ts:3736` deletes by key without ownership comparison. Sequence: another runtime claims the journal after this runtime’s checks; this runtime deletes that newer claim. No evidence that normal deployment runs two such runtimes concurrently. |
| Named cleanup failures prevent success | **CLOSED** | `composition.ts:3942–3962`: device verification, journal and log failures prevent success. Active-pointer/history rejection propagates to Settings’ error handling. |
| History skipped after log failure | **CLOSED** | `composition.ts:3967` depends on per-circuit `result.ok`, not the subsequently modified aggregate. |
| Broader history freshness on failure | **PARTIAL** | Device/telemetry failures no longer suppress refresh. Earlier rejection and partial per-circuit failure still can; concrete sequences below. |
| Missing test coverage | **PARTIAL** | `test/session/composition.deleteAllVinFence.test.ts:140` covers refusal-time completion. Settings rejection cleanup, late successful-wipe completion and several injected storage failures remain untested by this patch. |
| Policy: selected learned circuit | **CLOSED** | `docs/legal/privacy-policy.en.md:265`, `.ro.md:279` now describe fallback selection, matching `composition.ts:3699–3703`. |
| Policy: adopting-phase “stop” | **CLOSED in policy; PARTIAL in UI wording** | EN §7 (`:272`) and RO §7 (`:286`) allow waiting for saving. The displayed refusal still says only “stop”; see below. |
| Policy: channel scope and VIN retry eligibility | **CLOSED** | EN §3.3/§7 and corresponding RO sections explicitly cover custom definitions and adapter availability. |
| Unmentioned behavior changes | **CLOSED for policy scope; unchanged observations** | `src/persistence/deviceDataWipe.ts:134` retains the diagnostic rename. Refusal discards uncommitted drafts (`SettingsScreen.tsx:475`); terminal abandonment precedes known wipe success (`composition.ts:3822`). |

**Residual findings by severity**

1. **LOW — History freshness remains incomplete on unsuccessful deletion.**  
   Locations: `src/session/composition.ts:3952`, `:3967`; `src/session/sqlSessionHistoryStore.ts:47`.

   Concrete sequences:
   - Cache history for a bundled circuit → session deletion succeeds → active-pointer clearing rejects at `:3952` → refresh at `:3967` is never reached → cached deleted sessions remain available.
   - One circuit’s delete/verification rejects → a subsequent user-wide delete removes those sessions → aggregate `result.ok` remains false → refresh is skipped despite changed disk contents.

   Evidence: the refresh remains behind both an uncaught awaited operation and the all-circuits-success predicate. These paths report failure, **not false success**. The FIX3 change correctly closes the narrower device-wipe-failure case.

2. **LOW — Adopting refusal still offers an unavailable action in the app.**  
   Locations: `src/session/composition.ts:3812`; `src/ui/screens/SettingsScreen.tsx:1182–1183`; `src/ui/screens/TestLoopScreen.tsx:168`.

   Sequence: request deletion while Test Loop remains `adopting` → Settings displays “stop it before deleting all data” → adopting UI has no Stop control. Waiting for saving to settle is the available remedy. The policies now say this; the refusal string does not.

**VIN branch attack**

No new VIN resurrection, overlapping detection attempt, or inappropriate rearming found through this branch:

- It executes after transport close and reservation release, and returns before either cache assignment or settings persistence (`composition.ts:1050–1073`).
- While the fence remains raised, every new trigger returns at `:1021`, even though state is now `idle`.
- An actual wipe increments the generation and clears identity. Older completions take the generation-mismatch return without resetting a newer attempt.
- After refusal, rearming permits a later trigger; it does not itself start another read.
- The lifecycle lock serializes deletions, so one deletion’s `finally` cannot lower another deletion’s active fence.

**Settings assessment**

The reset reads the current singleton store after the await, rather than captured render-time settings (`SettingsScreen.tsx:479`). Updating the committed ref alongside the draft avoids the original unchanged-empty-field problem.

No new unmount or stale-closure persistence bug found: this cleanup updates component-local state/ref and does not write the discarded draft back to storage. An old component’s completion does not target a newly mounted component’s state.

**Regression-test assessment**

The new test structurally pins M1:

- Holds detection at transport close.
- Starts deletion in a refusing learning phase.
- Requires the intercepted result to be null.
- Requires the next trigger to return the VIN (`composition.deleteAllVinFence.test.ts:168`).

Under the `1dee7b2` branch, that intercepted completion leaves `attempting`; the final VIN assertion should fail. Thus it tests the observable recovery property, not merely an internal state assignment.

**Independent execution was blocked:** Vitest, with configuration/cache writing disabled, failed before collection because temporary-directory creation returned `EPERM`. I did not independently observe the current pass or baseline failure.

**Clean list**

- No remaining HIGH found in the cumulative rework.
- No demonstrated successful-delete VIN restoration through the reviewed detection paths.
- Persisted custom channels and VIN-selected identity are cleared and checked.
- Terminal Test Loop reset clears retained geometry and coordinate anchors.
- Live-session refusal precedes terminal teardown.
- The history predicate change introduces no wrong-circuit selection; normal fallback rebuilds the history store for the selected circuit.
- Settings rejection now receives the same draft cleanup as resolved outcomes.
- EN/RO policy changes agree on selection fallback and waiting for adoption to finish.
