# Ticket P5d — Test Loop mode (street-testable analysis), tests first — Opus
Binding: docs/architecture/contracts.md "Test Loop mode (Phase 5d)". Reuse relentlessly: the session pipeline, lap detection primitives, cornerMetrics' speed-drop windows, the Analysis screen (works on any catalog profile).
T1 core: `packages/core/src/testloop/` — (a) loop closure detection (return within R metres of start with heading within ±45°; R default 25 m; minimum lap length 300 m to reject U-turns); (b) centreline builder from lap-1 samples (resample ~5 m, light smoothing, ENU frame anchored at start); (c) synthetic corner derivation: curvature above a threshold sustained → corner candidates, merged with speed-drop windows; each corner gets entry/apex/exit distances + left/right; (d) a RuntimeProfile-compatible synthetic circuit profile (geometryStatus 'ad-hoc', corridor width generous e.g. 25 m). Pure + tested with synthetic traces (rectangle loop, figure-eight rejected or handled as one loop, noisy GPS).
T2 mobile: "Test loop" entry (visible without dev mode — Settings or the circuit selection screen; label RO/EN "Buclă de test / Test loop" with the legal/low-speed intro), flow: Start → drive lap 1 → banner "Track learned — N corners, L m" → laps 2+ timed (reuse the session controller with the synthetic profile) → Stop → session lands in History with the generated profile persisted (so Analysis/replays work later) → Analysis button.
T3 The Analysis screen on a test-loop session: works unchanged; header shows "Test loop" and the ad-hoc geometry note; channels as recorded (brake/pedal included when confirmed bindings exist).
T4 Persistence: the synthetic profile (centreline + corners) stored with the session (additive table or serialized asset) and reloadable for analysis after app restart; History rows label test loops distinctly.
T5 Guards: cues/voice OFF in test-loop mode; trackday stage inert ('ad-hoc' fails the geometry gate by construction — pin with a test); lap-1-only sessions (never closed the loop) end gracefully with an honest message.
Gates (real exit codes, repo root): `npm run typecheck`, `npm run test`, `npm run lint`, `npx expo export --platform ios` in apps/mobile — all 0. Report totals + red→green per item + all new strings (RO/EN).
WRITE SET: packages/core/src/testloop/** (+ tests), the minimal touch points in packages/core needed to accept a runtime profile (report them), apps/mobile/src/session/** (mode wiring, persistence additive), apps/mobile/src/ui/** (entry, banners, history label, strings), tests. Do not touch the Signal Finder/sweep/reservation/telemetry polling internals. No commit, no agents, no Expo dev server.
