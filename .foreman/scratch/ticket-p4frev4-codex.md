# Review ticket P4f-REV4 — Codex read-only FINAL bounded re-verify of P4f fix wave 3
Prior: `.foreman/scratch/p4frev3-codex-output.log`. Diff: `git diff ff92597..4dd9990`. Binding: "observation runner & lifecycle race amendment" (contracts.md, end). Read-only Node/Git path if spawning is denied.
TASK (bounded): verdict FIXED/PARTIAL/NOT FIXED + file:line + failing scenario for: (1) close/release race (transport retained until close settles; stop joins in-flight teardown; one lifecycle try/finally; generation guard — stopped never overwritten); (2) abortable network-info read (race vs abort + 1500 ms; stop returns; token released); (3) observation via core `runDidObservation` (single loop; keep-alive fires in a fast window; error budget; cadenceDegraded); (4) budget ≤0 = no probes. Then ONLY new defects in touched functions (shared createErrorBudget/createKeepAliveTicker refactor must be behavior-preserving for the sweep runner; stopObservationEarly vs stop generation semantics; ELM path untouched). Nothing else.
OUTPUT: first line PASS / FAIL / PASS_WITH_NOTES; 4-row table; NEW findings; Clean list. Stdout only. No agents.
