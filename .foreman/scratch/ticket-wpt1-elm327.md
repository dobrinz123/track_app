TASK: Implement the ELM327 telemetry engine for Phase 4 P4a in packages/core (pure TypeScript, zero React Native imports), exactly per the binding "Telemetry addendum" in docs/architecture/contracts.md (read it first — it is the source of truth; materialize its interfaces verbatim).

EXPECTED OUTCOME: New module packages/core/src/telemetry/ containing: (a) contracts.ts — the addendum's types verbatim (TelemetryChannelId, TelemetrySample, ObdTransport, Elm327State, Elm327Config, Elm327Session, TELEMETRY_SCHEMA_VERSION); (b) pidCodec.ts — mode-01 request encoding ("010C" etc.) and response decoding for the 6 channels (formulas in the addendum comments), tolerant of whitespace/echo/multi-frame "41 0C A B" hex; (c) elm327Session.ts — createElm327Session(transport, config, monotonicNow: () => number): full init handshake (ATZ, ATE0, ATL0, ATS0, ATSP0 each awaited to '>' prompt with initTimeoutMs), single in-flight command discipline, '>'-terminated response framing tolerant of arbitrary chunk splits, weighted round-robin scheduler honoring pollPlan hz ratios without busy-waiting (timer-driven), error taxonomy per the addendum ('NO DATA'/'STOPPED'/'?'/'CAN ERROR' = channel errors counted toward maxConsecutiveErrors; 'UNABLE TO CONNECT' during init = 'failed'), tMonoMs stamped at response arrival, graceful stop(), getDiagnostics() with observed Hz per channel; (d) simulatedTransport.ts — SimulatedElm327Transport implementing ObdTransport over a scripted deterministic vehicle model (rpm/speed/throttle/coolant as functions of scenario time; seeded PRNG — NO Math.random, NO Date.now, clock injected), with fault injection knobs: chunkFragmentation (split responses at every byte), noDataOnChannels, disconnectAfterNCommands, garbagePrefixBytes; (e) index.ts barrel export wired into the package's existing export surface.

CONTEXT: Repo D:\CODE\APLICTIE_Circuit (npm workspaces monorepo). Study packages/core/src/coach/coach-engine.ts and packages/core/src/controller/sessionController.ts for house style (named exports, injected clocks, no side effects at import). Tests live in packages/core/test/** and run with vitest (fast-check available for property tests). The monotonic clock pattern: engines take an injected now() function.

CONSTRAINTS: Pure TS only — importing react-native or expo anywhere in packages/core fails lint. No new npm dependencies. Deterministic tests only (no wall-clock sleeps; drive time via injected fake clock — look at existing test/support fake-clock helpers if present, else write a minimal one in test/support/).

MUST DO:
1. Read docs/architecture/contracts.md Telemetry addendum FIRST; interfaces must match it verbatim.
2. Tests in packages/core/test/telemetry/: pidCodec decode table for all 6 channels incl. boundary values; framing property test (any chunk split of a valid response stream parses identically — fast-check); init-handshake sequence pin (exact command order); scheduler ratio test (pollPlan [{rpm,10},{coolantC,1}] yields ~10:1 observed ratio on fake clock); error escalation (maxConsecutiveErrors -> 'failed', single NO DATA does not kill session); mid-command disconnect -> onStateChange('failed') and no unhandled rejection; stop() during polling resolves and closes transport.
3. Run: cd packages/core && npx vitest run test/telemetry > /tmp/wpt1.log 2>&1; ec=$?; report the real exit code and the pass/fail counts. If the sandbox denies running vitest (EPERM), say so explicitly — do NOT claim tests pass.
4. Run npx tsc --noEmit in packages/core and report exit code.

MUST NOT: No subagents. Do not modify any file outside packages/core/src/telemetry/, packages/core/src/index.ts (export line only), packages/core/test/telemetry/, packages/core/test/support/ (only if adding a fake-clock helper that does not exist). Do not touch sessionController, coach modules, or mobile app. Do not add dependencies. Do not use Date.now()/Math.random() anywhere in src.

WRITE SET: packages/core/src/telemetry/**, packages/core/src/index.ts, packages/core/test/telemetry/**, packages/core/test/support/** (additive only).

OUTPUT FORMAT: First line exactly one of DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED. Then: files created/modified with line counts; test command + REAL exit code + counts (or the exact denial error); tsc exit code; any deviations from the addendum with justification; open concerns.
