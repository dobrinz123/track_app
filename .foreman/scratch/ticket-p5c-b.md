# Ticket P5c-B — trackday flow: suggestion engine on the demonstrated envelope, live cue updates, interactive pit view — Opus, tests first
Binding: docs/architecture/contracts.md "Phase 5 REVISION 2" R2-3 (user-ratified) + the existing "Phase 5 — safety contract" bounds (MAX_BRAKE_LATER_M = 10, MAX_MIN_SPEED_GAIN_KPH = 3, one change per corner; honesty gates). Depends on P5c-A (labels/clean definition, landed) and P5b-FIX1 (interactive analysis screen, landed).
D1 core `packages/core/src/coaching/suggestions.ts` (new): pure engine — inputs = per-corner metrics of the CURRENT outing's clean laps + the demonstrated envelope (envelope.ts) + the active cue set (brake/lift points per corner); outputs = (a) `cueUpdates`: per corner, a new brake/lift point ONLY when a clean lap of the SAME outing demonstrated it, clamped to ≤ 10 m later than the current cue and at most ONE update per corner per stint — never beyond the demonstrated value, never on corners with `insufficient` data; (b) `pitSuggestions`: per corner, observation + concrete suggestion text data (RO/EN via reportText patterns) with the evidence (which lap demonstrated it), capped by the same bounds; suggestions beyond the demonstrated envelope are NOT generated (the report simply shows the demonstrated best). Property tests: no cue ever moves beyond the demonstrated envelope; bounds respected; insufficient → nothing; deterministic ordering.
D2 mobile live path: after each completed lap during an active session, the coaching cue provider consumes `cueUpdates` (computed off the hot path, next lap boundary) — wire into the existing corner-coaching cue source; a moved cue is logged (session event) and visible post-session; NO text/voice change beyond the existing "Brake/Lift" callouts. Setting `suggestionsEnabled` (default OFF) gates the whole stage; Settings row RO/EN under COACHING.
D3 mobile pit view: when a session is ACTIVE but the car is stationary/pitted (existing pit/stationary detection if present, else a "Pit view" button on the session screen), show the interactive between-stint view reusing the P5b analysis view-model on the laps so far: top-3 corners by time loss with badges → tap → detail + the pit suggestion line. Fast: reuse the shared runner/cache; must not disturb the running session (read-only over recorded laps; the active lap is excluded).
D4 report/export: pit suggestions that were SHOWN are included in the final export (observations + suggestions section, marked as suggestions); cue updates applied during the session listed with before/after and the demonstrating lap.
D5 honesty and safety tests: suggestionsEnabled OFF → zero behaviour change anywhere (snapshot test); < 2 clean laps in the outing → no cue updates, pit view shows observations only; a corner whose cue was updated shows the change in the pit view.
Gates (real exit codes, repo root): `npm run typecheck`, `npm run test`, `npm run lint`, `npx expo export --platform ios` in apps/mobile — all 0. Report totals + red→green per item + all new strings (RO/EN).
WRITE SET: packages/core/src/coaching/{suggestions.ts,index.ts,reportText.ts (suggestion text patterns only)}, packages/core/test/coaching/**, apps/mobile/src/session/** (cue wiring, pit view-model, export addition), the coaching cue source file(s) you identify (report which), apps/mobile/src/ui/screens/** (pit view + Settings row + strings), tests. Do not touch the Signal Finder, sweep, reservation or telemetryProvider polling internals. No commit, no agents, no Expo dev server.
