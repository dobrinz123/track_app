# Ticket P4q — VIN-based vehicle auto-detection (auto-select the active profile; manual choice stays), tests first — Sonnet
User: "the app should know the car from OBD from the start if possible; if not, let the user choose."
Q1 core/mobile VIN read: a one-shot read helper — ENET path: UDS 0x22 DID 0xF190 addressed to 0x12 over the existing raw UDS channel (reuse createRawUdsChannel; ASCII-decode, strip padding; reject non-printable); ELM327 path ONLY if the existing session infra already supports mode 09 (check; if not, report and ship ENET-only). Never blocks anything: runs when the adapter reservation is free (e.g. when the Signal Finder screen opens or telemetry connects), result cached per app run + persisted (last seen VIN, additive settings/table).
Q2 registry: vehicle profiles/catalogs get an optional `vinPatterns: string[]` (prefix or simple glob). Supra's list starts EMPTY — after the first real read the UI shows the VIN and the app logs it; do NOT invent WMI codes.
Q3 UX: profile section (Signal Finder) shows "VIN: <value>" when known; exactly ONE profile matches → auto-select it as the ACTIVE profile with a dismissible banner "Detected from VIN — <label>" (RO/EN) and the chip still overrides; zero or multiple matches → nothing changes (manual choice). Auto-select never overrides a profile the user chose EXPLICITLY this app-run.
Q4 Tests: VIN decode (padding, garbage, NRC → null), pattern matching, auto-select precedence (user override wins), persistence, ENET read against a fake channel answering 0xF190.
Gates (real exit codes, repo root): `npm run typecheck`, `npm run test`, `npm run lint`, `npx expo export --platform ios` in apps/mobile — all 0. Report totals + red→green + strings (RO/EN) + whether the ELM327 path shipped.
WRITE SET: packages/core/src/telemetry/** (VIN decode helper + catalog field), apps/mobile/src/session/{composition,signalFinderController}.ts (one-shot read + cache), the settings/persistence additive bits, apps/mobile/src/ui/screens/{SignalFinderScreen.tsx,signalFinderStrings.ts}, tests. No reservation/sweep/observation changes. No commit, no agents, no Expo dev server.
