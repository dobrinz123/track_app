# Ticket P5-FIX-ABS — streaming ABS-oscillation detector (Codex P5-REV finding 13), tests first — Sonnet
packages/core/src/coaching/cleanLap.ts (evaluateAbsOscillation/countSwings, ~line 468): the sliding-window scan rescans/slices/reduces per window → worst-case O(n²). Rewrite as a single-pass monotonic two-pointer/streaming window with identical semantics (absSwingG 0.35, absMinCycles 3, absWindowMs 700, absMinAvgDecelG 0.3). Tests: identical verdicts on the existing fixtures (keep every current test green), plus a property test comparing old-vs-new on random series (port the old implementation into the test as an oracle), plus a size test (50 000 samples completes well under a second — assert < 2 s with vitest timers off).
Gates: `npm run typecheck`, `npm run test`, `npm run lint` — all 0 (real exit codes, repo root). WRITE SET: packages/core/src/coaching/cleanLap.ts + packages/core/test/coaching/cleanLap.test.ts only. No commit, no agents. NOTE: other workers are editing other coaching files — do not touch anything else.
