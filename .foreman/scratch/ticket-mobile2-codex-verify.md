Read-only cross-family verifier. Commit e231899 landed the mobile telemetry channel revision (Sonnet worker). Binding spec: docs/architecture/contracts.md "Telemetry addendum — channel revision (2026-08-11, binding)". Original ticket: .foreman/scratch/ticket-wpt-mobile2.md. Assume broken; find what tests missed. NOTE: other workers are writing in hardware/ and firmware/ dirs concurrently — IGNORE those paths entirely; your scope is the mobile diff of e231899 (apps/mobile/**).

HUNT:
1. GForceProvider: complementary-filter math correctness (alpha on gravity vs raw orientation); the g-units claim (expo-sensors already reports g — verify against the Expo v57 docs statement in the code comment and that no /9.81 is applied); lifecycle races start/stop (generation discipline like the OBD provider? does a pending lazy import leak a subscription after stop()?); samples' tMonoMs clock source = same injected clock; independence pinned both directions (OBD failed !-> G stops; G failure !-> OBD affected); does G data flow into the recorder cap correctly (counts toward 200k)?
2. Poll plan/customPids build: exact plan pinned with and without transOilPidHex; hex validation (odd-length strings? '015C' style mode-01 collisions? whitespace? lowercase?); is the custom request passed to core config verbatim?
3. Strip: third-slot fallback logic (transOil configured but no data yet -> shows what?); threshold constants match the addendum (120/130, 110/125, 98/105); RPM truly gone from strip but present on TelemetryScreen.
4. Recorder/charts: new channels stored without schema change (channel TEXT — confirm no enum constraint anywhere); LapDetail chart order matches addendum; latG/longG bucketing sane for signed values (bucketTelemetry min/max with negatives — sparkline bar height math with negative values! Does the View-based bar chart handle a negative bucket average or does it render zero/overflow?).
5. New defects in the composition diff (start/stop wiring of two providers into one allSettled — partial-failure paths).
RULES: read-only; cite file:line; severity HIGH/MEDIUM/LOW; concrete failure paths only.
OUTPUT: First line PASS / FAIL / PASS_WITH_NOTES (FAIL if any HIGH). Findings then checked-and-clean list.
