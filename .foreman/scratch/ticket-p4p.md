# Ticket P4p — field bugs from test 9: one active vehicle profile, self-sufficient engine detection, actionable steering discovery — Opus, tests first
Field facts (ledger 'field test 9'): bindings live per profileId; the user confirmed brakePressure=0x12/0x58B7 under toyota-supra-b58 but the monitor polls 0x12/0x4002 (raw 131/155) from the generic profile → 0/100 %. Telemetry and the finder are EXCLUSIVE (one reservation) — the engine warning waited for telemetry rpm that can never be fresh.
G1 Active vehicle profile: a persisted setting `activeVehicleProfileId` (default 'generic'); the Signal Finder's profile chip SETS it (and reads it as its initial selection); composition's binding cache and the telemetry provider read bindings for THIS profile only; the monitor header shows the profile label; confirms/replaces write to the active profile. Migration: on first run with the new setting, if the supra profile has field-confirmed bindings and generic also has same-channel bindings, prefer supra as the initial active profile (one-time heuristic, logged) — never delete anything. Tests: confirm under supra → monitor polls 0x58B7; generic's stale 0x4002 binding ignored while supra is active.
G2 Engine detection inside the finder: the probe reads rpm itself — one mode-01 0x0C request to 0x12 over the finder's own channel at probe time (reuse the existing OBD PID encode/decode helpers); rpm > 400 → engine running (fresh by construction); telemetry samples used only as an additional signal when fresh. The warning text unchanged; it now clears when the engine is actually running. Tests with a fake channel answering 0x0C.
G3 Steering discovery actionable: on a target whose plan is empty because its discoveryRanges are unswept, the row's disabled-reason becomes a BUTTON "Scan 0x29 58F3–6FFF (≈ N min, engine off)" that runs the existing DID sweep (reuse didSweepController/persistence with the range prefilled — via navigation to the DID sweep screen with params, not a reimplementation), and after COMPLETE the finder row shows hypotheses/cached counts again. Tests at the view-model/navigation-params level.
G4 The pressure display: brakePct row keeps 'raw N / M' but M = the binding's observedMax updated to the session max when live values exceed it (display only, binding unchanged) — so a weak stored max cannot pin the display at 100 %. Test.
Gates (real exit codes, repo root): `npm run typecheck`, `npm run test`, `npm run lint`, `npx expo export --platform ios` in apps/mobile — all 0. Report totals + red→green per item + changed strings (RO/EN).
WRITE SET: apps/mobile/src/session/{telemetryProvider,composition,signalFinderController}.ts, apps/mobile/src/persistence/{didSweepStore,sqlSettingsStore}.ts (additive), the settings store, apps/mobile/src/ui/screens/{SignalFinderScreen.tsx,signalFinderStrings.ts,TelemetryScreen.tsx,DidSweepScreen.tsx (params only)}, navigation types, tests. packages/core read-only. No commit, no agents, no Expo dev server.
