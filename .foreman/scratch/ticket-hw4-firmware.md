TASK: Build the ESP32-C3 firmware for the TRACE OBD dongle (PlatformIO project) so the shipped TRACE app connects to it UNMODIFIED. Binding sources: hardware/DESIGN.md (pins, architecture) and the app-side protocol the firmware must satisfy: packages/core/src/telemetry/elm327Session.ts + pidCodec.ts (read them — the firmware is the SERVER side of exactly that client).

EXPECTED OUTCOME (firmware/ at repo root):
1. platformio.ini — env:esp32c3 (espressif32 platform, board esp32-c3-devkitm-1, arduino OR espidf framework — pick ONE, justify in a sentence; prefer espidf if TWAI listen-only mode support is cleaner there) + env:native for host unit tests.
2. src/: (a) wifi_ap: SoftAP ssid "TRACE-OBD" + random-suffix, WPA2 pass "tracetrace" (documented as changeable), static IP 192.168.4.1, TCP server port 35000, single client; (b) elm_server: line-oriented ELM327-compatible subset — ATZ (reset banner "ELM327 v1.5 compatible TRACE"), ATE0/ATL0/ATS0/ATSP0 -> "OK", '>' prompt after every response, mode-01 requests 010C/010D/0111/0105/010F/0104/015C -> "41 <pid> <data>" hex responses, unknown AT -> "OK", unknown request -> "NO DATA", custom raw hex requests (e.g. 22xxxx) forwarded to the CAN layer and the raw response echoed (matching the app's custom-PID last-byte-minus-40 decode contract); (c) can_obd: TWAI driver on IO4/IO5 500kbps, standard OBD request via 0x7DF functional address, response match 0x7E8-0x7EF, 100ms timeout -> NO DATA; TJA1051 S pin on IO6 held LOW (normal mode) during OBD polling — expose a compile-time SNIFF_ONLY flag that holds S HIGH and never transmits (future passive mode); (d) READ-ONLY GUARD: a single chokepoint function is the only place a CAN frame can be transmitted; it rejects (and logs) anything that is not mode 01 or a whitelisted custom read service (21/22) — no mode 04/08/2F/3E etc., unit-tested; (e) status LED on IO8 (slow blink = AP up no client, solid = client, fast = CAN error).
3. Host tests (env:native): ELM line parser (framing, echo settings, prompt emission), PID response formatting vectors matching pidCodec.ts's decode expectations byte-for-byte (rpm 3815 -> "41 0C 3B 9C" etc. — derive vectors FROM the app's own test files), read-only guard whitelist/blacklist table.
4. Build gates (REAL exit codes): pip/pipx install platformio into the scratchpad venv (or `uv tool install platformio`), then `pio run -e esp32c3` (full cross-compile; first run downloads the toolchain — allow 10+ min) and `pio test -e native`. Both must exit 0. If toolchain download fails on this network, report the exact error as a CONCERN — do not fake the gate.
5. firmware/README.md: flash instructions (pio run -t upload via the J2 UART header + BOOT button per DESIGN.md), AP credentials, app connection steps (Settings -> adapter host 192.168.4.1 port 35000).

CONTEXT: Repo D:\CODE\APLICTIE_Circuit. The app's transport sends one command line, expects '>'-terminated responses, tolerates echo — read tcpObdTransport.ts + elm327Session.ts semantics section in docs/architecture/contracts.md (Telemetry addendum). Scratchpad: C:\Users\dobri\AppData\Local\Temp\claude\D--CODE-APLICTIE-Circuit\99e8bb0d-e01a-46e7-8e09-704662c78ba7\scratchpad.

CONSTRAINTS: Keep the codebase small and readable (this is rev-A prototype firmware, not a product); no OTA, no BLE, no web UI. C/C++ per chosen framework idioms. All protocol logic in framework-free modules so env:native tests compile them directly.

MUST DO: real exit codes for both pio commands; the byte-for-byte vector provenance (which app test file each vector came from); read-only guard test table in the report.
MUST NOT: No subagents. Nothing outside firmware/ and the scratchpad. Do not touch apps/, packages/, hardware/.
WRITE SET: firmware/**, scratchpad venv/toolchain caches.
OUTPUT FORMAT: First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED. Then: framework choice + 1-sentence why; file tree; both gate exit codes verbatim; vector provenance; guard table; concerns.
