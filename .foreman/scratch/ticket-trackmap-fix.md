TASK: Fix the track map so the circuit is actually recognizable (user field feedback: "doesn't look at all like the real circuit") and verify/fix live marker updates. LEAD diagnosis: TMR's centerline spans 704m E-W x 1538m N-S (portrait 2.2:1) — the landscape map container letterboxes it into a ~100px-wide band, drawn as 150 sparse 2px dots -> unrecognizable blob. Markers additionally appeared static during a live replay run (unconfirmed — instrumentation was ambiguous; you must verify). Designs BINDING.

F1 Auto-rotate to fit: in trackMapModel.ts, when the track bounds are portrait (spanN > spanE), rotate all local points 90 degrees ((e,n) -> (n, -e)) BEFORE fitting, so the long axis lies along the container's long axis. Expose the rotation flag in the fit result. Deterministic, pure, tested (portrait input -> rotated fit fills >= 80% of container width; landscape input -> no rotation).
F2 Connected outline: replace the dot-cloud outline with CONNECTED SEGMENTS — consecutive decimated points joined by thin View lines (position absolute, width = segment px length, height 2px, transform: rotate(angleDeg), transformOrigin left-center). Segment count ~= decimation count (cap 250). Pure helper buildOutlineSegments(fittedPoints, containerW, containerH) -> [{x, y, lengthPx, angleDeg}] in trackMapModel.ts, fully unit-tested on a known square (4 segments, right angles, correct lengths); TrackMapView renders them memoized (outline layer must NOT re-render on marker updates — verify via React.memo + stable props).
F3 Densify: before decimation, linearly interpolate the centerline to ~2x point count if the source has < 250 points, so segments are short and curves look smooth (pure, tested: interpolated length preserved within 0.1%).
F4 Live markers, verified: give the raw and matched marker Views testID="trackmap-raw-dot" / "trackmap-matched-dot" AND accessibilityLabel mirrors. Trace the data path: ActiveCalibrationScreen reads state.calibration.{rawLocalX,...} from the facade on every state update — confirm the screen actually re-renders markers per update (find the bug if they are static: e.g., memoized parent swallowing updates, or the screen reading the fields once). Add a viewmodel-level test: two successive calibration states with different rawLocalX -> different marker container fractions from the pure mapping fn.
F5 S/F marker + direction chevron: keep the S/F tick; add a tiny arrow/chevron at 10% lap distance indicating travel direction (pure math for its angle from neighboring points; muted color).

CONTEXT: Repo D:\CODE\APLICTIE_Circuit, commit b0549de. Files: apps/mobile/src/session/trackMapModel.ts, apps/mobile/src/ui/components/TrackMapView.tsx, apps/mobile/src/ui/screens/ActiveCalibrationScreen.tsx, apps/mobile/test/session/trackMapModel.test.ts. Read them all first. TMR ground truth: packages/core/assets/circuits/transilvania-motor-ring.v2.json (150 centerline points, portrait aspect).
CONSTRAINTS: No new deps. Pure math in the model, thin .tsx. Do not touch packages/core.
MUST DO: Gates from repo root, REAL exit codes: npm run typecheck, npm test, npm run lint, cd apps/mobile && npx expo export --platform ios. Report all four + counts.
MUST NOT: No subagents. Nothing outside the four files above.
WRITE SET: the four files above.
OUTPUT FORMAT: First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED. Then: per-design confirmation + pinning tests; the F4 verdict (were markers actually static? root cause if yes); gate exit codes + counts; concerns.
