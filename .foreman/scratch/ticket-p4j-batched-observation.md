# Ticket P4j — batched guided observation, focused shortlist, mid-size blocks (mobile + core), tests first
Field evidence (binding context): data/field/sweeps/2026-08-29-supra-dme12-0x4000-0x4FFF.json — 533 responders, 128 small candidates; the guided phases ran 17 s each but the real rate was ~8–11 req/s, so each DID was read only 1–2× per phase → "brakeCandidate" ranking was noise (e.g. 0x4522 297→305→295). Real throttle signals exist (0x4659 0→4095, 0x4600, 0x4601, 0x4807, 0x472E, 0x4206). Contracts: DID-sweep addenda in docs/architecture/contracts.md (hard read-only whitelist, reservation, keep-alive rules unchanged).
Deliver:
1. CORE (`didObservationPhases.ts` / new `didObservationBatches.ts`): `planObservationBatches(candidates, {batchSize=16, minSamplesPerPhase=5, measuredReqPerSec})` → batches; phase duration per batch from the MEASURED rate of the sweep run (not the assumed 15), so every DID gets ≥ 5 samples per phase; ranking per DID uses ≥ 5 samples (a change counts only if the value range in an active phase exceeds the baseline range by a margin, e.g. > 2× baseline spread or > 3 raw units). Tests with the field file shapes (128 candidates at 9 req/s → 8 batches, ~11 s phases).
2. MOBILE guided flow: runs batch after batch (progress "Batch 3/8"), repeating the on-screen prompts per batch; pause/stop-safe; export includes `batchIndex` and ≥ 5 samples per DID per phase; keep-alive continuous across batches.
3. FOCUSED observation: the user can tick candidates (or type DIDs) → one long guided cycle on the shortlist only (≥ 10 samples per phase).
4. Mid-size blocks (9–32 bytes) join the candidate pool with per-byte-offset diffing: a block is "changed in phase" if any byte offset changes beyond baseline; the export lists changed offsets; UI shows "0x40B5 · bytes 4–5 changed (brake)". ASCII-like blocks still excluded.
5. Target address: Settings already has enetTargetAddress — make sure the sweep screen shows the active target (e.g. "DME 0x12" / "0x29") and the export's `run.targetAddress` is a hex string too.
Gates (real exit codes): typecheck, test, lint, expo export iOS all 0.
WRITE SET: packages/core/src/telemetry/enet/{didObservationPhases.ts,didObservationBatches.ts (new),didCandidates.ts} + tests; apps/mobile/src/session/{didSweepController.ts,didSweepExport.ts}, apps/mobile/src/ui/screens/DidSweepScreen.tsx, apps/mobile/test/**. No commit, no agents, leave the Expo server alone. Report DONE/DONE_WITH_CONCERNS/BLOCKED with evidence + totals.
