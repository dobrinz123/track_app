You are a read-only reviewer. Repo: D:\CODE\APLICTIE_Circuit (npm workspaces). If shell exec is blocked by the Windows sandbox, read files directly. Bounded re-verification of the latest commit on main ("P4m-FIX1: Signal Finder — probe-measured rate…") against these earlier findings (say FIXED / PARTIAL / NOT FIXED with file:line evidence): (1) the request rate was assumed 15/s and exported as measured; (2) serial polling let a silent ECU (0x29) starve the live ECU (0x12) inside a fixed budget; (3) planned-but-unattempted DIDs were counted as read/noResponse after a stop; (4) the same DID number on two ECUs was split across rounds; (5) cached/changed pools took DIDs from every ECU, not the target's; (7) a single-bit difference made any two-level series a flag, and combined with the sparse rule LSB noise could be `found`; (8) `never-moved → unrelated` on thin coverage; (9) Romanian mode still showed English evidence/banners/a11y/export markers; (10) a find with zero eligible DIDs ran no script. Files: packages/core/src/telemetry/signalFinder/{runner,plan,scoring,targets,index}.ts + tests (incl. fieldFixtures.test.ts, runner.test.ts), apps/mobile/src/session/{signalFinderController,signalFinderExport}.ts, apps/mobile/src/ui/screens/{SignalFinderScreen.tsx,signalFinderStrings.ts} + tests. Then hunt NEW defects in the probe/runner: probe duration vs. budget maths, an ECU that answers the probe but goes silent mid-run, back-off interplay with the 3 s windows (can back-off empty a window and turn a real signal `insufficient`?), attempted-key bookkeeping on stop/error, transport close on every path, and the RO/EN table completeness (every EN key has an RO value; no English literal in the screen). Output: numbered findings (HIGH/MEDIUM/LOW, file:line, one-line fix), then "NEW: n HIGH, m MEDIUM". No edits.
