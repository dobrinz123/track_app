TASK: Coaching fix wave — ALL findings from the Codex cross-review (.foreman/scratch/coach-codex-verify-output.log — read in full first; file:line evidence there). Foreman's binding designs below. Every fix testable gets a revert-detecting test.

EXPECTED OUTCOME: gates green from root (typecheck, test core+mobile, lint, export:ios). Paste decisive output.

BINDING DESIGNS:
F1+F2 (HIGH, cue timing/copy + frozen distance): CoachCue becomes a LIVE value — CoachEngine.onMatch re-emits the active cue EVERY match with updated distanceToTargetM while approaching; cue ends (null) immediately when target passed, match rejected/'unreliable', or corner exited. Voice fires ONCE per cue activation (dedupe unchanged) but the phrase INCLUDES distance + units: "Brake in 120 meters. Corner 5, hard." / "Corner 9 ahead, right, 70 miles per hour." Strip copy: kicker 'BRAKE IN' + live countdown ('84 m'), plus a small muted 'ADVISORY' tag on the strip; accessibility label mirrors ("Advisory: brake in 84 meters..."). Controller: currentCue replaced per match, cleared immediately on null (drop the 5s stale hold — keep a 2s hold ONLY for identical-corner flicker), cleared on pit entry and lap rollover.
F4 (HIGH, pit): controller skips CoachEngine feed and clears currentCue when sessionState==='inPit' OR match.onPitLane. Test: pit-transit replay segment → zero cues while onPitLane, cue cleared at pit entry.
F3 (HIGH, stale toggle): composition subscribes to settings; when coachingEnabled changes while controller is NOT in an active session (idle/sessionComplete/error), rebuild the production controller with fresh coachingConfig (reuse the existing preflight-gate rebuild machinery — extract a shared rebuild fn; keep the gate's serialization). Mid-active-session changes take effect at next session — SettingsScreen gains a one-line muted note saying so. Test: toggle off while idle → next session emits no cues (facade-level).
F5 (HIGH, units): shared formatter util (apps/mobile/src/ui/format.ts or session-level) reading settings units — used by CoachStrip, CornersList, and voiceCoach (voice says 'kilometers per hour'/'miles per hour'; strip 'km/h'/'mph' with converted values). Tests: mph setting → converted values in phrase util; km/h default unchanged.
M-voice-serialization: voiceCoach maintains ONE promise chain: each utterance = chain.then(stop).then(speak), stop-failure skips to speak only if stop rejected AND engine not speaking (just catch and proceed is fine — but never start speak before prior stop settles). Rapid C8→C9 test with delayed stop resolution → order preserved, no overlap.
M-voice-lifecycle: stop speech on app background (lifecycle listener) and when voiceCoachEnabled/coachingEnabled turn off (settings subscription in voiceCoach) — Speech.stop() guarded.
M-PB-refresh: on zone refresh mid-lap, preserve per-lap emitted-corner state (configure() gains preserveEmitted option used by the controller's refresh path); currently-displayed cue kept consistent. Test: PB replace mid-lap → C1 does NOT re-fire same lap.
M-speed-clamp: clamp speed used in lead-distance to config maxLeadSpeedMps default 90 (≈324 km/h); glitch test: 150 m/s sample doesn't fire far cues.
M-observed-version: observed-speeds asset schema gains required `analysisVersion` (set 2 in the TMR file); loader: mismatch with CORNER_ANALYSIS_VERSION → return empty observations + console.warn (never throw at module init); missing-ID → skip that observation with warn (never crash). Tests both.
M-direction-split: analyzeCorners splits a run when signed curvature crosses zero with sustained opposite sign (> gapToleranceM of opposite sign = split; brief wiggles tolerated). Existing TMR result MUST stay identical (9 corners — regression-pin) — add a synthetic chicane test profile asserting the split. CORNER_ANALYSIS_VERSION stays 2 IF TMR output unchanged (assert byte-stable corner set), else bump to 3 + update observed asset version.
M-contrast: severity chip palette reworked for >=4.5:1 text contrast on both idle and BRAKE-amber backgrounds (dark text on amber fills; verify ratios in a comment with computed values).

MUST NOT: change delta/timing semantics; no new deps; no modals; no subagents; no git commit. Do not weaken existing tests (TMR 9-corner pin included).

OUTPUT FORMAT: status first line; per-finding fix + test + file:line; commands + results.

WRITE SET: packages/core/src/{coach,corners,controller}/**, packages/core/src/contracts.ts (additive), packages/core/assets/circuits/transilvania-motor-ring.observed-speeds.v1.json, packages/core/test/**, apps/mobile/src/session/**, apps/mobile/src/ui/components/{CoachStrip,CornersList}.tsx, apps/mobile/src/ui/screens/SettingsScreen.tsx, apps/mobile/src/ui/format.ts, apps/mobile/test/**.
