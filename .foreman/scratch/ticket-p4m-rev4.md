You are a read-only reviewer. Repo: D:\CODE\APLICTIE_Circuit (npm workspaces). If shell exec is blocked by the Windows sandbox, read files directly. Bounded re-verification of the latest commit on main ("P4m-FIX3: Signal Finder — retained-entry rate…") against these findings (say FIXED / PARTIAL / NOT FIXED with file:line evidence): (A) budget/estimate rate was dominated by dropped silent entries; (B) cooldown reset misses to threshold−1 so the first miss per window re-cooled the DID; (C) per-entry pacing drifted after slow exchanges and could starve the ≥3 samples/window guarantee; (D) probe-silent hypotheses were retained and retried every window; (E) the complete-pass probe stalled up to 3.6 s with no UI; (F) the reservation could be released while close() was still pending; (G) RO banners interpolated raw English errors. Files: packages/core/src/telemetry/signalFinder/{runner,plan}.ts + tests (runner.test.ts, plan.test.ts, fieldFixtures.test.ts), apps/mobile/src/session/{signalFinderController,signalFinderExport}.ts + tests, apps/mobile/src/ui/screens/{SignalFinderScreen.tsx,signalFinderStrings.ts} + tests. Then hunt NEW defects in exactly this commit's mechanisms: the retained-rate formula (answeredCount×1000/max(answeredElapsedMs, answeredCount)) when zero entries answered; per-window state maps growing/leaking; pass-level pacing when a pass is longer than the interval; the explicit retry round's interaction with stop() and with the transport; the probing→running phase transition and onProgress; teardown 5 s hard release + pendingClose subscription (double release? Find refused forever if the promise never settles — is that acceptable and visible?); export schema 4 diagnostics leaking user data. Output: numbered findings (HIGH/MEDIUM/LOW, file:line, one-line fix), then "NEW: n HIGH, m MEDIUM". No edits.
