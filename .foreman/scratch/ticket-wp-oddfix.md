TASK: Fix wave for five integration findings surfaced by the documentation review (independent code-reading pass). Each finding below is verified-real; fix all five with tests where testable.

EXPECTED OUTCOME: `npm run typecheck`, `npm test`, `npm run lint` pass from repo root; each fix has a test or (UI-only) a clear manual-verification note. Existing tests stay green except where an assertion legitimately changes (justify each). Paste decisive output.

CONTEXT: Read the cited files first. HEAD includes recent security fixes (controller caps, Settings delete UI) — read packages/core/src/controller/ and apps/mobile/src/ui/screens/SettingsScreen.tsx as they are NOW, not as older docs describe.

MUST DO:
1. `CircuitProfile.corridorWidthM` is never wired into the live TrackMatcher/CalibrationEngine (both default to 20 m independently; profile says 15 m). Fix: SessionController (and runSessionPipeline/pipeline composition) must pass profile.corridorWidthM into matcher + calibration configs (as corridor base; keep existing config override ability). Test: construct pipeline with a profile whose corridorWidthM differs from defaults; assert the matcher/calibration actually use it (expose via config echo or behavioral test at the corridor boundary).
2. CalibrationResultScreen REASON_COPY keys don't match real engine codes: bespoke copy exists for ACCURACY_ABOVE_20M / DIRECTION_UNCERTAIN / LOW_SAMPLE_RATE (never emitted); real codes POOR_GNSS / WRONG_DIRECTION / RATE_TOO_LOW / COVERAGE_GAP (and now CALIBRATION_OVERRUN, if present in calibration-engine.ts) fall through to a generic humanizer. Fix the map to cover every code the engine can emit (grep calibration-engine.ts for failure-reason string literals; cover ALL), plain-language driver-appropriate copy; remove dead keys.
3. GnssDiagnostics has zero UI call sites. Fix: surface diagnostics in DevReplayScreen (dev builds) AND a read-only "Diagnostics" section in SettingsScreen (observed rate, accuracy p50/p95, rejected count, watchRestarts, reducedAccuracy) fed via the facade or a new diagnostics accessor on composition; refresh on screen focus or a manual refresh button; no polling timers while a session is timing.
4. `startLifecycleListener.onBackground` is never wired. Fix: in composition bootstrap, register the lifecycle listener; on background during an active session: trigger an immediate checkpoint via SessionController (add a public checkpointNow() if none exists) — do NOT auto-pause (keep-awake means backgrounding is deliberate user action; timing continues; watchdog handles GNSS gaps). On foreground return: no special action (recovery flow covers process death). Core test for checkpointNow(); wiring is composition-level (document manual verification path).
5. Lap numbering resets to 1 after recovery resume (restoreFromCheckpoint builds a fresh LapTimingEngine) — real lapNumber collision with restored historical laps. Fix: LapTimingEngine gains an initialLapNumber (constructor/config or a resume(nextLapNumber) API — your choice, additive, no behavior change for fresh sessions); restoreFromCheckpoint seeds it from max restored lapNumber + 1. Tests: fresh session still starts at lap 1 (existing tests unchanged); post-recovery next lap number = maxRestored+1; state machine lapNumber consistency (check how the reducer's lapNumber interacts — keep them in sync; document the sync mechanism).

MUST NOT: touch docs/** (stale references there get fixed in the final docs sweep, not by you); touch track-matcher.ts internals beyond config plumbing if the perf-modified version is present — preserve its hint/audit logic exactly; weaken tests; spawn subagents; git commit.

OUTPUT FORMAT: First line exactly one of DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED. Then per-finding fix summary (file:line + test), commands + pasted results, justified assertion changes if any.

WRITE SET: packages/core/src/controller/**, packages/core/src/timing/lap-timing-engine.ts, packages/core/src/replay/** (composition plumbing only), packages/core/test/{controller,timing,replay}/**, apps/mobile/src/session/**, apps/mobile/src/ui/screens/{CalibrationResultScreen,DevReplayScreen,SettingsScreen}.tsx, apps/mobile/App.tsx (lifecycle wiring only).
