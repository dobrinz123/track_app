TASK: Pre-install fix wave — the COMPLETE combined findings list from two independent reviews (Codex lifecycle cross-review + blind verifier pinning audit) of HEAD 7f892a4. The user has scarce Apple install slots; this wave must make the build genuinely phone-ready. Every fix gets a revert-detecting test where testable.

EXPECTED OUTCOME: `npm run typecheck`, `npm test` (core+mobile), `npm run lint`, `npm run export:ios` all green; every finding below addressed with the specified design (deviations require justification in your report); new tests fail if their fix is reverted. Paste decisive output.

CONTEXT: Read the two source reports first: .foreman/scratch/preinstall-codex-output.log (tail ~60 lines — findings with file:line) and the blind-verifier findings summarized below. Then the cited files. The foreman has PRE-DECIDED the designs below — implement them, don't redesign.

FINDINGS + BINDING DESIGNS:

C1 (CRITICAL, one-shot controller): Extract production-controller construction in apps/mobile/src/session/composition.ts into a `createProductionController()` factory (same deps as bootstrap). RealSessionFacade/facade wrapper path: when `startPreflight()` is invoked and the current controller's state is 'sessionComplete' (or 'error'), composition disposes the old controller and swaps in a fresh one (new facade inner) BEFORE forwarding the command. Add `SessionController.dispose(): Promise<void>` in core (stops provider subscription, stops watchdog scheduler, clears listeners; idempotent). TESTS: core — dispose idempotence + no emissions after dispose; mobile — full back-to-back double-session through the facade path (session 1 calibrate→lap→end, then session 2 calibrate→lap→end on the SAME app composition, assert session 2 records laps and history has 2 sessions). NOTE: soak scenario 2 constructed new controllers manually and masked this bug — leave it, the new mobile test covers the real path.

C2 (HIGH, no bootstrap gate): Replace the initial MockSessionFacade inner with a new inert `PendingFacade` (state 'idle', all commands no-op; NOT the scripted mock — mock stays only behind the DevReplay toggle). Export `bootstrapState: 'pending'|'ready'|'failed'` + subscribe function from composition; catch bootstrap rejection → 'failed'. UI: CircuitDetailScreen disables Start Session (and shows a small inline note) until 'ready'; if 'failed', inline error banner (non-modal). TEST: mobile — commands invoked while pending are no-ops (no throw, no state corruption); after bootstrap resolves, commands work.

C3 (HIGH, GNSS start/stop races): apps/mobile/src/platform/gnssLocationProvider.ts — serialize lifecycle: keep a private `op: Promise<void>` chain; start() and stop() both append to it; start while active → no-op; stop during pending start awaits the start then removes the subscription; never two live watchers. TEST: mobile with mocked expo-location — interleaved start/start/stop/start storm → exactly one active watcher at each settle point; stop-during-start leaves zero.

C4 (HIGH, endSession doesn't flush): In SessionController.endSession path: stop ingestion, `await this.flush()` BEFORE repository.saveSession + final emit (order: provider stop → flush → saveSession → emit sessionComplete). Failures propagate to the facade error path (C7). TEST (also covers blind-verifier B2): core — repo with deferred/failing saveTelemetry: (a) slow-resolve repo → endSession resolves only after telemetry+checkpoint+PB writes committed (assert repo contents complete at resolution); (b) rejecting repo → endSession/flush rejects (pins Promise.all at flush — reverting to allSettled MUST fail this).

C5 (HIGH, recovery pointer cleared too early): composition.resumeRecovery — do NOT clear activeSessionId; re-point it to the recovered sessionId (it is the active session again); it is cleared only by the existing onSessionEnded. TEST: mobile — resume recovery, simulate second death (new bootstrap on same repo/db state), assert recovery is offered again.

C6 (HIGH, DevReplay swap leaks + no way back): composition — keep a handle to the production facade/controller; `startDevReplaySession` disposes any previous replay controller before creating a new one; add `restoreProductionFacade(): Promise<void>` (disposes replay controller, swaps production facade back) and call it from DevReplayScreen on unmount (useEffect cleanup) and before starting a new replay. RealSessionFacade: retain the controller-subscription unsubscribe and expose `dispose()` that calls it. TEST: mobile — start replay, restore, assert commands reach the production controller and the replay controller emits nothing after disposal.

C7 (MEDIUM, silent async command failures): FacadeState gains `lastError: string | null`. RealSessionFacade wraps async command bodies with catch → sets lastError (cleared on next successful command/state change). UI: ActiveCalibrationScreen + ActiveDashboardScreen render existing StatusBanner inline when lastError non-null (never a modal). Mock/Pending facades: lastError always null. TEST: mobile — failing provider start → beginCalibration produces lastError, screen-level state observable via facade.

C8 (MEDIUM, background checkpoint unawaited): composition lifecycle onBackground handler: `controller.checkpointNow().catch(...)` with a console.warn — no unhandled rejection. Core checkpointNow already public; ensure it never throws synchronously. TEST: core — checkpointNow with rejecting repo → resolves/rejects handled, no unhandled rejection (vitest unhandled-rejection guard).

C9 (MEDIUM, screen can lock during Learn lap): ActiveCalibrationScreen gets the same keep-awake hook as ActiveDashboardScreen (expo-keep-awake useKeepAwake). Manual-verify note sufficient; no automated test required.

C10 (MEDIUM, recovery unlocked + wrong copy): composition — a single in-flight lock around resumeRecovery/discardRecovery (second call while first pending = no-op returning the same promise); CircuitDetailScreen recovery banner copy corrected: resume does NOT require a fresh calibration (it arms directly using the stored reference) — say "Resume continues the interrupted session; lap N was invalidated." TEST: mobile — double-invoke resumeRecovery concurrently → single execution.

C11 (LOW, preflight collectors linger): preflight collectors accept a cancel mechanism (return {promise, cancel} or AbortSignal); PreflightScreen unmount cancels — the GNSS fix collector must remove its location subscription immediately on cancel. TEST: mobile with mocked expo-location — unmount-cancel removes subscription before timeout.

B1 (HIGH, PB-capture-at-crossing unpinned): core test — session where lap 1 is the FASTEST, followed by enough samples (> the matches rolling-buffer cap, use a reduced cap via config if configurable, else >2000 samples) of slower laps; assert final stored PB equals lap 1's duration. Under the reverted (deferred-build) behavior this MUST fail because lap 1's matches are evicted before the queued build runs. Verify the test actually fails against a simulated revert (describe how you validated its sensitivity — e.g., temporarily deferring in a scratch copy is NOT allowed on src; instead reduce the buffer cap via existing config so eviction is provable, and assert the fix path builds from crossing-time matches).

B4 (LOW, DevReplayScreen ships in release): RootNavigator registers the DevReplay screen only when `__DEV__` (conditional Stack.Screen); Settings entry already gated. Typecheck-safe.

MUST NOT: change bundle id, app.json permissions, timing/geometry algorithms, or the delta engine; no new deps except none expected; no modals; no subagents; no git commit. Do not weaken any existing test.

OUTPUT FORMAT: First line DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED. Then per-finding: fix summary (file:line) + pinning test name + how you proved the test detects reversion (B1/C4 mandatory). Then commands + pasted results.

WRITE SET: packages/core/src/controller/**, packages/core/test/controller/**, packages/core/test/soak/** (additions only), apps/mobile/src/session/**, apps/mobile/src/platform/{gnssLocationProvider.ts,preflight.ts}, apps/mobile/src/ui/screens/{ActiveCalibrationScreen,ActiveDashboardScreen,CircuitDetailScreen,DevReplayScreen,PreflightScreen}.tsx, apps/mobile/src/ui/navigation/RootNavigator.tsx, apps/mobile/src/ui/components/StatusBanner.tsx (only if props need extending), apps/mobile/test/**.
